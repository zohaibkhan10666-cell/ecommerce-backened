import { createRequire as __createRequire } from 'node:module';
import { fileURLToPath as __fileURLToPath } from 'node:url';
import { dirname as __dirname_ } from 'node:path';
const require = __createRequire(import.meta.url);
const __filename = __fileURLToPath(import.meta.url);
const __dirname = __dirname_(__filename);
import {
  indent_default
} from "./chunks/chunk-A3NYPUKZ.js";
import {
  RoutesAddTelemetryClient,
  RoutesTelemetryClient
} from "./chunks/chunk-PV533MBH.js";
import {
  ALL_ACTION_CHOICES,
  MAX_CONDITIONS,
  MAX_DESCRIPTION_LENGTH,
  MAX_NAME_LENGTH,
  REDIRECT_STATUS_CODES,
  VALID_SYNTAXES,
  collectActionDetails,
  collectHeadersAndTransforms,
  collectInteractiveConditions,
  convertRouteToCurrentRoute,
  extractTransformFlags,
  generateRoute,
  generatedRouteToAddInput,
  parseConditions,
  populateRouteEnv,
  printGeneratedRoutePreview,
  runInteractiveEditLoop,
  stripQuotes,
  validateActionFlags
} from "./chunks/chunk-RIDTMO6P.js";
import {
  getRouteVersions
} from "./chunks/chunk-AHU7WNL2.js";
import {
  TRANSFORM_TYPE_LABELS,
  ensureProjectLink,
  findVersionById,
  formatCondition,
  formatTransform,
  getRouteTypeLabel,
  getRoutes,
  getSrcSyntaxLabel,
  offerAutoPromote,
  parsePosition,
  parseSubcommandArgs,
  withGlobalFlags
} from "./chunks/chunk-OPAWD6UK.js";
import {
  normalizeRepeatableStringFilters
} from "./chunks/chunk-5SYDEK2N.js";
import {
  OpenApiCache,
  api,
  box,
  did_you_mean_default,
  executeUpgrade,
  isAutoUpdateEnabled,
  login,
  resolveOpenApiTagForProjectsCli,
  resolveOpenApiTagForTeamsCli,
  setAutoUpdate,
  tryOpenApiFallback
} from "./chunks/chunk-4J5CBI5E.js";
import {
  getUpdateCommand,
  isGlobal
} from "./chunks/chunk-KCQWKLFD.js";
import {
  Now,
  collectContactInformation,
  createCertForCns,
  displayBuildLogs,
  displayRuntimeLogs,
  extractDomain,
  getDomain,
  getDomainByName,
  getDomainPrice,
  getDomainStatus,
  isDeploying,
  isWildcardAlias,
  pollForOrder,
  printDeploymentStatus,
  purchaseDomain,
  require_cjs,
  require_format,
  require_jsonlines,
  setupDomain
} from "./chunks/chunk-6YOW32LL.js";
import {
  getGlobalPathConfig,
  persistAuthConfig,
  readLocalConfig,
  sleep,
  writeToConfigFile
} from "./chunks/chunk-5LI3PLS3.js";
import "./chunks/chunk-V5P25P7F.js";
import {
  ellipsis,
  getCustomEnvironments,
  getInvalidSubcommand,
  pickCustomEnvironment,
  readStandardInput,
  require_dist as require_dist4
} from "./chunks/chunk-WEVQWSBT.js";
import {
  formatTable
} from "./chunks/chunk-RM4XGGZO.js";
import "./chunks/chunk-LKBI43XK.js";
import {
  isValidName
} from "./chunks/chunk-Y4JJYHUG.js";
import {
  getCommandFlags
} from "./chunks/chunk-EOZFDJSY.js";
import {
  getDeployment,
  mapCertError,
  toHost
} from "./chunks/chunk-TCF6MGBY.js";
import {
  formatEnvironment,
  validateLsArgs
} from "./chunks/chunk-6UAZGWVF.js";
import {
  validateJsonOutput
} from "./chunks/chunk-XPKWKPWA.js";
import {
  getSubcommand as getSubcommand5
} from "./chunks/chunk-YPQSDAEW.js";
import {
  ADDON_LABELS,
  CREDIT_TYPE_LABELS,
  SUPPORTED_ADDON_ALIASES,
  SUPPORTED_CREDIT_TYPES,
  abortSubcommand,
  acceptTermsSubcommand,
  accessGroupsSubcommand,
  accessSummarySubcommand,
  addSubcommand,
  addSubcommand10,
  addSubcommand11,
  addSubcommand2,
  addSubcommand3,
  addSubcommand4,
  addSubcommand5,
  addSubcommand6,
  addSubcommand8 as addSubcommand7,
  addSubcommand9 as addSubcommand8,
  addToGroupSubcommand,
  addonSubcommand,
  agentCommand,
  aiGatewayCommand,
  aliasCommand,
  apiKeysSubcommand,
  approveSubcommand,
  archiveSubcommand,
  attachSubcommand,
  attackModeDisableSubcommand,
  attackModeEnableSubcommand,
  attackModeSubcommand,
  balanceSubcommand,
  bisectCommand,
  blobCommand,
  buyCommand,
  buySubcommand,
  cacheCommand,
  certsCommand,
  checkSubcommand,
  checksAddFlags,
  checksRemoveFlags,
  checksSubcommand,
  claimSubcommand,
  completeSubcommand,
  configureSubcommand,
  connectSubcommand,
  connectSubcommand2,
  connexCommand,
  contractCommand,
  copySubcommand,
  createGroupSubcommand,
  createStoreSubcommand,
  createSubcommand,
  createSubcommand2,
  createSubcommand3,
  createSubcommand4,
  createSubcommand5,
  createThresholdSubcommand,
  creditsSubcommand,
  cronsCommand,
  curlCommand,
  dangerouslyDeleteSubcommand,
  delSubcommand,
  deleteGroupSubcommand,
  deleteStoreSubcommand,
  deployHooksCommand,
  detachSubcommand,
  diffSubcommand,
  disableSubcommand,
  disableSubcommand2,
  disableSubcommand3 as disableSubcommand4,
  discardSubcommand,
  disconnectSubcommand,
  disconnectSubcommand2,
  discoverSubcommand,
  dismissSubcommand,
  dnsCommand,
  domainSubcommand,
  domainsCommand,
  edgeConfigCommand,
  emptyStoreSubcommand,
  enableSubcommand,
  enableSubcommand2,
  enableSubcommand3 as enableSubcommand4,
  fetchSubcommand,
  firewallCommand,
  flagsCommand,
  flushSubcommand,
  getCommandAliases,
  getStoreInfoSubcommand,
  getSubcommand,
  getSubcommand2,
  getSubcommand3,
  getSubcommand4,
  gitCommand,
  guidanceCommand,
  guideSubcommand,
  httpstatCommand,
  importSubcommand,
  initCommand,
  inspectCommand,
  inspectGroupSubcommand,
  inspectSubcommand as inspectSubcommand2,
  inspectSubcommand2 as inspectSubcommand3,
  inspectSubcommand3 as inspectSubcommand4,
  installCommand,
  installSubcommand,
  installationsSubcommand,
  integrationCommand,
  integrationResourceCommand,
  invalidateSubcommand,
  inviteSubcommand,
  ipBlocksBlockSubcommand,
  ipBlocksListSubcommand,
  ipBlocksSubcommand,
  ipBlocksUnblockSubcommand,
  issueSubcommand,
  itemsSubcommand,
  listRequestsSubcommand,
  listStoresSubcommand,
  listSubcommand as listSubcommand2,
  listSubcommand10,
  listSubcommand11,
  listSubcommand12,
  listSubcommand13,
  listSubcommand14 as listSubcommand15,
  listSubcommand15 as listSubcommand16,
  listSubcommand16 as listSubcommand17,
  listSubcommand17 as listSubcommand18,
  listSubcommand18 as listSubcommand19,
  listSubcommand3,
  listSubcommand4,
  listSubcommand5,
  listSubcommand6,
  listSubcommand7,
  listSubcommand8,
  listSubcommand9,
  listVersionsSubcommand,
  logoutCommand,
  logsCommand,
  mcpCommand,
  membersSubcommand,
  membersSubcommand2,
  microfrontendsCommand,
  moveSubcommand,
  oauthAppsCommand,
  openCommand,
  openSubcommand,
  openSubcommand2,
  openSubcommand3,
  overrideSubcommand,
  overviewSubcommand,
  prepareSubcommand,
  priceSubcommand,
  proSubcommand,
  projectCommand,
  promoteCommand,
  promoteSubcommand,
  protectionSubcommand,
  publishSubcommand,
  pullSubcommand,
  purgeSubcommand,
  putSubcommand,
  redeployCommand,
  redirectsCommand,
  registerSubcommand,
  removeCommand,
  removeFromGroupSubcommand,
  removeSubcommand,
  removeSubcommand10,
  removeSubcommand11,
  removeSubcommand12,
  removeSubcommand13,
  removeSubcommand14,
  removeSubcommand15,
  removeSubcommand2,
  removeSubcommand3,
  removeSubcommand4,
  removeSubcommand5,
  removeSubcommand6,
  removeSubcommand7,
  removeSubcommand8,
  removeSubcommand9,
  renameSubcommand,
  requestSubcommand,
  resourceSubcommand,
  restoreSubcommand,
  revokeTokensSubcommand,
  rollbackCommand,
  rollingReleaseCommand,
  rolloutSubcommand,
  rulesAddSubcommand as rulesAddSubcommand2,
  rulesDisableSubcommand,
  rulesEditSubcommand,
  rulesEnableSubcommand,
  rulesInspectSubcommand as rulesInspectSubcommand2,
  rulesListSubcommand,
  rulesRemoveSubcommand,
  rulesReorderSubcommand,
  rulesSubcommand,
  runSubcommand,
  sdkKeysAddSubcommand,
  sdkKeysListSubcommand,
  sdkKeysRemoveSubcommand,
  sdkKeysSubcommand,
  searchSubcommand,
  setSubcommand,
  setSubcommand2,
  skillsCommand,
  speedInsightsSubcommand,
  splitSubcommand,
  ssoSubcommand,
  startSubcommand,
  statusSubcommand,
  statusSubcommand2,
  statusSubcommand3,
  statusSubcommand4,
  switchSubcommand,
  systemBypassAddSubcommand,
  systemBypassListSubcommand,
  systemBypassRemoveSubcommand,
  systemBypassSubcommand,
  systemMitigationsPauseSubcommand,
  systemMitigationsResumeSubcommand,
  systemMitigationsSubcommand,
  targetCommand,
  teamsCommand,
  telemetryCommand,
  tokenSubcommand,
  tokenSubcommand2,
  tokensCommand,
  tokensSubcommand,
  tracesCommand,
  transferInSubcommand,
  updateSubcommand,
  updateSubcommand2,
  updateSubcommand3,
  updateSubcommand4,
  upgradeCommand,
  uploadSubcommand,
  usageCommand,
  v0Subcommand,
  webAnalyticsSubcommand,
  webhooksCommand,
  whoamiCommand
} from "./chunks/chunk-4WWFHUVW.js";
import {
  metricsCommand,
  schemaSubcommand
} from "./chunks/chunk-DAASB6YQ.js";
import {
  addSubcommand as addSubcommand9,
  deleteSubcommand,
  disableSubcommand as disableSubcommand3,
  discardSubcommand as discardSubcommand2,
  editSubcommand,
  enableSubcommand as enableSubcommand3,
  exportSubcommand,
  inspectSubcommand as inspectSubcommand5,
  listSubcommand as listSubcommand14,
  listVersionsSubcommand as listVersionsSubcommand2,
  publishSubcommand as publishSubcommand2,
  reorderSubcommand,
  restoreSubcommand as restoreSubcommand2,
  routesCommand
} from "./chunks/chunk-WFRHKZFI.js";
import {
  alertsCommand,
  inspectSubcommand,
  listSubcommand
} from "./chunks/chunk-IQQJHYW4.js";
import {
  rulesAddSubcommand,
  rulesAggregateCommand,
  rulesInspectSubcommand,
  rulesLsSubcommand,
  rulesRmSubcommand,
  rulesUpdateSubcommand
} from "./chunks/chunk-NGRSQRSN.js";
import "./chunks/chunk-O4C4A7HM.js";
import {
  activityCommand,
  typesSubcommand
} from "./chunks/chunk-FMN3NXRC.js";
import "./chunks/chunk-ZTPOJE63.js";
import {
  main
} from "./chunks/chunk-SASCGHJW.js";
import "./chunks/chunk-HD23APLQ.js";
import {
  require_execa
} from "./chunks/chunk-YI3JV6GM.js";
import {
  ensureLink
} from "./chunks/chunk-GVYAYUAT.js";
import "./chunks/chunk-QUU263YC.js";
import {
  AGENT_ACTION,
  AGENT_REASON,
  AGENT_STATUS
} from "./chunks/chunk-LJ5WXXG6.js";
import "./chunks/chunk-PB37FIFM.js";
import "./chunks/chunk-DMKETFQS.js";
import {
  detectExplicitScope,
  getScope
} from "./chunks/chunk-6ULI5CCZ.js";
import {
  help
} from "./chunks/chunk-VNUNCNPE.js";
import {
  STANDARD_ENVIRONMENTS,
  VERCEL_CONFIG_EXTENSIONS,
  autoInstallVercelPlugin,
  checkExistsAndConnect,
  createEnvObject,
  createGitMeta,
  createProject,
  disconnectGitProvider,
  envPullCommandLogic,
  envTargetChoices,
  formatProject,
  getLinkFromDir,
  getLinkedProject,
  getProjectByNameOrId,
  getProjectLink,
  getTeamById,
  getTeams,
  getUser,
  getVercelDirectory,
  humanizePath,
  isValidEnvTarget,
  isVercelTomlEnabled,
  list,
  param,
  parseGitConfig,
  parseRepoUrl,
  pluckRemoteUrls,
  printAlignedLabel,
  printRemoteUrls,
  processRevocationResponse,
  pull,
  readJSONFile,
  require_dist as require_dist2,
  require_dist3,
  require_fast_deep_equal,
  require_frameworks,
  require_lib,
  require_open,
  require_pluralize,
  require_tar_fs,
  resolveProjectCwd,
  revocationRequest,
  selectAndParseRemoteUrl,
  selectOrg,
  ua_default
} from "./chunks/chunk-T77OYIET.js";
import {
  TelemetryClient
} from "./chunks/chunk-J5273CSE.js";
import {
  argvHasNonInteractive,
  buildCommandWithGlobalFlags,
  buildCommandWithYes,
  buildIntegrationCommandTailFromArgv,
  exitWithNonInteractiveError,
  getGlobalFlagsFromArgv,
  openUrlInBrowserCommand,
  outputActionRequired,
  outputAgentError,
  shouldEmitNonInteractiveCommandError
} from "./chunks/chunk-NHGCQRK5.js";
import {
  elapsed,
  require_ms,
  stamp_default
} from "./chunks/chunk-CO5D46AG.js";
import {
  require_lib as require_lib3
} from "./chunks/chunk-N2T234LO.js";
import {
  table
} from "./chunks/chunk-4NDOMD3E.js";
import {
  GLOBAL_CLI_FLAG_NAMES,
  getCommandNameWithGlobalFlags,
  getFlagsSpecification,
  getGlobalFlagsOnlyFromArgs,
  getSameSubcommandSuggestionFlags,
  globalCliFlagTakesValue,
  parseArguments,
  printError,
  require_strip_ansi
} from "./chunks/chunk-6IQZVQV6.js";
import {
  APIError,
  AliasInUse,
  CertConfigurationError,
  CertError,
  CertMissing,
  CertNotFound,
  CertOrderNotFound,
  CertsPermissionDenied,
  DNSConflictingRecord,
  DNSInvalidPort,
  DNSInvalidType,
  DNSPermissionDenied,
  DeploymentFailedAliasImpossible,
  DeploymentNotFound,
  DeploymentNotReady,
  DeploymentPermissionDenied,
  DomainAlreadyExists,
  DomainExternal,
  DomainMoveConflict,
  DomainNotAvailable,
  DomainNotFound,
  DomainNotVerified,
  DomainPaymentError,
  DomainPermissionDenied,
  DomainPurchasePending,
  DomainRemovalConflict,
  InvalidAlias,
  InvalidAliasInConfig,
  InvalidDeploymentId,
  InvalidDomain,
  InvalidMoveDestination,
  LinkRequiredError,
  NoAliasInConfig,
  NowError,
  ProjectNotFound,
  SourceNotFound,
  TLDNotSupportedViaCLI,
  TooManyRequests,
  UnexpectedDomainPurchaseError,
  UnexpectedDomainTransferError,
  UnsupportedTLD,
  UserAborted,
  cmd,
  code,
  getCommandName,
  getCommandNamePlain,
  isAPIError,
  packageName,
  require_bytes,
  require_lib as require_lib2,
  stripSensitiveAuthArgs
} from "./chunks/chunk-LN6B7ZI3.js";
import {
  pkg_default
} from "./chunks/chunk-P4QNYOFB.js";
import {
  emoji,
  eraseLines,
  link_default,
  output_manager_default,
  prependEmoji,
  require_dist
} from "./chunks/chunk-Z5SBJH6L.js";
import {
  require_source
} from "./chunks/chunk-S7KYDPEM.js";
import {
  __require,
  __toESM
} from "./chunks/chunk-TZ2YI2VH.js";

// src/util/telemetry/commands/agent/index.ts
var AgentTelemetryClient = class extends TelemetryClient {
  trackCliArgumentInit(value) {
    if (value) {
      this.trackCliArgument({
        arg: "init",
        value
      });
    }
  }
  trackCliFlagYes(value) {
    if (value) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/agent/init.ts
var import_chalk = __toESM(require_source(), 1);
import { readFile, writeFile } from "fs/promises";
import { join } from "path";
import { KNOWN_AGENTS } from "@vercel/detect-agent";
var BEST_PRACTICES_START = "<!-- VERCEL BEST PRACTICES START -->";
var BEST_PRACTICES_END = "<!-- VERCEL BEST PRACTICES END -->";
var BEST_PRACTICES_BODY = `## Best practices for developing on Vercel

These defaults are optimized for AI coding agents (and humans) working on apps that deploy to Vercel.

- Treat Vercel Functions as stateless + ephemeral (no durable RAM/FS, no background daemons), use Blob or marketplace integrations for preserving state
- Edge Functions (standalone) are deprecated; prefer Vercel Functions
- Don't start new projects on Vercel KV/Postgres (both discontinued); use Marketplace Redis/Postgres instead
- Store secrets in Vercel Env Variables; not in git or \`NEXT_PUBLIC_*\`
- Provision Marketplace native integrations with \`vercel integration add\` (CI/agent-friendly)
- Sync env + project settings with \`vercel env pull\` / \`vercel pull\` when you need local/offline parity
- Use \`waitUntil\` for post-response work; avoid the deprecated Function \`context\` parameter
- Set Function regions near your primary data source; avoid cross-region DB/service roundtrips
- Tune Fluid Compute knobs (e.g., \`maxDuration\`, memory/CPU) for long I/O-heavy calls (LLMs, APIs)
- Use Runtime Cache for fast **regional** caching + tag invalidation (don't treat it as global KV)
- Use Cron Jobs for schedules; cron runs in UTC and triggers your production URL via HTTP GET
- Use Vercel Blob for uploads/media; Use Edge Config for small, globally-read config
- If Enable Deployment Protection is enabled, use a bypass secret to directly access them
- Add OpenTelemetry via \`@vercel/otel\` on Node; don't expect OTEL support on the Edge runtime
- Enable Web Analytics + Speed Insights early
- Use AI Gateway for model routing, set AI_GATEWAY_API_KEY, using a model string (e.g. 'anthropic/claude-sonnet-4.6'), Gateway is already default in AI SDK
  needed. Always curl https://ai-gateway.vercel.sh/v1/models first; never trust model IDs from memory
- For durable agent loops or untrusted code: use Workflow (pause/resume/state) + Sandbox; use Vercel MCP for secure infra access`;
var BEST_PRACTICES_CONTENT = `${BEST_PRACTICES_START}
${BEST_PRACTICES_BODY}
${BEST_PRACTICES_END}
`;
function getTargetFile(agentName) {
  if (agentName === KNOWN_AGENTS.CLAUDE) {
    return "CLAUDE.md";
  }
  return "AGENTS.md";
}
async function agentInit(client, yes) {
  const targetFile = getTargetFile(client.agentName);
  const filePath = join(client.cwd, targetFile);
  let existing = null;
  try {
    existing = await readFile(filePath, "utf-8");
  } catch {
  }
  const hasMarkers = existing !== null && existing.includes(BEST_PRACTICES_START) && existing.includes(BEST_PRACTICES_END);
  const action = hasMarkers ? "update" : existing !== null ? "append" : "create";
  const promptMessage = hasMarkers ? `We're going to update Vercel best practices in your ${import_chalk.default.bold(targetFile)}. Proceed?` : `We're going to add Vercel best practices to your ${import_chalk.default.bold(targetFile)}. Proceed?`;
  if (!yes && client.stdin.isTTY) {
    const confirmed = await client.input.confirm(promptMessage, true);
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  } else if (!yes && !client.stdin.isTTY) {
    output_manager_default.error(
      "Missing required flag --yes. Use --yes to skip confirmation, or run interactively in a terminal."
    );
    return 1;
  }
  output_manager_default.spinner(`Writing Vercel best practices to ${targetFile}`);
  try {
    if (action === "update") {
      const startIdx = existing.indexOf(BEST_PRACTICES_START);
      const endIdx = existing.indexOf(BEST_PRACTICES_END) + BEST_PRACTICES_END.length;
      const trailingNewline = existing[endIdx] === "\n" ? 1 : 0;
      const updated = existing.slice(0, startIdx) + BEST_PRACTICES_CONTENT + existing.slice(endIdx + trailingNewline);
      await writeFile(filePath, updated, "utf-8");
      output_manager_default.stopSpinner();
      output_manager_default.success(
        `Updated Vercel best practices in ${import_chalk.default.bold(targetFile)}`
      );
    } else if (action === "append") {
      const separator = existing.endsWith("\n") ? "\n" : "\n\n";
      await writeFile(
        filePath,
        existing + separator + BEST_PRACTICES_CONTENT,
        "utf-8"
      );
      output_manager_default.stopSpinner();
      output_manager_default.success(
        `Appended Vercel best practices to ${import_chalk.default.bold(targetFile)}`
      );
    } else {
      await writeFile(filePath, BEST_PRACTICES_CONTENT, "utf-8");
      output_manager_default.stopSpinner();
      output_manager_default.success(
        `Created ${import_chalk.default.bold(targetFile)} with Vercel best practices`
      );
    }
  } catch (error) {
    output_manager_default.stopSpinner();
    printError(error);
    return 1;
  }
  output_manager_default.log(import_chalk.default.dim("Run vercel deploy to ship your project"));
  return 0;
}

// src/commands/agent/index.ts
async function agent(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(agentCommand.options);
  const telemetry2 = new AgentTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("agent");
    output_manager_default.print(help(agentCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const subcommand = parsedArgs.args[1];
  telemetry2.trackCliArgumentInit(subcommand);
  if (!subcommand || subcommand === "init") {
    const yes = parsedArgs.flags["--yes"] ?? false;
    telemetry2.trackCliFlagYes(yes);
    try {
      return await agentInit(client, yes);
    } catch (error) {
      printError(error);
      return 1;
    }
  }
  output_manager_default.error(`Unknown subcommand: ${subcommand}`);
  output_manager_default.print(help(agentCommand, { columns: client.stderr.columns }));
  return 1;
}

// src/util/telemetry/commands/activity/index.ts
var ActivityTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandLs(v) {
    if (v) {
      this.trackCliSubcommand({
        subcommand: "ls",
        value: v
      });
    }
  }
  trackCliSubcommandTypes(v) {
    if (v) {
      this.trackCliSubcommand({
        subcommand: "types",
        value: v
      });
    }
  }
  trackCliOptionType(v) {
    if (v && v.length > 0) {
      this.trackCliOption({
        option: "type",
        value: v.join(",")
      });
    }
  }
  trackCliOptionSince(v) {
    if (v) {
      this.trackCliOption({
        option: "since",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionUntil(v) {
    if (v) {
      this.trackCliOption({
        option: "until",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
  trackCliOptionLimit(v) {
    if (typeof v === "number") {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionNext(v) {
    if (typeof v === "number") {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/activity/index.ts
var COMMAND_CONFIG = {
  types: getCommandAliases(typesSubcommand)
};
async function activity(client) {
  const telemetry2 = new ActivityTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(activityCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("activity", subcommand);
    output_manager_default.print(help(activityCommand, { columns: client.stderr.columns }));
    return 0;
  }
  function printSubcommandHelp(command) {
    output_manager_default.print(
      help(command, { parent: activityCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "types": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("activity", subcommandOriginal);
        printSubcommandHelp(typesSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandTypes(subcommandOriginal);
      const typesFn = (await import("./chunks/types-P6JKZFEY.js")).default;
      return typesFn(client, telemetry2);
    }
    default: {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("activity", subcommandOriginal);
        output_manager_default.print(help(activityCommand, { columns: client.stderr.columns }));
        return 0;
      }
      telemetry2.trackCliSubcommandLs(subcommandOriginal);
      const listFn = (await import("./chunks/list-FIZTMQ44.js")).default;
      return listFn(client, telemetry2);
    }
  }
}

// src/commands/ai-gateway/api-keys-create.ts
var import_chalk2 = __toESM(require_source(), 1);

// src/util/ai-gateway/create-api-key.ts
async function createApiKey(client, payload) {
  return await client.fetch("/v1/api-keys", {
    method: "POST",
    body: {
      purpose: "ai-gateway",
      ...payload
    }
  });
}

// src/util/telemetry/commands/ai-gateway/api-keys-create.ts
var AiGatewayApiKeysCreateTelemetryClient = class extends TelemetryClient {
  trackCliOptionName(name) {
    if (name) {
      this.trackCliOption({
        option: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionBudget(budget) {
    if (budget !== void 0) {
      this.trackCliOption({
        option: "budget",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionRefreshPeriod(refreshPeriod) {
    if (refreshPeriod) {
      this.trackCliOption({
        option: "refresh-period",
        value: refreshPeriod
      });
    }
  }
  trackCliFlagIncludeByok(includeByok) {
    if (includeByok) {
      this.trackCliFlag("include-byok");
    }
  }
};

// src/commands/ai-gateway/api-keys-create.ts
var VALID_REFRESH_PERIODS = ["daily", "weekly", "monthly", "none"];
async function create(client, argv) {
  const telemetry2 = new AiGatewayApiKeysCreateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(createSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { flags: opts } = parsedArgs;
  const name = opts["--name"];
  const budget = opts["--budget"];
  const refreshPeriod = opts["--refresh-period"];
  const includeByok = opts["--include-byok"];
  telemetry2.trackCliOptionName(name);
  telemetry2.trackCliOptionBudget(budget);
  telemetry2.trackCliOptionRefreshPeriod(refreshPeriod);
  telemetry2.trackCliFlagIncludeByok(includeByok);
  if (budget !== void 0 && budget < 1) {
    const message = "Budget must be a positive number in dollars (minimum 1).";
    outputAgentError(
      client,
      {
        status: AGENT_STATUS.ERROR,
        reason: AGENT_REASON.INVALID_BUDGET,
        message,
        next: [
          {
            command: getCommandNamePlain(
              "ai-gateway api-keys create --budget 500"
            )
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  if (refreshPeriod && !VALID_REFRESH_PERIODS.includes(
    refreshPeriod
  )) {
    const message = `Invalid refresh period "${refreshPeriod}". Must be one of: ${VALID_REFRESH_PERIODS.join(", ")}.`;
    outputAgentError(
      client,
      {
        status: AGENT_STATUS.ERROR,
        reason: AGENT_REASON.INVALID_REFRESH_PERIOD,
        message,
        next: [
          {
            command: getCommandNamePlain(
              "ai-gateway api-keys create --refresh-period monthly"
            )
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  const effectiveRefreshPeriod = refreshPeriod && refreshPeriod !== "none" ? refreshPeriod : void 0;
  const aiGatewayQuota = budget !== void 0 || effectiveRefreshPeriod || includeByok ? {
    ...budget !== void 0 && { limitAmount: budget },
    ...effectiveRefreshPeriod && {
      refreshPeriod: effectiveRefreshPeriod
    },
    ...includeByok && { includeByokInQuota: true }
  } : void 0;
  if (!client.config.currentTeam) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "No team selected. Use `vercel --scope <team-slug> ai-gateway api-keys create` or run `vercel switch` first."
      );
      return 1;
    }
    const org = await selectOrg(client, "Which team should own this API key?");
    if (org.type === "team") {
      client.config.currentTeam = org.id;
    }
  }
  const createStamp = stamp_default();
  output_manager_default.spinner("Creating API key");
  try {
    const result = await createApiKey(client, {
      name,
      aiGatewayQuota
    });
    output_manager_default.stopSpinner();
    client.stdout.write(`${result.apiKeyString}
`);
    output_manager_default.success(
      `API key ${import_chalk2.default.bold(result.apiKey.name)} (${result.apiKey.id}) created ${createStamp()}`
    );
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err)) {
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
}

// src/util/telemetry/commands/ai-gateway/api-keys.ts
var AiGatewayApiKeysTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCreate(actual) {
    this.trackCliSubcommand({
      subcommand: "create",
      value: actual
    });
  }
};

// src/commands/ai-gateway/api-keys.ts
var COMMAND_CONFIG2 = {
  create: getCommandAliases(createSubcommand)
};
async function apiKeys(client) {
  const telemetry2 = new AiGatewayApiKeysTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(apiKeysSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(2);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG2
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("ai-gateway api-keys", subcommand);
    output_manager_default.print(help(apiKeysSubcommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: apiKeysSubcommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "create":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("ai-gateway api-keys", subcommandOriginal);
        printHelp(createSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandCreate(subcommandOriginal);
      return create(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG2));
      output_manager_default.print(help(apiKeysSubcommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/util/telemetry/commands/ai-gateway/index.ts
var AiGatewayTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandApiKeys(actual) {
    this.trackCliSubcommand({
      subcommand: "api-keys",
      value: actual
    });
  }
};

// src/commands/ai-gateway/index.ts
var COMMAND_CONFIG3 = {
  "api-keys": getCommandAliases(apiKeysSubcommand)
};
async function main2(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(aiGatewayCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new AiGatewayTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG3
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("ai-gateway");
    output_manager_default.print(help(aiGatewayCommand, { columns: client.stderr.columns }));
    return 2;
  }
  switch (subcommand) {
    case "api-keys":
      telemetry2.trackCliSubcommandApiKeys(subcommandOriginal);
      return apiKeys(client);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("ai-gateway", subcommandOriginal);
        output_manager_default.print(
          help(aiGatewayCommand, { columns: client.stderr.columns })
        );
        return 2;
      }
      output_manager_default.error(
        "Please specify a subcommand. Run `vercel ai-gateway --help` for usage information."
      );
      return 1;
  }
}

// src/util/telemetry/commands/alerts/index.ts
var AlertsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandLs(v) {
    if (v) {
      this.trackCliSubcommand({
        subcommand: "ls",
        value: v
      });
    }
  }
  trackCliSubcommandInspect(actual) {
    this.trackCliSubcommand({
      subcommand: "inspect",
      value: actual
    });
  }
  trackCliSubcommandRules(actual) {
    this.trackCliSubcommand({
      subcommand: "rules",
      value: actual
    });
  }
  trackCliOptionType(v) {
    if (v && v.length > 0) {
      this.trackCliOption({
        option: "type",
        value: v.join(",")
      });
    }
  }
  trackCliOptionSince(v) {
    if (v) {
      this.trackCliOption({
        option: "since",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionUntil(v) {
    if (v) {
      this.trackCliOption({
        option: "until",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
  trackCliFlagAi(v) {
    if (v) {
      this.trackCliFlag("ai");
    }
  }
  trackCliOptionLimit(v) {
    if (typeof v === "number") {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/alerts/index.ts
var COMMAND_CONFIG4 = {
  inspect: getCommandAliases(inspectSubcommand),
  ls: getCommandAliases(listSubcommand),
  rules: ["rules"]
};
async function alerts(client) {
  const telemetry2 = new AlertsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(alertsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err);
    const projectFlagMissingArg = msg.includes("--project") && msg.includes("requires argument");
    if (shouldEmitNonInteractiveCommandError(client)) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: projectFlagMissingArg ? "`--project` requires a project name or id (for example `--project my-app`)." : msg,
          next: projectFlagMissingArg ? [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "alerts --project <name-or-id>"
              ),
              when: "Re-run with a project name or id (replace placeholder)"
            },
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "alerts --help"
              ),
              when: "See all `alerts` flags and examples"
            }
          ] : [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "alerts --help"
              ),
              when: "See valid flags and examples"
            }
          ]
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG4
  );
  const needHelp = parsedArgs.flags["--help"];
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: alertsCommand, columns: client.stderr.columns })
    );
  }
  if (needHelp) {
    if (subcommand === "inspect") {
      telemetry2.trackCliFlagHelp("alerts", "inspect");
      printHelp(inspectSubcommand);
      return 0;
    }
    if (subcommand === "rules") {
      telemetry2.trackCliFlagHelp("alerts", "rules");
      const nested = args[0];
      if (nested === "ls" || nested === "list") {
        printHelp(rulesLsSubcommand);
        return 0;
      }
      if (nested === "add" || nested === "create") {
        printHelp(rulesAddSubcommand);
        return 0;
      }
      if (nested === "inspect" || nested === "get") {
        printHelp(rulesInspectSubcommand);
        return 0;
      }
      if (nested === "rm" || nested === "remove" || nested === "delete") {
        printHelp(rulesRmSubcommand);
        return 0;
      }
      if (nested === "update" || nested === "patch") {
        printHelp(rulesUpdateSubcommand);
        return 0;
      }
      output_manager_default.print(
        help(rulesAggregateCommand, {
          parent: alertsCommand,
          columns: client.stderr.columns
        })
      );
      return 0;
    }
    telemetry2.trackCliFlagHelp("alerts", subcommandOriginal);
    output_manager_default.print(help(alertsCommand, { columns: client.stderr.columns }));
    return 0;
  }
  switch (subcommand) {
    case "inspect": {
      telemetry2.trackCliSubcommandInspect(subcommandOriginal);
      const inspectFn = (await import("./chunks/inspect-MLAWEGMC.js")).default;
      return inspectFn(client, args);
    }
    case "rules": {
      telemetry2.trackCliSubcommandRules(args[0] ?? "ls");
      const rulesFn = (await import("./chunks/rules-3SSZZT7O.js")).default;
      return rulesFn(client, args);
    }
    default: {
      telemetry2.trackCliSubcommandLs(subcommandOriginal);
      const listFn = (await import("./chunks/list-74DI6236.js")).default;
      return listFn(client, telemetry2);
    }
  }
}

// src/commands/alias/ls.ts
var import_chalk3 = __toESM(require_source(), 1);
var import_ms = __toESM(require_ms(), 1);

// src/util/alias/get-aliases.ts
async function getAliases(client, deploymentId, next, limit = 20) {
  let aliasUrl = `/v3/now/aliases?limit=${limit}`;
  if (next) {
    aliasUrl += `&until=${next}`;
  }
  const to = deploymentId ? `/now/deployments/${deploymentId}/aliases` : aliasUrl;
  try {
    const payload = await client.fetch(to);
    return payload;
  } catch (err) {
    if (isAPIError(err)) {
      const contextName = await getScope(client).then(
        (scope) => scope.contextName
      );
      if (err.status === 404) {
        throw new DeploymentNotFound({
          id: deploymentId,
          context: contextName
        });
      }
      if (err.status === 403 && deploymentId) {
        throw new DeploymentPermissionDenied(deploymentId, contextName);
      }
      if (err.status === 400 && err.message.includes("`id`") && deploymentId) {
        throw new InvalidDeploymentId(deploymentId);
      }
    }
    throw err;
  }
}

// src/util/get-pagination-opts.ts
function getPaginationOpts(opts) {
  const { "--next": nextTimestamp, "--limit": limit } = opts;
  if (typeof nextTimestamp !== void 0 && Number.isNaN(nextTimestamp)) {
    throw new Error("Please provide a number for option --next");
  }
  if (typeof limit === "number" && (!Number.isInteger(limit) || limit > 100 || limit < 1)) {
    throw new Error(
      "Please provide an integer from 1 to 100 for option --limit"
    );
  }
  return [nextTimestamp, limit];
}

// src/util/telemetry/commands/alias/list.ts
var AliasListTelemetryClient = class extends TelemetryClient {
  trackCliOptionLimit(limit) {
    if (limit) {
      this.trackCliOption({
        option: "limit",
        value: String(limit)
      });
    }
  }
  trackCliOptionNext(next) {
    if (next) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/alias/ls.ts
async function ls(client, argv) {
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(listSubcommand2.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArguments;
  const validationResult = validateLsArgs({
    commandName: "alias ls",
    args
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  const { contextName } = await getScope(client);
  const telemetryClient = new AliasListTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let paginationOptions;
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  try {
    paginationOptions = getPaginationOpts(opts);
    const [next, limit] = paginationOptions;
    telemetryClient.trackCliOptionNext(next);
    telemetryClient.trackCliOptionLimit(limit);
    telemetryClient.trackCliOptionFormat(opts["--format"]);
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
  const lsStamp = stamp_default();
  output_manager_default.spinner(`Fetching aliases under ${import_chalk3.default.bold(contextName)}`);
  const { aliases, pagination } = await getAliases(
    client,
    void 0,
    ...paginationOptions
  );
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonOutput = {
      aliases: aliases.map((a) => ({
        alias: a.alias,
        deploymentId: a.deploymentId,
        url: a.deployment?.url ?? null,
        createdAt: a.createdAt
      })),
      pagination
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    output_manager_default.log(`aliases found under ${import_chalk3.default.bold(contextName)} ${lsStamp()}`);
    client.stdout.write(printAliasTable(aliases));
    if (pagination.count === 20) {
      const flags = getCommandFlags(opts, ["_", "--next", "--format"]);
      output_manager_default.log(
        `To display the next page run ${getCommandName(
          `alias ls${flags} --next ${pagination.next}`
        )}`
      );
    }
  }
  return 0;
}
function printAliasTable(aliases) {
  return `${table(
    [
      ["source", "url", "age"].map((header) => import_chalk3.default.gray(header)),
      ...aliases.map((a) => [
        // for legacy reasons, we might have situations
        // where the deployment was deleted and the alias
        // not collected appropriately, and we need to handle it
        a.deployment?.url ? a.deployment.url : import_chalk3.default.gray("\u2013"),
        a.alias,
        (0, import_ms.default)(Date.now() - a.createdAt)
      ])
    ],
    { align: ["l", "l", "r"], hsep: 4 }
  ).replace(/^/gm, "  ")}

`;
}

// src/commands/alias/rm.ts
var import_chalk4 = __toESM(require_source(), 1);
var import_ms2 = __toESM(require_ms(), 1);

// src/util/alias/remove-alias-by-id.ts
async function removeAliasById(client, id) {
  return client.fetch(`/now/aliases/${id}`, {
    method: "DELETE"
  });
}

// src/util/alias/find-alias-by-alias-or-id.ts
async function findAliasByAliasOrId(client, aliasOrId) {
  return client.fetch(
    `/now/aliases/${encodeURIComponent(getSafeAlias(aliasOrId))}`
  );
}
function getSafeAlias(alias2) {
  return alias2.replace(/^https:\/\//i, "").replace(/^\.+/, "").replace(/\.+$/, "").toLowerCase();
}

// src/util/telemetry/commands/alias/remove.ts
var AliasRemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentAlias(alias2) {
    if (alias2) {
      this.trackCliArgument({
        arg: "alias",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/alias/rm.ts
async function rm(client, argv) {
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(removeSubcommand.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArguments;
  const { contextName } = await getScope(client);
  const telemetryClient = new AliasRemoveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetryClient.trackCliFlagYes(opts["--yes"]);
  const [aliasOrId] = args;
  telemetryClient.trackCliArgumentAlias(aliasOrId);
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk4.default.cyan(
        `${getCommandName("alias rm <alias>")}`
      )}`
    );
    return 1;
  }
  if (!aliasOrId) {
    output_manager_default.error(`${getCommandName("alias rm <alias>")} expects one argument`);
    return 1;
  }
  if (!isValidName(aliasOrId)) {
    output_manager_default.error(`The provided argument "${aliasOrId}" is not a valid alias`);
    return 1;
  }
  const alias2 = await findAliasByAliasOrId(client, aliasOrId);
  if (!alias2) {
    output_manager_default.error(
      `Alias not found by "${aliasOrId}" under ${import_chalk4.default.bold(contextName)}`
    );
    output_manager_default.log(`Run ${getCommandName("alias ls")} to see your aliases.`);
    return 1;
  }
  const removeStamp = stamp_default();
  if (!opts["--yes"] && !await confirmAliasRemove(client, alias2)) {
    output_manager_default.log("Canceled");
    return 0;
  }
  await removeAliasById(client, alias2.uid);
  output_manager_default.success(`Alias ${import_chalk4.default.bold(alias2.alias)} removed ${removeStamp()}`);
  return 0;
}
async function confirmAliasRemove(client, alias2) {
  const srcUrl = alias2.deployment ? import_chalk4.default.underline(alias2.deployment.url) : null;
  const tbl = table(
    [
      [
        ...srcUrl ? [srcUrl] : [],
        import_chalk4.default.underline(alias2.alias),
        import_chalk4.default.gray(`${(0, import_ms2.default)(Date.now() - alias2.createdAt)} ago`)
      ]
    ],
    { hsep: 4 }
  );
  output_manager_default.log("The following alias will be removed permanently");
  output_manager_default.print(`  ${tbl}
`);
  return client.input.confirm(import_chalk4.default.red("Are you sure?"), false);
}

// src/commands/alias/set.ts
var import_chalk8 = __toESM(require_source(), 1);

// src/util/certs/get-wildcard-cns-for-alias.ts
var import_tldts = __toESM(require_cjs(), 1);
function getWildcardCNSForAlias(alias2) {
  if (isWildcardAlias(alias2)) {
    return [extractDomain(alias2), alias2];
  }
  const parsedDomain = (0, import_tldts.parse)(alias2);
  const { domain: domain2, subdomain } = parsedDomain;
  if (!domain2) {
    throw new InvalidDomain(alias2);
  }
  const secondLevel = subdomain && subdomain.includes(".") ? subdomain.split(".").slice(1).join(".") : null;
  const root = secondLevel ? `${secondLevel}.${domain2}` : domain2;
  return [root, `*.${root}`];
}

// src/util/output/join-words.ts
function joinWords(words = []) {
  if (words.length === 0) {
    return "";
  }
  if (words.length === 1) {
    return words[0];
  }
  const last = words[words.length - 1];
  const rest = words.slice(0, words.length - 1);
  return `${rest.join(", ")} and ${last}`;
}

// src/util/certs/create-cert-for-alias.ts
async function createCertificateForAlias(client, context, alias2, shouldBeWildcard) {
  output_manager_default.spinner(`Generating a certificate\u2026`);
  const cns = shouldBeWildcard ? getWildcardCNSForAlias(alias2) : [alias2];
  const certStamp = stamp_default();
  const cert = await createCertForCns(client, cns, context);
  if (cert instanceof NowError) {
    output_manager_default.stopSpinner();
    return cert;
  }
  output_manager_default.log(
    `Certificate for ${joinWords(cert.cns)} (${cert.uid}) created ${certStamp()}`
  );
  return cert;
}

// src/util/alias/create-alias.ts
async function createAlias(client, contextName, idOrUrl, alias2, externalDomain) {
  output_manager_default.spinner(`Creating alias`);
  const result = await performCreateAlias(client, contextName, idOrUrl, alias2);
  output_manager_default.stopSpinner();
  if (result instanceof CertMissing) {
    const cert = await createCertificateForAlias(
      client,
      contextName,
      alias2,
      !externalDomain
    );
    if (cert instanceof Error) {
      return cert;
    }
    output_manager_default.spinner(`Creating alias`);
    const secondTry = await performCreateAlias(
      client,
      contextName,
      idOrUrl,
      alias2
    );
    output_manager_default.stopSpinner();
    return secondTry;
  }
  return result;
}
async function performCreateAlias(client, contextName, idOrUrl, alias2) {
  try {
    return await client.fetch(
      `/now/deployments/${encodeURIComponent(idOrUrl)}/aliases`,
      {
        method: "POST",
        body: { alias: alias2 }
      }
    );
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "cert_missing" || err.code === "cert_expired") {
        return new CertMissing(alias2);
      }
      if (err.status === 409) {
        return { uid: err.uid, alias: err.alias };
      }
      if (err.code === "deployment_not_found" || err.code === "not_found") {
        return new DeploymentNotFound({
          context: contextName,
          id: idOrUrl
        });
      }
      if (err.code === "gone") {
        return new DeploymentFailedAliasImpossible();
      }
      if (err.code === "invalid_alias") {
        return new InvalidAlias(alias2);
      }
      if (err.code === "deployment_not_ready") {
        return new DeploymentNotReady({
          url: idOrUrl
        });
      }
      if (err.status === 403) {
        if (err.code === "alias_in_use") {
          return new AliasInUse(alias2);
        }
        if (err.code === "forbidden") {
          return new DomainPermissionDenied(alias2, contextName);
        }
      }
    }
    throw err;
  }
}

// src/util/domains/is-domain-external.ts
function isDomainExternal(domain2) {
  return domain2.serviceType !== "zeit.world";
}

// src/util/alias/assign-alias.ts
async function assignAlias(client, idOrUrl, alias2, contextName) {
  let externalDomain = false;
  if (alias2.indexOf(".") !== -1 && !alias2.endsWith(".now.sh") && !alias2.endsWith(".vercel.app")) {
    const result = await setupDomain(client, alias2, contextName);
    if (result instanceof Error) {
      return result;
    }
    externalDomain = isDomainExternal(result);
  }
  const record = await createAlias(
    client,
    contextName,
    idOrUrl,
    alias2,
    externalDomain
  );
  return record;
}

// src/util/alias/get-deployment-by-alias.ts
var import_chalk5 = __toESM(require_source(), 1);
import path from "path";

// src/util/deploy/get-deployments-by-appname.ts
async function fetchDeploymentsByAppName(client, appName) {
  const { deployments } = await client.fetch(
    `/v3/now/deployments?app=${encodeURIComponent(appName)}`
  );
  return deployments;
}

// src/util/alias/get-deployment-by-alias.ts
async function getAppLastDeployment(client, appName, user, contextName) {
  output_manager_default.debug(`Looking for deployments matching app ${appName}`);
  const deployments = await fetchDeploymentsByAppName(client, appName);
  const deploymentItem = deployments.sort((a, b) => b.created - a.created).filter((dep) => dep.state === "READY" && dep.creator.uid === user.id)[0];
  if (deploymentItem) {
    return await getDeployment(client, contextName, deploymentItem.uid);
  }
  return null;
}
async function getDeploymentForAlias(client, localConfigPath, user, contextName, localConfig) {
  output_manager_default.spinner(`Fetching deployment to alias in ${import_chalk5.default.bold(contextName)}`);
  const appName = localConfig?.name || path.basename(path.resolve(process.cwd(), localConfigPath || ""));
  if (!appName) {
    return null;
  }
  try {
    return await getAppLastDeployment(client, appName, user, contextName);
  } finally {
    output_manager_default.stopSpinner();
  }
}

// src/util/certs/handle-cert-error.ts
var import_ms3 = __toESM(require_ms(), 1);
var import_tldts2 = __toESM(require_cjs(), 1);
var import_chalk7 = __toESM(require_source(), 1);

// src/util/format-dns-table.ts
var import_chalk6 = __toESM(require_source(), 1);
var HEADER = ["name", "type", "value"].map((v) => (0, import_chalk6.gray)(v));
function formatDNSTable(rows) {
  return table([HEADER, ...rows], { hsep: 8 });
}

// src/util/certs/handle-cert-error.ts
function handleCertError(error) {
  if (error instanceof TooManyRequests) {
    output_manager_default.error(
      `Too many requests detected for ${error.meta.api} API. Try again in ${(0, import_ms3.default)(
        error.meta.retryAfterMs,
        {
          long: true
        }
      )}.`
    );
    return 1;
  }
  if (error instanceof CertError) {
    output_manager_default.error(error.message);
    if (error.meta.helpUrl) {
      output_manager_default.print(`  Read more: ${error.meta.helpUrl}
`);
    }
    return 1;
  }
  if (error instanceof DomainNotFound) {
    output_manager_default.error(error.message);
    return 1;
  }
  if (error instanceof CertConfigurationError) {
    const { external, cns } = error.meta;
    output_manager_default.error(
      `We couldn't verify the propagation of the DNS settings for ${error.meta.cns.map((cn) => import_chalk7.default.underline(cn)).join(", ")}`
    );
    if (external) {
      output_manager_default.print(
        `  The propagation may take a few minutes, but please verify your settings:

`
      );
      output_manager_default.print(
        `${formatDNSTable(
          cns.map((cn) => {
            const parsed = (0, import_tldts2.parse)(cn);
            return parsed.subdomain ? [parsed.subdomain, "ALIAS", "alias.vercel.com"] : ["", "ALIAS", "alias.vercel.com"];
          })
        )}

`
      );
      output_manager_default.log(
        `Alternatively, you can issue a certificate solving DNS challenges manually after running:`
      );
      output_manager_default.print(
        `  ${getCommandName(`certs issue --challenge-only <cns>`)}
`
      );
      output_manager_default.print(
        "  Read more: https://err.sh/vercel/dns-configuration-error\n"
      );
    } else {
      output_manager_default.print(
        `  We configured them for you, but the propagation may take a few minutes. Please try again later.
`
      );
      output_manager_default.print(
        "  Read more: https://err.sh/vercel/dns-configuration-error\n\n"
      );
    }
    return 1;
  }
  return error;
}

// src/util/telemetry/commands/alias/set.ts
var AliasSetTelemetryClient = class extends TelemetryClient {
  trackCliFlagDebug(flag) {
    if (flag) {
      this.trackCliFlag("debug");
    }
  }
  trackCliOptionLocalConfig(localConfig) {
    if (localConfig) {
      this.trackCliOption({
        option: "local-config",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentIdOrUrl(idOrUrl) {
    if (idOrUrl) {
      this.trackCliArgument({
        arg: "id-or-url",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentAlias(customDomain) {
    if (customDomain) {
      this.trackCliArgument({
        arg: "custom-domain",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/alias/set.ts
async function set(client, argv) {
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(listSubcommand2.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArguments;
  const setStamp = stamp_default();
  const { localConfig } = client;
  const telemetryClient = new AliasSetTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetryClient.trackCliFlagDebug(opts["--debug"]);
  telemetryClient.trackCliOptionLocalConfig(opts["--local-config"]);
  const { contextName, user } = await getScope(client);
  if (args.length > 2) {
    output_manager_default.error(
      `${getCommandName("alias <id-or-url> <target>")} accepts at most two arguments`
    );
    return 1;
  }
  if (args.length >= 1 && !isValidName(args[0])) {
    output_manager_default.error(`The provided argument "${args[0]}" is not a valid ID or URL`);
    return 1;
  }
  if (args.length >= 2 && !isValidName(args[1])) {
    output_manager_default.error(`The provided argument "${args[1]}" is not a valid domain`);
    return 1;
  }
  if (args.length === 0) {
    output_manager_default.error(
      `To ship to production, optionally configure your domains (${link_default(
        "https://vercel.link/domain-configuration"
      )}) and run ${getCommandName("--prod")}.`
    );
    return 1;
  }
  if (args.length === 1) {
    const [aliasTarget2] = args;
    telemetryClient.trackCliArgumentAlias(aliasTarget2);
    const deployment = handleCertError(
      await getDeploymentForAlias(
        client,
        opts["--local-config"],
        user,
        contextName,
        localConfig
      )
    );
    if (deployment === 1) {
      return deployment;
    }
    if (deployment instanceof Error) {
      output_manager_default.error(deployment.message);
      return 1;
    }
    if (!deployment) {
      output_manager_default.error(
        `Couldn't find a deployment to alias. Please provide one as an argument.`
      );
      return 1;
    }
    const targets = getTargetsForAlias(args, localConfig);
    if (targets instanceof Error) {
      output_manager_default.prettyError(targets);
      return 1;
    }
    for (const target of targets) {
      output_manager_default.log(`Assigning alias ${target} to deployment ${deployment.url}`);
      const record2 = await assignAlias(
        client,
        deployment.id,
        target,
        contextName
      );
      const handleResult2 = handleSetupDomainError(
        handleCreateAliasError(record2)
      );
      if (handleResult2 === 1) {
        return 1;
      }
      output_manager_default.success(
        `${import_chalk8.default.bold(
          `${isWildcardAlias(target) ? "" : "https://"}${handleResult2.alias}`
        )} now points to https://${deployment.url} ${setStamp()}`
      );
    }
    return 0;
  }
  const [idOrUrl, aliasTarget] = args;
  telemetryClient.trackCliArgumentIdOrUrl(idOrUrl);
  telemetryClient.trackCliArgumentAlias(aliasTarget);
  output_manager_default.log(`Assigning alias ${aliasTarget} to ${idOrUrl}`);
  const isWildcard = isWildcardAlias(aliasTarget);
  const idOrUrlForRequest = idOrUrl.includes(".") ? toHost(idOrUrl) : idOrUrl;
  const record = await assignAlias(
    client,
    idOrUrlForRequest,
    aliasTarget,
    contextName
  );
  const handleResult = handleSetupDomainError(handleCreateAliasError(record));
  if (handleResult === 1) {
    return 1;
  }
  const prefix = isWildcard ? "" : "https://";
  output_manager_default.success(
    `${import_chalk8.default.bold(
      `${prefix}${handleResult.alias}`
    )} now points to ${idOrUrl} ${setStamp()}`
  );
  return 0;
}
function handleSetupDomainError(error) {
  if (error instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You don't have permissions over domain ${import_chalk8.default.underline(
        error.meta.domain
      )} under ${import_chalk8.default.bold(error.meta.context)}.`
    );
    return 1;
  }
  if (error instanceof UserAborted) {
    output_manager_default.error("User canceled.");
    return 1;
  }
  if (error instanceof DomainNotFound) {
    output_manager_default.error("You should buy the domain before aliasing.");
    return 1;
  }
  if (error instanceof UnsupportedTLD) {
    output_manager_default.error(
      `The TLD for domain name ${error.meta.domain} is not supported.`
    );
    return 1;
  }
  if (error instanceof InvalidDomain) {
    output_manager_default.error(
      `The domain ${error.meta.domain} used for the alias is not valid.`
    );
    return 1;
  }
  if (error instanceof DomainNotAvailable) {
    output_manager_default.error(
      `The domain ${error.meta.domain} is not available to be purchased.`
    );
    return 1;
  }
  if (error instanceof UnexpectedDomainPurchaseError) {
    output_manager_default.error("There was an unexpected error while purchasing the domain.");
    return 1;
  }
  if (error instanceof DomainAlreadyExists) {
    output_manager_default.error(
      `The domain  ${error.meta.domain} exists for a different account.`
    );
    return 1;
  }
  if (error instanceof DomainPurchasePending) {
    output_manager_default.error(
      `The domain ${error.meta.domain} is processing and will be available once the order is completed.`
    );
    output_manager_default.print(
      "  An email will be sent upon completion so you can alias to your new domain.\n"
    );
    return 1;
  }
  if (error instanceof SourceNotFound) {
    output_manager_default.error(
      `You can't purchase the domain you're aliasing to since you have no valid payment method.`
    );
    output_manager_default.print("  Please add a valid payment method and retry.\n");
    return 1;
  }
  if (error instanceof DomainPaymentError) {
    output_manager_default.error(
      `You can't purchase the domain you're aliasing to since your card was declined.`
    );
    output_manager_default.print("  Please add a valid payment method and retry.\n");
    return 1;
  }
  if (error instanceof TLDNotSupportedViaCLI) {
    output_manager_default.error(
      `The TLD for domain name ${error.meta.domain} is not supported via the CLI. Use the REST API or the dashboard to purchase.`
    );
    return 1;
  }
  return error;
}
function handleCreateAliasError(errorOrResult) {
  const error = handleCertError(errorOrResult);
  if (error === 1) {
    return error;
  }
  if (error instanceof AliasInUse) {
    output_manager_default.error(
      `The alias ${import_chalk8.default.dim(
        error.meta.alias
      )} is a deployment URL or it's in use by a different team.`
    );
    return 1;
  }
  if (error instanceof DeploymentNotFound) {
    output_manager_default.error(
      `Failed to find ID or URL ${import_chalk8.default.dim(error.meta.id)} under ${import_chalk8.default.bold(
        error.meta.context
      )}`
    );
    return 1;
  }
  if (error instanceof InvalidAlias) {
    output_manager_default.error(
      "Invalid alias. Please confirm that the alias you provided is a valid hostname. Note: For `vercel.app`, only sub and sub-sub domains are supported."
    );
    return 1;
  }
  if (error instanceof DeploymentPermissionDenied) {
    output_manager_default.error(
      `No permission to access ID or URL ${import_chalk8.default.dim(
        error.meta.id
      )} under ${import_chalk8.default.bold(error.meta.context)}`
    );
    return 1;
  }
  if (error instanceof CertMissing) {
    output_manager_default.error(
      `There is no certificate for the domain ${error.meta.domain} and it could not be created.`
    );
    output_manager_default.log(
      `Please generate a new certificate manually with ${getCommandName(
        `certs issue ${error.meta.domain}`
      )}`
    );
    return 1;
  }
  if (error instanceof InvalidDomain) {
    output_manager_default.error(
      `The domain ${error.meta.domain} used for the alias is not valid.`
    );
    return 1;
  }
  if (error instanceof DomainPermissionDenied || error instanceof DeploymentFailedAliasImpossible || error instanceof InvalidDeploymentId) {
    output_manager_default.error(error.message);
    return 1;
  }
  if (error instanceof DeploymentNotReady) {
    output_manager_default.error(error.message);
    return 1;
  }
  return error;
}
function getTargetsForAlias(args, { alias: alias2 } = {}) {
  if (args.length) {
    return [args[args.length - 1]].map((target) => target.indexOf(".") !== -1 ? toHost(target) : target).filter((x) => !!x && typeof x === "string");
  }
  if (!alias2) {
    return new NoAliasInConfig();
  }
  if (typeof alias2 !== "string" && !Array.isArray(alias2)) {
    return new InvalidAliasInConfig(alias2);
  }
  return typeof alias2 === "string" ? [alias2] : alias2;
}

// src/util/telemetry/commands/alias/index.ts
var AliasTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandSet(actual) {
    this.trackCliSubcommand({
      subcommand: "set",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "rm",
      value: actual
    });
  }
};

// src/commands/alias/index.ts
var COMMAND_CONFIG5 = {
  ls: getCommandAliases(listSubcommand2),
  rm: getCommandAliases(removeSubcommand),
  set: getCommandAliases(setSubcommand)
};
async function alias(client) {
  const telemetry2 = new AliasTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(aliasCommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArguments.args.slice(1),
    COMMAND_CONFIG5
  );
  const needHelp = parsedArguments.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("alias");
    output_manager_default.print(help(aliasCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: aliasCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "ls":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("alias", subcommandOriginal);
        printHelp(listSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("alias", subcommandOriginal);
        printHelp(removeSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm(client, args);
    case "set":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("alias", subcommandOriginal);
        printHelp(setSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSet(subcommandOriginal);
      return set(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("alias", subcommandOriginal);
        printHelp(setSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSet(subcommandOriginal);
      return set(client, args);
  }
}

// src/commands/bisect/index.ts
var import_open = __toESM(require_open(), 1);
var import_execa = __toESM(require_execa(), 1);
var import_pluralize = __toESM(require_pluralize(), 1);
var import_chalk10 = __toESM(require_source(), 1);
import { resolve } from "path";
import { URLSearchParams as URLSearchParams2, parse as parse3 } from "url";

// src/util/format-date.ts
var import_ms4 = __toESM(require_ms(), 1);
var import_chalk9 = __toESM(require_source(), 1);
var import_format = __toESM(require_format(), 1);
function formatDate(dateStrOrNumber) {
  if (!dateStrOrNumber) {
    return import_chalk9.default.gray("-");
  }
  const date = new Date(dateStrOrNumber);
  const diff2 = date.getTime() - Date.now();
  return diff2 < 0 ? `${(0, import_format.default)(date, "DD MMMM YYYY HH:mm:ss")} ${import_chalk9.default.gray(
    `[${(0, import_ms4.default)(-diff2)} ago]`
  )}` : `${(0, import_format.default)(date, "DD MMMM YYYY HH:mm:ss")} ${import_chalk9.default.gray(
    `[in ${(0, import_ms4.default)(diff2)}]`
  )}`;
}
function formatDateWithoutTime(dateStrOrNumber) {
  if (!dateStrOrNumber) {
    return import_chalk9.default.gray("-");
  }
  const date = new Date(dateStrOrNumber);
  const diff2 = date.getTime() - Date.now();
  return diff2 < 0 ? `${(0, import_format.default)(date, "MMM DD YYYY")} ${import_chalk9.default.gray(`[${(0, import_ms4.default)(-diff2)} ago]`)}` : `${(0, import_format.default)(date, "MMM DD YYYY")} ${import_chalk9.default.gray(`[in ${(0, import_ms4.default)(diff2)}]`)}`;
}

// src/util/bisect/normalize-url.ts
function hasScheme(url) {
  return url.startsWith("http://") || url.startsWith("https://");
}
function normalizeURL(url) {
  return hasScheme(url) ? url : `https://${url}`;
}

// src/util/telemetry/commands/bisect/index.ts
var BisectTelemetryClient = class extends TelemetryClient {
  trackCliOptionGood(good) {
    if (good) {
      this.trackCliOption({
        option: "good",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionBad(bad) {
    if (bad) {
      this.trackCliOption({
        option: "bad",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionPath(path3) {
    if (path3) {
      this.trackCliOption({
        option: "path",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionRun(run2) {
    if (run2) {
      this.trackCliOption({
        option: "run",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagOpen(open15) {
    if (open15) {
      this.trackCliFlag("open");
    }
  }
};

// src/commands/bisect/index.ts
async function bisect(client) {
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(bisectCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new BisectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("bisect");
    output_manager_default.print(help(bisectCommand, { columns: client.stderr.columns }));
    return 2;
  }
  telemetry2.trackCliOptionGood(parsedArgs.flags["--good"]);
  telemetry2.trackCliOptionBad(parsedArgs.flags["--bad"]);
  telemetry2.trackCliOptionPath(parsedArgs.flags["--path"]);
  telemetry2.trackCliOptionRun(parsedArgs.flags["--run"]);
  telemetry2.trackCliFlagOpen(parsedArgs.flags["--open"]);
  const scope = await getScope(client);
  const { contextName } = scope;
  let bad = parsedArgs.flags["--bad"] || await client.input.text({
    message: `Specify a URL where the bug occurs:`,
    validate: (val) => val ? true : "A URL must be provided"
  });
  let good = parsedArgs.flags["--good"] || await client.input.text({
    message: `Specify a URL where the bug does not occur:`,
    validate: (val) => val ? true : "A URL must be provided"
  });
  let subpath = parsedArgs.flags["--path"] || "";
  let run2 = parsedArgs.flags["--run"] || "";
  const openEnabled = parsedArgs.flags["--open"] || false;
  if (run2) {
    run2 = resolve(run2);
  }
  bad = normalizeURL(bad);
  let parsed = parse3(bad);
  if (!parsed.hostname) {
    output_manager_default.error("Invalid input: no hostname provided");
    return 1;
  }
  bad = parsed.hostname;
  if (typeof parsed.path === "string" && parsed.path !== "/") {
    if (subpath && subpath !== parsed.path) {
      output_manager_default.note(
        `Ignoring subpath ${import_chalk10.default.bold(
          parsed.path
        )} in favor of \`--path\` argument ${import_chalk10.default.bold(subpath)}`
      );
    } else {
      subpath = parsed.path;
    }
  }
  good = normalizeURL(good);
  parsed = parse3(good);
  if (!parsed.hostname) {
    output_manager_default.error("Invalid input: no hostname provided");
    return 1;
  }
  good = parsed.hostname;
  if (typeof parsed.path === "string" && parsed.path !== "/" && subpath && subpath !== parsed.path) {
    output_manager_default.note(
      `Ignoring subpath ${import_chalk10.default.bold(
        parsed.path
      )} which does not match ${import_chalk10.default.bold(subpath)}`
    );
  }
  if (!subpath) {
    subpath = await client.input.text({
      message: `Specify the URL subpath where the bug occurs:`,
      validate: (val) => val ? true : "A subpath must be provided"
    });
  }
  output_manager_default.spinner("Retrieving deployments\u2026");
  const badDeployment = await getDeployment(client, contextName, bad).catch(
    (err) => err
  );
  if (badDeployment) {
    if (badDeployment instanceof Error) {
      badDeployment.message += ` when requesting bad deployment "${normalizeURL(
        bad
      )}"`;
      output_manager_default.prettyError(badDeployment);
      return 1;
    }
    bad = badDeployment.url;
  } else {
    output_manager_default.error(`Failed to retrieve ${import_chalk10.default.bold("bad")} Deployment: ${bad}`);
    return 1;
  }
  const goodDeployment = await getDeployment(client, contextName, good).catch(
    (err) => err
  );
  if (goodDeployment) {
    if (goodDeployment instanceof Error) {
      goodDeployment.message += ` when requesting good deployment "${normalizeURL(
        good
      )}"`;
      output_manager_default.prettyError(goodDeployment);
      return 1;
    }
    good = goodDeployment.url;
  } else {
    output_manager_default.error(
      `Failed to retrieve ${import_chalk10.default.bold("good")} Deployment: ${good}`
    );
    return 1;
  }
  const { projectId } = badDeployment;
  if (projectId !== goodDeployment.projectId) {
    output_manager_default.error(`Good and Bad deployments must be from the same Project`);
    return 1;
  }
  if (badDeployment.url === goodDeployment.url) {
    output_manager_default.error(`Good and Bad deployments must be different`);
    return 1;
  }
  if (badDeployment.createdAt < goodDeployment.createdAt) {
    output_manager_default.error(`Good deployment must be older than the Bad deployment`);
    return 1;
  }
  if (badDeployment.target !== goodDeployment.target) {
    output_manager_default.error(
      `Bad deployment target "${badDeployment.target || "preview"}" does not match good deployment target "${goodDeployment.target || "preview"}"`
    );
    return 1;
  }
  let deployments = [];
  const query = new URLSearchParams2();
  query.set("projectId", projectId);
  if (badDeployment.target) {
    query.set("target", badDeployment.target);
  }
  query.set("state", "READY");
  query.set("until", String(badDeployment.createdAt + 1));
  for await (const chunk of client.fetchPaginated(
    `/v6/deployments?${query}`,
    {
      accountId: badDeployment.ownerId
    }
  )) {
    let newDeployments = chunk.deployments;
    let hasGood = false;
    for (let i = 0; i < newDeployments.length; i++) {
      if (newDeployments[i].url === good) {
        newDeployments = newDeployments.slice(0, i);
        hasGood = true;
        break;
      }
    }
    deployments = deployments.concat(newDeployments);
    if (hasGood)
      break;
  }
  if (!deployments.length) {
    output_manager_default.error(
      "Cannot bisect because this project does not have any deployments"
    );
    return 1;
  }
  let lastBad = deployments.shift();
  while (deployments.length > 0) {
    output_manager_default.print("\n");
    const middleIndex = Math.floor(deployments.length / 2);
    const deployment = deployments[middleIndex];
    const rem = (0, import_pluralize.default)("deployment", deployments.length, true);
    const steps = Math.floor(Math.log2(deployments.length));
    const pSteps = (0, import_pluralize.default)("step", steps, true);
    output_manager_default.log(
      import_chalk10.default.magenta(
        `${import_chalk10.default.bold(
          "Bisecting:"
        )} ${rem} left to test after this (roughly ${pSteps})`
      ),
      import_chalk10.default.magenta
    );
    const testUrl = `https://${deployment.url}${subpath}`;
    output_manager_default.log(`${import_chalk10.default.bold("Deployment URL:")} ${link_default(testUrl)}`);
    output_manager_default.log(`${import_chalk10.default.bold("Date:")} ${formatDate(deployment.createdAt)}`);
    const commit2 = getCommit(deployment);
    if (commit2) {
      const shortSha = commit2.sha.substring(0, 7);
      const firstLine = commit2.message?.split("\n")[0];
      output_manager_default.log(`${import_chalk10.default.bold("Commit:")} [${shortSha}] ${firstLine}`);
    }
    let action;
    if (run2) {
      const proc = await (0, import_execa.default)(run2, [testUrl], {
        stdio: "inherit",
        reject: false,
        env: {
          ...process.env,
          HOST: deployment.url,
          URL: testUrl
        }
      });
      if (proc instanceof Error && typeof proc.exitCode !== "number") {
        output_manager_default.prettyError(proc);
        return 1;
      }
      const { exitCode: exitCode2 } = proc;
      let color;
      if (exitCode2 === 0) {
        color = import_chalk10.default.green;
        action = "good";
      } else if (exitCode2 === 125) {
        action = "skip";
        color = import_chalk10.default.grey;
      } else {
        action = "bad";
        color = import_chalk10.default.red;
      }
      output_manager_default.log(
        `Run script returned exit code ${import_chalk10.default.bold(String(exitCode2))}: ${color(
          action
        )}`
      );
    } else {
      if (openEnabled) {
        await (0, import_open.default)(testUrl);
      }
      action = await client.input.expand({
        message: "Select an action:",
        choices: [
          { key: "g", name: "Good", value: "good" },
          { key: "b", name: "Bad", value: "bad" },
          { key: "s", name: "Skip", value: "skip" }
        ]
      });
    }
    if (action === "good") {
      deployments = deployments.slice(0, middleIndex);
    } else if (action === "bad") {
      lastBad = deployment;
      deployments = deployments.slice(middleIndex + 1);
    } else if (action === "skip") {
      deployments.splice(middleIndex, 1);
    }
  }
  output_manager_default.print("\n");
  const result = [
    import_chalk10.default.bold(
      `The first bad deployment is: ${link_default(`https://${lastBad.url}`)}`
    ),
    "",
    `   ${import_chalk10.default.bold("Date:")} ${formatDate(lastBad.createdAt)}`
  ];
  const commit = getCommit(lastBad);
  if (commit) {
    const shortSha = commit.sha.substring(0, 7);
    const firstLine = commit.message?.split("\n")[0];
    result.push(` ${import_chalk10.default.bold("Commit:")} [${shortSha}] ${firstLine}`);
  }
  result.push(`${import_chalk10.default.bold("Inspect:")} ${link_default(lastBad.inspectorUrl)}`);
  output_manager_default.print(box(result.join("\n")));
  output_manager_default.print("\n");
  return 0;
}
function getCommit(deployment) {
  const sha = deployment.meta?.githubCommitSha || deployment.meta?.gitlabCommitSha || deployment.meta?.bitbucketCommitSha;
  if (!sha)
    return null;
  const message = deployment.meta?.githubCommitMessage || deployment.meta?.gitlabCommitMessage || deployment.meta?.bitbucketCommitMessage;
  return { sha, message };
}

// src/commands/blob/list.ts
import * as blob from "@vercel/blob";
var import_chalk12 = __toESM(require_source(), 1);
var import_ms5 = __toESM(require_ms(), 1);

// src/util/telemetry/commands/blob/list.ts
var BlobListTelemetryClient = class extends TelemetryClient {
  trackCliOptionLimit(limit) {
    if (limit) {
      this.trackCliOption({
        option: "limit",
        value: String(limit)
      });
    }
  }
  trackCliOptionCursor(cursor) {
    if (cursor) {
      this.trackCliOption({
        option: "cursor",
        value: cursor
      });
    }
  }
  trackCliOptionPrefix(prefix) {
    if (prefix) {
      this.trackCliOption({
        option: "prefix",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMode(mode) {
    if (mode) {
      this.trackCliOption({
        option: "mode",
        value: mode
      });
    }
  }
};

// src/util/blob/token.ts
import { resolve as resolve2 } from "path";

// src/util/output/list-item.ts
var import_chalk11 = __toESM(require_source(), 1);
var listItem = (msg, n) => {
  if (!n) {
    n = "-";
  }
  if (Number(n)) {
    n += ".";
  }
  return `${(0, import_chalk11.default)(n.toString())} ${msg}`;
};
var list_item_default = listItem;

// src/util/blob/token.ts
function findFlagValue(argv, flag) {
  const eqPrefix = `${flag}=`;
  let result;
  for (let i = 0; i < argv.length; i++) {
    const arg = argv[i];
    if (arg === flag)
      result = argv[i + 1];
    else if (arg.startsWith(eqPrefix))
      result = arg.slice(eqPrefix.length);
  }
  return result;
}
var ErrorMessage = `No Vercel Blob credentials found. To fix this issue, choose one of the following options:
${list_item_default(`Pass the read-write token as an option: ${getCommandName("blob list --rw-token BLOB_READ_WRITE_TOKEN")}`, 1)}
${list_item_default(`Pass OIDC credentials as options: ${getCommandName("blob list --oidc-token VERCEL_OIDC_TOKEN --store-id BLOB_STORE_ID")}`, 2)}
${list_item_default(`Set OIDC credentials as environment variables: ${cmd(`VERCEL_OIDC_TOKEN=... BLOB_STORE_ID=... ${packageName} blob list`)}`, 3)}
${list_item_default(`Set the read-write token as an environment variable: ${cmd(`BLOB_READ_WRITE_TOKEN=BLOB_READ_WRITE_TOKEN ${packageName} blob list`)}`, 4)}
${list_item_default("Link your current folder to a Vercel Project that has a Vercel Blob store connected", 5)}`;
var PartialOidcFlagsErrorMessage = `--oidc-token and --store-id must be passed together. Pass both flags, or omit them and rely on environment variables / linked project credentials.`;
var PartialOidcEnvErrorMessage = `VERCEL_OIDC_TOKEN and BLOB_STORE_ID must both be set, or both be unset. Set both to use OIDC, unset both to use the read-write token, or pass --rw-token / --oidc-token + --store-id explicitly.`;
async function getBlobRWToken(client, argv) {
  const rwToken = findFlagValue(argv, "--rw-token");
  if (rwToken) {
    return { success: true, kind: "rw", token: rwToken };
  }
  const oidcTokenFlag = findFlagValue(argv, "--oidc-token");
  const storeIdFlag = findFlagValue(argv, "--store-id");
  if (oidcTokenFlag || storeIdFlag) {
    if (!oidcTokenFlag || !storeIdFlag) {
      return { success: false, error: PartialOidcFlagsErrorMessage };
    }
    return {
      success: true,
      kind: "oidc",
      oidcToken: oidcTokenFlag,
      storeId: storeIdFlag
    };
  }
  const fromProcess = resolveFromEnv({
    VERCEL_OIDC_TOKEN: process.env.VERCEL_OIDC_TOKEN,
    BLOB_STORE_ID: process.env.BLOB_STORE_ID,
    BLOB_READ_WRITE_TOKEN: process.env.BLOB_READ_WRITE_TOKEN
  });
  if (fromProcess)
    return fromProcess;
  let envFile = {};
  try {
    envFile = await createEnvObject(resolve2(client.cwd, ".env.local")) ?? {};
  } catch (_error) {
  }
  const fromFile = resolveFromEnv({
    VERCEL_OIDC_TOKEN: envFile.VERCEL_OIDC_TOKEN,
    BLOB_STORE_ID: envFile.BLOB_STORE_ID,
    BLOB_READ_WRITE_TOKEN: envFile.BLOB_READ_WRITE_TOKEN
  });
  if (fromFile)
    return fromFile;
  return { success: false, error: ErrorMessage };
}
function resolveFromEnv(env) {
  const { VERCEL_OIDC_TOKEN, BLOB_STORE_ID, BLOB_READ_WRITE_TOKEN } = env;
  if (Boolean(VERCEL_OIDC_TOKEN) !== Boolean(BLOB_STORE_ID)) {
    return { success: false, error: PartialOidcEnvErrorMessage };
  }
  if (VERCEL_OIDC_TOKEN && BLOB_STORE_ID) {
    return {
      success: true,
      kind: "oidc",
      oidcToken: VERCEL_OIDC_TOKEN,
      storeId: BLOB_STORE_ID
    };
  }
  if (BLOB_READ_WRITE_TOKEN) {
    return { success: true, kind: "rw", token: BLOB_READ_WRITE_TOKEN };
  }
  return null;
}
function blobOpts(auth) {
  if (auth.success && auth.kind === "rw") {
    return { token: auth.token };
  }
  if (auth.success && auth.kind === "oidc") {
    return { oidcToken: auth.oidcToken, storeId: auth.storeId };
  }
  return {};
}
function getStoreIdFromAuth(auth) {
  if (!auth.success)
    return null;
  if (auth.kind === "oidc") {
    return auth.storeId.startsWith("store_") ? auth.storeId : `store_${auth.storeId}`;
  }
  const [, , , id] = auth.token.split("_");
  return id ? `store_${id}` : null;
}

// src/commands/blob/list.ts
function isMode(mode) {
  return mode === "folded" || mode === "expanded";
}
async function list3(client, argv, auth) {
  const telemetryClient = new BlobListTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(listSubcommand18.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "blob list",
    args
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  const {
    "--limit": limit,
    "--cursor": cursor,
    "--prefix": prefix,
    "--mode": modeFlag
  } = flags;
  telemetryClient.trackCliOptionLimit(limit);
  telemetryClient.trackCliOptionCursor(cursor);
  telemetryClient.trackCliOptionPrefix(prefix);
  telemetryClient.trackCliOptionMode(modeFlag);
  const mode = modeFlag ?? "expanded";
  if (!isMode(mode)) {
    output_manager_default.error(
      `Invalid mode: ${mode} has to be either 'folded' or 'expanded'`
    );
    return 1;
  }
  let list15;
  try {
    output_manager_default.debug("Fetching blobs");
    output_manager_default.spinner("Fetching blobs");
    list15 = await blob.list({
      ...blobOpts(auth),
      limit: limit ?? 10,
      cursor,
      mode,
      prefix
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const headers = ["Uploaded At", "Size", "Pathname", "URL"];
  const urls = [];
  const tablePrint = table(
    [
      headers.map((header) => import_chalk12.default.dim(header)),
      ...list15.blobs.map((blob7) => {
        urls.push(blob7.url);
        const uploadedAt = (0, import_ms5.default)(Date.now() - new Date(blob7.uploadedAt).getTime());
        return [uploadedAt, String(blob7.size), blob7.pathname, blob7.url];
      })
    ],
    { hsep: 5 }
  ).replace(/^/gm, "  ");
  if (list15.blobs.length > 0) {
    output_manager_default.print(`
${tablePrint}

`);
  } else {
    output_manager_default.log("No blobs in this store");
  }
  if (list15.cursor) {
    const nextFlags = getCommandFlags(flags, ["_", "--cursor"]);
    output_manager_default.log(
      `To display the next page run ${getCommandName(
        `blob list${nextFlags} --cursor ${list15.cursor}`
      )}`
    );
  }
  return 0;
}

// src/util/telemetry/commands/blob/index.ts
var BlobTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandPut(actual) {
    this.trackCliSubcommand({
      subcommand: "put",
      value: actual
    });
  }
  trackCliSubcommandGet(actual) {
    this.trackCliSubcommand({
      subcommand: "get",
      value: actual
    });
  }
  trackCliSubcommandDel(actual) {
    this.trackCliSubcommand({
      subcommand: "del",
      value: actual
    });
  }
  trackCliSubcommandCopy(actual) {
    this.trackCliSubcommand({
      subcommand: "copy",
      value: actual
    });
  }
  trackCliSubcommandCreateStore(actual) {
    this.trackCliSubcommand({
      subcommand: "create-store",
      value: actual
    });
  }
  trackCliSubcommandDeleteStore(actual) {
    this.trackCliSubcommand({
      subcommand: "delete-store",
      value: actual
    });
  }
  trackCliSubcommandGetStore(actual) {
    this.trackCliSubcommand({
      subcommand: "get-store",
      value: actual
    });
  }
  trackCliSubcommandListStores(actual) {
    this.trackCliSubcommand({
      subcommand: "list-stores",
      value: actual
    });
  }
  trackCliSubcommandEmptyStore(actual) {
    this.trackCliSubcommand({
      subcommand: "empty-store",
      value: actual
    });
  }
  trackCliOptionRwToken(value) {
    if (value) {
      this.trackCliOption({
        option: "--rw-token",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionOidcToken(value) {
    if (value) {
      this.trackCliOption({
        option: "--oidc-token",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionStoreId(value) {
    if (value) {
      this.trackCliOption({
        option: "--store-id",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/blob/put.ts
import * as blob2 from "@vercel/blob";
var import_error_utils = __toESM(require_dist(), 1);
import { statSync } from "fs";
import { open as open2 } from "fs/promises";
import { basename } from "path";
var import_chalk13 = __toESM(require_source(), 1);

// src/util/telemetry/commands/blob/put.ts
var BlobPutTelemetryClient = class extends TelemetryClient {
  trackCliOptionAccess(value) {
    if (value) {
      this.trackCliOption({
        option: "access",
        value
      });
    }
  }
  trackCliArgumentPathToFile(pathToFile) {
    if (pathToFile) {
      this.trackCliArgument({
        arg: "pathToFile",
        value: this.redactedValue
      });
    }
  }
  trackCliInputSourceStdin() {
    this.trackCliArgument({
      arg: "pathToFile",
      value: "__vercel_stdin__"
    });
  }
  trackCliFlagAddRandomSuffix(addRandomSuffix) {
    if (addRandomSuffix) {
      this.trackCliFlag("add-random-suffix");
    }
  }
  trackCliOptionPathname(pathname) {
    if (pathname) {
      this.trackCliOption({
        option: "pathname",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagMultipart(multipart) {
    if (multipart) {
      this.trackCliFlag("multipart");
    }
  }
  trackCliOptionContentType(contentType) {
    if (contentType) {
      this.trackCliOption({
        option: "content-type",
        value: contentType
      });
    }
  }
  trackCliOptionCacheControlMaxAge(cacheControlMaxAge) {
    if (cacheControlMaxAge) {
      this.trackCliOption({
        option: "cache-control-max-age",
        value: String(cacheControlMaxAge)
      });
    }
  }
  trackCliFlagAllowOverwrite(allowOverwrite) {
    if (allowOverwrite) {
      this.trackCliFlag("allow-overwrite");
    }
  }
  trackCliOptionIfMatch(ifMatch) {
    if (ifMatch) {
      this.trackCliOption({
        option: "if-match",
        value: this.redactedValue
      });
    }
  }
};

// src/util/blob/access.ts
var VALID_ACCESS_VALUES = ["public", "private"];
function isAccess(value) {
  return VALID_ACCESS_VALUES.includes(value);
}
function parseAccessFlag(accessFlag) {
  if (accessFlag === void 0) {
    output_manager_default.error(
      "Missing required --access flag. Must be 'public' or 'private'."
    );
    return null;
  }
  if (!isAccess(accessFlag)) {
    output_manager_default.error(
      `Invalid access value: '${accessFlag}'. Must be 'public' or 'private'.`
    );
    return null;
  }
  return accessFlag;
}

// src/commands/blob/put.ts
async function put2(client, argv, auth) {
  const telemetryClient = new BlobPutTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(putSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    flags,
    args: [filePath]
  } = parsedArgs;
  const {
    "--access": accessFlag,
    "--add-random-suffix": addRandomSuffix,
    "--pathname": pathnameFlag,
    "--multipart": multipart,
    "--content-type": contentType,
    "--cache-control-max-age": cacheControlMaxAge,
    "--allow-overwrite": allowOverwrite,
    "--if-match": ifMatch
  } = flags;
  const access3 = parseAccessFlag(accessFlag);
  if (!access3)
    return 1;
  if (filePath) {
    telemetryClient.trackCliArgumentPathToFile(filePath);
  }
  telemetryClient.trackCliOptionAccess(accessFlag);
  telemetryClient.trackCliFlagAddRandomSuffix(addRandomSuffix);
  telemetryClient.trackCliOptionPathname(pathnameFlag);
  telemetryClient.trackCliFlagMultipart(multipart);
  telemetryClient.trackCliOptionContentType(contentType);
  telemetryClient.trackCliOptionCacheControlMaxAge(cacheControlMaxAge);
  telemetryClient.trackCliFlagAllowOverwrite(allowOverwrite);
  telemetryClient.trackCliOptionIfMatch(ifMatch);
  let putBody;
  let pathname;
  if (!filePath) {
    if (client.stdin.isTTY) {
      output_manager_default.error(
        `Missing input. Usage: ${import_chalk13.default.cyan(
          `${getCommandName("blob put <file>")}`
        )} or pipe data: ${import_chalk13.default.cyan("cat file.txt | vercel blob put --pathname <pathname>")}`
      );
      return 1;
    }
    if (!pathnameFlag) {
      output_manager_default.error(
        `Missing pathname. When reading from stdin, you must specify --pathname. Usage: ${import_chalk13.default.cyan(
          "cat file.txt | vercel blob put --pathname <pathname>"
        )}`
      );
      return 1;
    }
    putBody = process.stdin;
    pathname = pathnameFlag;
    telemetryClient.trackCliInputSourceStdin();
  } else {
    try {
      const stats = statSync(filePath);
      const isFile = stats.isFile();
      if (isFile) {
        const file = await open2(filePath, "r");
        putBody = file.createReadStream();
        pathname = pathnameFlag ?? basename(filePath);
      } else {
        output_manager_default.error("Path to upload is not a file");
        return 1;
      }
    } catch (err) {
      output_manager_default.debug(`Error reading file: ${err}`);
      if ((0, import_error_utils.isErrnoException)(err)) {
        output_manager_default.error(`File doesn't exist at '${filePath}'`);
        return 1;
      }
      output_manager_default.error("Error while reading file");
      return 1;
    }
  }
  if (!pathname || !putBody) {
    output_manager_default.error(
      `Missing pathname or input. Usage: ${import_chalk13.default.cyan(
        `${getCommandName("blob put <file>")}`
      )} or ${import_chalk13.default.cyan(
        `cat file.txt | ${getCommandName("blob put --pathname <pathname>")}`
      )}`
    );
    return 1;
  }
  let result;
  try {
    output_manager_default.debug("Uploading blob");
    output_manager_default.spinner("Uploading blob");
    result = await blob2.put(pathname, putBody, {
      ...blobOpts(auth),
      access: access3,
      addRandomSuffix: addRandomSuffix ?? false,
      multipart: multipart ?? true,
      contentType,
      cacheControlMaxAge,
      allowOverwrite: allowOverwrite ?? false,
      ifMatch
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success(result.url);
  return 0;
}

// src/commands/blob/get.ts
import * as blob3 from "@vercel/blob";

// src/util/telemetry/commands/blob/get.ts
var BlobGetTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrPathname(value) {
    if (value) {
      this.trackCliArgument({
        arg: "urlOrPathname",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionAccess(value) {
    if (value) {
      this.trackCliOption({
        option: "access",
        value
      });
    }
  }
  trackCliOptionOutput(value) {
    if (value) {
      this.trackCliOption({
        option: "output",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionIfNoneMatch(ifNoneMatch) {
    if (ifNoneMatch) {
      this.trackCliOption({
        option: "if-none-match",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/blob/get.ts
var import_bytes = __toESM(require_bytes(), 1);
import { createWriteStream } from "fs";
import { Readable } from "stream";
import { pipeline } from "stream/promises";
async function get2(client, argv, auth) {
  const telemetryClient = new BlobGetTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(getSubcommand3.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    flags,
    args: [urlOrPathname]
  } = parsedArgs;
  const {
    "--access": accessFlag,
    "--output": outputPath,
    "--if-none-match": ifNoneMatch
  } = flags;
  if (!urlOrPathname) {
    output_manager_default.error(
      `Missing required argument. Usage: ${getCommandName("blob get <urlOrPathname>")}`
    );
    return 1;
  }
  const access3 = parseAccessFlag(accessFlag);
  if (!access3)
    return 1;
  telemetryClient.trackCliArgumentUrlOrPathname(urlOrPathname);
  telemetryClient.trackCliOptionAccess(accessFlag);
  telemetryClient.trackCliOptionOutput(outputPath);
  telemetryClient.trackCliOptionIfNoneMatch(ifNoneMatch);
  try {
    output_manager_default.debug("Downloading blob");
    if (outputPath) {
      output_manager_default.spinner("Downloading blob");
    }
    const result = await blob3.get(urlOrPathname, {
      ...blobOpts(auth),
      access: access3,
      ifNoneMatch
    });
    if (result && result.statusCode === 304) {
      output_manager_default.log("Not modified (304)");
      return 0;
    }
    if (!result || !result.stream) {
      output_manager_default.error(`Blob not found: ${urlOrPathname}`);
      return 1;
    }
    const nodeStream = Readable.fromWeb(result.stream);
    if (outputPath) {
      const writeStream = createWriteStream(outputPath);
      await pipeline(nodeStream, writeStream);
      output_manager_default.stopSpinner();
      const sizeInfo = result.blob.size ? ` (${(0, import_bytes.default)(result.blob.size)})` : "";
      const typeInfo = result.blob.contentType ? `, ${result.blob.contentType}` : "";
      output_manager_default.success(`Saved to ${outputPath}${sizeInfo}${typeInfo}`);
    } else {
      await pipeline(nodeStream, client.stdout, { end: false });
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/blob/del.ts
import * as blob4 from "@vercel/blob";

// src/util/telemetry/commands/blob/del.ts
var BlobDelTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlsOrPathnames(urlsOrPathnames) {
    if (urlsOrPathnames) {
      this.trackCliArgument({
        arg: "urlsOrPathnames",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionIfMatch(ifMatch) {
    if (ifMatch) {
      this.trackCliOption({
        option: "if-match",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/blob/del.ts
async function del2(client, argv, auth) {
  const telemetryClient = new BlobDelTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(delSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  if (!parsedArgs.args.length) {
    printError(
      `Missing required arguments: ${getCommandName("blob del urlOrPathname")}`
    );
    return 1;
  }
  const { args } = parsedArgs;
  const { "--if-match": ifMatch } = parsedArgs.flags;
  telemetryClient.trackCliArgumentUrlsOrPathnames(args[0]);
  telemetryClient.trackCliOptionIfMatch(ifMatch);
  try {
    output_manager_default.debug("Deleting blob");
    output_manager_default.spinner("Deleting blob");
    await blob4.del(args, { ...blobOpts(auth), ifMatch });
  } catch (err) {
    output_manager_default.error(`Error deleting blob: ${err}`);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success("Blob deleted");
  return 0;
}

// src/commands/blob/copy.ts
import * as blob5 from "@vercel/blob";

// src/util/telemetry/commands/blob/copy.ts
var BlobCopyTelemetryClient = class extends TelemetryClient {
  trackCliOptionAccess(value) {
    if (value) {
      this.trackCliOption({
        option: "access",
        value
      });
    }
  }
  trackCliArgumentFromUrlOrPathname(value) {
    if (value) {
      this.trackCliArgument({
        arg: "fromUrlOrPathname",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentToPathname(value) {
    if (value) {
      this.trackCliArgument({
        arg: "toPathname",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagAddRandomSuffix(value) {
    if (value) {
      this.trackCliFlag("add-random-suffix");
    }
  }
  trackCliOptionContentType(value) {
    if (value) {
      this.trackCliOption({
        option: "content-type",
        value
      });
    }
  }
  trackCliOptionCacheControlMaxAge(value) {
    if (value) {
      this.trackCliOption({
        option: "cache-control-max-age",
        value: String(value)
      });
    }
  }
  trackCliOptionIfMatch(ifMatch) {
    if (ifMatch) {
      this.trackCliOption({
        option: "if-match",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/blob/copy.ts
async function copy2(client, argv, auth) {
  const telemetryClient = new BlobCopyTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(copySubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  if (!parsedArgs.args || parsedArgs.args.length < 2) {
    printError(
      `Missing required arguments: ${getCommandName(
        "blob copy fromUrlOrPathname toPathname"
      )}`
    );
    return 1;
  }
  const {
    args: [fromUrl, toPathname],
    flags: {
      "--access": accessFlag,
      "--add-random-suffix": addRandomSuffix,
      "--content-type": contentType,
      "--cache-control-max-age": cacheControlMaxAge,
      "--if-match": ifMatch
    }
  } = parsedArgs;
  const access3 = parseAccessFlag(accessFlag);
  if (!access3)
    return 1;
  telemetryClient.trackCliArgumentFromUrlOrPathname(fromUrl);
  telemetryClient.trackCliArgumentToPathname(toPathname);
  telemetryClient.trackCliOptionAccess(accessFlag);
  telemetryClient.trackCliFlagAddRandomSuffix(addRandomSuffix);
  telemetryClient.trackCliOptionContentType(contentType);
  telemetryClient.trackCliOptionCacheControlMaxAge(cacheControlMaxAge);
  telemetryClient.trackCliOptionIfMatch(ifMatch);
  let result;
  try {
    output_manager_default.debug("Copying blob");
    output_manager_default.spinner("Copying blob");
    result = await blob5.copy(fromUrl, toPathname, {
      ...blobOpts(auth),
      access: access3,
      addRandomSuffix: addRandomSuffix ?? false,
      contentType,
      cacheControlMaxAge,
      ifMatch
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success(result.url);
  return 0;
}

// src/util/integration-resource/connect-resource-to-project.ts
async function connectResourceToProject(client, projectId, storeId, environments, options) {
  return client.fetch(`/v1/storage/stores/${storeId}/connections`, {
    json: true,
    method: "POST",
    body: {
      envVarEnvironments: environments,
      projectId,
      type: "integration",
      ...options?.envVarPrefix !== void 0 ? { envVarPrefix: options.envVarPrefix } : {}
    },
    accountId: options?.accountId
  });
}

// src/commands/blob/store-add.ts
var import_chalk15 = __toESM(require_source(), 1);

// src/util/telemetry/commands/blob/store-add.ts
var BlobAddStoreTelemetryClient = class extends TelemetryClient {
  trackCliOptionAccess(value) {
    if (value) {
      this.trackCliOption({
        option: "access",
        value
      });
    }
  }
  trackCliArgumentName(value) {
    if (value) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionRegion(value) {
    if (value) {
      this.trackCliOption({
        option: "region",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionEnvironment(value) {
    if (value) {
      this.trackCliOption({
        option: "environment",
        value: value.join(",")
      });
    }
  }
};

// src/util/integration/post-provision-setup.ts
var import_chalk14 = __toESM(require_source(), 1);
var VALID_ENVIRONMENTS = [
  "production",
  "preview",
  "development"
];
var ENV_PULL_FAILED_MESSAGE = "Failed to pull environment variables. You can run `vercel env pull` manually.";
function validateEnvironments(environments) {
  const invalid = environments.filter(
    (env) => !VALID_ENVIRONMENTS.includes(env)
  );
  if (invalid.length > 0) {
    return { valid: false, invalid };
  }
  return { valid: true };
}
async function postProvisionSetup(client, resourceName, resourceId, contextName, options = {}) {
  const dashboardUrl = options.integrationSlug && options.installationId ? `https://vercel.com/d/dashboard/integrations/${options.integrationSlug}/${options.installationId}/resources/${resourceId}` : `https://vercel.com/${contextName}/~/stores/integration/${resourceId}`;
  output_manager_default.log(
    indent_default(
      `Dashboard: ${output_manager_default.link(dashboardUrl, dashboardUrl, { fallback: false })}`,
      4
    )
  );
  const baseResult = {
    exitCode: 0,
    dashboardUrl,
    connected: false,
    environments: [],
    envPulled: false
  };
  if (options.noConnect) {
    return baseResult;
  }
  const linkedProject = await getLinkedProject(client);
  if (linkedProject.status === "error") {
    return { ...baseResult, exitCode: linkedProject.exitCode };
  }
  if (linkedProject.status === "not_linked") {
    return baseResult;
  }
  const { project } = linkedProject;
  const environments = [
    ...new Set(
      options.environments && options.environments.length > 0 ? options.environments : [...VALID_ENVIRONMENTS]
    )
  ];
  output_manager_default.debug(`Selected environments: ${JSON.stringify(environments)}`);
  output_manager_default.spinner(
    `Connecting ${import_chalk14.default.bold(resourceName)} to ${import_chalk14.default.bold(project.name)}...`
  );
  output_manager_default.debug(`Connecting resource ${resourceId} to project ${project.id}`);
  try {
    await connectResourceToProject(
      client,
      project.id,
      resourceId,
      environments,
      options.prefix ? { envVarPrefix: options.prefix } : void 0
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    options.onProjectConnectFailed?.(project.id, error);
    output_manager_default.error(`Failed to connect: ${error.message}`);
    return {
      ...baseResult,
      exitCode: 1,
      project: { id: project.id, name: project.name },
      environments,
      connectError: error.message
    };
  }
  output_manager_default.stopSpinner();
  output_manager_default.log(
    `${import_chalk14.default.bold(resourceName)} successfully connected to ${import_chalk14.default.bold(project.name)}`
  );
  options.onProjectConnected?.(project.id);
  let envPulled = false;
  if (!options.noEnvPull) {
    const pullExitCode = await pull(
      client,
      ["--yes"],
      "vercel-cli:integration:add"
    );
    if (pullExitCode !== 0) {
      output_manager_default.warn(ENV_PULL_FAILED_MESSAGE);
    } else {
      envPulled = true;
    }
  }
  return {
    ...baseResult,
    project: { id: project.id, name: project.name },
    connected: true,
    environments,
    envPulled
  };
}
async function getLinkedProjectField(client, noConnect, field = "name") {
  if (noConnect) {
    return { value: void 0 };
  }
  const linkedProject = await getLinkedProject(client);
  if (linkedProject.status === "error") {
    return { value: void 0, exitCode: linkedProject.exitCode };
  }
  if (linkedProject.status === "linked") {
    return { value: linkedProject.project[field] };
  }
  return { value: void 0 };
}

// src/commands/blob/store-add.ts
async function addStore(client, argv) {
  const telemetryClient = new BlobAddStoreTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    createStoreSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    args: [nameArg],
    flags
  } = parsedArgs;
  const yes = flags["--yes"] ?? false;
  const environmentFlags = flags["--environment"];
  if (environmentFlags?.length) {
    const envValidation = validateEnvironments(environmentFlags);
    if (!envValidation.valid) {
      output_manager_default.error(
        `Invalid environment value: ${envValidation.invalid.map((e2) => `"${e2}"`).join(", ")}. Must be one of: ${VALID_ENVIRONMENTS.join(", ")}`
      );
      return 1;
    }
  }
  let accessFlag = flags["--access"];
  if (!accessFlag && client.stdin.isTTY) {
    accessFlag = await client.input.select({
      message: "Choose the access type for the blob store",
      choices: [
        {
          name: "Private",
          value: "private",
          description: "For sensitive documents, user content, and apps with custom auth. https://vercel.com/docs/vercel-blob/private-storage"
        },
        {
          name: "Public",
          value: "public",
          description: "For images, videos, large media, and public assets. https://vercel.com/docs/vercel-blob/public-storage"
        }
      ]
    });
  }
  const access3 = parseAccessFlag(accessFlag);
  if (!access3)
    return 1;
  const region = flags["--region"] || "iad1";
  let name = nameArg;
  if (!name) {
    if (!client.stdin.isTTY) {
      output_manager_default.error("Missing required argument: name");
      return 1;
    }
    name = await client.input.text({
      message: "Enter a name for your blob store",
      validate: (value) => {
        if (value.length < 5) {
          return "Name must be at least 5 characters long";
        }
        return true;
      }
    });
  }
  telemetryClient.trackCliArgumentName(name);
  telemetryClient.trackCliOptionAccess(accessFlag);
  telemetryClient.trackCliOptionRegion(flags["--region"]);
  const link = await getLinkedProject(client);
  let storeId;
  let storeRegion;
  try {
    output_manager_default.debug("Creating new blob store");
    output_manager_default.spinner("Creating new blob store");
    const res = await client.fetch(
      "/v1/storage/stores/blob",
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ name, region, access: access3 }),
        accountId: link.status === "linked" ? link.org.id : void 0
      }
    );
    storeId = res.store.id;
    storeRegion = res.store.region;
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const regionInfo = storeRegion ? ` in ${storeRegion}` : "";
  output_manager_default.success(`Blob store created: ${name} (${storeId})${regionInfo}`);
  const docsUrl = access3 === "public" ? "https://vercel.com/docs/vercel-blob/public-storage" : "https://vercel.com/docs/vercel-blob/private-storage";
  output_manager_default.log(`Access: ${access3}. Learn more: ${output_manager_default.link(docsUrl, docsUrl)}`);
  if (link.status === "linked") {
    let shouldLink = yes;
    if (!shouldLink) {
      shouldLink = await client.input.confirm(
        `Would you like to link this blob store to ${link.project.name}?`,
        true
      );
    }
    if (shouldLink) {
      let environments;
      if (environmentFlags?.length) {
        environments = environmentFlags;
      } else if (yes) {
        environments = [...VALID_ENVIRONMENTS];
      } else {
        environments = await client.input.checkbox({
          message: "Select environments",
          choices: [
            { name: "Production", value: "production", checked: true },
            { name: "Preview", value: "preview", checked: true },
            { name: "Development", value: "development", checked: true }
          ]
        });
      }
      output_manager_default.spinner(
        `Connecting ${import_chalk15.default.bold(name)} to ${import_chalk15.default.bold(link.project.name)}...`
      );
      await connectResourceToProject(
        client,
        link.project.id,
        storeId,
        environments,
        { accountId: link.org.id }
      );
      output_manager_default.success(
        `Blob store ${import_chalk15.default.bold(name)} linked to ${import_chalk15.default.bold(
          link.project.name
        )}`
      );
      client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
      await envPullCommandLogic(
        client,
        ".env.local",
        true,
        "development",
        link,
        void 0,
        client.cwd,
        "vercel-cli:blob:store-add"
      );
    }
  }
  return 0;
}

// src/util/blob/confirm.ts
function formatStoreLabel(name, id) {
  return `${name} (${id})`;
}
function formatConnectedProjects(connections) {
  if (connections.length === 0) {
    return "";
  }
  const MAX_SHOWN = 2;
  const shown = connections.slice(0, MAX_SHOWN).map((c) => c.project.name).join(", ");
  const remaining = connections.length - MAX_SHOWN;
  if (remaining > 0) {
    return ` This store is connected to ${shown} and ${remaining} other project${remaining > 1 ? "s" : ""}.`;
  }
  return ` This store is connected to ${shown}.`;
}

// src/commands/blob/store-remove.ts
async function removeStore(client, argv, rwToken) {
  const flagsSpecification = getFlagsSpecification(
    deleteStoreSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    args: [storeIdArg],
    flags: { "--yes": yes }
  } = parsedArgs;
  let storeId = storeIdArg;
  if (!storeId) {
    storeId = getStoreIdFromAuth(rwToken) ?? void 0;
  }
  if (!storeId) {
    if (!client.stdin.isTTY) {
      output_manager_default.error("Missing required argument: storeId");
      return 1;
    }
    storeId = await client.input.text({
      message: "Enter the ID of the blob store you want to remove",
      validate: (value) => {
        if (value.length !== 22) {
          return "ID must be 22 characters long";
        }
        return true;
      }
    });
  }
  const link = await getLinkedProject(client);
  const accountId = link.status === "linked" ? link.org.id : void 0;
  try {
    const [store, connectionsResponse] = await Promise.all([
      client.fetch(
        `/v1/storage/stores/${storeId}`,
        {
          method: "GET",
          accountId
        }
      ),
      client.fetch(`/v1/storage/stores/${storeId}/connections`, {
        method: "GET",
        accountId
      })
    ]);
    const label = formatStoreLabel(store.store.name, store.store.id);
    const projectsInfo = formatConnectedProjects(
      connectionsResponse.connections
    );
    if (!yes) {
      if (!client.stdin.isTTY) {
        output_manager_default.error(
          "Confirmation required. Use --yes to skip confirmation in non-interactive environments."
        );
        return 1;
      }
      const res = await client.input.confirm(
        `Are you sure you want to remove ${label}?${projectsInfo} This action cannot be undone.`,
        false
      );
      if (!res) {
        output_manager_default.success("Blob store not removed");
        return 0;
      }
    }
    output_manager_default.debug("Deleting blob store");
    output_manager_default.spinner("Deleting blob store");
    await client.fetch(`/v1/storage/stores/${storeId}/connections`, {
      method: "DELETE",
      accountId
    });
    await client.fetch(
      `/v1/storage/stores/blob/${storeId}`,
      {
        method: "DELETE",
        accountId
      }
    );
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success("Blob store deleted");
  if (link.status === "linked") {
    client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
    await envPullCommandLogic(
      client,
      ".env.local",
      true,
      "development",
      link,
      void 0,
      client.cwd,
      "vercel-cli:blob:store-remove"
    );
  }
  return 0;
}

// src/util/telemetry/commands/blob/store-get.ts
var BlobGetStoreTelemetryClient = class extends TelemetryClient {
  trackCliArgumentStoreId(value) {
    if (value) {
      this.trackCliArgument({
        arg: "storeId",
        value
      });
    }
  }
};

// src/util/blob/format-store.ts
var import_bytes2 = __toESM(require_bytes(), 1);
var import_chalk16 = __toESM(require_source(), 1);
var import_format2 = __toESM(require_format(), 1);
function formatStoreDetails(store, teamSlug) {
  const dateTimeFormat = "MM/DD/YYYY HH:mm:ss.SS";
  const isPublic = store.access !== "private";
  const storeIdSuffix = store.id.replace("store_", "").toLowerCase();
  const accessDomain = isPublic ? "public" : "private";
  const billingState = store.billingState === "active" ? import_chalk16.default.green("Active") : import_chalk16.default.red("Inactive");
  const lines = [
    `Blob Store: ${import_chalk16.default.bold(store.name)} (${import_chalk16.default.dim(store.id)})`,
    `Billing State: ${billingState}`
  ];
  if (store.count !== void 0) {
    lines.push(`Blob Count: ${store.count.toLocaleString()}`);
  }
  lines.push(`Size: ${(0, import_bytes2.default)(store.size)}`);
  if (store.region) {
    lines.push(`Region: ${store.region}`);
  }
  lines.push(`Access: ${isPublic ? "Public" : "Private"}`);
  lines.push(
    `Base URL: ${storeIdSuffix}.${accessDomain}.blob.vercel-storage.com`
  );
  if (teamSlug) {
    const dashboardUrl = `https://vercel.com/${teamSlug}/~/stores/blob/${store.id}`;
    const link = output_manager_default.link(dashboardUrl, dashboardUrl);
    lines.push(`Dashboard: ${link || dashboardUrl}`);
  }
  lines.push(
    `Created At: ${(0, import_format2.default)(new Date(store.createdAt), dateTimeFormat)}`
  );
  lines.push(
    `Updated At: ${(0, import_format2.default)(new Date(store.updatedAt), dateTimeFormat)}`
  );
  return lines.join("\n") + "\n";
}

// src/commands/blob/store-get.ts
async function getStore(client, argv, rwToken) {
  const telemetryClient = new BlobGetStoreTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    getStoreInfoSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    args: [storeIdArg]
  } = parsedArgs;
  let storeId = storeIdArg;
  if (!storeId) {
    storeId = getStoreIdFromAuth(rwToken) ?? void 0;
  }
  if (!storeId) {
    if (!client.stdin.isTTY) {
      output_manager_default.error("Missing required argument: storeId");
      return 1;
    }
    storeId = await client.input.text({
      message: "Enter the ID of the blob store you want to get info about",
      validate: (value) => {
        if (value.length !== 22) {
          return "ID must be 22 characters long";
        }
        return true;
      }
    });
  }
  telemetryClient.trackCliArgumentStoreId(storeId);
  try {
    const link = await getLinkedProject(client);
    output_manager_default.debug("Getting blob store");
    output_manager_default.spinner("Getting blob store");
    const accountId = link.status === "linked" ? link.org.id : void 0;
    const store = await client.fetch(
      `/v1/storage/stores/${storeId}`,
      {
        method: "GET",
        accountId
      }
    );
    let teamSlug = link.status === "linked" ? link.org.slug : void 0;
    if (!teamSlug) {
      const { team } = await getScope(client);
      teamSlug = team?.slug;
    }
    output_manager_default.print(formatStoreDetails(store.store, teamSlug));
  } catch (err) {
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  return 0;
}

// src/commands/blob/store-list.ts
var import_bytes3 = __toESM(require_bytes(), 1);
var import_chalk17 = __toESM(require_source(), 1);
var import_ms6 = __toESM(require_ms(), 1);
var import_title = __toESM(require_lib2(), 1);

// src/util/telemetry/commands/blob/store-list.ts
var BlobListStoresTelemetryClient = class extends TelemetryClient {
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
  trackCliFlagJson(value) {
    if (value) {
      this.trackCliFlag("json");
    }
  }
  trackCliFlagNoProjects(value) {
    if (value) {
      this.trackCliFlag("no-projects");
    }
  }
};

// src/commands/blob/store-list.ts
async function listStores(client, argv) {
  const flagsSpecification = getFlagsSpecification(
    listStoresSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const showAll = parsedArgs.flags["--all"] ?? false;
  const jsonOutput = parsedArgs.flags["--json"] ?? false;
  const noProjects = parsedArgs.flags["--no-projects"] ?? false;
  const telemetryClient = new BlobListStoresTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetryClient.trackCliFlagAll(showAll || void 0);
  telemetryClient.trackCliFlagJson(jsonOutput || void 0);
  telemetryClient.trackCliFlagNoProjects(noProjects || void 0);
  try {
    let accountId;
    let teamSlug;
    let linkedProject;
    const dirLink = await getLinkFromDir(getVercelDirectory(client.cwd));
    if (dirLink) {
      accountId = dirLink.orgId;
      const project = await getProjectByNameOrId(
        client,
        dirLink.projectId,
        dirLink.orgId
      );
      if (project && !(project instanceof ProjectNotFound) && !showAll) {
        linkedProject = { id: project.id, name: project.name };
      }
    } else {
      const { team } = await getScope(client);
      if (!team) {
        output_manager_default.error("Team not found.");
        return 1;
      }
      accountId = team.id;
      teamSlug = team.slug;
    }
    output_manager_default.spinner("Fetching blob stores");
    const response = await client.fetch(
      "/v1/storage/stores",
      {
        method: "GET",
        accountId
      }
    );
    output_manager_default.stopSpinner();
    let stores = response.stores;
    if (linkedProject) {
      const projectId = linkedProject.id;
      stores = stores.filter(
        (store) => store.projectsMetadata?.some(
          (metadata) => metadata.projectId === projectId
        )
      );
    }
    stores = stores.filter((store) => !store.type || store.type === "blob");
    if (stores.length === 0) {
      if (jsonOutput) {
        client.stdout.write(`${JSON.stringify({ stores: [] }, null, 2)}
`);
        return 0;
      }
      if (linkedProject) {
        output_manager_default.log(
          `No blob stores connected to ${import_chalk17.default.bold(linkedProject.name)}. Use ${import_chalk17.default.cyan("--all")} to list all team stores.`
        );
      } else {
        output_manager_default.log("No blob stores found");
      }
      return 0;
    }
    if (jsonOutput) {
      outputJson(client, stores);
      return 0;
    }
    const header = linkedProject ? `Blob stores for project ${import_chalk17.default.bold(linkedProject.name)}:` : `Blob stores:`;
    output_manager_default.log(header);
    if (!client.stdin.isTTY || !client.stdout.isTTY) {
      output_manager_default.print(
        table(buildTableRows(stores, noProjects), { hsep: 3 }).replace(
          /^/gm,
          "  "
        ) + "\n\n"
      );
      return 0;
    }
    const choices = [
      ...stores.map((store) => ({
        name: `${store.name} (${import_chalk17.default.dim(store.id)})`,
        value: store.id
      })),
      { name: "Cancel", value: "" }
    ];
    const selected = await client.input.select({
      message: "Select a store to view details",
      choices,
      pageSize: 15
    });
    if (!selected) {
      return 0;
    }
    output_manager_default.spinner("Fetching store details");
    const storeResponse = await client.fetch(
      `/v1/storage/stores/${selected}`,
      {
        method: "GET",
        accountId
      }
    );
    output_manager_default.stopSpinner();
    output_manager_default.print("\n" + formatStoreDetails(storeResponse.store, teamSlug));
    return 0;
  } catch (err) {
    printError(err);
    return 1;
  }
}
function outputJson(client, stores) {
  const json = {
    stores: stores.map((store) => ({
      id: store.id,
      name: store.name,
      region: store.region,
      size: store.size,
      count: store.count,
      billingState: store.billingState,
      status: store.status,
      createdAt: store.createdAt,
      updatedAt: store.updatedAt,
      projects: (store.projectsMetadata ?? []).map((p) => ({
        id: p.projectId,
        name: p.name
      }))
    }))
  };
  client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
}
function buildTableRows(stores, noProjects) {
  const headers = noProjects ? ["Name", "ID", "Status", "Region", "Size", "Files", "Age"] : ["Name", "ID", "Status", "Region", "Size", "Files", "Projects", "Age"];
  const rows = stores.map((store) => {
    const age = store.createdAt !== void 0 ? (0, import_ms6.default)(Date.now() - store.createdAt) : "-";
    const baseRow = [
      store.name,
      import_chalk17.default.dim(store.id),
      formatStatus(store.billingState),
      store.region || "-",
      store.size !== void 0 ? (0, import_bytes3.default)(store.size) || "0B" : "-",
      formatCount(store.count)
    ];
    if (!noProjects) {
      baseRow.push(formatProjects(store.projectsMetadata ?? []));
    }
    baseRow.push(age);
    return baseRow;
  });
  return [headers.map((h) => import_chalk17.default.bold(import_chalk17.default.cyan(h))), ...rows];
}
function formatStatus(billingState) {
  if (!billingState) {
    return import_chalk17.default.gray("\u2013");
  }
  const CIRCLE = "\u25CF ";
  const statusText = (0, import_title.default)(billingState);
  if (billingState === "active") {
    return import_chalk17.default.green(CIRCLE) + statusText;
  }
  return import_chalk17.default.yellow(CIRCLE) + statusText;
}
function formatProjects(projects) {
  if (projects.length === 0) {
    return import_chalk17.default.gray("\u2013");
  }
  if (projects.length === 1) {
    return projects[0].name;
  }
  if (projects.length === 2) {
    return `${projects[0].name}, ${projects[1].name}`;
  }
  return `${projects[0].name}, ${projects[1].name} (+${projects.length - 2})`;
}
function formatCount(count) {
  if (count === void 0) {
    return "-";
  }
  if (count >= 1e3) {
    return `${(count / 1e3).toFixed(1)}k`;
  }
  return String(count);
}

// src/commands/blob/store-empty.ts
import * as blob6 from "@vercel/blob";

// src/util/telemetry/commands/blob/store-empty.ts
var BlobEmptyStoreTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(value) {
    if (value) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/blob/store-empty.ts
async function emptyStore(client, argv, auth) {
  const telemetryClient = new BlobEmptyStoreTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    emptyStoreSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    flags: { "--yes": yes }
  } = parsedArgs;
  telemetryClient.trackCliFlagYes(yes);
  const storeId = getStoreIdFromAuth(auth);
  if (!storeId) {
    printError(
      "Could not resolve a Blob store ID from the provided credentials."
    );
    return 1;
  }
  try {
    const link = await getLinkedProject(client);
    const accountId = link.status === "linked" ? link.org.id : void 0;
    const [storeResponse, connectionsResponse, initialList] = await Promise.all(
      [
        client.fetch(
          `/v1/storage/stores/${storeId}`,
          { method: "GET", accountId }
        ),
        client.fetch(`/v1/storage/stores/${storeId}/connections`, {
          method: "GET",
          accountId
        }),
        blob6.list({ ...blobOpts(auth), limit: 1 })
      ]
    );
    const { name } = storeResponse.store;
    const { connections } = connectionsResponse;
    if (initialList.blobs.length === 0) {
      output_manager_default.log("Store is already empty");
      return 0;
    }
    const label = formatStoreLabel(name, storeId);
    const projectsInfo = formatConnectedProjects(connections);
    const message = `Are you sure you want to delete all files in ${label}?${projectsInfo} This action cannot be undone.`;
    if (!yes) {
      if (!client.stdin.isTTY) {
        output_manager_default.error(
          "Missing --yes flag. This is a destructive operation, use --yes to confirm."
        );
        return 1;
      }
      const confirmed = await client.input.confirm(message, false);
      if (!confirmed) {
        output_manager_default.log("Canceled");
        return 0;
      }
    }
    let totalDeleted = 0;
    let hasMore = true;
    while (hasMore) {
      output_manager_default.spinner(`Deleting blobs... (${totalDeleted} deleted)`);
      const listResult = await blob6.list({
        ...blobOpts(auth),
        limit: 1e3
      });
      if (listResult.blobs.length === 0) {
        hasMore = false;
        break;
      }
      const urls = listResult.blobs.map((b) => b.url);
      await blob6.del(urls, { ...blobOpts(auth) });
      totalDeleted += urls.length;
    }
    output_manager_default.stopSpinner();
    output_manager_default.success(`All blobs deleted (${totalDeleted} total)`);
    return 0;
  } catch (err) {
    printError(err);
    return 1;
  }
}

// src/commands/blob/index.ts
var COMMAND_CONFIG6 = {
  list: getCommandAliases(listSubcommand18),
  put: getCommandAliases(putSubcommand),
  get: getCommandAliases(getSubcommand3),
  del: getCommandAliases(delSubcommand),
  copy: getCommandAliases(copySubcommand),
  "create-store": getCommandAliases(createStoreSubcommand),
  "delete-store": getCommandAliases(deleteStoreSubcommand),
  "get-store": getCommandAliases(getStoreInfoSubcommand),
  "list-stores": getCommandAliases(listStoresSubcommand),
  "empty-store": getCommandAliases(emptyStoreSubcommand)
};
async function main3(client) {
  const telemetry2 = new BlobTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(blobCommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG6
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("blob", subcommand);
    output_manager_default.print(help(blobCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: blobCommand, columns: client.stderr.columns })
    );
  }
  const token2 = await getBlobRWToken(client, client.argv);
  telemetry2.trackCliOptionRwToken(findFlagValue(client.argv, "--rw-token"));
  telemetry2.trackCliOptionOidcToken(findFlagValue(client.argv, "--oidc-token"));
  telemetry2.trackCliOptionStoreId(findFlagValue(client.argv, "--store-id"));
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(listSubcommand18);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return list3(client, args, token2);
    case "put":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(putSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandPut(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return put2(client, args, token2);
    case "get":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(getSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandGet(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return get2(client, args, token2);
    case "del":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(delSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDel(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return del2(client, args, token2);
    case "copy":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(copySubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandCopy(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return copy2(client, args, token2);
    case "create-store":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(createStoreSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandCreateStore(subcommandOriginal);
      return addStore(client, args);
    case "delete-store":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(deleteStoreSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDeleteStore(subcommandOriginal);
      return removeStore(client, args, token2);
    case "get-store":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(getStoreInfoSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandGetStore(subcommandOriginal);
      return getStore(client, args, token2);
    case "list-stores":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(listStoresSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandListStores(subcommandOriginal);
      return listStores(client, args);
    case "empty-store":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("blob", subcommandOriginal);
        printHelp(emptyStoreSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandEmptyStore(subcommandOriginal);
      if (!token2.success) {
        printError(token2.error);
        return 1;
      }
      return emptyStore(client, args, token2);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG6));
      output_manager_default.print(help(blobCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/buy/credits.ts
var import_chalk18 = __toESM(require_source(), 1);

// src/util/buy/create-purchase.ts
async function createPurchase(client, item) {
  return client.fetch("/v1/billing/buy?source=cli", {
    method: "POST",
    body: { item }
  });
}

// src/util/buy/handle-purchase-error.ts
var import_open2 = __toESM(require_open(), 1);
var import_error_utils2 = __toESM(require_dist(), 1);
var getBillingUrl = (teamSlug) => `https://vercel.com/${teamSlug}/~/settings/billing`;
function tryOpenBillingPage(teamSlug, openBrowser) {
  if (!openBrowser)
    return;
  const billingUrl = teamSlug ? getBillingUrl(teamSlug) : "https://vercel.com/dashboard";
  void (0, import_open2.default)(billingUrl).catch((err) => {
    output_manager_default.debug(`Failed to open browser: ${err}`);
  });
}
function handlePurchaseError(err, teamSlug, opts) {
  const openBrowser = opts?.openBrowser ?? false;
  if (isAPIError(err)) {
    if (err.code === "invalid_plan_iteration") {
      output_manager_default.error("Your team must be on the Flex plan to purchase add-ons.");
      return 1;
    }
    if (err.code === "missing_subscription") {
      output_manager_default.error(
        "Your team does not have an active subscription. Please contact support."
      );
      return 1;
    }
    if (err.code === "missing_stripe_customer") {
      const billingUrl = teamSlug ? getBillingUrl(teamSlug) : "https://vercel.com/dashboard";
      const dashboardHint = teamSlug ? ` Please add one: ${output_manager_default.link(billingUrl, billingUrl)}` : ` Please add one in the ${output_manager_default.link("Vercel dashboard", billingUrl)}.`;
      output_manager_default.error(
        `Your team does not have a payment method on file.${dashboardHint}`
      );
      tryOpenBillingPage(teamSlug, openBrowser);
      return 1;
    }
    if (err.code === "invalid_plan") {
      output_manager_default.error("Your team's current plan does not support this purchase.");
      return 1;
    }
    if (err.code === "already_on_plan") {
      output_manager_default.error(
        "Your team already has an active subscription for this plan."
      );
      return 1;
    }
    if (err.code === "invalid_status") {
      output_manager_default.error(
        "Your team is not in a state that allows plan changes. Please contact support."
      );
      return 1;
    }
    if (err.code === "forbidden") {
      output_manager_default.error(
        "You do not have permission to make purchases for this team. Please contact your account administrator."
      );
      return 1;
    }
    if (err.code === "purchase_create_failed" || err.code === "purchase_confirm_failed" || err.code === "purchase_complete_failed" || err.code === "purchase_create_hosted_failed") {
      if (err.status === 402) {
        output_manager_default.error(
          "Payment failed. Please check that your team has a valid payment method on file."
        );
        tryOpenBillingPage(teamSlug, openBrowser);
        return 1;
      }
      output_manager_default.error(
        "An error occurred while processing your purchase. Please try again later."
      );
      output_manager_default.debug(`Error code: ${err.code}`);
      return 1;
    }
    if (err.status === 402 || err.code === "payment_failed") {
      output_manager_default.error(
        "Payment failed. Please check the payment method on file for your team."
      );
      tryOpenBillingPage(teamSlug, openBrowser);
      return 1;
    }
  }
  output_manager_default.error(
    "An unexpected error occurred while completing your purchase. Please try again later."
  );
  output_manager_default.debug(`Server response: ${(0, import_error_utils2.errorToString)(err)}`);
  return 1;
}

// src/commands/buy/credits.ts
var MAX_CREDIT_PURCHASE_AMOUNT = 1e3;
async function credits(client, argv) {
  const flagsSpecification = getFlagsSpecification(creditsSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const { args } = parsedArgs;
  const [creditType, amountStr] = args;
  if (!creditType) {
    output_manager_default.error(
      `Missing credit type. Supported types: ${SUPPORTED_CREDIT_TYPES.join(", ")}`
    );
    output_manager_default.log(`Run ${getCommandName("buy credits --help")} for usage.`);
    return 1;
  }
  if (!SUPPORTED_CREDIT_TYPES.includes(creditType)) {
    output_manager_default.error(
      `Invalid credit type "${creditType}". Supported types: ${SUPPORTED_CREDIT_TYPES.join(", ")}`
    );
    return 1;
  }
  if (!amountStr) {
    output_manager_default.error("Missing amount. Please specify the amount in dollars.");
    output_manager_default.log(`Run ${getCommandName("buy credits --help")} for usage.`);
    return 1;
  }
  const amount = Number(amountStr);
  if (!Number.isInteger(amount)) {
    output_manager_default.error(
      `Invalid amount "${amountStr}". Please specify a whole number (in dollars).`
    );
    return 1;
  }
  if (amount <= 0) {
    output_manager_default.error(
      `Invalid amount "${amountStr}". Please specify a positive amount in dollars.`
    );
    return 1;
  }
  if (amount > MAX_CREDIT_PURCHASE_AMOUNT) {
    output_manager_default.error(
      `Amount cannot exceed $${MAX_CREDIT_PURCHASE_AMOUNT.toLocaleString()} per purchase via CLI.`
    );
    return 1;
  }
  const { team, contextName } = await getScope(client);
  if (!team) {
    output_manager_default.error(
      "This command must be run with a team scope. Use --scope to specify a team."
    );
    return 1;
  }
  const typedCreditType = creditType;
  const label = CREDIT_TYPE_LABELS[typedCreditType];
  const yes = parsedArgs.flags["--yes"];
  if (!yes) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Confirmation required. Use --yes to skip the confirmation prompt in non-interactive mode."
      );
      return 1;
    }
    if (!await client.input.confirm(
      `Purchase ${import_chalk18.default.bold(`$${amount}`)} of ${label} credits for team ${import_chalk18.default.bold(contextName)}?`,
      false
    )) {
      return 0;
    }
  }
  const purchaseStamp = stamp_default();
  output_manager_default.spinner("Processing purchase");
  try {
    const result = await createPurchase(client, {
      type: "credits",
      creditType: typedCreditType,
      amount
    });
    output_manager_default.stopSpinner();
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            creditType: typedCreditType,
            amount,
            team: contextName,
            purchaseIntent: result.purchaseIntent
          },
          null,
          2
        )}
`
      );
    } else {
      output_manager_default.success(
        `Purchased ${import_chalk18.default.bold(`$${amount}`)} of ${label} credits for ${import_chalk18.default.bold(contextName)} ${purchaseStamp()}`
      );
      if (result.purchaseIntent) {
        output_manager_default.debug(`Purchase intent: ${result.purchaseIntent.id}`);
      }
    }
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    return handlePurchaseError(err, contextName, {
      openBrowser: !!client.stdout.isTTY
    });
  }
}

// src/commands/buy/addon.ts
var import_chalk19 = __toESM(require_source(), 1);
async function addon(client, argv) {
  const flagsSpecification = getFlagsSpecification(addonSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const { args } = parsedArgs;
  const [addonName, quantityStr] = args;
  if (!addonName) {
    output_manager_default.error(
      `Missing addon name. Supported addons: ${SUPPORTED_ADDON_ALIASES.join(", ")}`
    );
    output_manager_default.log(`Run ${getCommandName("buy addon --help")} for usage.`);
    return 1;
  }
  if (!SUPPORTED_ADDON_ALIASES.includes(addonName)) {
    output_manager_default.error(
      `Invalid addon "${addonName}". Supported addons: ${SUPPORTED_ADDON_ALIASES.join(", ")}`
    );
    return 1;
  }
  if (!quantityStr) {
    output_manager_default.error("Missing quantity. Please specify the number of units.");
    output_manager_default.log(`Run ${getCommandName("buy addon --help")} for usage.`);
    return 1;
  }
  const quantity = Number(quantityStr);
  if (!Number.isInteger(quantity)) {
    output_manager_default.error(
      `Invalid quantity "${quantityStr}". Please specify a whole number.`
    );
    return 1;
  }
  if (quantity <= 0) {
    output_manager_default.error(
      `Invalid quantity "${quantityStr}". Please specify a positive number.`
    );
    return 1;
  }
  const { team, contextName } = await getScope(client);
  if (!team) {
    output_manager_default.error(
      "This command must be run with a team scope. Use --scope to specify a team."
    );
    return 1;
  }
  const typedAddonAlias = addonName;
  const label = ADDON_LABELS[typedAddonAlias];
  const yes = parsedArgs.flags["--yes"];
  if (!yes) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Confirmation required. Use --yes to skip the confirmation prompt in non-interactive mode."
      );
      return 1;
    }
    if (!await client.input.confirm(
      `Purchase ${import_chalk19.default.bold(quantity)} unit${quantity === 1 ? "" : "s"} of ${label} for team ${import_chalk19.default.bold(contextName)}?`,
      false
    )) {
      return 0;
    }
  }
  const purchaseStamp = stamp_default();
  output_manager_default.spinner("Processing purchase");
  try {
    const result = await createPurchase(client, {
      type: "addon",
      productAlias: typedAddonAlias,
      quantity
    });
    output_manager_default.stopSpinner();
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            productAlias: typedAddonAlias,
            quantity,
            team: contextName,
            subscriptionIntent: result.subscriptionIntent
          },
          null,
          2
        )}
`
      );
    } else {
      output_manager_default.success(
        `Purchased ${import_chalk19.default.bold(quantity)} unit${quantity === 1 ? "" : "s"} of ${label} for ${import_chalk19.default.bold(contextName)} ${purchaseStamp()}`
      );
      if (result.subscriptionIntent) {
        output_manager_default.debug(`Subscription intent: ${result.subscriptionIntent.id}`);
      }
    }
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    return handlePurchaseError(err, contextName, {
      openBrowser: !!client.stdout.isTTY
    });
  }
}

// src/commands/buy/pro.ts
var import_chalk20 = __toESM(require_source(), 1);
async function pro(client, argv) {
  const flagsSpecification = getFlagsSpecification(proSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const { team, contextName } = await getScope(client);
  if (!team) {
    output_manager_default.error(
      "This command must be run with a team scope. Use --scope to specify a team."
    );
    return 1;
  }
  const yes = parsedArgs.flags["--yes"];
  if (!yes) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Confirmation required. Use --yes to skip the confirmation prompt in non-interactive mode."
      );
      return 1;
    }
    if (!await client.input.confirm(
      `Upgrade team ${import_chalk20.default.bold(contextName)} to Vercel Pro?`,
      false
    )) {
      return 0;
    }
  }
  const purchaseStamp = stamp_default();
  output_manager_default.spinner("Processing purchase");
  try {
    const result = await createPurchase(client, {
      type: "subscription",
      planSlug: "pro"
    });
    output_manager_default.stopSpinner();
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            team: contextName,
            subscriptionIntent: result.subscriptionIntent
          },
          null,
          2
        )}
`
      );
    } else {
      output_manager_default.success(
        `Upgraded ${import_chalk20.default.bold(contextName)} to Vercel Pro ${purchaseStamp()}`
      );
      if (result.subscriptionIntent) {
        output_manager_default.debug(`Subscription intent: ${result.subscriptionIntent.id}`);
      }
    }
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    return handlePurchaseError(err, contextName, {
      openBrowser: !!client.stdout.isTTY
    });
  }
}

// src/commands/buy/v0.ts
async function v0(client, argv) {
  const flagsSpecification = getFlagsSpecification(v0Subcommand.options);
  try {
    parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { team, contextName } = await getScope(client);
  if (!team) {
    output_manager_default.error(
      "This command must be run with a team scope. Use --scope to specify a team."
    );
    return 1;
  }
  output_manager_default.log(`Purchasing v0 subscription for team ${contextName}...`);
  output_manager_default.error("v0 subscription purchase is not yet available via the CLI.");
  return 1;
}

// src/commands/domains/buy.ts
var import_chalk21 = __toESM(require_source(), 1);
var import_tldts3 = __toESM(require_cjs(), 1);
var import_error_utils3 = __toESM(require_dist(), 1);

// src/util/telemetry/commands/domains/buy.ts
var DomainsBuyTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(v) {
    if (v) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/buy.ts
var VERCEL_DOMAINS_URL = "https://vercel.com/dashboard/domains";
async function buy(client, argv) {
  const telemetry2 = new DomainsBuyTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(buySubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  const [domainName] = args;
  const skipConfirmation = !!process.env.CI;
  telemetry2.trackCliArgumentDomain(domainName);
  if (!domainName) {
    if (client.nonInteractive) {
      const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
      const cmd2 = getCommandNamePlain(
        `domains buy <domain> ${flags.join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: `Missing domain name. This command cannot be completed non-interactively without a domain. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "user must run interactively; agents must not purchase on behalf of a user"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing domain name. Run ${getCommandName(`domains --help`)}`
    );
    return 1;
  }
  if (client.nonInteractive) {
    const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
    const interactiveCmd = getCommandNamePlain(
      `domains buy ${domainName} ${flags.filter((f) => f !== "--non-interactive").join(" ")}`.trim()
    );
    outputAgentError(
      client,
      {
        status: "error",
        reason: "purchase_requires_user",
        message: "Domain purchase cannot be performed non-interactively. Agents must not purchase domains on behalf of a user. The user must run this command interactively in a terminal to confirm price, auto-renew, and provide registrant contact details. Alternatively the user can complete purchase in the Vercel dashboard.",
        next: [
          {
            command: openUrlInBrowserCommand(VERCEL_DOMAINS_URL),
            when: "user opens Domains in the browser to purchase"
          },
          {
            command: interactiveCmd,
            when: "user runs this command interactively (remove --non-interactive)"
          }
        ]
      },
      1
    );
    return 1;
  }
  const { contextName } = await getScope(client);
  const parsedDomain = (0, import_tldts3.parse)(domainName);
  const { domain: rootDomain, subdomain } = parsedDomain;
  if (subdomain || !rootDomain) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_domain",
          message: `Invalid domain name '${domainName}'. Use a registrable root domain (no subdomain).`
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid domain name "${domainName}". Run ${getCommandName(
        `domains --help`
      )}`
    );
    return 1;
  }
  const availableStamp = stamp_default();
  const [domainPrice, domainStatus] = await Promise.all([
    getDomainPrice(client, domainName),
    getDomainStatus(client, domainName)
  ]);
  if (domainPrice instanceof Error) {
    output_manager_default.prettyError(domainPrice);
    return 1;
  }
  if (!domainStatus.available) {
    output_manager_default.error(
      `The domain ${param(domainName)} is ${import_chalk21.default.underline(
        "unavailable"
      )}! ${availableStamp()}`
    );
    return 1;
  }
  const { years, purchasePrice, renewalPrice } = domainPrice;
  if (purchasePrice === null || renewalPrice === null) {
    output_manager_default.error("Domain price not found");
    return 1;
  }
  output_manager_default.log(
    `The domain ${param(domainName)} is ${import_chalk21.default.underline(
      "available"
    )} to buy under ${import_chalk21.default.bold(contextName)}! ${availableStamp()}`
  );
  if (skipConfirmation) {
    output_manager_default.error(
      "Domain purchase in CI mode is not supported. Please run this command interactively to provide contact information."
    );
    return 1;
  }
  if (!await client.input.confirm(
    `Buy now for ${import_chalk21.default.bold(`$${purchasePrice}`)} (${`${years}yr${years > 1 ? "s" : ""}`})?`,
    false
  )) {
    return 0;
  }
  const autoRenew = await client.input.confirm(
    years === 1 ? `Auto renew yearly for ${import_chalk21.default.bold(`$${renewalPrice}`)}?` : `Auto renew every ${years} years for ${import_chalk21.default.bold(
      `$${renewalPrice}`
    )}?`,
    true
  );
  const contactInformation = await collectContactInformation(client);
  let buyResult;
  const purchaseStamp = stamp_default();
  output_manager_default.spinner("Purchasing");
  try {
    buyResult = await purchaseDomain(
      client,
      domainName,
      purchasePrice,
      years,
      autoRenew,
      contactInformation
    );
  } catch (err) {
    output_manager_default.error(
      "An unexpected error occurred while purchasing your domain. Please try again later."
    );
    output_manager_default.debug(`Server response: ${(0, import_error_utils3.errorToString)(err)}`);
    return 1;
  }
  output_manager_default.stopSpinner();
  if (buyResult instanceof UnsupportedTLD) {
    output_manager_default.error(
      `The TLD for domain name ${buyResult.meta.domain} is not supported.`
    );
    return 1;
  }
  if (buyResult instanceof TLDNotSupportedViaCLI) {
    output_manager_default.error(
      `Purchased for the TLD for domain name ${buyResult.meta.domain} are not supported via the CLI. Use the REST API or the dashboard to purchase.`
    );
    return 1;
  }
  if (buyResult instanceof InvalidDomain) {
    output_manager_default.error(`The domain ${buyResult.meta.domain} is not valid.`);
    return 1;
  }
  if (buyResult instanceof DomainNotAvailable) {
    output_manager_default.error(`The domain ${buyResult.meta.domain} is not available.`);
    return 1;
  }
  if (buyResult instanceof UnexpectedDomainPurchaseError) {
    output_manager_default.error(`An unexpected error happened while performing the purchase.`);
    return 1;
  }
  if (buyResult instanceof DomainPaymentError) {
    output_manager_default.error(`Your card was declined.`);
    return 1;
  }
  output_manager_default.success(`Domain ${param(domainName)} purchased ${purchaseStamp()}`);
  output_manager_default.note(
    `You may now use your domain as an alias to your deployments. Run ${getCommandName(
      `alias --help`
    )}`
  );
  return 0;
}

// src/commands/buy/domain.ts
async function domain(client, argv) {
  return buy(client, argv);
}

// src/util/telemetry/commands/buy/index.ts
var BuyTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCredits(actual) {
    this.trackCliSubcommand({
      subcommand: "credits",
      value: actual
    });
  }
  trackCliSubcommandAddon(actual) {
    this.trackCliSubcommand({
      subcommand: "addon",
      value: actual
    });
  }
  trackCliSubcommandPro(actual) {
    this.trackCliSubcommand({
      subcommand: "pro",
      value: actual
    });
  }
  trackCliSubcommandV0(actual) {
    this.trackCliSubcommand({
      subcommand: "v0",
      value: actual
    });
  }
  trackCliSubcommandDomain(actual) {
    this.trackCliSubcommand({
      subcommand: "domain",
      value: actual
    });
  }
};

// src/commands/buy/index.ts
var COMMAND_CONFIG7 = {
  credits: getCommandAliases(creditsSubcommand),
  addon: getCommandAliases(addonSubcommand),
  pro: getCommandAliases(proSubcommand),
  v0: getCommandAliases(v0Subcommand),
  domain: getCommandAliases(domainSubcommand)
};
async function main4(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(buyCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new BuyTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG7
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("buy");
    output_manager_default.print(help(buyCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: buyCommand, columns: client.stderr.columns })
    );
    return 2;
  }
  if (!subcommand) {
    output_manager_default.print(help(buyCommand, { columns: client.stderr.columns }));
    return 2;
  }
  switch (subcommand) {
    case "credits":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("buy", subcommandOriginal);
        return printHelp(creditsSubcommand);
      }
      telemetry2.trackCliSubcommandCredits(subcommandOriginal);
      return credits(client, args);
    case "addon":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("buy", subcommandOriginal);
        return printHelp(addonSubcommand);
      }
      telemetry2.trackCliSubcommandAddon(subcommandOriginal);
      return addon(client, args);
    case "pro":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("buy", subcommandOriginal);
        return printHelp(proSubcommand);
      }
      telemetry2.trackCliSubcommandPro(subcommandOriginal);
      return pro(client, args);
    case "v0":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("buy", subcommandOriginal);
        return printHelp(v0Subcommand);
      }
      telemetry2.trackCliSubcommandV0(subcommandOriginal);
      return v0(client, args);
    case "domain":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("buy", subcommandOriginal);
        return printHelp(domainSubcommand);
      }
      telemetry2.trackCliSubcommandDomain(subcommandOriginal);
      return domain(client, args);
    default:
      output_manager_default.error(`Unknown subcommand: ${subcommand}`);
      output_manager_default.print(help(buyCommand, { columns: client.stderr.columns }));
      return 1;
  }
}

// src/util/telemetry/commands/cache/purge.ts
var CachePurgeTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionType(type) {
    if (type) {
      this.trackCliOption({
        option: "type",
        value: type
      });
    }
  }
};

// src/commands/cache/purge.ts
async function purge(client, argv) {
  const telemetry2 = new CachePurgeTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(purgeSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const link = await getLinkedProject(client);
  if (link.status === "not_linked") {
    output_manager_default.error(
      "No project linked. Run `vercel link` to link a project to this directory."
    );
    return 1;
  }
  if (link.status === "error") {
    return link.exitCode;
  }
  const { project, org } = link;
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  const yes = Boolean(parsedArgs.flags["--yes"]);
  telemetry2.trackCliFlagYes(yes);
  const type = parsedArgs.flags["--type"] || "all";
  telemetry2.trackCliOptionType(parsedArgs.flags["--type"]);
  const cacheTypeMap = {
    cdn: "the CDN cache",
    data: "the Data cache",
    all: "the CDN cache and Data cache"
  };
  const validTypes = Object.keys(cacheTypeMap);
  if (!validTypes.includes(type)) {
    output_manager_default.error(
      `Invalid cache type "${type}". Valid types are: ${validTypes.join(", ")}`
    );
    return 1;
  }
  const typeDesciption = cacheTypeMap[type];
  const msg = `You are about to purge ${typeDesciption} for project ${project.name}`;
  const query = new URLSearchParams({ projectIdOrName: project.id }).toString();
  if (!yes) {
    if (!process.stdin.isTTY) {
      output_manager_default.print(
        `${msg}. To continue, run ${getCommandName("cache purge --yes")}.`
      );
      return 1;
    }
    const confirmed = await client.input.confirm(`${msg}. Continue?`, true);
    if (!confirmed) {
      output_manager_default.print(`Canceled.
`);
      return 0;
    }
  }
  const requests = [];
  if (type === "cdn" || type === "all") {
    requests.push(
      client.fetch(`/v1/edge-cache/purge-all?${query}`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        }
      })
    );
  }
  if (type === "data" || type === "all") {
    requests.push(
      client.fetch(`/v1/data-cache/purge-all?${query}`, {
        method: "DELETE",
        headers: {
          "Content-Type": "application/json"
        }
      })
    );
  }
  await Promise.all(requests);
  output_manager_default.print(
    prependEmoji(`Successfully purged ${typeDesciption}`, emoji("success")) + `
`
  );
  return 0;
}

// src/util/telemetry/commands/cache/invalidate.ts
var CacheInvalidateTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionTag(tag) {
    if (tag) {
      this.trackCliOption({
        option: "tag",
        value: tag
      });
    }
  }
  trackCliOptionSrcimg(srcimg) {
    if (srcimg) {
      this.trackCliOption({
        option: "srcimg",
        value: srcimg
      });
    }
  }
};

// src/commands/cache/invalidate.ts
var import_pluralize2 = __toESM(require_pluralize(), 1);
async function invalidate(client, argv) {
  const telemetry2 = new CacheInvalidateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    invalidateSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const link = await getLinkedProject(client);
  if (link.status === "not_linked") {
    output_manager_default.error(
      "No project linked. Run `vercel link` to link a project to this directory."
    );
    return 1;
  }
  if (link.status === "error") {
    return link.exitCode;
  }
  const { project, org } = link;
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  const yes = Boolean(parsedArgs.flags["--yes"]);
  const tag = parsedArgs.flags["--tag"];
  const srcimg = parsedArgs.flags["--srcimg"];
  telemetry2.trackCliFlagYes(yes);
  telemetry2.trackCliOptionTag(tag);
  telemetry2.trackCliOptionSrcimg(srcimg);
  if (tag && srcimg) {
    output_manager_default.error(`Cannot use both --tag and --srcimg options`);
    return 1;
  }
  let itemName = "";
  let itemValue = "";
  let flag = "";
  let postUrl = "";
  let postBody = {};
  if (tag) {
    itemName = (0, import_pluralize2.default)("tag", tag.split(",").length, false);
    itemValue = tag;
    flag = "--tag";
    postUrl = "/v1/edge-cache/invalidate-by-tags";
    postBody = { tags: tag };
  } else if (srcimg) {
    itemName = "source image";
    itemValue = srcimg;
    flag = "--srcimg";
    postUrl = "/v1/edge-cache/invalidate-by-src-images";
    postBody = { srcImages: [srcimg] };
  } else {
    output_manager_default.error(`The --tag or --srcimg option is required`);
    return 1;
  }
  const msg = `You are about to invalidate all cached content associated with ${itemName} ${itemValue} for project ${project.name}`;
  if (!yes) {
    if (!process.stdin.isTTY) {
      output_manager_default.print(
        `${msg}. To continue, run ${getCommandName(`cache invalidate ${flag} ${itemValue} --yes`)}.`
      );
      return 1;
    }
    const confirmed = await client.input.confirm(`${msg}. Continue?`, true);
    if (!confirmed) {
      output_manager_default.print(`Canceled.
`);
      return 0;
    }
  }
  await client.fetch(`${postUrl}?projectIdOrName=${project.id}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(postBody)
  });
  output_manager_default.print(
    prependEmoji(
      `Successfully invalidated all cached content associated with ${itemName} ${itemValue}`,
      emoji("success")
    ) + `
`
  );
  return 0;
}

// src/util/telemetry/commands/cache/dangerously-delete.ts
var CacheDangerouslyDeleteTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionTag(tag) {
    if (tag) {
      this.trackCliOption({
        option: "tag",
        value: tag
      });
    }
  }
  trackCliOptionSrcimg(srcimg) {
    if (srcimg) {
      this.trackCliOption({
        option: "srcimg",
        value: srcimg
      });
    }
  }
  trackCliOptionRevalidationDeadlineSeconds(seconds) {
    if (seconds) {
      this.trackCliOption({
        option: "revalidation-deadline-seconds",
        value: seconds?.toString()
      });
    }
  }
};

// src/commands/cache/dangerously-delete.ts
var import_pluralize3 = __toESM(require_pluralize(), 1);
async function dangerouslyDelete(client, argv) {
  const telemetry2 = new CacheDangerouslyDeleteTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    dangerouslyDeleteSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const link = await getLinkedProject(client);
  if (link.status === "not_linked") {
    output_manager_default.error(
      "No project linked. Run `vercel link` to link a project to this directory."
    );
    return 1;
  }
  if (link.status === "error") {
    return link.exitCode;
  }
  const { project, org } = link;
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  const yes = Boolean(parsedArgs.flags["--yes"]);
  const tag = parsedArgs.flags["--tag"];
  const srcimg = parsedArgs.flags["--srcimg"];
  const revalidate = parsedArgs.flags["--revalidation-deadline-seconds"];
  telemetry2.trackCliFlagYes(yes);
  telemetry2.trackCliOptionTag(tag);
  telemetry2.trackCliOptionSrcimg(srcimg);
  telemetry2.trackCliOptionRevalidationDeadlineSeconds(revalidate);
  if (tag && srcimg) {
    output_manager_default.error(`Cannot use both --tag and --srcimg options`);
    return 1;
  }
  let itemName = "";
  let itemValue = "";
  let flag = "";
  let postUrl = "";
  let postBody = {};
  if (tag) {
    itemName = (0, import_pluralize3.default)("tag", tag.split(",").length, false);
    itemValue = tag;
    flag = "--tag";
    postUrl = "/v1/edge-cache/dangerously-delete-by-tags";
    postBody = { tags: tag, revalidationDeadlineSeconds: revalidate };
  } else if (srcimg) {
    itemName = "source image";
    itemValue = srcimg;
    flag = "--srcimg";
    postUrl = "/v1/edge-cache/dangerously-delete-by-src-images";
    postBody = { srcImages: [srcimg], revalidationDeadlineSeconds: revalidate };
  } else {
    output_manager_default.error(`The --tag or --srcimg option is required`);
    return 1;
  }
  const msg = `You are about to dangerously delete all cached content associated with ${itemName} ${itemValue} for project ${project.name}`;
  if (!yes) {
    if (!process.stdin.isTTY) {
      const optional = typeof revalidate !== "undefined" ? ` --revalidation-deadline-seconds ${revalidate}` : "";
      output_manager_default.print(
        `${msg}. To continue, run ${getCommandName(`cache dangerously-delete ${flag} ${itemValue}${optional} --yes`)}.`
      );
      return 1;
    }
    const confirmed = await client.input.confirm(`${msg}. Continue?`, true);
    if (!confirmed) {
      output_manager_default.print(`Canceled.
`);
      return 0;
    }
  }
  await client.fetch(`${postUrl}?projectIdOrName=${project.id}`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(postBody)
  });
  output_manager_default.print(
    prependEmoji(
      `Successfully deleted all cached content associated with ${itemName} ${itemValue}`,
      emoji("success")
    ) + `
`
  );
  return 0;
}

// src/util/telemetry/commands/cache/index.ts
var CacheTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandPurge(actual) {
    this.trackCliSubcommand({
      subcommand: "purge",
      value: actual
    });
  }
  trackCliSubcommandInvalidate(actual) {
    this.trackCliSubcommand({
      subcommand: "invalidate",
      value: actual
    });
  }
  trackCliSubcommandDangerouslyDelete(actual) {
    this.trackCliSubcommand({
      subcommand: "dangerously-delete",
      value: actual
    });
  }
};

// src/commands/cache/index.ts
var COMMAND_CONFIG8 = {
  purge: getCommandAliases(purgeSubcommand),
  invalidate: getCommandAliases(invalidateSubcommand),
  "dangerously-delete": getCommandAliases(dangerouslyDeleteSubcommand)
};
async function main5(client) {
  const telemetry2 = new CacheTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(cacheCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG8
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp(cacheCommand.name);
    output_manager_default.print(help(cacheCommand, { columns: client.stderr.columns }));
    return 0;
  }
  function printHelp(command) {
    telemetry2.trackCliFlagHelp(command.name, subcommandOriginal);
    output_manager_default.print(
      help(command, { parent: cacheCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "purge":
      if (needHelp) {
        printHelp(purgeSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandPurge(subcommandOriginal);
      return purge(client, args);
    case "invalidate":
      if (needHelp) {
        printHelp(invalidateSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandInvalidate(subcommandOriginal);
      return invalidate(client, args);
    case "dangerously-delete":
      if (needHelp) {
        printHelp(dangerouslyDeleteSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDangerouslyDelete(subcommandOriginal);
      return dangerouslyDelete(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG8));
      output_manager_default.print(help(cacheCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/util/connex/validate-hex.ts
var HEX_COLOR_REGEX = /^#[0-9a-fA-F]{6}$/;
function isValidHexColor(value) {
  return HEX_COLOR_REGEX.test(value);
}
function validateHexColor(value, label) {
  if (value === void 0) {
    return;
  }
  if (!isValidHexColor(value)) {
    throw new Error(
      `Invalid ${label} "${value}". Expected 6-digit hex like #1A2B3C.`
    );
  }
}

// src/util/telemetry/commands/connex/index.ts
var ConnexTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCreate(actual) {
    this.trackCliSubcommand({
      subcommand: "create",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandToken(actual) {
    this.trackCliSubcommand({
      subcommand: "token",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandOpen(actual) {
    this.trackCliSubcommand({
      subcommand: "open",
      value: actual
    });
  }
  trackCliSubcommandAttach(actual) {
    this.trackCliSubcommand({
      subcommand: "attach",
      value: actual
    });
  }
  trackCliSubcommandDetach(actual) {
    this.trackCliSubcommand({
      subcommand: "detach",
      value: actual
    });
  }
  trackCliSubcommandUpdate(actual) {
    this.trackCliSubcommand({
      subcommand: "update",
      value: actual
    });
  }
  trackCliSubcommandRevokeTokens(actual) {
    this.trackCliSubcommand({
      subcommand: "revoke-tokens",
      value: actual
    });
  }
  trackCliArgumentClient(v) {
    if (v) {
      this.trackCliArgument({
        arg: "client",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentId(v) {
    if (v) {
      this.trackCliArgument({
        arg: "id",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionIcon(v) {
    if (v) {
      this.trackCliOption({
        option: "icon",
        // Path can leak username/repo location — redact.
        value: this.redactedValue
      });
    }
  }
  trackCliOptionBackgroundColor(v) {
    if (v) {
      this.trackCliOption({
        option: "background-color",
        // Hex colors are not sensitive; arbitrary text might be. Only emit
        // the raw value when it parses as #RRGGBB, otherwise redact so a
        // rejected value isn't sent verbatim before validation runs.
        value: isValidHexColor(v) ? v : this.redactedValue
      });
    }
  }
  trackCliOptionAccentColor(v) {
    if (v) {
      this.trackCliOption({
        option: "accent-color",
        value: isValidHexColor(v) ? v : this.redactedValue
      });
    }
  }
  trackCliOptionData(v) {
    if (v !== void 0) {
      this.trackCliOption({
        option: "data",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionConnectorType(v) {
    if (v) {
      this.trackCliOption({
        option: "connector-type",
        value: v
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
  trackCliFlagDisconnectAll(v) {
    if (v) {
      this.trackCliFlag("disconnect-all");
    }
  }
  trackCliFlagTriggers(v) {
    if (v) {
      this.trackCliFlag("triggers");
    }
  }
  trackCliOptionTriggerBranch(v) {
    if (v) {
      this.trackCliOption({
        option: "trigger-branch",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionTriggerPath(v) {
    if (v) {
      this.trackCliOption({
        option: "trigger-path",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagMyTokens(v) {
    if (v) {
      this.trackCliFlag("my-tokens");
    }
  }
  trackCliFlagAllTokens(v) {
    if (v) {
      this.trackCliFlag("all-tokens");
    }
  }
  trackCliFlagAllProjects(v) {
    if (v) {
      this.trackCliFlag("all-projects");
    }
  }
  trackCliOptionLimit(v) {
    if (v !== void 0) {
      this.trackCliOption({
        option: "limit",
        value: String(v)
      });
    }
  }
  trackCliOptionNext(v) {
    if (v) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSearch(v) {
    if (v) {
      this.trackCliOption({
        option: "search",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionService(v) {
    if (!v || v.length === 0) {
      return;
    }
    for (const raw of v) {
      for (const svc of raw.split(",")) {
        const trimmed = svc.trim();
        if (!trimmed) {
          continue;
        }
        this.trackCliOption({
          option: "service",
          value: this.redactedValue
        });
      }
    }
  }
  trackCliOptionType(v) {
    if (!v || v.length === 0) {
      return;
    }
    for (const raw of v) {
      for (const t of raw.split(",")) {
        const trimmed = t.trim();
        if (!trimmed) {
          continue;
        }
        this.trackCliOption({
          option: "type",
          value: trimmed
        });
      }
    }
  }
  trackCliOptionFormat(v) {
    if (v) {
      this.trackCliOption({
        option: "format",
        value: v
      });
    }
  }
  trackCliOptionEnvironment(v) {
    if (!v || v.length === 0) {
      return;
    }
    for (const raw of v) {
      for (const env of raw.split(",")) {
        const trimmed = env.trim();
        if (!trimmed) {
          continue;
        }
        this.trackCliOption({
          option: "environment",
          value: this.redactedTargetName(trimmed)
        });
      }
    }
  }
};

// src/commands/connex/create.ts
var import_open3 = __toESM(require_open(), 1);

// src/util/connex/select-team.ts
async function selectConnexTeam(client, message) {
  const scope = await getScope(client, { resolveLocalScope: true });
  if (scope.org.type === "team") {
    client.config.currentTeam = scope.org.id;
    return;
  }
  const org = await selectOrg(client, message, false);
  if (org.type !== "team") {
    throw new Error(
      "Connect requires a team. Re-run and select a team to continue."
    );
  }
  client.config.currentTeam = org.id;
  client.writeToConfigFile();
}

// src/util/connex/request-code.ts
import { randomBytes, createHash } from "crypto";
var POLL_INTERVAL_MS = 2e3;
var MAX_POLL_DURATION_MS = 30 * 60 * 1e3;
var MAX_EARLY_404_COUNT = 3;
function generateRequestCode() {
  const verifier = randomBytes(37).toString("base64url");
  const requestCode = createHash("sha256").update(verifier).digest("base64url");
  return { verifier, requestCode };
}
async function awaitConnexResult(client, verifier) {
  const deadline = Date.now() + MAX_POLL_DURATION_MS;
  let early404Count = 0;
  let lastProgress;
  while (Date.now() < deadline) {
    await sleep(POLL_INTERVAL_MS);
    try {
      const result = await client.fetch(
        `/v1/connect/result/${encodeURIComponent(verifier)}`
      );
      if (result.status === "success" && result.data) {
        return result.data;
      }
      if (result.status === "partial" && result.data) {
        if (result.progress && result.progress !== lastProgress) {
          lastProgress = result.progress;
          output_manager_default.stopSpinner();
          const clientId = result.data.clientId;
          if (clientId) {
            output_manager_default.log(`Connector created: ${clientId}`);
          }
          output_manager_default.spinner(`${result.progress}...`);
        }
        continue;
      }
      if (result.status === "error" && result.error) {
        output_manager_default.error(
          `Setup failed: ${result.error.message} (${result.error.code})`
        );
        return null;
      }
    } catch (err) {
      const status3 = err.status;
      if (status3 === 404) {
        early404Count++;
        if (early404Count > MAX_EARLY_404_COUNT) {
          output_manager_default.error("Setup request expired. Please try again.");
          return null;
        }
        output_manager_default.debug(
          `Polling 404 (${early404Count}/${MAX_EARLY_404_COUNT}), will retry`
        );
        continue;
      }
      output_manager_default.debug(`Polling error (will retry): ${err}`);
    }
  }
  output_manager_default.error("Timed out waiting for setup to complete. Please try again.");
  return null;
}

// src/util/connex/upload-icon.ts
import { readFile as readFile2, stat } from "fs/promises";
import { resolve as resolve3 } from "path";
import { createHash as createHash2 } from "crypto";
var MAX_ICON_BYTES = 5 * 1024 * 1024;
var MIN_IMAGE_BYTES = 12;
var IMAGE_SIGNATURES = [
  /^ffd8ffdb/,
  // JPEG raw
  /^ffd8ffe000104a4649460001/,
  // JFIF with marker
  /^ffd8ffee/,
  // Adobe JPEG
  /^ffd8ffe1.{4}457869660000/,
  // Exif: 2-byte segment length then "Exif\0\0"
  /^ffd8ffe0/,
  // JFIF (lenient)
  /^89504e470d0a1a0a/
  // PNG
];
function isImageBuffer(buf) {
  if (buf.length < MIN_IMAGE_BYTES) {
    return false;
  }
  const hex = buf.subarray(0, MIN_IMAGE_BYTES).toString("hex");
  return IMAGE_SIGNATURES.some((re) => re.test(hex));
}
async function prepareConnexIcon(filePath, cwd) {
  const absPath = resolve3(cwd, filePath);
  let size;
  try {
    size = (await stat(absPath)).size;
  } catch (err) {
    throw new Error(
      `Could not read icon file at "${filePath}": ${err.message}`
    );
  }
  if (size > MAX_ICON_BYTES) {
    throw new Error(
      `Icon file at "${filePath}" is ${size} bytes; maximum is ${MAX_ICON_BYTES} bytes (5 MB).`
    );
  }
  let buf;
  try {
    buf = await readFile2(absPath);
  } catch (err) {
    throw new Error(
      `Could not read icon file at "${filePath}": ${err.message}`
    );
  }
  if (!isImageBuffer(buf)) {
    throw new Error(`Icon file at "${filePath}" is not a PNG or JPEG.`);
  }
  const sha = createHash2("sha1").update(new Uint8Array(buf)).digest("hex");
  return { buf, sha };
}
async function uploadConnexIcon(client, prepared) {
  try {
    await client.fetch("/v2/files", {
      method: "POST",
      json: false,
      body: prepared.buf,
      headers: {
        "Content-Type": "application/octet-stream",
        "Content-Length": String(prepared.buf.byteLength),
        "x-now-digest": prepared.sha,
        "x-now-size": String(prepared.buf.byteLength)
      }
    });
  } catch (err) {
    const e2 = err;
    throw new Error(`Failed to upload icon: ${e2.serverMessage ?? e2.message}`);
  }
  return prepared.sha;
}

// src/commands/connex/create.ts
async function create2(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const serviceType = args[0];
  if (!serviceType) {
    output_manager_default.error("Missing service type. Usage: vercel connect create <type>");
    return 1;
  }
  const dataFlag = flags["--data"];
  const connectorType = flags["--connector-type"];
  if (connectorType && dataFlag === void 0) {
    output_manager_default.error("The --connector-type flag requires --data.");
    return 1;
  }
  const hasDataFlag = dataFlag !== void 0;
  const isDataFlagEmpty = hasDataFlag && dataFlag.trim().length === 0;
  let nonManagedData;
  if (hasDataFlag && !isDataFlagEmpty) {
    try {
      nonManagedData = parseDataFlag(dataFlag);
    } catch (err) {
      output_manager_default.error(err.message);
      return 1;
    }
  }
  const isNonManagedCreate = hasDataFlag;
  const iconFlag = flags["--icon"];
  const backgroundColor = flags["--background-color"];
  const accentColor = flags["--accent-color"];
  if (iconFlag !== void 0 && iconFlag.length === 0) {
    output_manager_default.error("Icon path cannot be empty.");
    return 1;
  }
  try {
    validateHexColor(backgroundColor, "background color");
    validateHexColor(accentColor, "accent color");
  } catch (err) {
    output_manager_default.error(err.message);
    return 1;
  }
  let preparedIcon;
  if (iconFlag) {
    try {
      preparedIcon = await prepareConnexIcon(iconFlag, client.cwd);
    } catch (err) {
      output_manager_default.error(err.message);
      return 1;
    }
  }
  await selectConnexTeam(
    client,
    "Select the team where you want to create this connector"
  );
  if (isDataFlagEmpty) {
    return await outputMissingDataError(client, serviceType, connectorType);
  }
  let name = flags["--name"];
  if (!name) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Missing required flag --name. In non-interactive mode, provide --name for the connector."
      );
      return 1;
    }
    name = await client.input.text({
      message: `What would you like to name your ${serviceType} app?`,
      validate: (val) => val.trim().length > 0 || "Name cannot be empty"
    });
  }
  let iconSha;
  if (preparedIcon) {
    try {
      output_manager_default.spinner("Uploading icon...");
      iconSha = await uploadConnexIcon(client, preparedIcon);
    } catch (err) {
      output_manager_default.stopSpinner();
      output_manager_default.error(err.message);
      return 1;
    }
    output_manager_default.stopSpinner();
  }
  const link = await getProjectLink(client, client.cwd);
  const body = {
    service: serviceType,
    name
  };
  if (link?.projectId) {
    body.projectId = link.projectId;
  }
  body.triggers = { enabled: flags["--triggers"] === true };
  if (iconSha) {
    body.icon = iconSha;
  }
  if (backgroundColor) {
    body.backgroundColor = backgroundColor;
  }
  if (accentColor) {
    body.accentColor = accentColor;
  }
  output_manager_default.spinner("Setting up...");
  let createdClient = null;
  let browserUrl;
  let verifier;
  if (isNonManagedCreate) {
    try {
      const resolvedConnectorType = connectorType ?? await discoverConnectorType(client, serviceType) ?? "oauth";
      body.data = nonManagedData;
      body.type = resolvedConnectorType;
      createdClient = await client.fetch(
        "/v1/connect/connectors",
        { method: "POST", body }
      );
    } catch (err) {
      const apiErr = err;
      if (apiErr.status === 404) {
        output_manager_default.stopSpinner();
        output_manager_default.error(
          "Connect is not enabled for this team. Contact support to enable it."
        );
        return 1;
      }
      output_manager_default.stopSpinner();
      printError(err);
      return 1;
    }
  } else {
    const request2 = generateRequestCode();
    verifier = request2.verifier;
    body.request_code = request2.requestCode;
    try {
      createdClient = await client.fetch(
        "/v1/connect/connectors/managed?autoinstall=true",
        { method: "POST", body }
      );
    } catch (err) {
      const apiErr = err;
      if (apiErr.status === 422 && apiErr.registerUrl) {
        browserUrl = apiErr.registerUrl;
      } else if (apiErr.status === 404) {
        output_manager_default.stopSpinner();
        output_manager_default.error(
          "Connect is not enabled for this team. Contact support to enable it."
        );
        return 1;
      } else {
        output_manager_default.stopSpinner();
        printError(err);
        return 1;
      }
    }
  }
  output_manager_default.stopSpinner();
  let hasBeenInstalled = false;
  let brandingPatchFailed = false;
  if (browserUrl) {
    const urlWithBranding = new URL(browserUrl);
    if (iconSha) {
      urlWithBranding.searchParams.set("icon", iconSha);
    }
    if (backgroundColor) {
      urlWithBranding.searchParams.set("backgroundColor", backgroundColor);
    }
    if (accentColor) {
      urlWithBranding.searchParams.set("accentColor", accentColor);
    }
    const finalBrowserUrl = urlWithBranding.toString();
    output_manager_default.log(`Opening browser for ${serviceType} app setup...`);
    output_manager_default.log(`If the browser doesn't open, visit:
${finalBrowserUrl}`);
    (0, import_open3.default)(finalBrowserUrl).catch(
      (err) => output_manager_default.debug(`Failed to open browser: ${err}`)
    );
    output_manager_default.spinner("Waiting for you to complete setup in the browser...");
    if (!verifier) {
      output_manager_default.stopSpinner();
      output_manager_default.error("Missing browser setup verifier.");
      return 1;
    }
    const resultFromBrowser = await awaitConnexResult(client, verifier);
    output_manager_default.stopSpinner();
    if (resultFromBrowser && "clientId" in resultFromBrowser && typeof resultFromBrowser.clientId === "string") {
      const clientId = resultFromBrowser.clientId;
      createdClient = await client.fetch(
        `/v1/connect/connectors/${encodeURIComponent(clientId)}`
      );
      const hasBranding = !!(iconSha || backgroundColor || accentColor);
      if (hasBranding) {
        const brandingBody = {};
        if (iconSha) {
          brandingBody.icon = iconSha;
        }
        if (backgroundColor) {
          brandingBody.backgroundColor = backgroundColor;
        }
        if (accentColor) {
          brandingBody.accentColor = accentColor;
        }
        try {
          output_manager_default.spinner("Applying branding...");
          createdClient = await client.fetch(
            `/v1/connect/connectors/${encodeURIComponent(clientId)}`,
            { method: "PATCH", body: brandingBody }
          );
        } catch (err) {
          output_manager_default.stopSpinner();
          output_manager_default.warn(
            `Failed to apply branding: ${err.message}. The connector was created but branding was not applied.`
          );
          brandingPatchFailed = true;
        }
        output_manager_default.stopSpinner();
      }
    }
    if (resultFromBrowser && "installationId" in resultFromBrowser && resultFromBrowser.installationId) {
      hasBeenInstalled = true;
    }
  }
  if (!createdClient) {
    return 1;
  }
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          id: createdClient.id,
          uid: createdClient.uid,
          type: createdClient.type,
          name: createdClient.name,
          supportedSubjectTypes: createdClient.supportedSubjectTypes,
          icon: createdClient.icon ?? null,
          backgroundColor: createdClient.backgroundColor ?? null,
          accentColor: createdClient.accentColor ?? null
        },
        null,
        2
      )}
`
    );
  } else if (hasBeenInstalled) {
    output_manager_default.success(
      `${serviceType} connector created and installed: ${createdClient.id} (UID ${createdClient.uid})`
    );
  } else {
    output_manager_default.success(
      `${serviceType} connector created: ${createdClient.id} (UID ${createdClient.uid})`
    );
  }
  return brandingPatchFailed ? 1 : 0;
}
function parseDataFlag(raw) {
  let parsed;
  try {
    parsed = JSON.parse(raw);
  } catch {
    throw new Error("Invalid JSON for --data. Expected a JSON object.");
  }
  if (parsed === null || typeof parsed !== "object" || Array.isArray(parsed)) {
    throw new Error("The --data value must be a JSON object.");
  }
  return parsed;
}
async function discoverConnectorType(client, service) {
  const serviceInfo = await fetchServiceInfo(client, service);
  return defaultConnectorType(serviceInfo);
}
async function outputMissingDataError(client, service, inputConnectorType) {
  const { connectorType, createInputSchema } = await resolveMissingDataSchemaInfo(client, service, inputConnectorType);
  let message = "--data requires a non-empty JSON object.";
  if (createInputSchema) {
    message += `

Expected --data schema for connector type "${connectorType}":
${JSON.stringify(
      createInputSchema,
      null,
      2
    )}`;
  }
  output_manager_default.error(message);
  return 1;
}
async function resolveMissingDataSchemaInfo(client, service, inputConnectorType) {
  let serviceInfo = await fetchServiceInfo(client, service);
  const connectorType = inputConnectorType ?? defaultConnectorType(serviceInfo) ?? "oauth";
  if (!serviceInfo) {
    serviceInfo = await fetchServiceInfo(client, inputConnectorType || "oauth");
  }
  return {
    connectorType,
    createInputSchema: createInputSchemaForType(serviceInfo, connectorType)
  };
}
async function fetchServiceInfo(client, service) {
  try {
    return await client.fetch(
      `/v1/connect/services/${encodeURIComponent(service)}?schemas=true`
    );
  } catch (err) {
    const apiErr = err;
    if (apiErr.status === 404) {
      return void 0;
    }
    throw err;
  }
}
function defaultConnectorType(serviceInfo) {
  const discoveredType = serviceInfo?.types?.[0]?.type;
  if (typeof discoveredType === "string" && discoveredType.length > 0) {
    return discoveredType;
  }
}
function createInputSchemaForType(serviceInfo, connectorType) {
  return serviceInfo?.types?.find((typeInfo) => typeInfo.type === connectorType)?.createInputSchema;
}

// src/commands/connex/update.ts
var import_chalk22 = __toESM(require_source(), 1);
async function update(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect update <id>"
    );
    return 1;
  }
  const iconFlag = flags["--icon"];
  const backgroundColor = flags["--background-color"];
  const accentColor = flags["--accent-color"];
  if (iconFlag !== void 0 && iconFlag.length === 0) {
    output_manager_default.error("Icon path cannot be empty.");
    return 1;
  }
  try {
    validateHexColor(backgroundColor, "background color");
    validateHexColor(accentColor, "accent color");
  } catch (err) {
    output_manager_default.error(err.message);
    return 1;
  }
  if (iconFlag === void 0 && backgroundColor === void 0 && accentColor === void 0) {
    output_manager_default.error(
      "Specify at least one of: --icon, --background-color, --accent-color."
    );
    return 1;
  }
  let preparedIcon;
  if (iconFlag) {
    try {
      preparedIcon = await prepareConnexIcon(iconFlag, client.cwd);
    } catch (err) {
      output_manager_default.error(err.message);
      return 1;
    }
  }
  await selectConnexTeam(client, "Select the team for this connector");
  if (preparedIcon) {
    try {
      output_manager_default.spinner("Verifying connector...");
      await client.fetch(
        `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
      );
    } catch (err) {
      output_manager_default.stopSpinner();
      const status3 = err.status;
      if (status3 === 404) {
        output_manager_default.error(`Connector not found: ${import_chalk22.default.bold(clientIdOrUid)}`);
      } else {
        output_manager_default.error(
          `Failed to verify ${import_chalk22.default.bold(clientIdOrUid)}: ${err.message}`
        );
      }
      return 1;
    }
    output_manager_default.stopSpinner();
  }
  let iconSha;
  if (preparedIcon) {
    try {
      output_manager_default.spinner("Uploading icon...");
      iconSha = await uploadConnexIcon(client, preparedIcon);
    } catch (err) {
      output_manager_default.stopSpinner();
      output_manager_default.error(err.message);
      return 1;
    }
    output_manager_default.stopSpinner();
  }
  const body = {};
  if (iconSha) {
    body.icon = iconSha;
  }
  if (backgroundColor) {
    body.backgroundColor = backgroundColor;
  }
  if (accentColor) {
    body.accentColor = accentColor;
  }
  let updated;
  try {
    output_manager_default.spinner("Updating connector...");
    updated = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`,
      { method: "PATCH", body }
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(`Connector not found: ${import_chalk22.default.bold(clientIdOrUid)}`);
      return 1;
    }
    output_manager_default.error(
      `Failed to update ${import_chalk22.default.bold(clientIdOrUid)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          id: updated.id,
          uid: updated.uid,
          type: updated.type,
          name: updated.name,
          icon: updated.icon ?? null,
          backgroundColor: updated.backgroundColor ?? null,
          accentColor: updated.accentColor ?? null
        },
        null,
        2
      )}
`
    );
  } else {
    const displayName = updated.uid || updated.id;
    output_manager_default.success(`Connector ${import_chalk22.default.bold(displayName)} updated.`);
  }
  return 0;
}

// src/commands/connex/list.ts
var import_chalk23 = __toESM(require_source(), 1);
var import_strip_ansi = __toESM(require_strip_ansi(), 1);
async function list5(client, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const allProjects = flags["--all-projects"] === true;
  const types = (flags["--type"] ?? []).flatMap((v) => v.split(",").map((s) => s.trim().toLowerCase())).filter(Boolean);
  const services = (flags["--service"] ?? []).flatMap((v) => v.split(",").map((s) => s.trim().toLowerCase())).filter(Boolean);
  const searchQuery = flags["--search"];
  let projectId;
  let projectName;
  if (!allProjects) {
    const linked = await getLinkedProject(client);
    if (linked.status === "error") {
      return linked.exitCode;
    }
    if (linked.status === "linked") {
      if (linked.org.type === "team") {
        client.config.currentTeam = linked.org.id;
      } else {
        client.config.currentTeam = void 0;
      }
      projectId = linked.project.id;
      projectName = linked.project.name;
    }
  }
  const unscoped = !projectId;
  if (unscoped) {
    await selectConnexTeam(
      client,
      "Select the team whose connectors you want to list"
    );
  }
  const params = new URLSearchParams();
  if (flags["--limit"] !== void 0) {
    params.set("limit", String(flags["--limit"]));
  }
  if (flags["--next"]) {
    params.set("cursor", flags["--next"]);
  }
  if (unscoped) {
    params.set("include", "projects");
  } else if (projectId) {
    params.set("projectId", projectId);
  }
  if (types.length > 0) {
    params.set("type", types.join(","));
  }
  if (services.length > 0) {
    params.set("service", services.join(","));
  }
  if (searchQuery) {
    params.set("search", searchQuery);
  }
  const query = params.toString();
  const url = `/v1/connect/connectors${query ? `?${query}` : ""}`;
  output_manager_default.spinner("Fetching connectors\u2026");
  let response;
  try {
    response = await client.fetch(url);
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(
        "Connect is not enabled for this team. Contact support to enable it."
      );
      return 1;
    }
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const clients = response.clients ?? [];
  if (asJson) {
    const jsonClients = clients.map((c) => {
      const item = {
        uid: c.uid,
        id: c.id,
        name: c.name,
        type: c.type,
        typeName: c.typeName,
        createdAt: c.createdAt,
        icon: c.icon ?? null,
        backgroundColor: c.backgroundColor ?? null,
        accentColor: c.accentColor ?? null
      };
      if (unscoped) {
        const projectsInclude = c.includes?.projects;
        item.projects = (projectsInclude?.items ?? []).map((p) => p.project).filter((p) => Boolean(p));
        item.hasMoreProjects = projectsInclude?.hasMore === true;
      }
      return item;
    });
    client.stdout.write(
      `${JSON.stringify({ connectors: jsonClients, cursor: response.cursor }, null, 2)}
`
    );
    return 0;
  }
  if (clients.length === 0) {
    if (unscoped) {
      output_manager_default.log(
        `No connectors found. Create one with \`${packageName} connect create <type>\`.`
      );
    } else {
      output_manager_default.log(
        `No connectors linked to ${import_chalk23.default.bold(projectName ?? "this project")}. Run \`${packageName} connect list --all-projects\` to see every connector in the team.`
      );
    }
    return 0;
  }
  if (!unscoped && projectName) {
    output_manager_default.log(`Connectors linked to ${import_chalk23.default.bold(projectName)}:`);
  }
  const headers = ["UID", "ID", "Name", "Type"];
  if (unscoped) {
    headers.push("Projects");
  }
  const rows = clients.map((c) => {
    const row = [
      (0, import_strip_ansi.default)(c.uid || "") || import_chalk23.default.gray("\u2013"),
      c.id,
      (0, import_strip_ansi.default)(c.name || "") || import_chalk23.default.gray("\u2013"),
      c.typeName || c.type
    ];
    if (unscoped) {
      const projectsInclude = c.includes?.projects;
      const names = (projectsInclude?.items ?? []).map((p) => p.project?.name).filter((n) => Boolean(n));
      const more = projectsInclude?.hasMore === true;
      let cell;
      if (names.length === 0 && !more) {
        cell = import_chalk23.default.gray("\u2013");
      } else {
        const parts = [];
        if (names.length)
          parts.push(names.join(", "));
        if (more)
          parts.push(import_chalk23.default.gray("+ more"));
        cell = parts.join(" ");
      }
      row.push(cell);
    }
    return row;
  });
  output_manager_default.print(
    `${table([headers.map((h) => import_chalk23.default.bold(import_chalk23.default.cyan(h))), ...rows], {
      hsep: 4
    })}
`
  );
  if (response.cursor) {
    const parts = [`${packageName} connect list`];
    if (allProjects)
      parts.push("--all-projects");
    for (const t of types)
      parts.push(`--type ${t}`);
    for (const s of services)
      parts.push(`--service ${s}`);
    if (searchQuery)
      parts.push(`--search "${searchQuery}"`);
    parts.push(`--next ${response.cursor}`);
    output_manager_default.log(`To see more, run \`${parts.join(" ")}\``);
  }
  return 0;
}

// src/commands/connex/token.ts
var import_open4 = __toESM(require_open(), 1);
async function token(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const clientId = args[0];
  if (!clientId) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect token <id>"
    );
    return 1;
  }
  const subject = flags["--subject"];
  if (subject && subject !== "app" && subject !== "user") {
    output_manager_default.error('Invalid --subject value. Must be "app" or "user".');
    return 1;
  }
  await selectConnexTeam(client, "Select the team for this token request");
  const tokenParams = {};
  if (flags["--installation-id"]) {
    tokenParams.installationId = flags["--installation-id"];
  }
  if (flags["--scopes"]) {
    tokenParams.scopes = parseScopes(flags["--scopes"]);
  }
  const body = { ...tokenParams };
  if (subject === "app") {
    body.subject = { type: "app" };
  } else if (subject === "user") {
    body.subject = { type: "user", id: client.authConfig.userId };
  }
  output_manager_default.spinner("Fetching token...");
  const result = await fetchToken(client, clientId, body);
  output_manager_default.stopSpinner();
  if (result.ok) {
    return printTokenResult(client, result.data, asJson);
  }
  const errorCode = result.errorCode;
  const errorMessage = result.errorMessage ?? "Failed to get token";
  if (errorCode === "not_found") {
    output_manager_default.error(
      "Connector not found or Connect is not enabled for this team."
    );
    return 1;
  }
  if (errorCode === "unresolved_token") {
    output_manager_default.error(
      `${errorMessage} This connector does not support getting a token for the requested subject.`
    );
    return 1;
  }
  if (!isActionable(errorCode)) {
    output_manager_default.error(errorMessage);
    return 1;
  }
  const teamId = client.config.currentTeam;
  if (!teamId) {
    output_manager_default.error(
      `${errorMessage} Unable to build recovery URL: no team resolved.`
    );
    return 1;
  }
  const actionLabel = errorCode === "user_authorization_required" ? "authorization" : "installation";
  const isInteractive = Boolean(client.stdin.isTTY && client.stdout.isTTY);
  const attemptRecovery = Boolean(flags["--yes"]) || isInteractive;
  if (!attemptRecovery) {
    const { requestCode: requestCode2 } = generateRequestCode();
    const actionUrl2 = buildActionUrl(
      errorCode,
      clientId,
      teamId,
      requestCode2,
      tokenParams
    );
    output_manager_default.error(errorMessage);
    output_manager_default.log(`To ${actionLabel}, open: ${actionUrl2}`);
    output_manager_default.log(
      `Or re-run with --yes to open the browser automatically: vercel connect token ${clientId} --yes`
    );
    return 1;
  }
  output_manager_default.error(errorMessage);
  if (!flags["--yes"] && isInteractive) {
    const confirmed = await client.input.confirm(
      `Open browser to ${actionLabel}?`,
      true
    );
    if (!confirmed) {
      return 0;
    }
  }
  const { verifier, requestCode } = generateRequestCode();
  const actionUrl = buildActionUrl(
    errorCode,
    clientId,
    teamId,
    requestCode,
    tokenParams
  );
  output_manager_default.log(`Opening browser for ${actionLabel}...`);
  output_manager_default.log(`If the browser doesn't open, visit:
${actionUrl}`);
  (0, import_open4.default)(actionUrl).catch(
    (err) => output_manager_default.debug(`Failed to open browser: ${err}`)
  );
  output_manager_default.spinner(`Waiting for ${actionLabel} to complete in the browser...`);
  const pollData = await awaitConnexResult(client, verifier);
  output_manager_default.stopSpinner();
  if (!pollData) {
    return 1;
  }
  const retryBody = { ...body };
  if (pollData.installationId && !retryBody.installationId) {
    retryBody.installationId = pollData.installationId;
  }
  output_manager_default.spinner("Fetching token...");
  const retryResult = await fetchToken(client, clientId, retryBody);
  output_manager_default.stopSpinner();
  if (retryResult.ok) {
    return printTokenResult(client, retryResult.data, asJson);
  }
  output_manager_default.error(
    retryResult.errorMessage ?? `Failed to get token after ${actionLabel}`
  );
  return 1;
}
function parseScopes(raw) {
  return raw.split(/[\s,]+/).map((s) => s.trim()).filter(Boolean);
}
function isActionable(code2) {
  return code2 === "user_authorization_required" || code2 === "client_installation_required";
}
function buildActionUrl(code2, clientId, teamId, requestCode, tokenParams) {
  const path3 = code2 === "user_authorization_required" ? "authorize" : "install";
  const params = new URLSearchParams({
    teamId,
    request_code: requestCode
  });
  for (const [key, value] of Object.entries(tokenParams)) {
    if (value === void 0) {
      continue;
    }
    params.set(key, Array.isArray(value) ? value.join(",") : value);
  }
  return `https://vercel.com/api/v1/connect/${path3}/${encodeURIComponent(clientId)}?${params.toString()}`;
}
function printTokenResult(client, data, asJson) {
  if (asJson) {
    client.stdout.write(`${JSON.stringify(data, null, 2)}
`);
  } else {
    client.stdout.write(`${data.token}
`);
  }
  return 0;
}
async function fetchToken(client, clientId, body) {
  try {
    const data = await client.fetch(
      `/v1/connect/token/${encodeURIComponent(clientId)}`,
      {
        method: "POST",
        body: JSON.stringify(body),
        headers: { "Content-Type": "application/json" }
      }
    );
    return { ok: true, data };
  } catch (err) {
    const serverError = extractApiError(err);
    return {
      ok: false,
      errorCode: serverError.code,
      errorMessage: serverError.message
    };
  }
}
function extractApiError(err) {
  if (typeof err === "object" && err !== null) {
    const errObj = err;
    const code2 = typeof errObj.code === "string" ? errObj.code : void 0;
    const message = typeof errObj.serverMessage === "string" ? errObj.serverMessage : typeof errObj.message === "string" ? errObj.message : "Unknown error";
    return { code: code2, message };
  }
  return { message: "Unknown error" };
}

// src/commands/connex/attach.ts
var import_chalk25 = __toESM(require_source(), 1);

// src/util/connex/trigger-destinations.ts
var import_chalk24 = __toESM(require_source(), 1);
var MAX_TRIGGER_DESTINATIONS = 3;
function destinationsMatch(a, b) {
  return a.projectId === b.projectId && (a.branch ?? null) === (b.branch ?? null) && (a.path ?? null) === (b.path ?? null);
}
function findMatchingDestination(destinations, desired) {
  return destinations.find((d) => destinationsMatch(d, desired));
}
function buildTriggerDestination(input) {
  const dest = { projectId: input.projectId };
  if (input.branch !== void 0) {
    dest.branch = input.branch;
  }
  if (input.path !== void 0) {
    dest.path = input.path;
  }
  return dest;
}
function formatDestination(d) {
  return [
    `project ${import_chalk24.default.bold(d.projectId)}`,
    `branch ${import_chalk24.default.bold(d.branch ?? "production")}`,
    `path ${import_chalk24.default.bold(d.path ?? "<default>")}`
  ].join(", ");
}
function toJsonDestination(d) {
  const entry = { projectId: d.projectId };
  if (d.branch !== void 0) {
    entry.branch = d.branch;
  }
  if (d.path !== void 0) {
    entry.path = d.path;
  }
  return entry;
}
async function patchTriggerDestinations(client, connectorId, destinations) {
  const body = destinations.map(toJsonDestination);
  await client.fetch(
    `/v1/connect/connectors/${encodeURIComponent(connectorId)}/trigger-destinations`,
    {
      method: "PATCH",
      body: { destinations: body }
    }
  );
}

// src/commands/connex/attach.ts
var ALL_ENVS = ["production", "preview", "development"];
function envSetsEqual(a, b) {
  if (a.length !== b.length) {
    return false;
  }
  const aSet = new Set(a);
  return b.every((env) => aSet.has(env));
}
async function attach(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!flags["--yes"];
  const withTriggers = !!flags["--triggers"];
  const triggerBranch = flags["--trigger-branch"];
  const triggerPath = flags["--trigger-path"];
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  if (!withTriggers && (triggerBranch || triggerPath)) {
    output_manager_default.error(
      "--trigger-branch and --trigger-path require --triggers to also be set."
    );
    return 1;
  }
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect attach <connector>"
    );
    return 1;
  }
  const requestedEnvsRaw = normalizeRepeatableStringFilters(
    flags["--environment"]
  );
  for (const env of requestedEnvsRaw) {
    if (!isValidEnvTarget(env)) {
      output_manager_default.error(
        `Invalid environment ${import_chalk25.default.bold(env)}. Allowed values: ${envTargetChoices.map((c) => c.value).join(", ")}.`
      );
      return 1;
    }
  }
  const environments = requestedEnvsRaw.length > 0 ? requestedEnvsRaw : [...ALL_ENVS];
  let projectId;
  let projectName;
  const projectFlag = flags["--project"];
  if (projectFlag) {
    await selectConnexTeam(client, "Select the team that owns this project");
    const team = client.config.currentTeam;
    output_manager_default.spinner("Looking up project\u2026");
    let resolvedProject;
    try {
      resolvedProject = await getProjectByNameOrId(client, projectFlag, team);
    } catch (err) {
      output_manager_default.stopSpinner();
      printError(err);
      return 1;
    }
    output_manager_default.stopSpinner();
    if (resolvedProject instanceof ProjectNotFound) {
      output_manager_default.error(
        `Project ${import_chalk25.default.bold(projectFlag)} was not found. Check the name/ID and try again.`
      );
      return 1;
    }
    projectId = resolvedProject.id;
    projectName = resolvedProject.name;
  } else {
    const linked = await getLinkedProject(client);
    if (linked.status === "error") {
      return linked.exitCode;
    }
    if (linked.status === "not_linked") {
      output_manager_default.error(
        `No linked project found. Run \`${packageName} link\` first or pass --project=<name_or_id>.`
      );
      return 1;
    }
    if (linked.org.type === "team") {
      client.config.currentTeam = linked.org.id;
    } else {
      client.config.currentTeam = void 0;
    }
    projectId = linked.project.id;
    projectName = linked.project.name;
  }
  output_manager_default.spinner("Retrieving connector\u2026");
  let target;
  try {
    target = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(`No connector found for ${import_chalk25.default.bold(clientIdOrUid)}.`);
      return 1;
    }
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const displayName = target.uid || target.name || target.id;
  let desiredDestination;
  let triggerAlreadyRegistered = false;
  let triggersEnabledOnConnector = true;
  if (withTriggers) {
    if (target.supportsTriggers === false) {
      output_manager_default.error(
        `Connector ${import_chalk25.default.bold(displayName)} does not support triggers (only Slack supports incoming webhooks today).`
      );
      return 1;
    }
    triggersEnabledOnConnector = target.triggers?.enabled === true;
    desiredDestination = buildTriggerDestination({
      projectId,
      branch: triggerBranch,
      path: triggerPath
    });
    const currentDestinations = target.triggerDestinations ?? [];
    triggerAlreadyRegistered = findMatchingDestination(currentDestinations, desiredDestination) !== void 0;
    if (!triggerAlreadyRegistered && currentDestinations.length >= MAX_TRIGGER_DESTINATIONS) {
      output_manager_default.error(
        `Connector ${import_chalk25.default.bold(displayName)} already has ${MAX_TRIGGER_DESTINATIONS} trigger destinations. Remove one in the dashboard before adding a new one.`
      );
      return 1;
    }
  }
  let existingAttachment;
  try {
    existingAttachment = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}/projects/${encodeURIComponent(projectId)}`
    );
  } catch (err) {
    const status3 = err.status;
    if (status3 !== 404) {
      printError(err);
      return 1;
    }
  }
  const attachmentMatches = existingAttachment !== void 0 && envSetsEqual(existingAttachment.environments ?? [], environments);
  const shouldAttach = !attachmentMatches;
  const shouldRegisterTrigger = withTriggers && !triggerAlreadyRegistered;
  if (!shouldAttach && !shouldRegisterTrigger) {
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            clientId: target.id,
            uid: target.uid,
            projectId,
            environments,
            triggerDestination: withTriggers ? desiredDestination : void 0,
            unchanged: true
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    const triggerPart = withTriggers ? ` (trigger destination already registered)` : "";
    output_manager_default.log(
      `Connector ${import_chalk25.default.bold(displayName)} is already attached to ${import_chalk25.default.bold(
        projectName
      )} for environments: ${environments.join(", ")}${triggerPart}. Nothing to do.`
    );
    return 0;
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation) {
    if (shouldAttach) {
      if (existingAttachment) {
        const current = (existingAttachment.environments ?? []).join(", ") || "\u2014";
        const next = environments.join(", ");
        output_manager_default.log(
          `Connector ${import_chalk25.default.bold(displayName)} is already attached to ${import_chalk25.default.bold(
            projectName
          )}.`
        );
        output_manager_default.log(`  Current:  ${current}`);
        output_manager_default.log(`  Will set: ${next}`);
      } else {
        output_manager_default.log(
          `Connector ${import_chalk25.default.bold(displayName)} will be attached to ${import_chalk25.default.bold(
            projectName
          )} for environments: ${environments.join(", ")}.`
        );
      }
    }
    if (shouldRegisterTrigger && desiredDestination) {
      output_manager_default.log(
        `Will register as trigger destination: ${formatDestination(desiredDestination)}.`
      );
      if (!triggersEnabledOnConnector) {
        output_manager_default.warn(
          `Triggers are not enabled on this connector. The destination will be registered, but no events will flow until the connector is recreated with triggers enabled.`
        );
      }
    }
    const confirmed = await client.input.confirm("Continue?", false);
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  if (shouldAttach) {
    output_manager_default.spinner("Attaching project\u2026");
    try {
      await client.fetch(
        `/v1/connect/connectors/${encodeURIComponent(target.id)}/projects/${encodeURIComponent(projectId)}`,
        {
          method: "POST",
          body: { environments }
        }
      );
    } catch (err) {
      output_manager_default.stopSpinner();
      const status3 = err.status;
      if (status3 === 403) {
        output_manager_default.error(
          `You don't have permission to attach projects on this team. Owner or Member role required.`
        );
        return 1;
      }
      if (status3 === 404) {
        output_manager_default.error(
          `No connector found for ${import_chalk25.default.bold(displayName)}, or project ${import_chalk25.default.bold(projectName)} is no longer accessible.`
        );
        return 1;
      }
      printError(err);
      return 1;
    }
    output_manager_default.stopSpinner();
  }
  if (shouldRegisterTrigger && desiredDestination) {
    const merged = [
      ...target.triggerDestinations ?? [],
      desiredDestination
    ];
    output_manager_default.spinner("Registering trigger destination\u2026");
    try {
      await patchTriggerDestinations(client, target.id, merged);
    } catch (err) {
      output_manager_default.stopSpinner();
      const status3 = err.status;
      if (status3 === 403) {
        output_manager_default.error(
          `You don't have permission to update trigger destinations on this team. Owner or Member role required.`
        );
        return 1;
      }
      if (status3 === 404) {
        output_manager_default.error(
          `Trigger destinations are not available for this team (the connex-triggers feature is not enabled).`
        );
        return 1;
      }
      printError(err);
      return 1;
    }
    output_manager_default.stopSpinner();
  }
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          clientId: target.id,
          uid: target.uid,
          projectId,
          environments,
          triggerDestination: withTriggers ? desiredDestination : void 0
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  if (shouldAttach) {
    output_manager_default.success(
      `Attached connector ${import_chalk25.default.bold(displayName)} to ${import_chalk25.default.bold(projectName)} for environments: ${environments.join(", ")}.`
    );
  }
  if (shouldRegisterTrigger && desiredDestination) {
    output_manager_default.success(
      `Registered ${import_chalk25.default.bold(projectName)} as a trigger destination (${formatDestination(desiredDestination)}).`
    );
  }
  return 0;
}

// src/commands/connex/detach.ts
var import_chalk26 = __toESM(require_source(), 1);
async function detach(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!flags["--yes"];
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect detach <connector>"
    );
    return 1;
  }
  let projectId;
  let projectName;
  const projectFlag = flags["--project"];
  if (projectFlag) {
    await selectConnexTeam(client, "Select the team that owns this project");
    const team = client.config.currentTeam;
    output_manager_default.spinner("Looking up project\u2026");
    let resolvedProject;
    try {
      resolvedProject = await getProjectByNameOrId(client, projectFlag, team);
    } catch (err) {
      output_manager_default.stopSpinner();
      printError(err);
      return 1;
    }
    output_manager_default.stopSpinner();
    if (resolvedProject instanceof ProjectNotFound) {
      output_manager_default.error(
        `Project ${import_chalk26.default.bold(projectFlag)} was not found. Check the name/ID and try again.`
      );
      return 1;
    }
    projectId = resolvedProject.id;
    projectName = resolvedProject.name;
  } else {
    const linked = await getLinkedProject(client);
    if (linked.status === "error") {
      return linked.exitCode;
    }
    if (linked.status === "not_linked") {
      output_manager_default.error(
        `No linked project found. Run \`${packageName} link\` first or pass --project=<name_or_id>.`
      );
      return 1;
    }
    if (linked.org.type === "team") {
      client.config.currentTeam = linked.org.id;
    } else {
      client.config.currentTeam = void 0;
    }
    projectId = linked.project.id;
    projectName = linked.project.name;
  }
  output_manager_default.spinner("Retrieving connector\u2026");
  let target;
  try {
    target = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(`No connector found for ${import_chalk26.default.bold(clientIdOrUid)}.`);
      return 1;
    }
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const displayName = target.uid || target.name || target.id;
  let existingAttachment;
  try {
    existingAttachment = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}/projects/${encodeURIComponent(projectId)}`
    );
  } catch (err) {
    const status3 = err.status;
    if (status3 !== 404) {
      printError(err);
      return 1;
    }
  }
  if (!existingAttachment) {
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            clientId: target.id,
            uid: target.uid,
            projectId,
            unchanged: true
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(
      `Connector ${import_chalk26.default.bold(displayName)} is not attached to ${import_chalk26.default.bold(
        projectName
      )}. Nothing to do.`
    );
    return 0;
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation) {
    const envs = (existingAttachment.environments ?? []).join(", ") || "\u2014";
    output_manager_default.log(
      `Connector ${import_chalk26.default.bold(displayName)} will be detached from ${import_chalk26.default.bold(
        projectName
      )}.`
    );
    output_manager_default.log(`  Environments: ${envs}`);
    const confirmed = await client.input.confirm("Continue?", false);
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  output_manager_default.spinner("Detaching project\u2026");
  try {
    await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}/projects/${encodeURIComponent(projectId)}`,
      { method: "DELETE" }
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 403) {
      output_manager_default.error(
        `You don't have permission to detach projects on this team. Owner or Member role required.`
      );
      return 1;
    }
    if (status3 === 404) {
      if (asJson) {
        client.stdout.write(
          `${JSON.stringify(
            {
              clientId: target.id,
              uid: target.uid,
              projectId,
              unchanged: true
            },
            null,
            2
          )}
`
        );
        return 0;
      }
      output_manager_default.log(
        `Connector ${import_chalk26.default.bold(displayName)} is not attached to ${import_chalk26.default.bold(
          projectName
        )}. Nothing to do.`
      );
      return 0;
    }
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          clientId: target.id,
          uid: target.uid,
          projectId,
          detached: true
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  output_manager_default.success(
    `Detached connector ${import_chalk26.default.bold(displayName)} from ${import_chalk26.default.bold(projectName)}.`
  );
  return 0;
}

// src/commands/connex/remove.ts
var import_chalk27 = __toESM(require_source(), 1);
async function remove(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!flags["--yes"];
  const disconnectAll = !!flags["--disconnect-all"];
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect remove <connector>"
    );
    return 1;
  }
  await selectConnexTeam(client, "Select the team for this connector");
  output_manager_default.spinner("Retrieving connector\u2026");
  let target;
  try {
    target = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(`No connector found for ${import_chalk27.default.bold(clientIdOrUid)}.`);
      return 1;
    }
    output_manager_default.error(
      `Failed to look up ${import_chalk27.default.bold(clientIdOrUid)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  const displayName = target.uid || target.id;
  let projectLinks;
  try {
    output_manager_default.spinner("Checking connected projects\u2026");
    const res = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}/projects`
    );
    projectLinks = res.projects ?? [];
  } catch (err) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `Failed to list connected projects for ${import_chalk27.default.bold(displayName)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  if (projectLinks.length > 0 && !disconnectAll) {
    const count = projectLinks.length;
    const plural14 = count === 1 ? "project" : "projects";
    output_manager_default.error(
      `Cannot delete connector ${import_chalk27.default.bold(displayName)} while it has ${count} connected ${plural14}. Please disconnect any projects using this connector first or use the \`--disconnect-all\` flag.`
    );
    return 1;
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation) {
    const cascadeNote = projectLinks.length > 0 ? ` ${projectLinks.length} connected ${projectLinks.length === 1 ? "project" : "projects"} will be disconnected.` : "";
    output_manager_default.log(
      `Connector ${import_chalk27.default.bold(displayName)} will be deleted permanently.${cascadeNote}`
    );
    const confirmed = await client.input.confirm(
      `${import_chalk27.default.red("Are you sure?")}`,
      false
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  try {
    output_manager_default.spinner("Deleting connector\u2026");
    await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}`,
      { method: "DELETE" }
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `A problem occurred when attempting to delete ${import_chalk27.default.bold(displayName)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify({ id: target.id, uid: target.uid, removed: true }, null, 2)}
`
    );
    return 0;
  }
  output_manager_default.success(`Connector ${import_chalk27.default.bold(displayName)} successfully removed.`);
  return 0;
}

// src/commands/connex/revoke-tokens.ts
var import_chalk28 = __toESM(require_source(), 1);
async function revokeTokens(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  if (asJson && !flags["--yes"]) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect revoke-tokens <connector>"
    );
    return 1;
  }
  const myTokens = !!flags["--my-tokens"];
  const allTokens = !!flags["--all-tokens"];
  if (myTokens && allTokens) {
    output_manager_default.error("--my-tokens and --all-tokens are mutually exclusive.");
    return 1;
  }
  if (flags["--yes"] && !myTokens && !allTokens) {
    output_manager_default.error(
      "--yes requires a scope flag. Use --yes --my-tokens or --yes --all-tokens."
    );
    return 1;
  }
  await selectConnexTeam(client, "Select the team for this connector");
  output_manager_default.spinner("Retrieving connector\u2026");
  let target;
  try {
    target = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(`No connector found for ${import_chalk28.default.bold(clientIdOrUid)}.`);
      return 1;
    }
    output_manager_default.error(
      `Failed to look up ${import_chalk28.default.bold(clientIdOrUid)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  const displayName = target.name || target.uid || target.id;
  const supportsRevocation = target.supportsRevocation !== false;
  let subjectScope;
  if (myTokens) {
    subjectScope = "mine";
  } else if (allTokens) {
    subjectScope = "all";
  } else if (client.stdin.isTTY) {
    const choice = await client.input.select({
      message: "Which tokens do you want to revoke?",
      choices: [
        {
          name: `My tokens \u2014 revoke only your own tokens for ${displayName}`,
          value: "mine"
        },
        {
          name: "All tokens \u2014 revoke every token for all users and installations",
          value: "all"
        }
      ]
    });
    subjectScope = choice;
  } else {
    output_manager_default.error(
      "Scope required. Use --my-tokens to revoke your tokens or --all-tokens to revoke all tokens."
    );
    return 1;
  }
  if (!flags["--yes"]) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Confirmation required. Use `--yes` to skip the confirmation prompt."
      );
      return 1;
    }
    if (subjectScope === "mine") {
      output_manager_default.log(
        `Tokens issued from ${import_chalk28.default.bold(displayName)} for your account will stop working. You'll need to re-authorize to use this connector again.`
      );
    } else {
      output_manager_default.log(
        `Every token issued from ${import_chalk28.default.bold(displayName)} will stop working. Anyone using this connector will need to re-authorize.`
      );
    }
    if (!supportsRevocation) {
      output_manager_default.warn(
        `${import_chalk28.default.bold(displayName)} does not support provider-side token revocation. Tokens will be removed from Vercel Connect but may remain valid at the provider until they expire.`
      );
    }
    const confirmed = await client.input.confirm(
      `${import_chalk28.default.red("Are you sure?")}`,
      false
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  const body = subjectScope === "mine" ? { subject: { type: "user", id: client.authConfig.userId } } : {};
  output_manager_default.spinner("Revoking tokens\u2026");
  let result;
  try {
    result = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(target.id)}/tokens`,
      {
        method: "DELETE",
        body: JSON.stringify(body),
        headers: { "Content-Type": "application/json" }
      }
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 403) {
      output_manager_default.error(
        `You don't have permission to revoke tokens for ${import_chalk28.default.bold(displayName)}.`
      );
      return 1;
    }
    output_manager_default.error(
      `Failed to revoke tokens for ${import_chalk28.default.bold(displayName)}: ${err.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          id: target.id,
          uid: target.uid,
          scope: subjectScope,
          supportsRevocation,
          tokensFound: result.tokensFound,
          deleted: result.deleted,
          providerRevoked: result.providerRevoked,
          providerSkipped: result.providerSkipped,
          providerFailed: result.providerFailed
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  const plural14 = result.deleted === 1 ? "token" : "tokens";
  if (subjectScope === "mine") {
    output_manager_default.success(
      `Revoked your ${plural14} from ${import_chalk28.default.bold(displayName)} (${result.deleted} ${plural14} deleted).`
    );
  } else {
    output_manager_default.success(
      `Revoked all ${plural14} from ${import_chalk28.default.bold(displayName)} (${result.deleted} ${plural14} deleted).`
    );
  }
  if (!supportsRevocation) {
    output_manager_default.warn(
      `${import_chalk28.default.bold(displayName)} does not support provider-side token revocation. Tokens were removed from Vercel Connect but may remain valid at the provider until they expire.`
    );
  } else if (result.providerFailed > 0) {
    output_manager_default.warn(
      `${result.providerFailed} token(s) could not be revoked from the provider. They have been removed from Vercel Connect.`
    );
  }
  return 0;
}

// src/commands/connex/open.ts
var import_chalk29 = __toESM(require_source(), 1);
var import_open5 = __toESM(require_open(), 1);
async function openClient(client, args, flags) {
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const clientIdOrUid = args[0];
  if (!clientIdOrUid) {
    output_manager_default.error(
      "Missing connector ID or UID. Usage: vercel connect open <id>"
    );
    return 1;
  }
  await selectConnexTeam(client, "Select the team whose connector to open");
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("No team resolved. Run `vercel switch` or pass `--scope`.");
    return 1;
  }
  output_manager_default.spinner("Looking up connector\u2026");
  let resolvedId;
  try {
    const resolved = await client.fetch(
      `/v1/connect/connectors/${encodeURIComponent(clientIdOrUid)}`
    );
    resolvedId = resolved.id;
  } catch (err) {
    output_manager_default.stopSpinner();
    const status3 = err.status;
    if (status3 === 404) {
      output_manager_default.error(
        `Connector ${import_chalk29.default.bold(`"${clientIdOrUid}"`)} not found on team ${import_chalk29.default.bold(team.slug)}, or Connect is not enabled for this team.`
      );
      return 1;
    }
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  const url = `https://vercel.com/${encodeURIComponent(team.slug)}/~/connect/${resolvedId}`;
  if (asJson) {
    client.stdout.write(`${JSON.stringify({ url }, null, 2)}
`);
    return 0;
  }
  if (client.stdout.isTTY) {
    output_manager_default.print(
      `Opening connector ${import_chalk29.default.bold(clientIdOrUid)} in the dashboard\u2026
`
    );
    (0, import_open5.default)(url);
    return 0;
  }
  client.stdout.write(`${url}
`);
  return 0;
}

// src/commands/connex/index.ts
var COMMAND_CONFIG9 = {
  create: getCommandAliases(createSubcommand2),
  update: getCommandAliases(updateSubcommand),
  list: getCommandAliases(listSubcommand4),
  token: getCommandAliases(tokenSubcommand),
  attach: getCommandAliases(attachSubcommand),
  detach: getCommandAliases(detachSubcommand),
  remove: getCommandAliases(removeSubcommand3),
  "revoke-tokens": getCommandAliases(revokeTokensSubcommand),
  open: getCommandAliases(openSubcommand)
};
async function connex(client) {
  const telemetry2 = new ConnexTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { args, flags } = parseArguments(
    client.argv.slice(2),
    getFlagsSpecification(connexCommand.options),
    { permissive: true }
  );
  const {
    subcommand,
    subcommandOriginal,
    args: subArgs
  } = getSubcommand5(args.slice(1), COMMAND_CONFIG9);
  const needHelp = flags["--help"];
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        columns: client.stderr.columns,
        parent: connexCommand
      })
    );
  }
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("connex");
    output_manager_default.print(
      help(connexCommand, {
        columns: client.stderr.columns
      })
    );
    return 0;
  }
  try {
    switch (subcommand) {
      case "create": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(createSubcommand2);
          return 0;
        }
        telemetry2.trackCliSubcommandCreate(subcommandOriginal);
        const createFlagsSpec = getFlagsSpecification(createSubcommand2.options);
        const createParsedArgs = parseArguments(
          normalizeCreateDataArgs(subArgs),
          createFlagsSpec
        );
        telemetry2.trackCliOptionIcon(createParsedArgs.flags["--icon"]);
        telemetry2.trackCliOptionBackgroundColor(
          createParsedArgs.flags["--background-color"]
        );
        telemetry2.trackCliOptionAccentColor(
          createParsedArgs.flags["--accent-color"]
        );
        telemetry2.trackCliOptionData(createParsedArgs.flags["--data"]);
        telemetry2.trackCliOptionConnectorType(
          createParsedArgs.flags["--connector-type"]
        );
        return await create2(
          client,
          createParsedArgs.args,
          createParsedArgs.flags
        );
      }
      case "update": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(updateSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandUpdate(subcommandOriginal);
        const updateFlagsSpec = getFlagsSpecification(updateSubcommand.options);
        const updateParsedArgs = parseArguments(subArgs, updateFlagsSpec);
        telemetry2.trackCliArgumentId(updateParsedArgs.args[0]);
        telemetry2.trackCliOptionIcon(updateParsedArgs.flags["--icon"]);
        telemetry2.trackCliOptionBackgroundColor(
          updateParsedArgs.flags["--background-color"]
        );
        telemetry2.trackCliOptionAccentColor(
          updateParsedArgs.flags["--accent-color"]
        );
        telemetry2.trackCliOptionFormat(updateParsedArgs.flags["--format"]);
        return await update(
          client,
          updateParsedArgs.args,
          updateParsedArgs.flags
        );
      }
      case "list": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(listSubcommand4);
          return 0;
        }
        telemetry2.trackCliSubcommandList(subcommandOriginal);
        const listFlagsSpec = getFlagsSpecification(listSubcommand4.options);
        const listParsedArgs = parseArguments(subArgs, listFlagsSpec);
        telemetry2.trackCliFlagAllProjects(
          listParsedArgs.flags["--all-projects"]
        );
        telemetry2.trackCliOptionLimit(listParsedArgs.flags["--limit"]);
        telemetry2.trackCliOptionNext(listParsedArgs.flags["--next"]);
        telemetry2.trackCliOptionSearch(listParsedArgs.flags["--search"]);
        telemetry2.trackCliOptionService(listParsedArgs.flags["--service"]);
        telemetry2.trackCliOptionType(listParsedArgs.flags["--type"]);
        telemetry2.trackCliOptionFormat(listParsedArgs.flags["--format"]);
        return await list5(client, listParsedArgs.flags);
      }
      case "token": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(tokenSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandToken(subcommandOriginal);
        const tokenFlagsSpec = getFlagsSpecification(tokenSubcommand.options);
        const tokenParsedArgs = parseArguments(subArgs, tokenFlagsSpec);
        return await token(client, tokenParsedArgs.args, tokenParsedArgs.flags);
      }
      case "attach": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(attachSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandAttach(subcommandOriginal);
        const attachFlagsSpec = getFlagsSpecification(attachSubcommand.options);
        const attachParsedArgs = parseArguments(subArgs, attachFlagsSpec);
        telemetry2.trackCliArgumentClient(attachParsedArgs.args[0]);
        telemetry2.trackCliOptionEnvironment(
          attachParsedArgs.flags["--environment"]
        );
        telemetry2.trackCliOptionProject(attachParsedArgs.flags["--project"]);
        telemetry2.trackCliFlagTriggers(attachParsedArgs.flags["--triggers"]);
        telemetry2.trackCliOptionTriggerBranch(
          attachParsedArgs.flags["--trigger-branch"]
        );
        telemetry2.trackCliOptionTriggerPath(
          attachParsedArgs.flags["--trigger-path"]
        );
        telemetry2.trackCliFlagYes(attachParsedArgs.flags["--yes"]);
        telemetry2.trackCliOptionFormat(attachParsedArgs.flags["--format"]);
        return await attach(
          client,
          attachParsedArgs.args,
          attachParsedArgs.flags
        );
      }
      case "detach": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(detachSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandDetach(subcommandOriginal);
        const detachFlagsSpec = getFlagsSpecification(detachSubcommand.options);
        const detachParsedArgs = parseArguments(subArgs, detachFlagsSpec);
        telemetry2.trackCliArgumentClient(detachParsedArgs.args[0]);
        telemetry2.trackCliOptionProject(detachParsedArgs.flags["--project"]);
        telemetry2.trackCliFlagYes(detachParsedArgs.flags["--yes"]);
        telemetry2.trackCliOptionFormat(detachParsedArgs.flags["--format"]);
        return await detach(
          client,
          detachParsedArgs.args,
          detachParsedArgs.flags
        );
      }
      case "remove": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(removeSubcommand3);
          return 0;
        }
        telemetry2.trackCliSubcommandRemove(subcommandOriginal);
        const removeFlagsSpec = getFlagsSpecification(removeSubcommand3.options);
        const removeParsedArgs = parseArguments(subArgs, removeFlagsSpec);
        telemetry2.trackCliArgumentClient(removeParsedArgs.args[0]);
        telemetry2.trackCliFlagYes(removeParsedArgs.flags["--yes"]);
        telemetry2.trackCliFlagDisconnectAll(
          removeParsedArgs.flags["--disconnect-all"]
        );
        telemetry2.trackCliOptionFormat(removeParsedArgs.flags["--format"]);
        return await remove(
          client,
          removeParsedArgs.args,
          removeParsedArgs.flags
        );
      }
      case "revoke-tokens": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(revokeTokensSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandRevokeTokens(subcommandOriginal);
        const revokeTokensFlagsSpec = getFlagsSpecification(
          revokeTokensSubcommand.options
        );
        const revokeTokensParsedArgs = parseArguments(
          subArgs,
          revokeTokensFlagsSpec
        );
        telemetry2.trackCliArgumentId(revokeTokensParsedArgs.args[0]);
        telemetry2.trackCliFlagMyTokens(
          revokeTokensParsedArgs.flags["--my-tokens"]
        );
        telemetry2.trackCliFlagAllTokens(
          revokeTokensParsedArgs.flags["--all-tokens"]
        );
        telemetry2.trackCliFlagYes(revokeTokensParsedArgs.flags["--yes"]);
        telemetry2.trackCliOptionFormat(
          revokeTokensParsedArgs.flags["--format"]
        );
        return await revokeTokens(
          client,
          revokeTokensParsedArgs.args,
          revokeTokensParsedArgs.flags
        );
      }
      case "open": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("connex", subcommandOriginal);
          printHelp(openSubcommand);
          return 0;
        }
        telemetry2.trackCliSubcommandOpen(subcommandOriginal);
        const openFlagsSpec = getFlagsSpecification(openSubcommand.options);
        const openParsedArgs = parseArguments(subArgs, openFlagsSpec);
        telemetry2.trackCliOptionFormat(openParsedArgs.flags["--format"]);
        return await openClient(
          client,
          openParsedArgs.args,
          openParsedArgs.flags
        );
      }
      default: {
        const validSubcommands = Object.keys(COMMAND_CONFIG9).join(" | ");
        const missingSubcommand = subArgs.length === 0;
        const message = missingSubcommand ? `Please specify a valid subcommand: ${validSubcommands}` : `Unknown subcommand "${subArgs[0]}". Valid subcommands: ${validSubcommands}`;
        outputAgentError(
          client,
          {
            status: "error",
            reason: missingSubcommand ? AGENT_REASON.MISSING_ARGUMENTS : AGENT_REASON.INVALID_ARGUMENTS,
            message,
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "connect --help",
                  packageName,
                  { prependGlobalFlags: true }
                ),
                when: "Show all connect subcommands and options"
              }
            ]
          },
          2
        );
        output_manager_default.error(message);
        return 2;
      }
    }
  } catch (err) {
    printError(err);
    return 1;
  }
}
function normalizeCreateDataArgs(args) {
  const normalized = [];
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    normalized.push(arg);
    if (arg === "--data") {
      const next = args[i + 1];
      if (next === void 0 || next.startsWith("-")) {
        normalized.push("");
      }
    }
  }
  return normalized;
}

// src/commands/contract/index.ts
var import_chalk30 = __toESM(require_source(), 1);

// src/util/telemetry/commands/contract/index.ts
var ContractTelemetryClient = class extends TelemetryClient {
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
};

// src/commands/contract/index.ts
var import_error_utils4 = __toESM(require_dist(), 1);

// src/util/billing/format.ts
function formatCurrency(amount) {
  return `$${amount.toFixed(4)}`;
}
function formatQuantity(quantity, unit) {
  if (unit === "USD") {
    return `$${quantity.toFixed(4)}`;
  }
  return quantity.toFixed(4);
}
function extractDatePortion(isoString) {
  return isoString.slice(0, 10);
}

// src/commands/contract/index.ts
async function contract(client) {
  const { print, log, error, spinner } = output_manager_default;
  const flagsSpecification = getFlagsSpecification(contractCommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const telemetry2 = new ContractTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("contract");
    print(help(contractCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  let contextName;
  let teamId;
  try {
    const scope = await getScope(client);
    contextName = scope.contextName;
    teamId = scope.team?.id;
  } catch (err) {
    if ((0, import_error_utils4.isErrnoException)(err) && (err.code === "NOT_AUTHORIZED" || err.code === "TEAM_DELETED")) {
      error(err.message);
      return 1;
    }
    throw err;
  }
  const start = Date.now();
  if (!asJson) {
    spinner(`Fetching contract commitments for ${import_chalk30.default.bold(contextName)}`);
  }
  const query = new URLSearchParams();
  if (teamId) {
    query.set("teamId", teamId);
  }
  try {
    const response = await client.fetch(
      `/v1/billing/contract-commitments?${query}`,
      {
        json: false,
        useCurrentTeam: false
      }
    );
    if (!response.ok) {
      const errorText = await response.text();
      error(
        `Failed to fetch contract commitments: ${response.status} ${errorText}`
      );
      return 1;
    }
    const text = await response.text();
    const data = text ? JSON.parse(text) : null;
    const commitments = Array.isArray(data) ? data : [];
    if (asJson) {
      const jsonOutput = {
        context: contextName,
        commitments: commitments.map((c) => ({
          contractId: c.ContractId,
          contractPeriodStart: c.ContractPeriodStart,
          contractPeriodEnd: c.ContractPeriodEnd,
          commitmentId: c.ContractCommitmentId,
          commitmentType: c.ContractCommitmentType,
          commitmentCategory: c.ContractCommitmentCategory,
          commitmentPeriodStart: c.ContractCommitmentPeriodStart,
          commitmentPeriodEnd: c.ContractCommitmentPeriodEnd,
          commitmentCost: c.ContractCommitmentCost,
          commitmentQuantity: c.ContractCommitmentQuantity,
          commitmentUnit: c.ContractCommitmentUnit,
          billingCurrency: c.BillingCurrency,
          description: c.ContractCommitmentDescription
        })),
        totalCommitments: commitments.length
      };
      client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
      return 0;
    }
    log(
      `Contract commitments for ${import_chalk30.default.bold(contextName)} ${elapsed(Date.now() - start)}`
    );
    log("");
    if (commitments.length === 0) {
      log("No contract commitments found.");
      return 0;
    }
    const contractGroups = /* @__PURE__ */ new Map();
    for (const commitment of commitments) {
      const existing = contractGroups.get(commitment.ContractId) || [];
      existing.push(commitment);
      contractGroups.set(commitment.ContractId, existing);
    }
    for (const [contractId, contractCommitments] of contractGroups) {
      const firstCommitment = contractCommitments[0];
      log(import_chalk30.default.bold(`Contract: ${contractId}`));
      log(
        `${import_chalk30.default.gray("Period:")} ${extractDatePortion(firstCommitment.ContractPeriodStart)} to ${extractDatePortion(firstCommitment.ContractPeriodEnd)}`
      );
      log("");
      const headers = [
        "Type",
        "Category",
        "Period",
        "Commitment",
        "Description"
      ];
      const rows = contractCommitments.map((c) => {
        const periodStr = `${extractDatePortion(c.ContractCommitmentPeriodStart)} - ${extractDatePortion(c.ContractCommitmentPeriodEnd)}`;
        let commitmentValue;
        if (c.ContractCommitmentCategory === "Spend" && c.ContractCommitmentCost !== void 0) {
          commitmentValue = formatCurrency(c.ContractCommitmentCost);
        } else if (c.ContractCommitmentCategory === "Usage" && c.ContractCommitmentQuantity !== void 0) {
          commitmentValue = formatQuantity(
            c.ContractCommitmentQuantity,
            c.ContractCommitmentUnit
          );
        } else {
          commitmentValue = "-";
        }
        return [
          c.ContractCommitmentType,
          c.ContractCommitmentCategory,
          periodStr,
          commitmentValue,
          c.ContractCommitmentDescription || "-"
        ];
      });
      const tablePrint = table(
        [headers.map((h) => import_chalk30.default.bold(import_chalk30.default.cyan(h))), ...rows],
        { hsep: 3, align: ["l", "l", "l", "r", "l"] }
      ).replace(/^/gm, "  ");
      print(`${tablePrint}

`);
    }
    log(`${import_chalk30.default.gray("Total commitments:")} ${commitments.length}`);
    return 0;
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
}

// src/commands/certs/add.ts
var import_chalk31 = __toESM(require_source(), 1);

// src/util/certs/create-cert-from-file.ts
var import_error_utils5 = __toESM(require_dist(), 1);
import { readFileSync } from "fs";
import { resolve as resolve4 } from "path";
async function createCertFromFile(client, keyPath, certPath, caPath) {
  output_manager_default.spinner("Adding your custom certificate");
  try {
    const cert = readFileSync(resolve4(certPath), "utf8");
    const key = readFileSync(resolve4(keyPath), "utf8");
    const ca = readFileSync(resolve4(caPath), "utf8");
    const certificate = await client.fetch("/v3/certs", {
      method: "PUT",
      body: {
        ca,
        cert,
        key
      }
    });
    return certificate;
  } catch (err) {
    if ((0, import_error_utils5.isErrnoException)(err) && err.code === "ENOENT") {
      return new Error(`The specified file "${err.path}" doesn't exist.`);
    }
    if (isAPIError(err) && err.status < 500) {
      return err;
    }
    throw err;
  }
}

// src/util/telemetry/commands/certs/add.ts
var CertsAddTelemetryClient = class extends TelemetryClient {
  trackCliFlagOverwrite(v) {
    if (v) {
      this.trackCliFlag("overwrite");
    }
  }
  trackCliOptionCrt(v) {
    if (v) {
      this.trackCliOption({
        option: "crt",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionKey(v) {
    if (v) {
      this.trackCliOption({
        option: "key",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionCa(v) {
    if (v) {
      this.trackCliOption({
        option: "ca",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/certs/add.ts
async function add(client, argv) {
  const { telemetryEventStore } = client;
  const addStamp = stamp_default();
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  let cert;
  const {
    "--overwrite": overwrite,
    "--crt": crtPath,
    "--key": keyPath,
    "--ca": caPath
  } = opts;
  const telemetry2 = new CertsAddTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  telemetry2.trackCliFlagOverwrite(overwrite);
  telemetry2.trackCliOptionCrt(crtPath);
  telemetry2.trackCliOptionKey(keyPath);
  telemetry2.trackCliOptionCa(caPath);
  if (overwrite) {
    output_manager_default.error("Overwrite option is deprecated");
    return 1;
  }
  if (crtPath || keyPath || caPath) {
    if (args.length !== 0 || !crtPath || !keyPath || !caPath) {
      output_manager_default.error(
        `Invalid number of arguments to create a custom certificate entry. Usage:`
      );
      output_manager_default.print(
        `  ${import_chalk31.default.cyan(
          `${getCommandName(
            "certs add --crt <domain.crt> --key <domain.key> --ca <ca.crt>"
          )}`
        )}
`
      );
      return 1;
    }
    cert = await createCertFromFile(client, keyPath, crtPath, caPath);
  } else {
    output_manager_default.warn(
      `${import_chalk31.default.cyan(
        getCommandName("certs add")
      )} will be soon deprecated. Please use ${import_chalk31.default.cyan(
        getCommandName("certs issue <cn> <cns>")
      )} instead`
    );
    if (args.length < 1) {
      output_manager_default.error(
        `Invalid number of arguments to create a custom certificate entry. Usage:`
      );
      output_manager_default.print(
        `  ${import_chalk31.default.cyan(getCommandName("certs add <cn>[, <cn>]"))}
`
      );
      return 1;
    }
    const cns = args.reduce(
      (res, item) => res.concat(item.split(",")),
      []
    );
    output_manager_default.spinner(
      `Generating a certificate for ${import_chalk31.default.bold(cns.join(", "))}`
    );
    const { contextName } = await getScope(client);
    cert = await createCertForCns(client, cns, contextName);
    output_manager_default.stopSpinner();
  }
  if (cert instanceof Error) {
    output_manager_default.error(cert.message);
    return 1;
  } else {
    output_manager_default.success(
      `Certificate entry for ${import_chalk31.default.bold(
        cert.cns.join(", ")
      )} created ${addStamp()}`
    );
  }
  return 0;
}
var add_default = add;

// src/commands/certs/issue.ts
var import_chalk34 = __toESM(require_source(), 1);
var import_tldts4 = __toESM(require_cjs(), 1);

// src/util/certs/finish-cert-order.ts
var import_chalk32 = __toESM(require_source(), 1);
async function startCertOrder(client, cns, context) {
  output_manager_default.spinner(`Issuing a certificate for ${import_chalk32.default.bold(cns.join(", "))}`);
  try {
    const cert = await client.fetch("/v3/certs", {
      method: "PATCH",
      body: {
        op: "finalizeOrder",
        domains: cns
      }
    });
    return cert;
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "cert_order_not_found") {
        return new CertOrderNotFound(cns);
      }
      const mappedError = mapCertError(err, cns);
      if (mappedError) {
        return mappedError;
      }
    }
    throw err;
  }
}

// src/util/certs/get-cns-from-args.ts
function getCnsFromArgs(args) {
  return args.reduce((res, item) => [...res, ...item.split(",")], []).filter((i) => i);
}

// src/util/certs/start-cert-order.ts
var import_chalk33 = __toESM(require_source(), 1);
async function startCertOrder2(client, cns, contextName) {
  output_manager_default.spinner(
    `Starting certificate issuance for ${import_chalk33.default.bold(
      cns.join(", ")
    )} under ${import_chalk33.default.bold(contextName)}`
  );
  const order = await client.fetch("/v3/certs", {
    method: "PATCH",
    body: {
      op: "startOrder",
      domains: cns
    }
  });
  return order;
}

// src/util/telemetry/commands/certs/issue.ts
var CertsIssueTelemetryClient = class extends CertsAddTelemetryClient {
  trackCliArgumentCn(value) {
    if (value) {
      this.trackCliArgument({
        arg: "cn",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagChallengeOnly(v) {
    if (v) {
      this.trackCliFlag("challenge-only");
    }
  }
};

// src/commands/certs/issue.ts
async function issue(client, argv) {
  let cert;
  const { telemetryEventStore } = client;
  const addStamp = stamp_default();
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(issueSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const {
    "--challenge-only": challengeOnly,
    "--overwrite": overwrite,
    "--crt": crtPath,
    "--key": keyPath,
    "--ca": caPath
  } = opts;
  const telemetry2 = new CertsIssueTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  telemetry2.trackCliFlagChallengeOnly(challengeOnly);
  telemetry2.trackCliFlagOverwrite(overwrite);
  telemetry2.trackCliOptionCrt(crtPath);
  telemetry2.trackCliOptionKey(keyPath);
  telemetry2.trackCliOptionCa(caPath);
  if (overwrite) {
    output_manager_default.error("Overwrite option is deprecated");
    return 1;
  }
  if (crtPath || keyPath || caPath) {
    if (args.length !== 0 || !crtPath || !keyPath || !caPath) {
      output_manager_default.error(
        `Invalid number of arguments to create a custom certificate entry. Usage:`
      );
      output_manager_default.print(
        `  ${import_chalk34.default.cyan(
          getCommandName(
            "certs issue --crt <domain.crt> --key <domain.key> --ca <ca.crt>"
          )
        )}
`
      );
      return 1;
    }
    cert = await createCertFromFile(client, keyPath, crtPath, caPath);
    if (cert instanceof Error) {
      output_manager_default.error(cert.message);
      return 1;
    }
    output_manager_default.success(
      `Certificate entry for ${import_chalk34.default.bold(
        cert.cns.join(", ")
      )} created ${addStamp()}`
    );
    return 0;
  }
  if (args.length < 1) {
    output_manager_default.error(
      `Invalid number of arguments to create a custom certificate entry. Usage:`
    );
    output_manager_default.print(
      `  ${import_chalk34.default.cyan(getCommandName("certs issue <cn>[, <cn>]"))}
`
    );
    return 1;
  }
  telemetry2.trackCliArgumentCn(args[0]);
  const cns = getCnsFromArgs(args);
  const { contextName } = await getScope(client);
  if (challengeOnly) {
    return runStartOrder(client, cns, contextName, addStamp);
  }
  cert = await startCertOrder(client, cns, contextName);
  if (cert instanceof CertOrderNotFound) {
    cert = await createCertForCns(client, cns, contextName);
  }
  if (cert instanceof CertError) {
    if (cert.meta.code === "wildcard_not_allowed") {
      return runStartOrder(client, cns, contextName, addStamp, {
        fallingBack: true
      });
    }
  }
  const handledResult = handleCertError(cert);
  if (handledResult === 1) {
    return handledResult;
  }
  if (handledResult instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You do not have permissions over domain ${import_chalk34.default.underline(
        handledResult.meta.domain
      )} under ${import_chalk34.default.bold(handledResult.meta.context)}.`
    );
    return 1;
  }
  output_manager_default.success(
    `Certificate entry for ${import_chalk34.default.bold(
      handledResult.cns.join(", ")
    )} created ${addStamp()}`
  );
  return 0;
}
async function runStartOrder(client, cns, contextName, stamp, { fallingBack = false } = {}) {
  const { challengesToResolve } = await startCertOrder2(
    client,
    cns,
    contextName
  );
  const pendingChallenges = challengesToResolve.filter(
    (challenge) => challenge.status === "pending"
  );
  if (fallingBack) {
    output_manager_default.warn(
      `To generate a wildcard certificate for domain for an external domain you must solve challenges manually.`
    );
  }
  if (pendingChallenges.length === 0) {
    output_manager_default.log(
      `A certificate issuance for ${import_chalk34.default.bold(
        cns.join(", ")
      )} has been started ${stamp()}`
    );
    output_manager_default.print(
      `  There are no pending challenges. Finish the issuance by running: 
`
    );
    output_manager_default.print(
      `  ${import_chalk34.default.cyan(getCommandName(`certs issue ${cns.join(" ")}`))}
`
    );
    return 0;
  }
  output_manager_default.log(
    `A certificate issuance for ${import_chalk34.default.bold(
      cns.join(", ")
    )} has been started ${stamp()}`
  );
  output_manager_default.print(
    `  Add the following TXT records with your registrar to be able to the solve the DNS challenge:

`
  );
  const [header, ...rows] = formatDNSTable(
    pendingChallenges.map((challenge) => {
      const subdomain = (0, import_tldts4.getSubdomain)(challenge.domain);
      if (subdomain === null) {
        throw new InvalidDomain(challenge.domain);
      }
      return [
        subdomain ? `_acme-challenge.${subdomain}` : `_acme-challenge`,
        "TXT",
        challenge.value
      ];
    })
  ).split("\n");
  output_manager_default.print(`${header}
`);
  client.stdout.write(`${rows.join("\n")}

`);
  output_manager_default.log(`To issue the certificate once the records are added, run:`);
  output_manager_default.print(
    `  ${import_chalk34.default.cyan(getCommandName(`certs issue ${cns.join(" ")}`))}
`
  );
  output_manager_default.print(
    "  Read more: https://err.sh/vercel/solve-challenges-manually\n"
  );
  return 0;
}

// src/commands/certs/ls.ts
var import_chalk35 = __toESM(require_source(), 1);
var import_ms7 = __toESM(require_ms(), 1);

// src/util/certs/get-certs.ts
async function getCerts(client, next, limit = 20) {
  let certsUrl = `/v4/certs?limit=${limit}`;
  if (next) {
    certsUrl += `&until=${next}`;
  }
  return await client.fetch(certsUrl);
}

// src/util/telemetry/commands/certs/ls.ts
var CertsLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionLimit(limit) {
    if (limit) {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionNext(next) {
    if (next) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/certs/ls.ts
async function ls2(client, argv) {
  const { telemetryEventStore } = client;
  const telemetry2 = new CertsLsTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand3.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "certs ls",
    args
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  telemetry2.trackCliOptionLimit(opts["--limit"]);
  telemetry2.trackCliOptionNext(opts["--next"]);
  let paginationOptions;
  try {
    paginationOptions = getPaginationOpts(opts);
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
  const lsStamp = stamp_default();
  const { certs, pagination } = await getCerts(client, ...paginationOptions);
  const { contextName } = await getScope(client);
  output_manager_default.log(
    `${certs.length > 0 ? "Certificates" : "No certificates"} found under ${import_chalk35.default.bold(contextName)} ${lsStamp()}`
  );
  if (certs.length > 0) {
    client.stdout.write(formatCertsTable(certs));
  }
  if (pagination && pagination.count === 20) {
    const flags = getCommandFlags(opts, ["_", "--next"]);
    output_manager_default.log(
      `To display the next page run ${getCommandName(
        `certs ls${flags} --next ${pagination.next}`
      )}`
    );
  }
  return 0;
}
function formatCertsTable(certsList) {
  return `${table(
    [formatCertsTableHead(), ...formatCertsTableBody(certsList)],
    { align: ["l", "l", "r", "c", "r"], hsep: 2 }
  ).replace(/^(.*)/gm, "  $1")}
`;
}
function formatCertsTableHead() {
  return [
    import_chalk35.default.dim("id"),
    import_chalk35.default.dim("cns"),
    import_chalk35.default.dim("expiration"),
    import_chalk35.default.dim("renew"),
    import_chalk35.default.dim("age")
  ];
}
function formatCertsTableBody(certsList) {
  const now = /* @__PURE__ */ new Date();
  return certsList.reduce(
    (result, cert) => result.concat(formatCert(now, cert)),
    []
  );
}
function formatCert(time, cert) {
  return cert.cns.map(
    (cn, idx) => idx === 0 ? formatCertFirstCn(time, cert, cn, cert.cns.length > 1) : formatCertNonFirstCn(cn, cert.cns.length > 1)
  );
}
function formatCertNonFirstCn(cn, multiple) {
  return ["", formatCertCn(cn, multiple), "", "", ""];
}
function formatCertCn(cn, multiple) {
  return multiple ? `${import_chalk35.default.gray("-")} ${import_chalk35.default.bold(cn)}` : import_chalk35.default.bold(cn);
}
function formatCertFirstCn(time, cert, cn, multiple) {
  return [
    cert.uid,
    formatCertCn(cn, multiple),
    formatExpirationDate(new Date(cert.expiration)),
    cert.autoRenew ? "yes" : "no",
    import_chalk35.default.gray((0, import_ms7.default)(time.getTime() - new Date(cert.created).getTime()))
  ];
}
function formatExpirationDate(date) {
  const diff2 = date.getTime() - Date.now();
  return diff2 < 0 ? import_chalk35.default.gray(`${(0, import_ms7.default)(-diff2)} ago`) : import_chalk35.default.gray(`in ${(0, import_ms7.default)(diff2)}`);
}
var ls_default = ls2;

// src/commands/certs/rm.ts
var import_chalk36 = __toESM(require_source(), 1);
var import_ms8 = __toESM(require_ms(), 1);
var import_pluralize4 = __toESM(require_pluralize(), 1);

// src/util/certs/delete-cert-by-id.ts
async function deleteCertById(client, id) {
  return client.fetch(`/v5/certs/${id}`, {
    method: "DELETE"
  });
}

// src/util/certs/get-cert-by-id.ts
async function getCertById(client, id) {
  try {
    return await client.fetch(`/v6/certs/${id}`);
  } catch (err) {
    if (isAPIError(err) && err.code === "cert_not_found") {
      return new CertNotFound(id);
    }
    throw err;
  }
}

// src/util/certs/get-custom-certs-for-domain.ts
import { stringify } from "querystring";
async function getCustomCertsForDomain(client, context, domain2) {
  try {
    const { certs } = await client.fetch(
      `/v5/now/certs?${stringify({ domain: domain2, custom: true })}`
    );
    return certs;
  } catch (err) {
    if (isAPIError(err) && err.code === "forbidden") {
      return new CertsPermissionDenied(context, domain2);
    }
    throw err;
  }
}

// src/util/telemetry/commands/certs/remove.ts
var CertsRemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentId(v) {
    if (v) {
      this.trackCliArgument({
        arg: "id",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/certs/rm.ts
async function rm2(client, argv) {
  const rmStamp = stamp_default();
  const { telemetryEventStore } = client;
  const telemetry2 = new CertsRemoveTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args } = parsedArgs;
  const id = args[0];
  telemetry2.trackCliArgumentId(id);
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk36.default.cyan(
        `${getCommandName("certs rm <id or cn>")}`
      )}`
    );
    return 1;
  }
  const { contextName } = await getScope(client);
  const certs = await getCertsToDelete(client, contextName, id);
  if (certs instanceof CertsPermissionDenied) {
    output_manager_default.error(
      `You don't have access to ${param(id)}'s certs under ${contextName}.`
    );
    return 1;
  }
  if (certs.length === 0) {
    if (id.includes(".")) {
      output_manager_default.error(
        `No custom certificates found for "${id}" under ${import_chalk36.default.bold(
          contextName
        )}`
      );
    } else {
      output_manager_default.error(
        `No certificates found by id "${id}" under ${import_chalk36.default.bold(contextName)}`
      );
    }
    return 1;
  }
  const yes = await readConfirmation(
    client,
    "The following certificates will be removed permanently",
    certs
  );
  if (!yes) {
    return 0;
  }
  await Promise.all(certs.map((cert) => deleteCertById(client, cert.uid)));
  output_manager_default.success(
    `${import_chalk36.default.bold(
      (0, import_pluralize4.default)("Certificate", certs.length, true)
    )} removed ${rmStamp()}`
  );
  return 0;
}
async function getCertsToDelete(client, contextName, id) {
  const cert = await getCertById(client, id);
  if (cert instanceof CertNotFound) {
    const certs = await getCustomCertsForDomain(client, contextName, id);
    if (certs instanceof CertsPermissionDenied) {
      return certs;
    }
    return certs;
  }
  return [cert];
}
function readConfirmation(client, msg, certs) {
  return new Promise((resolve8) => {
    output_manager_default.log(msg);
    output_manager_default.print(
      `${table(certs.map(formatCertRow), {
        align: ["l", "r", "l"],
        hsep: 6
      }).replace(/^(.*)/gm, "  $1")}
`
    );
    output_manager_default.print(
      `${import_chalk36.default.bold.red("> Are you sure?")} ${import_chalk36.default.gray("(y/N) ")}`
    );
    client.stdin.on("data", (d) => {
      process.stdin.pause();
      resolve8(d.toString().trim().toLowerCase() === "y");
    }).resume();
  });
}
function formatCertRow(cert) {
  return [
    cert.uid,
    import_chalk36.default.bold(cert.cns ? cert.cns.join(", ") : "\u2013"),
    ...cert.created ? [import_chalk36.default.gray(`${(0, import_ms8.default)(Date.now() - new Date(cert.created).getTime())} ago`)] : []
  ];
}
var rm_default = rm2;

// src/util/telemetry/commands/certs/index.ts
var CertsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandIssue(actual) {
    this.trackCliSubcommand({
      subcommand: "issue",
      value: actual
    });
  }
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
};

// src/commands/certs/index.ts
var COMMAND_CONFIG10 = {
  add: getCommandAliases(addSubcommand),
  issue: getCommandAliases(issueSubcommand),
  ls: getCommandAliases(listSubcommand3),
  rm: getCommandAliases(removeSubcommand2)
};
async function main6(client) {
  const { telemetryEventStore } = client;
  const telemetry2 = new CertsTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(certsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, subcommandOriginal, args } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG10
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("certs", subcommand);
    output_manager_default.print(help(certsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: certsCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "issue":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("certs", subcommandOriginal);
        printHelp(issueSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandIssue(subcommandOriginal);
      return issue(client, args);
    case "ls":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("certs", subcommandOriginal);
        printHelp(listSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls_default(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("certs", subcommandOriginal);
        printHelp(removeSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm_default(client, args);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("certs", subcommandOriginal);
        printHelp(addSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add_default(client, args);
    default:
      output_manager_default.error("Please specify a valid subcommand: ls | issue | rm");
      output_manager_default.print(help(certsCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/crons/add.ts
var import_chalk37 = __toESM(require_source(), 1);
import { resolve as resolve5 } from "path";
import { access, readFile as readFile3, writeFile as writeFile2 } from "fs/promises";

// src/util/telemetry/commands/crons/add.ts
var CronsAddTelemetryClient = class extends TelemetryClient {
  trackCliOptionPath(path3) {
    if (path3) {
      this.trackCliOption({
        option: "path",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSchedule(schedule) {
    if (schedule) {
      this.trackCliOption({
        option: "schedule",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/crons/add.ts
var CRON_FIELD_RANGES = [
  ["minute", 0, 59],
  ["hour", 0, 23],
  ["day of month", 1, 31],
  ["month", 1, 12],
  ["day of week", 0, 7]
];
function validateCronSchedule(expression) {
  if (expression.length > 256) {
    return "Schedule expression must be 256 characters or less";
  }
  const fields = expression.trim().split(/\s+/);
  if (fields.length !== 5) {
    return `Schedule must have exactly 5 fields (minute hour day-of-month month day-of-week), got ${fields.length}`;
  }
  for (let i = 0; i < fields.length; i++) {
    const [name, min, max] = CRON_FIELD_RANGES[i];
    const error = validateCronField(fields[i], name, min, max);
    if (error) {
      return error;
    }
  }
  return true;
}
function validateCronField(field, name, min, max) {
  const parts = field.split(",");
  for (const part of parts) {
    const [range, stepStr] = part.split("/");
    if (stepStr !== void 0) {
      const step = Number(stepStr);
      if (!Number.isInteger(step) || step < 1) {
        return `Invalid step value "${stepStr}" in ${name} field`;
      }
    }
    if (range === "*") {
      continue;
    }
    if (range.includes("-")) {
      const rangeParts = range.split("-");
      if (rangeParts.length !== 2 || rangeParts[0] === "" || rangeParts[1] === "") {
        return `Invalid range "${range}" in ${name} field`;
      }
      const [startStr, endStr] = rangeParts;
      const start = Number(startStr);
      const end = Number(endStr);
      if (!Number.isInteger(start) || !Number.isInteger(end)) {
        return `Invalid range "${range}" in ${name} field`;
      }
      if (start < min || start > max || end < min || end > max) {
        return `Value out of range in ${name} field (${min}-${max})`;
      }
      if (start > end) {
        return `Invalid range "${range}" in ${name} field: start is greater than end`;
      }
      continue;
    }
    const value = Number(range);
    if (!Number.isInteger(value)) {
      return `Invalid value "${range}" in ${name} field`;
    }
    if (value < min || value > max) {
      return `Value ${value} out of range in ${name} field (${min}-${max})`;
    }
  }
  return null;
}
async function add2(client, argv) {
  const telemetry2 = new CronsAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { flags } = parsedArgs;
  let cronPath = flags["--path"];
  let schedule = flags["--schedule"];
  telemetry2.trackCliOptionPath(cronPath);
  telemetry2.trackCliOptionSchedule(schedule);
  if (!cronPath || !schedule) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        `Missing required flags. Use ${getCommandName('crons add --path /api/cron --schedule "0 10 * * *"')} in non-interactive mode.`
      );
      return 1;
    }
    if (!cronPath) {
      cronPath = await client.input.text({
        message: "What is the API route path for the cron job?",
        validate: (value) => {
          if (!value.startsWith("/")) {
            return "Path must start with /";
          }
          if (value.length > 512) {
            return "Path must be 512 characters or less";
          }
          return true;
        }
      });
    }
    if (!schedule) {
      schedule = await client.input.text({
        message: "What is the cron schedule expression?",
        validate: validateCronSchedule
      });
    }
  }
  if (!cronPath.startsWith("/")) {
    output_manager_default.error("Path must start with /");
    return 1;
  }
  if (cronPath.length > 512) {
    output_manager_default.error("Path must be 512 characters or less");
    return 1;
  }
  const scheduleValidation = validateCronSchedule(schedule);
  if (scheduleValidation !== true) {
    output_manager_default.error(scheduleValidation);
    return 1;
  }
  const nonJsonConfigs = [
    ...VERCEL_CONFIG_EXTENSIONS.map((ext) => `vercel.${ext}`),
    ...isVercelTomlEnabled() ? ["vercel.toml"] : []
  ];
  for (const configName of nonJsonConfigs) {
    const altPath = resolve5(client.cwd, configName);
    try {
      await access(altPath);
      output_manager_default.error(
        `Found ${import_chalk37.default.cyan(configName)} \u2014 ${getCommandName("crons add")} only supports ${import_chalk37.default.cyan("vercel.json")}. Add cron jobs directly to your ${import_chalk37.default.cyan(configName)} file instead.`
      );
      return 1;
    } catch {
    }
  }
  const configPath = resolve5(client.cwd, "vercel.json");
  let config;
  try {
    const content = await readFile3(configPath, "utf-8");
    config = JSON.parse(content);
  } catch (err) {
    if (err instanceof SyntaxError) {
      output_manager_default.error(
        `Failed to parse ${import_chalk37.default.cyan("vercel.json")}: ${err.message}`
      );
      return 1;
    }
    if (err instanceof Error && "code" in err && err.code === "ENOENT") {
      config = {};
    } else {
      output_manager_default.error(
        `Failed to read ${import_chalk37.default.cyan("vercel.json")}: ${err instanceof Error ? err.message : String(err)}`
      );
      return 1;
    }
  }
  const existingCrons = Array.isArray(config.crons) ? config.crons : [];
  if (existingCrons.some((c) => c.path === cronPath)) {
    output_manager_default.error(
      `A cron job with path ${import_chalk37.default.bold(cronPath)} already exists in vercel.json`
    );
    return 1;
  }
  existingCrons.push({ path: cronPath, schedule });
  config.crons = existingCrons;
  try {
    await writeFile2(
      configPath,
      JSON.stringify(config, null, 2) + "\n",
      "utf-8"
    );
  } catch (err) {
    output_manager_default.error(
      `Failed to write ${import_chalk37.default.cyan("vercel.json")}: ${err instanceof Error ? err.message : String(err)}`
    );
    return 1;
  }
  output_manager_default.log(
    `Added cron job ${import_chalk37.default.bold(cronPath)} with schedule ${import_chalk37.default.bold(schedule)} to ${import_chalk37.default.cyan("vercel.json")}`
  );
  output_manager_default.warn(
    `This cron job won't be active until the project is deployed to production. Run ${getCommandName("deploy --prod")} to deploy.`
  );
  return 0;
}

// src/commands/crons/ls.ts
var import_chalk38 = __toESM(require_source(), 1);

// src/util/telemetry/commands/crons/ls.ts
var CronsLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/crons/ls.ts
async function ls3(client, argv) {
  const telemetry2 = new CronsLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand5.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "crons ls",
    args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  telemetry2.trackCliOptionFormat(opts["--format"]);
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. ${client.nonInteractive ? `Run ${getCommandName("link --yes --team <team-id> --project <project-id>")} to link non-interactively.` : `Run ${getCommandName("link")} to begin.`}`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  const lsStamp = stamp_default();
  output_manager_default.spinner(
    `Fetching cron jobs for ${import_chalk38.default.bold(`${org.slug}/${project.name}`)}`
  );
  const projectData = await client.fetch(`/v9/projects/${encodeURIComponent(project.id)}`);
  const definitions = projectData.crons?.definitions ?? [];
  const isDisabled = projectData.crons?.disabledAt != null;
  const localConfig = readLocalConfig(client.cwd);
  const localCrons = Array.isArray(localConfig?.crons) ? localConfig.crons : [];
  const deployedByPath = new Map(definitions.map((d) => [d.path, d]));
  const undeployedCrons = [];
  const modifiedCrons = [];
  for (const local of localCrons) {
    const deployed = deployedByPath.get(local.path);
    if (!deployed) {
      undeployedCrons.push(local);
    } else if (deployed.schedule !== local.schedule) {
      modifiedCrons.push({ local, deployed });
    }
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonOutput = {
      crons: definitions.map((cron) => ({
        path: cron.path,
        schedule: cron.schedule,
        host: cron.host
      })),
      undeployed: undeployedCrons.map((cron) => ({
        path: cron.path,
        schedule: cron.schedule
      })),
      modified: modifiedCrons.map(({ local, deployed }) => ({
        path: local.path,
        localSchedule: local.schedule,
        deployedSchedule: deployed.schedule
      })),
      enabled: !isDisabled
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else if (definitions.length === 0 && undeployedCrons.length === 0 && modifiedCrons.length === 0) {
    output_manager_default.log(
      `No cron jobs found for ${import_chalk38.default.bold(`${org.slug}/${project.name}`)} ${import_chalk38.default.gray(lsStamp())}`
    );
  } else {
    const totalDeployed = definitions.length;
    if (totalDeployed > 0) {
      output_manager_default.log(
        `${totalDeployed} cron ${totalDeployed === 1 ? "job" : "jobs"} found for ${import_chalk38.default.bold(`${org.slug}/${project.name}`)}${isDisabled ? import_chalk38.default.yellow(" (disabled)") : ""} ${import_chalk38.default.gray(lsStamp())}`
      );
      output_manager_default.print(
        formatCronsTable(definitions).replace(/^(.*)/gm, `${" ".repeat(1)}$1`)
      );
      output_manager_default.print("\n\n");
    }
    if (undeployedCrons.length > 0 || modifiedCrons.length > 0) {
      const pendingCount = undeployedCrons.length + modifiedCrons.length;
      output_manager_default.log(
        `${pendingCount} local ${pendingCount === 1 ? "change" : "changes"} pending deploy`
      );
      output_manager_default.print(
        formatPendingCronsTable(undeployedCrons, modifiedCrons).replace(
          /^(.*)/gm,
          `${" ".repeat(1)}$1`
        )
      );
      output_manager_default.print("\n\n");
      output_manager_default.warn(
        `Run ${getCommandName("deploy --prod")} to apply local changes.`
      );
    }
  }
  return 0;
}
function formatCronsTable(definitions) {
  const rows = definitions.map((cron) => [
    import_chalk38.default.bold(cron.path),
    cron.schedule
  ]);
  return formatTable(["Path", "Schedule"], ["l", "l"], [{ rows }]);
}
function formatPendingCronsTable(undeployed, modified) {
  const rows = [
    ...modified.map(({ local, deployed }) => [
      import_chalk38.default.bold(local.path),
      `${import_chalk38.default.dim(deployed.schedule)} \u2192 ${local.schedule}`,
      import_chalk38.default.yellow("modified")
    ]),
    ...undeployed.map((cron) => [
      import_chalk38.default.dim(cron.path),
      import_chalk38.default.dim(cron.schedule),
      import_chalk38.default.yellow("not deployed")
    ])
  ];
  return formatTable(
    ["Path", "Schedule", "Status"],
    ["l", "l", "l"],
    [{ rows }]
  );
}

// src/commands/crons/run.ts
var import_chalk39 = __toESM(require_source(), 1);

// src/util/telemetry/commands/crons/run.ts
var CronsRunTelemetryClient = class extends TelemetryClient {
  trackCliArgumentPath(path3) {
    if (path3) {
      this.trackCliArgument({
        arg: "path",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/crons/run.ts
async function run(client, argv) {
  const telemetry2 = new CronsRunTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(runSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  let [cronPath] = args;
  telemetry2.trackCliArgumentPath(cronPath);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. ${client.nonInteractive ? `Run ${getCommandName("link --yes --team <team-id> --project <project-id>")} to link non-interactively.` : `Run ${getCommandName("link")} to begin.`}`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  const runStamp = stamp_default();
  output_manager_default.spinner(
    `Fetching cron jobs for ${import_chalk39.default.bold(`${org.slug}/${project.name}`)}`
  );
  const projectData = await client.fetch(`/v9/projects/${encodeURIComponent(project.id)}`);
  const definitions = projectData.crons?.definitions ?? [];
  if (definitions.length === 0) {
    output_manager_default.error(
      `No cron jobs found for ${import_chalk39.default.bold(`${org.slug}/${project.name}`)}. Define cron jobs in your vercel.json file.`
    );
    return 1;
  }
  if (projectData.crons?.disabledAt != null) {
    output_manager_default.error(
      `Cron jobs are disabled for ${import_chalk39.default.bold(`${org.slug}/${project.name}`)}. Enable them in the project settings.`
    );
    return 1;
  }
  if (!cronPath) {
    if (client.nonInteractive) {
      output_manager_default.error(
        `A cron path argument is required in non-interactive mode. Usage: ${getCommandName("crons run <path>")}`
      );
      return 1;
    }
    output_manager_default.stopSpinner();
    if (definitions.length === 1) {
      cronPath = definitions[0].path;
      output_manager_default.log(`Auto-selected ${import_chalk39.default.bold(cronPath)} (only cron job)`);
    } else {
      cronPath = await client.input.select({
        message: "Which cron job would you like to run?",
        choices: definitions.map((cron) => ({
          name: `${cron.path} (${cron.schedule})`,
          value: cron.path
        }))
      });
    }
  }
  const cronDef = definitions.find((d) => d.path === cronPath);
  if (!cronDef) {
    output_manager_default.error(
      `Cron job with path ${import_chalk39.default.bold(cronPath)} not found. Run ${getCommandName("crons ls")} to see available cron jobs.`
    );
    return 1;
  }
  output_manager_default.spinner(`Triggering cron job ${import_chalk39.default.bold(cronPath)}`);
  const teamId = link.org.type === "team" ? link.org.id : void 0;
  const qs = teamId ? `?teamId=${encodeURIComponent(teamId)}` : "";
  let result;
  try {
    result = await client.fetch(
      `/v1/projects/${encodeURIComponent(project.id)}/crons/run${qs}`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          path: cronDef.path,
          schedule: cronDef.schedule
        })
      }
    );
  } catch (err) {
    if (isAPIError(err)) {
      output_manager_default.error(
        `Failed to trigger cron job ${import_chalk39.default.bold(cronPath)}: ${err.message}`
      );
      return 1;
    }
    throw err;
  }
  output_manager_default.log(
    `Cron job ${import_chalk39.default.bold(cronPath)} triggered ${import_chalk39.default.gray(runStamp())}`
  );
  output_manager_default.log(
    `  Invocation time: ${import_chalk39.default.cyan(new Date(result.invocationAt).toISOString())}`
  );
  return 0;
}

// src/util/telemetry/commands/crons/index.ts
var CronsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandRun(actual) {
    this.trackCliSubcommand({
      subcommand: "run",
      value: actual
    });
  }
};

// src/commands/crons/index.ts
var COMMAND_CONFIG11 = {
  add: ["add"],
  ls: ["ls", "list"],
  run: ["run"]
};
async function main7(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(cronsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new CronsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG11
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("crons");
    output_manager_default.print(help(cronsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: cronsCommand, columns: client.stderr.columns })
    );
    return 2;
  }
  switch (subcommand) {
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("crons", subcommandOriginal);
        return printHelp(addSubcommand2);
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add2(client, args);
    case "run":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("crons", subcommandOriginal);
        return printHelp(runSubcommand);
      }
      telemetry2.trackCliSubcommandRun(subcommandOriginal);
      return run(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("crons", subcommandOriginal);
        return printHelp(listSubcommand5);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls3(client, args);
  }
}

// src/commands/curl/index.ts
import { spawn as spawn2 } from "child_process";

// src/commands/curl/utils.ts
var requoteArgs = (arg) => {
  if (arg.includes(" ")) {
    if (arg.includes('"')) {
      return `'${arg}'`;
    } else {
      return `"${arg}"`;
    }
  }
  return arg;
};

// src/util/telemetry/commands/curl/index.ts
var CurlTelemetryClient = class extends TelemetryClient {
  trackCliArgumentPath(path3) {
    if (path3) {
      const value = path3.startsWith("/") ? "slash" : "no-slash";
      this.trackCliArgument({
        arg: "path",
        value
      });
    }
  }
  trackCliOptionDeployment(deploymentId) {
    if (deploymentId) {
      const value = deploymentId.startsWith("http://") || deploymentId.startsWith("https://") ? "url" : deploymentId.startsWith("dpl_") ? "dpl_" : "no-prefix";
      this.trackCliOption({
        option: "deployment",
        value
      });
    }
  }
  trackCliOptionProtectionBypass(secret) {
    if (secret) {
      this.trackCliOption({
        option: "protection-bypass",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliFlagTrace(trace2) {
    if (trace2) {
      this.trackCliFlag("trace");
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
  /**
   * Fired only when --yes is supplied AND the target is a production
   * deployment. Lets us distinguish "user explicitly confirmed prod via
   * --yes" from generic --yes usage on previews.
   */
  trackCliFlagYesOnProduction(value) {
    if (value) {
      this.trackCliFlag("yes-on-production");
    }
  }
};

// src/commands/curl/shared.ts
var import_chalk41 = __toESM(require_source(), 1);
var import_error_utils6 = __toESM(require_dist(), 1);

// src/commands/curl/bypass-token.ts
var import_chalk40 = __toESM(require_source(), 1);
async function createDeploymentProtectionToken(client, projectId, orgId) {
  if (!client.authConfig.token) {
    output_manager_default.debug(
      "No auth token available, skipping deployment protection token"
    );
    throw new Error(
      "Authentication required to create protection bypass token"
    );
  }
  try {
    const response = await client.fetch(`/v1/projects/${projectId}/protection-bypass`, {
      method: "PATCH",
      body: "{}",
      headers: {
        "Content-Type": "application/json"
      },
      accountId: orgId
    });
    const { protectionBypass } = response;
    output_manager_default.log(
      `You require a deployment protection bypass token to access this deployment... Generating one now...`
    );
    output_manager_default.log(
      `Successfully generated deployment protection bypass token for project ${import_chalk40.default.bold(projectId)}
`
    );
    output_manager_default.debug(`Protection Bypass Response: ${protectionBypass}`);
    await sleep(1e3);
    return getAutomationBypassToken(protectionBypass);
  } catch (error) {
    output_manager_default.debug(
      `Failed to generate deployment protection bypass token: ${error}`
    );
    output_manager_default.note(
      'To bypass deployment protection, create a "Protection Bypass for Automation" secret in your project settings:'
    );
    output_manager_default.log(`  1. Visit ${import_chalk40.default.cyan("https://vercel.com/dashboard")}`);
    output_manager_default.log(`  2. Go to your project settings \u2192 Deployment Protection`);
    output_manager_default.log(`  3. Generate a "Protection Bypass for Automation" secret`);
    output_manager_default.log(
      `  4. Use it with ${import_chalk40.default.cyan(
        "--protection-bypass"
      )} flag or set ${import_chalk40.default.cyan("VERCEL_AUTOMATION_BYPASS_SECRET")} env var`
    );
    output_manager_default.log("");
    throw new Error("Failed to create deployment protection bypass token");
  }
}
function getAutomationBypassToken(protectionBypass) {
  if (!protectionBypass) {
    throw new Error("No protection bypass tokens found");
  }
  const token2 = Object.keys(protectionBypass).find(
    (key) => protectionBypass[key].scope === "automation-bypass"
  );
  if (!token2) {
    throw new Error(
      "No automation bypass token found in protection bypass settings"
    );
  }
  return token2;
}
async function getOrCreateDeploymentProtectionToken(client, { project, org }) {
  if (process.env.VERCEL_AUTOMATION_BYPASS_SECRET) {
    output_manager_default.debug("Using protection bypass secret from environment variable");
    return process.env.VERCEL_AUTOMATION_BYPASS_SECRET;
  }
  if (project.protectionBypass && Object.values(project.protectionBypass).length) {
    const protectionBypass = getAutomationBypassToken(project.protectionBypass);
    if (protectionBypass) {
      output_manager_default.debug(
        `Using existing protection bypass token from project settings: ${protectionBypass}`
      );
      return protectionBypass;
    }
  }
  const token2 = await createDeploymentProtectionToken(
    client,
    project.id,
    org.id
  );
  return token2;
}

// src/commands/curl/deployment-url.ts
async function getDeploymentUrlById(client, deploymentIdOrUrl, accountId) {
  try {
    if (deploymentIdOrUrl.startsWith("http://") || deploymentIdOrUrl.startsWith("https://")) {
      try {
        const url = new URL(deploymentIdOrUrl);
        return url.origin;
      } catch (_err) {
        output_manager_default.debug(`Invalid deployment URL provided: ${deploymentIdOrUrl}`);
        return null;
      }
    }
    if (deploymentIdOrUrl.includes("vercel.app")) {
      return `https://${deploymentIdOrUrl}`;
    }
    let fullDeploymentId = deploymentIdOrUrl;
    if (!fullDeploymentId.startsWith("dpl_")) {
      fullDeploymentId = `dpl_${deploymentIdOrUrl}`;
    }
    const deployment = await client.fetch(
      `/v13/deployments/${fullDeploymentId}`,
      { accountId }
    );
    if (!deployment || !deployment.url) {
      return null;
    }
    return `https://${deployment.url}`;
  } catch (error) {
    output_manager_default.debug(`Failed to fetch deployment by ID: ${error}`);
    return null;
  }
}

// src/commands/curl/shared.ts
function looksLikeHostname(path3) {
  const firstSegment = path3.split("/")[0];
  return firstSegment.includes(".") && !firstSegment.startsWith(".");
}
function orgFromOwner(id, slug = id) {
  return { type: id.startsWith("team_") ? "team" : "user", id, slug };
}
var VC_STRING_FLAGS = /* @__PURE__ */ new Set(["--deployment", "--protection-bypass"]);
var VC_BOOLEAN_FLAGS = /* @__PURE__ */ new Set(["--yes", "--help", "--trace", "--json"]);
function flagName(arg) {
  const eqIdx = arg.indexOf("=");
  return eqIdx === -1 ? arg : arg.slice(0, eqIdx);
}
function flagValue(args, index) {
  const arg = args[index];
  const eqIdx = arg.indexOf("=");
  if (eqIdx !== -1) {
    return arg.slice(eqIdx + 1);
  }
  const next = args[index + 1];
  return next && !next.startsWith("-") ? next : void 0;
}
function parseCurlLikeArgs(rawArgs, commandName) {
  const result = {
    target: void 0,
    deployment: void 0,
    protectionBypass: void 0,
    yes: false,
    help: false,
    trace: false,
    json: false,
    toolFlags: []
  };
  const args = rawArgs[0] === commandName ? rawArgs.slice(1) : [...rawArgs];
  const separatorIndex = args.indexOf("--");
  const beforeSeparator = separatorIndex === -1 ? args : args.slice(0, separatorIndex);
  const afterSeparator = separatorIndex === -1 ? [] : args.slice(separatorIndex + 1);
  for (let i = 0; i < beforeSeparator.length; i++) {
    const arg = beforeSeparator[i];
    const name = flagName(arg);
    if (VC_STRING_FLAGS.has(name)) {
      const value = flagValue(beforeSeparator, i);
      if (!arg.includes("=") && value !== void 0) {
        i++;
      }
      if (name === "--deployment") {
        result.deployment = value;
      } else {
        result.protectionBypass = value;
      }
      continue;
    }
    if (VC_BOOLEAN_FLAGS.has(name)) {
      if (name === "--yes") {
        result.yes = true;
      } else if (name === "--trace") {
        result.trace = true;
      } else if (name === "--json") {
        result.json = true;
      } else {
        result.help = true;
      }
      continue;
    }
    if (!result.target && name === "--url") {
      const value = flagValue(beforeSeparator, i);
      if (!arg.includes("=") && value !== void 0) {
        i++;
      }
      result.target = value;
      continue;
    }
    if (!result.target && !arg.startsWith("-")) {
      result.target = arg;
    } else {
      result.toolFlags.push(arg);
    }
  }
  result.toolFlags.push(...afterSeparator);
  return result;
}
function setupCurlLikeCommand(client, command, telemetryClient, options = {}) {
  const { print } = output_manager_default;
  if (options.allowFullUrl) {
    const parsed = parseCurlLikeArgs(client.argv.slice(2), command.name);
    if (parsed.help) {
      print(help(command, { columns: client.stderr.columns }));
      return 2;
    }
    const path4 = parsed.target;
    telemetryClient.trackCliArgumentPath(path4);
    if (parsed.deployment) {
      telemetryClient.trackCliOptionDeployment(parsed.deployment);
    }
    if (parsed.protectionBypass) {
      telemetryClient.trackCliOptionProtectionBypass(parsed.protectionBypass);
    }
    if (!path4) {
      output_manager_default.error(
        `${getCommandName(`${command.name} <url|path>`)} requires a URL or API path (e.g., 'https://example.vercel.app/api/hello' or '/api/hello')`
      );
      print(help(command, { columns: client.stderr.columns }));
      return 1;
    }
    let isFullUrl2 = path4.startsWith("http://") || path4.startsWith("https://");
    if (!isFullUrl2 && looksLikeHostname(path4)) {
      isFullUrl2 = true;
    }
    output_manager_default.debug(
      `${command.name} flags (${parsed.toolFlags.length} args): ${JSON.stringify(parsed.toolFlags)}`
    );
    return {
      path: isFullUrl2 && !path4.startsWith("http") ? `https://${path4}` : path4,
      isFullUrl: isFullUrl2,
      deploymentFlag: parsed.deployment,
      protectionBypassFlag: parsed.protectionBypass,
      toolFlags: parsed.toolFlags,
      yes: parsed.yes,
      trace: parsed.trace,
      json: parsed.json
    };
  }
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(command.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { flags } = parsedArgs;
  if (parsedArgs.flags["--help"]) {
    print(help(command, { columns: client.stderr.columns }));
    return 2;
  }
  if (parsedArgs.args[0] === command.name) {
    parsedArgs.args.shift();
  }
  const separatorIndex = process.argv.indexOf("--");
  const path3 = parsedArgs.args[0];
  telemetryClient.trackCliArgumentPath(path3);
  const deploymentFlag = flags["--deployment"];
  if (deploymentFlag) {
    telemetryClient.trackCliOptionDeployment(deploymentFlag);
  }
  const protectionBypassFlag = flags["--protection-bypass"];
  if (protectionBypassFlag) {
    telemetryClient.trackCliOptionProtectionBypass(protectionBypassFlag);
  }
  if (!path3 || path3 === "--" || path3.startsWith("-")) {
    if (options.allowFullUrl) {
      output_manager_default.error(
        `${getCommandName(`${command.name} <url|path>`)} requires a URL or API path (e.g., 'https://example.vercel.app/api/hello' or '/api/hello')`
      );
    } else {
      output_manager_default.error(
        `${getCommandName(`${command.name} <path>`)} requires an API path (e.g., '/' or '/api/hello' or 'api/hello')`
      );
    }
    print(help(command, { columns: client.stderr.columns }));
    return 1;
  }
  let isFullUrl = path3.startsWith("http://") || path3.startsWith("https://");
  if (!isFullUrl && looksLikeHostname(path3)) {
    isFullUrl = true;
  }
  if (isFullUrl && !options.allowFullUrl) {
    output_manager_default.error(
      `The <path> argument must be a relative API path (e.g., '/' or '/api/hello'), not a full URL.`
    );
    output_manager_default.print(
      `To target a specific deployment within the currently linked project, use the --deployment <id|url> flag.`
    );
    print(help(command, { columns: client.stderr.columns }));
    return 1;
  }
  const toolFlags = separatorIndex !== -1 ? process.argv.slice(separatorIndex + 1) : [];
  output_manager_default.debug(
    `${command.name} flags (${toolFlags.length} args): ${JSON.stringify(toolFlags)}`
  );
  return {
    path: isFullUrl && !path3.startsWith("http") ? `https://${path3}` : path3,
    isFullUrl,
    deploymentFlag,
    protectionBypassFlag,
    toolFlags,
    yes: !!flags["--yes"],
    trace: !!flags["--trace"],
    json: !!flags["--json"]
  };
}
async function resolveProjectFromUrl(client, url) {
  const host = toHost(url);
  for (const useCurrentTeam of [void 0, false]) {
    try {
      const deployment = await client.fetch(
        `/v13/deployments/${encodeURIComponent(host)}`,
        { useCurrentTeam }
      );
      if (deployment.projectId && deployment.ownerId) {
        const project = await client.fetch(
          `/v9/projects/${encodeURIComponent(deployment.projectId)}`,
          { accountId: deployment.ownerId }
        );
        return {
          status: "linked",
          project,
          org: orgFromOwner(deployment.ownerId)
        };
      }
    } catch (err) {
      output_manager_default.debug(`Deployment lookup failed for ${host}: ${err}`);
    }
  }
  try {
    const aliasUrl = `/now/aliases/${encodeURIComponent(host)}`;
    try {
      const alias2 = await client.fetch(aliasUrl, { useCurrentTeam: false });
      if (alias2.projectId && alias2.ownerId) {
        const project = await client.fetch(
          `/v9/projects/${encodeURIComponent(alias2.projectId)}`,
          { accountId: alias2.ownerId }
        );
        return {
          status: "linked",
          project,
          org: orgFromOwner(alias2.ownerId)
        };
      }
    } catch (err) {
      output_manager_default.debug(`User alias lookup failed for ${host}: ${err}`);
    }
    const teams2 = (await getTeams(client)).filter((team) => !team.limited);
    for (const team of teams2) {
      try {
        const alias2 = await client.fetch(aliasUrl, { accountId: team.id });
        const projectId = alias2.projectId;
        const ownerId = alias2.ownerId || team.id;
        if (projectId) {
          const project = await client.fetch(
            `/v9/projects/${encodeURIComponent(projectId)}`,
            { accountId: ownerId }
          );
          return {
            status: "linked",
            project,
            org: orgFromOwner(ownerId, team.slug)
          };
        }
      } catch (err) {
        output_manager_default.debug(`Alias lookup failed for ${host} in ${team.slug}: ${err}`);
      }
    }
  } catch (err) {
    output_manager_default.debug(`Team lookup failed for ${host}: ${err}`);
  }
  return null;
}
async function getFullUrlAndToken(client, fullUrl, protectionBypassFlag) {
  if (protectionBypassFlag) {
    return {
      fullUrl,
      deploymentProtectionToken: protectionBypassFlag,
      link: null
    };
  }
  if (process.env.VERCEL_AUTOMATION_BYPASS_SECRET) {
    output_manager_default.debug("Using protection bypass secret from environment variable");
    return {
      fullUrl,
      deploymentProtectionToken: process.env.VERCEL_AUTOMATION_BYPASS_SECRET,
      link: null
    };
  }
  const link = await resolveProjectFromUrl(client, fullUrl);
  let deploymentProtectionToken = null;
  if (link) {
    try {
      deploymentProtectionToken = await getOrCreateDeploymentProtectionToken(
        client,
        link
      );
    } catch (err) {
      output_manager_default.debug(`Failed to get deployment protection bypass token: ${err}`);
    }
  }
  return {
    fullUrl,
    deploymentProtectionToken,
    link
  };
}
async function getDeploymentUrlAndToken(client, commandName, path3, options) {
  const { deploymentFlag, protectionBypassFlag, autoConfirm } = options;
  let link;
  let scope;
  try {
    scope = await getScope(client);
  } catch (err) {
    if ((0, import_error_utils6.isErrnoException)(err) && (err.code === "NOT_AUTHORIZED" || err.code === "TEAM_DELETED")) {
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
  try {
    link = await ensureLink(commandName, client, client.cwd, {
      autoConfirm
    });
  } catch (err) {
    if ((0, import_error_utils6.isErrnoException)(err) && err.code === "NOT_AUTHORIZED") {
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
  if (typeof link === "number") {
    return link;
  }
  const { project } = link;
  const linkedProject = await getLinkedProject(client, client.cwd);
  if (linkedProject.status !== "linked") {
    output_manager_default.error("This command requires a linked project. Please run:");
    output_manager_default.print("  vercel link");
    return 1;
  }
  if (!linkedProject.project || !linkedProject.org) {
    output_manager_default.error("Failed to get project information");
    return 1;
  }
  const preferredAlias = linkedProject.project.targets?.production?.alias?.[0];
  const backupAlias = linkedProject.project.latestDeployments?.[0]?.url;
  const target = preferredAlias || backupAlias;
  let baseUrl;
  if (deploymentFlag) {
    const accountId = scope.team?.id || scope.user.id;
    const deploymentUrl = await getDeploymentUrlById(
      client,
      deploymentFlag,
      accountId
    );
    if (!deploymentUrl) {
      output_manager_default.error(`No deployment found for ID "${deploymentFlag}"`);
      return 1;
    }
    baseUrl = deploymentUrl;
  } else if (target) {
    baseUrl = `https://${target}`;
  } else {
    throw new Error("No deployment URL found for the project");
  }
  const fullUrl = `${baseUrl}${path3.startsWith("/") ? path3 : `/${path3}`}`;
  output_manager_default.debug(`${import_chalk41.default.cyan("Target URL:")} ${import_chalk41.default.bold(fullUrl)}`);
  let deploymentProtectionToken = null;
  if (project.id) {
    try {
      deploymentProtectionToken = protectionBypassFlag ?? await getOrCreateDeploymentProtectionToken(client, link);
    } catch (err) {
      output_manager_default.error(
        `Failed to get deployment protection bypass token: ${err instanceof Error ? err.message : String(err)}`
      );
      return 1;
    }
  }
  return {
    fullUrl,
    deploymentProtectionToken,
    link
  };
}

// src/commands/curl/trace.ts
var import_error_utils7 = __toESM(require_dist(), 1);
import { spawn } from "child_process";
import { tmpdir } from "os";
import { join as join3 } from "path";
import { mkdtemp, readFile as readFile5, rm as rm3 } from "fs/promises";

// src/commands/curl/confirm-production.ts
async function confirmProduction(client, { deploymentTarget, yes, isTTY }) {
  if (deploymentTarget !== "production") {
    return "proceed";
  }
  if (yes) {
    return "proceed";
  }
  if (!isTTY) {
    output_manager_default.error(
      "Use --yes to capture traces on production from non-interactive contexts."
    );
    return "non-tty-no-yes";
  }
  const confirmed = await client.input.confirm(
    "You are about to capture a trace against a production deployment. Continue?",
    false
  );
  return confirmed ? "proceed" : "declined";
}

// src/commands/curl/trace-session-token-provider.ts
import { createHash as createHash3 } from "crypto";
import { chmod, mkdir, readFile as readFile4, unlink, writeFile as writeFile3 } from "fs/promises";
import { dirname, join as join2 } from "path";
var FALLBACK_TTL_MS = 5 * 60 * 1e3;
var EXPIRY_BUFFER_MS = 30 * 1e3;
var CACHE_SCHEMA_VERSION = 1;
function resolveCacheDir(override2) {
  if (override2)
    return override2;
  return join2(getGlobalPathConfig(), "cache", "traces");
}
function cacheFilename(teamId, host) {
  const key = `${teamId ?? ""}:${host}`;
  return `${createHash3("sha256").update(key).digest("hex")}.json`;
}
function cachePath(cacheDir, teamId, host) {
  return join2(cacheDir, cacheFilename(teamId, host));
}
async function readCache(path3) {
  let raw;
  try {
    raw = await readFile4(path3, "utf8");
  } catch {
    return null;
  }
  try {
    const parsed = JSON.parse(raw);
    if (!parsed || typeof parsed.token !== "string" || typeof parsed.expiresAt !== "number" || parsed.schemaVersion !== CACHE_SCHEMA_VERSION) {
      return null;
    }
    return parsed;
  } catch {
    output_manager_default.debug(`Trace cookie cache file is corrupt at ${path3}; re-issuing.`);
    return null;
  }
}
async function writeCache(path3, entry) {
  await mkdir(dirname(path3), { recursive: true });
  await writeFile3(path3, JSON.stringify(entry), { mode: 384 });
  await chmod(path3, 384);
}
async function issueToken(client, {
  teamId,
  projectId,
  hostname
}) {
  const body = JSON.stringify({ projectId, hostname });
  const response = await client.fetch(
    "/v1/projects/traces/session",
    {
      method: "POST",
      body,
      headers: { "Content-Type": "application/json" },
      accountId: teamId
    }
  );
  if (!response?.token) {
    throw new Error("Trace session response is missing a token");
  }
  const expiresAt = typeof response.expiresAt === "number" ? response.expiresAt : Date.now() + FALLBACK_TTL_MS;
  return { token: response.token, expiresAt };
}
async function getTraceSessionToken({
  client,
  teamId,
  projectId,
  host,
  evictedToken,
  cacheDir
}) {
  const dir = resolveCacheDir(cacheDir);
  const hostname = toHost(host);
  const path3 = cachePath(dir, teamId, hostname);
  const cached = await readCache(path3);
  if (cached && evictedToken && cached.token === evictedToken) {
    await unlink(path3).catch(() => {
    });
  } else if (cached && Date.now() < cached.expiresAt - EXPIRY_BUFFER_MS) {
    return {
      token: cached.token,
      expiresAt: cached.expiresAt,
      fromCache: true
    };
  }
  const issued = await issueToken(client, { teamId, projectId, hostname });
  try {
    await writeCache(path3, {
      token: issued.token,
      expiresAt: issued.expiresAt,
      schemaVersion: CACHE_SCHEMA_VERSION
    });
  } catch (err) {
    output_manager_default.debug(`Failed to write trace cookie cache: ${err}`);
  }
  return {
    token: issued.token,
    expiresAt: issued.expiresAt,
    fromCache: false
  };
}

// src/commands/curl/trace.ts
var TRACE_COOKIE_NAME = "_vercel_tracing";
var SESSION_COOKIE_NAME = "_vercel_session";
async function lookupDeployment(client, fullUrl, accountId) {
  const host = toHost(fullUrl);
  return client.fetch(
    `/v13/deployments/${encodeURIComponent(host)}`,
    accountId ? { accountId } : {}
  );
}
function resolveTeamId(link, deployment) {
  if (link?.org.type === "team") {
    return link.org.id;
  }
  if (deployment.ownerId?.startsWith("team_")) {
    return deployment.ownerId;
  }
  return void 0;
}
function extractVercelId(headerDump) {
  const lines = headerDump.split(/\r?\n/);
  let lastValue;
  for (const line of lines) {
    const idx = line.indexOf(":");
    if (idx === -1)
      continue;
    const name = line.slice(0, idx).trim().toLowerCase();
    if (name === "x-vercel-id") {
      lastValue = line.slice(idx + 1).trim();
    }
  }
  if (!lastValue)
    return void 0;
  const sepIdx = lastValue.lastIndexOf("::");
  return sepIdx === -1 ? lastValue : lastValue.slice(sepIdx + 2);
}
function extractFinalStatus(headerDump) {
  const lines = headerDump.split(/\r?\n/);
  let lastStatus;
  for (const line of lines) {
    const match = line.match(/^HTTP\/[\d.]+\s+(\d{3})/);
    if (match) {
      lastStatus = Number(match[1]);
    }
  }
  return lastStatus;
}
async function runCurlAndCaptureHeaders(curlFlags, json) {
  const tmpDir = await mkdtemp(join3(tmpdir(), "vc-curl-trace-"));
  const headerFile = join3(tmpDir, "headers");
  const flags = ["--dump-header", headerFile, ...curlFlags];
  try {
    const { exitCode: exitCode2, capturedBody } = await new Promise((resolve8) => {
      const child = spawn("curl", flags, {
        stdio: json ? ["inherit", "pipe", "inherit"] : "inherit",
        shell: false
      });
      let buf = "";
      if (json && child.stdout) {
        child.stdout.setEncoding("utf8");
        child.stdout.on("data", (chunk) => {
          buf += chunk;
        });
      }
      child.on("error", (err) => {
        if (err.code === "ENOENT") {
          output_manager_default.error("curl command not found. Please install curl.");
        } else {
          output_manager_default.error(`Failed to execute curl: ${err.message}`);
        }
        resolve8({ exitCode: 1, capturedBody: buf });
      });
      child.on("close", (code2) => {
        resolve8({ exitCode: code2 ?? 1, capturedBody: buf });
      });
    });
    let headerDump = "";
    try {
      headerDump = await readFile5(headerFile, "utf8");
    } catch (err) {
      output_manager_default.debug(`Failed to read curl header dump: ${err}`);
    }
    return { exitCode: exitCode2, headerDump, capturedBody };
  } finally {
    await rm3(tmpDir, { recursive: true, force: true }).catch(() => {
    });
  }
}
async function trace(client, { fullUrl, link, curlFlags, json, yes, telemetry: telemetry2 }) {
  telemetry2.trackCliFlagTrace(true);
  if (json) {
    telemetry2.trackCliFlagJson(true);
  }
  const accountId = link?.org.id;
  let deployment;
  try {
    deployment = await lookupDeployment(client, fullUrl, accountId);
  } catch (err) {
    output_manager_default.error(
      `Failed to look up deployment for tracing: ${err instanceof Error ? err.message : String(err)}`
    );
    return 1;
  }
  const projectId = link?.project.id ?? deployment.projectId;
  if (!projectId) {
    output_manager_default.error(
      "Could not resolve project for trace session. Run `vercel link` and retry."
    );
    return 1;
  }
  const teamId = resolveTeamId(link, deployment);
  let userId;
  try {
    userId = client.authConfig.userId ?? (await getUser(client)).id;
  } catch (err) {
    output_manager_default.error(
      `Failed to resolve user for trace session: ${(0, import_error_utils7.errorToString)(err)}`
    );
    return 1;
  }
  const deploymentTarget = deployment.target === "production" ? "production" : "preview";
  const confirmation = await confirmProduction(client, {
    deploymentTarget,
    yes,
    isTTY: !!client.stdin.isTTY
  });
  if (confirmation === "non-tty-no-yes") {
    return 1;
  }
  if (confirmation === "declined") {
    output_manager_default.log("Trace cancelled.");
    return 0;
  }
  if (deploymentTarget === "production" && yes) {
    telemetry2.trackCliFlagYesOnProduction(true);
  }
  let session;
  try {
    session = await getTraceSessionToken({
      client,
      teamId,
      projectId,
      host: fullUrl
    });
  } catch (err) {
    output_manager_default.error(
      `Failed to acquire trace session: ${err instanceof Error ? err.message : String(err)}`
    );
    return 1;
  }
  let { exitCode: exitCode2, headerDump, capturedBody } = await runCurlWithCookie(
    session.token,
    userId,
    curlFlags,
    json
  );
  if (session.fromCache && extractFinalStatus(headerDump) === 401 && exitCode2 === 0) {
    output_manager_default.debug("Trace cookie returned 401; evicting cache and retrying.");
    let refreshed;
    try {
      refreshed = await getTraceSessionToken({
        client,
        teamId,
        projectId,
        host: fullUrl,
        evictedToken: session.token
      });
    } catch (err) {
      output_manager_default.error(
        `Failed to refresh trace session after 401: ${err instanceof Error ? err.message : String(err)}`
      );
      return 1;
    }
    ({ exitCode: exitCode2, headerDump, capturedBody } = await runCurlWithCookie(
      refreshed.token,
      userId,
      curlFlags,
      json
    ));
  }
  const requestId = extractVercelId(headerDump);
  if (json) {
    client.stdout.write(
      `${JSON.stringify({ response: capturedBody, requestId: requestId ?? null }, null, 2)}
`
    );
    return requestId ? exitCode2 : exitCode2 === 0 ? 1 : exitCode2;
  }
  if (!requestId) {
    output_manager_default.error(
      "Trace cookie was set but the response did not include an x-vercel-id header."
    );
    return exitCode2 === 0 ? 1 : exitCode2;
  }
  client.stderr.write(
    `
Run \`vercel traces get ${requestId}\` to fetch the trace.
`
  );
  return exitCode2;
}
async function runCurlWithCookie(token2, userId, curlFlags, json) {
  const flagsWithCookie = [
    "--header",
    `Cookie: ${TRACE_COOKIE_NAME}=${token2}; ${SESSION_COOKIE_NAME}=${userId}`,
    ...curlFlags
  ];
  output_manager_default.debug(`Executing: curl ${flagsWithCookie.map(requoteArgs).join(" ")}`);
  return runCurlAndCaptureHeaders(flagsWithCookie, json);
}

// src/commands/curl/index.ts
async function curl(client) {
  const telemetryClient = new CurlTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const setup = setupCurlLikeCommand(client, curlCommand, telemetryClient, {
    allowFullUrl: true
  });
  if (typeof setup === "number") {
    return setup;
  }
  const {
    path: path3,
    isFullUrl,
    deploymentFlag,
    protectionBypassFlag,
    toolFlags,
    trace: traceFlag,
    json: jsonFlag
  } = setup;
  const result = isFullUrl ? await getFullUrlAndToken(client, path3, protectionBypassFlag) : await getDeploymentUrlAndToken(client, "curl", path3, {
    deploymentFlag,
    protectionBypassFlag,
    autoConfirm: setup.yes
  });
  if (typeof result === "number") {
    return result;
  }
  const { fullUrl, deploymentProtectionToken } = result;
  const curlFlags = [...toolFlags];
  if (deploymentProtectionToken) {
    curlFlags.unshift(
      "--header",
      `x-vercel-protection-bypass: ${deploymentProtectionToken}`
    );
  }
  curlFlags.unshift("--url", fullUrl);
  if (traceFlag) {
    return trace(client, {
      fullUrl,
      link: result.link ?? null,
      curlFlags,
      json: jsonFlag,
      yes: setup.yes,
      telemetry: telemetryClient
    });
  }
  output_manager_default.debug(`Executing: curl ${curlFlags.map(requoteArgs).join(" ")}`);
  return new Promise((resolve8) => {
    const curlProcess = spawn2("curl", curlFlags, {
      stdio: "inherit",
      shell: false
    });
    curlProcess.on("error", (err) => {
      if ("code" in err && err.code === "ENOENT") {
        output_manager_default.error("curl command not found. Please install curl.");
        resolve8(1);
      } else {
        output_manager_default.error(`Failed to execute curl: ${err.message}`);
        resolve8(1);
      }
    });
    curlProcess.on("close", (code2) => {
      resolve8(code2 ?? 1);
    });
  });
}

// src/commands/deploy-hooks/ls.ts
var import_chalk42 = __toESM(require_source(), 1);

// src/util/projects/get-project-by-cwd-or-link.ts
async function getProjectByCwdOrLink({
  autoConfirm,
  nonInteractive,
  client,
  commandName,
  cwd,
  projectNameOrId,
  forReadOnlyCommand
}) {
  if (projectNameOrId) {
    const project = await getProjectByNameOrId(client, projectNameOrId);
    if (project instanceof ProjectNotFound) {
      throw project;
    }
    return project;
  }
  const effectiveNonInteractive = nonInteractive ?? client.nonInteractive ?? argvHasNonInteractive(client.argv);
  if (forReadOnlyCommand && effectiveNonInteractive) {
    const resolvedCwd = await resolveProjectCwd(cwd ?? client.cwd);
    const link = await getLinkedProject(client, resolvedCwd);
    if (link.status === "linked" && link.project) {
      return link.project;
    }
    if (link.status === "error") {
      const err = new Error("Link project error");
      err.code = "ERR_LINK_PROJECT";
      throw err;
    }
    throw new LinkRequiredError();
  }
  const linkedProject = await ensureLink(
    commandName,
    client,
    cwd ?? client.cwd,
    {
      autoConfirm,
      nonInteractive
    }
  );
  if (typeof linkedProject === "number") {
    const err = new Error("Link project error");
    err.code = "ERR_LINK_PROJECT";
    throw err;
  }
  return linkedProject.project;
}

// src/util/telemetry/commands/deploy-hooks/ls.ts
var DeployHooksLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/deploy-hooks/ls.ts
async function ls4(client, argv) {
  const telemetry2 = new DeployHooksLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand6.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "deploy-hooks ls",
    args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  telemetry2.trackCliOptionFormat(opts["--format"]);
  telemetry2.trackCliOptionProject(opts["--project"]);
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || client.nonInteractive;
  const { contextName } = await getScope(client);
  const lsStamp = stamp_default();
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Fetching deploy hooks under ${import_chalk42.default.bold(contextName)}`);
  }
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "deploy-hooks ls",
      projectNameOrId: opts["--project"],
      forReadOnlyCommand: true
    });
    const hooks = project.link?.deployHooks ?? [];
    if (asJson) {
      output_manager_default.stopSpinner();
      const rows2 = hooks.map((h) => ({
        id: h.id,
        name: h.name,
        ref: h.ref,
        url: h.url,
        createdAt: h.createdAt
      }));
      if (client.nonInteractive) {
        client.stdout.write(
          `${JSON.stringify(
            {
              status: AGENT_STATUS.OK,
              projectId: project.id,
              projectName: project.name,
              hooks: rows2,
              message: hooks.length === 0 ? "No deploy hooks for this project." : `Listed ${hooks.length} deploy hook${hooks.length === 1 ? "" : "s"}.`,
              next: [
                {
                  command: getCommandNamePlain(
                    "deploy-hooks create <name> --ref <branch>"
                  ),
                  when: "Create a deploy hook"
                }
              ]
            },
            null,
            2
          )}
`
        );
      } else {
        client.stdout.write(`${JSON.stringify(rows2, null, 2)}
`);
      }
      return 0;
    }
    output_manager_default.stopSpinner();
    if (hooks.length === 0) {
      output_manager_default.log(
        `No deploy hooks configured for ${import_chalk42.default.bold(project.name)} ${lsStamp()}`
      );
      return 0;
    }
    output_manager_default.log(
      `Deploy hooks for ${import_chalk42.default.bold(project.name)} ${import_chalk42.default.gray(lsStamp())}
`
    );
    const rows = hooks.map((h) => [h.name, h.id, h.ref, h.url]);
    output_manager_default.print(
      `${formatTable(["name", "id", "ref", "url"], ["l", "l", "l", "l"], [{ rows }])}
`
    );
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
}

// src/commands/deploy-hooks/create.ts
var import_chalk43 = __toESM(require_source(), 1);

// src/util/telemetry/commands/deploy-hooks/create.ts
var DeployHooksCreateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(name) {
    if (name) {
      this.trackCliArgument({
        arg: "name",
        value: name
      });
    }
  }
  trackCliOptionRef(ref) {
    if (ref) {
      this.trackCliOption({
        option: "ref",
        value: ref
      });
    }
  }
};

// src/commands/deploy-hooks/create.ts
async function create3(client, argv) {
  const telemetry2 = new DeployHooksCreateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(createSubcommand3.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  let [nameArg] = args;
  let ref = opts["--ref"];
  telemetry2.trackCliArgumentName(nameArg);
  telemetry2.trackCliOptionRef(ref);
  telemetry2.trackCliOptionProject(opts["--project"]);
  const { contextName } = await getScope(client);
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Loading project under ${import_chalk43.default.bold(contextName)}`);
  }
  let project;
  try {
    project = await getProjectByCwdOrLink({
      client,
      commandName: "deploy-hooks create",
      projectNameOrId: opts["--project"]
    });
  } catch (err) {
    output_manager_default.stopSpinner();
    exitWithNonInteractiveError(client, err, 1, { variant: "checks" });
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  if (!project.link) {
    const message = "This project is not connected to a Git repository, so it cannot have deploy hooks. Connect a repo under Project Settings \u2192 Git.";
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message
        },
        1
      );
    }
    output_manager_default.error(message);
    return 1;
  }
  if (!nameArg) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: "Deploy hook name is required. Pass it as the first argument after `create`.",
          next: [
            {
              command: getCommandNamePlain(
                "deploy-hooks create <name> --ref <branch>"
              )
            }
          ]
        },
        1
      );
    }
    nameArg = await client.input.text({
      message: "Deploy hook name:",
      validate: (val) => {
        if (!val?.trim())
          return "Name is required";
        if (val.trim().length > 180)
          return "Name must be at most 180 characters";
        return true;
      }
    });
  }
  const name = nameArg.trim();
  if (!name) {
    output_manager_default.error("Deploy hook name cannot be empty.");
    return 1;
  }
  if (!ref) {
    const defaultRef = project.link.productionBranch && project.link.productionBranch.trim() ? project.link.productionBranch.trim() : "main";
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: "Git branch ref is required in non-interactive mode. Pass `--ref <branch>`.",
          next: [
            {
              command: getCommandNamePlain(
                `deploy-hooks create ${name} --ref <branch>`
              )
            }
          ]
        },
        1
      );
    }
    ref = await client.input.text({
      message: "Git branch ref to deploy:",
      default: defaultRef,
      validate: (val) => val?.trim() ? true : "Branch ref is required"
    });
  }
  const refTrimmed = ref.trim();
  if (!refTrimmed) {
    output_manager_default.error("Branch ref cannot be empty.");
    return 1;
  }
  const previousIds = new Set((project.link.deployHooks ?? []).map((h) => h.id));
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Creating deploy hook on ${import_chalk43.default.bold(project.name)}`);
  }
  const createStamp = stamp_default();
  try {
    const updated = await client.fetch(
      `/v2/projects/${encodeURIComponent(project.id)}/deploy-hooks`,
      {
        method: "POST",
        body: { name, ref: refTrimmed }
      }
    );
    const created = (updated.link?.deployHooks ?? []).find(
      (h) => !previousIds.has(h.id)
    );
    output_manager_default.stopSpinner();
    if (client.nonInteractive) {
      client.stdout.write(
        `${JSON.stringify(
          {
            status: AGENT_STATUS.OK,
            hook: created ?? { name, ref: refTrimmed },
            projectId: project.id,
            projectName: project.name,
            message: created ? `Created deploy hook ${created.id} on ${project.name}.` : `Created deploy hook on ${project.name}.`,
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "deploy-hooks ls"
                ),
                when: "List deploy hooks for this project"
              }
            ]
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.success(
      `Deploy hook created on ${import_chalk43.default.bold(project.name)} ${createStamp()}`
    );
    if (created) {
      output_manager_default.print("\n");
      output_manager_default.print(`  ${import_chalk43.default.cyan("Name".padEnd(12))}${created.name}
`);
      output_manager_default.print(`  ${import_chalk43.default.cyan("Branch".padEnd(12))}${created.ref}
`);
      output_manager_default.print(`  ${import_chalk43.default.cyan("ID".padEnd(12))}${created.id}
`);
      output_manager_default.print(`  ${import_chalk43.default.cyan("URL".padEnd(12))}${created.url}
`);
      output_manager_default.print(
        `
${import_chalk43.default.dim("Anyone with the URL can trigger a deployment. Store it securely.")}
`
      );
    }
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err)) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.API_ERROR,
            message: err.message
          },
          1
        );
        return 1;
      }
      output_manager_default.error(err.message);
      return 1;
    }
    exitWithNonInteractiveError(client, err, 1, { variant: "checks" });
    printError(err);
    return 1;
  }
}

// src/commands/deploy-hooks/rm.ts
var import_chalk44 = __toESM(require_source(), 1);

// src/util/telemetry/commands/deploy-hooks/rm.ts
var DeployHooksRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentId(id) {
    if (id) {
      this.trackCliArgument({
        arg: "id",
        value: id
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/deploy-hooks/rm.ts
function deployHooksCommandWithGlobalFlags(baseSubcommand, argv) {
  const globalFlags = getGlobalFlagsOnlyFromArgs(argv.slice(2));
  const full = globalFlags.length ? `${baseSubcommand} ${globalFlags.join(" ")}` : baseSubcommand;
  return getCommandNamePlain(full);
}
async function rm4(client, argv) {
  const telemetry2 = new DeployHooksRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand4.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const [hookId] = args;
  telemetry2.trackCliArgumentId(hookId);
  telemetry2.trackCliOptionProject(opts["--project"]);
  telemetry2.trackCliFlagYes(opts["--yes"]);
  if (!hookId) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_id",
          message: "Deploy hook id is required. Pass it as the first argument.",
          next: [
            {
              command: deployHooksCommandWithGlobalFlags(
                "deploy-hooks ls",
                client.argv
              )
            },
            {
              command: deployHooksCommandWithGlobalFlags(
                "deploy-hooks rm <id> --yes",
                client.argv
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `${getCommandName("deploy-hooks rm <id>")} expects one argument`
    );
    return 1;
  }
  const skipConfirmation = opts["--yes"] || false;
  if (client.nonInteractive && !skipConfirmation) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Removing a deploy hook requires confirmation. Re-run with --yes.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
  }
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk44.default.cyan(
        getCommandName("deploy-hooks rm <id>")
      )}`
    );
    return 1;
  }
  const { contextName } = await getScope(client);
  let project;
  try {
    project = await getProjectByCwdOrLink({
      client,
      commandName: "deploy-hooks rm",
      projectNameOrId: opts["--project"],
      forReadOnlyCommand: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const existing = project.link?.deployHooks?.find((h) => h.id === hookId);
  if (!existing) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No deploy hook ${param(hookId)} on project ${project.name}.`,
          next: [
            {
              command: deployHooksCommandWithGlobalFlags(
                "deploy-hooks ls",
                client.argv
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `No deploy hook ${param(hookId)} on project ${param(project.name)}`
    );
    return 1;
  }
  if (!client.nonInteractive) {
    const confirmed = await client.input.confirm(
      `Remove deploy hook ${import_chalk44.default.bold(existing.name)} (${hookId}) from ${import_chalk44.default.bold(project.name)}?`,
      false
    );
    if (!confirmed) {
      output_manager_default.log("Canceled.");
      return 0;
    }
  }
  const rmStamp = stamp_default();
  if (!client.nonInteractive) {
    output_manager_default.spinner(
      `Removing deploy hook under ${import_chalk44.default.bold(contextName)} ${import_chalk44.default.gray(rmStamp)}`
    );
  }
  try {
    await client.fetch(
      `/v2/projects/${encodeURIComponent(project.id)}/deploy-hooks/${encodeURIComponent(hookId)}`,
      { method: "DELETE" }
    );
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Removed deploy hook ${import_chalk44.default.bold(hookId)} from ${import_chalk44.default.bold(project.name)} ${rmStamp()}`
    );
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err) && err.status === 404) {
      output_manager_default.error(
        `No deploy hook ${param(hookId)} on project ${param(project.name)}`
      );
      return 1;
    }
    if (isAPIError(err)) {
      output_manager_default.error(err.message);
      return 1;
    }
    printError(err);
    return 1;
  }
}

// src/util/telemetry/commands/deploy-hooks/index.ts
var DeployHooksTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCreate(actual) {
    this.trackCliSubcommand({
      subcommand: "create",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
};

// src/commands/deploy-hooks/index.ts
var COMMAND_CONFIG12 = {
  create: ["create", "add"],
  ls: ["ls", "list"],
  rm: ["rm", "remove", "delete"]
};
async function main8(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(deployHooksCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new DeployHooksTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG12
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("deploy-hooks");
    output_manager_default.print(help(deployHooksCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: deployHooksCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  switch (subcommand) {
    case "create":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("deploy-hooks", subcommandOriginal);
        return printHelp(createSubcommand3);
      }
      telemetry2.trackCliSubcommandCreate(subcommandOriginal);
      return create3(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("deploy-hooks", subcommandOriginal);
        return printHelp(removeSubcommand4);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm4(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("deploy-hooks", subcommandOriginal);
        return printHelp(listSubcommand6);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls4(client, args);
  }
}

// src/commands/dns/add.ts
var import_chalk46 = __toESM(require_source(), 1);

// src/util/dns/add-dns-record.ts
async function addDNSRecord(client, domain2, recordData) {
  try {
    const record = await client.fetch(
      `/v3/domains/${encodeURIComponent(domain2)}/records`,
      {
        body: recordData,
        method: "POST"
      }
    );
    return record;
  } catch (err) {
    if (isAPIError(err)) {
      if (err.status === 400 && err.code === "invalid_type") {
        return new DNSInvalidType(recordData.type);
      }
      if (err.status === 400 && err.message.includes("port")) {
        return new DNSInvalidPort();
      }
      if (err.status === 400) {
        return err;
      }
      if (err.status === 403) {
        return new DNSPermissionDenied(domain2);
      }
      if (err.status === 404) {
        return new DomainNotFound(domain2);
      }
      if (err.status === 409) {
        const { oldId = "" } = err;
        return new DNSConflictingRecord(oldId);
      }
    }
    throw err;
  }
}

// src/util/dns/parse-add-dns-record-args.ts
function parseAddArgs(args) {
  if (!args || args.length < 1) {
    return null;
  }
  const [domain2, ...rest] = args;
  if (domain2 && rest.length === 0) {
    return {
      domain: domain2,
      data: null
    };
  }
  const name = args[1] === "@" ? "" : args[1].toString();
  const type = args[2];
  const value = args[3];
  if (!(domain2 && typeof name === "string" && type)) {
    return null;
  }
  if (type === "MX" && args.length === 5) {
    return {
      domain: domain2,
      data: { name, type, value, mxPriority: Number(args[4]) }
    };
  }
  if (type === "SRV" && args.length === 7) {
    return {
      domain: domain2,
      data: {
        name,
        type,
        srv: {
          priority: Number(value),
          weight: Number(args[4]),
          port: Number(args[5]),
          target: args[6]
        }
      }
    };
  }
  if (args.length === 4) {
    return {
      domain: domain2,
      data: {
        name,
        type,
        value
      }
    };
  }
  return null;
}

// src/util/dns/get-dns-data.ts
var import_chalk45 = __toESM(require_source(), 1);
var RECORD_TYPES = ["A", "AAAA", "ALIAS", "CAA", "CNAME", "MX", "SRV", "TXT"];
async function getDNSData(client, data) {
  if (data) {
    return data;
  }
  try {
    const possibleTypes = new Set(RECORD_TYPES);
    const type = (await client.input.text({
      message: `- Record type (${RECORD_TYPES.join(", ")}): `,
      validate: (v) => Boolean(v && possibleTypes.has(v.trim().toUpperCase()))
    })).trim().toUpperCase();
    const name = await getRecordName(client, type);
    if (type === "SRV") {
      const priority = await getNumber(client, `- ${type} priority: `);
      const weight = await getNumber(client, `- ${type} weight: `);
      const port = await getNumber(client, `- ${type} port: `);
      const target = await getTrimmedString(client, `- ${type} target: `);
      output_manager_default.log(
        `${import_chalk45.default.cyan(name)} ${import_chalk45.default.bold(type)} ${import_chalk45.default.cyan(
          `${priority}`
        )} ${import_chalk45.default.cyan(`${weight}`)} ${import_chalk45.default.cyan(`${port}`)} ${import_chalk45.default.cyan(
          target
        )}.`
      );
      return await verifyData(client) ? {
        name,
        type,
        srv: {
          priority,
          weight,
          port,
          target
        }
      } : null;
    }
    if (type === "MX") {
      const mxPriority = await getNumber(client, `- ${type} priority: `);
      const value2 = await getTrimmedString(client, `- ${type} host: `);
      output_manager_default.log(
        `${import_chalk45.default.cyan(name)} ${import_chalk45.default.bold(type)} ${import_chalk45.default.cyan(
          `${mxPriority}`
        )} ${import_chalk45.default.cyan(value2)}`
      );
      return await verifyData(client) ? {
        name,
        type,
        value: value2,
        mxPriority
      } : null;
    }
    const value = await getTrimmedString(client, `- ${type} value: `);
    output_manager_default.log(`${import_chalk45.default.cyan(name)} ${import_chalk45.default.bold(type)} ${import_chalk45.default.cyan(value)}`);
    return await verifyData(client) ? {
      name,
      type,
      value
    } : null;
  } catch (_error) {
    return null;
  }
}
async function verifyData(client) {
  return client.input.confirm("Is this correct?", false);
}
async function getRecordName(client, type) {
  const input = await client.input.text({
    message: `- ${type} name: `
  });
  return input === "@" ? "" : input;
}
async function getNumber(client, label) {
  return Number(
    await client.input.text({
      message: label,
      validate: (v) => Boolean(v && Number(v))
    })
  );
}
async function getTrimmedString(client, label) {
  const res = await client.input.text({
    message: label,
    validate: (v) => Boolean(v && v.trim().length > 0)
  });
  return res.trim();
}

// src/util/telemetry/commands/dns/add.ts
var ALLOWED_RECORD_TYPES = [
  "A",
  "AAAA",
  "ALIAS",
  "CNAME",
  "TXT",
  "MX",
  "SRV"
];
var DnsAddTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(domain2) {
    if (domain2) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentName(name) {
    if (name) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentType(type) {
    if (type) {
      const allowedType = ALLOWED_RECORD_TYPES.includes(type) ? type : this.redactedValue;
      this.trackCliArgument({
        arg: "type",
        value: allowedType
      });
    }
  }
  trackCliArgumentValues(values) {
    if (values?.length) {
      this.trackCliArgument({
        arg: "values",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/dns/add.ts
function withGlobalFlags2(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
async function add3(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand3.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: err instanceof Error ? err.message : String(err)
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  const { args } = parsedArgs;
  const parsedParams = parseAddArgs(args);
  if (!parsedParams) {
    if (client.nonInteractive) {
      const cmd2 = withGlobalFlags2(
        client,
        "dns add <domain> <name> <type> <value>"
      );
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `Invalid number of arguments. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to add a DNS record (see dns --help for MX/SRV forms)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid number of arguments. See: ${import_chalk46.default.cyan(
        `${getCommandName("dns --help")}`
      )} for usage.`
    );
    return 1;
  }
  const addStamp = stamp_default();
  const { domain: domain2, data: argData } = parsedParams;
  const valueArgs = args.slice(3);
  if (client.nonInteractive && !argData) {
    const cmd2 = withGlobalFlags2(
      client,
      "dns add <domain> <name> <type> <value>"
    );
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        action: AGENT_ACTION.MISSING_ARGUMENTS,
        message: "In non-interactive mode full record details are required. Run: " + cmd2,
        next: [
          {
            command: cmd2,
            when: "to add a DNS record (replace placeholders)"
          },
          {
            command: withGlobalFlags2(client, "dns --help"),
            when: "for usage and examples"
          }
        ]
      },
      1
    );
    return 1;
  }
  const telemetryClient = new DnsAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetryClient.trackCliArgumentDomain(domain2);
  telemetryClient.trackCliArgumentName(parsedParams.data?.name);
  telemetryClient.trackCliArgumentType(parsedParams.data?.type);
  telemetryClient.trackCliArgumentValues(valueArgs);
  const data = await getDNSData(client, argData);
  if (!data) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INCOMPLETE_RECORD,
          message: "Record details could not be determined non-interactively. Provide full arguments for dns add.",
          next: [
            {
              command: withGlobalFlags2(client, "dns --help")
            }
          ]
        },
        1
      );
    }
    output_manager_default.log(`Canceled`);
    return 1;
  }
  const { contextName } = await getScope(client);
  const record = await addDNSRecord(client, domain2, data);
  if (record instanceof DomainNotFound) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.DOMAIN_NOT_FOUND,
          message: `The domain ${domain2} can't be found under ${contextName}.`,
          next: [
            {
              command: withGlobalFlags2(client, "dns ls"),
              when: "to list DNS records for your scope"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `The domain ${domain2} can't be found under ${import_chalk46.default.bold(
        contextName
      )} ${import_chalk46.default.gray(addStamp())}`
    );
    return 1;
  }
  if (record instanceof DNSPermissionDenied) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.PERMISSION_DENIED,
          message: `You don't have permissions to add records to domain ${domain2} under ${contextName}.`
        },
        1
      );
    }
    output_manager_default.error(
      `You don't have permissions to add records to domain ${domain2} under ${import_chalk46.default.bold(
        contextName
      )} ${import_chalk46.default.gray(addStamp())}`
    );
    return 1;
  }
  if (record instanceof DNSInvalidPort) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_PORT,
          message: "Invalid <port> parameter. A number was expected."
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid <port> parameter. A number was expected ${import_chalk46.default.gray(
        addStamp()
      )}`
    );
    return 1;
  }
  if (record instanceof DNSInvalidType) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_DNS_TYPE,
          message: `Invalid <type> parameter "${record.meta.type}". Expected one of A, AAAA, ALIAS, CAA, CNAME, MX, SRV, TXT.`
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid <type> parameter "${record.meta.type}". Expected one of A, AAAA, ALIAS, CAA, CNAME, MX, SRV, TXT ${import_chalk46.default.gray(
        addStamp()
      )}`
    );
    return 1;
  }
  if (record instanceof Error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.DNS_ADD_FAILED,
          message: record.message
        },
        1
      );
    }
    output_manager_default.error(record.message);
    return 1;
  }
  output_manager_default.success(
    `DNS record for domain ${import_chalk46.default.bold(domain2)} ${import_chalk46.default.gray(
      `(${record.uid})`
    )} created under ${import_chalk46.default.bold(contextName)} ${import_chalk46.default.gray(addStamp())}`
  );
  return 0;
}

// src/commands/dns/import.ts
var import_chalk48 = __toESM(require_source(), 1);

// src/util/dns/import-zonefile.ts
var import_chalk47 = __toESM(require_source(), 1);
import { readFileSync as readFileSync2 } from "fs";
import { resolve as resolve6 } from "path";
async function importZonefile(client, contextName, domain2, zonefilePath) {
  output_manager_default.spinner(
    `Importing Zone file for domain ${domain2} under ${import_chalk47.default.bold(contextName)}`
  );
  const zonefile = readFileSync2(resolve6(zonefilePath), "utf8");
  try {
    const res = await client.fetch(
      `/v3/domains/${encodeURIComponent(domain2)}/records`,
      {
        headers: { "Content-Type": "text/dns" },
        body: zonefile,
        method: "PUT",
        json: false
      }
    );
    const { recordIds } = await res.json();
    return recordIds;
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "not_found") {
        return new DomainNotFound(domain2, contextName);
      }
      if (err.code === "invalid_domain") {
        return new InvalidDomain(domain2);
      }
    }
    throw err;
  }
}

// src/util/telemetry/commands/dns/import.ts
var DnsImportTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(domain2) {
    if (domain2) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentZonefile(path3) {
    if (path3) {
      this.trackCliArgument({
        arg: "zoneFilePath",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/dns/import.ts
function withGlobalFlags3(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
async function importZone(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(importSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: err instanceof Error ? err.message : String(err)
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  const { args } = parsedArgs;
  const { telemetryEventStore } = client;
  const { contextName } = await getScope(client);
  const telemetry2 = new DnsImportTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  if (args.length !== 2) {
    if (client.nonInteractive) {
      const cmd2 = withGlobalFlags3(client, "dns import <domain> <zonefile>");
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `Invalid number of arguments. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to import a zone file"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk48.default.cyan(
        `${getCommandName("dns import <domain> <zonefile>")}`
      )}`
    );
    return 1;
  }
  const addStamp = stamp_default();
  const [domain2, zonefilePath] = args;
  telemetry2.trackCliArgumentDomain(domain2);
  telemetry2.trackCliArgumentZonefile(zonefilePath);
  const recordIds = await importZonefile(
    client,
    contextName,
    domain2,
    zonefilePath
  );
  if (recordIds instanceof DomainNotFound) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.DOMAIN_NOT_FOUND,
          message: `The domain ${domain2} can't be found under ${contextName}.`,
          next: [
            {
              command: withGlobalFlags3(client, "dns ls"),
              when: "to list DNS records for your scope"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `The domain ${domain2} can't be found under ${import_chalk48.default.bold(
        contextName
      )} ${import_chalk48.default.gray(addStamp())}`
    );
    return 1;
  }
  if (recordIds instanceof InvalidDomain) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_DOMAIN,
          message: `The domain ${domain2} doesn't match the one found in the zone file.`
        },
        1
      );
    }
    output_manager_default.error(
      `The domain ${domain2} doesn't match with the one found in the Zone file ${import_chalk48.default.gray(
        addStamp()
      )}`
    );
    return 1;
  }
  output_manager_default.success(
    `${recordIds.length} DNS records for domain ${import_chalk48.default.bold(
      domain2
    )} created under ${import_chalk48.default.bold(contextName)} ${import_chalk48.default.gray(addStamp())}`
  );
  return 0;
}

// src/commands/dns/ls.ts
var import_chalk50 = __toESM(require_source(), 1);
var import_ms9 = __toESM(require_ms(), 1);

// src/util/dns/get-domain-dns-records.ts
async function getDomainDNSRecords(client, domain2, apiVersion = 3, nextTimestamp, limit = 20) {
  output_manager_default.debug(`Fetching for DNS records of domain ${domain2}`);
  try {
    let url = `/v${apiVersion}/domains/${encodeURIComponent(
      domain2
    )}/records?limit=${limit}`;
    if (nextTimestamp) {
      url += `&until=${nextTimestamp}`;
    }
    const data = await client.fetch(url);
    return data;
  } catch (err) {
    if (isAPIError(err) && err.code === "not_found") {
      return new DomainNotFound(domain2);
    }
    throw err;
  }
}

// src/util/domains/get-domains.ts
async function getDomains(client, next, limit = 20) {
  let domainUrl = `/v5/domains?limit=${limit}`;
  if (next) {
    domainUrl += `&until=${next}`;
  }
  return await client.fetch(domainUrl);
}

// src/util/dns/get-dns-records.ts
var import_chalk49 = __toESM(require_source(), 1);
async function getDNSRecords(client, contextName, next) {
  const { domainNames, pagination } = await getDomainNames(
    client,
    contextName,
    next
  );
  const domainsRecords = await Promise.all(
    domainNames.map(createGetDomainRecords(client))
  );
  const onlyRecords = domainsRecords.map(
    (item) => item instanceof DomainNotFound ? [] : item
  );
  return {
    records: onlyRecords.reduce(getAddDomainName(domainNames), []),
    pagination
  };
}
function createGetDomainRecords(client) {
  return async (domainName) => {
    const data = await getDomainDNSRecords(client, domainName);
    if (data instanceof DomainNotFound) {
      return [];
    }
    return data.records;
  };
}
function getAddDomainName(domainNames) {
  return (prev, item, idx) => [
    ...prev,
    {
      domainName: domainNames[idx],
      records: item
    }
  ];
}
async function getDomainNames(client, contextName, next) {
  output_manager_default.spinner(`Fetching domains under ${import_chalk49.default.bold(contextName)}`);
  const { domains: domains2, pagination } = await getDomains(client, next);
  return { domainNames: domains2.map((domain2) => domain2.name), pagination };
}

// src/util/telemetry/commands/dns/ls.ts
var DnsLsTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(domainName) {
    if (domainName) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionLimit(limit) {
    if (limit) {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionNext(next) {
    if (next) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/dns/ls.ts
async function ls5(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand7.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: err instanceof Error ? err.message : String(err)
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  if (client.nonInteractive && args.length > 1) {
    const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
    const cmd2 = getCommandNamePlain(
      `dns ls <domain> ${flags.join(" ")}`.trim()
    );
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        action: AGENT_ACTION.MISSING_ARGUMENTS,
        message: `Invalid number of arguments. Run: ${cmd2}`,
        next: [
          {
            command: cmd2,
            when: "to list DNS records (optional single domain)"
          }
        ]
      },
      1
    );
    return 1;
  }
  const validationResult = validateLsArgs({
    commandName: "dns ls <domain>",
    args,
    maxArgs: 1,
    exitCode: 1
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  const { telemetryEventStore } = client;
  const { contextName } = await getScope(client);
  const telemetry2 = new DnsLsTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  const [domainName] = args;
  const lsStamp = stamp_default();
  telemetry2.trackCliArgumentDomain(domainName);
  telemetry2.trackCliOptionLimit(opts["--limit"]);
  telemetry2.trackCliOptionNext(opts["--next"]);
  let paginationOptions;
  try {
    paginationOptions = getPaginationOpts(opts);
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
  if (domainName) {
    const data = await getDomainDNSRecords(
      client,
      domainName,
      4,
      ...paginationOptions
    );
    if (data instanceof DomainNotFound) {
      if (client.nonInteractive) {
        const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
        const cmd2 = getCommandNamePlain(`dns ls ${flags.join(" ")}`.trim());
        outputAgentError(
          client,
          {
            status: AGENT_STATUS.ERROR,
            reason: AGENT_REASON.DOMAIN_NOT_FOUND,
            message: `The domain ${domainName} can't be found under ${contextName}.`,
            next: [{ command: cmd2, when: "to list available DNS records" }]
          },
          1
        );
      }
      output_manager_default.error(
        `The domain ${domainName} can't be found under ${import_chalk50.default.bold(
          contextName
        )} ${import_chalk50.default.gray(lsStamp())}`
      );
      return 1;
    }
    const { records, pagination: pagination2 } = data;
    output_manager_default.log(
      `${records.length > 0 ? "Records" : "No records"} found under ${import_chalk50.default.bold(contextName)} ${import_chalk50.default.gray(lsStamp())}`
    );
    client.stdout.write(getDNSRecordsTable([{ domainName, records }]));
    if (pagination2 && pagination2.count === 20) {
      const flags = getCommandFlags(opts, ["_", "--next"]);
      output_manager_default.log(
        `To display the next page run ${getCommandName(
          `dns ls ${domainName}${flags} --next ${pagination2.next}`
        )}`
      );
    }
    return 0;
  }
  const { records: dnsRecords, pagination } = await getDNSRecords(
    client,
    contextName,
    ...paginationOptions
  );
  const nRecords = dnsRecords.reduce((p, r) => r.records.length + p, 0);
  output_manager_default.log(
    `${nRecords > 0 ? "Records" : "No records"} found under ${import_chalk50.default.bold(
      contextName
    )} ${import_chalk50.default.gray(lsStamp())}`
  );
  output_manager_default.log(getDNSRecordsTable(dnsRecords));
  if (pagination && pagination.count === 20) {
    const flags = getCommandFlags(opts, ["_", "--next"]);
    output_manager_default.log(
      `To display the next page run ${getCommandName(
        `dns ls${flags} --next ${pagination.next}`
      )}`
    );
  }
  return 0;
}
function getDNSRecordsTable(dnsRecords) {
  return formatTable(
    ["", "id", "name", "type", "value", "created"],
    ["l", "r", "l", "l", "l", "l"],
    dnsRecords.map(({ domainName, records }) => ({
      name: import_chalk50.default.bold(domainName),
      rows: records.map(getDNSRecordRow)
    }))
  );
}
function getDNSRecordRow(record) {
  const isSystemRecord = record.creator === "system";
  const createdAt = `${(0, import_ms9.default)(
    Date.now() - new Date(Number(record.createdAt)).getTime()
  )} ago`;
  const priority = record.mxPriority || record.priority || null;
  return [
    "",
    !isSystemRecord ? record.id : "",
    record.name,
    record.type,
    priority ? `${priority} ${record.value}` : record.value,
    import_chalk50.default.gray(isSystemRecord ? "default" : createdAt)
  ];
}

// src/commands/dns/rm.ts
var import_chalk51 = __toESM(require_source(), 1);
var import_ms10 = __toESM(require_ms(), 1);

// src/util/dns/delete-dns-record-by-id.ts
async function deleteDNSRecordById(client, domain2, recordId) {
  return client.fetch(
    `/v3/domains/${encodeURIComponent(domain2)}/records/${recordId}`,
    {
      method: "DELETE"
    }
  );
}

// src/util/dns/get-dns-record-by-id.ts
async function getDNSRecordById(client, id) {
  return client.fetch(`/v5/domains/records/${id}`);
}

// src/util/telemetry/commands/dns/rm.ts
var DnsRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentId(recordId) {
    if (recordId) {
      this.trackCliArgument({
        arg: "recordId",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/dns/rm.ts
function withGlobalFlags4(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
async function rm5(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand5.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: err instanceof Error ? err.message : String(err)
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const { telemetryEventStore } = client;
  const telemetry2 = new DnsRmTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  await getScope(client);
  const [recordId] = args;
  if (args.length !== 1) {
    if (client.nonInteractive) {
      const cmd2 = withGlobalFlags4(client, "dns rm <id> --yes");
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `Invalid number of arguments. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to remove a DNS record by id (use dns ls to find ids)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk51.default.cyan(
        `${getCommandName("dns rm <id>")}`
      )}`
    );
    return 1;
  }
  telemetry2.trackCliArgumentId(recordId);
  telemetry2.trackCliFlagYes(flags["--yes"]);
  const record = await getDNSRecordById(client, recordId);
  if (!record) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.DNS_RECORD_NOT_FOUND,
          message: "DNS record not found.",
          next: [
            {
              command: withGlobalFlags4(client, "dns ls"),
              when: "to list DNS records and ids"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error("DNS record not found");
    return 1;
  }
  const { domain: domainName } = record;
  const skipConfirmation = flags["--yes"];
  if (client.nonInteractive && !skipConfirmation) {
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: "In non-interactive mode --yes is required to remove a DNS record.",
        next: [
          {
            command: buildCommandWithYes(client.argv),
            when: "to confirm removal"
          }
        ]
      },
      1
    );
    return 1;
  }
  const yes = skipConfirmation || await readConfirmation2(
    client,
    "The following record will be removed permanently",
    domainName,
    record
  );
  if (!yes) {
    output_manager_default.error(`User canceled.`);
    return 0;
  }
  const rmStamp = stamp_default();
  await deleteDNSRecordById(client, domainName, record.id);
  output_manager_default.success(
    `Record ${import_chalk51.default.gray(`${record.id}`)} removed ${import_chalk51.default.gray(rmStamp())}`
  );
  return 0;
}
function readConfirmation2(client, msg, domainName, record) {
  return new Promise((resolve8) => {
    output_manager_default.log(msg);
    output_manager_default.print(
      `${table([getDeleteTableRow(domainName, record)], {
        align: ["l", "r", "l"],
        hsep: 6
      }).replace(/^(.*)/gm, "  $1")}
`
    );
    output_manager_default.print(
      `${import_chalk51.default.bold.red("> Are you sure?")} ${import_chalk51.default.gray("(y/N) ")}`
    );
    client.stdin.on("data", (d) => {
      process.stdin.pause();
      resolve8(d.toString().trim().toLowerCase() === "y");
    }).resume();
  });
}
function getDeleteTableRow(domainName, record) {
  const recordName = `${record.name.length > 0 ? `${record.name}.` : ""}${domainName}`;
  return [
    record.id,
    import_chalk51.default.bold(
      `${recordName} ${record.type} ${record.value} ${record.mxPriority || ""}`
    ),
    import_chalk51.default.gray(
      `${(0, import_ms10.default)(Date.now() - new Date(Number(record.createdAt)).getTime())} ago`
    )
  ];
}

// src/util/telemetry/commands/dns/index.ts
var DnsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandImport(actual) {
    this.trackCliSubcommand({
      subcommand: "import",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
};

// src/commands/dns/index.ts
var COMMAND_CONFIG13 = {
  add: getCommandAliases(addSubcommand3),
  import: getCommandAliases(importSubcommand),
  ls: getCommandAliases(listSubcommand7),
  rm: getCommandAliases(removeSubcommand5)
};
async function dns(client) {
  const { telemetryEventStore } = client;
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(dnsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const telemetry2 = new DnsTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  const { subcommand, subcommandOriginal, args } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG13
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("dns", subcommand);
    output_manager_default.print(help(dnsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: dnsCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("dns", subcommandOriginal);
        printHelp(addSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add3(client, args);
    case "import":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("dns", subcommandOriginal);
        printHelp(importSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandImport(subcommandOriginal);
      return importZone(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("dns", subcommandOriginal);
        printHelp(removeSubcommand5);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm5(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("dns", subcommandOriginal);
        printHelp(listSubcommand7);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls5(client, args);
  }
}

// src/commands/domains/add.ts
var import_chalk55 = __toESM(require_source(), 1);

// src/util/format-ns-table.ts
var import_chalk52 = __toESM(require_source(), 1);

// src/util/output/chars.ts
var chars = {
  tick: process.platform === "win32" ? "\u221A" : "\u2714",
  cross: process.platform === "win32" ? "\u2613" : "\u2718"
};
var chars_default = chars;

// src/util/format-ns-table.ts
function formatNSTable(intendedNameservers, currentNameservers, { extraSpace = "" } = {}) {
  const sortedIntended = intendedNameservers.sort();
  const sortedCurrent = currentNameservers.sort();
  const maxLength = Math.max(
    intendedNameservers.length,
    currentNameservers.length
  );
  const rows = [];
  for (let i = 0; i < maxLength; i++) {
    rows.push([
      sortedIntended[i] || import_chalk52.default.gray("-"),
      sortedCurrent[i] || import_chalk52.default.gray("-"),
      sortedIntended[i] === sortedCurrent[i] ? import_chalk52.default.green(chars_default.tick) : import_chalk52.default.red(chars_default.cross)
    ]);
  }
  return table(
    [
      [
        import_chalk52.default.gray("Intended Nameservers"),
        import_chalk52.default.gray("Current Nameservers"),
        ""
      ],
      ...rows
    ],
    { hsep: 4 }
  ).replace(/^(.*)/gm, `${extraSpace}$1`);
}

// src/util/domains/is-public-suffix.ts
function isPublicSuffix(domainName) {
  return domainName.endsWith(".vercel.app") || domainName.endsWith(".now.sh");
}

// src/util/domains/get-domain-config.ts
async function getDomainConfig(client, domainName) {
  try {
    const config = await client.fetch(
      `/v4/domains/${domainName}/config`
    );
    return config;
  } catch (err) {
    if (isAPIError(err) && err.status < 500) {
      return err;
    }
    throw err;
  }
}

// src/util/projects/add-domain-to-project.ts
var import_chalk53 = __toESM(require_source(), 1);
async function addDomainToProject(client, projectNameOrId, domain2) {
  output_manager_default.spinner(
    `Adding domain ${domain2} to project ${import_chalk53.default.bold(projectNameOrId)}`
  );
  try {
    const response = await client.fetch(
      `/projects/${encodeURIComponent(projectNameOrId)}/alias`,
      {
        method: "POST",
        body: JSON.stringify({
          target: "PRODUCTION",
          domain: domain2
        })
      }
    );
    const aliasTarget = response.find(
      (aliasTarget2) => aliasTarget2.domain === domain2
    );
    if (!aliasTarget) {
      return new Error(
        `Adding domain '${domain2}' to project '${projectNameOrId}' did not return a matching alias in the API response. The domain may already be on another project\u2014try 'vercel domains add ${domain2} ${projectNameOrId} --force' after removing it there, or run 'vercel domains inspect ${domain2}'.`
      );
    }
    return aliasTarget;
  } catch (err) {
    if (isAPIError(err) && err.status < 500) {
      return err;
    }
    throw err;
  }
}

// src/util/projects/remove-domain-from-project.ts
var import_chalk54 = __toESM(require_source(), 1);
async function removeDomainFromProject(client, projectNameOrId, domain2) {
  output_manager_default.spinner(
    `Removing domain ${domain2} from project ${import_chalk54.default.bold(projectNameOrId)}`
  );
  try {
    const response = await client.fetch(
      `/projects/${encodeURIComponent(
        projectNameOrId
      )}/alias?domain=${encodeURIComponent(domain2)}`,
      {
        method: "DELETE"
      }
    );
    return response;
  } catch (err) {
    if (isAPIError(err) && err.status < 500) {
      return err;
    }
    throw err;
  }
}

// src/util/telemetry/commands/domains/add.ts
var DomainsAddTelemetryClient = class extends TelemetryClient {
  trackCliFlagForce(force) {
    if (force) {
      this.trackCliFlag("force");
    }
  }
  trackCliArgumentDomain(domainName) {
    if (domainName) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentProject(projectName) {
    if (projectName) {
      this.trackCliArgument({
        arg: "project",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/add.ts
var import_error_utils8 = __toESM(require_dist(), 1);
function withGlobalFlags5(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
var VERCEL_DOMAINS_DASHBOARD = "https://vercel.com/dashboard/domains";
function nextCommandsForDomainsAddFailure(client, domainName, projectName, err, linkedProject) {
  const next = [
    {
      command: withGlobalFlags5(client, `domains inspect ${domainName}`),
      when: "to inspect domain configuration and ownership"
    }
  ];
  const apiErr = err;
  const code2 = typeof apiErr.code === "string" ? apiErr.code : "";
  const msg = err.message.toLowerCase();
  const status3 = isAPIError(err) ? err.status : void 0;
  const looksLikeOwnershipOrPurchaseIssue = code2 === "not_domain_owner" || code2 === "invalid_domain" || code2 === "domain_not_found" || status3 === 403 && (msg.includes("not authorized") || msg.includes("forbidden")) || msg.includes("not verified") || msg.includes("do not own") || msg.includes("not the owner");
  const aliasConflict = code2 === "ALIAS_DOMAIN_EXIST" || status3 === 409 || msg.includes("already assigned") || msg.includes("already in use");
  if (looksLikeOwnershipOrPurchaseIssue || aliasConflict) {
    next.push({
      command: withGlobalFlags5(client, `domains buy ${domainName}`),
      when: "user must run interactively in a terminal\u2014agents must not purchase; purchase also available in dashboard"
    });
    next.push({
      command: withGlobalFlags5(client, "domains transfer-in"),
      when: "to transfer a domain you already own from another registrar into Vercel"
    });
    next.push({
      command: openUrlInBrowserCommand(VERCEL_DOMAINS_DASHBOARD),
      when: "to open the Domains dashboard in your browser"
    });
  }
  if (aliasConflict && !looksLikeOwnershipOrPurchaseIssue) {
    const forceCmd = linkedProject ? `domains add ${domainName} --force` : `domains add ${domainName} ${projectName} --force`;
    next.push({
      command: withGlobalFlags5(client, forceCmd),
      when: "to force move from another project (only if API returns project id\u2014otherwise remove domain from the other project first)"
    });
  }
  return next;
}
async function add4(client, argv) {
  const telemetry2 = new DomainsAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand4.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const force = opts["--force"];
  telemetry2.trackCliFlagForce(force);
  const { contextName } = await getScope(client);
  const project = await getLinkedProject(client).then((result) => {
    if (result.status === "linked") {
      return result.project;
    }
    return null;
  });
  if (project && args.length !== 1) {
    if (client.nonInteractive) {
      outputActionRequired(
        client,
        {
          status: "action_required",
          reason: "missing_arguments",
          action: "missing_arguments",
          message: `Linked project is "${project.name}". Run: ${withGlobalFlags5(client, `domains add <domain>`)}`,
          next: [
            {
              command: withGlobalFlags5(client, `domains add <domain>`),
              when: "to add a domain to the linked project (single argument)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `${getCommandName("domains add <domain>")} expects one argument.`
    );
    return 1;
  }
  if (!project && args.length !== 2) {
    if (client.nonInteractive) {
      const cmd2 = withGlobalFlags5(client, "domains add <domain> <project>");
      outputActionRequired(
        client,
        {
          status: "action_required",
          reason: "missing_arguments",
          action: "missing_arguments",
          message: `No linked project and domain needs a project. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to add a domain to a project (or link a project first)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `${getCommandName(
        "domains add <domain> <project>"
      )} expects two arguments.`
    );
    return 1;
  }
  const domainName = String(args[0]);
  const projectName = project ? project.name : String(args[1]);
  telemetry2.trackCliArgumentDomain(domainName);
  telemetry2.trackCliArgumentProject(args[1]);
  const addStamp = stamp_default();
  let aliasTarget = await addDomainToProject(client, projectName, domainName);
  if (aliasTarget instanceof Error) {
    if (aliasTarget instanceof APIError && aliasTarget.code === "ALIAS_DOMAIN_EXIST" && aliasTarget.project && aliasTarget.project.id) {
      if (force) {
        const removeResponse = await removeDomainFromProject(
          client,
          aliasTarget.project.id,
          domainName
        );
        if (removeResponse instanceof Error) {
          if (client.nonInteractive) {
            outputAgentError(
              client,
              {
                status: "error",
                reason: "domain_remove_failed",
                message: (0, import_error_utils8.errorToString)(removeResponse)
              },
              1
            );
          }
          output_manager_default.prettyError(removeResponse);
          return 1;
        }
        aliasTarget = await addDomainToProject(client, projectName, domainName);
      }
    }
    if (aliasTarget instanceof Error) {
      if (client.nonInteractive) {
        const status3 = isAPIError(aliasTarget) ? aliasTarget.status : void 0;
        const apiErr = aliasTarget;
        const code2 = typeof apiErr.code === "string" ? apiErr.code : "";
        const msg = aliasTarget.message.toLowerCase();
        let reason = status3 === 403 ? "forbidden" : status3 === 404 ? "not_found" : status3 === 409 || msg.includes("already") ? "alias_conflict" : "domain_add_failed";
        if (code2 === "not_domain_owner" || status3 === 403 && msg.includes("not authorized")) {
          reason = "domain_not_owned";
        }
        let message = (0, import_error_utils8.errorToString)(aliasTarget);
        if (reason === "domain_not_owned" || code2 === "invalid_domain" || msg.includes("not authorized to use")) {
          message += " domains add is for domains you already own or control via DNS. If you have not purchased the domain yet, the user must run 'domains buy' interactively (agents must not purchase) or buy in the dashboard; use 'domains transfer-in' to move an existing registration to Vercel.";
        }
        outputAgentError(
          client,
          {
            status: "error",
            reason,
            message,
            next: nextCommandsForDomainsAddFailure(
              client,
              domainName,
              projectName,
              aliasTarget,
              !!project
            )
          },
          1
        );
      }
      output_manager_default.prettyError(aliasTarget);
      return 1;
    }
  }
  output_manager_default.success(
    `Domain ${import_chalk55.default.bold(domainName)} added to project ${import_chalk55.default.bold(
      projectName
    )}. ${addStamp()}`
  );
  if (isPublicSuffix(domainName)) {
    output_manager_default.log(
      "The domain will automatically get assigned to your latest production deployment."
    );
    return 0;
  }
  const domainResponse = await getDomain(client, contextName, domainName);
  if (domainResponse instanceof Error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "domain_fetch_failed",
          message: (0, import_error_utils8.errorToString)(domainResponse)
        },
        1
      );
    }
    output_manager_default.prettyError(domainResponse);
    return 1;
  }
  const domainConfig = await getDomainConfig(client, domainName);
  if (domainConfig.misconfigured) {
    output_manager_default.warn(
      "This domain is not configured properly. To configure it you should either:"
    );
    output_manager_default.print(
      `  ${import_chalk55.default.grey("a)")} Set the following record on your DNS provider to continue: ${code(`A ${domainName} 76.76.21.21`)} ${import_chalk55.default.grey("[recommended]")}
`
    );
    output_manager_default.print(
      `  ${import_chalk55.default.grey("b)")} Change your Domains's nameservers to the intended set`
    );
    output_manager_default.print(
      `
${formatNSTable(
        domainResponse.intendedNameservers,
        domainResponse.nameservers,
        { extraSpace: "     " }
      )}

`
    );
    output_manager_default.print(
      "  We will run a verification for you and you will receive an email upon completion.\n"
    );
    output_manager_default.print("  Read more: https://vercel.link/domain-configuration\n\n");
  } else {
    output_manager_default.log(
      "The domain will automatically get assigned to your latest production deployment."
    );
  }
  return 0;
}

// src/commands/domains/check.ts
var import_chalk56 = __toESM(require_source(), 1);

// src/util/is-root-domain.ts
var import_tldts5 = __toESM(require_cjs(), 1);
function isRootDomain(domainName) {
  const parsedDomain = (0, import_tldts5.parse)(domainName);
  const { domain: rootDomain, subdomain } = parsedDomain;
  return Boolean(!subdomain && rootDomain);
}

// src/commands/domains/check.ts
async function check(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(checkSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const domains2 = args;
  const hasMultipleInputDomains = domains2.length > 1;
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  if (domains2.length === 0) {
    output_manager_default.error("Missing domain name. Usage: `vercel domains check <domain>`");
    return 1;
  }
  if (domains2.length > 50) {
    output_manager_default.error(
      "Too many domains. You can check up to 50 domains per request."
    );
    return 1;
  }
  const invalidDomains = domains2.filter((domain2) => !isRootDomain(domain2));
  if (invalidDomains.length > 0) {
    output_manager_default.error(
      `Invalid domain name(s): ${invalidDomains.map((domain2) => param(domain2)).join(", ")}. Run ${getCommandName(`domains --help`)}`
    );
    return 1;
  }
  try {
    const results = domains2.length === 1 ? [
      {
        domain: domains2[0],
        available: (await getDomainStatus(client, domains2[0])).available
      }
    ] : (await client.fetch(
      "/v1/registrar/domains/availability",
      {
        method: "POST",
        body: {
          domains: domains2
        }
      }
    )).results;
    if (formatResult.jsonOutput) {
      if (!hasMultipleInputDomains && results.length === 1) {
        client.stdout.write(
          `${JSON.stringify(
            {
              domain: results[0].domain,
              available: Boolean(results[0].available)
            },
            null,
            2
          )}
`
        );
      } else {
        client.stdout.write(
          `${JSON.stringify(
            {
              results: results.map((result) => ({
                domain: result.domain,
                available: Boolean(result.available)
              }))
            },
            null,
            2
          )}
`
        );
      }
      return 0;
    }
    for (const result of results) {
      output_manager_default.log(
        `The domain ${param(result.domain)} is ${import_chalk56.default.underline(
          result.available ? "available" : "unavailable"
        )}.`
      );
    }
    return 0;
  } catch (error) {
    if (isAPIError(error)) {
      const message = error.serverMessage || `API error (${error.status})`;
      if (formatResult.jsonOutput) {
        client.stdout.write(
          `${JSON.stringify(
            { error: error.code || "api_error", message },
            null,
            2
          )}
`
        );
      } else {
        output_manager_default.error(message);
      }
      return 1;
    }
    printError(error);
    return 1;
  }
}

// src/commands/domains/transfer-in.ts
var import_chalk57 = __toESM(require_source(), 1);

// src/util/domains/transfer-in-domain.ts
async function transferInDomain(client, name, authCode, expectedPrice, years) {
  const { team } = await getScope(client);
  const teamParam = team ? `?teamId=${team.slug}` : "";
  try {
    const { orderId } = await client.fetch(
      `/v1/registrar/domains/${name}/transfer${teamParam}`,
      {
        body: {
          authCode,
          autoRenew: true,
          years,
          expectedPrice,
          contactInformation: {
            firstName: "Vercel",
            lastName: "Whois",
            email: "domains@registrar.vercel.com",
            phone: "+14153985463",
            address1: "100 First Street, Suite 2400",
            city: "San Fransisco",
            state: "CA",
            zip: "94105",
            country: "US",
            companyName: "Vercel Inc."
          }
        },
        method: "POST"
      }
    );
    const order = await pollForOrder(client, orderId);
    if (order === null) {
      return new UnexpectedDomainTransferError(name);
    }
    if (order.status === "completed") {
      const domain2 = order.domains.find((domain3) => domain3.domainName === name);
      if (domain2?.status === "completed") {
        return { ok: true };
      }
    }
    if (order.error?.code === "payment_failed") {
      return new DomainPaymentError();
    }
    return new UnexpectedDomainTransferError(name);
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "invalid_name") {
        return new InvalidDomain(name);
      }
      if (err.code === "tld_not_supported") {
        return new UnsupportedTLD(name);
      }
      if (err.code === "domain_not_available") {
        return new DomainNotAvailable(name);
      }
    }
    throw err;
  }
}

// src/util/domains/get-auth-code.ts
async function getAuthCode(client, code2) {
  if (isValidAuthCode(code2)) {
    return code2;
  }
  return client.input.text({
    message: `- Transfer auth code: `,
    validate: isValidAuthCode
  });
}
function isValidAuthCode(code2) {
  return !!(code2 && code2.length > 0);
}

// src/util/telemetry/commands/domains/transfer-in.ts
var DomainsTransferInTelemetryClient = class extends TelemetryClient {
  trackCliOptionCode(code2) {
    if (code2) {
      this.trackCliOption({
        option: "code",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentDomain(domainName) {
    if (domainName) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/transfer-in.ts
async function transferIn(client, argv) {
  const telemetry2 = new DomainsTransferInTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    transferInSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  telemetry2.trackCliOptionCode(opts["--code"]);
  const [domainName] = args;
  if (!domainName) {
    output_manager_default.error(
      `Missing domain name. Run ${getCommandName(`domains --help`)}`
    );
    return 1;
  }
  telemetry2.trackCliArgumentDomain(domainName);
  if (!isRootDomain(domainName)) {
    output_manager_default.error(
      `Invalid domain name ${param(domainName)}. Run ${getCommandName(
        `domains --help`
      )}`
    );
    return 1;
  }
  const availableStamp = stamp_default();
  const domainPrice = await getDomainPrice(client, domainName);
  if (domainPrice instanceof Error) {
    output_manager_default.prettyError(domainPrice);
    return 1;
  }
  const { transferPrice, years } = domainPrice;
  if (transferPrice === null) {
    output_manager_default.error(`The domain ${param(domainName)} is not transferable.`);
    return 1;
  }
  const { contextName } = await getScope(client);
  output_manager_default.log(
    `The domain ${param(domainName)} is ${import_chalk57.default.underline(
      "available"
    )} to transfer under ${import_chalk57.default.bold(contextName)}! ${availableStamp()}`
  );
  const authCode = await getAuthCode(client, opts["--code"]);
  const shouldTransfer = await client.input.confirm(
    `Transfer now with 1yr renewal for ${import_chalk57.default.bold(`$${transferPrice}`)}?`,
    false
  );
  if (!shouldTransfer) {
    return 0;
  }
  const transferStamp = stamp_default();
  output_manager_default.spinner(`Initiating transfer for domain ${domainName}`);
  const transferInResult = await transferInDomain(
    client,
    domainName,
    authCode,
    transferPrice,
    years
  );
  if (transferInResult instanceof InvalidDomain) {
    output_manager_default.error(`The domain ${transferInResult.meta.domain} is not valid.`);
    return 1;
  }
  if (transferInResult instanceof DomainNotAvailable) {
    output_manager_default.error(
      `The domain "${transferInResult.meta.domain}" is not transferable.`
    );
    return 1;
  }
  if (transferInResult instanceof UnsupportedTLD) {
    output_manager_default.error(
      `The TLD for domain name ${transferInResult.meta.domain} is not supported.`
    );
    return 1;
  }
  if (transferInResult instanceof DomainPaymentError) {
    output_manager_default.error(`Your card was declined.`);
    return 1;
  }
  if (transferInResult instanceof UnexpectedDomainTransferError) {
    output_manager_default.error(`An unexpected error happened while initiating the transfer.`);
    return 1;
  }
  output_manager_default.success(
    `Domain ${param(domainName)} transfer started ${transferStamp()}`
  );
  output_manager_default.print(
    `  To finalize the transfer, we are waiting for approval from your current registrar.
`
  );
  output_manager_default.print(`  You will receive an email upon completion.
`);
  output_manager_default.warn(
    `Once transferred, your domain ${param(
      domainName
    )} will be using Vercel DNS.
`
  );
  output_manager_default.print(
    `  To transfer with previous DNS records, export the zone file from your previous registrar.
`
  );
  output_manager_default.print(`  Then import it to Vercel DNS by using:
`);
  output_manager_default.print(
    `    ${getCommandName(`dns import ${domainName} <zonefile>`)}

`
  );
  return 0;
}

// src/commands/domains/inspect.ts
var import_chalk58 = __toESM(require_source(), 1);

// src/util/projects/find-projects-for-domain.ts
async function findProjectsForDomain(client, domainName) {
  try {
    const result = [];
    for await (const chunk of client.fetchPaginated(
      "/v9/projects"
    )) {
      for (const project of chunk.projects) {
        if (project.targets?.production?.alias?.some?.(
          (alias2) => alias2.endsWith(domainName)
        )) {
          result.push(project);
        }
      }
    }
    return result;
  } catch (err) {
    if (isAPIError(err) && err.status < 500) {
      return err;
    }
    throw err;
  }
}

// src/util/domains/get-domain-registrar.ts
function getDomainRegistrar(domain2) {
  if (domain2.boughtAt) {
    return "Vercel";
  }
  if (typeof domain2.orderedAt === "number" && !domain2.boughtAt) {
    return "Purchase in Process";
  }
  return "Third Party";
}

// src/util/telemetry/commands/domains/inspect.ts
var DomainsInspectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(name) {
    if (name) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/inspect.ts
async function inspect(client, argv) {
  const telemetry2 = new DomainsInspectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(inspectSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  const [domainName] = args;
  const inspectStamp = stamp_default();
  if (!domainName) {
    output_manager_default.error(
      `${getCommandName(`domains inspect <domain>`)} expects one argument`
    );
    return 1;
  }
  telemetry2.trackCliArgumentDomain(domainName);
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk58.default.cyan(
        `${getCommandName("domains inspect <domain>")}`
      )}`
    );
    return 1;
  }
  output_manager_default.debug(`Fetching domain info`);
  const { contextName } = await getScope(client);
  output_manager_default.spinner(
    `Fetching Domain ${domainName} under ${import_chalk58.default.bold(contextName)}`
  );
  const information = await fetchInformation({
    client,
    contextName,
    domainName
  });
  if (typeof information === "number") {
    return information;
  }
  const { domain: domain2, projects, renewalPrice, domainConfig } = information;
  output_manager_default.log(
    `Domain ${domainName} found under ${import_chalk58.default.bold(contextName)} ${import_chalk58.default.gray(
      inspectStamp()
    )}`
  );
  output_manager_default.print("\n");
  output_manager_default.print(import_chalk58.default.bold("  General\n\n"));
  output_manager_default.print(`    ${import_chalk58.default.cyan("Name")}			${domain2.name}
`);
  output_manager_default.print(
    `    ${import_chalk58.default.cyan("Registrar")}			${getDomainRegistrar(domain2)}
`
  );
  output_manager_default.print(
    `    ${import_chalk58.default.cyan("Expiration Date")}		${formatDate(domain2.expiresAt)}
`
  );
  output_manager_default.print(
    `    ${import_chalk58.default.cyan("Creator")}			${domain2.creator.username}
`
  );
  output_manager_default.print(
    `    ${import_chalk58.default.cyan("Created At")}			${formatDate(domain2.createdAt)}
`
  );
  output_manager_default.print(`    ${import_chalk58.default.cyan("Edge Network")}		yes
`);
  output_manager_default.print(
    `    ${import_chalk58.default.cyan("Renewal Price")}		${domain2.boughtAt && renewalPrice ? `$${renewalPrice} USD` : import_chalk58.default.gray("-")}
`
  );
  output_manager_default.print("\n");
  output_manager_default.print(import_chalk58.default.bold("  Nameservers\n\n"));
  output_manager_default.print(
    `${formatNSTable(domain2.intendedNameservers, domain2.nameservers, {
      extraSpace: "    "
    })}
`
  );
  output_manager_default.print("\n");
  if (Array.isArray(projects) && projects.length > 0) {
    output_manager_default.print(import_chalk58.default.bold("  Projects\n"));
    const table3 = formatTable(
      ["Project", "Domains"],
      ["l", "l"],
      [
        {
          rows: projects.map((project) => {
            const name = project.name;
            const domains2 = (project.targets?.production?.alias || []).filter(
              (alias2) => alias2.endsWith(domainName)
            );
            const cols = domains2.length ? domains2.join(", ") : "-";
            return [name, cols];
          })
        }
      ]
    );
    output_manager_default.print(
      table3.split("\n").map((line) => `   ${line}`).join("\n")
    );
    output_manager_default.print("\n");
  }
  if (domainConfig.misconfigured) {
    output_manager_default.warn(
      `This Domain is not configured properly. To configure it you should either:`,
      null,
      null,
      null
    );
    output_manager_default.print(
      `  ${import_chalk58.default.grey("a)")} Set the following record on your DNS provider to continue: ${code(`A ${domainName} 76.76.21.21`)} ${import_chalk58.default.grey("[recommended]")}
`
    );
    output_manager_default.print(
      `  ${import_chalk58.default.grey("b)")} Change your Domains's nameservers to the intended set detailed above.

`
    );
    output_manager_default.print(
      `  We will run a verification for you and you will receive an email upon completion.
`
    );
    const contextNameConst = contextName;
    const projectNames = Array.from(
      new Set(projects.map((project) => project.name))
    );
    if (projectNames.length) {
      projectNames.forEach((name, index) => {
        const prefix = index === 0 ? "  Read more:" : " ".repeat(12);
        output_manager_default.print(
          `${prefix} https://vercel.com/${contextNameConst}/${name}/settings/domains
`
        );
      });
    } else {
      output_manager_default.print(`  Read more: https://vercel.link/domain-configuration
`);
    }
    output_manager_default.print("\n");
  }
  return null;
}
async function fetchInformation({
  client,
  contextName,
  domainName
}) {
  const [domain2, renewalPrice] = await Promise.all([
    getDomainByName(client, contextName, domainName, { ignoreWait: true }),
    getDomainPrice(client, domainName).then((res) => res instanceof Error ? null : res.renewalPrice).catch(() => null)
  ]);
  if (domain2 instanceof DomainNotFound) {
    output_manager_default.prettyError(domain2);
    return 1;
  }
  if (domain2 instanceof DomainPermissionDenied) {
    output_manager_default.prettyError(domain2);
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  const projects = await findProjectsForDomain(client, domainName);
  if (projects instanceof Error) {
    output_manager_default.prettyError(projects);
    return 1;
  }
  const domainConfig = await getDomainConfig(client, domainName);
  return {
    domain: domain2,
    projects,
    renewalPrice,
    domainConfig
  };
}

// src/commands/domains/ls.ts
var import_ms11 = __toESM(require_ms(), 1);
var import_chalk59 = __toESM(require_source(), 1);
var import_pluralize5 = __toESM(require_pluralize(), 1);

// src/util/telemetry/commands/domains/ls.ts
var DomainsLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionLimit(limit) {
    if (limit) {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionNext(next) {
    if (next) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/ls.ts
async function ls6(client, argv) {
  const telemetry2 = new DomainsLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand8.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "domains ls",
    args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  telemetry2.trackCliOptionLimit(opts["--limit"]);
  telemetry2.trackCliOptionNext(opts["--next"]);
  telemetry2.trackCliOptionFormat(opts["--format"]);
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let paginationOptions;
  try {
    paginationOptions = getPaginationOpts(opts);
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
  const { contextName } = await getScope(client, {
    resolveLocalScope: true
  });
  const lsStamp = stamp_default();
  output_manager_default.spinner(`Fetching Domains under ${import_chalk59.default.bold(contextName)}`);
  const { domains: domains2, pagination } = await getDomains(
    client,
    ...paginationOptions
  );
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonOutput = {
      domains: domains2.map((domain2) => ({
        name: domain2.name,
        registrar: getDomainRegistrar(domain2),
        nameservers: isDomainExternal(domain2) ? "external" : "vercel",
        expiresAt: domain2.expiresAt,
        createdAt: domain2.createdAt,
        creator: domain2.creator.username
      })),
      pagination
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    output_manager_default.log(
      `${(0, import_pluralize5.default)("Domain", domains2.length, true)} found under ${import_chalk59.default.bold(
        contextName
      )} ${import_chalk59.default.gray(lsStamp())}`
    );
    if (domains2.length > 0) {
      output_manager_default.print(
        formatDomainsTable(domains2).replace(/^(.*)/gm, `${" ".repeat(1)}$1`)
      );
      output_manager_default.print("\n\n");
    }
    if (pagination && pagination.count === 20) {
      const flags = getCommandFlags(opts, ["_", "--next", "--format"]);
      output_manager_default.log(
        `To display the next page, run ${getCommandName(
          `domains ls${flags} --next ${pagination.next}`
        )}`
      );
    }
  }
  return 0;
}
function formatDomainsTable(domains2) {
  const current = Date.now();
  const rows = domains2.map((domain2) => {
    const expiration = formatDateWithoutTime(domain2.expiresAt);
    const age = domain2.createdAt ? (0, import_ms11.default)(current - domain2.createdAt) : "-";
    return [
      domain2.name,
      getDomainRegistrar(domain2),
      isDomainExternal(domain2) ? "Third Party" : "Vercel",
      expiration,
      domain2.creator.username,
      import_chalk59.default.gray(age)
    ];
  });
  return formatTable(
    ["Domain", "Registrar", "Nameservers", "Expiration Date", "Creator", "Age"],
    ["l", "l", "l", "l", "l", "l"],
    [{ rows }]
  );
}

// src/commands/domains/rm.ts
var import_chalk60 = __toESM(require_source(), 1);
var import_pluralize6 = __toESM(require_pluralize(), 1);

// src/util/domains/remove-domain-by-name.ts
async function removeDomainByName(now, contextName, domain2) {
  try {
    return await now.fetch(`/v3/domains/${encodeURIComponent(domain2)}`, {
      method: "DELETE"
    });
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "not_found") {
        return new DomainNotFound(domain2);
      }
      if (err.code === "forbidden") {
        return new DomainPermissionDenied(domain2, contextName);
      }
      if (err.code === "domain_removal_conflict") {
        return new DomainRemovalConflict({
          aliases: err.aliases,
          certs: err.certs,
          message: err.message,
          pendingAsyncPurchase: err.pendingAsyncPurchase,
          resolvable: err.resolvable,
          suffix: err.suffix,
          transferring: err.transferring
        });
      }
    }
    throw err;
  }
}

// src/util/domains/set-custom-suffix.ts
async function setCustomSuffix(client, contextName, domain2, suffix) {
  try {
    return await client.fetch(`/v1/custom-suffix`, {
      method: "PATCH",
      body: {
        suffix
      }
    });
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "forbidden") {
        return new DomainPermissionDenied(domain2, contextName);
      }
      if (err.code === "domain_external") {
        return new DomainExternal(domain2);
      }
      if (err.code === "domain_invalid") {
        return new InvalidDomain(domain2);
      }
      if (err.code === "domain_not_found") {
        return new DomainNotFound(domain2);
      }
      if (err.code === "domain_not_verified") {
        return new DomainNotVerified(domain2);
      }
      if (err.code === "domain_permission_denied") {
        return new DomainPermissionDenied(domain2, contextName);
      }
    }
    throw err;
  }
}

// src/util/telemetry/commands/domains/rm.ts
var DomainsRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentDomain(v) {
    if (v) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/domains/rm.ts
async function rm6(client, argv) {
  const telemetry2 = new DomainsRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand6.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const [domainName] = args;
  telemetry2.trackCliArgumentDomain(domainName);
  telemetry2.trackCliFlagYes(opts["--yes"]);
  if (!domainName) {
    output_manager_default.error(
      `${getCommandName(`domains rm <domain>`)} expects one argument`
    );
    return 1;
  }
  const { contextName } = await getScope(client);
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk60.default.cyan(
        `${getCommandName("domains rm <domain>")}`
      )}`
    );
    return 1;
  }
  const domain2 = await getDomainByName(client, contextName, domainName);
  if (domain2 instanceof DomainNotFound || domain2.name !== domainName) {
    output_manager_default.error(
      `Domain not found by "${domainName}" under ${import_chalk60.default.bold(contextName)}`
    );
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  if (domain2 instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You don't have access to the domain ${domainName} under ${import_chalk60.default.bold(
        contextName
      )}`
    );
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  const projects = await findProjectsForDomain(client, domain2.name);
  if (Array.isArray(projects) && projects.length > 0) {
    output_manager_default.warn(
      `The domain is currently used by ${(0, import_pluralize6.default)(
        "project",
        projects.length,
        true
      )}.`
    );
  }
  const skipConfirmation = opts["--yes"] || false;
  if (!skipConfirmation && !await client.input.confirm(
    `Are you sure you want to remove ${param(domainName)}?`,
    false
  )) {
    output_manager_default.log("Canceled");
    return 0;
  }
  return removeDomain(client, contextName, skipConfirmation, domain2);
}
async function removeDomain(client, contextName, skipConfirmation, domain2, aliasIds = [], certIds = [], suffix = false, attempt = 1) {
  const removeStamp = stamp_default();
  output_manager_default.debug(`Removing domain`);
  for (const id of aliasIds) {
    output_manager_default.debug(`Removing alias ${id}`);
    try {
      await removeAliasById(client, id);
    } catch (err) {
      if (!isAPIError(err) || err.status !== 404) {
        throw err;
      }
    }
  }
  for (const id of certIds) {
    output_manager_default.debug(`Removing cert ${id}`);
    try {
      await deleteCertById(client, id);
    } catch (err) {
      if (!isAPIError(err) || err.status !== 404) {
        throw err;
      }
    }
  }
  if (suffix) {
    output_manager_default.debug(`Removing custom suffix`);
    await setCustomSuffix(client, contextName, domain2.name, null);
  }
  const removeResult = await removeDomainByName(
    client,
    contextName,
    domain2.name
  );
  if (removeResult instanceof DomainNotFound) {
    output_manager_default.error(`Domain not found under ${import_chalk60.default.bold(contextName)}`);
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  if (removeResult instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You don't have permissions over domain ${import_chalk60.default.underline(
        removeResult.meta.domain
      )} under ${import_chalk60.default.bold(removeResult.meta.context)}.`
    );
    return 1;
  }
  if (removeResult instanceof DomainRemovalConflict) {
    if (attempt >= 2) {
      output_manager_default.error(removeResult.message);
      return 1;
    }
    const {
      aliases,
      certs,
      suffix: suffix2,
      transferring,
      pendingAsyncPurchase,
      resolvable
    } = removeResult.meta;
    if (transferring) {
      output_manager_default.error(
        `${param(
          domain2.name
        )} transfer should be declined or approved before removing.`
      );
      return 1;
    }
    if (pendingAsyncPurchase) {
      output_manager_default.error(
        `Cannot remove ${param(
          domain2.name
        )} because it is still in the process of being purchased.`
      );
      return 1;
    }
    if (!resolvable) {
      output_manager_default.error(removeResult.message);
      return 1;
    }
    output_manager_default.log(
      `We found conflicts when attempting to remove ${param(domain2.name)}.`
    );
    if (aliases.length > 0) {
      output_manager_default.warn(
        `This domain's ${import_chalk60.default.bold(
          (0, import_pluralize6.default)("alias", aliases.length, true)
        )} will be removed. Run ${getCommandName(`alias ls`)} to list them.`
      );
    }
    if (certs.length > 0) {
      output_manager_default.warn(
        `This domain's ${import_chalk60.default.bold(
          (0, import_pluralize6.default)("certificate", certs.length, true)
        )} will be removed. Run ${getCommandName(`cert ls`)} to list them.`
      );
    }
    if (suffix2) {
      output_manager_default.warn(
        `The ${import_chalk60.default.bold(`custom suffix`)} associated with this domain.`
      );
    }
    if (!skipConfirmation && !await client.input.confirm(
      `Remove conflicts associated with domain?`,
      false
    )) {
      output_manager_default.log("Canceled");
      return 0;
    }
    return removeDomain(
      client,
      contextName,
      skipConfirmation,
      domain2,
      aliases,
      certs,
      suffix2,
      attempt + 1
    );
  }
  output_manager_default.success(`Domain ${import_chalk60.default.bold(domain2.name)} removed ${removeStamp()}`);
  return 0;
}

// src/commands/domains/move.ts
var import_chalk61 = __toESM(require_source(), 1);
var import_pluralize7 = __toESM(require_pluralize(), 1);

// src/util/domains/move-out-domain.ts
async function moveOutDomain(client, contextName, name, destination) {
  try {
    return await client.fetch(
      `/v4/domains/${encodeURIComponent(name)}`,
      {
        body: { op: "move-out", destination },
        method: "PATCH"
      }
    );
  } catch (err) {
    if (isAPIError(err)) {
      if (err.code === "forbidden") {
        return new DomainPermissionDenied(name, contextName);
      }
      if (err.code === "not_found") {
        return new DomainNotFound(name);
      }
      if (err.code === "invalid_move_destination") {
        return new InvalidMoveDestination(destination);
      }
      if (err.code === "domain_move_conflict") {
        const { pendingAsyncPurchase, resolvable, suffix, message } = err;
        return new DomainMoveConflict({
          message,
          pendingAsyncPurchase,
          resolvable,
          suffix
        });
      }
    }
    throw err;
  }
}

// src/util/alias/get-domain-aliases.ts
async function getDomainAliases(client, domain2) {
  const { aliases } = await getAliases(client);
  return aliases.filter((alias2) => alias2.alias.endsWith(domain2));
}

// src/util/telemetry/commands/domains/move.ts
var DomainsMoveTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliArgumentDomain(domainName) {
    if (domainName) {
      this.trackCliArgument({
        arg: "domain",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentDestination(destination) {
    if (destination) {
      this.trackCliArgument({
        arg: "destination",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/domains/move.ts
async function move(client, argv) {
  const telemetry2 = new DomainsMoveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(moveSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  telemetry2.trackCliFlagYes(opts["--yes"]);
  telemetry2.trackCliArgumentDomain(args[0]);
  telemetry2.trackCliArgumentDestination(args[1]);
  const { contextName, user } = await getScope(client);
  const { domainName, destination } = await getArgs(client, args);
  if (!isRootDomain(domainName)) {
    output_manager_default.error(
      `Invalid domain name "${domainName}". Run ${getCommandName(
        `domains --help`
      )}`
    );
    return 1;
  }
  const domain2 = await getDomainByName(client, contextName, domainName);
  if (domain2 instanceof DomainNotFound) {
    output_manager_default.error(`Domain not found under ${import_chalk61.default.bold(contextName)}`);
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  if (domain2 instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You don't have permissions over domain ${import_chalk61.default.underline(
        domain2.meta.domain
      )} under ${import_chalk61.default.bold(domain2.meta.context)}.`
    );
    return 1;
  }
  const teams2 = await getTeams(client);
  const matchId = await findDestinationMatch(destination, user, teams2);
  if (matchId && matchId === user.id && user.version === "northstar") {
    output_manager_default.error(`You may not move your domain to your user account.`);
    return 1;
  }
  if (!matchId && !opts["--yes"]) {
    output_manager_default.warn(
      `You're not a member of ${param(destination)}. ${param(
        destination
      )} will have 24 hours to accept your move request before it expires.`
    );
    if (!await client.input.confirm(
      `Are you sure you want to move ${param(domainName)} to ${param(
        destination
      )}?`,
      false
    )) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  if (!opts["--yes"]) {
    const aliases = await getDomainAliases(client, domainName);
    if (aliases.length > 0) {
      output_manager_default.warn(
        `This domain's ${import_chalk61.default.bold(
          (0, import_pluralize7.default)("alias", aliases.length, true)
        )} will be removed. Run ${getCommandName(`alias ls`)} to list them.`
      );
      if (!await client.input.confirm(
        `Are you sure you want to move ${param(domainName)}?`,
        false
      )) {
        output_manager_default.log("Canceled");
        return 0;
      }
    }
  }
  const context = contextName;
  output_manager_default.spinner("Moving");
  const moveTokenResult = await moveOutDomain(
    client,
    context,
    domainName,
    matchId || destination
  );
  if (moveTokenResult instanceof DomainMoveConflict) {
    const { suffix, pendingAsyncPurchase } = moveTokenResult.meta;
    if (suffix) {
      output_manager_default.error(
        `Please remove custom suffix for ${param(domainName)} before moving out`
      );
      return 1;
    }
    if (pendingAsyncPurchase) {
      output_manager_default.error(
        `Cannot remove ${param(
          domain2.name
        )} because it is still in the process of being purchased.`
      );
      return 1;
    }
    output_manager_default.error(moveTokenResult.message);
    return 1;
  }
  if (moveTokenResult instanceof DomainNotFound) {
    output_manager_default.error(`Domain not found under ${import_chalk61.default.bold(contextName)}`);
    output_manager_default.log(`Run ${getCommandName(`domains ls`)} to see your domains.`);
    return 1;
  }
  if (moveTokenResult instanceof DomainPermissionDenied) {
    output_manager_default.error(
      `You don't have permissions over domain ${import_chalk61.default.underline(
        moveTokenResult.meta.domain
      )} under ${import_chalk61.default.bold(moveTokenResult.meta.context)}.`
    );
    return 1;
  }
  if (moveTokenResult instanceof InvalidMoveDestination) {
    output_manager_default.error(
      `Destination ${import_chalk61.default.bold(
        destination
      )} is invalid. Please supply a valid username, email, team slug, user id, or team id.`
    );
    return 1;
  }
  const { moved } = moveTokenResult;
  if (moved) {
    output_manager_default.success(`${param(domainName)} was moved to ${param(destination)}.`);
  } else {
    output_manager_default.success(
      `Sent ${param(destination)} an email to approve the ${param(
        domainName
      )} move request.`
    );
  }
  return 0;
}
async function getArgs(client, args) {
  let [domainName, destination] = args;
  if (!domainName) {
    domainName = await client.input.text({
      message: `- Domain name: `,
      validate: isRootDomain
    });
  }
  if (!destination) {
    destination = await client.input.text({
      message: `- Destination: `,
      validate: (v) => Boolean(v && v.length > 0)
    });
  }
  return { domainName, destination };
}
async function findDestinationMatch(destination, user, teams2) {
  if (user.id === destination || user.username === destination) {
    return user.id;
  }
  for (const team of teams2) {
    if (team.id === destination || team.slug === destination) {
      return team.id;
    }
  }
  return null;
}

// src/commands/domains/price.ts
var import_chalk62 = __toESM(require_source(), 1);
async function price(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(priceSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const domains2 = parsedArgs.args;
  const hasMultipleInputDomains = domains2.length > 1;
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  if (domains2.length === 0) {
    output_manager_default.error("Missing domain name. Usage: `vercel domains price <domain>`");
    return 1;
  }
  try {
    const quotes = domains2.length === 1 ? await getSingleDomainQuote(client, domains2[0]) : await getBulkDomainQuotes(client, domains2);
    if (formatResult.jsonOutput) {
      if (!hasMultipleInputDomains && quotes.length === 1) {
        client.stdout.write(`${JSON.stringify(quotes[0], null, 2)}
`);
      } else {
        client.stdout.write(
          `${JSON.stringify({ results: quotes }, null, 2)}
`
        );
      }
      return 0;
    }
    for (let i = 0; i < quotes.length; i++) {
      const quote = quotes[i];
      output_manager_default.log(
        `${import_chalk62.default.bold("Registrar pricing")} for ${import_chalk62.default.cyan(quote.domain)}`
      );
      output_manager_default.log(
        `  Purchase: ${quote.purchasePrice != null ? `$${quote.purchasePrice}` : "n/a"}`
      );
      output_manager_default.log(
        `  Renewal:  ${quote.renewalPrice != null ? `$${quote.renewalPrice}` : "n/a"}`
      );
      output_manager_default.log(
        `  Transfer: ${quote.transferPrice != null ? `$${quote.transferPrice}` : "n/a"}`
      );
      output_manager_default.log(`  Term:     ${quote.years} year(s)`);
      if (i < quotes.length - 1) {
        output_manager_default.log("");
      }
    }
    return 0;
  } catch (error) {
    if (isAPIError(error)) {
      const msg = error.serverMessage || `API error (${error.status})`;
      if (formatResult.jsonOutput) {
        client.stdout.write(
          `${JSON.stringify(
            { error: error.code || "api_error", message: msg },
            null,
            2
          )}
`
        );
      } else {
        output_manager_default.error(msg);
      }
      return 1;
    }
    printError(error);
    return 1;
  }
}
async function getSingleDomainQuote(client, domain2) {
  const result = await getDomainPrice(client, domain2);
  if (result instanceof UnsupportedTLD) {
    throw createApiLikeError(
      "unsupported_tld",
      `TLD not supported for price lookup: ${result.meta.domain}`
    );
  }
  if (result instanceof InvalidDomain) {
    throw createApiLikeError(
      "invalid_domain",
      `Invalid domain: ${result.meta.domain}`
    );
  }
  if (isAPIError(result)) {
    throw result;
  }
  return [
    {
      domain: domain2,
      purchasePrice: result.purchasePrice,
      renewalPrice: result.renewalPrice,
      transferPrice: result.transferPrice,
      years: result.years
    }
  ];
}
async function getBulkDomainQuotes(client, domains2) {
  const response = await client.fetch(
    "/v1/registrar/domains/price",
    {
      method: "POST",
      body: {
        domains: domains2
      }
    }
  );
  if (Array.isArray(response)) {
    return response;
  }
  return response.results;
}
function createApiLikeError(code2, serverMessage) {
  const error = new Error(serverMessage);
  error.code = code2;
  error.status = 400;
  error.serverMessage = serverMessage;
  return error;
}

// src/commands/domains/search.ts
import { URLSearchParams as URLSearchParams3 } from "url";
import { z } from "zod";
var DEFAULT_LIMIT = 20;
var MAX_LIMIT = 200;
var DEFAULT_ORDER = "relevance";
var ORDERS = ["relevance", "alphabetical", "length"];
var SUPPORTED_TLDS_CACHE_FILE = "cache/domains-search-supported-tlds.json";
var SUPPORTED_TLDS_CACHE_TTL_MS = 30 * 60 * 1e3;
var supportedTldsSchema = z.array(z.string());
var supportedTldsCacheSchema = z.object({
  entries: z.record(
    z.string(),
    z.object({
      fetchedAt: z.number(),
      tlds: supportedTldsSchema
    })
  )
});
var domainSearchResultSchema = z.discriminatedUnion("available", [
  z.object({
    domain: z.string(),
    available: z.literal(false)
  }),
  z.object({
    domain: z.string(),
    available: z.literal(true),
    years: z.number(),
    price: z.number(),
    renewalPrice: z.number(),
    premium: z.boolean()
  })
]);
var domainsSearchResponseSchema = z.object({
  results: z.array(domainSearchResultSchema)
});
async function search(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(searchSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const queryResult = normalizeQuery(parsedArgs.args);
  if (!queryResult.valid) {
    output_manager_default.error(queryResult.error);
    return 1;
  }
  const orderResult = getOrder(parsedArgs.flags["--order"]);
  if (!orderResult.valid) {
    output_manager_default.error(orderResult.error);
    return 1;
  }
  const limitResult = getLimit(parsedArgs.flags["--limit"]);
  if (!limitResult.valid) {
    output_manager_default.error(limitResult.error);
    return 1;
  }
  const tldsResult = normalizeTldFilters(parsedArgs.flags["--tld"]);
  if (!tldsResult.valid) {
    output_manager_default.error(tldsResult.error);
    return 1;
  }
  const { query, keyword, fragment } = queryResult;
  const order = orderResult.order;
  const limit = limitResult.limit;
  const tlds = tldsResult.tlds;
  const availableOnly = parsedArgs.flags["--available"] ?? false;
  const cursorResult = decodeCursor(parsedArgs.flags["--next"]);
  if (!cursorResult.valid) {
    output_manager_default.error(cursorResult.error);
    return 1;
  }
  const cursor = cursorResult.cursor;
  if (cursor && (cursor.query !== query || cursor.fragment !== fragment || cursor.order !== order || !areStringArraysEqual(cursor.tlds, tlds) || cursor.availableOnly !== availableOnly)) {
    output_manager_default.error(
      "The continuation cursor does not match the current query, order, or filters."
    );
    return 1;
  }
  try {
    const supportedTlds = await getSupportedTlds(client, order);
    const matchingTlds = filterTlds(supportedTlds, fragment, tlds, order);
    const offsetResult = getOffset(matchingTlds, cursor);
    if (!offsetResult.valid) {
      output_manager_default.error(offsetResult.error);
      return 1;
    }
    const page = await getCandidatePage(
      client,
      keyword,
      matchingTlds,
      offsetResult.offset,
      limit,
      availableOnly
    );
    const next = page.lastTld !== null && page.hasMore ? encodeCursor({
      query,
      fragment,
      order,
      tlds,
      availableOnly,
      lastTld: page.lastTld
    }) : null;
    if (formatResult.jsonOutput) {
      client.stdout.write(
        `${JSON.stringify(
          {
            query,
            order,
            results: page.results,
            pagination: {
              next,
              limit
            }
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(renderTable(page.results));
    if (next) {
      output_manager_default.log("");
      output_manager_default.log(
        `To continue, run ${getCommandName(getContinuationCommand(query, order, tlds, availableOnly, next))}`
      );
    }
    return 0;
  } catch (error) {
    if (isAPIError(error)) {
      const message = error.serverMessage || `API error (${error.status})`;
      if (formatResult.jsonOutput) {
        client.stdout.write(
          `${JSON.stringify(
            { error: error.code || "api_error", message },
            null,
            2
          )}
`
        );
      } else {
        output_manager_default.error(message);
      }
      return 1;
    }
    printError(error);
    return 1;
  }
}
async function getSupportedTlds(client, order) {
  const cache = await client.maybeReadConfig(
    SUPPORTED_TLDS_CACHE_FILE,
    supportedTldsCacheSchema
  );
  const cacheKey = JSON.stringify([client.config.currentTeam ?? null, order]);
  const cachedEntry = cache?.entries[cacheKey];
  if (cachedEntry && isSupportedTldsCacheEntryFresh(cachedEntry.fetchedAt)) {
    output_manager_default.debug("Using cached supported TLD catalog");
    return cachedEntry.tlds;
  }
  const params = new URLSearchParams3({ order });
  const supportedTlds = supportedTldsSchema.parse(
    await client.fetch(`/v1/registrar/tlds/supported?${params}`)
  );
  try {
    const fetchedAt = Date.now();
    const freshEntries = Object.fromEntries(
      Object.entries(cache?.entries ?? {}).filter(
        ([, entry]) => isSupportedTldsCacheEntryFresh(entry.fetchedAt, fetchedAt)
      )
    );
    await client.writeConfig(
      SUPPORTED_TLDS_CACHE_FILE,
      supportedTldsCacheSchema,
      {
        entries: {
          ...freshEntries,
          [cacheKey]: {
            fetchedAt,
            tlds: supportedTlds
          }
        }
      }
    );
  } catch (error) {
    output_manager_default.debug(`Failed to cache supported TLD catalog: ${error}`);
  }
  return supportedTlds;
}
function isSupportedTldsCacheEntryFresh(fetchedAt, now = Date.now()) {
  const age = now - fetchedAt;
  return age >= 0 && age <= SUPPORTED_TLDS_CACHE_TTL_MS;
}
function encodeCursor(cursor) {
  return Buffer.from(JSON.stringify(cursor), "utf8").toString("base64url");
}
function decodeCursor(value) {
  if (value === void 0) {
    return { valid: true, cursor: null };
  }
  try {
    if (!value || !/^[A-Za-z0-9_-]+$/.test(value)) {
      throw new Error("Invalid base64url value");
    }
    const cursor = JSON.parse(
      Buffer.from(value, "base64url").toString("utf8")
    );
    if (!isContinuationCursor(cursor)) {
      throw new Error("Invalid cursor shape");
    }
    return { valid: true, cursor };
  } catch {
    return {
      valid: false,
      error: "Invalid continuation cursor. Run Domain Discovery again without `--next`."
    };
  }
}
function isContinuationCursor(value) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    return false;
  }
  const cursor = value;
  const keys = Object.keys(cursor).sort();
  if (keys.length !== 6 || keys.join(",") !== "availableOnly,fragment,lastTld,order,query,tlds") {
    return false;
  }
  return typeof cursor.query === "string" && (typeof cursor.fragment === "string" || cursor.fragment === null) && typeof cursor.order === "string" && ORDERS.includes(cursor.order) && Array.isArray(cursor.tlds) && cursor.tlds.every((tld) => typeof tld === "string") && typeof cursor.availableOnly === "boolean" && typeof cursor.lastTld === "string" && cursor.lastTld.length > 0;
}
function getOffset(tlds, cursor) {
  if (!cursor) {
    return { valid: true, offset: 0 };
  }
  const lastTldIndex = tlds.indexOf(cursor.lastTld);
  if (lastTldIndex === -1) {
    return {
      valid: false,
      error: "The continuation cursor is stale. Run Domain Discovery again without `--next`."
    };
  }
  return { valid: true, offset: lastTldIndex + 1 };
}
function normalizeQuery(args) {
  if (args.length === 0) {
    return {
      valid: false,
      error: "Missing query. Usage: `vercel domains search <query>`"
    };
  }
  if (args.length > 1) {
    return {
      valid: false,
      error: "Please provide one keyword or domain fragment."
    };
  }
  const query = args[0].trim().toLowerCase();
  if (query.length === 0) {
    return { valid: false, error: "Query cannot be empty." };
  }
  if (!/^[\x00-\x7F]+$/.test(query)) {
    return {
      valid: false,
      error: "Only ASCII queries are supported in Domain Discovery."
    };
  }
  if (query.includes("://") || /[/?#]/.test(query)) {
    return {
      valid: false,
      error: "URLs are not supported. Provide one keyword or domain fragment."
    };
  }
  if (/\s/.test(query)) {
    return {
      valid: false,
      error: "Queries cannot contain whitespace. Provide one keyword or domain fragment."
    };
  }
  const [keyword, ...fragmentParts] = query.split(".");
  const fragment = fragmentParts.length > 0 ? fragmentParts.join(".") : null;
  if (!isValidLabel(keyword)) {
    return {
      valid: false,
      error: `Invalid keyword: "${keyword}".`
    };
  }
  if (fragment !== null && !fragment.split(".").every(isValidLabel)) {
    return {
      valid: false,
      error: `Invalid TLD fragment: "${fragment}".`
    };
  }
  return { valid: true, query, keyword, fragment };
}
function isValidLabel(value) {
  return value.length <= 63 && /^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$/.test(value);
}
function getOrder(value) {
  const order = value ?? DEFAULT_ORDER;
  if (!ORDERS.includes(order)) {
    return {
      valid: false,
      error: `Invalid order: "${order}". Valid orders: ${ORDERS.join(", ")}`
    };
  }
  return { valid: true, order };
}
function getContinuationCommand(query, order, tlds, availableOnly, cursor) {
  const flags = [];
  if (order !== DEFAULT_ORDER) {
    flags.push(`--order=${order}`);
  }
  flags.push(...tlds.map((tld) => `--tld=${tld}`));
  if (availableOnly) {
    flags.push("--available");
  }
  flags.push(`--next ${cursor}`);
  return `domains search ${query} ${flags.join(" ")}`;
}
function normalizeTldFilters(values) {
  if (values === void 0) {
    return { valid: true, tlds: [] };
  }
  const tlds = Array.from(
    new Set(values.map((value) => value.trim().toLowerCase().replace(/^\./, "")))
  ).sort();
  const invalidTld = tlds.find(
    (tld) => tld.length === 0 || !tld.split(".").every(isValidLabel)
  );
  if (invalidTld !== void 0) {
    return {
      valid: false,
      error: `Invalid TLD filter: "${invalidTld}".`
    };
  }
  return { valid: true, tlds };
}
function areStringArraysEqual(left, right) {
  return left.length === right.length && left.every((value, index) => value === right[index]);
}
function getLimit(value) {
  const limit = value ?? DEFAULT_LIMIT;
  if (!Number.isInteger(limit) || limit < 1 || limit > MAX_LIMIT) {
    return {
      valid: false,
      error: `Invalid limit: "${limit}". Provide a number from 1 to ${MAX_LIMIT}.`
    };
  }
  return { valid: true, limit };
}
function filterTlds(tlds, fragment, filters, order) {
  const filterSet = new Set(filters);
  const matchingTlds = tlds.filter(
    (tld) => (fragment === null || tld.startsWith(fragment)) && (filterSet.size === 0 || filterSet.has(tld))
  );
  return order === "relevance" ? matchingTlds.sort(
    (a, b) => a === fragment ? -1 : b === fragment ? 1 : 0
  ) : matchingTlds;
}
async function getCandidatePage(client, keyword, tlds, offset, limit, availableOnly) {
  const pageTlds = tlds.slice(offset, offset + limit);
  const candidates = await quoteCandidates(
    client,
    pageTlds.map((tld) => `${keyword}.${tld}`)
  );
  return {
    results: availableOnly ? candidates.filter((candidate) => candidate.available) : candidates,
    lastTld: pageTlds.at(-1) ?? null,
    hasMore: offset + pageTlds.length < tlds.length
  };
}
async function quoteCandidates(client, domains2) {
  if (domains2.length === 0) {
    return [];
  }
  const response = domainsSearchResponseSchema.parse(
    await client.fetch("/v1/registrar/domains/search", {
      method: "POST",
      body: {
        domains: domains2
      }
    })
  );
  const resultsByDomain = new Map(
    response.results.map((result) => [result.domain, result])
  );
  return domains2.map((domain2) => {
    const result = resultsByDomain.get(domain2);
    if (!result) {
      throw new Error(`Missing registrar search result for ${domain2}.`);
    }
    if (!result.available) {
      return {
        domain: result.domain,
        available: false,
        purchasePrice: null,
        renewalPrice: null,
        years: null
      };
    }
    return {
      domain: result.domain,
      available: true,
      purchasePrice: result.price,
      renewalPrice: result.renewalPrice,
      years: result.years
    };
  });
}
function renderTable(results) {
  return table([
    ["Domain", "Availability", "Purchase", "Renewal"],
    ...results.map((result) => [
      result.domain,
      result.available ? "Available" : "Unavailable",
      result.available ? formatPrice(result.purchasePrice, result.years) : "-",
      result.available ? formatPrice(result.renewalPrice, result.years) : "-"
    ])
  ]);
}
function formatPrice(price2, years) {
  if (price2 === null || years === null) {
    return "-";
  }
  return `$${price2} / ${years} ${years === 1 ? "year" : "years"}`;
}

// src/util/telemetry/commands/domains/index.ts
var DomainsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandInspect(actual) {
    this.trackCliSubcommand({
      subcommand: "inspect",
      value: actual
    });
  }
  trackCliSubcommandMove(actual) {
    this.trackCliSubcommand({
      subcommand: "move",
      value: actual
    });
  }
  trackCliSubcommandBuy(actual) {
    this.trackCliSubcommand({
      subcommand: "buy",
      value: actual
    });
  }
  trackCliSubcommandCheck(actual) {
    this.trackCliSubcommand({
      subcommand: "check",
      value: actual
    });
  }
  trackCliSubcommandPrice(actual) {
    this.trackCliSubcommand({
      subcommand: "price",
      value: actual
    });
  }
  trackCliSubcommandSearch(actual) {
    this.trackCliSubcommand({
      subcommand: "search",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandTransferIn(actual) {
    this.trackCliSubcommand({
      subcommand: "transfer-in",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
};

// src/commands/domains/index.ts
var COMMAND_CONFIG14 = {
  add: ["add"],
  buy: ["buy"],
  check: ["check"],
  inspect: ["inspect"],
  ls: ["ls", "list"],
  move: ["move"],
  price: ["price"],
  search: ["search"],
  rm: ["rm", "remove"],
  transferIn: ["transfer-in"]
};
async function main9(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(domainsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new DomainsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG14
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("domains");
    output_manager_default.print(help(domainsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: domainsCommand, columns: client.stderr.columns })
    );
    return 2;
  }
  switch (subcommand) {
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(addSubcommand4);
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add4(client, args);
    case "inspect":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(inspectSubcommand2);
      }
      telemetry2.trackCliSubcommandInspect(subcommandOriginal);
      return inspect(client, args);
    case "move":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(moveSubcommand);
      }
      telemetry2.trackCliSubcommandMove(subcommandOriginal);
      return move(client, args);
    case "buy":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(buySubcommand);
      }
      telemetry2.trackCliSubcommandBuy(subcommandOriginal);
      return buy(client, args);
    case "check":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(checkSubcommand);
      }
      telemetry2.trackCliSubcommandCheck(subcommandOriginal);
      return check(client, args);
    case "price":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(priceSubcommand);
      }
      telemetry2.trackCliSubcommandPrice(subcommandOriginal);
      return price(client, args);
    case "search":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(searchSubcommand);
      }
      telemetry2.trackCliSubcommandSearch(subcommandOriginal);
      return search(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(removeSubcommand6);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm6(client, args);
    case "transferIn":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(transferInSubcommand);
      }
      telemetry2.trackCliSubcommandTransferIn(subcommandOriginal);
      return transferIn(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("domains", subcommandOriginal);
        return printHelp(transferInSubcommand);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls6(client, args);
  }
}

// src/commands/firewall/overview.ts
var import_chalk65 = __toESM(require_source(), 1);

// src/commands/firewall/shared.ts
var import_chalk63 = __toESM(require_source(), 1);

// src/util/firewall/list-firewall-configs.ts
async function listFirewallConfigs(client, projectId, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/config?${query.toString()}`;
  return client.fetch(url);
}

// src/util/firewall/activate-firewall-config.ts
async function activateFirewallConfig(client, projectId, configVersion, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/config/${configVersion}/activate?${query.toString()}`;
  return client.fetch(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({})
  });
}

// src/commands/firewall/shared.ts
function withGlobalFlags6(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
async function parseSubcommandArgs2(argv, command, client, commandPath) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(command.options);
  const fullPath = commandPath || command.name;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    if (client?.nonInteractive) {
      const rawMessage = err instanceof Error ? err.message : String(err);
      const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: rawMessage,
          next: [
            {
              command: getCommandNamePlain(
                `firewall ${fullPath} ${flags.join(" ")}`.trim()
              ),
              when: "fix flags and retry"
            }
          ]
        },
        1
      );
      return 1;
    }
    printError(err);
    return 1;
  }
  return parsedArgs;
}
async function ensureProjectLink2(client) {
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    if (client.nonInteractive) {
      const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
      const cmd2 = getCommandNamePlain(`link ${flags.join(" ")}`.trim());
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          userActionRequired: true,
          message: "Your codebase is not linked to a Vercel project. Run link first, then retry firewall commands.",
          next: [
            {
              command: cmd2,
              when: "to link this directory to a project"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  return link;
}
async function confirmAction(client, skipConfirmation, message, details) {
  if (skipConfirmation)
    return true;
  if (client.nonInteractive || !client.stdin.isTTY) {
    outputAgentError(client, {
      status: AGENT_STATUS.ERROR,
      reason: AGENT_REASON.CONFIRMATION_REQUIRED,
      message: `${message} Re-run with --yes to confirm.`,
      next: [
        {
          command: buildCommandWithYes(client.argv),
          when: "re-run with --yes to confirm"
        }
      ]
    });
    process.exit(1);
    return false;
  }
  if (details) {
    output_manager_default.print(`  ${details}
`);
  }
  return await client.input.confirm(message, false);
}
function outputJson2(client, data) {
  output_manager_default.stopSpinner();
  client.stdout.write(JSON.stringify(data, null, 2) + "\n");
}
async function detectExistingDraft(client, projectId, teamId) {
  const { draft } = await listFirewallConfigs(client, projectId, { teamId });
  return draft !== null && draft.changes.length > 0;
}
async function offerAutoPublish(client, projectId, hadExistingDraft, opts) {
  output_manager_default.print(
    `
  ${import_chalk63.default.gray(`This change is staged. Run ${import_chalk63.default.cyan(getCommandName("firewall publish"))} to make it live, or ${import_chalk63.default.cyan(getCommandName("firewall discard"))} to undo.`)}
`
  );
  if (!hadExistingDraft && !opts.skipPrompts && client.stdin.isTTY && !client.nonInteractive) {
    output_manager_default.print("\n");
    const shouldPublish = await client.input.confirm(
      "This is the only draft change. Publish to production now?",
      false
    );
    if (shouldPublish) {
      const publishStamp = stamp_default();
      output_manager_default.spinner("Publishing to production");
      try {
        await activateFirewallConfig(client, projectId, "draft", {
          teamId: opts.teamId
        });
        output_manager_default.log(
          `${import_chalk63.default.cyan("Published")} to production ${import_chalk63.default.gray(publishStamp())}`
        );
      } catch (e2) {
        const err = e2;
        output_manager_default.error(
          `Failed to publish to production: ${err.message || "Unknown error"}`
        );
      }
    }
  } else if (hadExistingDraft) {
    output_manager_default.warn(
      `There are other draft changes. Review with ${import_chalk63.default.cyan(getCommandName("firewall diff"))} before publishing.`
    );
  }
}
function resolveIpRule(ips, identifier) {
  if (!identifier)
    return [];
  const byId = ips.find((r) => r.id === identifier);
  if (byId)
    return [byId];
  const query = identifier.toLowerCase();
  const byIp = ips.filter((r) => r.ip.toLowerCase() === query);
  if (byIp.length > 0)
    return byIp;
  const byPartialId = ips.filter((r) => r.id.toLowerCase().includes(query));
  return byPartialId;
}
function resolveRule(rules, identifier) {
  if (!identifier)
    return [];
  const byId = rules.find((r) => r.id === identifier);
  if (byId)
    return [byId];
  const query = identifier.toLowerCase();
  const byName = rules.filter((r) => r.name.toLowerCase() === query);
  if (byName.length > 0)
    return byName;
  const byPartialName = rules.filter((r) => r.name.toLowerCase().includes(query));
  if (byPartialName.length > 0)
    return byPartialName;
  const byPartialId = rules.filter((r) => r.id.toLowerCase().includes(query));
  return byPartialId;
}
function printActionImpactWarning(action) {
  const actionType = action.mitigate?.action;
  if (!actionType)
    return;
  switch (actionType) {
    case "deny":
      output_manager_default.warn(
        "This rule will deny matching requests. Legitimate traffic may be blocked if conditions are too broad."
      );
      break;
    case "challenge":
      output_manager_default.warn(
        "This rule will challenge matching requests with a verification page. Some legitimate users or automated clients may be unable to complete the challenge."
      );
      break;
    case "rate_limit":
      output_manager_default.warn(
        "This rule will rate limit matching requests. Legitimate traffic may be throttled if the limit is too low or keys are too broad."
      );
      break;
    default:
      break;
  }
}

// src/util/firewall/get-bypass.ts
async function getBypass(client, projectId, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/bypass?${query.toString()}`;
  return client.fetch(url);
}

// src/util/firewall/format.ts
var import_chalk64 = __toESM(require_source(), 1);
function isAllSourcesBypass(ip) {
  return ip === "0.0.0.0/0" || ip === "::/0";
}
function isMitigationsPaused(bypass) {
  const now = Math.floor(Date.now() / 1e3);
  return bypass.some(
    (b) => isAllSourcesBypass(b.Ip) && b.Domain === "*" && (b.ExpiresAt === null || b.ExpiresAt === void 0 || b.ExpiresAt > now)
  );
}
function formatAttackModeStatus(status3) {
  if (!status3.enabled) {
    return import_chalk64.default.dim("Off");
  }
  if (status3.activeUntil) {
    const remainingMs = status3.activeUntil - Date.now();
    if (remainingMs <= 0) {
      return import_chalk64.default.dim("Off (expired)");
    }
    const hours = Math.floor(remainingMs / (60 * 60 * 1e3));
    const minutes = Math.floor(remainingMs % (60 * 60 * 1e3) / (60 * 1e3));
    return import_chalk64.default.red(`On (expires in ${hours}h ${minutes}m)`);
  }
  return import_chalk64.default.red("On");
}
function formatMitigationsStatus(bypass) {
  if (isMitigationsPaused(bypass)) {
    const entry = bypass.find(
      (b) => isAllSourcesBypass(b.Ip) && b.Domain === "*"
    );
    if (entry?.ExpiresAt) {
      const remainingMs = entry.ExpiresAt * 1e3 - Date.now();
      if (remainingMs > 0) {
        const hours = Math.floor(remainingMs / (60 * 60 * 1e3));
        const minutes = Math.floor(
          remainingMs % (60 * 60 * 1e3) / (60 * 1e3)
        );
        return import_chalk64.default.yellow(`Paused (auto-resumes in ${hours}h ${minutes}m)`);
      }
    }
    return import_chalk64.default.yellow("Paused");
  }
  return import_chalk64.default.green("Active");
}
function formatStatusOutput(active, draft, bypass, attackMode) {
  const lines = [];
  if (active) {
    const enabled = active.firewallEnabled;
    lines.push(
      `  ${import_chalk64.default.bold("Firewall:")}             ${enabled ? import_chalk64.default.green("Enabled") : import_chalk64.default.red("Disabled")}`
    );
    const activeRules = active.rules.filter((r) => r.active).length;
    const inactiveRules = active.rules.filter((r) => !r.active).length;
    const totalRules = active.rules.length;
    lines.push(
      `  ${import_chalk64.default.bold("Custom Rules:")}         ${activeRules} active, ${inactiveRules} inactive (${totalRules} total)`
    );
    lines.push(`  ${import_chalk64.default.bold("IP Blocks:")}            ${active.ips.length}`);
  } else {
    lines.push(
      `  ${import_chalk64.default.bold("Firewall:")}             ${import_chalk64.default.dim("Not configured")}`
    );
  }
  const regularBypasses = bypass.filter((b) => !isAllSourcesBypass(b.Ip));
  lines.push(
    `  ${import_chalk64.default.bold("System Bypass:")}        ${regularBypasses.length} IP${regularBypasses.length !== 1 ? "s" : ""}`
  );
  lines.push("");
  if (attackMode) {
    lines.push(
      `  ${import_chalk64.default.bold("Attack Mode:")}          ${formatAttackModeStatus(attackMode)}`
    );
  }
  lines.push(
    `  ${import_chalk64.default.bold("System Mitigations:")}   ${formatMitigationsStatus(bypass)}`
  );
  if (draft && draft.changes.length > 0) {
    lines.push("");
    lines.push(
      `  ${import_chalk64.default.bold("Pending Draft:")}        ${import_chalk64.default.yellow(`${draft.changes.length} unpublished change${draft.changes.length !== 1 ? "s" : ""}`)}`
    );
    const activeRulesMap = new Map((active?.rules || []).map((r) => [r.id, r]));
    lines.push(formatDiffOutput(draft.changes, activeRulesMap));
  }
  return lines.join("\n");
}
function formatBypassTable(bypasses) {
  const lines = [];
  const domains2 = bypasses.map(
    (b) => b.Domain === "*" ? "All domains" : b.Domain
  );
  const ipWidth = Math.max("IP/CIDR".length, ...bypasses.map((b) => b.Ip.length));
  const domainWidth = Math.max("Domain".length, ...domains2.map((d) => d.length));
  const gap = 3;
  lines.push(
    `  ${import_chalk64.default.dim("IP/CIDR".padEnd(ipWidth + gap))}${import_chalk64.default.dim("Domain".padEnd(domainWidth + gap))}${import_chalk64.default.dim("Note")}`
  );
  for (let i = 0; i < bypasses.length; i++) {
    const bypass = bypasses[i];
    const ip = bypass.Ip.padEnd(ipWidth + gap);
    const domain2 = domains2[i].padEnd(domainWidth + gap);
    const note = bypass.Note || "";
    lines.push(`  ${ip}${domain2}${note}`);
  }
  return lines.join("\n");
}
function annotateIpRules(activeIps, draftIps, changes) {
  if (!draftIps) {
    return activeIps.map((rule) => ({ rule, status: "live" }));
  }
  const addedIds = new Set(
    changes.filter((c) => c.action === "ip.insert").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const removedIds = new Set(
    changes.filter((c) => c.action === "ip.remove").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const modifiedIds = new Set(
    changes.filter((c) => c.action === "ip.update").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const result = [];
  for (const rule of draftIps) {
    if (addedIds.has(rule.id)) {
      result.push({ rule, status: "added" });
    } else if (modifiedIds.has(rule.id)) {
      result.push({ rule, status: "modified" });
    } else {
      result.push({ rule, status: "live" });
    }
  }
  for (const rule of activeIps) {
    if (removedIds.has(rule.id)) {
      result.push({ rule, status: "removed" });
    }
  }
  return result;
}
function formatIpBlocksTable(annotated) {
  const lines = [];
  const hostnames = annotated.map(
    (a) => a.rule.hostname === "*" || a.rule.hostname === "" ? "All hosts" : a.rule.hostname
  );
  const gap = 3;
  const prefixWidth = 2;
  const ipWidth = Math.max(
    "IP/CIDR".length,
    ...annotated.map((a) => a.rule.ip.length)
  );
  const hostnameWidth = Math.max(
    "Hostname".length,
    ...hostnames.map((h) => h.length)
  );
  lines.push(
    `  ${" ".repeat(prefixWidth)}${import_chalk64.default.dim("IP/CIDR".padEnd(ipWidth + gap))}${import_chalk64.default.dim("Hostname".padEnd(hostnameWidth + gap))}${import_chalk64.default.dim("Notes")}`
  );
  for (let i = 0; i < annotated.length; i++) {
    const { rule, status: status3 } = annotated[i];
    const ip = rule.ip.padEnd(ipWidth + gap);
    const hostname = hostnames[i].padEnd(hostnameWidth + gap);
    const notes = rule.notes || "";
    let prefix = "  ";
    let colorFn = (s) => s;
    if (status3 === "added") {
      prefix = "+ ";
      colorFn = import_chalk64.default.green;
    } else if (status3 === "removed") {
      prefix = "- ";
      colorFn = import_chalk64.default.red;
    } else if (status3 === "modified") {
      prefix = "~ ";
      colorFn = import_chalk64.default.yellow;
    }
    lines.push(colorFn(`  ${prefix}${ip}${hostname}${notes}`));
  }
  return lines.join("\n");
}
function getDiffSymbol(action) {
  if (action.endsWith(".insert")) {
    return { symbol: "+", color: import_chalk64.default.green };
  }
  if (action.endsWith(".remove")) {
    return { symbol: "-", color: import_chalk64.default.red };
  }
  return { symbol: "~", color: import_chalk64.default.yellow };
}
function formatChangeDescription(change2, activeRules) {
  const { action, id, value } = change2;
  switch (action) {
    case "rules.insert": {
      const rule = value;
      return `Added rule "${rule?.name || id || "unknown"}"`;
    }
    case "rules.update": {
      const draft = value;
      const ruleName = draft?.name || id || "unknown";
      if (activeRules && id && draft && typeof draft.active === "boolean") {
        const activeRule = activeRules.get(id);
        if (activeRule && activeRule.active !== draft.active) {
          const nameUnchanged = activeRule.name === draft.name;
          const conditionsUnchanged = JSON.stringify(activeRule.conditionGroup) === JSON.stringify(draft.conditionGroup);
          const actionUnchanged = JSON.stringify(activeRule.action) === JSON.stringify(draft.action);
          const descriptionUnchanged = (activeRule.description || "") === (draft.description || "");
          if (nameUnchanged && conditionsUnchanged && actionUnchanged && descriptionUnchanged) {
            return draft.active ? `Enabled rule "${ruleName}"` : `Disabled rule "${ruleName}"`;
          }
        }
      }
      return `Modified rule "${ruleName}"`;
    }
    case "rules.remove":
      return `Removed rule "${id || "unknown"}"`;
    case "rules.priority": {
      const rule = value;
      return `Reordered rule "${rule?.name || id || "unknown"}"`;
    }
    case "ip.insert": {
      const ip = value;
      return `Added IP block ${ip?.ip || "unknown"}`;
    }
    case "ip.remove":
      return `Removed IP block ${id || "unknown"}`;
    case "ip.update": {
      const ip = value;
      return `Modified IP block ${ip?.ip || id || "unknown"}`;
    }
    case "firewallEnabled":
      return `${value ? "Enabled" : "Disabled"} firewall`;
    case "crs.update":
      return "Updated OWASP CRS configuration";
    case "crs.disable":
      return "Disabled OWASP CRS";
    case "managedRules.update":
      return "Updated managed ruleset configuration";
    case "managedRuleGroup.update":
      return "Updated managed rule group";
    case "botId.toggle":
      return `${value ? "Enabled" : "Disabled"} Bot ID detection`;
    case "ja3Enabled":
      return `${value ? "Enabled" : "Disabled"} JA3 fingerprinting`;
    case "ja4Enabled":
      return `${value ? "Enabled" : "Disabled"} JA4 fingerprinting`;
    case "logHeaders.update":
      return "Updated log headers configuration";
    default:
      return `${action}${id ? ` (${id})` : ""}`;
  }
}
function formatRuleFieldDiff(activeRule, draftValue) {
  const lines = [];
  if (draftValue.name && activeRule.name !== draftValue.name) {
    lines.push(
      import_chalk64.default.yellow(`      ~ Name: "${activeRule.name}" \u2192 "${draftValue.name}"`)
    );
  }
  if (draftValue.action && JSON.stringify(activeRule.action) !== JSON.stringify(draftValue.action)) {
    const oldAction = formatActionDisplay(activeRule.action);
    const newAction = formatActionDisplay(draftValue.action);
    lines.push(import_chalk64.default.yellow(`      ~ Action: ${oldAction} \u2192 ${newAction}`));
  }
  if (draftValue.conditionGroup && JSON.stringify(activeRule.conditionGroup) !== JSON.stringify(draftValue.conditionGroup)) {
    const oldConditions = new Set(
      activeRule.conditionGroup.flatMap(
        (g) => g.conditions.map((c) => formatConditionCompact(c))
      )
    );
    const newConditions = new Set(
      draftValue.conditionGroup.flatMap(
        (g) => g.conditions.map((c) => formatConditionCompact(c))
      )
    );
    const added = [...newConditions].filter((c) => !oldConditions.has(c));
    const removed = [...oldConditions].filter((c) => !newConditions.has(c));
    for (const c of added) {
      lines.push(import_chalk64.default.green(`      + Condition: ${c}`));
    }
    for (const c of removed) {
      lines.push(import_chalk64.default.red(`      - Condition: ${c}`));
    }
    if (added.length === 0 && removed.length === 0) {
      lines.push(import_chalk64.default.yellow("      ~ Condition groups restructured"));
    }
  }
  return lines;
}
var CHANGE_ACTION_ORDER = {
  firewallEnabled: 0,
  "rules.insert": 1,
  "rules.update": 2,
  "rules.remove": 3,
  "rules.priority": 4,
  "ip.insert": 5,
  "ip.update": 6,
  "ip.remove": 7,
  "crs.update": 8,
  "crs.disable": 9,
  "managedRules.update": 10,
  "managedRuleGroup.update": 11,
  "botId.toggle": 12,
  ja3Enabled: 13,
  ja4Enabled: 14,
  "logHeaders.update": 15
};
function formatDiffOutput(changes, activeRules) {
  const lines = [];
  const sorted = [...changes].sort(
    (a, b) => (CHANGE_ACTION_ORDER[a.action] ?? 99) - (CHANGE_ACTION_ORDER[b.action] ?? 99)
  );
  for (const change2 of sorted) {
    const { symbol, color } = getDiffSymbol(change2.action);
    const description = formatChangeDescription(change2, activeRules);
    lines.push(color(`  ${symbol} ${description}`));
    if (change2.action === "rules.update" && activeRules && change2.id) {
      const activeRule = activeRules.get(change2.id);
      if (activeRule && change2.value) {
        const subLines = formatRuleFieldDiff(
          activeRule,
          change2.value
        );
        lines.push(...subLines);
      }
    }
  }
  return lines.join("\n");
}
var OPERATOR_LABELS = {
  eq: "equals",
  neq: "does not equal",
  re: "matches regex",
  ex: "exists",
  nex: "does not exist",
  inc: "is any of",
  ninc: "is not any of",
  pre: "starts with",
  suf: "ends with",
  sub: "contains",
  gt: ">",
  gte: ">=",
  lt: "<",
  lte: "<="
};
var NEGATED_OPERATOR_LABELS = {
  eq: "does not equal",
  neq: "equals",
  re: "does not match regex",
  ex: "does not exist",
  nex: "exists",
  inc: "is not any of",
  ninc: "is any of",
  pre: "does not start with",
  suf: "does not end with",
  sub: "does not contain",
  gt: "NOT >",
  gte: "NOT >=",
  lt: "NOT <",
  lte: "NOT <="
};
var CONDITION_TYPE_LABELS = {
  path: "path",
  raw_path: "raw path",
  target_path: "target path",
  route: "route",
  server_action: "server action",
  method: "method",
  host: "host",
  protocol: "protocol",
  scheme: "scheme",
  environment: "environment",
  region: "region",
  ip_address: "IP address",
  user_agent: "user agent",
  geo_country: "geo country",
  geo_continent: "geo continent",
  geo_country_region: "state/region",
  geo_city: "geo city",
  geo_as_number: "geo AS number",
  header: "header",
  cookie: "cookie",
  query: "query string",
  ja3_digest: "JA3 digest",
  ja4_digest: "JA4 digest",
  request_body: "request body",
  rate_limit_api_id: "rate limit API ID",
  bot_name: "bot name",
  bot_category: "bot category"
};
function getConditionTypeLabel(type, key) {
  const label = CONDITION_TYPE_LABELS[type] || type;
  if (key)
    return `${label}[${key}]`;
  return label;
}
function getOperatorLabel(op, neg) {
  if (neg) {
    return NEGATED_OPERATOR_LABELS[op] || `NOT ${OPERATOR_LABELS[op] || op}`;
  }
  return OPERATOR_LABELS[op] || op;
}
function formatConditionValue(condition) {
  if (condition.op === "ex" || condition.op === "nex") {
    return "";
  }
  if (Array.isArray(condition.value)) {
    return condition.value.join(", ");
  }
  return String(condition.value ?? "");
}
function formatConditionCompact(condition) {
  const type = getConditionTypeLabel(condition.type, condition.key);
  const op = getOperatorLabel(condition.op, condition.neg);
  const value = formatConditionValue(condition);
  if (!value)
    return `${type} ${op}`;
  return `${type} ${op} ${value}`;
}
function formatActionDisplay(action) {
  const mitigate = action.mitigate;
  if (!mitigate)
    return import_chalk64.default.dim("None");
  const actionType = mitigate.action;
  const duration = mitigate.actionDuration;
  switch (actionType) {
    case "deny":
      return duration ? `Deny (${duration})` : "Deny";
    case "challenge":
      return duration ? `Challenge (${duration})` : "Challenge";
    case "log":
      return "Log";
    case "bypass":
      return "Bypass";
    case "rate_limit": {
      const rl = mitigate.rateLimit;
      if (rl) {
        return `Rate Limit (${rl.limit}/${rl.window}s)`;
      }
      return "Rate Limit";
    }
    case "redirect": {
      const rd = mitigate.redirect;
      if (rd) {
        const code2 = rd.permanent ? "301" : "307";
        return `Redirect \u2192 ${rd.location} (${code2})`;
      }
      return "Redirect";
    }
    default:
      return actionType;
  }
}
function annotateRules(activeRules, draftRules, changes) {
  if (!draftRules) {
    return activeRules.map((rule) => ({ rule, status: "live" }));
  }
  const addedIds = new Set(
    changes.filter((c) => c.action === "rules.insert").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const removedIds = new Set(
    changes.filter((c) => c.action === "rules.remove").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const modifiedIds = new Set(
    changes.filter((c) => c.action === "rules.update" || c.action === "rules.priority").map((c) => c.id).filter((id) => id !== null && id !== void 0)
  );
  const result = [];
  for (const rule of draftRules) {
    if (addedIds.has(rule.id)) {
      result.push({ rule, status: "added" });
    } else if (modifiedIds.has(rule.id)) {
      result.push({ rule, status: "modified" });
    } else {
      result.push({ rule, status: "live" });
    }
  }
  for (const rule of activeRules) {
    if (removedIds.has(rule.id)) {
      result.push({ rule, status: "removed" });
    }
  }
  return result;
}
function formatRulesTable(annotated) {
  const lines = [];
  const gap = 3;
  const prefixWidth = 2;
  const numWidth = Math.max("#".length, String(annotated.length).length);
  const nameWidth = Math.max(
    "Name".length,
    ...annotated.map((a) => a.rule.name.length)
  );
  const actionTexts = annotated.map((a) => formatActionDisplay(a.rule.action));
  const actionWidth = Math.max(
    "Action".length,
    ...actionTexts.map((t) => t.length)
  );
  lines.push(
    `  ${" ".repeat(prefixWidth)}${import_chalk64.default.dim("#".padEnd(numWidth + gap))}${import_chalk64.default.dim("Name".padEnd(nameWidth + gap))}${import_chalk64.default.dim("Action".padEnd(actionWidth + gap))}${import_chalk64.default.dim("Status")}`
  );
  for (let i = 0; i < annotated.length; i++) {
    const { rule, status: status3 } = annotated[i];
    const num = String(i + 1).padEnd(numWidth + gap);
    const name = rule.name.padEnd(nameWidth + gap);
    const actionText = actionTexts[i].padEnd(actionWidth + gap);
    let prefix = "  ";
    let colorFn = (s) => s;
    if (status3 === "added") {
      prefix = "+ ";
      colorFn = import_chalk64.default.green;
    } else if (status3 === "removed") {
      prefix = "- ";
      colorFn = import_chalk64.default.red;
    } else if (status3 === "modified") {
      prefix = "~ ";
      colorFn = import_chalk64.default.yellow;
    }
    const activeStatusText = rule.active ? "Enabled" : "Disabled";
    const activeStatus = status3 === "removed" ? import_chalk64.default.dim(activeStatusText) : rule.active ? import_chalk64.default.green(activeStatusText) : import_chalk64.default.red(activeStatusText);
    lines.push(colorFn(`  ${prefix}${num}${name}${actionText}${activeStatus}`));
    const idIndent = " ".repeat(prefixWidth + numWidth + gap);
    lines.push(colorFn(`  ${idIndent}${import_chalk64.default.dim(rule.id)}`));
  }
  return lines.join("\n");
}
function formatConditionGroup(group, groupIndex, totalGroups) {
  const lines = [];
  const label = totalGroups > 1 ? `Group ${groupIndex + 1} (AND):` : "Conditions:";
  lines.push(`     ${import_chalk64.default.dim(label)}`);
  for (const condition of group.conditions) {
    lines.push(`       ${formatConditionCompact(condition)}`);
  }
  return lines.join("\n");
}
function formatRuleExpanded(rule, index) {
  const lines = [];
  const prefix = index !== void 0 ? `${index + 1}. ` : "";
  const status3 = rule.active ? import_chalk64.default.green("Enabled") : import_chalk64.default.red("Disabled");
  const action = formatActionDisplay(rule.action);
  lines.push(`  ${prefix}${import_chalk64.default.bold(rule.name)} [${status3}]`);
  if (rule.description) {
    lines.push(`     ${import_chalk64.default.dim(rule.description)}`);
  }
  lines.push("");
  if (rule.conditionGroup.length === 0) {
    lines.push(`     ${import_chalk64.default.dim("No conditions")}`);
  } else {
    for (let i = 0; i < rule.conditionGroup.length; i++) {
      lines.push(
        formatConditionGroup(
          rule.conditionGroup[i],
          i,
          rule.conditionGroup.length
        )
      );
      if (i < rule.conditionGroup.length - 1) {
        lines.push(`     ${import_chalk64.default.dim("OR")}`);
      }
    }
  }
  lines.push("");
  lines.push(`     ${import_chalk64.default.dim("Action:")} ${action}`);
  const duration = rule.action.mitigate?.actionDuration;
  if (duration && rule.action.mitigate?.action !== "rate_limit") {
    lines.push(`     ${import_chalk64.default.dim("Duration:")} ${duration}`);
  }
  const rl = rule.action.mitigate?.rateLimit;
  if (rl) {
    lines.push(
      `     ${import_chalk64.default.dim("Rate Limit:")} ${rl.limit} req / ${rl.window}s (${rl.algo})`
    );
    lines.push(`     ${import_chalk64.default.dim("Keys:")} ${rl.keys.join(", ")}`);
    if (rl.action) {
      const exceeded = duration ? `${rl.action} (${duration})` : rl.action;
      lines.push(`     ${import_chalk64.default.dim("If exceeded:")} ${exceeded}`);
    }
  }
  const rd = rule.action.mitigate?.redirect;
  if (rd) {
    lines.push(
      `     ${import_chalk64.default.dim("Redirect:")} ${rd.location} (${rd.permanent ? "301 permanent" : "307 temporary"})`
    );
  }
  return lines.join("\n");
}
function formatRuleDetail(rule) {
  const lines = [];
  lines.push(`  ${import_chalk64.default.bold("Rule:")}        ${rule.name}`);
  lines.push(`  ${import_chalk64.default.bold("ID:")}          ${import_chalk64.default.dim(rule.id)}`);
  lines.push(
    `  ${import_chalk64.default.bold("Status:")}      ${rule.active ? import_chalk64.default.green("Enabled") : import_chalk64.default.red("Disabled")}`
  );
  if (rule.description) {
    lines.push(`  ${import_chalk64.default.bold("Description:")} ${rule.description}`);
  }
  lines.push("");
  if (rule.conditionGroup.length === 0) {
    lines.push(`  ${import_chalk64.default.bold("Conditions:")}  ${import_chalk64.default.dim("No conditions")}`);
  } else {
    lines.push(`  ${import_chalk64.default.bold("Conditions:")}`);
    for (let i = 0; i < rule.conditionGroup.length; i++) {
      if (rule.conditionGroup.length > 1) {
        lines.push(`    ${import_chalk64.default.dim(`Group ${i + 1} (AND):`)}`);
      }
      for (const condition of rule.conditionGroup[i].conditions) {
        lines.push(`      ${formatConditionCompact(condition)}`);
      }
      if (i < rule.conditionGroup.length - 1) {
        lines.push(`    ${import_chalk64.default.dim("OR")}`);
      }
    }
  }
  lines.push("");
  lines.push(
    `  ${import_chalk64.default.bold("Action:")}      ${formatActionDisplay(rule.action)}`
  );
  const duration = rule.action.mitigate?.actionDuration;
  if (duration && rule.action.mitigate?.action !== "rate_limit") {
    lines.push(`  ${import_chalk64.default.bold("Duration:")}    ${duration}`);
  }
  const rl = rule.action.mitigate?.rateLimit;
  if (rl) {
    lines.push(`  ${import_chalk64.default.bold("Rate Limit:")}`);
    lines.push(`    Algorithm:    ${rl.algo}`);
    lines.push(`    Window:       ${rl.window}s`);
    lines.push(`    Limit:        ${rl.limit} requests`);
    lines.push(`    Keys:         ${rl.keys.join(", ")}`);
    if (rl.action) {
      const exceeded = duration ? `${rl.action} (${duration})` : rl.action;
      lines.push(`    If exceeded:  ${exceeded}`);
    }
  }
  const rd = rule.action.mitigate?.redirect;
  if (rd) {
    lines.push(`  ${import_chalk64.default.bold("Redirect:")}`);
    lines.push(`    Location:   ${rd.location}`);
    lines.push(
      `    Type:       ${rd.permanent ? "301 (permanent)" : "307 (temporary)"}`
    );
  }
  return lines.join("\n");
}

// src/commands/firewall/overview.ts
async function overview(client, argv) {
  const parsed = await parseSubcommandArgs2(argv, overviewSubcommand, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching firewall overview for ${import_chalk65.default.bold(project.name)}`);
  try {
    const [configList, bypassList, freshProject] = await Promise.all([
      listFirewallConfigs(client, project.id, { teamId }),
      getBypass(client, project.id, { teamId }),
      client.fetch(
        `/v9/projects/${encodeURIComponent(project.id)}`,
        { accountId: teamId }
      )
    ]);
    const { active, draft } = configList;
    const attackMode = {
      enabled: freshProject.security?.attackModeEnabled ?? false,
      activeUntil: freshProject.security?.attackModeActiveUntil
    };
    if (parsed.flags["--json"]) {
      outputJson2(client, {
        active,
        draft,
        bypass: bypassList.result,
        attackMode
      });
      return 0;
    }
    output_manager_default.print("\n");
    output_manager_default.print(
      formatStatusOutput(active, draft, bypassList.result, attackMode)
    );
    output_manager_default.print("\n\n");
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch firewall overview";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [{ command: withGlobalFlags6(client, "firewall overview") }]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/diff.ts
var import_chalk66 = __toESM(require_source(), 1);
async function diff(client, argv) {
  const parsed = await parseSubcommandArgs2(argv, diffSubcommand, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching draft changes for ${import_chalk66.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    if (!draft || draft.changes.length === 0) {
      if (parsed.flags["--json"]) {
        outputJson2(client, { changes: [] });
        return 0;
      }
      output_manager_default.log("No pending changes.");
      return 0;
    }
    if (parsed.flags["--json"]) {
      outputJson2(client, { changes: draft.changes });
      return 0;
    }
    const activeRulesMap = new Map((active?.rules || []).map((r) => [r.id, r]));
    output_manager_default.print(
      `
${import_chalk66.default.bold(`Pending changes (${draft.changes.length}):`)}

`
    );
    output_manager_default.print(formatDiffOutput(draft.changes, activeRulesMap));
    output_manager_default.print("\n\n");
    output_manager_default.print(
      `  Run ${import_chalk66.default.cyan(getCommandName("firewall publish"))} to publish, or ${import_chalk66.default.cyan(getCommandName("firewall discard"))} to discard.

`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch draft changes";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [{ command: withGlobalFlags6(client, "firewall diff") }]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/publish.ts
var import_chalk67 = __toESM(require_source(), 1);
async function publish(client, argv) {
  const parsed = await parseSubcommandArgs2(argv, publishSubcommand, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching draft changes for ${import_chalk67.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    if (!draft || draft.changes.length === 0) {
      output_manager_default.warn("No draft changes to publish.");
      return 0;
    }
    const activeRulesMap = new Map((active?.rules || []).map((r) => [r.id, r]));
    output_manager_default.print(
      `
${import_chalk67.default.bold(`Changes to be published (${draft.changes.length}):`)}

`
    );
    output_manager_default.print(formatDiffOutput(draft.changes, activeRulesMap));
    output_manager_default.print("\n\n");
    const confirmed = await confirmAction(
      client,
      parsed.flags["--yes"],
      "Publish these changes to production?",
      `This will make them live for ${import_chalk67.default.bold(project.name)}.`
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
    const updateStamp = stamp_default();
    output_manager_default.spinner("Publishing to production");
    await activateFirewallConfig(client, project.id, "draft", { teamId });
    output_manager_default.log(
      `${import_chalk67.default.cyan("Success!")} Firewall config published to production ${import_chalk67.default.gray(updateStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to publish firewall config";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [{ command: withGlobalFlags6(client, "firewall publish --yes") }]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/discard.ts
var import_chalk68 = __toESM(require_source(), 1);

// src/util/firewall/delete-firewall-draft.ts
async function deleteFirewallDraft(client, projectId, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/config/draft?${query.toString()}`;
  await client.fetch(url, {
    method: "DELETE"
  });
}

// src/commands/firewall/discard.ts
async function discard(client, argv) {
  const parsed = await parseSubcommandArgs2(argv, discardSubcommand, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching draft changes for ${import_chalk68.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    if (!draft || draft.changes.length === 0) {
      output_manager_default.warn("No draft changes to discard.");
      return 0;
    }
    const activeRulesMap = new Map((active?.rules || []).map((r) => [r.id, r]));
    output_manager_default.print(
      `
${import_chalk68.default.bold(`Changes to be discarded (${draft.changes.length}):`)}

`
    );
    output_manager_default.print(formatDiffOutput(draft.changes, activeRulesMap));
    output_manager_default.print("\n\n");
    const confirmed = await confirmAction(
      client,
      parsed.flags["--yes"],
      "Discard all draft changes?",
      "This action cannot be undone."
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
    const updateStamp = stamp_default();
    output_manager_default.spinner("Discarding draft changes");
    await deleteFirewallDraft(client, project.id, { teamId });
    output_manager_default.log(
      `${import_chalk68.default.cyan("Success!")} Draft changes discarded ${import_chalk68.default.gray(updateStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to discard draft changes";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [{ command: withGlobalFlags6(client, "firewall discard --yes") }]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/system-bypass/list.ts
var import_chalk69 = __toESM(require_source(), 1);
async function list6(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    systemBypassListSubcommand,
    client,
    "system-bypass list"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(
    `Fetching system bypass rules for ${import_chalk69.default.bold(project.name)}`
  );
  try {
    const { result: allBypasses } = await getBypass(client, project.id, {
      teamId
    });
    const bypasses = allBypasses.filter((b) => !isAllSourcesBypass(b.Ip));
    if (parsed.flags["--json"]) {
      outputJson2(client, bypasses);
      return 0;
    }
    if (bypasses.length === 0) {
      output_manager_default.log("No system bypass rules configured.");
      return 0;
    }
    output_manager_default.print(`
${formatBypassTable(bypasses)}

`);
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch bypass rules";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(client, "firewall system-bypass list")
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/system-bypass/add.ts
var import_chalk70 = __toESM(require_source(), 1);

// src/util/firewall/add-bypass.ts
async function addBypass(client, projectId, body, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/bypass?${query.toString()}`;
  return client.fetch(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body)
  });
}

// src/util/firewall/validate.ts
import { isIP } from "net";
function parseCidr(input) {
  const slashIndex = input.lastIndexOf("/");
  if (slashIndex === -1)
    return null;
  const ip = input.slice(0, slashIndex);
  const prefixStr = input.slice(slashIndex + 1);
  const prefix = Number.parseInt(prefixStr, 10);
  if (Number.isNaN(prefix) || prefix < 0)
    return null;
  if (String(prefix) !== prefixStr)
    return null;
  return { ip, prefix };
}
function ipVersion(ip) {
  return isIP(ip);
}
function validateBypassIp(input) {
  const version = ipVersion(input);
  if (version === 4 || version === 6) {
    return null;
  }
  const cidr = parseCidr(input);
  if (!cidr) {
    return "Please enter a valid IP address or CIDR.";
  }
  const cidrVersion = ipVersion(cidr.ip);
  if (cidrVersion === 0) {
    return "Please enter a valid IP address or CIDR.";
  }
  if (cidrVersion === 4) {
    if (cidr.prefix > 32) {
      return "Please enter a valid IP address or CIDR.";
    }
    if (input === "0.0.0.0/0")
      return null;
    if (cidr.prefix < 16) {
      return "IPv4 CIDR cannot have a net mask less than /16.";
    }
  }
  if (cidrVersion === 6) {
    if (cidr.prefix > 128) {
      return "Please enter a valid IP address or CIDR.";
    }
    if (input === "::/0")
      return null;
    if (cidr.prefix < 16) {
      return "IPv6 CIDR cannot have a net mask less than /16.";
    }
  }
  return null;
}
function validateBlockingIp(input) {
  const version = ipVersion(input);
  if (version === 4 || version === 6) {
    return null;
  }
  const cidr = parseCidr(input);
  if (!cidr) {
    return "Please enter a valid IP address or CIDR.";
  }
  const cidrVersion = ipVersion(cidr.ip);
  if (cidrVersion === 0) {
    return "Please enter a valid IP address or CIDR.";
  }
  if (cidrVersion === 4) {
    if (cidr.prefix > 32) {
      return "Please enter a valid IP address or CIDR.";
    }
    if (cidr.prefix < 16) {
      return "IPv4 CIDR cannot have a net mask less than /16.";
    }
  }
  if (cidrVersion === 6) {
    if (cidr.prefix > 128) {
      return "Please enter a valid IP address or CIDR.";
    }
    if (cidr.prefix < 48) {
      return "IPv6 CIDR cannot have a net mask less than /48.";
    }
  }
  return null;
}
function validateHostname(input) {
  const hostnameRegex = /^\*{1}$|^(?!-)(?:\*\.)?[A-Za-z0-9-]{1,63}(?<!-)(?:\.(?!-)[A-Za-z0-9-]{1,63}(?<!-))*$/;
  if (!hostnameRegex.test(input)) {
    return "Please enter a valid domain (e.g. example.com, *.example.com, or * for all domains).";
  }
  return null;
}
function validateNote(input) {
  if (input.length > 500) {
    return `Note must be 500 characters or less (currently ${input.length}).`;
  }
  return null;
}

// src/commands/firewall/system-bypass/add.ts
async function add5(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    systemBypassAddSubcommand,
    client,
    "system-bypass add"
  );
  if (typeof parsed === "number")
    return parsed;
  const ip = parsed.args[0];
  if (!ip) {
    output_manager_default.error("Missing required argument: <ip>");
    return 1;
  }
  const ipError = validateBypassIp(ip);
  if (ipError) {
    output_manager_default.error(ipError);
    return 1;
  }
  const domain2 = parsed.flags["--domain"];
  const notes = parsed.flags["--notes"];
  if (domain2) {
    const domainError = validateHostname(domain2);
    if (domainError) {
      output_manager_default.error(domainError);
      return 1;
    }
  }
  if (notes) {
    const noteError = validateNote(notes);
    if (noteError) {
      output_manager_default.error(noteError);
      return 1;
    }
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const scopeLabel = domain2 || "all domains";
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Add system bypass for ${import_chalk70.default.bold(ip)} on ${import_chalk70.default.bold(scopeLabel)}?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const addStamp = stamp_default();
  output_manager_default.spinner("Adding system bypass rule");
  try {
    await addBypass(
      client,
      project.id,
      {
        sourceIp: ip,
        ...domain2 ? { domain: domain2 } : { projectScope: true },
        ...notes ? { note: notes } : {}
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk70.default.cyan("Success!")} Added system bypass for ${import_chalk70.default.bold(ip)} on ${import_chalk70.default.bold(scopeLabel)} ${import_chalk70.default.gray(addStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to add bypass rule";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall system-bypass add ${ip}${domain2 ? ` --domain ${domain2}` : ""}${notes ? ` --notes "${notes}"` : ""} --yes`
            )
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/system-bypass/remove.ts
var import_chalk71 = __toESM(require_source(), 1);

// src/util/firewall/remove-bypass.ts
async function removeBypass(client, projectId, body, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/bypass?${query.toString()}`;
  return client.fetch(url, {
    method: "DELETE",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body)
  });
}

// src/commands/firewall/system-bypass/remove.ts
async function remove2(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    systemBypassRemoveSubcommand,
    client,
    "system-bypass remove"
  );
  if (typeof parsed === "number")
    return parsed;
  const ip = parsed.args[0];
  if (!ip) {
    output_manager_default.error("Missing required argument: <ip>");
    return 1;
  }
  const ipError = validateBypassIp(ip);
  if (ipError) {
    output_manager_default.error(ipError);
    return 1;
  }
  const domain2 = parsed.flags["--domain"];
  if (domain2) {
    const domainError = validateHostname(domain2);
    if (domainError) {
      output_manager_default.error(domainError);
      return 1;
    }
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const scopeLabel = domain2 || "all domains";
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Remove system bypass for ${import_chalk71.default.bold(ip)} on ${import_chalk71.default.bold(scopeLabel)}?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const removeStamp = stamp_default();
  output_manager_default.spinner("Removing system bypass rule");
  try {
    await removeBypass(
      client,
      project.id,
      {
        sourceIp: ip,
        ...domain2 ? { domain: domain2 } : { projectScope: true }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk71.default.cyan("Success!")} Removed system bypass for ${import_chalk71.default.bold(ip)} ${import_chalk71.default.gray(removeStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    if (error.status === 404) {
      output_manager_default.error(
        `No bypass rule found for ${import_chalk71.default.bold(ip)}. Run ${import_chalk71.default.cyan(getCommandName("firewall system-bypass list"))} to view all rules.`
      );
      return 1;
    }
    const msg = error.message || "Failed to remove bypass rule";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall system-bypass remove ${ip}${domain2 ? ` --domain ${domain2}` : ""} --yes`
            )
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/util/telemetry/commands/firewall/index.ts
var FirewallTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandOverview(actual) {
    this.trackCliSubcommand({
      subcommand: "overview",
      value: actual
    });
  }
  trackCliSubcommandDiff(actual) {
    this.trackCliSubcommand({
      subcommand: "diff",
      value: actual
    });
  }
  trackCliSubcommandPublish(actual) {
    this.trackCliSubcommand({
      subcommand: "publish",
      value: actual
    });
  }
  trackCliSubcommandDiscard(actual) {
    this.trackCliSubcommand({
      subcommand: "discard",
      value: actual
    });
  }
  trackCliSubcommandRules(actual) {
    this.trackCliSubcommand({
      subcommand: "rules",
      value: actual
    });
  }
  trackCliSubcommandRulesList(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:list",
      value: actual
    });
  }
  trackCliSubcommandRulesInspect(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:inspect",
      value: actual
    });
  }
  trackCliSubcommandRulesAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:add",
      value: actual
    });
  }
  trackCliSubcommandRulesEdit(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:edit",
      value: actual
    });
  }
  trackCliSubcommandRulesEnable(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:enable",
      value: actual
    });
  }
  trackCliSubcommandRulesDisable(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:disable",
      value: actual
    });
  }
  trackCliSubcommandRulesRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:remove",
      value: actual
    });
  }
  trackCliSubcommandRulesReorder(actual) {
    this.trackCliSubcommand({
      subcommand: "rules:reorder",
      value: actual
    });
  }
  trackCliSubcommandIpBlocks(actual) {
    this.trackCliSubcommand({
      subcommand: "ip-blocks",
      value: actual
    });
  }
  trackCliSubcommandIpBlocksList(actual) {
    this.trackCliSubcommand({
      subcommand: "ip-blocks:list",
      value: actual
    });
  }
  trackCliSubcommandIpBlocksBlock(actual) {
    this.trackCliSubcommand({
      subcommand: "ip-blocks:block",
      value: actual
    });
  }
  trackCliSubcommandIpBlocksUnblock(actual) {
    this.trackCliSubcommand({
      subcommand: "ip-blocks:unblock",
      value: actual
    });
  }
  trackCliSubcommandSystemBypass(actual) {
    this.trackCliSubcommand({
      subcommand: "system-bypass",
      value: actual
    });
  }
  trackCliSubcommandSystemBypassList(actual) {
    this.trackCliSubcommand({
      subcommand: "system-bypass:list",
      value: actual
    });
  }
  trackCliSubcommandSystemBypassAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "system-bypass:add",
      value: actual
    });
  }
  trackCliSubcommandSystemBypassRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "system-bypass:remove",
      value: actual
    });
  }
  trackCliSubcommandAttackMode(actual) {
    this.trackCliSubcommand({
      subcommand: "attack-mode",
      value: actual
    });
  }
  trackCliSubcommandAttackModeEnable(actual) {
    this.trackCliSubcommand({
      subcommand: "attack-mode:enable",
      value: actual
    });
  }
  trackCliSubcommandAttackModeDisable(actual) {
    this.trackCliSubcommand({
      subcommand: "attack-mode:disable",
      value: actual
    });
  }
  trackCliSubcommandSystemMitigations(actual) {
    this.trackCliSubcommand({
      subcommand: "system-mitigations",
      value: actual
    });
  }
  trackCliSubcommandSystemMitigationsPause(actual) {
    this.trackCliSubcommand({
      subcommand: "system-mitigations:pause",
      value: actual
    });
  }
  trackCliSubcommandSystemMitigationsResume(actual) {
    this.trackCliSubcommand({
      subcommand: "system-mitigations:resume",
      value: actual
    });
  }
};

// src/commands/firewall/system-bypass/index.ts
var COMMAND_CONFIG15 = {
  list: getCommandAliases(systemBypassListSubcommand),
  add: getCommandAliases(systemBypassAddSubcommand),
  remove: getCommandAliases(systemBypassRemoveSubcommand)
};
async function main10(client, args) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    systemBypassSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(args, flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    subcommand,
    args: subArgs,
    subcommandOriginal
  } = getSubcommand5(parsedArgs.args, COMMAND_CONFIG15);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall", "system-bypass");
    output_manager_default.print(
      help(systemBypassSubcommand, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `system-bypass:${subcommandOriginal}`
        );
        printHelp(systemBypassListSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSystemBypassList(subcommandOriginal);
      return list6(client, subArgs);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `system-bypass:${subcommandOriginal}`
        );
        printHelp(systemBypassAddSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSystemBypassAdd(subcommandOriginal);
      return add5(client, subArgs);
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `system-bypass:${subcommandOriginal}`
        );
        printHelp(systemBypassRemoveSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSystemBypassRemove(subcommandOriginal);
      return remove2(client, subArgs);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG15));
      output_manager_default.print(
        help(systemBypassSubcommand, {
          parent: firewallCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/firewall/attack-mode/enable.ts
var import_chalk72 = __toESM(require_source(), 1);

// src/util/firewall/update-attack-mode.ts
async function updateAttackMode(client, body) {
  return client.fetch("/security/attack-mode", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body)
  });
}

// src/commands/firewall/attack-mode/enable.ts
var DURATION_MAP = {
  "1h": 60 * 60 * 1e3,
  "6h": 6 * 60 * 60 * 1e3,
  "24h": 24 * 60 * 60 * 1e3
};
var VALID_DURATIONS = Object.keys(DURATION_MAP);
async function enable(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    attackModeEnableSubcommand,
    client,
    "attack-mode enable"
  );
  if (typeof parsed === "number")
    return parsed;
  if (client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "dangerous_operation_requires_user",
        message: "Enabling attack mode challenges all visitors and cannot be performed non-interactively. Agents must not make this change on behalf of a user. The user must run this command interactively in a terminal to review the impact and confirm.",
        next: [
          {
            command: withGlobalFlags6(client, "firewall attack-mode enable"),
            when: "user runs this command interactively"
          }
        ]
      },
      1
    );
    return 1;
  }
  if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Enabling attack mode requires interactive confirmation. Please run this command in an interactive terminal."
    );
    return 1;
  }
  const duration = parsed.flags["--duration"] || "1h";
  if (!DURATION_MAP[duration]) {
    output_manager_default.error(
      `Invalid duration "${duration}". Valid options: ${VALID_DURATIONS.join(", ")}`
    );
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project } = link;
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Enable attack mode for ${import_chalk72.default.bold(project.name)} (${import_chalk72.default.bold(duration)})?`,
    `${import_chalk72.default.yellow("Warning:")} Every visitor will be shown a verification challenge before accessing your site. This may impact legitimate traffic and SEO. Attack mode automatically expires after ${duration}.`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const updateStamp = stamp_default();
  output_manager_default.spinner("Enabling attack mode");
  try {
    await updateAttackMode(client, {
      projectId: project.id,
      attackModeEnabled: true,
      attackModeActiveUntil: Date.now() + DURATION_MAP[duration]
    });
    output_manager_default.log(
      `${import_chalk72.default.cyan("Success!")} Attack mode enabled for ${import_chalk72.default.bold(duration)} ${import_chalk72.default.gray(updateStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to enable attack mode";
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/attack-mode/disable.ts
var import_chalk73 = __toESM(require_source(), 1);
async function disable(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    attackModeDisableSubcommand,
    client,
    "attack-mode disable"
  );
  if (typeof parsed === "number")
    return parsed;
  if (client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "dangerous_operation_requires_user",
        message: "Disabling attack mode affects traffic handling and cannot be performed non-interactively. Agents must not make this change on behalf of a user. The user must run this command interactively in a terminal.",
        next: [
          {
            command: withGlobalFlags6(client, "firewall attack-mode disable"),
            when: "user runs this command interactively"
          }
        ]
      },
      1
    );
    return 1;
  }
  if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Disabling attack mode requires interactive confirmation. Please run this command in an interactive terminal."
    );
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project } = link;
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Disable attack mode for ${import_chalk73.default.bold(project.name)}?`,
    "Visitors will no longer be shown a verification challenge."
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const updateStamp = stamp_default();
  output_manager_default.spinner("Disabling attack mode");
  try {
    await updateAttackMode(client, {
      projectId: project.id,
      attackModeEnabled: false
    });
    output_manager_default.log(
      `${import_chalk73.default.cyan("Success!")} Attack mode disabled ${import_chalk73.default.gray(updateStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to disable attack mode";
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/attack-mode/index.ts
var COMMAND_CONFIG16 = {
  enable: getCommandAliases(attackModeEnableSubcommand),
  disable: getCommandAliases(attackModeDisableSubcommand)
};
async function main11(client, args) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    attackModeSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(args, flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    subcommand,
    args: subArgs,
    subcommandOriginal
  } = getSubcommand5(parsedArgs.args, COMMAND_CONFIG16);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall", "attack-mode");
    output_manager_default.print(
      help(attackModeSubcommand, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "enable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `attack-mode:${subcommandOriginal}`
        );
        printHelp(attackModeEnableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandAttackModeEnable(subcommandOriginal);
      return enable(client, subArgs);
    case "disable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `attack-mode:${subcommandOriginal}`
        );
        printHelp(attackModeDisableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandAttackModeDisable(subcommandOriginal);
      return disable(client, subArgs);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG16));
      output_manager_default.print(
        help(attackModeSubcommand, {
          parent: firewallCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/firewall/system-mitigations/pause.ts
var import_chalk74 = __toESM(require_source(), 1);
async function pause(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    systemMitigationsPauseSubcommand,
    client,
    "system-mitigations pause"
  );
  if (typeof parsed === "number")
    return parsed;
  if (client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "dangerous_operation_requires_user",
        message: "Pausing system mitigations disables DDoS protection and cannot be performed non-interactively. Agents must not make this change on behalf of a user. The user must run this command interactively in a terminal to review the impact and confirm.",
        next: [
          {
            command: withGlobalFlags6(
              client,
              "firewall system-mitigations pause"
            ),
            when: "user runs this command interactively"
          }
        ]
      },
      1
    );
    return 1;
  }
  if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Pausing system mitigations requires interactive confirmation. Please run this command in an interactive terminal."
    );
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Pause system mitigations for ${import_chalk74.default.bold(project.name)}?`,
    `${import_chalk74.default.yellow("Warning:")} This disables automatic DDoS protection, bot mitigation, and system-level traffic filtering for 24 hours. Your project will be unprotected from automated attacks during this period. Auto-resumes after 24 hours.`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const pauseStamp = stamp_default();
  output_manager_default.spinner("Pausing system mitigations");
  try {
    await addBypass(
      client,
      project.id,
      {
        allSources: true,
        projectScope: true
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk74.default.cyan("Success!")} System mitigations paused for ${import_chalk74.default.bold(project.name)}. Auto-resumes in 24 hours. ${import_chalk74.default.gray(pauseStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to pause system mitigations";
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/system-mitigations/resume.ts
var import_chalk75 = __toESM(require_source(), 1);
async function resume(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    systemMitigationsResumeSubcommand,
    client,
    "system-mitigations resume"
  );
  if (typeof parsed === "number")
    return parsed;
  if (client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "dangerous_operation_requires_user",
        message: "Resuming system mitigations affects traffic protection and cannot be performed non-interactively. Agents must not make this change on behalf of a user. The user must run this command interactively in a terminal.",
        next: [
          {
            command: withGlobalFlags6(
              client,
              "firewall system-mitigations resume"
            ),
            when: "user runs this command interactively"
          }
        ]
      },
      1
    );
    return 1;
  }
  if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Resuming system mitigations requires interactive confirmation. Please run this command in an interactive terminal."
    );
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Resume system mitigations for ${import_chalk75.default.bold(project.name)}?`,
    "Automatic DDoS protection and system-level traffic filtering will be re-enabled immediately."
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const resumeStamp = stamp_default();
  output_manager_default.spinner("Resuming system mitigations");
  try {
    await removeBypass(
      client,
      project.id,
      {
        allSources: true,
        projectScope: true
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk75.default.cyan("Success!")} System mitigations resumed for ${import_chalk75.default.bold(project.name)} ${import_chalk75.default.gray(resumeStamp())}`
    );
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to resume system mitigations";
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/system-mitigations/index.ts
var COMMAND_CONFIG17 = {
  pause: getCommandAliases(systemMitigationsPauseSubcommand),
  resume: getCommandAliases(systemMitigationsResumeSubcommand)
};
async function main12(client, args) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(
    systemMitigationsSubcommand.options
  );
  let parsedArgs;
  try {
    parsedArgs = parseArguments(args, flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    subcommand,
    args: subArgs,
    subcommandOriginal
  } = getSubcommand5(parsedArgs.args, COMMAND_CONFIG17);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall", "system-mitigations");
    output_manager_default.print(
      help(systemMitigationsSubcommand, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "pause":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `system-mitigations:${subcommandOriginal}`
        );
        printHelp(systemMitigationsPauseSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSystemMitigationsPause(subcommandOriginal);
      return pause(client, subArgs);
    case "resume":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `system-mitigations:${subcommandOriginal}`
        );
        printHelp(systemMitigationsResumeSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSystemMitigationsResume(subcommandOriginal);
      return resume(client, subArgs);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG17));
      output_manager_default.print(
        help(systemMitigationsSubcommand, {
          parent: firewallCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/firewall/ip-blocks/list.ts
var import_chalk76 = __toESM(require_source(), 1);
async function list7(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    ipBlocksListSubcommand,
    client,
    "ip-blocks list"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching IP blocking rules for ${import_chalk76.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    const activeIps = active?.ips || [];
    const draftIps = draft?.ips || null;
    const changes = draft?.changes || [];
    const hasDraftChanges = changes.length > 0;
    const annotated = annotateIpRules(activeIps, draftIps, changes);
    if (parsed.flags["--json"]) {
      outputJson2(client, {
        rules: annotated.map((a) => ({
          ...a.rule,
          _status: a.status
        })),
        hasDraft: hasDraftChanges,
        pendingChanges: changes.length
      });
      return 0;
    }
    if (annotated.length === 0) {
      output_manager_default.log("No IP blocking rules configured.");
      return 0;
    }
    output_manager_default.print(`
${formatIpBlocksTable(annotated)}
`);
    const ipChanges = changes.filter((c) => c.action.startsWith("ip.")).length;
    if (ipChanges > 0) {
      output_manager_default.print(
        `
  ${import_chalk76.default.yellow(`${ipChanges} unpublished IP block change${ipChanges !== 1 ? "s" : ""}.`)} Run ${import_chalk76.default.cyan(getCommandName("firewall publish"))} to publish.
`
      );
    } else {
      output_manager_default.print(`
  ${import_chalk76.default.dim("Showing live configuration.")}
`);
    }
    output_manager_default.print("\n");
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch IP blocking rules";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(client, "firewall ip-blocks list")
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/ip-blocks/block.ts
var import_chalk77 = __toESM(require_source(), 1);

// src/util/firewall/patch-firewall-draft.ts
async function patchFirewallDraft(client, projectId, patch, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/config/draft?${query.toString()}`;
  return client.fetch(url, {
    method: "PATCH",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(patch)
  });
}

// src/commands/firewall/ip-blocks/block.ts
async function block(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    ipBlocksBlockSubcommand,
    client,
    "ip-blocks block"
  );
  if (typeof parsed === "number")
    return parsed;
  const ip = parsed.args[0];
  if (!ip) {
    output_manager_default.error("Missing required argument: <ip>");
    return 1;
  }
  const ipError = validateBlockingIp(ip);
  if (ipError) {
    output_manager_default.error(ipError);
    return 1;
  }
  const hostnameFlag = parsed.flags["--hostname"];
  const hostname = hostnameFlag && hostnameFlag !== "" ? hostnameFlag : "*";
  const notes = parsed.flags["--notes"];
  if (hostname !== "*") {
    const hostnameError = validateHostname(hostname);
    if (hostnameError) {
      output_manager_default.error(hostnameError);
      return 1;
    }
  }
  if (notes) {
    const noteError = validateNote(notes);
    if (noteError) {
      output_manager_default.error(noteError);
      return 1;
    }
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const hostnameLabel = hostname === "*" || hostname === "" ? "all hosts" : hostname;
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Block ${import_chalk77.default.bold(ip)} on ${import_chalk77.default.bold(hostnameLabel)}?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const blockStamp = stamp_default();
  output_manager_default.spinner("Staging IP block rule");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "ip.insert",
        id: null,
        value: {
          ip,
          hostname,
          action: "deny",
          ...notes ? { notes } : {}
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk77.default.cyan("Success!")} IP block for ${import_chalk77.default.bold(ip)} on ${import_chalk77.default.bold(hostnameLabel)} staged ${import_chalk77.default.gray(blockStamp())}`
    );
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to stage IP block rule";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall ip-blocks block ${ip}${hostname !== "*" ? ` --hostname ${hostname}` : ""} --yes`
            )
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/ip-blocks/unblock.ts
var import_chalk78 = __toESM(require_source(), 1);
async function unblock(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    ipBlocksUnblockSubcommand,
    client,
    "ip-blocks unblock"
  );
  if (typeof parsed === "number")
    return parsed;
  const identifier = parsed.args[0];
  if (!identifier) {
    output_manager_default.error("Missing required argument: <id-or-ip>");
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching IP blocking rules for ${import_chalk78.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    const currentIps = draft?.ips || active?.ips || [];
    const hostnameFlag = parsed.flags["--hostname"];
    let matches = resolveIpRule(currentIps, identifier);
    if (hostnameFlag && matches.length > 1) {
      const filtered = matches.filter(
        (r) => r.hostname.toLowerCase() === hostnameFlag.toLowerCase()
      );
      if (filtered.length > 0) {
        matches = filtered;
      }
    }
    if (matches.length === 0) {
      output_manager_default.error(
        `No IP block found for "${identifier}". Run ${import_chalk78.default.cyan(getCommandName("firewall ip-blocks list"))} to view all rules.`
      );
      return 1;
    }
    let rule = matches[0];
    if (matches.length > 1) {
      output_manager_default.stopSpinner();
      if (client.nonInteractive || !client.stdin.isTTY) {
        if (client.nonInteractive) {
          outputAgentError(
            client,
            {
              status: "error",
              reason: "ambiguous_match",
              message: `Multiple IP blocks match "${identifier}". Use --hostname to narrow the match or specify the full rule ID.`,
              next: matches.map((r) => ({
                command: withGlobalFlags6(
                  client,
                  `firewall ip-blocks unblock "${identifier}" --hostname "${r.hostname}" --yes`
                ),
                when: `unblock on ${r.hostname === "*" ? "all hosts" : r.hostname}`
              }))
            },
            1
          );
        }
        output_manager_default.error(
          `Multiple IP blocks match "${identifier}". Use --hostname to narrow the match or specify the full rule ID.`
        );
        return 1;
      }
      const selectedId = await client.input.select({
        message: `Multiple IP blocks match "${identifier}". Select one:`,
        choices: matches.map((r) => ({
          value: r.id,
          name: `${r.ip} on ${r.hostname === "*" ? "all hosts" : r.hostname} (${r.action})${r.notes ? ` \u2014 ${r.notes}` : ""}`
        }))
      });
      const selected = matches.find((r) => r.id === selectedId);
      if (!selected) {
        output_manager_default.error("No rule selected");
        return 1;
      }
      rule = selected;
    }
    const hostnameLabel = rule.hostname === "*" || rule.hostname === "" ? "all hosts" : rule.hostname;
    output_manager_default.stopSpinner();
    const confirmed = await confirmAction(
      client,
      parsed.flags["--yes"],
      `Remove IP block for ${import_chalk78.default.bold(rule.ip)} on ${import_chalk78.default.bold(hostnameLabel)}?`
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
    const unblockStamp = stamp_default();
    output_manager_default.spinner("Staging IP block removal");
    const hadExistingDraft = draft !== null && draft.changes.length > 0;
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "ip.remove",
        id: rule.id,
        value: null
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk78.default.cyan("Success!")} IP block removal for ${import_chalk78.default.bold(rule.ip)} staged ${import_chalk78.default.gray(unblockStamp())}`
    );
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to stage IP block removal";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall ip-blocks unblock ${identifier} --yes`
            )
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/ip-blocks/index.ts
var COMMAND_CONFIG18 = {
  list: getCommandAliases(ipBlocksListSubcommand),
  block: getCommandAliases(ipBlocksBlockSubcommand),
  unblock: getCommandAliases(ipBlocksUnblockSubcommand)
};
async function main13(client, args) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(ipBlocksSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(args, flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    subcommand,
    args: subArgs,
    subcommandOriginal
  } = getSubcommand5(parsedArgs.args, COMMAND_CONFIG18);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall", "ip-blocks");
    output_manager_default.print(
      help(ipBlocksSubcommand, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `ip-blocks:${subcommandOriginal}`
        );
        printHelp(ipBlocksListSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandIpBlocksList(subcommandOriginal);
      return list7(client, subArgs);
    case "block":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `ip-blocks:${subcommandOriginal}`
        );
        printHelp(ipBlocksBlockSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandIpBlocksBlock(subcommandOriginal);
      return block(client, subArgs);
    case "unblock":
      if (needHelp) {
        telemetry2.trackCliFlagHelp(
          "firewall",
          `ip-blocks:${subcommandOriginal}`
        );
        printHelp(ipBlocksUnblockSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandIpBlocksUnblock(subcommandOriginal);
      return unblock(client, subArgs);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG18));
      output_manager_default.print(
        help(ipBlocksSubcommand, {
          parent: firewallCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/firewall/rules/list.ts
var import_chalk79 = __toESM(require_source(), 1);
async function list8(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesListSubcommand,
    client,
    "rules list"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching custom rules for ${import_chalk79.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    const activeRules = active?.rules || [];
    const draftRules = draft?.rules || null;
    const changes = draft?.changes || [];
    const annotated = annotateRules(activeRules, draftRules, changes);
    if (parsed.flags["--json"]) {
      outputJson2(client, {
        rules: annotated.map((a) => ({
          ...a.rule,
          _status: a.status
        })),
        hasDraft: changes.length > 0,
        pendingChanges: changes.length
      });
      return 0;
    }
    if (annotated.length === 0) {
      output_manager_default.log("No custom rules configured.");
      return 0;
    }
    if (parsed.flags["--expand"]) {
      output_manager_default.print("\n");
      for (let i = 0; i < annotated.length; i++) {
        const { rule, status: status3 } = annotated[i];
        let colorFn = (s) => s;
        let prefix = "";
        if (status3 === "added") {
          colorFn = import_chalk79.default.green;
          prefix = "+ ";
        } else if (status3 === "removed") {
          colorFn = import_chalk79.default.red;
          prefix = "- ";
        } else if (status3 === "modified") {
          colorFn = import_chalk79.default.yellow;
          prefix = "~ ";
        }
        const expanded = formatRuleExpanded(rule, i);
        if (prefix) {
          const expandedLines = expanded.split("\n");
          expandedLines[0] = colorFn(
            expandedLines[0].replace(/^ {2}/, `  ${prefix}`)
          );
          output_manager_default.print(expandedLines.join("\n"));
        } else {
          output_manager_default.print(expanded);
        }
        if (i < annotated.length - 1) {
          output_manager_default.print("\n\n");
        }
      }
      output_manager_default.print("\n");
    } else {
      output_manager_default.print(`
${formatRulesTable(annotated)}
`);
    }
    const ruleChanges = changes.filter(
      (c) => c.action.startsWith("rules.")
    ).length;
    if (ruleChanges > 0) {
      output_manager_default.print(
        `
  ${import_chalk79.default.yellow(`${ruleChanges} unpublished rule change${ruleChanges !== 1 ? "s" : ""}.`)} Run ${import_chalk79.default.cyan(getCommandName("firewall publish"))} to publish.
`
      );
    } else {
      output_manager_default.print(`
  ${import_chalk79.default.dim("Showing live configuration.")}
`);
    }
    output_manager_default.print("\n");
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch custom rules";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [{ command: withGlobalFlags6(client, "firewall rules list") }]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/rules/inspect.ts
var import_chalk80 = __toESM(require_source(), 1);
async function inspect2(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesInspectSubcommand2,
    client,
    "rules inspect"
  );
  if (typeof parsed === "number")
    return parsed;
  const identifier = parsed.args[0];
  if (!identifier) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Rule name or ID is required.",
          next: [
            {
              command: withGlobalFlags6(
                client,
                "firewall rules inspect <name-or-id>"
              ),
              when: "replace <name-or-id>"
            },
            {
              command: withGlobalFlags6(client, "firewall rules list"),
              when: "list rules"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Rule name or ID is required. Usage: ${getCommandName("firewall rules inspect <name-or-id>")}`
    );
    return 1;
  }
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching rules for ${import_chalk80.default.bold(project.name)}`);
  try {
    const { active, draft } = await listFirewallConfigs(client, project.id, {
      teamId
    });
    const currentRules = draft?.rules || active?.rules || [];
    const matches = resolveRule(currentRules, identifier);
    if (matches.length === 0) {
      output_manager_default.stopSpinner();
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "not_found",
            message: `No rule found for "${identifier}".`,
            next: [
              {
                command: withGlobalFlags6(client, "firewall rules list"),
                when: "list rules"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `No rule found for "${identifier}". Run ${import_chalk80.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
      );
      return 1;
    }
    let rule = matches[0];
    if (matches.length > 1) {
      output_manager_default.stopSpinner();
      if (client.nonInteractive || !client.stdin.isTTY) {
        if (client.nonInteractive) {
          outputAgentError(
            client,
            {
              status: "error",
              reason: "ambiguous_match",
              message: `Multiple rules match "${identifier}". Specify the full rule ID.`,
              next: matches.map((r) => ({
                command: withGlobalFlags6(
                  client,
                  `firewall rules inspect "${r.id}"`
                ),
                when: `inspect "${r.name}"`
              }))
            },
            1
          );
        }
        output_manager_default.error(
          `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
        );
        return 1;
      }
      const selectedId = await client.input.select({
        message: `Multiple rules match "${identifier}". Select one:`,
        choices: matches.map((r) => ({
          value: r.id,
          name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] (${r.id})`
        }))
      });
      const selected = matches.find((r) => r.id === selectedId);
      if (!selected) {
        output_manager_default.error("No rule selected");
        return 1;
      }
      rule = selected;
    }
    if (parsed.flags["--json"]) {
      outputJson2(client, rule);
      return 0;
    }
    output_manager_default.print(`
${formatRuleDetail(rule)}

`);
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to fetch rules";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall rules inspect ${identifier}`
            )
          }
        ]
      });
      process.exit(1);
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/rules/add.ts
var import_chalk84 = __toESM(require_source(), 1);

// src/util/firewall/condition-types.ts
var STRING_OPS = ["eq", "inc"];
var STRING_MATCH_OPS = ["sub", "pre", "suf", "re"];
var KEY_EXISTS_OPS = ["ex"];
var STRING_ONLY = [...STRING_OPS];
var STRING_AND_MATCH = [...STRING_OPS, ...STRING_MATCH_OPS];
var STRING_MATCH_AND_EXISTS = [
  ...STRING_OPS,
  ...STRING_MATCH_OPS,
  ...KEY_EXISTS_OPS
];
var CONDITION_TYPES = [
  // Request
  {
    type: "path",
    displayName: "Request Path",
    description: "URL path of the request",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "route",
    displayName: "Route",
    description: "Route pattern (e.g., /blog/[slug])",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "raw_path",
    displayName: "Raw Path",
    description: "Pre-rewrite URL path",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "server_action",
    displayName: "Server Action Name",
    description: "Next.js Server Action name",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "method",
    displayName: "Method",
    description: "HTTP method (GET, POST, DELETE, etc.)",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY,
    presetValues: [
      { label: "GET", value: "GET" },
      { label: "HEAD", value: "HEAD" },
      { label: "POST", value: "POST" },
      { label: "DELETE", value: "DELETE" },
      { label: "PATCH", value: "PATCH" },
      { label: "PUT", value: "PUT" },
      { label: "CONNECT", value: "CONNECT" },
      { label: "OPTIONS", value: "OPTIONS" },
      { label: "TRACE", value: "TRACE" },
      { label: "DEBUG", value: "DEBUG" },
      { label: "QUERY", value: "QUERY" }
    ]
  },
  {
    type: "host",
    displayName: "Hostname",
    description: "Request hostname",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "protocol",
    displayName: "Protocol",
    description: "HTTP protocol version (HTTP/1.1, HTTP/2.0)",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY,
    presetValues: [
      { label: "HTTP/1.1", value: "HTTP/1.1" },
      { label: "HTTP/2.0", value: "HTTP/2.0" }
    ]
  },
  {
    type: "environment",
    displayName: "Environment",
    description: "Deployment environment (preview, production)",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY,
    presetValues: [
      { label: "Preview", value: "preview" },
      { label: "Production", value: "production" }
    ]
  },
  {
    type: "region",
    displayName: "Vercel Region",
    description: "Edge region serving the request",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY
  },
  {
    type: "target_path",
    displayName: "Target Path",
    description: "Post-rewrite destination path",
    category: "request",
    requiresKey: false,
    operators: STRING_AND_MATCH,
    valueValidation: "path"
  },
  {
    type: "scheme",
    displayName: "Scheme",
    description: "Request scheme (http, https)",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY,
    presetValues: [
      { label: "http", value: "http" },
      { label: "https", value: "https" }
    ]
  },
  {
    type: "rate_limit_api_id",
    displayName: "Rate Limit API ID",
    description: "Identifier for rate-limit API grouping",
    category: "request",
    requiresKey: false,
    operators: STRING_ONLY
  },
  // Client
  {
    type: "ip_address",
    displayName: "IP Address",
    description: "Client IP address or CIDR range",
    category: "client",
    requiresKey: false,
    operators: STRING_ONLY
  },
  {
    type: "user_agent",
    displayName: "User Agent",
    description: "Client User-Agent string",
    category: "client",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  // Geo
  {
    type: "geo_country",
    displayName: "Country",
    description: "Country code (ISO 3166-1 alpha-2)",
    category: "geo",
    requiresKey: false,
    operators: STRING_ONLY
  },
  {
    type: "geo_continent",
    displayName: "Continent",
    description: "Continent code (AF, AN, AS, EU, NA, OC, SA)",
    category: "geo",
    requiresKey: false,
    operators: STRING_ONLY,
    presetValues: [
      { label: "Africa (AF)", value: "AF" },
      { label: "Antarctica (AN)", value: "AN" },
      { label: "Asia (AS)", value: "AS" },
      { label: "Europe (EU)", value: "EU" },
      { label: "North America (NA)", value: "NA" },
      { label: "Oceania (OC)", value: "OC" },
      { label: "South America (SA)", value: "SA" }
    ]
  },
  {
    type: "geo_city",
    displayName: "City",
    description: "City name",
    category: "geo",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "geo_country_region",
    displayName: "State / Region",
    description: "State or region code",
    category: "geo",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "geo_as_number",
    displayName: "AS Number",
    description: "Autonomous System Number",
    category: "geo",
    requiresKey: false,
    operators: STRING_AND_MATCH,
    valueValidation: "digits"
  },
  // Key-Value
  {
    type: "header",
    displayName: "Request Header",
    description: "HTTP request header (requires key)",
    category: "key-value",
    requiresKey: true,
    operators: STRING_MATCH_AND_EXISTS
  },
  {
    type: "query",
    displayName: "Query Parameter",
    description: "URL query parameter (requires key)",
    category: "key-value",
    requiresKey: true,
    operators: STRING_MATCH_AND_EXISTS
  },
  {
    type: "cookie",
    displayName: "Cookie",
    description: "HTTP cookie (requires key)",
    category: "key-value",
    requiresKey: true,
    operators: STRING_MATCH_AND_EXISTS
  },
  // Security
  {
    type: "ja4_digest",
    displayName: "JA4 Digest",
    description: "JA4 TLS fingerprint",
    category: "security",
    requiresKey: false,
    operators: STRING_AND_MATCH
  },
  {
    type: "ja3_digest",
    displayName: "JA3 Digest",
    description: "JA3 TLS fingerprint",
    category: "security",
    requiresKey: false,
    operators: STRING_AND_MATCH,
    planRequirement: "enterprise"
  },
  // Bot
  {
    type: "bot_name",
    displayName: "Bot Name",
    description: "Verified bot name",
    category: "bot",
    requiresKey: false,
    operators: STRING_AND_MATCH,
    planRequirement: "security-plus"
  },
  {
    type: "bot_category",
    displayName: "Bot Category",
    description: "Verified bot category",
    category: "bot",
    requiresKey: false,
    operators: STRING_AND_MATCH,
    planRequirement: "security-plus"
  }
];
var CONDITION_TYPE_MAP = Object.fromEntries(CONDITION_TYPES.map((ct) => [ct.type, ct]));
var ALL_OPERATORS = [
  "eq",
  "neq",
  "inc",
  "ninc",
  "sub",
  "pre",
  "suf",
  "re",
  "ex",
  "nex",
  "gt",
  "gte",
  "lt",
  "lte"
];
var CATEGORY_LABELS = {
  request: "Request",
  client: "Client",
  geo: "Geo / Location",
  "key-value": "Key-Value (Header / Query / Cookie)",
  security: "Security / TLS",
  bot: "Bot Detection"
};
var VALID_ACTIONS = [
  "deny",
  "challenge",
  "log",
  "bypass",
  "rate_limit",
  "redirect"
];
var VALID_DURATIONS2 = ["1m", "5m", "15m", "30m", "1h"];
var VALID_ALGORITHMS = ["fixed_window", "token_bucket"];

// src/util/firewall/parse-conditions.ts
function parseConditionFlags(conditionFlags) {
  if (conditionFlags.length === 0) {
    return "At least one --condition is required.";
  }
  const groups = [{ conditions: [] }];
  for (const flag of conditionFlags) {
    if (flag === "--or") {
      if (groups[groups.length - 1].conditions.length === 0) {
        return "--or cannot be placed before any conditions or consecutively.";
      }
      groups.push({ conditions: [] });
      continue;
    }
    const result = parseConditionFlag(flag);
    if (typeof result === "string") {
      return result;
    }
    groups[groups.length - 1].conditions.push(result);
  }
  if (groups[groups.length - 1].conditions.length === 0) {
    return "--or cannot be the last flag \u2014 add conditions after it.";
  }
  return { groups };
}
function parseConditionFlag(flag) {
  let parsed;
  try {
    parsed = JSON.parse(flag);
  } catch {
    return `Invalid condition JSON: "${flag}". Expected a JSON object like {"type":"path","op":"pre","value":"/api"}.`;
  }
  if (typeof parsed !== "object" || parsed === null || Array.isArray(parsed)) {
    return `Invalid condition: expected a JSON object, got ${typeof parsed}.`;
  }
  const type = parsed.type;
  if (!type || typeof type !== "string") {
    return 'Condition must have a "type" field (string).';
  }
  const meta = CONDITION_TYPE_MAP[type];
  const op = parsed.op;
  if (!op || typeof op !== "string") {
    return `Condition must have an "op" field (string). Valid operators: ${ALL_OPERATORS.join(", ")}`;
  }
  if (!ALL_OPERATORS.includes(op)) {
    const validOps = meta ? meta.operators.join(", ") : ALL_OPERATORS.join(", ");
    return `Invalid operator "${op}" for condition type "${type}". Valid operators: ${validOps}`;
  }
  const requiresKey = meta?.requiresKey ?? false;
  const key = parsed.key;
  if (requiresKey && !key) {
    return `Condition type "${type}" requires a "key" field. Example: {"type":"${type}","key":"name","op":"${op}","value":"..."}`;
  }
  const neg = parsed.neg === true;
  const condition = { type, op };
  if (neg) {
    condition.neg = true;
  }
  if (key) {
    condition.key = key;
  }
  const value = parsed.value;
  if (op === "ex" || op === "nex") {
  } else if (op === "inc" || op === "ninc") {
    if (!value) {
      return `Operator "inc" requires a "value" field. Example: {"type":"${type}","op":"inc","value":"val1,val2"}`;
    }
    if (Array.isArray(value)) {
      condition.value = value.map(String);
    } else {
      condition.value = String(value).split(",").map((v) => v.trim());
    }
  } else if (op === "gt" || op === "gte" || op === "lt" || op === "lte") {
    if (value === void 0 || value === null) {
      return `Operator "${op}" requires a numeric "value" field.`;
    }
    const num = Number(value);
    if (Number.isNaN(num)) {
      return `Operator "${op}" requires a numeric value, got "${value}".`;
    }
    condition.value = num;
  } else if (op === "re") {
    if (!value) {
      return 'Operator "re" requires a "value" field with a regex pattern.';
    }
    try {
      new RegExp(String(value));
    } catch {
      return `Invalid regex pattern: "${value}".`;
    }
    condition.value = String(value);
  } else {
    if (value === void 0 || value === null) {
      return `Operator "${op}" requires a "value" field.`;
    }
    condition.value = String(value);
  }
  return condition;
}

// src/util/firewall/build-action.ts
function buildActionFromFlags(flags, actionType) {
  const duration = flags["--duration"];
  if (duration && !VALID_DURATIONS2.includes(duration)) {
    return `Invalid duration "${duration}". Valid durations: ${VALID_DURATIONS2.join(", ")}`;
  }
  const action = {
    mitigate: {
      action: actionType,
      rateLimit: null,
      redirect: null,
      actionDuration: duration || null
    }
  };
  if (actionType === "rate_limit") {
    const algo = flags["--rate-limit-algo"] || "fixed_window";
    const window = flags["--rate-limit-window"];
    const requests = flags["--rate-limit-requests"];
    const keys = flags["--rate-limit-keys"] || ["ip"];
    if (!VALID_ALGORITHMS.includes(algo)) {
      return `Invalid rate limit algorithm "${algo}". Valid: ${VALID_ALGORITHMS.join(", ")}`;
    }
    if (!window || window < 10) {
      return "Rate limit --rate-limit-window is required (minimum 10 seconds).";
    }
    if (window > 3600) {
      return "Rate limit --rate-limit-window maximum is 3600 seconds (1 hour).";
    }
    if (!requests || requests < 1) {
      return "Rate limit --rate-limit-requests is required (minimum 1).";
    }
    if (requests > 1e7) {
      return "Rate limit --rate-limit-requests maximum is 10,000,000.";
    }
    const rlAction = flags["--rate-limit-action"] || "rate_limit";
    const validRlActions = ["log", "deny", "challenge", "rate_limit"];
    if (!validRlActions.includes(rlAction)) {
      return `Invalid rate limit action "${rlAction}". Valid: ${validRlActions.join(", ")}`;
    }
    action.mitigate.rateLimit = {
      algo,
      window,
      limit: requests,
      keys,
      action: rlAction
    };
  }
  if (actionType === "redirect") {
    const redirectUrl = flags["--redirect-url"];
    const permanent = !!flags["--redirect-permanent"];
    if (!redirectUrl) {
      return "Redirect action requires --redirect-url.";
    }
    if (!redirectUrl.startsWith("/") && !redirectUrl.startsWith("http://") && !redirectUrl.startsWith("https://")) {
      return "Redirect URL must start with /, http://, or https://";
    }
    action.mitigate.redirect = {
      location: redirectUrl,
      permanent
    };
  }
  return action;
}

// src/commands/firewall/rules/add-ai.ts
var import_chalk83 = __toESM(require_source(), 1);

// src/util/firewall/generate-firewall-rule.ts
async function generateFirewallRule(client, projectId, body, options = {}) {
  const { teamId } = options;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  if (teamId)
    query.set("teamId", teamId);
  const url = `/v1/security/firewall/config/generate-rule?${query.toString()}`;
  return client.fetch(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body)
  });
}

// src/commands/firewall/rules/edit-interactive.ts
var import_chalk82 = __toESM(require_source(), 1);

// src/commands/firewall/rules/add-interactive.ts
var import_chalk81 = __toESM(require_source(), 1);

// src/util/firewall/interactive-helpers.ts
import { isIP as isIP2 } from "net";
async function fetchPlanInfo(client) {
  try {
    const { team } = await getScope(client);
    if (team) {
      return {
        isEnterprise: team.billing.plan === "enterprise",
        hasSecurityPlus: team.securityPlus?.enabled === true
      };
    }
  } catch {
  }
  return { isEnterprise: false, hasSecurityPlus: false };
}
function getAvailableConditionTypes(planInfo) {
  return CONDITION_TYPES.filter((ct) => {
    if (ct.deprecated)
      return false;
    if (ct.planRequirement === "enterprise" && !planInfo?.isEnterprise)
      return false;
    if (ct.planRequirement === "security-plus" && !planInfo?.hasSecurityPlus)
      return false;
    return true;
  });
}
function getActionLabel(action) {
  const labels = {
    deny: "Deny \u2014 Block traffic (403)",
    challenge: "Challenge \u2014 Verify at security checkpoint",
    log: "Log \u2014 Monitor without blocking",
    bypass: "Bypass \u2014 Skip other custom rules",
    rate_limit: "Rate Limit \u2014 Enforce request limits",
    redirect: "Redirect \u2014 Send to a different URL"
  };
  return labels[action] || action;
}
function getOperatorDisplayName(op, neg) {
  const labels = {
    eq: ["equals", "does not equal"],
    inc: ["is any of", "is not any of"],
    sub: ["contains", "does not contain"],
    pre: ["starts with", "does not start with"],
    suf: ["ends with", "does not end with"],
    re: ["matches regex", "does not match regex"],
    ex: ["exists", "does not exist"]
  };
  const pair = labels[op];
  if (pair)
    return neg ? pair[1] : pair[0];
  return neg ? `NOT ${op}` : op;
}
function validateConditionValue(val, meta) {
  if (!meta?.valueValidation)
    return true;
  switch (meta.valueValidation) {
    case "path":
      if (!val.startsWith("/"))
        return "Path must start with /";
      return true;
    case "ip": {
      if (isIP2(val))
        return true;
      const slashIdx = val.lastIndexOf("/");
      if (slashIdx !== -1) {
        const ip = val.slice(0, slashIdx);
        const prefix = Number.parseInt(val.slice(slashIdx + 1), 10);
        if (isIP2(ip) && !Number.isNaN(prefix) && prefix >= 0)
          return true;
      }
      return "Please enter a valid IP address or CIDR range.";
    }
    case "hostname":
      if (!/^[A-Za-z0-9-]{1,63}(?:\.[A-Za-z0-9-]{1,63})*$/.test(val))
        return "Please enter a valid hostname.";
      return true;
    case "digits":
      if (!/^\d+$/.test(val))
        return "Please enter digits only.";
      return true;
    default:
      return true;
  }
}

// src/commands/firewall/rules/add-interactive.ts
async function addInteractive(client, project, teamId, opts = {}) {
  const pre = opts.prePopulated;
  const planInfo = await fetchPlanInfo(client);
  const name = await client.input.text({
    message: "Rule name:",
    default: pre?.name,
    validate: (val) => {
      if (!val.trim())
        return "Name is required.";
      if (val.length > 160)
        return "Name must be 160 characters or less.";
      return true;
    }
  });
  const description = await client.input.text({
    message: "Description (optional, press Enter to skip):",
    default: pre?.description || ""
  });
  const conditionGroups = await buildConditionGroupLoop(
    client,
    planInfo,
    pre?.conditionGroup
  );
  const action = await buildActionInteractive(client, pre?.action);
  const active = pre?.active !== void 0 ? pre.active : true;
  const previewRule = {
    id: "(new)",
    name,
    description: description || void 0,
    active,
    conditionGroup: conditionGroups,
    action
  };
  output_manager_default.print(`
${formatRuleExpanded(previewRule)}

`);
  const confirmed = await confirmAction(
    client,
    opts.skipPrompts ?? false,
    "Create this rule?"
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const createStamp = stamp_default();
  output_manager_default.spinner("Staging rule");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.insert",
        id: null,
        value: {
          name,
          description: description || void 0,
          active,
          conditionGroup: conditionGroups,
          action
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk81.default.cyan("Success!")} Rule "${import_chalk81.default.bold(name)}" staged ${import_chalk81.default.gray(createStamp())}`
    );
    printActionImpactWarning(action);
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: opts.skipPrompts
    });
    return 0;
  } catch (e2) {
    const error = e2;
    output_manager_default.error(error.message || "Failed to stage rule");
    return 1;
  }
}
async function buildConditionGroupLoop(client, planInfo, prePopulated) {
  const groups = prePopulated ? JSON.parse(JSON.stringify(prePopulated)) : [];
  if (groups.length === 0) {
    const condition = await buildConditionInteractive(client, planInfo);
    groups.push({ conditions: [condition] });
  }
  for (; ; ) {
    output_manager_default.print(`
  ${import_chalk81.default.bold("Current conditions:")}
`);
    for (let g = 0; g < groups.length; g++) {
      if (groups.length > 1) {
        output_manager_default.print(`  ${import_chalk81.default.dim(`Group ${g + 1} (AND):`)}
`);
      }
      for (const cond of groups[g].conditions) {
        output_manager_default.print(`    ${formatConditionCompact(cond)}
`);
      }
      if (g < groups.length - 1) {
        output_manager_default.print(`  ${import_chalk81.default.dim("OR")}
`);
      }
    }
    output_manager_default.print("\n");
    const choice = await client.input.select({
      message: "What next?",
      choices: [
        {
          value: "and",
          name: "Add another condition (AND with current group)"
        },
        {
          value: "or",
          name: "Start new group (OR \u2014 matches if this group OR previous groups match)"
        },
        {
          value: "done",
          name: "Done with conditions"
        }
      ]
    });
    if (choice === "done") {
      break;
    }
    const condition = await buildConditionInteractive(client, planInfo);
    if (choice === "and") {
      groups[groups.length - 1].conditions.push(condition);
    } else {
      groups.push({ conditions: [condition] });
    }
  }
  return groups;
}
async function buildConditionInteractive(client, planInfo) {
  const availableTypes = getAvailableConditionTypes(planInfo);
  const categories = /* @__PURE__ */ new Map();
  for (const ct of availableTypes) {
    const existing = categories.get(ct.category) || [];
    existing.push(ct);
    categories.set(ct.category, existing);
  }
  const choices = [];
  for (const [category, types] of categories) {
    const label = CATEGORY_LABELS[category] || category;
    choices.push({
      value: `__sep_${category}`,
      name: import_chalk81.default.dim(`\u2500\u2500 ${label} \u2500\u2500`)
    });
    for (const ct of types) {
      choices.push({
        value: ct.type,
        name: `  ${ct.displayName}  ${import_chalk81.default.dim(ct.description)}`
      });
    }
  }
  let type;
  for (; ; ) {
    type = await client.input.select({
      message: "Condition type:",
      choices
    });
    if (!type.startsWith("__sep_"))
      break;
  }
  const meta = CONDITION_TYPES.find((ct) => ct.type === type);
  let key;
  if (meta?.requiresKey) {
    key = await client.input.text({
      message: `${meta.displayName} key:`,
      validate: (val) => val.trim() ? true : "Key is required."
    });
  }
  const operatorChoices = (meta?.operators || ["eq"]).flatMap((op2) => [
    { value: op2, name: getOperatorDisplayName(op2, false) },
    { value: `!${op2}`, name: getOperatorDisplayName(op2, true) }
  ]);
  const opChoice = await client.input.select({
    message: "Operator:",
    choices: operatorChoices
  });
  const neg = opChoice.startsWith("!");
  const op = neg ? opChoice.slice(1) : opChoice;
  let value;
  const baseOp = op;
  const hasPresets = meta?.presetValues && meta.presetValues.length > 0;
  if (baseOp === "ex") {
  } else if (baseOp === "inc" && hasPresets) {
    for (; ; ) {
      const selected = await client.input.checkbox({
        message: "Select values (space to toggle, enter to confirm):",
        choices: meta.presetValues.map((p) => ({
          name: p.label,
          value: p.value
        })),
        pageSize: meta.presetValues.length
      });
      if (selected.length === 0) {
        output_manager_default.warn("Please select at least one value.");
        continue;
      }
      value = selected;
      break;
    }
  } else if (baseOp === "inc") {
    const valStr = await client.input.text({
      message: "Values (comma-separated):",
      validate: (val) => val.trim() ? true : "At least one value is required."
    });
    value = valStr.split(",").map((v) => v.trim());
  } else if (baseOp === "eq" && hasPresets) {
    value = await client.input.select({
      message: "Value:",
      choices: meta.presetValues.map((p) => ({
        name: p.label,
        value: p.value
      })),
      pageSize: meta.presetValues.length
    });
  } else if (baseOp === "re") {
    const valStr = await client.input.text({
      message: "Regex pattern (max 512 chars):",
      validate: (val) => {
        if (!val.trim())
          return "Regex pattern is required.";
        if (val.length > 512)
          return "Regex pattern must be 512 characters or less.";
        try {
          new RegExp(val);
          return true;
        } catch {
          return "Invalid regex pattern. Please enter a valid regular expression.";
        }
      }
    });
    value = valStr;
  } else {
    const valStr = await client.input.text({
      message: "Value:",
      validate: (val) => {
        if (!val.trim())
          return "Value is required.";
        return validateConditionValue(val, meta);
      }
    });
    value = valStr;
  }
  const condition = { type, op };
  if (neg)
    condition.neg = true;
  if (key)
    condition.key = key;
  if (value !== void 0)
    condition.value = value;
  return condition;
}
async function buildActionInteractive(client, prePopulated) {
  const preAction = prePopulated?.mitigate?.action;
  const actionType = await client.input.select({
    message: "Action:",
    choices: VALID_ACTIONS.map((a) => ({
      value: a,
      name: getActionLabel(a)
    })),
    default: preAction
  });
  const action = {
    mitigate: {
      action: actionType,
      rateLimit: null,
      redirect: null,
      actionDuration: null
    }
  };
  if (actionType === "rate_limit") {
    const rl = await buildRateLimitInteractive(client);
    action.mitigate.rateLimit = rl;
  }
  if (actionType === "redirect") {
    const location = await client.input.text({
      message: "Redirect URL or path:",
      validate: (val) => {
        if (!val.trim())
          return "URL is required.";
        if (!val.startsWith("/") && !val.startsWith("http://") && !val.startsWith("https://"))
          return "URL must start with /, http://, or https://";
        return true;
      }
    });
    const permanent = await client.input.confirm(
      "Permanent redirect (301)?",
      false
    );
    action.mitigate.redirect = { location, permanent };
  }
  if (["deny", "challenge", "rate_limit"].includes(actionType)) {
    const durationChoice = await client.input.select({
      message: "Action duration (optional \u2014 block matching clients for a period):",
      choices: [
        { value: "none", name: "None (no persistent block)" },
        ...VALID_DURATIONS2.map((d) => ({ value: d, name: d }))
      ]
    });
    if (durationChoice !== "none") {
      action.mitigate.actionDuration = durationChoice;
    }
  }
  return action;
}
async function buildRateLimitInteractive(client) {
  const algo = "fixed_window";
  output_manager_default.print(
    `  ${import_chalk81.default.dim("Algorithm:")} Fixed Window ${import_chalk81.default.dim("(Token Bucket available on Enterprise via --rate-limit-algo flag)")}
`
  );
  const windowStr = await client.input.text({
    message: "Window in seconds (10-3600):",
    default: "60",
    validate: (val) => {
      const n = Number(val);
      if (Number.isNaN(n) || !Number.isInteger(n))
        return "Please enter a whole number.";
      if (n < 10)
        return "Minimum 10 seconds.";
      if (n > 3600)
        return "Maximum 3600 seconds (1 hour).";
      return true;
    }
  });
  const limitStr = await client.input.text({
    message: "Max requests per window (1-10,000,000):",
    default: "100",
    validate: (val) => {
      const n = Number(val);
      if (Number.isNaN(n) || !Number.isInteger(n))
        return "Please enter a whole number.";
      if (n < 1)
        return "Minimum 1 request.";
      if (n > 1e7)
        return "Maximum 10,000,000 requests.";
      return true;
    }
  });
  const selectedKeys = await client.input.checkbox({
    message: "Rate limit keys (space to toggle, enter to confirm):",
    choices: [
      { name: "IP Address (ip)", value: "ip", checked: true },
      { name: "JA4 Digest (ja4)", value: "ja4" },
      { name: "User Agent (header:user-agent)", value: "header:user-agent" }
    ],
    pageSize: 3
  });
  let keys = [...selectedKeys];
  const addCustom = await client.input.confirm(
    "Add a custom header key?",
    false
  );
  if (addCustom) {
    const customKey = await client.input.text({
      message: "Header name (e.g. x-api-key):",
      validate: (val) => val.trim() ? true : "Header name is required."
    });
    keys.push(`header:${customKey}`);
  }
  if (keys.length === 0) {
    keys = ["ip"];
  }
  const subAction = await client.input.select({
    message: "Action when limit is exceeded:",
    choices: [
      { value: "deny", name: "Deny (403)" },
      { value: "challenge", name: "Challenge" },
      { value: "log", name: "Log only" },
      { value: "rate_limit", name: "Rate limit (429)" }
    ]
  });
  return {
    algo,
    window: Number(windowStr),
    limit: Number(limitStr),
    keys,
    action: subAction
  };
}

// src/commands/firewall/rules/edit-interactive.ts
async function runInteractiveEditLoop2(client, rule) {
  const edited = JSON.parse(JSON.stringify(rule));
  const planInfo = await fetchPlanInfo(client);
  for (; ; ) {
    const condCount = edited.conditionGroup.reduce(
      (sum, g) => sum + g.conditions.length,
      0
    );
    const groupCount = edited.conditionGroup.length;
    const actionLabel = formatActionDisplay(edited.action);
    const choice = await client.input.select({
      message: "What would you like to edit?",
      choices: [
        {
          value: "name",
          name: `Name (${import_chalk82.default.cyan(edited.name)})`
        },
        {
          value: "description",
          name: `Description (${import_chalk82.default.cyan(edited.description || import_chalk82.default.dim("none"))})`
        },
        {
          value: "conditions",
          name: `Conditions (${import_chalk82.default.cyan(`${condCount} condition${condCount !== 1 ? "s" : ""} in ${groupCount} group${groupCount !== 1 ? "s" : ""}`)})`
        },
        {
          value: "action",
          name: `Action (${import_chalk82.default.cyan(actionLabel)})`
        },
        {
          value: "active",
          name: `Status (${edited.active ? import_chalk82.default.green("Enabled") : import_chalk82.default.dim("Disabled")})`
        },
        {
          value: "done",
          name: import_chalk82.default.bold("Done \u2014 save changes")
        }
      ]
    });
    switch (choice) {
      case "name": {
        edited.name = await client.input.text({
          message: "Rule name:",
          default: edited.name,
          validate: (val) => {
            if (!val.trim())
              return "Name is required.";
            if (val.length > 160)
              return "Name must be 160 characters or less.";
            return true;
          }
        });
        break;
      }
      case "description": {
        edited.description = await client.input.text({
          message: "Description (press Enter to keep, empty to clear):",
          default: edited.description || "",
          validate: (val) => {
            if (val.length > 256) {
              return "Description must be 256 characters or less.";
            }
            return true;
          }
        });
        if (edited.description === "") {
          edited.description = void 0;
        }
        break;
      }
      case "conditions": {
        const result = await editConditions(
          client,
          edited.conditionGroup,
          planInfo
        );
        if (result) {
          edited.conditionGroup = result;
        }
        break;
      }
      case "action": {
        edited.action = await editAction(client, edited.action);
        break;
      }
      case "active": {
        const toggle = await client.input.select({
          message: "Rule status:",
          choices: [
            { value: "active", name: "Enabled" },
            { value: "inactive", name: "Disabled" }
          ],
          default: edited.active ? "active" : "inactive"
        });
        edited.active = toggle === "active";
        break;
      }
      case "done": {
        if (edited.conditionGroup.length === 0) {
          output_manager_default.warn(
            "Rule has no conditions. Please add at least one condition."
          );
          continue;
        }
        return edited;
      }
    }
  }
}
async function editConditions(client, groups, planInfo) {
  const edited = JSON.parse(JSON.stringify(groups));
  for (; ; ) {
    output_manager_default.print(`
${import_chalk82.default.bold("  Current conditions:")}
`);
    let condIndex = 1;
    for (let g = 0; g < edited.length; g++) {
      if (edited.length > 1) {
        output_manager_default.print(`  ${import_chalk82.default.dim(`Group ${g + 1} (AND):`)}
`);
      }
      for (const cond of edited[g].conditions) {
        output_manager_default.print(`    ${condIndex}. ${formatConditionCompact(cond)}
`);
        condIndex++;
      }
      if (g < edited.length - 1) {
        output_manager_default.print(`  ${import_chalk82.default.dim("OR")}
`);
      }
    }
    output_manager_default.print("\n");
    const choices = [];
    choices.push({
      value: "done",
      name: import_chalk82.default.bold("Done with conditions")
    });
    if (edited.length > 0) {
      choices.push({
        value: "add-and",
        name: `Add condition to ${edited.length > 1 ? "a group" : "current group"} (AND)`
      });
    }
    choices.push({
      value: "add-or",
      name: "Add new group (OR)"
    });
    if (condIndex > 1) {
      choices.push({
        value: "remove",
        name: "Remove a condition"
      });
    }
    let idx = 1;
    for (let g = 0; g < edited.length; g++) {
      for (let c = 0; c < edited[g].conditions.length; c++) {
        choices.push({
          value: `edit:${g}:${c}`,
          name: `Edit condition ${idx} (${formatConditionCompact(edited[g].conditions[c])})`
        });
        idx++;
      }
    }
    const action = await client.input.select({
      message: "What would you like to do?",
      choices
    });
    if (action === "done") {
      return edited;
    }
    if (action.startsWith("edit:")) {
      const [, gStr, cStr] = action.split(":");
      const g = Number.parseInt(gStr, 10);
      const c = Number.parseInt(cStr, 10);
      const newCond = await editSingleCondition(
        client,
        edited[g].conditions[c],
        planInfo
      );
      edited[g].conditions[c] = newCond;
      continue;
    }
    if (action === "remove") {
      const removeChoices = [];
      let removeIdx = 1;
      for (let g2 = 0; g2 < edited.length; g2++) {
        for (let c2 = 0; c2 < edited[g2].conditions.length; c2++) {
          removeChoices.push({
            value: `${g2}:${c2}`,
            name: `${removeIdx}. ${formatConditionCompact(edited[g2].conditions[c2])}`
          });
          removeIdx++;
        }
      }
      const removeTarget = await client.input.select({
        message: "Which condition to remove?",
        choices: removeChoices
      });
      const [gStr, cStr] = removeTarget.split(":");
      const g = Number.parseInt(gStr, 10);
      const c = Number.parseInt(cStr, 10);
      edited[g].conditions.splice(c, 1);
      if (edited[g].conditions.length === 0) {
        edited.splice(g, 1);
        output_manager_default.log("Empty group removed.");
      }
      continue;
    }
    if (action === "add-and") {
      let targetGroup = 0;
      if (edited.length > 1) {
        const groupChoice = await client.input.select({
          message: "Add to which group?",
          choices: edited.map((_, i) => ({
            value: String(i),
            name: `Group ${i + 1} (${edited[i].conditions.length} conditions)`
          }))
        });
        targetGroup = Number.parseInt(groupChoice, 10);
      }
      const newCond = await buildCondition(client, planInfo);
      edited[targetGroup].conditions.push(newCond);
      continue;
    }
    if (action === "add-or") {
      const newCond = await buildCondition(client, planInfo);
      edited.push({ conditions: [newCond] });
    }
  }
}
async function editSingleCondition(client, current, planInfo) {
  output_manager_default.print(
    `
  ${import_chalk82.default.dim("Current:")} ${formatConditionCompact(current)}

`
  );
  const availableTypes = getAvailableConditionTypes(planInfo);
  const type = await client.input.select({
    message: "Condition type:",
    choices: availableTypes.map((ct) => ({
      value: ct.type,
      name: `${ct.displayName}  ${import_chalk82.default.dim(ct.description)}`
    })),
    default: current.type
  });
  const meta = CONDITION_TYPES.find((ct) => ct.type === type);
  let key;
  if (meta?.requiresKey) {
    key = await client.input.text({
      message: `${meta.displayName} key:`,
      default: current.type === type ? current.key : void 0,
      validate: (val) => val.trim() ? true : "Key is required."
    });
  }
  const operatorChoices = (meta?.operators || ["eq"]).flatMap((op2) => [
    { value: op2, name: getOperatorDisplayName(op2, false) },
    { value: `!${op2}`, name: getOperatorDisplayName(op2, true) }
  ]);
  const currentOpValue = current.type === type ? current.neg ? `!${current.op}` : current.op : void 0;
  const opChoice = await client.input.select({
    message: "Operator:",
    choices: operatorChoices,
    default: currentOpValue
  });
  const neg = opChoice.startsWith("!");
  const op = neg ? opChoice.slice(1) : opChoice;
  let value;
  if (op !== "ex") {
    const currentValue = current.type === type && current.op === op ? current.value : void 0;
    if (op === "inc" && meta?.presetValues) {
      const currentArr = Array.isArray(currentValue) ? currentValue : [];
      for (; ; ) {
        const selected = await client.input.checkbox({
          message: "Select values:",
          choices: meta.presetValues.map((p) => ({
            name: p.label,
            value: p.value,
            checked: currentArr.includes(p.value)
          })),
          pageSize: meta.presetValues.length
        });
        if (selected.length === 0) {
          output_manager_default.warn("Please select at least one value.");
          continue;
        }
        value = selected;
        break;
      }
    } else if (op === "inc") {
      const defaultVal = Array.isArray(currentValue) ? currentValue.join(", ") : "";
      const valStr = await client.input.text({
        message: "Values (comma-separated):",
        default: defaultVal,
        validate: (val) => val.trim() ? true : "At least one value is required."
      });
      value = valStr.split(",").map((v) => v.trim());
    } else if (op === "eq" && meta?.presetValues) {
      value = await client.input.select({
        message: "Value:",
        choices: meta.presetValues.map((p) => ({
          name: p.label,
          value: p.value
        })),
        default: typeof currentValue === "string" ? currentValue : void 0,
        pageSize: meta.presetValues.length
      });
    } else {
      const defaultVal = typeof currentValue === "string" ? currentValue : "";
      const valStr = await client.input.text({
        message: "Value:",
        default: defaultVal,
        validate: (val) => {
          if (!val.trim())
            return "Value is required.";
          return validateConditionValue(val, meta);
        }
      });
      value = valStr;
    }
  }
  const condition = { type, op };
  if (neg)
    condition.neg = true;
  if (key)
    condition.key = key;
  if (value !== void 0)
    condition.value = value;
  return condition;
}
async function buildCondition(client, planInfo) {
  return buildConditionInteractive(client, planInfo);
}
async function editAction(client, current) {
  const currentActionType = current.mitigate?.action;
  const actionType = await client.input.select({
    message: "Action:",
    choices: VALID_ACTIONS.map((a) => ({
      value: a,
      name: getActionLabel(a)
    })),
    default: currentActionType
  });
  const action = {
    mitigate: {
      action: actionType,
      rateLimit: null,
      redirect: null,
      actionDuration: null
    }
  };
  if (actionType === "rate_limit") {
    const currentRL = current.mitigate?.rateLimit;
    output_manager_default.print(
      `  ${import_chalk82.default.dim("Algorithm:")} Fixed Window ${import_chalk82.default.dim("(Token Bucket available on Enterprise via flags)")}
`
    );
    const windowStr = await client.input.text({
      message: "Window in seconds (10-3600):",
      default: String(currentRL?.window || 60),
      validate: (val) => {
        const n = Number(val);
        if (Number.isNaN(n) || !Number.isInteger(n))
          return "Please enter a whole number.";
        if (n < 10)
          return "Minimum 10 seconds.";
        if (n > 3600)
          return "Maximum 3600 seconds (1 hour).";
        return true;
      }
    });
    const limitStr = await client.input.text({
      message: "Max requests per window (1-10,000,000):",
      default: String(currentRL?.limit || 100),
      validate: (val) => {
        const n = Number(val);
        if (Number.isNaN(n) || !Number.isInteger(n))
          return "Please enter a whole number.";
        if (n < 1)
          return "Minimum 1 request.";
        if (n > 1e7)
          return "Maximum 10,000,000 requests.";
        return true;
      }
    });
    const currentKeys = currentRL?.keys || ["ip"];
    const selectedKeys = await client.input.checkbox({
      message: "Rate limit keys:",
      choices: [
        {
          name: "IP Address (ip)",
          value: "ip",
          checked: currentKeys.includes("ip")
        },
        {
          name: "JA4 Digest (ja4)",
          value: "ja4",
          checked: currentKeys.includes("ja4")
        },
        {
          name: "User Agent (header:user-agent)",
          value: "header:user-agent",
          checked: currentKeys.includes("header:user-agent")
        }
      ],
      pageSize: 3
    });
    let keys = [...selectedKeys];
    const customKeys = currentKeys.filter(
      (k) => !["ip", "ja4", "header:user-agent"].includes(k)
    );
    if (customKeys.length > 0) {
      output_manager_default.print(
        `  ${import_chalk82.default.dim("Custom keys preserved:")} ${customKeys.join(", ")}
`
      );
      keys = [...keys, ...customKeys];
    }
    if (keys.length === 0)
      keys = ["ip"];
    const subAction = await client.input.select({
      message: "Action when limit is exceeded:",
      choices: [
        { value: "deny", name: "Deny (403)" },
        { value: "challenge", name: "Challenge" },
        { value: "log", name: "Log only" },
        { value: "rate_limit", name: "Rate limit (429)" }
      ],
      default: currentRL?.action || "deny"
    });
    action.mitigate.rateLimit = {
      algo: currentRL?.algo || "fixed_window",
      window: Number(windowStr),
      limit: Number(limitStr),
      keys,
      action: subAction
    };
  }
  if (actionType === "redirect") {
    const currentRD = current.mitigate?.redirect;
    const location = await client.input.text({
      message: "Redirect URL or path:",
      default: currentRD?.location || "",
      validate: (val) => {
        if (!val.trim())
          return "URL is required.";
        if (!val.startsWith("/") && !val.startsWith("http://") && !val.startsWith("https://"))
          return "URL must start with /, http://, or https://";
        return true;
      }
    });
    const permanent = await client.input.confirm(
      "Permanent redirect (301)?",
      currentRD?.permanent ?? false
    );
    action.mitigate.redirect = { location, permanent };
  }
  if (["deny", "challenge", "rate_limit"].includes(actionType)) {
    const currentDuration = current.mitigate?.actionDuration;
    const durationChoice = await client.input.select({
      message: "Action duration:",
      choices: [
        { value: "none", name: "None (no persistent block)" },
        ...VALID_DURATIONS2.map((d) => ({ value: d, name: d }))
      ],
      default: currentDuration || "none"
    });
    if (durationChoice !== "none") {
      action.mitigate.actionDuration = durationChoice;
    }
  }
  return action;
}

// src/commands/firewall/rules/add-ai.ts
async function handleAIAdd(client, project, teamId, opts) {
  let prompt = opts.prompt;
  if (!prompt) {
    if (!client.stdin.isTTY || client.nonInteractive) {
      output_manager_default.error(
        '--ai requires a description. Example: firewall rules add --ai "Rate limit /api to 100 requests per minute by IP"'
      );
      return 1;
    }
    prompt = await client.input.text({
      message: "Describe the rule you want to create:",
      validate: (val) => val.trim() ? true : "Please provide a description."
    });
  }
  let currentRule;
  let retryCount = 0;
  const maxRetries = client.nonInteractive ? 1 : 0;
  for (; ; ) {
    output_manager_default.spinner("Generating rule with AI...");
    try {
      const response = await generateFirewallRule(
        client,
        project.id,
        {
          prompt,
          ...currentRule ? { currentRule } : {}
        },
        { teamId }
      );
      if (response.error) {
        output_manager_default.stopSpinner();
        if (client.nonInteractive && retryCount < maxRetries) {
          retryCount++;
          output_manager_default.warn(`AI generation failed: ${response.error}. Retrying...`);
          continue;
        }
        if (client.stdin.isTTY && !client.nonInteractive && !opts.skipPrompts) {
          const retryChoice = await client.input.select({
            message: `AI could not generate a rule: ${response.error}`,
            choices: [
              {
                value: "retry",
                name: "Try again with a different description"
              },
              { value: "cancel", name: "Cancel" }
            ]
          });
          if (retryChoice === "cancel") {
            output_manager_default.log("Canceled");
            return 0;
          }
          prompt = await client.input.text({
            message: "Describe the rule you want to create:",
            validate: (val) => val.trim() ? true : "Please provide a description."
          });
          continue;
        }
        if (client.nonInteractive) {
          outputAgentError(
            client,
            {
              status: "error",
              reason: "ai_generation_failed",
              message: `AI could not generate a rule: ${response.error}`,
              next: [
                {
                  command: withGlobalFlags6(
                    client,
                    'firewall rules add --ai "more specific prompt" --yes'
                  ),
                  when: "try with a more specific description"
                },
                {
                  command: withGlobalFlags6(
                    client,
                    `firewall rules add "Name" --condition '{"type":"path","op":"pre","value":"/api"}' --action deny --yes`
                  ),
                  when: "create with flags instead"
                }
              ]
            },
            1
          );
        }
        output_manager_default.error(`AI could not generate a rule: ${response.error}`);
        return 1;
      }
      if (!response.rule) {
        output_manager_default.stopSpinner();
        if (client.stdin.isTTY && !client.nonInteractive && !opts.skipPrompts) {
          const retryChoice = await client.input.select({
            message: "AI did not return a rule.",
            choices: [
              {
                value: "retry",
                name: "Try again with a different description"
              },
              { value: "cancel", name: "Cancel" }
            ]
          });
          if (retryChoice === "cancel") {
            output_manager_default.log("Canceled");
            return 0;
          }
          prompt = await client.input.text({
            message: "Describe the rule you want to create:",
            validate: (val) => val.trim() ? true : "Please provide a description."
          });
          continue;
        }
        if (client.nonInteractive) {
          outputAgentError(
            client,
            {
              status: "error",
              reason: "ai_no_result",
              message: "AI did not return a rule. Try a more specific description.",
              next: [
                {
                  command: withGlobalFlags6(
                    client,
                    'firewall rules add --ai "more specific prompt" --yes'
                  ),
                  when: "try with a more specific description"
                }
              ]
            },
            1
          );
        }
        output_manager_default.error(
          "AI did not return a rule. Try a more specific description."
        );
        return 1;
      }
      currentRule = response.rule;
      if (opts.name) {
        currentRule.name = opts.name;
      }
      output_manager_default.stopSpinner();
      break;
    } catch (e2) {
      output_manager_default.stopSpinner();
      const error = e2;
      const msg = error.message || "Failed to generate rule";
      if (client.nonInteractive && retryCount < maxRetries) {
        retryCount++;
        output_manager_default.warn(`Generation failed: ${msg}. Retrying...`);
        continue;
      }
      if (!client.stdin.isTTY || client.nonInteractive || opts.skipPrompts) {
        if (client.nonInteractive) {
          outputAgentError(
            client,
            {
              status: "error",
              reason: "ai_generation_failed",
              message: msg,
              next: [
                {
                  command: withGlobalFlags6(
                    client,
                    'firewall rules add --ai "more specific prompt" --yes'
                  ),
                  when: "try again"
                }
              ]
            },
            1
          );
        }
        output_manager_default.error(msg);
        return 1;
      }
      const choice = await client.input.select({
        message: `Generation failed: ${msg}`,
        choices: [
          { value: "retry", name: "Try again" },
          { value: "cancel", name: "Cancel" }
        ]
      });
      if (choice === "cancel") {
        output_manager_default.log("Canceled");
        return 0;
      }
    }
  }
  const previewRule = {
    ...currentRule,
    id: "(AI-generated)"
  };
  output_manager_default.print(`
${formatRuleExpanded(previewRule)}

`);
  if (opts.skipPrompts) {
    return createFromGenerated(client, project, teamId, currentRule, opts);
  }
  for (; ; ) {
    const choice = await client.input.select({
      message: "What would you like to do?",
      choices: [
        { value: "create", name: "Create this rule" },
        { value: "edit-ai", name: "Edit with AI (describe changes)" },
        { value: "edit-manual", name: "Edit manually (step by step)" },
        { value: "discard", name: "Discard" }
      ]
    });
    if (choice === "create") {
      return createFromGenerated(client, project, teamId, currentRule, opts);
    }
    if (choice === "edit-ai") {
      const editPrompt = await client.input.text({
        message: "Describe the changes you want:",
        validate: (val) => val.trim() ? true : "Please describe the changes."
      });
      output_manager_default.spinner("Regenerating rule...");
      try {
        const response = await generateFirewallRule(
          client,
          project.id,
          { prompt: editPrompt, currentRule },
          { teamId }
        );
        output_manager_default.stopSpinner();
        if (response.error || !response.rule) {
          output_manager_default.error(
            `AI could not update the rule: ${response.error || "No rule returned"}`
          );
          continue;
        }
        currentRule = response.rule;
        const updatedPreview = {
          ...currentRule,
          id: "(AI-generated)"
        };
        output_manager_default.print(`
${formatRuleExpanded(updatedPreview)}

`);
        continue;
      } catch (e2) {
        output_manager_default.stopSpinner();
        const error = e2;
        output_manager_default.error(
          `Failed to regenerate: ${error.message || "Unknown error"}`
        );
        continue;
      }
    }
    if (choice === "edit-manual") {
      const prePopulated = {
        ...currentRule,
        id: "(new)"
      };
      const modified = await runInteractiveEditLoop2(client, prePopulated);
      if (!modified) {
        continue;
      }
      return createFromGenerated(client, project, teamId, modified, opts);
    }
    if (choice === "discard") {
      output_manager_default.log("Discarded");
      return 0;
    }
  }
}
async function createFromGenerated(client, project, teamId, rule, opts) {
  const createStamp = stamp_default();
  output_manager_default.spinner("Staging rule");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.insert",
        id: null,
        value: {
          name: rule.name,
          description: rule.description,
          active: rule.active !== false,
          conditionGroup: rule.conditionGroup,
          action: rule.action
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk83.default.cyan("Success!")} Rule "${import_chalk83.default.bold(rule.name)}" staged ${import_chalk83.default.gray(createStamp())}`
    );
    printActionImpactWarning(rule.action);
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: opts.skipPrompts
    });
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to stage rule";
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "staging_failed",
          message: msg,
          next: [
            {
              command: withGlobalFlags6(client, "firewall rules add --yes"),
              when: "try again"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/rules/add.ts
async function add6(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesAddSubcommand2,
    client,
    "rules add"
  );
  if (typeof parsed === "number")
    return parsed;
  const aiPrompt = parsed.flags["--ai"];
  const jsonInput = parsed.flags["--json"];
  const conditionFlags = parsed.flags["--condition"];
  const modeCount = [aiPrompt, jsonInput, conditionFlags].filter(
    Boolean
  ).length;
  if (modeCount > 1) {
    output_manager_default.error(
      "Cannot use --ai, --json, and --condition together. Use only one mode."
    );
    return 1;
  }
  if (aiPrompt) {
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ai_not_available",
          message: "AI mode is not available in non-interactive mode. AI-generated rules require human review before staging. Use --json or --condition flags instead.",
          next: [
            {
              command: withGlobalFlags6(
                client,
                `firewall rules add "Name" --condition '{"type":"path","op":"pre","value":"/api"}' --action deny --yes`
              ),
              when: "create with flags"
            },
            {
              command: withGlobalFlags6(
                client,
                `firewall rules add --json '{"name":"...","conditionGroup":[...],"action":{...}}' --yes`
              ),
              when: "create with JSON"
            }
          ]
        });
      }
      output_manager_default.error(
        "AI mode is not available in non-interactive mode. Use --json or --condition flags instead."
      );
      return 1;
    }
    const link = await ensureProjectLink2(client);
    if (typeof link === "number")
      return link;
    const { project, org } = link;
    const teamId = org.type === "team" ? org.id : void 0;
    return handleAIAdd(client, project, teamId, {
      prompt: aiPrompt,
      name: parsed.args[0],
      skipPrompts: !!parsed.flags["--yes"]
    });
  }
  if (jsonInput) {
    return handleJsonAdd(client, parsed, jsonInput);
  }
  if (conditionFlags) {
    return handleFlagAdd(client, parsed, conditionFlags);
  }
  if (client.stdin.isTTY && !client.nonInteractive) {
    const mode = await client.input.select({
      message: "How would you like to create the rule?",
      choices: [
        {
          value: "ai",
          name: "Describe what you want (AI-powered)"
        },
        {
          value: "manual",
          name: "Build manually (step by step)"
        },
        {
          value: "json",
          name: "Paste JSON"
        }
      ]
    });
    const link = await ensureProjectLink2(client);
    if (typeof link === "number")
      return link;
    const { project, org } = link;
    const teamId = org.type === "team" ? org.id : void 0;
    if (mode === "ai") {
      return handleAIAdd(client, project, teamId, {
        skipPrompts: false
      });
    }
    if (mode === "json") {
      const jsonStr = await client.input.text({
        message: "Paste the JSON rule definition:"
      });
      return handleJsonAdd(client, parsed, jsonStr);
    }
    return addInteractive(client, project, teamId, {
      skipPrompts: !!parsed.flags["--yes"]
    });
  }
  if (client.nonInteractive) {
    outputAgentError(client, {
      status: "error",
      reason: "missing_flags",
      message: "No rule definition provided. Use --json or --condition flags.",
      next: [
        {
          command: withGlobalFlags6(
            client,
            `firewall rules add "Name" --condition '{"type":"path","op":"pre","value":"/api"}' --action deny --yes`
          ),
          when: "create with flags"
        },
        {
          command: withGlobalFlags6(
            client,
            `firewall rules add --json '{"name":"Name","conditionGroup":[...],"action":{"mitigate":{"action":"deny"}}}' --yes`
          ),
          when: "create with JSON"
        }
      ]
    });
  }
  output_manager_default.error(
    "Interactive mode is not available. Use --ai, --json, or --condition flags to create a rule."
  );
  return 1;
}
async function handleJsonAdd(client, parsed, jsonStr) {
  let ruleData;
  try {
    ruleData = JSON.parse(jsonStr);
  } catch {
    output_manager_default.error("Invalid JSON. Please provide a valid JSON object.");
    return 1;
  }
  if (!ruleData.name || typeof ruleData.name !== "string") {
    output_manager_default.error('JSON must include a "name" field (string).');
    return 1;
  }
  if (!ruleData.conditionGroup || !Array.isArray(ruleData.conditionGroup)) {
    output_manager_default.error('JSON must include a "conditionGroup" field (array).');
    return 1;
  }
  for (let i = 0; i < ruleData.conditionGroup.length; i++) {
    const group = ruleData.conditionGroup[i];
    if (!group.conditions || !Array.isArray(group.conditions)) {
      output_manager_default.error(`conditionGroup[${i}] must have a "conditions" array.`);
      return 1;
    }
    for (let j = 0; j < group.conditions.length; j++) {
      const cond = group.conditions[j];
      if (!cond.type || typeof cond.type !== "string") {
        output_manager_default.error(
          `conditionGroup[${i}].conditions[${j}] must have a "type" field.`
        );
        return 1;
      }
      if (!cond.op || typeof cond.op !== "string") {
        output_manager_default.error(
          `conditionGroup[${i}].conditions[${j}] must have an "op" field.`
        );
        return 1;
      }
    }
  }
  if (!ruleData.action || typeof ruleData.action !== "object") {
    output_manager_default.error('JSON must include an "action" field (object).');
    return 1;
  }
  const actionObj = ruleData.action;
  if (!actionObj.mitigate || typeof actionObj.mitigate !== "object") {
    output_manager_default.error(
      'action must have a "mitigate" field. Example: { "mitigate": { "action": "deny" } }'
    );
    return 1;
  }
  const mitigateObj = actionObj.mitigate;
  if (!mitigateObj.action || typeof mitigateObj.action !== "string") {
    output_manager_default.error(
      'action.mitigate must have an "action" field (string). Valid: deny, challenge, rate_limit, log, redirect.'
    );
    return 1;
  }
  if (ruleData.name.length > 160) {
    output_manager_default.error("Rule name must be 160 characters or less.");
    return 1;
  }
  if (ruleData.description && ruleData.description.length > 256) {
    output_manager_default.error("Rule description must be 256 characters or less.");
    return 1;
  }
  if (ruleData.conditionGroup.length > 25) {
    output_manager_default.error("Maximum 25 condition groups allowed.");
    return 1;
  }
  const rule = {
    name: ruleData.name,
    description: ruleData.description,
    active: ruleData.active !== false,
    conditionGroup: ruleData.conditionGroup,
    action: ruleData.action
  };
  return createRule(
    client,
    parsed,
    rule
  );
}
async function handleFlagAdd(client, parsed, conditionFlags) {
  const name = parsed.args[0];
  if (!name) {
    output_manager_default.error(
      `Missing rule name. Provide as the first argument: ${import_chalk84.default.cyan(getCommandName('firewall rules add "Rule name" --condition ...'))}`
    );
    return 1;
  }
  if (name.length > 160) {
    output_manager_default.error("Rule name must be 160 characters or less.");
    return 1;
  }
  const rawArgs = parsed.flags["--condition"];
  if (!rawArgs || rawArgs.length === 0) {
    output_manager_default.error("At least one --condition is required.");
    return 1;
  }
  const hasOr = parsed.flags["--or"];
  let conditionGroups;
  if (hasOr) {
    const interleaved = [];
    const condValues = [...rawArgs];
    let condIdx = 0;
    for (const arg of client.argv) {
      if (arg === "--or") {
        interleaved.push("--or");
      } else if (arg === "--condition" || arg.startsWith("--condition=")) {
        if (condIdx < condValues.length) {
          interleaved.push(condValues[condIdx]);
          condIdx++;
        }
      }
    }
    const result = parseConditionFlags(interleaved);
    if (typeof result === "string") {
      output_manager_default.error(result);
      return 1;
    }
    conditionGroups = result.groups;
  } else {
    const result = parseConditionFlags(rawArgs);
    if (typeof result === "string") {
      output_manager_default.error(result);
      return 1;
    }
    conditionGroups = result.groups;
  }
  const actionType = parsed.flags["--action"];
  if (!actionType) {
    output_manager_default.error(
      `Missing --action. Valid actions: ${VALID_ACTIONS.join(", ")}`
    );
    return 1;
  }
  if (!VALID_ACTIONS.includes(actionType)) {
    output_manager_default.error(
      `Invalid action "${actionType}". Valid actions: ${VALID_ACTIONS.join(", ")}`
    );
    return 1;
  }
  const action = buildActionFromFlags(parsed.flags, actionType);
  if (typeof action === "string") {
    output_manager_default.error(action);
    return 1;
  }
  const description = parsed.flags["--description"];
  if (description && description.length > 256) {
    output_manager_default.error("Rule description must be 256 characters or less.");
    return 1;
  }
  const active = !parsed.flags["--disabled"];
  const rule = {
    name,
    description,
    active,
    conditionGroup: conditionGroups,
    action
  };
  return createRule(client, parsed, rule);
}
async function createRule(client, parsed, rule) {
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const previewRule = { ...rule, id: "(new)" };
  output_manager_default.print(`
${formatRuleExpanded(previewRule)}

`);
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Create this rule?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const createStamp = stamp_default();
  output_manager_default.spinner("Staging rule");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.insert",
        id: null,
        value: rule
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk84.default.cyan("Success!")} Rule "${import_chalk84.default.bold(rule.name)}" staged ${import_chalk84.default.gray(createStamp())}`
    );
    printActionImpactWarning(rule.action);
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to stage rule";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(client, "firewall rules add --yes")
          }
        ]
      });
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/rules/edit.ts
var import_chalk85 = __toESM(require_source(), 1);
async function edit(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesEditSubcommand,
    client,
    "rules edit"
  );
  if (typeof parsed === "number")
    return parsed;
  let identifier = parsed.args[0];
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  output_manager_default.spinner(`Fetching rules for ${import_chalk85.default.bold(project.name)}`);
  const { active, draft } = await listFirewallConfigs(client, project.id, {
    teamId
  });
  const currentRules = draft?.rules || active?.rules || [];
  if (!identifier) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      output_manager_default.error(
        `Missing required argument: <name-or-id>. Run ${import_chalk85.default.cyan(getCommandName("firewall rules list"))} to see all rules.`
      );
      return 1;
    }
    if (currentRules.length === 0) {
      output_manager_default.error(
        "No custom rules configured. Create one first with `vercel firewall rules add`."
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: "Select a rule to edit:",
      choices: currentRules.map((r) => ({
        value: r.id,
        name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = currentRules.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    identifier = selected.name;
  }
  const matches = resolveRule(currentRules, identifier);
  if (matches.length === 0) {
    output_manager_default.error(
      `No rule found for "${identifier}". Run ${import_chalk85.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
    );
    return 1;
  }
  let originalRule = matches[0];
  if (matches.length > 1) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      output_manager_default.error(
        `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: `Multiple rules match "${identifier}". Select one:`,
      choices: matches.map((r) => ({
        value: r.id,
        name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] (${r.id})`
      }))
    });
    const selected = matches.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    originalRule = selected;
  }
  output_manager_default.stopSpinner();
  const aiPrompt = parsed.flags["--ai"];
  const jsonInput = parsed.flags["--json"];
  const conditionFlags = parsed.flags["--condition"];
  const modeCount = [aiPrompt, jsonInput, conditionFlags].filter(
    Boolean
  ).length;
  if (modeCount > 1) {
    output_manager_default.error(
      "Cannot use --ai, --json, and --condition together. Use only one mode."
    );
    return 1;
  }
  if (aiPrompt) {
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ai_not_available",
          message: "AI mode is not available in non-interactive mode. AI-generated changes require human review. Use --json or flag-based editing instead.",
          next: [
            {
              command: withGlobalFlags6(
                client,
                `firewall rules edit "${identifier}" --action challenge --yes`
              ),
              when: "edit with flags"
            },
            {
              command: withGlobalFlags6(
                client,
                `firewall rules edit "${identifier}" --json '{"name":"...","conditionGroup":[...],"action":{...}}' --yes`
              ),
              when: "edit with JSON"
            }
          ]
        });
      }
      output_manager_default.error(
        "AI mode is not available in non-interactive mode. Use --json or flag-based editing instead."
      );
      return 1;
    }
    return handleAIEdit(client, project, teamId, originalRule, {
      prompt: aiPrompt,
      skipPrompts: !!parsed.flags["--yes"]
    });
  }
  if (jsonInput) {
    return handleJsonEdit(
      client,
      parsed,
      originalRule,
      jsonInput,
      project,
      teamId
    );
  }
  const hasEditFlags = conditionFlags || parsed.flags["--action"] || parsed.flags["--name"] || parsed.flags["--description"] !== void 0 || parsed.flags["--duration"] || parsed.flags["--enabled"] || parsed.flags["--disabled"] || parsed.flags["--rate-limit-algo"] || parsed.flags["--rate-limit-window"] || parsed.flags["--rate-limit-requests"] || parsed.flags["--rate-limit-keys"] || parsed.flags["--rate-limit-action"] || parsed.flags["--redirect-url"] || parsed.flags["--redirect-permanent"];
  if (hasEditFlags) {
    return handleFlagEdit(client, parsed, originalRule, argv, project, teamId);
  }
  if (client.stdin.isTTY && !client.nonInteractive) {
    output_manager_default.print(`
${import_chalk85.default.bold("Current rule:")}
`);
    output_manager_default.print(`${formatRuleExpanded(originalRule)}

`);
    const mode = await client.input.select({
      message: "How would you like to edit this rule?",
      choices: [
        { value: "ai", name: "Describe changes (AI-powered)" },
        { value: "manual", name: "Edit manually (field by field)" }
      ]
    });
    if (mode === "ai") {
      return handleAIEdit(client, project, teamId, originalRule, {
        skipPrompts: false
      });
    }
    const modified = await runInteractiveEditLoop2(client, originalRule);
    if (!modified) {
      output_manager_default.log("Canceled");
      return 0;
    }
    if (JSON.stringify(modified) === JSON.stringify(originalRule)) {
      output_manager_default.log("No changes detected.");
      return 0;
    }
    return saveEdit(client, project, teamId, originalRule, modified, parsed, {
      skipConfirm: true
    });
  }
  if (client.nonInteractive) {
    outputAgentError(client, {
      status: "error",
      reason: "missing_flags",
      message: "No edit flags provided. Use --json, --condition, --action, --name, etc.",
      next: [
        {
          command: withGlobalFlags6(
            client,
            `firewall rules edit "${identifier}" --action challenge --yes`
          ),
          when: "edit with flags"
        },
        {
          command: withGlobalFlags6(
            client,
            `firewall rules edit "${identifier}" --json '{"name":"...","conditionGroup":[...],"action":{"mitigate":{"action":"deny"}}}' --yes`
          ),
          when: "edit with JSON"
        }
      ]
    });
  }
  output_manager_default.error(
    "Interactive mode is not available. Use --json or flag-based editing."
  );
  return 1;
}
async function handleAIEdit(client, project, teamId, originalRule, opts) {
  let prompt = opts.prompt;
  if (!prompt) {
    if (!client.stdin.isTTY || client.nonInteractive) {
      output_manager_default.error("--ai requires a description.");
      return 1;
    }
    prompt = await client.input.text({
      message: "Describe the changes you want:",
      validate: (val) => val.trim() ? true : "Please describe the changes."
    });
  }
  let currentRule = JSON.parse(JSON.stringify(originalRule));
  for (; ; ) {
    output_manager_default.spinner("Generating changes with AI...");
    try {
      const response = await generateFirewallRule(
        client,
        project.id,
        { prompt, currentRule },
        { teamId }
      );
      output_manager_default.stopSpinner();
      if (response.error) {
        if (client.stdin.isTTY && !client.nonInteractive && !opts.skipPrompts) {
          const retryChoice = await client.input.select({
            message: `AI could not update the rule: ${response.error}`,
            choices: [
              {
                value: "retry",
                name: "Try again with a different description"
              },
              { value: "cancel", name: "Cancel" }
            ]
          });
          if (retryChoice === "cancel") {
            output_manager_default.log("Canceled");
            return 0;
          }
          prompt = await client.input.text({
            message: "Describe the changes you want:",
            validate: (val) => val.trim() ? true : "Please describe the changes."
          });
          continue;
        }
        output_manager_default.error(`AI could not update the rule: ${response.error}`);
        return 1;
      }
      if (!response.rule) {
        if (client.stdin.isTTY && !client.nonInteractive && !opts.skipPrompts) {
          const retryChoice = await client.input.select({
            message: "AI did not return a rule.",
            choices: [
              {
                value: "retry",
                name: "Try again with a different description"
              },
              { value: "cancel", name: "Cancel" }
            ]
          });
          if (retryChoice === "cancel") {
            output_manager_default.log("Canceled");
            return 0;
          }
          prompt = await client.input.text({
            message: "Describe the changes you want:",
            validate: (val) => val.trim() ? true : "Please describe the changes."
          });
          continue;
        }
        output_manager_default.error("AI did not return a rule.");
        return 1;
      }
      currentRule = { ...response.rule, id: originalRule.id };
      break;
    } catch (e2) {
      output_manager_default.stopSpinner();
      const error = e2;
      if (!client.stdin.isTTY || client.nonInteractive) {
        output_manager_default.error(error.message || "Failed to generate changes");
        return 1;
      }
      const retryChoice = await client.input.select({
        message: `Generation failed: ${error.message || "Unknown error"}`,
        choices: [
          { value: "retry", name: "Try again" },
          { value: "cancel", name: "Cancel" }
        ]
      });
      if (retryChoice === "cancel") {
        output_manager_default.log("Canceled");
        return 0;
      }
    }
  }
  output_manager_default.print(`
${formatRuleExpanded(currentRule)}

`);
  if (opts.skipPrompts) {
    return saveEdit(client, project, teamId, originalRule, currentRule, {
      flags: { "--yes": true }
    });
  }
  for (; ; ) {
    let choice;
    try {
      choice = await client.input.select({
        message: "What would you like to do?",
        choices: [
          { value: "save", name: "Save changes" },
          { value: "edit-ai", name: "Edit with AI (describe more changes)" },
          { value: "edit-manual", name: "Edit manually (field by field)" },
          { value: "discard", name: "Discard" }
        ]
      });
    } catch {
      return 1;
    }
    if (choice === "save") {
      return saveEdit(
        client,
        project,
        teamId,
        originalRule,
        currentRule,
        {
          flags: {}
        },
        { skipConfirm: true }
      );
    }
    if (choice === "edit-ai") {
      prompt = await client.input.text({
        message: "Describe the changes you want:",
        validate: (val) => val.trim() ? true : "Please describe the changes."
      });
      output_manager_default.spinner("Regenerating...");
      try {
        const response = await generateFirewallRule(
          client,
          project.id,
          { prompt, currentRule },
          { teamId }
        );
        output_manager_default.stopSpinner();
        if (response.error || !response.rule) {
          output_manager_default.error(
            `AI could not update the rule: ${response.error || "No rule returned"}`
          );
          continue;
        }
        currentRule = { ...response.rule, id: originalRule.id };
        output_manager_default.print(`
${formatRuleExpanded(currentRule)}

`);
        continue;
      } catch (e2) {
        output_manager_default.stopSpinner();
        const error = e2;
        output_manager_default.error(error.message || "Failed to regenerate");
        continue;
      }
    }
    if (choice === "edit-manual") {
      const modified = await runInteractiveEditLoop2(client, currentRule);
      if (!modified) {
        continue;
      }
      currentRule = modified;
      output_manager_default.print(`
${formatRuleExpanded(currentRule)}

`);
      continue;
    }
    if (choice === "discard") {
      output_manager_default.log("Discarded");
      return 0;
    }
  }
}
async function handleJsonEdit(client, parsed, originalRule, jsonStr, project, teamId) {
  let ruleData;
  try {
    ruleData = JSON.parse(jsonStr);
  } catch {
    output_manager_default.error("Invalid JSON. Please provide a valid JSON object.");
    return 1;
  }
  if (!ruleData.name || typeof ruleData.name !== "string") {
    output_manager_default.error('JSON must include a "name" field (string).');
    return 1;
  }
  if (!ruleData.conditionGroup || !Array.isArray(ruleData.conditionGroup)) {
    output_manager_default.error('JSON must include a "conditionGroup" field (array).');
    return 1;
  }
  if (!ruleData.action || typeof ruleData.action !== "object") {
    output_manager_default.error('JSON must include an "action" field (object).');
    return 1;
  }
  if (ruleData.name.length > 160) {
    output_manager_default.error("Rule name must be 160 characters or less.");
    return 1;
  }
  if (ruleData.description !== void 0 && ruleData.description !== null) {
    if (typeof ruleData.description !== "string") {
      output_manager_default.error('JSON "description" field must be a string.');
      return 1;
    }
    if (ruleData.description.length > 256) {
      output_manager_default.error("Rule description must be 256 characters or less.");
      return 1;
    }
  }
  const actionObj = ruleData.action;
  if (!actionObj.mitigate || typeof actionObj.mitigate !== "object") {
    output_manager_default.error(
      'action must have a "mitigate" field. Example: { "mitigate": { "action": "deny" } }'
    );
    return 1;
  }
  const mitigateObj = actionObj.mitigate;
  if (!mitigateObj.action || typeof mitigateObj.action !== "string") {
    output_manager_default.error(
      'action.mitigate must have an "action" field (string). Valid: deny, challenge, rate_limit, log, redirect.'
    );
    return 1;
  }
  const modified = {
    id: originalRule.id,
    name: ruleData.name,
    description: ruleData.description,
    active: ruleData.active !== void 0 ? ruleData.active !== false : originalRule.active !== false,
    conditionGroup: ruleData.conditionGroup,
    action: ruleData.action
  };
  return saveEdit(client, project, teamId, originalRule, modified, parsed);
}
function extractConditionFlags(argv) {
  const result = [];
  let i = 0;
  while (i < argv.length) {
    if (argv[i] === "--or") {
      result.push("--or");
      i++;
    } else if (argv[i] === "--condition" && i + 1 < argv.length) {
      result.push(argv[i + 1]);
      i += 2;
    } else if (argv[i].startsWith("--condition=")) {
      result.push(argv[i].slice("--condition=".length));
      i++;
    } else {
      i++;
    }
  }
  return result;
}
async function handleFlagEdit(client, parsed, originalRule, rawArgv, project, teamId) {
  const modified = JSON.parse(JSON.stringify(originalRule));
  const newName = parsed.flags["--name"];
  if (newName) {
    if (newName.length > 160) {
      output_manager_default.error("Rule name must be 160 characters or less.");
      return 1;
    }
    modified.name = newName;
  }
  if (parsed.flags["--description"] !== void 0) {
    const desc = parsed.flags["--description"];
    if (desc.length > 256) {
      output_manager_default.error("Description must be 256 characters or less.");
      return 1;
    }
    modified.description = desc === "" ? void 0 : desc;
  }
  if (parsed.flags["--enabled"] && parsed.flags["--disabled"]) {
    output_manager_default.error("Cannot use --enabled and --disabled together.");
    return 1;
  }
  if (parsed.flags["--enabled"]) {
    modified.active = true;
  }
  if (parsed.flags["--disabled"]) {
    modified.active = false;
  }
  const conditionFlags = parsed.flags["--condition"];
  if (conditionFlags) {
    const interleaved = extractConditionFlags(rawArgv);
    if (interleaved.length === 0) {
      output_manager_default.error("At least one --condition is required.");
      return 1;
    }
    const condResult = parseConditionFlags(interleaved);
    if (typeof condResult === "string") {
      output_manager_default.error(condResult);
      return 1;
    }
    modified.conditionGroup = condResult.groups;
  }
  const actionType = parsed.flags["--action"];
  if (actionType) {
    if (!VALID_ACTIONS.includes(actionType)) {
      output_manager_default.error(
        `Invalid action "${actionType}". Valid actions: ${VALID_ACTIONS.join(", ")}`
      );
      return 1;
    }
    const actionResult = buildActionFromFlags(parsed.flags, actionType);
    if (typeof actionResult === "string") {
      output_manager_default.error(actionResult);
      return 1;
    }
    modified.action = actionResult;
  } else {
    const duration = parsed.flags["--duration"];
    if (duration) {
      if (!VALID_DURATIONS2.includes(duration)) {
        output_manager_default.error(
          `Invalid duration "${duration}". Valid durations: ${VALID_DURATIONS2.join(", ")}`
        );
        return 1;
      }
      if (modified.action.mitigate) {
        modified.action.mitigate.actionDuration = duration;
      }
    }
  }
  if (JSON.stringify(modified) === JSON.stringify(originalRule)) {
    output_manager_default.log("No changes detected.");
    return 0;
  }
  return saveEdit(client, project, teamId, originalRule, modified, parsed);
}
async function saveEdit(client, project, teamId, originalRule, modified, parsed, opts) {
  if (!opts?.skipConfirm) {
    output_manager_default.print(`
${formatRuleExpanded(modified)}

`);
    const confirmed = await confirmAction(
      client,
      parsed.flags["--yes"],
      `Save changes to "${modified.name}"?`
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  const editStamp = stamp_default();
  output_manager_default.spinner("Staging changes");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.update",
        id: originalRule.id,
        value: {
          name: modified.name,
          description: modified.description,
          active: modified.active,
          conditionGroup: modified.conditionGroup,
          action: modified.action
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk85.default.cyan("Success!")} Rule "${import_chalk85.default.bold(modified.name)}" updated and staged ${import_chalk85.default.gray(editStamp())}`
    );
    printActionImpactWarning(modified.action);
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    const msg = error.message || "Failed to stage rule changes";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "api_error",
        message: msg,
        next: [
          {
            command: withGlobalFlags6(
              client,
              `firewall rules edit "${originalRule.name}" --yes`
            )
          }
        ]
      });
      return 1;
    }
    output_manager_default.error(msg);
    return 1;
  }
}

// src/commands/firewall/rules/enable.ts
var import_chalk86 = __toESM(require_source(), 1);
async function enable2(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesEnableSubcommand,
    client,
    "rules enable"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  let identifier = parsed.args[0];
  output_manager_default.spinner(`Fetching rules for ${import_chalk86.default.bold(project.name)}`);
  const { active, draft } = await listFirewallConfigs(client, project.id, {
    teamId
  });
  const currentRules = draft?.rules || active?.rules || [];
  if (currentRules.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      "No custom rules configured. Create one first with `vercel firewall rules add`."
    );
    return 1;
  }
  if (!identifier) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "missing_arguments",
            message: "Rule name or ID is required.",
            next: [
              {
                command: withGlobalFlags6(
                  client,
                  "firewall rules enable <name-or-id>"
                ),
                when: "replace <name-or-id>"
              },
              {
                command: withGlobalFlags6(client, "firewall rules list"),
                when: "list rules"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `Rule name or ID is required. Usage: ${getCommandName("firewall rules enable <name-or-id>")}`
      );
      return 1;
    }
    const disabledRules = currentRules.filter((r) => r.active === false);
    if (disabledRules.length === 0) {
      output_manager_default.log("All rules are already enabled.");
      return 0;
    }
    const selectedId = await client.input.select({
      message: "Select a rule to enable:",
      choices: disabledRules.map((r) => ({
        value: r.id,
        name: `${r.name} \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = currentRules.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    identifier = selected.name;
  }
  const allMatches = resolveRule(currentRules, identifier);
  if (allMatches.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `No rule found for "${identifier}". Run ${import_chalk86.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
    );
    return 1;
  }
  const matches = allMatches.filter((r) => r.active === false);
  if (matches.length === 0) {
    output_manager_default.stopSpinner();
    if (allMatches.length === 1) {
      output_manager_default.log(`Rule "${allMatches[0].name}" is already enabled.`);
    } else {
      output_manager_default.log(`All matching rules are already enabled.`);
    }
    return 0;
  }
  let rule = matches[0];
  if (matches.length > 1) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ambiguous_match",
          message: `Multiple rules match "${identifier}". Specify the full rule ID.`,
          next: matches.map((r) => ({
            command: withGlobalFlags6(
              client,
              `firewall rules enable "${r.id}" --yes`
            ),
            when: `enable "${r.name}"`
          }))
        });
      }
      output_manager_default.error(
        `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: `Multiple rules match "${identifier}". Select one:`,
      choices: matches.map((r) => ({
        value: r.id,
        name: `${r.name} \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = matches.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    rule = selected;
  }
  output_manager_default.stopSpinner();
  const enableStamp = stamp_default();
  output_manager_default.spinner("Staging changes");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.update",
        id: rule.id,
        value: {
          name: rule.name,
          description: rule.description,
          active: true,
          conditionGroup: rule.conditionGroup,
          action: rule.action
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk86.default.cyan("Enabled")} rule "${import_chalk86.default.bold(rule.name)}" ${import_chalk86.default.gray(enableStamp())}`
    );
    printActionImpactWarning(rule.action);
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    output_manager_default.error(error.message || "Failed to enable rule");
    return 1;
  }
}

// src/commands/firewall/rules/disable.ts
var import_chalk87 = __toESM(require_source(), 1);
async function disable2(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesDisableSubcommand,
    client,
    "rules disable"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  let identifier = parsed.args[0];
  output_manager_default.spinner(`Fetching rules for ${import_chalk87.default.bold(project.name)}`);
  const { active, draft } = await listFirewallConfigs(client, project.id, {
    teamId
  });
  const currentRules = draft?.rules || active?.rules || [];
  if (currentRules.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      "No custom rules configured. Create one first with `vercel firewall rules add`."
    );
    return 1;
  }
  if (!identifier) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "missing_arguments",
            message: "Rule name or ID is required.",
            next: [
              {
                command: withGlobalFlags6(
                  client,
                  "firewall rules disable <name-or-id>"
                ),
                when: "replace <name-or-id>"
              },
              {
                command: withGlobalFlags6(client, "firewall rules list"),
                when: "list rules"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `Rule name or ID is required. Usage: ${getCommandName("firewall rules disable <name-or-id>")}`
      );
      return 1;
    }
    const enabledRules = currentRules.filter((r) => r.active !== false);
    if (enabledRules.length === 0) {
      output_manager_default.log("All rules are already disabled.");
      return 0;
    }
    const selectedId = await client.input.select({
      message: "Select a rule to disable:",
      choices: enabledRules.map((r) => ({
        value: r.id,
        name: `${r.name} \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = currentRules.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    identifier = selected.name;
  }
  const allMatches = resolveRule(currentRules, identifier);
  if (allMatches.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `No rule found for "${identifier}". Run ${import_chalk87.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
    );
    return 1;
  }
  const matches = allMatches.filter((r) => r.active !== false);
  if (matches.length === 0) {
    output_manager_default.stopSpinner();
    if (allMatches.length === 1) {
      output_manager_default.log(`Rule "${allMatches[0].name}" is already disabled.`);
    } else {
      output_manager_default.log(`All matching rules are already disabled.`);
    }
    return 0;
  }
  let rule = matches[0];
  if (matches.length > 1) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ambiguous_match",
          message: `Multiple rules match "${identifier}". Specify the full rule ID.`,
          next: matches.map((r) => ({
            command: withGlobalFlags6(
              client,
              `firewall rules disable "${r.id}" --yes`
            ),
            when: `disable "${r.name}"`
          }))
        });
      }
      output_manager_default.error(
        `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: `Multiple rules match "${identifier}". Select one:`,
      choices: matches.map((r) => ({
        value: r.id,
        name: `${r.name} \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = matches.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    rule = selected;
  }
  output_manager_default.stopSpinner();
  const disableStamp = stamp_default();
  output_manager_default.spinner("Staging changes");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.update",
        id: rule.id,
        value: {
          name: rule.name,
          description: rule.description,
          active: false,
          conditionGroup: rule.conditionGroup,
          action: rule.action
        }
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk87.default.cyan("Disabled")} rule "${import_chalk87.default.bold(rule.name)}" ${import_chalk87.default.gray(disableStamp())}`
    );
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    output_manager_default.error(error.message || "Failed to disable rule");
    return 1;
  }
}

// src/commands/firewall/rules/remove.ts
var import_chalk88 = __toESM(require_source(), 1);
async function remove3(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesRemoveSubcommand,
    client,
    "rules remove"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  let identifier = parsed.args[0];
  output_manager_default.spinner(`Fetching rules for ${import_chalk88.default.bold(project.name)}`);
  const { active, draft } = await listFirewallConfigs(client, project.id, {
    teamId
  });
  const currentRules = draft?.rules || active?.rules || [];
  if (currentRules.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      "No custom rules configured. Create one first with `vercel firewall rules add`."
    );
    return 1;
  }
  if (!identifier) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "missing_arguments",
            message: "Rule name or ID is required.",
            next: [
              {
                command: withGlobalFlags6(
                  client,
                  "firewall rules remove <name-or-id> --yes"
                ),
                when: "replace <name-or-id>"
              },
              {
                command: withGlobalFlags6(client, "firewall rules list"),
                when: "list rules"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `Rule name or ID is required. Usage: ${getCommandName("firewall rules remove <name-or-id> --yes")}`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: "Select a rule to remove:",
      choices: currentRules.map((r) => ({
        value: r.id,
        name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = currentRules.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    identifier = selected.name;
  }
  const matches = resolveRule(currentRules, identifier);
  if (matches.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `No rule found for "${identifier}". Run ${import_chalk88.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
    );
    return 1;
  }
  let rule = matches[0];
  if (matches.length > 1) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ambiguous_match",
          message: `Multiple rules match "${identifier}". Specify the full rule ID.`,
          next: matches.map((r) => ({
            command: withGlobalFlags6(
              client,
              `firewall rules remove "${r.id}" --yes`
            ),
            when: `remove "${r.name}"`
          }))
        });
      }
      output_manager_default.error(
        `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: `Multiple rules match "${identifier}". Select one:`,
      choices: matches.map((r) => ({
        value: r.id,
        name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = matches.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    rule = selected;
  }
  output_manager_default.stopSpinner();
  const action = formatActionDisplay(rule.action);
  const conditionCount = rule.conditionGroup.reduce(
    (sum, g) => sum + g.conditions.length,
    0
  );
  output_manager_default.log(
    `Rule: ${import_chalk88.default.bold(rule.name)} [${rule.active ? "Enabled" : "Disabled"}] \u2014 ${action} (${conditionCount} condition${conditionCount !== 1 ? "s" : ""})`
  );
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Remove rule "${rule.name}"?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const removeStamp = stamp_default();
  output_manager_default.spinner("Staging removal");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.remove",
        id: rule.id,
        value: null
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk88.default.cyan("Removed")} rule "${import_chalk88.default.bold(rule.name)}" ${import_chalk88.default.gray(removeStamp())}`
    );
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    output_manager_default.error(error.message || "Failed to remove rule");
    return 1;
  }
}

// src/commands/firewall/rules/reorder.ts
var import_chalk89 = __toESM(require_source(), 1);
async function reorder(client, argv) {
  const parsed = await parseSubcommandArgs2(
    argv,
    rulesReorderSubcommand,
    client,
    "rules reorder"
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink2(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  let identifier = parsed.args[0];
  output_manager_default.spinner(`Fetching rules for ${import_chalk89.default.bold(project.name)}`);
  const { active, draft } = await listFirewallConfigs(client, project.id, {
    teamId
  });
  const currentRules = draft?.rules || active?.rules || [];
  if (currentRules.length < 2) {
    output_manager_default.stopSpinner();
    output_manager_default.error("Need at least 2 rules to reorder.");
    return 1;
  }
  if (!identifier) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "missing_arguments",
            message: "Rule name or ID is required.",
            next: [
              {
                command: withGlobalFlags6(
                  client,
                  "firewall rules reorder <name-or-id> --first --yes"
                ),
                when: "replace <name-or-id>"
              },
              {
                command: withGlobalFlags6(client, "firewall rules list"),
                when: "list rules"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `Rule name or ID is required. Usage: ${getCommandName("firewall rules reorder <name-or-id> --position N --yes")}`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: "Select a rule to reorder:",
      choices: currentRules.map((r, i) => ({
        value: r.id,
        name: `${i + 1}. ${r.name} [${r.active ? "Enabled" : "Disabled"}] \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = currentRules.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    identifier = selected.name;
  }
  const matches = resolveRule(currentRules, identifier);
  if (matches.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `No rule found for "${identifier}". Run ${import_chalk89.default.cyan(getCommandName("firewall rules list"))} to view all rules.`
    );
    return 1;
  }
  let rule = matches[0];
  if (matches.length > 1) {
    output_manager_default.stopSpinner();
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "ambiguous_match",
          message: `Multiple rules match "${identifier}". Specify the full rule ID.`,
          next: matches.map((r) => ({
            command: withGlobalFlags6(
              client,
              `firewall rules reorder "${r.id}" --first --yes`
            ),
            when: `reorder "${r.name}"`
          }))
        });
      }
      output_manager_default.error(
        `Multiple rules match "${identifier}". Specify the full rule ID to disambiguate.`
      );
      return 1;
    }
    const selectedId = await client.input.select({
      message: `Multiple rules match "${identifier}". Select one:`,
      choices: matches.map((r) => ({
        value: r.id,
        name: `${r.name} [${r.active ? "Enabled" : "Disabled"}] \u2014 ${formatActionDisplay(r.action)}`
      }))
    });
    const selected = matches.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("No rule selected");
      return 1;
    }
    rule = selected;
  }
  output_manager_default.stopSpinner();
  const currentIndex = currentRules.findIndex((r) => r.id === rule.id);
  const flagFirst = parsed.flags["--first"];
  const flagLast = parsed.flags["--last"];
  const flagPosition = parsed.flags["--position"];
  const positionFlagCount = [flagFirst, flagLast, flagPosition].filter(
    Boolean
  ).length;
  if (positionFlagCount > 1) {
    output_manager_default.error(
      "Cannot use --first, --last, and --position together. Use only one."
    );
    return 1;
  }
  let targetIndex;
  if (flagFirst) {
    targetIndex = 0;
  } else if (flagLast) {
    targetIndex = currentRules.length - 1;
  } else if (flagPosition !== void 0) {
    if (flagPosition < 1) {
      output_manager_default.error("Position must be at least 1.");
      return 1;
    }
    if (flagPosition > currentRules.length) {
      output_manager_default.error(
        `Position ${flagPosition} exceeds the number of rules (${currentRules.length}).`
      );
      return 1;
    }
    targetIndex = flagPosition - 1;
  } else {
    if (client.nonInteractive || !client.stdin.isTTY) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "missing_arguments",
            message: "A position flag is required. Use --position N, --first, or --last.",
            next: [
              {
                command: withGlobalFlags6(
                  client,
                  `firewall rules reorder "${rule.name}" --first --yes`
                ),
                when: "move to first position"
              },
              {
                command: withGlobalFlags6(
                  client,
                  `firewall rules reorder "${rule.name}" --last --yes`
                ),
                when: "move to last position"
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(
        `A position flag is required. Use --position N, --first, or --last.`
      );
      return 1;
    }
    output_manager_default.print("\n  Current rule order:\n");
    for (let i = 0; i < currentRules.length; i++) {
      const r = currentRules[i];
      const marker = r.id === rule.id ? "\u2192" : " ";
      output_manager_default.print(
        `  ${marker} ${i + 1}. ${r.name} [${r.active ? "Enabled" : "Disabled"}]
`
      );
    }
    output_manager_default.print("\n");
    const positionStr = await client.input.text({
      message: `Move "${rule.name}" to position (1-${currentRules.length}):`,
      validate: (val) => {
        const num = Number(val);
        if (Number.isNaN(num) || !Number.isInteger(num)) {
          return "Enter a number.";
        }
        if (num < 1 || num > currentRules.length) {
          return `Position must be between 1 and ${currentRules.length}.`;
        }
        return true;
      }
    });
    targetIndex = Number(positionStr) - 1;
  }
  if (currentIndex === targetIndex) {
    output_manager_default.log(
      `Rule "${rule.name}" is already at position ${targetIndex + 1}.`
    );
    return 0;
  }
  const confirmed = await confirmAction(
    client,
    parsed.flags["--yes"],
    `Move "${rule.name}" from position ${currentIndex + 1} to position ${targetIndex + 1}?`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const reorderStamp = stamp_default();
  output_manager_default.spinner("Staging reorder");
  try {
    const hadExistingDraft = await detectExistingDraft(
      client,
      project.id,
      teamId
    );
    await patchFirewallDraft(
      client,
      project.id,
      {
        action: "rules.priority",
        id: rule.id,
        value: targetIndex
      },
      { teamId }
    );
    output_manager_default.log(
      `${import_chalk89.default.cyan("Moved")} rule "${import_chalk89.default.bold(rule.name)}" to position ${targetIndex + 1} ${import_chalk89.default.gray(reorderStamp())}`
    );
    await offerAutoPublish(client, project.id, hadExistingDraft, {
      teamId,
      skipPrompts: parsed.flags["--yes"]
    });
    return 0;
  } catch (e2) {
    const error = e2;
    output_manager_default.error(error.message || "Failed to reorder rule");
    return 1;
  }
}

// src/commands/firewall/rules/index.ts
var COMMAND_CONFIG19 = {
  list: getCommandAliases(rulesListSubcommand),
  inspect: getCommandAliases(rulesInspectSubcommand2),
  add: getCommandAliases(rulesAddSubcommand2),
  edit: getCommandAliases(rulesEditSubcommand),
  enable: getCommandAliases(rulesEnableSubcommand),
  disable: getCommandAliases(rulesDisableSubcommand),
  remove: getCommandAliases(rulesRemoveSubcommand),
  reorder: getCommandAliases(rulesReorderSubcommand)
};
async function main14(client, args) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(rulesSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(args, flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const {
    subcommand,
    args: subArgs,
    subcommandOriginal
  } = getSubcommand5(parsedArgs.args, COMMAND_CONFIG19);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall", "rules");
    output_manager_default.print(
      help(rulesSubcommand, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesListSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesList(subcommandOriginal);
      return list8(client, subArgs);
    case "inspect":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesInspectSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesInspect(subcommandOriginal);
      return inspect2(client, subArgs);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesAddSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesAdd(subcommandOriginal);
      return add6(client, subArgs);
    case "edit":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesEditSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesEdit(subcommandOriginal);
      return edit(client, subArgs);
    case "enable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesEnableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesEnable(subcommandOriginal);
      return enable2(client, subArgs);
    case "disable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesDisableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesDisable(subcommandOriginal);
      return disable2(client, subArgs);
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesRemoveSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesRemove(subcommandOriginal);
      return remove3(client, subArgs);
    case "reorder":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", `rules:${subcommandOriginal}`);
        printHelp(rulesReorderSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRulesReorder(subcommandOriginal);
      return reorder(client, subArgs);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG19));
      output_manager_default.print(
        help(rulesSubcommand, {
          parent: firewallCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/firewall/index.ts
var COMMAND_CONFIG20 = {
  overview: getCommandAliases(overviewSubcommand),
  rules: getCommandAliases(rulesSubcommand),
  diff: getCommandAliases(diffSubcommand),
  publish: getCommandAliases(publishSubcommand),
  discard: getCommandAliases(discardSubcommand),
  "ip-blocks": getCommandAliases(ipBlocksSubcommand),
  "system-bypass": getCommandAliases(systemBypassSubcommand),
  "attack-mode": getCommandAliases(attackModeSubcommand),
  "system-mitigations": getCommandAliases(systemMitigationsSubcommand)
};
async function main15(client) {
  const telemetry2 = new FirewallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(firewallCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG20
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("firewall");
    output_manager_default.print(help(firewallCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: firewallCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "overview":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", subcommandOriginal);
        printHelp(overviewSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandOverview(subcommandOriginal);
      return overview(client, args);
    case "diff":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", subcommandOriginal);
        printHelp(diffSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDiff(subcommandOriginal);
      return diff(client, args);
    case "publish":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", subcommandOriginal);
        printHelp(publishSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandPublish(subcommandOriginal);
      return publish(client, args);
    case "discard":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("firewall", subcommandOriginal);
        printHelp(discardSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDiscard(subcommandOriginal);
      return discard(client, args);
    case "rules": {
      telemetry2.trackCliSubcommandRules(subcommandOriginal);
      const nestedArgs = needHelp ? [...args, "--help"] : args;
      return main14(client, nestedArgs);
    }
    case "ip-blocks": {
      telemetry2.trackCliSubcommandIpBlocks(subcommandOriginal);
      const nestedArgs = needHelp ? [...args, "--help"] : args;
      return main13(client, nestedArgs);
    }
    case "system-bypass": {
      telemetry2.trackCliSubcommandSystemBypass(subcommandOriginal);
      const nestedArgs = needHelp ? [...args, "--help"] : args;
      return main10(client, nestedArgs);
    }
    case "attack-mode": {
      telemetry2.trackCliSubcommandAttackMode(subcommandOriginal);
      const nestedArgs = needHelp ? [...args, "--help"] : args;
      return main11(client, nestedArgs);
    }
    case "system-mitigations": {
      telemetry2.trackCliSubcommandSystemMitigations(subcommandOriginal);
      const nestedArgs = needHelp ? [...args, "--help"] : args;
      return main12(client, nestedArgs);
    }
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG20));
      output_manager_default.print(help(firewallCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/edge-config/list.ts
var import_chalk90 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/ls.ts
var EdgeConfigLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/list.ts
async function listCmd(client, argv) {
  const telemetry2 = new EdgeConfigLsTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(listSubcommand9.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const lsCheck = validateLsArgs({
    commandName: "edge-config list",
    args,
    maxArgs: 0,
    exitCode: 2
  });
  if (lsCheck !== 0) {
    return lsCheck;
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(flags["--format"]);
  let rows;
  try {
    rows = await client.fetch("/v1/edge-config");
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(rows, null, 2)}
`);
    return 0;
  }
  const { contextName } = await getScope(client);
  output_manager_default.print(
    `${(0, import_chalk90.gray)(`${rows.length} Edge Config${rows.length === 1 ? "" : "s"} found under ${contextName} ${stamp_default()}`)}
`
  );
  if (rows.length === 0) {
    return 0;
  }
  const tableRows = [
    ["id", "slug", "items", "size", "updated"].map((h) => (0, import_chalk90.gray)(h)),
    ...rows.map((r) => [
      r.id,
      r.slug,
      String(r.itemCount ?? ""),
      String(r.sizeInBytes ?? ""),
      r.updatedAt != null ? new Date(r.updatedAt).toISOString() : ""
    ])
  ];
  client.stderr.write(`${table(tableRows, { hsep: 2 })}
`);
  return 0;
}

// src/commands/edge-config/add.ts
var import_chalk91 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/add.ts
var EdgeConfigAddTelemetryClient = class extends TelemetryClient {
  trackCliArgumentSlug(value) {
    this.trackCliArgument({ arg: "slug", value });
  }
  trackCliOptionItems(value) {
    if (value) {
      this.trackCliOption({ option: "items", value: this.redactedValue });
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/add.ts
async function addCmd(client, argv) {
  const telemetry2 = new EdgeConfigAddTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(addSubcommand5.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [slug] = args;
  telemetry2.trackCliArgumentSlug(slug);
  telemetry2.trackCliOptionItems(flags["--items"]);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!slug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Slug is required. Usage: `vercel edge-config add <slug>`",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config add <slug>"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing slug. Usage: ${import_chalk91.default.cyan(getCommandName("edge-config add <slug>"))}`
    );
    return 1;
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let items;
  if (flags["--items"]) {
    try {
      const parsed = JSON.parse(String(flags["--items"]));
      if (typeof parsed !== "object" || parsed === null || Array.isArray(parsed)) {
        output_manager_default.error("`--items` must be a JSON object of key \u2192 value pairs.");
        return 1;
      }
      items = parsed;
    } catch {
      output_manager_default.error("`--items` must be valid JSON.");
      return 1;
    }
  }
  let created;
  try {
    created = await client.fetch("/v1/edge-config", {
      method: "POST",
      body: items ? { slug, items } : { slug }
    });
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(created, null, 2)}
`);
    return 0;
  }
  output_manager_default.success(`Edge Config ${import_chalk91.default.bold(slug)} created.`);
  return 0;
}

// src/commands/edge-config/get.ts
var import_chalk92 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/get.ts
var EdgeConfigGetTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIdOrSlug(value) {
    this.trackCliArgument({ arg: "id-or-slug", value });
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/resolve-edge-config-id.ts
async function resolveEdgeConfigId(client, idOrSlug) {
  if (!idOrSlug) {
    return null;
  }
  if (idOrSlug.startsWith("ecfg_")) {
    return idOrSlug;
  }
  const list15 = await client.fetch("/v1/edge-config");
  const match = list15.find((c) => c.slug === idOrSlug);
  return match?.id ?? null;
}

// src/commands/edge-config/get.ts
async function getCmd(client, argv) {
  const telemetry2 = new EdgeConfigGetTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(getSubcommand.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [idOrSlug] = args;
  telemetry2.trackCliArgumentIdOrSlug(idOrSlug);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!idOrSlug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Edge Config id or slug is required. Usage: `vercel edge-config get <id-or-slug>`",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing id or slug. Usage: ${import_chalk92.default.cyan(getCommandName("edge-config get <id-or-slug>"))}`
    );
    return 1;
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let id;
  try {
    id = await resolveEdgeConfigId(client, idOrSlug);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (!id) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No Edge Config matches "${idOrSlug}" in the current team.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No Edge Config matches "${idOrSlug}" in the current team.`);
    return 1;
  }
  let data;
  try {
    data = await client.fetch(`/v1/edge-config/${encodeURIComponent(id)}`);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(data, null, 2)}
`);
    return 0;
  }
  output_manager_default.log(JSON.stringify(data, null, 2));
  return 0;
}

// src/commands/edge-config/update.ts
var import_chalk93 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/update.ts
var EdgeConfigUpdateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIdOrSlug(value) {
    this.trackCliArgument({ arg: "id-or-slug", value });
  }
  trackCliOptionSlug(value) {
    if (value) {
      this.trackCliOption({ option: "slug", value: this.redactedValue });
    }
  }
  trackCliOptionPatch(value) {
    if (value) {
      this.trackCliOption({ option: "patch", value: this.redactedValue });
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/parse-patch-body.ts
function parsePatchJson(raw) {
  const trimmed = raw.trim();
  try {
    return JSON.parse(trimmed);
  } catch (firstError) {
    const normalized = trimmed.replace(/\\r\\n/g, "\n").replace(/\\n/g, "\n").replace(/\\t/g, "	");
    if (normalized === trimmed) {
      throw firstError;
    }
    try {
      return JSON.parse(normalized);
    } catch {
      throw firstError;
    }
  }
}
function parsePatchBody(raw) {
  const parsed = parsePatchJson(raw);
  if (Array.isArray(parsed)) {
    return { items: parsed };
  }
  if (typeof parsed === "object" && parsed !== null && "items" in parsed && Array.isArray(parsed.items)) {
    return { items: parsed.items };
  }
  throw new Error(
    '`--patch` must be a JSON array of item operations or an object `{ "items": [...] }`.'
  );
}

// src/commands/edge-config/update.ts
async function updateCmd(client, argv) {
  const telemetry2 = new EdgeConfigUpdateTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(updateSubcommand2.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [idOrSlug] = args;
  telemetry2.trackCliArgumentIdOrSlug(idOrSlug);
  telemetry2.trackCliOptionSlug(flags["--slug"]);
  telemetry2.trackCliOptionPatch(flags["--patch"]);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!idOrSlug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Edge Config id or slug is required. Provide `--slug` and/or `--patch`.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing id or slug. Usage: ${import_chalk93.default.cyan(getCommandName("edge-config update <id-or-slug>"))}`
    );
    return 1;
  }
  const newSlug = flags["--slug"];
  const patchRaw = flags["--patch"];
  if (!newSlug && !patchRaw) {
    output_manager_default.error(
      "Provide `--slug` to rename the store and/or `--patch` to update items."
    );
    return 1;
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let id;
  try {
    id = await resolveEdgeConfigId(client, idOrSlug);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (!id) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No Edge Config matches "${idOrSlug}" in the current team.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No Edge Config matches "${idOrSlug}" in the current team.`);
    return 1;
  }
  let parsedPatchBody;
  if (patchRaw) {
    try {
      parsedPatchBody = parsePatchBody(String(patchRaw));
    } catch (e2) {
      output_manager_default.error(e2 instanceof Error ? e2.message : String(e2));
      return 1;
    }
  }
  let lastPayload;
  try {
    if (newSlug) {
      lastPayload = await client.fetch(
        `/v1/edge-config/${encodeURIComponent(id)}`,
        {
          method: "PUT",
          body: { slug: newSlug }
        }
      );
    }
    if (parsedPatchBody) {
      lastPayload = await client.fetch(
        `/v1/edge-config/${encodeURIComponent(id)}/items`,
        {
          method: "PATCH",
          body: parsedPatchBody
        }
      );
    }
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(lastPayload, null, 2)}
`);
    return 0;
  }
  output_manager_default.success("Edge Config updated.");
  return 0;
}

// src/commands/edge-config/remove.ts
var import_chalk94 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/remove.ts
var EdgeConfigRemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIdOrSlug(value) {
    this.trackCliArgument({ arg: "id-or-slug", value });
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/remove.ts
async function removeCmd(client, argv) {
  const telemetry2 = new EdgeConfigRemoveTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(removeSubcommand7.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [idOrSlug] = args;
  const skipConfirmation = flags["--yes"] === true;
  telemetry2.trackCliArgumentIdOrSlug(idOrSlug);
  telemetry2.trackCliFlagYes(flags["--yes"]);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!idOrSlug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Edge Config id or slug is required. Usage: `vercel edge-config remove <id-or-slug>`",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing id or slug. Usage: ${import_chalk94.default.cyan(getCommandName("edge-config remove <id-or-slug>"))}`
    );
    return 1;
  }
  if (client.nonInteractive && !skipConfirmation) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Removing an Edge Config requires confirmation. Re-run with `--yes`.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let id;
  try {
    id = await resolveEdgeConfigId(client, idOrSlug);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (!id) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No Edge Config matches "${idOrSlug}" in the current team.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No Edge Config matches "${idOrSlug}" in the current team.`);
    return 1;
  }
  if (!skipConfirmation && !await client.input.confirm(
    `Delete Edge Config ${import_chalk94.default.bold(id)} (${import_chalk94.default.bold(idOrSlug)})?`,
    false
  )) {
    output_manager_default.log("Canceled");
    return 0;
  }
  try {
    await client.fetch(`/v1/edge-config/${encodeURIComponent(id)}`, {
      method: "DELETE"
    });
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          status: "ok",
          id,
          message: `Edge Config ${id} removed.`
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  output_manager_default.success(`Edge Config ${import_chalk94.default.bold(id)} removed.`);
  return 0;
}

// src/commands/edge-config/items.ts
var import_chalk95 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/items.ts
var EdgeConfigItemsTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIdOrSlug(value) {
    this.trackCliArgument({ arg: "id-or-slug", value });
  }
  trackCliOptionKey(value) {
    if (value) {
      this.trackCliOption({ option: "key", value: this.redactedValue });
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/items.ts
async function itemsCmd(client, argv) {
  const telemetry2 = new EdgeConfigItemsTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(itemsSubcommand.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [idOrSlug] = args;
  const key = flags["--key"];
  telemetry2.trackCliArgumentIdOrSlug(idOrSlug);
  telemetry2.trackCliOptionKey(key);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!idOrSlug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Edge Config id or slug is required. Usage: `vercel edge-config items <id-or-slug>`",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing id or slug. Usage: ${import_chalk95.default.cyan(getCommandName("edge-config items <id-or-slug>"))}`
    );
    return 1;
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let id;
  try {
    id = await resolveEdgeConfigId(client, idOrSlug);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (!id) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No Edge Config matches "${idOrSlug}" in the current team.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No Edge Config matches "${idOrSlug}" in the current team.`);
    return 1;
  }
  let data;
  try {
    if (key) {
      const path3 = `/v1/edge-config/${encodeURIComponent(id)}/item/${encodeURIComponent(key)}`;
      data = await client.fetch(path3);
    } else {
      data = await client.fetch(
        `/v1/edge-config/${encodeURIComponent(id)}/items`
      );
    }
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (key && (data === null || data === void 0)) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No item with key "${key}" in Edge Config ${id}.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `edge-config items ${idOrSlug}`
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No item with key "${key}" in Edge Config ${id}.`);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(data, null, 2)}
`);
    return 0;
  }
  output_manager_default.log(JSON.stringify(data, null, 2));
  return 0;
}

// src/commands/edge-config/tokens.ts
var import_chalk96 = __toESM(require_source(), 1);

// src/util/telemetry/commands/edge-config/tokens.ts
var EdgeConfigTokensTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIdOrSlug(value) {
    this.trackCliArgument({ arg: "id-or-slug", value });
  }
  trackCliOptionAdd(value) {
    if (value) {
      this.trackCliOption({ option: "add", value: this.redactedValue });
    }
  }
  trackCliOptionRemove(values) {
    if (values?.length) {
      this.trackCliOption({
        option: "remove",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/edge-config/tokens.ts
async function tokensCmd(client, argv) {
  const telemetry2 = new EdgeConfigTokensTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      argv,
      getFlagsSpecification(tokensSubcommand.options)
    );
  } catch (error) {
    if (client.nonInteractive) {
      exitWithNonInteractiveError(client, error, 1, { variant: "edge-config" });
    }
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [idOrSlug] = args;
  const addLabel = flags["--add"];
  const removeValues = flags["--remove"];
  const removeCount = removeValues?.length ?? 0;
  const skipConfirmation = flags["--yes"] === true;
  telemetry2.trackCliArgumentIdOrSlug(idOrSlug);
  telemetry2.trackCliOptionAdd(addLabel);
  telemetry2.trackCliOptionRemove(removeValues);
  telemetry2.trackCliFlagYes(flags["--yes"]);
  telemetry2.trackCliOptionFormat(flags["--format"]);
  if (!idOrSlug) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: "Edge Config id or slug is required. Usage: `vercel edge-config tokens <id-or-slug>`",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Missing id or slug. Usage: ${import_chalk96.default.cyan(getCommandName("edge-config tokens <id-or-slug>"))}`
    );
    return 1;
  }
  if (addLabel && removeCount > 0) {
    output_manager_default.error("Use either `--add` or `--remove`, not both.");
    return 1;
  }
  if (removeCount > 0 && client.nonInteractive && !skipConfirmation) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Revoking Edge Config tokens requires confirmation. Re-run with `--yes`.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
  }
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let id;
  try {
    id = await resolveEdgeConfigId(client, idOrSlug);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
  if (!id) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "not_found",
          message: `No Edge Config matches "${idOrSlug}" in the current team.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "edge-config list"
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`No Edge Config matches "${idOrSlug}" in the current team.`);
    return 1;
  }
  const base = `/v1/edge-config/${encodeURIComponent(id)}`;
  try {
    if (addLabel) {
      const created = await client.fetch(
        `${base}/token`,
        {
          method: "POST",
          body: { label: addLabel }
        }
      );
      if (asJson) {
        client.stdout.write(`${JSON.stringify(created, null, 2)}
`);
        return 0;
      }
      output_manager_default.success("Token created.");
      output_manager_default.print(
        `${import_chalk96.default.bold("Token")} (copy now; it will not be shown again):
${created.token}
`
      );
      output_manager_default.print(`${(0, import_chalk96.gray)(`id: ${created.id}`)}
`);
      return 0;
    }
    if (removeValues?.length) {
      if (!skipConfirmation && !await client.input.confirm(
        `Revoke ${removeValues.length} token(s) on ${import_chalk96.default.bold(id)}?`,
        false
      )) {
        output_manager_default.log("Canceled");
        return 0;
      }
      const rows2 = await client.fetch(`${base}/tokens`);
      const knownIds = new Set(
        rows2.map((r) => r.id).filter((v) => Boolean(v))
      );
      const ids = [];
      const tokens = [];
      for (const value of removeValues) {
        if (knownIds.has(value))
          ids.push(value);
        else
          tokens.push(value);
      }
      const body = {};
      if (tokens.length)
        body.tokens = tokens;
      if (ids.length)
        body.ids = ids;
      await client.fetch(`${base}/tokens`, {
        method: "DELETE",
        body
      });
      if (asJson) {
        client.stdout.write(
          `${JSON.stringify({ status: "ok", revoked: removeValues.length }, null, 2)}
`
        );
        return 0;
      }
      output_manager_default.success(`Revoked ${removeValues.length} token(s).`);
      return 0;
    }
    const rows = await client.fetch(`${base}/tokens`);
    if (asJson) {
      const sanitized = rows.map((row) => ({
        id: row.id,
        label: row.label,
        partialToken: row.partialToken,
        createdAt: row.createdAt
      }));
      client.stdout.write(`${JSON.stringify(sanitized, null, 2)}
`);
      return 0;
    }
    if (!rows.length) {
      output_manager_default.log("No tokens.");
      return 0;
    }
    const tableRows = [
      ["id", "label", "value", "created"].map((h) => (0, import_chalk96.gray)(h)),
      ...rows.map((t) => [
        t.id ?? "",
        t.label ?? "",
        t.partialToken ?? "",
        t.createdAt != null ? new Date(t.createdAt).toISOString() : ""
      ])
    ];
    client.stderr.write(`${table(tableRows, { hsep: 2 })}
`);
    return 0;
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "edge-config" });
    printError(err);
    return 1;
  }
}

// src/util/telemetry/commands/edge-config/index.ts
var EdgeConfigTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({ subcommand: "list", value: actual });
  }
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({ subcommand: "add", value: actual });
  }
  trackCliSubcommandGet(actual) {
    this.trackCliSubcommand({ subcommand: "get", value: actual });
  }
  trackCliSubcommandUpdate(actual) {
    this.trackCliSubcommand({ subcommand: "update", value: actual });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({ subcommand: "remove", value: actual });
  }
  trackCliSubcommandItems(actual) {
    this.trackCliSubcommand({ subcommand: "items", value: actual });
  }
  trackCliSubcommandTokens(actual) {
    this.trackCliSubcommand({ subcommand: "tokens", value: actual });
  }
};

// src/commands/edge-config/apply-linked-project-team.ts
async function applyLinkedProjectTeam(client) {
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  }
  if (link.status === "linked") {
    client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  }
  return void 0;
}

// src/commands/edge-config/index.ts
var COMMAND_CONFIG21 = {
  list: ["list", "ls"],
  add: ["add", "create"],
  get: ["get", "inspect"],
  update: ["update"],
  remove: ["remove", "rm", "delete"],
  items: ["items"],
  tokens: ["tokens"]
};
async function main16(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(edgeConfigCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new EdgeConfigTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG21
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("edge-config");
    output_manager_default.print(help(edgeConfigCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: edgeConfigCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  if (!needHelp) {
    const linkExitCode = await applyLinkedProjectTeam(client);
    if (linkExitCode !== void 0) {
      return linkExitCode;
    }
  }
  switch (subcommand) {
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(addSubcommand5);
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return addCmd(client, args);
    case "get":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(getSubcommand);
      }
      telemetry2.trackCliSubcommandGet(subcommandOriginal);
      return getCmd(client, args);
    case "update":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(updateSubcommand2);
      }
      telemetry2.trackCliSubcommandUpdate(subcommandOriginal);
      return updateCmd(client, args);
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(removeSubcommand7);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return removeCmd(client, args);
    case "items":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(itemsSubcommand);
      }
      telemetry2.trackCliSubcommandItems(subcommandOriginal);
      return itemsCmd(client, args);
    case "tokens":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(tokensSubcommand);
      }
      telemetry2.trackCliSubcommandTokens(subcommandOriginal);
      return tokensCmd(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("edge-config", subcommandOriginal);
        return printHelp(listSubcommand9);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return listCmd(client, args);
  }
}

// src/util/telemetry/commands/flags/index.ts
var FlagsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "ls",
      value: actual
    });
  }
  trackCliSubcommandInspect(actual) {
    this.trackCliSubcommand({
      subcommand: "inspect",
      value: actual
    });
  }
  trackCliSubcommandOpen(actual) {
    this.trackCliSubcommand({
      subcommand: "open",
      value: actual
    });
  }
  trackCliSubcommandCreate(actual) {
    this.trackCliSubcommand({
      subcommand: "create",
      value: actual
    });
  }
  trackCliSubcommandUpdate(actual) {
    this.trackCliSubcommand({
      subcommand: "update",
      value: actual
    });
  }
  trackCliSubcommandSet(actual) {
    this.trackCliSubcommand({
      subcommand: "set",
      value: actual
    });
  }
  trackCliSubcommandSplit(actual) {
    this.trackCliSubcommand({
      subcommand: "split",
      value: actual
    });
  }
  trackCliSubcommandRollout(actual) {
    this.trackCliSubcommand({
      subcommand: "rollout",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "rm",
      value: actual
    });
  }
  trackCliSubcommandArchive(actual) {
    this.trackCliSubcommand({
      subcommand: "archive",
      value: actual
    });
  }
  trackCliSubcommandDisable(actual) {
    this.trackCliSubcommand({
      subcommand: "disable",
      value: actual
    });
  }
  trackCliSubcommandEnable(actual) {
    this.trackCliSubcommand({
      subcommand: "enable",
      value: actual
    });
  }
  trackCliSubcommandSdkKeys(actual) {
    this.trackCliSubcommand({
      subcommand: "sdk-keys",
      value: actual
    });
  }
  trackCliSubcommandPrepare(actual) {
    this.trackCliSubcommand({
      subcommand: "prepare",
      value: actual
    });
  }
  trackCliSubcommandOverride(actual) {
    this.trackCliSubcommand({
      subcommand: "override",
      value: actual
    });
  }
};

// src/commands/flags/ls.ts
var import_chalk97 = __toESM(require_source(), 1);
var import_ms12 = __toESM(require_ms(), 1);
var import_pluralize8 = __toESM(require_pluralize(), 1);

// src/util/flags/get-flags.ts
async function getFlags(client, projectId, state = "active") {
  output_manager_default.debug(`Fetching feature flags for project ${projectId}`);
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/flags?state=${state}`;
  const response = await client.fetch(url);
  return response.data;
}
async function getFlag(client, projectId, flagIdOrSlug) {
  output_manager_default.debug(
    `Fetching feature flag ${flagIdOrSlug} for project ${projectId}`
  );
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/flags/${encodeURIComponent(flagIdOrSlug)}`;
  const response = await client.fetch(url);
  return response;
}
async function getFlagSettings(client, projectId) {
  output_manager_default.debug(`Fetching feature flag settings for project ${projectId}`);
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/settings`;
  const response = await client.fetch(url);
  return response;
}

// src/util/telemetry/commands/flags/ls.ts
var FlagsLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionState(state) {
    if (state) {
      this.trackCliOption({
        option: "state",
        value: state
      });
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
};

// src/commands/flags/ls.ts
async function ls7(client, argv) {
  const telemetryClient = new FlagsLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand10.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { flags } = parsedArgs;
  const state = flags["--state"] || "active";
  const json = flags["--json"];
  telemetryClient.trackCliOptionState(state);
  telemetryClient.trackCliFlagJson(json);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  const projectSlugLink = formatProject(org.slug, project.name);
  const lsStamp = stamp_default();
  output_manager_default.spinner(`Fetching ${state} feature flags for ${projectSlugLink}`);
  try {
    const flagsList = await getFlags(client, project.id, state);
    output_manager_default.stopSpinner();
    const sortedFlags = flagsList.sort((a, b) => b.updatedAt - a.updatedAt);
    if (json) {
      outputJson3(client, sortedFlags);
    } else if (flagsList.length === 0) {
      output_manager_default.log(
        `No ${state} feature flags found for ${projectSlugLink} ${import_chalk97.default.gray(lsStamp())}`
      );
    } else {
      output_manager_default.log(
        `${(0, import_pluralize8.default)("feature flag", flagsList.length, true)} found for ${projectSlugLink} ${import_chalk97.default.gray(lsStamp())}`
      );
      printFlagsTable(sortedFlags);
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
function outputJson3(client, flags) {
  const jsonOutput = {
    flags: flags.map((flag) => ({
      id: flag.id,
      slug: flag.slug,
      description: flag.description ?? null,
      kind: flag.kind,
      state: flag.state,
      variants: flag.variants,
      createdAt: flag.createdAt,
      updatedAt: flag.updatedAt
    }))
  };
  client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
}
function printFlagsTable(flags) {
  const headers = ["Name", "Kind", "State", "Variants", "Updated"];
  const now = Date.now();
  const rows = flags.map((flag) => [
    import_chalk97.default.bold(flag.slug),
    flag.kind,
    flag.state === "active" ? import_chalk97.default.green(flag.state) : import_chalk97.default.gray(flag.state),
    String(flag.variants.length),
    (0, import_ms12.default)(now - flag.updatedAt) + " ago"
  ]);
  const table3 = formatTable(
    headers,
    ["l", "l", "l", "r", "l"],
    [{ name: "", rows }]
  );
  output_manager_default.print(`
${table3}
`);
}

// src/util/telemetry/commands/flags/inspect.ts
var FlagsInspectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
};

// src/util/flags/print-flag-details.ts
var import_chalk99 = __toESM(require_source(), 1);
var import_ms13 = __toESM(require_ms(), 1);

// src/util/flags/dashboard-url.ts
function getFlagDashboardUrl(orgSlug, projectName, flagSlug) {
  return `https://vercel.com/${orgSlug}/${projectName}/flag/${flagSlug}`;
}
function getFlagsDashboardUrl(orgSlug, projectName) {
  return `https://vercel.com/${orgSlug}/${projectName}/flags`;
}

// src/util/flags/resolve-variant.ts
var import_chalk98 = __toESM(require_source(), 1);
var import_fast_deep_equal = __toESM(require_fast_deep_equal(), 1);
function formatVariantValue(value) {
  return JSON.stringify(value);
}
function formatVariantForDisplay(variant) {
  const parts = [formatVariantValue(variant.value)];
  if (variant.label) {
    parts.push(import_chalk98.default.dim(variant.label));
  }
  return parts.join(" ");
}
function formatAvailableVariants(variants) {
  return variants.map((v) => `  - ${formatAvailableVariant(v)}`).join("\n");
}
function resolveVariant(input, variants) {
  const byId = variants.find((v) => v.id === input);
  if (byId) {
    return { variant: byId, error: null };
  }
  const parsedInput = parseVariantValue(input);
  const byValue = variants.find((v) => valuesMatch(v.value, parsedInput));
  if (byValue) {
    return { variant: byValue, error: null };
  }
  const byExactStringValue = variants.find((v) => v.value === input);
  if (byExactStringValue) {
    return { variant: byExactStringValue, error: null };
  }
  const availableList = formatAvailableVariants(variants);
  const error = `Variant "${input}" not found.

Available variants:
${availableList}

You can specify a variant by its ID or value.`;
  return { variant: null, error };
}
function resolveVariantOrThrow(input, variants, optionName) {
  const result = resolveVariant(input, variants);
  if (result.error || !result.variant) {
    throw new Error(
      `${optionName} ${import_chalk98.default.bold(input)} is invalid. ${result.error || "Variant not found."}`
    );
  }
  return result.variant;
}
function resolveVariantByIdOrThrow(variantId2, variants, optionName) {
  const variant = variants.find((candidate) => candidate.id === variantId2);
  if (!variant) {
    throw new Error(
      `${optionName} references an unknown variant ${import_chalk98.default.bold(variantId2)}.`
    );
  }
  return variant;
}
function parseVariantValue(input) {
  const trimmed = input.trim();
  if (!trimmed) {
    return input;
  }
  try {
    return JSON.parse(trimmed);
  } catch {
    const lowerInput = trimmed.toLowerCase();
    if (lowerInput === "true") {
      return true;
    }
    if (lowerInput === "false") {
      return false;
    }
    const parsedNumber = Number(trimmed);
    if (!Number.isNaN(parsedNumber)) {
      return parsedNumber;
    }
    return input;
  }
}
function valuesMatch(variantValue, inputValue) {
  return (0, import_fast_deep_equal.default)(variantValue, inputValue);
}
function formatAvailableVariant(variant) {
  const value = formatStyledVariantValue(variant.value);
  if (!variant.label) {
    return value;
  }
  return `${value} ${import_chalk98.default.dim(variant.label)}`;
}
function formatStyledVariantValue(value) {
  const formattedValue = formatVariantValue(value);
  if (typeof value !== "string") {
    return import_chalk98.default.bold(formattedValue);
  }
  return `"${import_chalk98.default.bold(formattedValue.slice(1, -1))}"`;
}

// src/util/flags/print-flag-details.ts
function printFlagDetails({
  flag,
  settings,
  projectSlugLink,
  orgSlug,
  projectName,
  showTimestamps = true
}) {
  const dashboardUrl = getFlagDashboardUrl(orgSlug, projectName, flag.slug);
  output_manager_default.log(
    `
Feature flag ${import_chalk99.default.bold(flag.slug)} for ${projectSlugLink}
`
  );
  output_manager_default.print(`  ${import_chalk99.default.cyan(dashboardUrl)}

`);
  output_manager_default.print(`  ${import_chalk99.default.dim("ID:")}           ${flag.id}
`);
  output_manager_default.print(`  ${import_chalk99.default.dim("Kind:")}         ${flag.kind}
`);
  output_manager_default.print(
    `  ${import_chalk99.default.dim("State:")}        ${flag.state === "active" ? import_chalk99.default.green(flag.state) : import_chalk99.default.gray(flag.state)}
`
  );
  if (flag.description) {
    output_manager_default.print(`  ${import_chalk99.default.dim("Description:")}  ${flag.description}
`);
  }
  if (showTimestamps) {
    output_manager_default.print(
      `  ${import_chalk99.default.dim("Created:")}      ${formatDate(flag.createdAt)}
`
    );
    output_manager_default.print(
      `  ${import_chalk99.default.dim("Updated:")}      ${formatDate(flag.updatedAt)}
`
    );
  }
  output_manager_default.print(`
  ${import_chalk99.default.dim("Variants:")}
`);
  for (const [index, variant] of flag.variants.entries()) {
    output_manager_default.print(`    ${formatVariantListSummary(variant)}
`);
    output_manager_default.print(`      ${import_chalk99.default.dim(`id: ${variant.id}`)}
`);
    if (index < flag.variants.length - 1) {
      output_manager_default.print("\n");
    }
  }
  printFlagEnvironmentDetails(flag, settings);
}
function printFlagEnvironmentDetails(flag, settings, environments) {
  const sortedEnvs = getSortedEnvironmentEntries(flag, environments);
  output_manager_default.print(`
  ${import_chalk99.default.dim("Environments:")}
`);
  for (const [envName, envConfig] of sortedEnvs) {
    if (envConfig.reuse?.active) {
      output_manager_default.print(
        `    ${import_chalk99.default.bold(envName)}: reuses ${import_chalk99.default.cyan(envConfig.reuse.environment)} environment
`
      );
      continue;
    }
    if (envConfig.active) {
      const hasCustomConfiguration = hasCustomConfigurationEnabled(envConfig);
      const envSummary = hasCustomConfiguration ? "custom" : formatEnvironmentOutcome(envConfig.fallthrough, flag.variants);
      output_manager_default.print(`    ${import_chalk99.default.bold(envName)}: ${envSummary}
`);
      if (envConfig.targets && Object.keys(envConfig.targets).length > 0) {
        output_manager_default.print(`      ${import_chalk99.default.dim("Targeting:")}
`);
        for (const [variantId2, entityKinds] of Object.entries(
          envConfig.targets
        )) {
          const variant = flag.variants.find((v) => v.id === variantId2);
          const variantSummary = formatEnvironmentVariantSummary(
            variant,
            variantId2
          );
          for (const [entityKind, attributes] of Object.entries(entityKinds)) {
            for (const [attribute, values] of Object.entries(attributes)) {
              const valueList = values.map((v) => {
                const label = resolveTargetingLabel(
                  settings,
                  entityKind,
                  attribute,
                  v.value
                );
                return label ? `${v.value} ${import_chalk99.default.gray(label)}` : v.value;
              }).join(", ");
              output_manager_default.print(
                `        ${import_chalk99.default.dim(`${entityKind}.${attribute}:`)} ${valueList} ${import_chalk99.default.dim("\u2192")} ${variantSummary}
`
              );
            }
          }
        }
      }
      if (envConfig.rules && envConfig.rules.length > 0) {
        output_manager_default.print(`      ${import_chalk99.default.dim("Rules:")}
`);
        for (const rule of envConfig.rules) {
          const outcome = formatEnvironmentOutcome(rule.outcome, flag.variants);
          output_manager_default.print(`        ${import_chalk99.default.dim("\u2192")} ${outcome}
`);
          for (const condition of rule.conditions) {
            const { text, listItems } = formatCondition2(condition, settings);
            output_manager_default.print(`          ${import_chalk99.default.dim("if")} ${text}
`);
            if (listItems && listItems.length > 0) {
              for (const item of listItems) {
                output_manager_default.print(`             ${import_chalk99.default.dim("-")} ${item}
`);
              }
            }
          }
        }
      }
      if (hasCustomConfiguration && envConfig.fallthrough) {
        const fallthrough = envConfig.fallthrough;
        if (fallthrough.type === "variant") {
          const defaultVariant = flag.variants.find(
            (v) => v.id === fallthrough.variantId
          );
          const defaultSummary = formatEnvironmentVariantSummary(
            defaultVariant,
            fallthrough.variantId
          );
          output_manager_default.print(`      ${import_chalk99.default.dim("Default:")} ${defaultSummary}
`);
        } else if (fallthrough.type === "split") {
          const weights = formatSplitWeights(
            fallthrough.weights,
            flag.variants
          );
          output_manager_default.print(`      ${import_chalk99.default.dim("Default split:")} ${weights}
`);
        } else if (fallthrough.type === "rollout") {
          output_manager_default.print(
            `      ${import_chalk99.default.dim("Rollout:")} ${formatRolloutOutcome(
              fallthrough,
              flag.variants
            )}
`
          );
        }
      }
    } else {
      const pausedVariant = flag.variants.find(
        (v) => v.id === envConfig.pausedOutcome?.variantId
      );
      const pausedSummary = formatEnvironmentVariantSummary(
        pausedVariant,
        envConfig.pausedOutcome?.variantId || "paused"
      );
      output_manager_default.print(`    ${import_chalk99.default.bold(envName)}: ${pausedSummary}
`);
    }
  }
  output_manager_default.print("\n");
}
function getSortedEnvironmentEntries(flag, environments) {
  const envOrder = ["production", "preview", "development"];
  const selectedEnvironments = environments ? new Set(environments) : void 0;
  return Object.entries(flag.environments).filter(
    ([envName]) => selectedEnvironments ? selectedEnvironments.has(envName) : true
  ).sort(([a], [b]) => {
    const aIndex = envOrder.indexOf(a);
    const bIndex = envOrder.indexOf(b);
    if (aIndex === -1 && bIndex === -1) {
      return a.localeCompare(b);
    }
    if (aIndex === -1) {
      return 1;
    }
    if (bIndex === -1) {
      return -1;
    }
    return aIndex - bIndex;
  });
}
function resolveTargetingLabel(settings, entityKind, attribute, value) {
  if (!settings) {
    return void 0;
  }
  const entity = settings.entities.find((e2) => e2.kind === entityKind);
  if (!entity) {
    return void 0;
  }
  const attr = entity.attributes.find((a) => a.key === attribute);
  if (!attr?.labels) {
    return void 0;
  }
  const labelEntry = attr.labels.find((l) => l.value === value);
  return labelEntry?.label;
}
function hasCustomConfigurationEnabled(envConfig) {
  return Boolean(envConfig.targets && Object.keys(envConfig.targets).length > 0) || envConfig.rules.length > 0 || envConfig.fallthrough.type === "split" || envConfig.fallthrough.type === "rollout";
}
function formatEnvironmentOutcome(outcome, variants) {
  if (outcome.type === "variant") {
    const variant = variants.find((v) => v.id === outcome.variantId);
    return formatEnvironmentVariantSummary(variant, outcome.variantId);
  }
  if (outcome.type === "split") {
    const weights = formatSplitWeights(outcome.weights, variants);
    return `split (${weights})`;
  }
  if (outcome.type === "rollout") {
    return formatRolloutOutcome(outcome, variants);
  }
  return "unknown";
}
function formatSplitWeights(weights, variants) {
  const total = Object.values(weights).reduce((sum, weight) => sum + weight, 0);
  return Object.entries(weights).map(([id, weight]) => {
    const variant = variants.find((v) => v.id === id);
    const summary = formatEnvironmentVariantSummary(variant, id);
    const percentage = total > 0 ? weight / total * 100 : 0;
    const formattedPercentage = Number.isInteger(percentage) ? String(percentage) : String(Number(percentage.toFixed(2)));
    return `${summary}: ${formattedPercentage}%`;
  }).join(", ");
}
function formatEnvironmentVariantSummary(variant, fallback) {
  if (!variant) {
    return import_chalk99.default.bold(fallback);
  }
  if (variant.label) {
    return import_chalk99.default.bold(variant.label);
  }
  return import_chalk99.default.bold(formatVariantValue(variant.value));
}
function formatRolloutOutcome(outcome, variants) {
  const fromVariant = variants.find((v) => v.id === outcome.rollFromVariantId);
  const toVariant = variants.find((v) => v.id === outcome.rollToVariantId);
  const defaultVariant = variants.find((v) => v.id === outcome.defaultVariantId);
  const stages = outcome.slots.map((slot) => {
    const percentage = slot.promille / 1e3;
    const formattedPercentage = Number.isInteger(percentage) ? String(percentage) : String(Number(percentage.toFixed(3)));
    return `${formattedPercentage}% for ${(0, import_ms13.default)(slot.durationMs, { long: true })}`;
  }).join(", ");
  return `${formatEnvironmentVariantSummary(
    fromVariant,
    outcome.rollFromVariantId
  )} -> ${formatEnvironmentVariantSummary(
    toVariant,
    outcome.rollToVariantId
  )}; ${stages}; then 100%; Fallback: ${formatEnvironmentVariantSummary(
    defaultVariant,
    outcome.defaultVariantId
  )}`;
}
function formatVariantListSummary(variant) {
  const value = formatVariantValue(variant.value);
  if (!variant.label) {
    return value;
  }
  return `${value}: ${import_chalk99.default.gray(variant.label)}`;
}
function formatCondition2(condition, settings) {
  let lhs;
  if (condition.lhs.type === "segment") {
    lhs = "segment";
  } else {
    lhs = `${condition.lhs.kind}.${condition.lhs.attribute}`;
  }
  const cmp = import_chalk99.default.dim(formatComparison(condition));
  if (condition.rhs === void 0 || condition.rhs === null) {
    return { text: `${lhs} ${cmp}` };
  }
  if (typeof condition.rhs === "object") {
    if ((condition.rhs.type === "list" || condition.rhs.type === "list/inline") && Array.isArray(condition.rhs.items)) {
      const items = condition.rhs.items.map((item) => {
        const itemValue = typeof item === "object" && item !== null && "value" in item ? String(item.value) : String(item);
        if (condition.lhs.type === "entity") {
          const label = resolveTargetingLabel(
            settings,
            condition.lhs.kind,
            condition.lhs.attribute,
            itemValue
          );
          return label ? `${itemValue} ${import_chalk99.default.gray(label)}` : itemValue;
        }
        return itemValue;
      });
      return { text: `${lhs} ${cmp}`, listItems: items };
    }
    return { text: `${lhs} ${cmp} ${JSON.stringify(condition.rhs)}` };
  }
  let rhs;
  if (condition.lhs.type === "entity") {
    const label = resolveTargetingLabel(
      settings,
      condition.lhs.kind,
      condition.lhs.attribute,
      String(condition.rhs)
    );
    rhs = label ? `${condition.rhs} ${import_chalk99.default.gray(label)}` : String(condition.rhs);
  } else {
    rhs = String(condition.rhs);
  }
  return { text: `${lhs} ${cmp} ${rhs}` };
}
function formatComparison(condition) {
  const operators = {
    eq: "is",
    oneOf: "is in",
    gt: "is greater than",
    gte: "is greater than or equal to",
    lt: "is less than",
    lte: "is less than or equal to",
    ex: "has any value",
    "!ex": "has no value",
    startsWith: "starts with",
    endsWith: "ends with",
    containsAnyOf: "contains any of",
    containsAllOf: "contains all of",
    containsNoneOf: "contains none of"
  };
  const label = operators[condition.cmp] || condition.cmp;
  if (condition.cmpOptions?.ignoreCase) {
    return `${label} (case-insensitive)`;
  }
  return label;
}

// src/commands/flags/inspect.ts
async function inspect3(client, argv) {
  const telemetryClient = new FlagsInspectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(inspectSubcommand3.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args } = parsedArgs;
  const [flagArg] = args;
  if (!flagArg) {
    output_manager_default.error(
      `Missing required argument: flag. Usage: ${getCommandName("flags inspect <flag>")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  const projectSlugLink = formatProject(org.slug, project.name);
  try {
    const [flag, settings] = await Promise.all([
      getFlag(client, project.id, flagArg),
      getFlagSettings(client, project.id)
    ]);
    printFlagDetails({
      flag,
      settings,
      projectSlugLink,
      orgSlug: org.slug,
      projectName: project.name
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/add.ts
var import_chalk100 = __toESM(require_source(), 1);
import { randomBytes as randomBytes2 } from "crypto";

// src/util/flags/create-flag.ts
async function createFlag(client, projectId, request2) {
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/flags`;
  output_manager_default.debug(
    `Creating feature flag ${request2.slug} for project ${projectId}`
  );
  output_manager_default.debug(`API endpoint: PUT ${url}`);
  output_manager_default.debug(`Request body: ${JSON.stringify(request2, null, 2)}`);
  try {
    const response = await client.fetch(url, {
      method: "PUT",
      body: request2
    });
    output_manager_default.debug(`Response: ${JSON.stringify(response, null, 2)}`);
    return response;
  } catch (err) {
    output_manager_default.debug(`API error occurred`);
    if (err instanceof Error) {
      output_manager_default.debug(`Error message: ${err.message}`);
      output_manager_default.debug(`Error name: ${err.name}`);
      output_manager_default.debug(`Error stack: ${err.stack}`);
      const errWithBody = err;
      if (errWithBody.status) {
        output_manager_default.debug(`Error status: ${errWithBody.status}`);
      }
      if (errWithBody.body) {
        output_manager_default.debug(
          `Error body: ${JSON.stringify(errWithBody.body, null, 2)}`
        );
      }
    }
    throw err;
  }
}

// src/util/telemetry/commands/flags/add.ts
var FlagsCreateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentSlug(slug) {
    if (slug) {
      this.trackCliArgument({
        arg: "slug",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionKind(kind) {
    if (kind) {
      this.trackCliOption({
        option: "kind",
        value: kind
      });
    }
  }
  trackCliOptionDescription(description) {
    if (description) {
      this.trackCliOption({
        option: "description",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/add.ts
function variantId(size = 21) {
  const alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
  const bytes5 = randomBytes2(size);
  let id = "";
  for (let i = 0; i < size; i++) {
    id += alphabet[bytes5[i] % alphabet.length];
  }
  return id;
}
async function create4(client, argv) {
  const telemetryClient = new FlagsCreateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(createSubcommand4.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [slug] = args;
  if (!slug) {
    output_manager_default.error("Please provide a slug for the feature flag");
    output_manager_default.log(`Example: ${getCommandName("flags create my-feature")}`);
    return 1;
  }
  const kind = flags["--kind"] || "boolean";
  const description = flags["--description"];
  const variantInputs = flags["--variant"] || [];
  telemetryClient.trackCliArgumentSlug(slug);
  telemetryClient.trackCliOptionKind(kind);
  telemetryClient.trackCliOptionDescription(description);
  if (kind !== "boolean" && kind !== "string" && kind !== "number" && kind !== "json") {
    output_manager_default.error(
      `Invalid kind: ${kind}. Must be one of: boolean, string, number, json`
    );
    return 1;
  }
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  const projectSlugLink = formatProject(link.org.slug, project.name);
  let variants;
  try {
    variants = await getVariants(client, kind, variantInputs);
  } catch (err) {
    output_manager_default.error(err.message);
    return 1;
  }
  const request2 = {
    slug,
    kind,
    description: description || "",
    variants,
    environments: {
      production: createEnvironmentConfig(
        variants[0].id,
        variants[0].id,
        false
      ),
      preview: createEnvironmentConfig(variants[0].id, variants[0].id, false),
      development: createEnvironmentConfig(
        kind === "boolean" ? variants[1].id : variants[0].id,
        kind === "boolean" ? variants[1].id : variants[0].id,
        false
      )
    }
  };
  try {
    output_manager_default.spinner("Creating feature flag...");
    const flag = await createFlag(client, project.id, request2);
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Feature flag ${import_chalk100.default.bold(flag.slug)} created successfully`
    );
    printFlagDetails({
      flag,
      projectSlugLink,
      orgSlug: link.org.slug,
      projectName: project.name,
      showTimestamps: false
    });
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
async function getVariants(client, kind, variantInputs) {
  if (kind === "boolean") {
    if (variantInputs.length > 0) {
      throw new Error(
        "Boolean flags always use true/false variants. Omit --variant when creating a boolean flag."
      );
    }
    return getDefaultVariants(kind);
  }
  if (variantInputs.length > 0) {
    return variantInputs.map(
      (input, index) => parseVariantInput(input, kind, index)
    );
  }
  if (!client.stdin.isTTY) {
    throw new Error(
      "Missing required flag --variant. Use --variant <value>[=label] (repeat as needed), or run interactively in a terminal."
    );
  }
  return collectVariantsInteractively(client, kind);
}
async function collectVariantsInteractively(client, kind) {
  const variants = [];
  let addAnother = true;
  while (addAnother || variants.length === 0) {
    const variantNumber = variants.length + 1;
    const valueInput = await client.input.text({
      message: kind === "json" ? `Enter JSON value for variant ${variantNumber}:` : `Enter value for variant ${variantNumber}:`,
      validate: (value) => {
        const result = validateVariantValue(value, kind);
        return result === null ? true : result;
      }
    });
    const label = await client.input.text({
      message: `Enter an optional label for variant ${variantNumber}:`
    });
    variants.push(
      parseVariantInput(
        formatVariantInput(valueInput, label),
        kind,
        variants.length
      )
    );
    addAnother = await client.input.confirm("Add another variant?", false);
  }
  return variants;
}
function createEnvironmentConfig(pausedVariantId, fallthroughVariantId, active) {
  return {
    revision: 0,
    active,
    pausedOutcome: {
      type: "variant",
      variantId: pausedVariantId
    },
    fallthrough: {
      type: "variant",
      variantId: fallthroughVariantId
    },
    rules: [],
    reuse: {
      active: false,
      environment: ""
    }
  };
}
function formatVariantInput(valueInput, label) {
  const trimmedLabel = label.trim();
  return trimmedLabel ? `${valueInput}=${trimmedLabel}` : valueInput;
}
function parseVariantInput(input, kind, index) {
  const { rawValue, rawLabel } = splitVariantInput(input, kind);
  const validationError = validateVariantValue(rawValue, kind);
  if (validationError) {
    throw new Error(`Invalid variant "${input}": ${validationError}`);
  }
  return {
    id: variantId(),
    value: parseVariantValue2(rawValue, kind),
    label: getVariantLabel(rawLabel, kind, index),
    description: ""
  };
}
function splitVariantInput(input, kind) {
  if (kind !== "json") {
    const separatorIndex = input.indexOf("=");
    return {
      rawValue: separatorIndex === -1 ? input.trim() : input.slice(0, separatorIndex).trim(),
      rawLabel: separatorIndex === -1 ? void 0 : input.slice(separatorIndex + 1).trim() || void 0
    };
  }
  const trimmed = input.trim();
  if (isValidJsonVariantValue(trimmed)) {
    return { rawValue: trimmed };
  }
  for (let index = trimmed.length - 1; index >= 0; index--) {
    if (trimmed[index] !== "=") {
      continue;
    }
    const rawValue = trimmed.slice(0, index).trim();
    const rawLabel = trimmed.slice(index + 1).trim();
    if (!rawValue || !isValidJsonVariantValue(rawValue)) {
      continue;
    }
    return {
      rawValue,
      rawLabel: rawLabel || void 0
    };
  }
  return { rawValue: trimmed };
}
function getVariantLabel(rawLabel, kind, index) {
  if (rawLabel) {
    return rawLabel;
  }
  if (kind === "json") {
    return `Variant ${index + 1}`;
  }
  return void 0;
}
function validateVariantValue(value, kind) {
  if (!value.trim()) {
    return "value cannot be empty";
  }
  if (kind === "number") {
    const parsed = Number(value);
    if (!Number.isFinite(parsed)) {
      return "number variants must be valid numeric values";
    }
  }
  if (kind === "json" && !isValidJsonVariantValue(value)) {
    return "JSON variants must be valid JSON";
  }
  return null;
}
function parseVariantValue2(value, kind) {
  if (kind === "number") {
    return Number(value);
  }
  if (kind === "json") {
    return JSON.parse(value);
  }
  return value;
}
function getDefaultVariants(kind) {
  switch (kind) {
    case "boolean":
      return [
        {
          id: variantId(),
          value: false,
          label: "Off",
          description: "not enabled"
        },
        {
          id: variantId(),
          value: true,
          label: "On",
          description: "enabled"
        }
      ];
    case "string":
    case "number":
    case "json":
      throw new Error(`Default variants are not supported for kind: ${kind}`);
  }
}
function isValidJsonVariantValue(value) {
  try {
    JSON.parse(value);
    return true;
  } catch {
    return false;
  }
}

// src/commands/flags/open.ts
var import_open7 = __toESM(require_open(), 1);

// src/util/telemetry/commands/flags/open.ts
var FlagsOpenTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/open.ts
async function openFlag(client, argv) {
  const telemetryClient = new FlagsOpenTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(openSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args } = parsedArgs;
  const [flagArg, extraArg] = args;
  if (extraArg) {
    output_manager_default.error(
      `Too many arguments. Usage: ${getCommandName("flags open [flag]")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  try {
    let url = getFlagsDashboardUrl(org.slug, project.name);
    let label = "feature flags dashboard";
    if (flagArg) {
      output_manager_default.spinner("Fetching flag...");
      const flag = await getFlag(client, project.id, flagArg);
      output_manager_default.stopSpinner();
      url = getFlagDashboardUrl(org.slug, project.name, flag.slug);
      label = `feature flag ${flag.slug}`;
    }
    if (client.stdout.isTTY) {
      output_manager_default.log(`Opening ${label} in your browser...`);
      output_manager_default.log(`Visit this URL if the browser does not open: ${url}`);
      void (0, import_open7.default)(url).catch(() => void 0);
    } else {
      client.stdout.write(`${url}
`);
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/update.ts
var import_chalk101 = __toESM(require_source(), 1);
var import_fast_deep_equal2 = __toESM(require_fast_deep_equal(), 1);

// src/util/flags/update-flag.ts
async function updateFlag(client, projectId, flagIdOrSlug, request2) {
  output_manager_default.debug(
    `Updating feature flag ${flagIdOrSlug} for project ${projectId}`
  );
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/flags/${encodeURIComponent(flagIdOrSlug)}`;
  const response = await client.fetch(url, {
    method: "PATCH",
    body: request2
  });
  return response;
}

// src/util/flags/normalize-optional-input.ts
function normalizeOptionalInput(input) {
  const value = input?.trim();
  return value ? value : void 0;
}
async function resolveOptionalInput(client, input, defaultValue, promptMessage) {
  const value = normalizeOptionalInput(input);
  if (value !== void 0) {
    return value;
  }
  if (!client.stdin.isTTY) {
    return defaultValue;
  }
  const response = await client.input.text({
    message: promptMessage,
    default: defaultValue
  });
  return normalizeOptionalInput(response) || defaultValue;
}

// src/util/telemetry/commands/flags/update.ts
var FlagsUpdateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionValue(value) {
    if (value) {
      this.trackCliOption({
        option: "value",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionLabel(label) {
    if (label) {
      this.trackCliOption({
        option: "label",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/update.ts
var DEFAULT_UPDATE_MESSAGE = "Updated via CLI";
async function update2(client, argv) {
  const telemetryClient = new FlagsUpdateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(updateSubcommand3.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const variantSelector = normalizeOptionalInput(
    flags["--variant"]
  );
  const valueInput = normalizeOptionalInput(
    flags["--value"]
  );
  const label = normalizeOptionalInput(flags["--label"]);
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to update");
    output_manager_default.log(
      `Example: ${getCommandName('flags update my-feature --variant control --value welcome-back --label "Welcome back"')}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionVariant(variantSelector);
  telemetryClient.trackCliOptionValue(valueInput);
  telemetryClient.trackCliOptionLabel(label);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk101.default.bold(flag.slug)} is archived and cannot be updated`
      );
      return 1;
    }
    const variantUpdate = await collectVariantUpdate(client, flag, {
      selector: variantSelector,
      valueInput,
      label
    });
    const { variants, changedVariants } = applyVariantUpdates(flag, [
      variantUpdate
    ]);
    if (changedVariants.length === 0) {
      output_manager_default.warn(`Flag ${import_chalk101.default.bold(flag.slug)} is already up to date`);
      return 0;
    }
    const updateMessage = await resolveOptionalInput(
      client,
      message,
      DEFAULT_UPDATE_MESSAGE,
      "Enter a message for this update:"
    );
    output_manager_default.spinner("Updating flag...");
    await updateFlag(
      client,
      project.id,
      flagArg,
      updateMessage ? {
        variants,
        message: updateMessage
      } : {
        variants
      }
    );
    output_manager_default.stopSpinner();
    output_manager_default.success(`Feature flag ${import_chalk101.default.bold(flag.slug)} has been updated`);
    for (const variant of changedVariants) {
      output_manager_default.log(
        `  ${import_chalk101.default.dim("Variant:")} ${formatVariantForDisplay(variant)}`
      );
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
async function collectVariantUpdate(client, flag, input) {
  const selectedVariant = await resolveSelectedVariant(
    client,
    flag,
    input.selector
  );
  let nextValueInput = input.valueInput;
  let nextLabel = input.label;
  if (flag.kind !== "boolean" && nextValueInput === void 0 && client.stdin.isTTY) {
    nextValueInput = await promptForVariantValueUpdate(
      client,
      flag,
      selectedVariant
    );
  }
  if (nextLabel === void 0 && client.stdin.isTTY) {
    nextLabel = await promptForVariantLabelUpdate(client, selectedVariant);
  }
  if (!client.stdin.isTTY) {
    ensureVariantUpdateInput(nextValueInput, nextLabel);
  }
  return {
    selector: selectedVariant.id,
    valueInput: nextValueInput,
    label: nextLabel
  };
}
function ensureVariantUpdateInput(valueInput, label) {
  if (!valueInput && !label) {
    throw new Error(
      "At least one of --value or --label must be provided. Use --value <VALUE>, --label <LABEL>, or run interactively in a terminal."
    );
  }
}
async function resolveSelectedVariant(client, flag, selector) {
  if (selector) {
    const result = resolveVariant(selector, flag.variants);
    if (result.error || !result.variant) {
      throw new Error(result.error || `Variant "${selector}" not found`);
    }
    return result.variant;
  }
  if (!client.stdin.isTTY) {
    throw new Error(
      "Missing required flag --variant. Use --variant <VARIANT>, or run interactively in a terminal."
    );
  }
  const selectedVariantId = await client.input.select({
    message: "Select a variant to update:",
    choices: flag.variants.map((variant) => ({
      name: `${formatVariantForDisplay(variant)} ${import_chalk101.default.dim(`[id: ${variant.id}]`)}`,
      value: variant.id
    }))
  });
  const selectedVariant = flag.variants.find((v) => v.id === selectedVariantId);
  if (!selectedVariant) {
    throw new Error("No variant selected");
  }
  return selectedVariant;
}
async function promptForVariantValueUpdate(client, flag, selectedVariant) {
  const valueResponse = await client.input.text({
    message: `Enter a new value for ${formatVariantForDisplay(selectedVariant)} (press Enter to keep ${formatVariantValue(selectedVariant.value)}):`,
    validate: (value) => {
      if (!value.trim()) {
        return true;
      }
      const result = validateVariantValue2(value, flag.kind);
      return result === null ? true : result;
    }
  });
  return normalizeOptionalInput(valueResponse);
}
async function promptForVariantLabelUpdate(client, selectedVariant) {
  const currentLabel = selectedVariant.label;
  const labelResponse = await client.input.text({
    message: currentLabel ? `Enter a new label for ${formatVariantForDisplay(selectedVariant)} (press Enter to keep ${JSON.stringify(currentLabel)}):` : `Enter an optional label for ${formatVariantForDisplay(selectedVariant)} (press Enter to skip):`
  });
  return normalizeOptionalInput(labelResponse);
}
function applyVariantUpdates(flag, updates) {
  const variants = flag.variants.map((variant) => ({ ...variant }));
  const changedVariantIds = /* @__PURE__ */ new Set();
  for (const update4 of updates) {
    const result = resolveVariant(update4.selector, variants);
    if (result.error || !result.variant) {
      throw new Error(result.error || `Variant "${update4.selector}" not found`);
    }
    const nextValue = update4.valueInput !== void 0 ? parseUpdatedVariantValue(update4.valueInput, flag.kind, result.variant) : result.variant.value;
    const nextLabel = update4.label ?? result.variant.label;
    const variantIndex = variants.findIndex((v) => v.id === result.variant.id);
    const existingVariant = variants[variantIndex];
    const hasChanged = !(0, import_fast_deep_equal2.default)(existingVariant.value, nextValue) || existingVariant.label !== nextLabel;
    variants[variantIndex] = {
      ...existingVariant,
      value: nextValue,
      label: nextLabel
    };
    if (hasChanged) {
      changedVariantIds.add(existingVariant.id);
    }
  }
  return {
    variants,
    changedVariants: variants.filter(
      (variant) => changedVariantIds.has(variant.id)
    )
  };
}
function parseUpdatedVariantValue(valueInput, kind, existingVariant) {
  const validationError = validateVariantValue2(valueInput, kind);
  if (validationError) {
    throw new Error(validationError);
  }
  if (kind === "boolean") {
    const nextValue = valueInput.toLowerCase() === "true";
    if (nextValue !== existingVariant.value) {
      throw new Error(
        "Boolean variant values cannot be changed. You can update the label only."
      );
    }
    return nextValue;
  }
  if (kind === "number") {
    return Number(valueInput);
  }
  if (kind === "json") {
    return JSON.parse(valueInput);
  }
  return valueInput;
}
function validateVariantValue2(value, kind) {
  if (!value.trim()) {
    return "Variant value cannot be empty";
  }
  if (kind === "boolean") {
    const loweredValue = value.toLowerCase();
    if (loweredValue !== "true" && loweredValue !== "false") {
      return "Boolean variant values must be true or false";
    }
  }
  if (kind === "number") {
    const parsed = Number(value);
    if (!Number.isFinite(parsed)) {
      return "Number variants must be valid numeric values";
    }
  }
  if (kind === "json") {
    try {
      JSON.parse(value);
    } catch {
      return "JSON variant values must be valid JSON";
    }
  }
  return null;
}

// src/commands/flags/set.ts
var import_chalk103 = __toESM(require_source(), 1);

// src/util/flags/environment-variant.ts
var import_chalk102 = __toESM(require_source(), 1);

// src/util/flags/can-prompt.ts
function canPrompt(client) {
  return Boolean(client.stdin.isTTY) && !client.nonInteractive;
}

// src/util/flags/environment-variant.ts
async function resolveFlagEnvironment(client, flag, environment, promptMessage, options = {}) {
  let nextEnvironment = environment;
  if (!nextEnvironment) {
    if (!canPrompt(client)) {
      throw new Error(
        "Missing required flag --environment. Use --environment <ENV>, or run interactively in a terminal."
      );
    }
    const availableEnvironments = STANDARD_ENVIRONMENTS.filter(
      (env) => Object.prototype.hasOwnProperty.call(flag.environments, env)
    );
    if (availableEnvironments.length === 0) {
      throw new Error("No valid environments found for this flag");
    }
    if (options.showEnvironmentDetails) {
      printFlagEnvironmentDetails(flag, void 0, availableEnvironments);
    }
    nextEnvironment = await client.input.select({
      message: promptMessage,
      choices: availableEnvironments.map((env) => {
        return {
          name: options.decorateChoices === false ? env : formatEnvironmentChoiceLabel(env, flag.environments[env]),
          value: env
        };
      })
    });
  }
  if (!STANDARD_ENVIRONMENTS.includes(nextEnvironment)) {
    throw new Error(
      `Invalid environment: ${nextEnvironment}. Must be one of: ${STANDARD_ENVIRONMENTS.join(", ")}`
    );
  }
  if (!flag.environments[nextEnvironment]) {
    throw new Error(`Environment ${nextEnvironment} not found for this flag`);
  }
  return nextEnvironment;
}
function formatEnvironmentChoiceLabel(envName, envConfig) {
  const status3 = envConfig?.active ? import_chalk102.default.green("active") : import_chalk102.default.yellow("paused");
  return `${envName} (${status3})`;
}
function isOverridingEnvironmentToVariant(envConfig, variantId2) {
  return !envConfig.active && envConfig.pausedOutcome?.variantId === variantId2 && envConfig.fallthrough.type === "variant" && envConfig.fallthrough.variantId === variantId2;
}
function isPausingEnvironmentToVariant(envConfig, variantId2) {
  return !envConfig.active && envConfig.pausedOutcome?.variantId === variantId2;
}
function buildVariantOverrideEnvironmentConfig(envConfig, variantId2) {
  return {
    ...envConfig,
    active: false,
    pausedOutcome: {
      type: "variant",
      variantId: variantId2
    },
    fallthrough: {
      type: "variant",
      variantId: variantId2
    }
  };
}
function buildPausedEnvironmentConfig(envConfig, variantId2) {
  return {
    ...envConfig,
    active: false,
    pausedOutcome: {
      type: "variant",
      variantId: variantId2
    }
  };
}
function buildOutcomeEnvConfig(envConfig, options) {
  const { reuse, ...restConfig } = envConfig;
  const nextConfig = {
    ...restConfig,
    active: true,
    pausedOutcome: envConfig.pausedOutcome ?? {
      type: "variant",
      variantId: options.defaultVariantId
    },
    fallthrough: options.outcome
  };
  if (reuse) {
    nextConfig.reuse = {
      ...reuse,
      active: false
    };
  }
  return nextConfig;
}
function getBooleanVariant(flag, value) {
  const variant = flag.variants.find((candidate) => candidate.value === value);
  if (!variant) {
    throw new Error(
      `Flag ${import_chalk102.default.bold(flag.slug)} is missing the standard boolean variants`
    );
  }
  return variant;
}
async function resolveFlagUpdateMessage(client, message, defaultMessage) {
  if (message !== void 0) {
    return message;
  }
  if (!canPrompt(client)) {
    return defaultMessage;
  }
  const response = await client.input.text({
    message: "Enter a message for this update:",
    default: defaultMessage
  });
  return normalizeOptionalInput(response) || defaultMessage;
}

// src/util/telemetry/commands/flags/set.ts
var FlagsSetTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: STANDARD_ENVIRONMENTS.includes(
          environment
        ) ? environment : this.redactedValue
      });
    }
  }
  trackCliOptionVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/set.ts
async function set2(client, argv) {
  const telemetryClient = new FlagsSetTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(setSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const environment = flags["--environment"];
  const variantSelector = normalizeOptionalInput(
    flags["--variant"]
  );
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to set");
    output_manager_default.log(
      `Example: ${getCommandName("flags set my-feature --environment production --variant true")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionVariant(variantSelector);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk103.default.bold(flag.slug)} is archived and cannot be set`
      );
      return 1;
    }
    const selectedEnvironment = await resolveFlagEnvironment(
      client,
      flag,
      environment,
      "Select an environment to set the variant in:",
      {
        showEnvironmentDetails: true,
        decorateChoices: false
      }
    );
    const selectedVariant = await resolveVariantToSet(
      client,
      flag,
      variantSelector
    );
    const envConfig = flag.environments[selectedEnvironment];
    if (isOverridingEnvironmentToVariant(envConfig, selectedVariant.id)) {
      output_manager_default.warn(
        `Flag ${import_chalk103.default.bold(flag.slug)} is already serving ${formatVariantForDisplay(selectedVariant)} in ${selectedEnvironment}`
      );
      return 0;
    }
    const presentation = getSetPresentation(flag, selectedVariant);
    const updateMessage = await resolveFlagUpdateMessage(
      client,
      message,
      presentation.defaultMessage(selectedEnvironment)
    );
    output_manager_default.spinner(presentation.spinner(selectedEnvironment));
    await updateFlag(client, project.id, flagArg, {
      environments: {
        [selectedEnvironment]: buildVariantOverrideEnvironmentConfig(
          envConfig,
          selectedVariant.id
        )
      },
      message: updateMessage
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(presentation.success(flag.slug, selectedEnvironment));
    output_manager_default.log(
      `  ${import_chalk103.default.dim("Serving variant:")} ${formatVariantForDisplay(selectedVariant)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
async function resolveVariantToSet(client, flag, selector) {
  if (selector) {
    const result = resolveVariant(selector, flag.variants);
    if (result.error || !result.variant) {
      throw new Error(result.error || `Variant "${selector}" not found`);
    }
    return result.variant;
  }
  if (!client.stdin.isTTY) {
    throw new Error(
      "Missing required flag --variant. Use --variant <VARIANT>, or run interactively in a terminal."
    );
  }
  const selectedVariantId = await client.input.select({
    message: "Select a variant to serve:",
    choices: flag.variants.map((variant) => ({
      name: formatVariantForDisplay(variant),
      value: variant.id
    }))
  });
  const selectedVariant = flag.variants.find(
    (variant) => variant.id === selectedVariantId
  );
  if (!selectedVariant) {
    throw new Error("No variant selected");
  }
  return selectedVariant;
}
function getSetPresentation(flag, variant) {
  if (flag.kind === "boolean" && variant.value === true) {
    return {
      spinner: (environment) => `Enabling flag in ${environment}...`,
      success: (slug, environment) => `Feature flag ${import_chalk103.default.bold(slug)} has been enabled in ${import_chalk103.default.bold(environment)}`,
      defaultMessage: (environment) => `Enabled for ${environment} via CLI`
    };
  }
  if (flag.kind === "boolean" && variant.value === false) {
    return {
      spinner: (environment) => `Disabling flag in ${environment}...`,
      success: (slug, environment) => `Feature flag ${import_chalk103.default.bold(slug)} has been disabled in ${import_chalk103.default.bold(environment)}`,
      defaultMessage: (environment) => `Disabled for ${environment} via CLI`
    };
  }
  return {
    spinner: (environment) => `Setting variant in ${environment}...`,
    success: (slug, environment) => `Feature flag ${import_chalk103.default.bold(slug)} has been set in ${import_chalk103.default.bold(environment)}`,
    defaultMessage: (environment) => `Set variant for ${environment} via CLI`
  };
}

// src/commands/flags/split.ts
var import_chalk106 = __toESM(require_source(), 1);
var import_fast_deep_equal3 = __toESM(require_fast_deep_equal(), 1);

// src/util/flags/split.ts
var import_chalk105 = __toESM(require_source(), 1);

// src/util/flags/bucketing-base.ts
var import_chalk104 = __toESM(require_source(), 1);
function resolveFlagBucketingBase(settings, selector) {
  const separatorIndex = selector.indexOf(".");
  if (separatorIndex <= 0 || separatorIndex === selector.length - 1) {
    throw new Error(
      "Invalid value for --by. Use the format <entity.attribute>, for example --by user.userId."
    );
  }
  const kind = selector.slice(0, separatorIndex);
  const attribute = selector.slice(separatorIndex + 1);
  const entity = settings.entities.find((candidate) => candidate.kind === kind);
  if (!entity) {
    const availableKinds = settings.entities.map((candidate) => candidate.kind);
    throw new Error(
      `Unknown entity ${import_chalk104.default.bold(kind)}. Available entities: ${availableKinds.join(", ")}`
    );
  }
  const matchingAttribute = entity.attributes.find(
    (candidate) => candidate.key === attribute
  );
  if (!matchingAttribute) {
    const availableAttributes = entity.attributes.map(
      (candidate) => candidate.key
    );
    throw new Error(
      `Unknown attribute ${import_chalk104.default.bold(selector)}. Available attributes for ${kind}: ${availableAttributes.join(", ")}`
    );
  }
  return {
    type: "entity",
    kind,
    attribute
  };
}
function formatFlagBucketingBaseSelector(base) {
  if (!base) {
    return void 0;
  }
  return `${base.kind}.${base.attribute}`;
}

// src/util/flags/split.ts
function resolveFlagSplit(flag, settings, options) {
  const currentSplit = options.currentOutcome?.type === "split" ? options.currentOutcome : void 0;
  const baseSelector = options.baseSelector || formatFlagBucketingBaseSelector(currentSplit?.base);
  if (!baseSelector) {
    throw new Error(
      "Missing required flag --by. Use --by <entity.attribute> to choose how users are bucketed."
    );
  }
  const base = resolveFlagBucketingBase(settings, baseSelector);
  const weights = resolveSplitWeights(flag, options.weightInputs, currentSplit);
  const defaultVariant = resolveDefaultVariant(
    flag,
    options.defaultVariantSelector,
    currentSplit
  );
  return {
    outcome: {
      type: "split",
      base,
      weights,
      defaultVariantId: defaultVariant.id
    },
    defaultVariant,
    baseLabel: `${base.kind}.${base.attribute}`,
    summary: formatSplitSummary(flag.variants, weights)
  };
}
function resolveSplitWeights(flag, weightInputs, currentSplit) {
  if (weightInputs.length === 0 && currentSplit) {
    return validateAndNormalizeWeights(flag, currentSplit.weights);
  }
  if (weightInputs.length === 0) {
    throw new Error(
      "At least one --weight is required. Use --weight <VARIANT=WEIGHT> for each variant, or run interactively in a terminal."
    );
  }
  const weights = {};
  const seen = /* @__PURE__ */ new Set();
  for (const input of weightInputs) {
    const { variantSelector, weight } = parseWeightInput(input);
    const variant = resolveVariantOrThrow(
      variantSelector,
      flag.variants,
      "--weight"
    );
    if (seen.has(variant.id)) {
      throw new Error(
        `Duplicate weight for variant ${import_chalk105.default.bold(formatVariantForDisplay(variant))}.`
      );
    }
    seen.add(variant.id);
    weights[variant.id] = weight;
  }
  const missingVariants = flag.variants.filter(
    (variant) => !seen.has(variant.id)
  );
  if (missingVariants.length > 0) {
    throw new Error(
      `Missing weights for variants: ${missingVariants.map((variant) => formatVariantForDisplay(variant)).join(
        ", "
      )}. Use --weight <VARIANT=WEIGHT> for each variant, including 0 for variants that should receive no traffic.`
    );
  }
  return validateAndNormalizeWeights(flag, weights);
}
function parseWeightInput(input) {
  const separatorIndex = input.lastIndexOf("=");
  if (separatorIndex <= 0 || separatorIndex === input.length - 1) {
    throw new Error(
      `Invalid weight ${import_chalk105.default.bold(input)}. Use --weight <VARIANT=WEIGHT>, for example --weight on=5.`
    );
  }
  const variantSelector = input.slice(0, separatorIndex).trim();
  const rawWeight = input.slice(separatorIndex + 1).trim();
  if (!variantSelector) {
    throw new Error(
      `Invalid weight ${import_chalk105.default.bold(input)}. Variant cannot be empty.`
    );
  }
  if (!rawWeight) {
    throw new Error(
      `Invalid weight ${import_chalk105.default.bold(input)}. Weight cannot be empty.`
    );
  }
  const weight = Number(rawWeight);
  if (!Number.isFinite(weight) || weight < 0) {
    throw new Error(
      `Invalid weight "${rawWeight}". Use a number greater than or equal to 0.`
    );
  }
  return { variantSelector, weight };
}
function validateAndNormalizeWeights(flag, weights) {
  const normalizedWeights = Object.fromEntries(
    flag.variants.map((variant) => [variant.id, weights[variant.id] ?? 0])
  );
  const totalWeight = Object.values(normalizedWeights).reduce(
    (sum, weight) => sum + weight,
    0
  );
  if (totalWeight <= 0) {
    throw new Error("At least one weight must be greater than 0.");
  }
  return normalizedWeights;
}
function resolveDefaultVariant(flag, selector, currentSplit) {
  if (selector) {
    return resolveVariantOrThrow(selector, flag.variants, "--default-variant");
  }
  if (currentSplit) {
    return resolveVariantByIdOrThrow(
      currentSplit.defaultVariantId,
      flag.variants,
      "--default-variant"
    );
  }
  if (flag.kind === "boolean") {
    return getBooleanVariant(flag, false);
  }
  throw new Error(
    "Missing required flag --default-variant. Use --default-variant <VARIANT> for non-boolean splits."
  );
}
function formatSplitSummary(variants, weights) {
  const total = Object.values(weights).reduce((sum, weight) => sum + weight, 0);
  return variants.map((variant) => {
    const weight = weights[variant.id] ?? 0;
    const percentage = total > 0 ? weight / total * 100 : 0;
    return `${formatPlainVariant(variant)}: ${formatPercentage(percentage)}`;
  }).join(", ");
}
function formatPlainVariant(variant) {
  const value = formatVariantValue(variant.value);
  return variant.label ? `${value} ${variant.label}` : value;
}
function formatPercentage(percentage) {
  return Number.isInteger(percentage) ? `${percentage}%` : `${Number(percentage.toFixed(2))}%`;
}

// src/util/telemetry/commands/flags/split.ts
var FlagsSplitTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: STANDARD_ENVIRONMENTS.includes(
          environment
        ) ? environment : this.redactedValue
      });
    }
  }
  trackCliOptionBy(base) {
    if (base) {
      this.trackCliOption({
        option: "by",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionWeight(weights) {
    if (weights?.length) {
      this.trackCliOption({
        option: "weight",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionDefaultVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "default-variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/split.ts
async function split(client, argv) {
  const telemetryClient = new FlagsSplitTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(splitSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const environment = flags["--environment"];
  const baseSelector = normalizeOptionalInput(
    flags["--by"]
  );
  const defaultVariantSelector = normalizeOptionalInput(
    flags["--default-variant"]
  );
  const weightInputs = flags["--weight"] || [];
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to split");
    output_manager_default.log(
      `Example: ${getCommandName("flags split my-feature --environment production --by user.userId --weight off=95 --weight on=5")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionBy(baseSelector);
  telemetryClient.trackCliOptionWeight(
    weightInputs.length > 0 ? [weightInputs[0]] : void 0
  );
  telemetryClient.trackCliOptionDefaultVariant(defaultVariantSelector);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const [flag, settings] = await Promise.all([
      getFlag(client, project.id, flagArg),
      getFlagSettings(client, project.id)
    ]);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk106.default.bold(flag.slug)} is archived and cannot be split`
      );
      return 1;
    }
    const selectedEnvironment = await resolveFlagEnvironment(
      client,
      flag,
      environment,
      "Select an environment to split traffic in:",
      {
        showEnvironmentDetails: true,
        decorateChoices: false
      }
    );
    const envConfig = flag.environments[selectedEnvironment];
    const currentSplit = envConfig.fallthrough.type === "split" ? envConfig.fallthrough : void 0;
    const shouldPromptCurrentSplit = canPrompt(client) && currentSplit !== void 0 && !baseSelector && weightInputs.length === 0 && !defaultVariantSelector && !message;
    const resolvedBaseSelector = await resolveBaseSelector(
      client,
      settings,
      baseSelector,
      currentSplit,
      shouldPromptCurrentSplit
    );
    const resolvedWeightInputs = await resolveWeightInputs(
      client,
      flag,
      weightInputs,
      currentSplit,
      shouldPromptCurrentSplit
    );
    const resolvedDefaultVariantSelector = await resolveDefaultVariantSelector(
      client,
      flag,
      defaultVariantSelector,
      currentSplit,
      shouldPromptCurrentSplit
    );
    const splitConfig = resolveFlagSplit(flag, settings, {
      baseSelector: resolvedBaseSelector,
      weightInputs: resolvedWeightInputs,
      defaultVariantSelector: resolvedDefaultVariantSelector,
      currentOutcome: envConfig.fallthrough
    });
    const nextEnvironmentConfig = buildOutcomeEnvConfig(envConfig, {
      outcome: splitConfig.outcome,
      defaultVariantId: splitConfig.defaultVariant.id
    });
    if (!message && envConfig.active && (0, import_fast_deep_equal3.default)(envConfig.fallthrough, splitConfig.outcome) && (0, import_fast_deep_equal3.default)(envConfig, nextEnvironmentConfig)) {
      output_manager_default.warn(
        `Flag ${import_chalk106.default.bold(flag.slug)} is already configured with this split in ${selectedEnvironment}`
      );
      return 0;
    }
    const updateMessage = await resolveFlagUpdateMessage(
      client,
      message,
      getDefaultSplitMessage(selectedEnvironment, splitConfig)
    );
    output_manager_default.spinner(`Updating split in ${selectedEnvironment}...`);
    await updateFlag(client, project.id, flagArg, {
      environments: {
        [selectedEnvironment]: nextEnvironmentConfig
      },
      message: updateMessage
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Feature flag ${import_chalk106.default.bold(flag.slug)} split has been updated in ${import_chalk106.default.bold(selectedEnvironment)}`
    );
    output_manager_default.log(`  ${import_chalk106.default.dim("Based on:")} ${splitConfig.baseLabel}`);
    output_manager_default.log(
      `  ${import_chalk106.default.dim("Fallback:")} ${formatVariantForDisplay(splitConfig.defaultVariant)}`
    );
    output_manager_default.log(`  ${import_chalk106.default.dim("Weights:")} ${splitConfig.summary}`);
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
function getDefaultSplitMessage(environment, splitConfig) {
  return `Configure split for ${environment}: ${splitConfig.summary}`;
}
async function resolveBaseSelector(client, settings, baseSelector, currentSplit, shouldPromptCurrentSplit) {
  if (baseSelector || currentSplit && !shouldPromptCurrentSplit) {
    return baseSelector;
  }
  if (!canPrompt(client)) {
    return void 0;
  }
  const choices = settings.entities.flatMap(
    (entity) => entity.attributes.map((attribute) => ({
      name: `${entity.label} \u203A ${attribute.key}`,
      value: `${entity.kind}.${attribute.key}`
    }))
  );
  const currentBaseSelector = formatFlagBucketingBaseSelector(
    currentSplit?.base
  );
  const defaultChoice = choices.find(
    (choice) => choice.value === currentBaseSelector
  )?.value;
  if (choices.length === 0) {
    throw new Error("No entities are configured for weighted splits.");
  }
  return client.input.select({
    message: "Select an attribute to split by:",
    choices,
    default: defaultChoice
  });
}
async function resolveWeightInputs(client, flag, weightInputs, currentSplit, shouldPromptCurrentSplit) {
  if (weightInputs.length > 0 || currentSplit && !shouldPromptCurrentSplit) {
    return weightInputs;
  }
  if (!canPrompt(client)) {
    return weightInputs;
  }
  output_manager_default.log(
    "Enter weights for each variant. Weights are ratios; use 0 for variants that should receive no traffic."
  );
  const promptedWeights = [];
  for (const variant of flag.variants) {
    const defaultWeight = currentSplit?.weights[variant.id] !== void 0 ? String(currentSplit.weights[variant.id]) : currentSplit ? "0" : "1";
    const weight = await client.input.text({
      message: `Weight for ${formatVariantForDisplay(variant)}:`,
      default: defaultWeight,
      validate: (value) => {
        const trimmed = value.trim();
        if (!trimmed) {
          return "Enter a weight";
        }
        const parsed = Number(trimmed);
        if (!Number.isFinite(parsed) || parsed < 0) {
          return "Enter a number greater than or equal to 0";
        }
        return true;
      }
    });
    promptedWeights.push(`${variant.id}=${weight.trim()}`);
  }
  return promptedWeights;
}
async function resolveDefaultVariantSelector(client, flag, defaultVariantSelector, currentSplit, shouldPromptCurrentSplit) {
  if (defaultVariantSelector || currentSplit && !shouldPromptCurrentSplit || flag.kind === "boolean" && !canPrompt(client)) {
    return defaultVariantSelector;
  }
  if (!canPrompt(client)) {
    return void 0;
  }
  return client.input.select({
    message: "Select a fallback variant:",
    choices: flag.variants.map((variant) => ({
      name: formatVariantForDisplay(variant),
      value: variant.id
    })),
    default: flag.variants.find(
      (variant) => variant.id === currentSplit?.defaultVariantId
    )?.id || getBooleanFallbackVariantId(flag)
  });
}
function getBooleanFallbackVariantId(flag) {
  if (flag.kind !== "boolean") {
    return void 0;
  }
  return flag.variants.find((variant) => variant.value === false)?.id;
}

// src/commands/flags/rollout.ts
var import_chalk108 = __toESM(require_source(), 1);
var import_fast_deep_equal4 = __toESM(require_fast_deep_equal(), 1);

// src/util/flags/rollout.ts
var import_chalk107 = __toESM(require_source(), 1);
var import_ms14 = __toESM(require_ms(), 1);
function resolveFlagRollout(flag, settings, options) {
  const currentRollout = options.currentOutcome?.type === "rollout" ? options.currentOutcome : void 0;
  const baseSelector = options.baseSelector || formatFlagBucketingBaseSelector(currentRollout?.base);
  if (!baseSelector) {
    throw new Error(
      "Missing required flag --by. Use --by <entity.attribute> to choose how users are bucketed."
    );
  }
  const base = resolveFlagBucketingBase(settings, baseSelector);
  const rollFromVariant = resolveRollFromVariant(
    flag,
    options.rollFromVariantSelector,
    currentRollout
  );
  const rollToVariant = resolveRollToVariant(
    flag,
    options.rollToVariantSelector,
    currentRollout
  );
  if (rollFromVariant.id === rollToVariant.id) {
    throw new Error(
      "`--from-variant` and `--to-variant` must resolve to different variants."
    );
  }
  const defaultVariant = resolveDefaultVariant2(
    flag,
    options.defaultVariantSelector,
    currentRollout,
    rollFromVariant
  );
  const slots = options.stageInputs.length > 0 ? parseStageInputs(options.stageInputs) : currentRollout?.slots;
  if (!slots || slots.length === 0) {
    throw new Error(
      "At least one --stage is required. Use --stage <PERCENTAGE,DURATION>, for example --stage 5,6h."
    );
  }
  const startTimestamp = resolveRolloutStartTimestamp(
    options.start,
    currentRollout?.startTimestamp
  );
  return {
    outcome: {
      type: "rollout",
      base,
      startTimestamp,
      rollFromVariantId: rollFromVariant.id,
      rollToVariantId: rollToVariant.id,
      defaultVariantId: defaultVariant.id,
      slots
    },
    defaultVariant,
    rollFromVariant,
    rollToVariant,
    baseLabel: `${base.kind}.${base.attribute}`,
    summary: formatRolloutStages(slots),
    startLabel: formatStartLabel(options.start, currentRollout?.startTimestamp)
  };
}
function resolveRollFromVariant(flag, selector, currentRollout) {
  if (selector) {
    return resolveVariantOrThrow(selector, flag.variants, "--from-variant");
  }
  if (currentRollout) {
    return resolveVariantByIdOrThrow(
      currentRollout.rollFromVariantId,
      flag.variants,
      "--from-variant"
    );
  }
  return inferBooleanVariant(flag, false, "--from-variant");
}
function resolveRollToVariant(flag, selector, currentRollout) {
  if (selector) {
    return resolveVariantOrThrow(selector, flag.variants, "--to-variant");
  }
  if (currentRollout) {
    return resolveVariantByIdOrThrow(
      currentRollout.rollToVariantId,
      flag.variants,
      "--to-variant"
    );
  }
  return inferBooleanVariant(flag, true, "--to-variant");
}
function resolveDefaultVariant2(flag, selector, currentRollout, rollFromVariant) {
  if (selector) {
    return resolveVariantOrThrow(selector, flag.variants, "--default-variant");
  }
  if (currentRollout) {
    return resolveVariantByIdOrThrow(
      currentRollout.defaultVariantId,
      flag.variants,
      "--default-variant"
    );
  }
  return rollFromVariant;
}
function inferBooleanVariant(flag, value, optionName) {
  if (flag.kind !== "boolean") {
    throw new Error(
      `Missing required flag ${optionName}. Use ${optionName} <VARIANT> for non-boolean rollouts.`
    );
  }
  const variant = flag.variants.find((candidate) => candidate.value === value);
  if (!variant) {
    throw new Error(
      `Flag ${import_chalk107.default.bold(flag.slug)} is missing the standard boolean variants`
    );
  }
  return variant;
}
function parseStageInputs(stageInputs) {
  const slots = stageInputs.map(parseStageInput);
  for (let index = 1; index < slots.length; index++) {
    if (slots[index].promille <= slots[index - 1].promille) {
      throw new Error("Stage percentages must be in ascending order.");
    }
  }
  return slots;
}
function parseStageInput(input) {
  const parts = input.split(",").map((part) => part.trim());
  if (parts.length !== 2) {
    throw new Error(
      `Invalid stage ${import_chalk107.default.bold(input)}. Use --stage <PERCENTAGE,DURATION>, for example --stage 5,6h.`
    );
  }
  const percentage = Number(parts[0]);
  if (!Number.isFinite(percentage) || percentage <= 0 || percentage >= 100 || !Number.isInteger(percentage * 1e3)) {
    throw new Error(
      `Invalid stage percentage "${parts[0]}". Use a number greater than 0 and less than 100 with up to 3 decimal places.`
    );
  }
  const durationMs = parseStageDuration(parts[1]);
  if (durationMs === void 0) {
    throw new Error(
      `Invalid duration "${parts[1]}". Use minutes (e.g. "30m"), hours (e.g. "6h"), or days (e.g. "1d"). Seconds are not supported.`
    );
  }
  return {
    durationMs,
    promille: Math.round(percentage * 1e3)
  };
}
function parseStageDuration(value) {
  if (!value) {
    return void 0;
  }
  if (/^\d+(s|ms)$/i.test(value)) {
    return void 0;
  }
  const durationMs = (0, import_ms14.default)(/^\d+$/.test(value) ? `${value}m` : value);
  if (durationMs === void 0 || durationMs <= 0) {
    return void 0;
  }
  const roundedMinutes = Math.round(durationMs / 6e4);
  if (roundedMinutes <= 0) {
    return void 0;
  }
  return roundedMinutes * 6e4;
}
function resolveRolloutStartTimestamp(start, currentStartTimestamp) {
  if (start === void 0) {
    return currentStartTimestamp ?? Date.now();
  }
  if (start === "now") {
    return Date.now();
  }
  const relativeDurationMs = (0, import_ms14.default)(start);
  if (relativeDurationMs !== void 0) {
    if (relativeDurationMs <= 0) {
      throw new Error(
        `Invalid start time "${start}". Use "now", a future relative duration such as "1h", or an ISO 8601 datetime.`
      );
    }
    return Date.now() + relativeDurationMs;
  }
  const absoluteTime = new Date(start).getTime();
  if (Number.isNaN(absoluteTime)) {
    throw new Error(
      `Invalid start time "${start}". Use "now", a future relative duration such as "1h", or an ISO 8601 datetime.`
    );
  }
  return absoluteTime;
}
function formatRolloutStages(slots) {
  return `${slots.map(
    (slot) => `${formatPromille(slot.promille)} for ${(0, import_ms14.default)(slot.durationMs, { long: true })}`
  ).join(", ")}, then 100% indefinitely`;
}
function formatPromille(promille) {
  const percentage = promille / 1e3;
  return Number.isInteger(percentage) ? `${percentage}%` : `${Number(percentage.toFixed(3))}%`;
}
function formatStartLabel(start, currentStartTimestamp) {
  if (start === void 0) {
    return currentStartTimestamp ? "preserve current start time" : "immediately";
  }
  if (start === "now") {
    return "immediately";
  }
  return `start at ${start}`;
}

// src/util/telemetry/commands/flags/rollout.ts
var FlagsRolloutTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: STANDARD_ENVIRONMENTS.includes(
          environment
        ) ? environment : this.redactedValue
      });
    }
  }
  trackCliOptionFromVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "from-variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionToVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "to-variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionDefaultVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "default-variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionBy(base) {
    if (base) {
      this.trackCliOption({
        option: "by",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionStage(stages) {
    if (stages?.length) {
      this.trackCliOption({
        option: "stage",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionStart(start) {
    if (start) {
      this.trackCliOption({
        option: "start",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/rollout.ts
async function rollout(client, argv) {
  const telemetryClient = new FlagsRolloutTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(rolloutSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const environment = flags["--environment"];
  const rollFromVariantSelector = normalizeOptionalInput(
    flags["--from-variant"]
  );
  const rollToVariantSelector = normalizeOptionalInput(
    flags["--to-variant"]
  );
  const defaultVariantSelector = normalizeOptionalInput(
    flags["--default-variant"]
  );
  const baseSelector = normalizeOptionalInput(
    flags["--by"]
  );
  const stageInputs = (flags["--stage"] || []).map(
    (input) => input.trim()
  );
  const start = normalizeOptionalInput(flags["--start"]);
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to roll out");
    output_manager_default.log(
      `Example: ${getCommandName("flags rollout my-feature --environment production --by user.userId --stage 5,6h --stage 25,1d")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionFromVariant(rollFromVariantSelector);
  telemetryClient.trackCliOptionToVariant(rollToVariantSelector);
  telemetryClient.trackCliOptionDefaultVariant(defaultVariantSelector);
  telemetryClient.trackCliOptionBy(baseSelector);
  telemetryClient.trackCliOptionStage(
    stageInputs.length > 0 ? [stageInputs[0]] : void 0
  );
  telemetryClient.trackCliOptionStart(start);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const [flag, settings] = await Promise.all([
      getFlag(client, project.id, flagArg),
      getFlagSettings(client, project.id)
    ]);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk108.default.bold(flag.slug)} is archived and cannot be rolled out`
      );
      return 1;
    }
    const selectedEnvironment = await resolveFlagEnvironment(
      client,
      flag,
      environment,
      "Select an environment to roll out in:",
      {
        showEnvironmentDetails: true,
        decorateChoices: false
      }
    );
    const rolloutConfig = resolveFlagRollout(flag, settings, {
      stageInputs,
      baseSelector,
      rollFromVariantSelector,
      rollToVariantSelector,
      defaultVariantSelector,
      start,
      currentOutcome: flag.environments[selectedEnvironment].fallthrough
    });
    const nextEnvironmentConfig = buildOutcomeEnvConfig(
      flag.environments[selectedEnvironment],
      {
        outcome: rolloutConfig.outcome,
        defaultVariantId: rolloutConfig.defaultVariant.id
      }
    );
    if (flag.environments[selectedEnvironment].active && (0, import_fast_deep_equal4.default)(
      flag.environments[selectedEnvironment].fallthrough,
      rolloutConfig.outcome
    ) && (0, import_fast_deep_equal4.default)(flag.environments[selectedEnvironment], nextEnvironmentConfig)) {
      output_manager_default.warn(
        `Flag ${import_chalk108.default.bold(flag.slug)} is already configured with this rollout in ${selectedEnvironment}`
      );
      return 0;
    }
    const updateMessage = await resolveFlagUpdateMessage(
      client,
      message,
      getDefaultRolloutMessage(selectedEnvironment, rolloutConfig)
    );
    output_manager_default.spinner(`Updating rollout in ${selectedEnvironment}...`);
    await updateFlag(client, project.id, flagArg, {
      environments: {
        [selectedEnvironment]: nextEnvironmentConfig
      },
      message: updateMessage
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Feature flag ${import_chalk108.default.bold(flag.slug)} rollout has been updated in ${import_chalk108.default.bold(selectedEnvironment)}`
    );
    output_manager_default.log(`  ${import_chalk108.default.dim("Based on:")} ${rolloutConfig.baseLabel}`);
    output_manager_default.log(
      `  ${import_chalk108.default.dim("Roll from:")} ${formatVariantLabel(rolloutConfig.rollFromVariant)}`
    );
    output_manager_default.log(
      `  ${import_chalk108.default.dim("Roll to:")} ${formatVariantLabel(rolloutConfig.rollToVariant)}`
    );
    output_manager_default.log(
      `  ${import_chalk108.default.dim("Fallback:")} ${formatVariantLabel(rolloutConfig.defaultVariant)}`
    );
    output_manager_default.log(`  ${import_chalk108.default.dim("Start:")} ${rolloutConfig.startLabel}`);
    output_manager_default.log(`  ${import_chalk108.default.dim("Stages:")} ${rolloutConfig.summary}`);
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
function getDefaultRolloutMessage(environment, rolloutConfig) {
  return `Configure rollout for ${environment}: ${rolloutConfig.summary}`;
}
function formatVariantLabel(variant) {
  return variant.label || variant.id;
}

// src/commands/flags/rm.ts
var import_chalk109 = __toESM(require_source(), 1);

// src/util/flags/delete-flag.ts
async function deleteFlag(client, projectId, flagIdOrSlug) {
  output_manager_default.debug(
    `Deleting feature flag ${flagIdOrSlug} for project ${projectId}`
  );
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/flags/${encodeURIComponent(flagIdOrSlug)}`;
  await client.fetch(url, {
    method: "DELETE"
  });
}

// src/util/telemetry/commands/flags/rm.ts
var FlagsRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/flags/rm.ts
async function rm7(client, argv) {
  const telemetryClient = new FlagsRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand8.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const skipConfirmation = flags["--yes"];
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to delete");
    output_manager_default.log(`Example: ${getCommandName("flags rm my-feature")}`);
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliFlagYes(skipConfirmation);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state !== "archived") {
      output_manager_default.error(
        `Flag ${import_chalk109.default.bold(flag.slug)} must be archived before it can be deleted. Run ${getCommandName(`flags archive ${flag.slug}`)} first.`
      );
      return 1;
    }
    if (!skipConfirmation) {
      if (!client.stdin.isTTY) {
        output_manager_default.error(
          "Missing required flag --yes. Use --yes to skip the confirmation prompt in non-interactive mode."
        );
        return 1;
      }
      const confirmed = await client.input.confirm(
        `Are you sure you want to delete ${import_chalk109.default.bold(flag.slug)}? This action cannot be undone.`,
        false
      );
      if (!confirmed) {
        output_manager_default.log("Aborted");
        return 0;
      }
    }
    output_manager_default.spinner("Deleting flag...");
    await deleteFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    output_manager_default.success(`Feature flag ${import_chalk109.default.bold(flag.slug)} has been deleted`);
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/archive.ts
var import_chalk110 = __toESM(require_source(), 1);

// src/util/telemetry/commands/flags/archive.ts
var FlagsArchiveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/flags/archive.ts
async function archive(client, argv) {
  const telemetryClient = new FlagsArchiveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(archiveSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  const skipConfirmation = flags["--yes"];
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to archive");
    output_manager_default.log(`Example: ${getCommandName("flags archive my-feature")}`);
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliFlagYes(skipConfirmation);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.warn(`Flag ${import_chalk110.default.bold(flag.slug)} is already archived`);
      return 0;
    }
    if (!skipConfirmation) {
      if (!client.stdin.isTTY) {
        output_manager_default.error(
          "Missing required flag --yes. Use --yes to skip the confirmation prompt in non-interactive mode."
        );
        return 1;
      }
      const confirmed = await client.input.confirm(
        `Are you sure you want to archive ${import_chalk110.default.bold(flag.slug)}?`,
        false
      );
      if (!confirmed) {
        output_manager_default.log("Aborted");
        return 0;
      }
    }
    output_manager_default.spinner("Archiving flag...");
    await updateFlag(client, project.id, flagArg, {
      state: "archived",
      message: "Archived via CLI"
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(`Feature flag ${import_chalk110.default.bold(flag.slug)} has been archived`);
    output_manager_default.log(
      `
To restore this flag, visit the dashboard: ${import_chalk110.default.cyan(getFlagsDashboardUrl(link.org.slug, project.name) + "/archive")}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/disable.ts
var import_chalk112 = __toESM(require_source(), 1);

// src/util/flags/log-non-boolean-guidance.ts
var import_chalk111 = __toESM(require_source(), 1);
function logNonBooleanFlagGuidance(flag, {
  attemptedSubcommand,
  environment,
  isInteractive,
  teamSlug,
  projectName
}) {
  const dashboardUrl = getFlagDashboardUrl(teamSlug, projectName, flag.slug);
  const suggestedEnvironment = environment && environment in flag.environments ? environment : void 0;
  output_manager_default.warn(
    `The ${getCommandName(`flags ${attemptedSubcommand}`)} command only works with boolean flags.`
  );
  output_manager_default.log(
    `Flag ${import_chalk111.default.bold(flag.slug)} is a ${import_chalk111.default.cyan(flag.kind)} flag. Set a specific variant instead:`
  );
  output_manager_default.log(
    `  ${getCommandName(
      getSuggestedSetCommand(flag.slug, suggestedEnvironment, isInteractive)
    )}`
  );
  if (flag.variants.length > 0) {
    output_manager_default.log("Available variants:");
    for (const variant of flag.variants) {
      output_manager_default.log(`  - ${formatVariantForDisplay(variant)}`);
    }
  }
  output_manager_default.log(
    `See full flag details with ${getCommandName(`flags inspect ${flag.slug}`)}`
  );
  output_manager_default.log(`Open in the dashboard: ${import_chalk111.default.cyan(dashboardUrl)}`);
}
function getSuggestedSetCommand(slug, environment, isInteractive) {
  const parts = [`flags set ${slug}`];
  if (environment) {
    parts.push(`--environment ${environment}`);
  } else if (!isInteractive) {
    parts.push("--environment <ENV>");
  }
  if (!isInteractive) {
    parts.push("--variant <VARIANT>");
  }
  return parts.join(" ");
}

// src/util/telemetry/commands/flags/disable.ts
var FlagsDisableTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: STANDARD_ENVIRONMENTS.includes(
          environment
        ) ? environment : this.redactedValue
      });
    }
  }
  trackCliOptionVariant(variant) {
    if (variant) {
      this.trackCliOption({
        option: "variant",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/disable.ts
async function disable3(client, argv) {
  const telemetryClient = new FlagsDisableTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(disableSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  let environment = flags["--environment"];
  const variantId2 = flags["--variant"];
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    if (client.nonInteractive) {
      const envPart = environment ? `--environment ${environment}` : "--environment <production|preview|development>";
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Please provide a flag slug or ID to disable.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags disable <flag> ${envPart}`
              ),
              when: "disable a feature flag by slug or ID"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error("Please provide a flag slug or ID to disable");
    output_manager_default.log(
      `Example: ${getCommandName("flags disable my-feature --environment production")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionVariant(variantId2);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          message: "Your codebase is not linked to a project. Run link first.",
          next: [
            {
              command: buildCommandWithGlobalFlags(client.argv, "link"),
              when: "link the project"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk112.default.bold(flag.slug)} is archived and cannot be disabled`
      );
      return 1;
    }
    if (flag.kind !== "boolean") {
      logNonBooleanFlagGuidance(flag, {
        attemptedSubcommand: "disable",
        environment,
        isInteractive: Boolean(client.stdin.isTTY),
        teamSlug: link.org.slug,
        projectName: project.name
      });
      return 0;
    }
    if (!environment && client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Please provide --environment (production, preview, or development) to disable the flag in.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags disable ${flagArg} --environment <production|preview|development>`
              ),
              when: "disable the flag in a specific environment"
            }
          ]
        },
        1
      );
      return 1;
    }
    environment = await resolveFlagEnvironment(
      client,
      flag,
      environment,
      "Select an environment to disable the flag in:"
    );
    const envConfig = flag.environments[environment];
    let selectedVariant = getBooleanVariant(flag, false);
    if (variantId2) {
      const result = resolveVariant(variantId2, flag.variants);
      if (result.error) {
        output_manager_default.error(result.error);
        return 1;
      }
      selectedVariant = result.variant;
    }
    if (isPausingEnvironmentToVariant(envConfig, selectedVariant.id)) {
      output_manager_default.warn(
        `Flag ${import_chalk112.default.bold(flag.slug)} is already disabled in ${environment}`
      );
      return 0;
    }
    const updateMessage = await resolveFlagUpdateMessage(
      client,
      message,
      getDefaultDisableMessage(environment)
    );
    output_manager_default.spinner(`Disabling flag in ${environment}...`);
    await updateFlag(client, project.id, flagArg, {
      environments: {
        [environment]: buildPausedEnvironmentConfig(
          envConfig,
          selectedVariant.id
        )
      },
      message: updateMessage
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Feature flag ${import_chalk112.default.bold(flag.slug)} has been disabled in ${import_chalk112.default.bold(environment)}`
    );
    output_manager_default.log(
      `  ${import_chalk112.default.dim("Serving variant:")} ${formatVariantForDisplay(selectedVariant)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const message2 = err instanceof Error ? err.message : String(err);
    if (client.nonInteractive && message2.startsWith("Invalid environment:")) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: message2,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags disable ${flagArg} --environment <production|preview|development>`
              ),
              when: "use a valid environment (production, preview, or development)"
            }
          ]
        },
        1
      );
      return 1;
    }
    printError(err);
    return 1;
  }
  return 0;
}
function getDefaultDisableMessage(environment) {
  return `Disabled for ${environment} via CLI`;
}

// src/commands/flags/enable.ts
var import_chalk113 = __toESM(require_source(), 1);

// src/util/telemetry/commands/flags/enable.ts
var FlagsEnableTelemetryClient = class extends TelemetryClient {
  trackCliArgumentFlag(flag) {
    if (flag) {
      this.trackCliArgument({
        arg: "flag",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: STANDARD_ENVIRONMENTS.includes(
          environment
        ) ? environment : this.redactedValue
      });
    }
  }
  trackCliOptionMessage(message) {
    if (message) {
      this.trackCliOption({
        option: "message",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/flags/enable.ts
async function enable3(client, argv) {
  const telemetryClient = new FlagsEnableTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(enableSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const [flagArg] = args;
  let environment = flags["--environment"];
  const message = normalizeOptionalInput(
    flags["--message"]
  );
  if (!flagArg) {
    output_manager_default.error("Please provide a flag slug or ID to enable");
    output_manager_default.log(
      `Example: ${getCommandName("flags enable my-feature --environment production")}`
    );
    return 1;
  }
  telemetryClient.trackCliArgumentFlag(flagArg);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionMessage(message);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    output_manager_default.spinner("Fetching flag...");
    const flag = await getFlag(client, project.id, flagArg);
    output_manager_default.stopSpinner();
    if (flag.state === "archived") {
      output_manager_default.error(
        `Flag ${import_chalk113.default.bold(flag.slug)} is archived and cannot be enabled`
      );
      return 1;
    }
    if (flag.kind !== "boolean") {
      logNonBooleanFlagGuidance(flag, {
        attemptedSubcommand: "enable",
        environment,
        isInteractive: Boolean(client.stdin.isTTY),
        teamSlug: link.org.slug,
        projectName: project.name
      });
      return 0;
    }
    if (!environment && client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Please provide --environment (production, preview, or development) to enable the flag in.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags enable ${flagArg} --environment <production|preview|development>`
              ),
              when: "enable the flag in a specific environment"
            }
          ]
        },
        1
      );
      return 1;
    }
    environment = await resolveFlagEnvironment(
      client,
      flag,
      environment,
      "Select an environment to enable the flag in:"
    );
    const envConfig = flag.environments[environment];
    const onVariant = getBooleanVariant(flag, true);
    if (isPausingEnvironmentToVariant(envConfig, onVariant.id)) {
      output_manager_default.warn(
        `Flag ${import_chalk113.default.bold(flag.slug)} is already enabled in ${environment}`
      );
      return 0;
    }
    const updateMessage = await resolveFlagUpdateMessage(
      client,
      message,
      getDefaultEnableMessage(environment)
    );
    output_manager_default.spinner(`Enabling flag in ${environment}...`);
    await updateFlag(client, project.id, flagArg, {
      environments: {
        [environment]: buildPausedEnvironmentConfig(envConfig, onVariant.id)
      },
      message: updateMessage
    });
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Feature flag ${import_chalk113.default.bold(flag.slug)} has been enabled in ${import_chalk113.default.bold(environment)}`
    );
    output_manager_default.log(
      `  ${import_chalk113.default.dim("Serving variant:")} ${formatVariantForDisplay(onVariant)}`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    const message2 = err instanceof Error ? err.message : String(err);
    if (client.nonInteractive && message2.startsWith("Invalid environment:")) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: message2,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags enable ${flagArg} --environment <production|preview|development>`
              ),
              when: "use a valid environment (production, preview, or development)"
            }
          ]
        },
        1
      );
      return 1;
    }
    printError(err);
    return 1;
  }
  return 0;
}
function getDefaultEnableMessage(environment) {
  return `Enabled for ${environment} via CLI`;
}

// src/util/telemetry/commands/flags/sdk-keys.ts
var FlagsSdkKeysTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "ls",
      value: actual
    });
  }
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "rm",
      value: actual
    });
  }
};
var FlagsSdkKeysLsTelemetryClient = class extends TelemetryClient {
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
};
var FlagsSdkKeysAddTelemetryClient = class extends TelemetryClient {
  trackCliOptionType(type) {
    if (type) {
      this.trackCliOption({
        option: "type",
        value: type
      });
    }
  }
  trackCliOptionEnvironment(environment) {
    if (environment) {
      this.trackCliOption({
        option: "environment",
        value: environment
      });
    }
  }
  trackCliOptionLabel(label) {
    if (label) {
      this.trackCliOption({
        option: "label",
        value: this.redactedValue
      });
    }
  }
};
var FlagsSdkKeysRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentKey(key) {
    if (key) {
      this.trackCliArgument({
        arg: "key",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/flags/sdk-keys-ls.ts
var import_chalk114 = __toESM(require_source(), 1);
var import_ms15 = __toESM(require_ms(), 1);

// src/util/flags/sdk-keys.ts
async function getSdkKeys(client, projectId) {
  output_manager_default.debug(`Fetching SDK keys for project ${projectId}`);
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/sdk-keys`;
  const response = await client.fetch(url);
  return response.data;
}
async function createSdkKey(client, projectId, request2) {
  output_manager_default.debug(`Creating SDK key for project ${projectId}`);
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/sdk-keys`;
  const response = await client.fetch(url, {
    method: "PUT",
    body: request2
  });
  return response;
}
async function deleteSdkKey(client, projectId, hashKey) {
  output_manager_default.debug(`Deleting SDK key ${hashKey} for project ${projectId}`);
  const url = `/v1/projects/${encodeURIComponent(projectId)}/feature-flags/sdk-keys/${encodeURIComponent(hashKey)}`;
  await client.fetch(url, {
    method: "DELETE",
    json: false
  });
}

// src/commands/flags/sdk-keys-ls.ts
async function sdkKeysLs(client, argv) {
  const telemetryClient = new FlagsSdkKeysLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    sdkKeysListSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { flags } = parsedArgs;
  const json = flags["--json"];
  telemetryClient.trackCliFlagJson(json);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project, org } = link;
  const projectSlugLink = formatProject(org.slug, project.name);
  try {
    output_manager_default.spinner("Fetching SDK keys...");
    const keys = await getSdkKeys(client, project.id);
    output_manager_default.stopSpinner();
    const sortedKeys = keys.sort((a, b) => b.createdAt - a.createdAt);
    if (json) {
      outputSdkKeysJson(client, sortedKeys);
    } else if (keys.length === 0) {
      output_manager_default.log(`No SDK keys found for ${projectSlugLink}`);
      output_manager_default.log(
        `
Create one with: ${getCommandName("flags sdk-keys add --type server --environment production")}`
      );
    } else {
      output_manager_default.log(
        `${import_chalk114.default.bold(keys.length)} SDK key${keys.length === 1 ? "" : "s"} found for ${projectSlugLink}`
      );
      printSdkKeysTable(sortedKeys);
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}
function outputSdkKeysJson(client, keys) {
  const jsonOutput = {
    sdkKeys: keys.map((key) => ({
      hashKey: key.hashKey,
      type: key.type,
      environment: key.environment,
      label: key.label ?? null,
      partialKeyValue: key.partialKeyValue ?? null,
      createdAt: key.createdAt,
      updatedAt: key.updatedAt
    }))
  };
  client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
}
function printSdkKeysTable(keys) {
  const headers = [
    "Hash Key",
    "Type",
    "Environment",
    "Label",
    "Partial Key Value",
    "Created"
  ];
  const now = Date.now();
  const rows = keys.map((key) => [
    import_chalk114.default.dim(key.hashKey.slice(0, 12) + "..."),
    getTypeLabel(key.type),
    key.environment,
    key.label || import_chalk114.default.dim("-"),
    key.partialKeyValue || import_chalk114.default.dim("-"),
    (0, import_ms15.default)(now - key.createdAt) + " ago"
  ]);
  const table3 = formatTable(
    headers,
    ["l", "l", "l", "l", "l", "l"],
    [{ name: "", rows }]
  );
  output_manager_default.print(`
${table3}
`);
}
function getTypeLabel(type) {
  switch (type) {
    case "server":
      return import_chalk114.default.blue("server");
    case "client":
      return import_chalk114.default.green("client");
    case "mobile":
      return import_chalk114.default.yellow("mobile");
    default:
      return type;
  }
}

// src/commands/flags/sdk-keys-add.ts
var import_chalk115 = __toESM(require_source(), 1);
var VALID_TYPES = ["server", "client", "mobile"];
var VALID_ENVIRONMENTS2 = ["production", "preview", "development"];
async function sdkKeysAdd(client, argv) {
  const telemetryClient = new FlagsSdkKeysAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    sdkKeysAddSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { flags } = parsedArgs;
  let sdkKeyType = flags["--type"];
  let environment = flags["--environment"];
  const label = flags["--label"];
  telemetryClient.trackCliOptionType(sdkKeyType);
  telemetryClient.trackCliOptionEnvironment(environment);
  telemetryClient.trackCliOptionLabel(label);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          message: "Your codebase is not linked to a project. Run link first.",
          next: [
            {
              command: buildCommandWithGlobalFlags(client.argv, "link"),
              when: "link the project"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  if (!sdkKeyType) {
    if (client.nonInteractive) {
      const envPart = environment ? `--environment ${environment}` : "--environment <production|preview|development>";
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Please provide --type (server, client, or mobile) for the SDK key.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags sdk-keys add --type <server|client|mobile> ${envPart}`
              ),
              when: "create an SDK key with a specific type"
            }
          ]
        },
        1
      );
      return 1;
    }
    sdkKeyType = await client.input.select({
      message: "Select the SDK key type:",
      choices: [
        {
          name: "Server (for backend/server-side usage)",
          value: "server"
        },
        {
          name: "Client (for browser/frontend usage)",
          value: "client"
        },
        { name: "Mobile (for mobile app usage)", value: "mobile" }
      ]
    });
  }
  if (!VALID_TYPES.includes(sdkKeyType)) {
    if (client.nonInteractive) {
      const envPart = environment ? `--environment ${environment}` : "--environment <production|preview|development>";
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: `Invalid type: ${sdkKeyType}. Must be one of: ${VALID_TYPES.join(", ")}`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags sdk-keys add --type <server|client|mobile> ${envPart}`
              ),
              when: "use a valid type (server, client, or mobile)"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Invalid type: ${sdkKeyType}. Must be one of: ${VALID_TYPES.join(", ")}`
    );
    return 1;
  }
  if (!environment) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Please provide --environment (production, preview, or development) for the SDK key.",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags sdk-keys add --type ${sdkKeyType} --environment <production|preview|development>`
              ),
              when: "create an SDK key for a specific environment"
            }
          ]
        },
        1
      );
      return 1;
    }
    environment = await client.input.select({
      message: "Select the environment:",
      choices: VALID_ENVIRONMENTS2.map((env) => ({
        name: env,
        value: env
      }))
    });
  }
  if (!VALID_ENVIRONMENTS2.includes(environment)) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: `Invalid environment: ${environment}. Must be one of: ${VALID_ENVIRONMENTS2.join(", ")}`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                `flags sdk-keys add --type ${sdkKeyType} --environment <production|preview|development>`
              ),
              when: "use a valid environment (production, preview, or development)"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Invalid environment: ${environment}. Must be one of: ${VALID_ENVIRONMENTS2.join(", ")}`
    );
    return 1;
  }
  let finalLabel = label;
  if (!finalLabel && client.stdin.isTTY && !client.nonInteractive) {
    finalLabel = await client.input.text({
      message: "Enter an optional label for this SDK key (press Enter to skip):"
    });
    if (finalLabel === "") {
      finalLabel = void 0;
    }
  }
  const request2 = {
    sdkKeyType,
    environment,
    label: finalLabel
  };
  try {
    output_manager_default.spinner("Creating SDK key...");
    const key = await createSdkKey(client, project.id, request2);
    output_manager_default.stopSpinner();
    output_manager_default.success("SDK key created successfully");
    output_manager_default.print("\n");
    output_manager_default.print(`  ${import_chalk115.default.dim("Hash Key:")}     ${key.hashKey}
`);
    output_manager_default.print(`  ${import_chalk115.default.dim("Type:")}         ${key.type}
`);
    output_manager_default.print(`  ${import_chalk115.default.dim("Environment:")}  ${key.environment}
`);
    if (key.label) {
      output_manager_default.print(`  ${import_chalk115.default.dim("Label:")}        ${key.label}
`);
    }
    if (key.keyValue) {
      output_manager_default.print("\n");
      output_manager_default.warn("Save this key - it will not be shown again:");
      output_manager_default.print(`
  ${import_chalk115.default.cyan(key.keyValue)}
`);
    }
    if (key.connectionString) {
      output_manager_default.print("\n");
      output_manager_default.log(`${import_chalk115.default.dim("Connection string:")}`);
      output_manager_default.print(`  ${import_chalk115.default.cyan(key.connectionString)}
`);
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/sdk-keys-rm.ts
var import_chalk116 = __toESM(require_source(), 1);
async function sdkKeysRm(client, argv) {
  const telemetryClient = new FlagsSdkKeysRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    sdkKeysRemoveSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  let [hashKey] = args;
  const skipConfirmation = flags["--yes"];
  telemetryClient.trackCliArgumentKey(hashKey);
  telemetryClient.trackCliFlagYes(skipConfirmation);
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          message: "Your codebase is not linked to a project. A user must run `vercel link` in a terminal to continue (this will prompt for login if needed).",
          next: [
            {
              command: buildCommandWithGlobalFlags(client.argv, "link"),
              when: "run in a terminal to log in if needed and link this codebase to a Vercel project, then retry this command"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  const { project } = link;
  try {
    if (!hashKey) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: AGENT_STATUS.ERROR,
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Please provide the SDK key hash to delete. Run `vercel flags sdk-keys ls` to list keys.",
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "flags sdk-keys rm <hashKey>"
                ),
                when: "delete an SDK key by hash"
              },
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "flags sdk-keys ls"
                ),
                when: "list SDK keys to find the hash"
              }
            ]
          },
          1
        );
        return 1;
      }
      output_manager_default.spinner("Fetching SDK keys...");
      const keys = await getSdkKeys(client, project.id);
      output_manager_default.stopSpinner();
      if (keys.length === 0) {
        output_manager_default.log("No SDK keys found");
        return 0;
      }
      hashKey = await client.input.select({
        message: "Select an SDK key to delete:",
        choices: keys.map((key) => ({
          name: `${key.hashKey.slice(0, 12)}... (${key.type}, ${key.environment}${key.label ? `, ${key.label}` : ""})`,
          value: key.hashKey
        }))
      });
    }
    if (!skipConfirmation) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: AGENT_STATUS.ERROR,
            reason: AGENT_REASON.CONFIRMATION_REQUIRED,
            message: `Confirm deletion of SDK key ${hashKey.slice(0, 12)}... by adding --yes.`,
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  `flags sdk-keys rm ${hashKey} --yes`
                ),
                when: "confirm and delete the SDK key"
              }
            ]
          },
          1
        );
        return 1;
      }
      const confirmed = await client.input.confirm(
        `Are you sure you want to delete SDK key ${import_chalk116.default.bold(hashKey.slice(0, 12) + "...")}?`,
        false
      );
      if (!confirmed) {
        output_manager_default.log("Aborted");
        return 0;
      }
    }
    output_manager_default.spinner("Deleting SDK key...");
    await deleteSdkKey(client, project.id, hashKey);
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `SDK key ${import_chalk116.default.bold(hashKey.slice(0, 12) + "...")} has been deleted`
    );
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  return 0;
}

// src/commands/flags/sdk-keys.ts
var COMMAND_CONFIG22 = {
  ls: getCommandAliases(sdkKeysListSubcommand),
  add: getCommandAliases(sdkKeysAddSubcommand),
  rm: getCommandAliases(sdkKeysRemoveSubcommand)
};
async function sdkKeys(client) {
  const telemetry2 = new FlagsSdkKeysTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(sdkKeysSubcommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(4), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(0);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG22
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("flags sdk-keys", subcommand);
    output_manager_default.print(
      help(sdkKeysSubcommand, {
        parent: flagsCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: sdkKeysSubcommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "ls":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags sdk-keys", subcommandOriginal);
        printHelp(sdkKeysListSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return sdkKeysLs(client, args);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags sdk-keys", subcommandOriginal);
        printHelp(sdkKeysAddSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return sdkKeysAdd(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags sdk-keys", subcommandOriginal);
        printHelp(sdkKeysRemoveSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return sdkKeysRm(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG22));
      output_manager_default.print(
        help(sdkKeysSubcommand, {
          parent: flagsCommand,
          columns: client.stderr.columns
        })
      );
      return 2;
  }
}

// src/commands/flags/emit-datafiles.ts
var import_env = __toESM(require_dist4(), 1);
import { prepareFlagsDefinitions } from "@vercel/prepare-flags-definitions";
async function emitDatafiles(client) {
  try {
    let localEnv = {};
    try {
      localEnv = (0, import_env.loadEnvConfig)(client.cwd, true).combinedEnv;
    } catch (err) {
      output_manager_default.debug(`Failed to load local env files: ${err}`);
    }
    await prepareFlagsDefinitions({
      cwd: client.cwd,
      env: { ...localEnv, ...process.env },
      userAgentSuffix: ua_default,
      output: output_manager_default
    });
    return 0;
  } catch (err) {
    printError(err);
    return 1;
  }
}

// src/commands/flags/override.ts
var import_env2 = __toESM(require_dist4(), 1);
import { base64url, EncryptJWT, jwtDecrypt } from "jose";
async function override(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(overrideSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const decryptToken = flags["--decrypt"];
  if (decryptToken) {
    return handleDecrypt(client, decryptToken);
  }
  return handleEncrypt(client, args, flags);
}
async function handleEncrypt(client, args, flags) {
  const expiration = flags["--expiration"] ?? "1y";
  if (args.length === 0) {
    output_manager_default.error(
      "Please provide at least one flag override as flag=value.\nExample: vercel flags override my-flag=true"
    );
    return 1;
  }
  const overrides = {};
  for (const arg of args) {
    const eqIndex = arg.indexOf("=");
    if (eqIndex === -1) {
      output_manager_default.error(`Invalid override format: "${arg}". Expected flag=value.`);
      return 1;
    }
    const key = arg.slice(0, eqIndex);
    const rawValue = arg.slice(eqIndex + 1);
    overrides[key] = parseValue(rawValue);
  }
  const secret = resolveSecret(client);
  if (!secret) {
    output_manager_default.error(
      "FLAGS_SECRET not found. Set it in the environment, .env.local, or .env file."
    );
    return 1;
  }
  try {
    const encrypted = await encryptOverrides(overrides, secret, expiration);
    client.stdout.write(encrypted + "\n");
    return 0;
  } catch (err) {
    printError(err);
    return 1;
  }
}
async function handleDecrypt(client, token2) {
  const secret = resolveSecret(client);
  if (!secret) {
    output_manager_default.error(
      "FLAGS_SECRET not found. Set it in the environment, .env.local, or .env file."
    );
    return 1;
  }
  try {
    const overrides = await decryptOverrides(token2, secret);
    if (overrides === void 0) {
      output_manager_default.error("Invalid token: not a valid flag overrides token.");
      return 1;
    }
    client.stdout.write(JSON.stringify(overrides, null, 2) + "\n");
    return 0;
  } catch (err) {
    printError(err);
    return 1;
  }
}
function parseValue(raw) {
  if (raw === "true")
    return true;
  if (raw === "false")
    return false;
  const num = Number(raw);
  if (!Number.isNaN(num) && raw.trim() !== "")
    return num;
  return raw;
}
function resolveSecret(client) {
  const { combinedEnv } = (0, import_env2.loadEnvConfig)(client.cwd, true);
  return combinedEnv.FLAGS_SECRET;
}
async function encryptOverrides(overrides, secret, expirationTime) {
  const encodedSecret = base64url.decode(secret);
  if (encodedSecret.length !== 32) {
    throw new Error(
      "Invalid FLAGS_SECRET: must be a 256-bit base64url-encoded key (32 bytes)."
    );
  }
  return new EncryptJWT({ o: overrides, pur: "overrides" }).setExpirationTime(expirationTime).setProtectedHeader({ alg: "dir", enc: "A256GCM" }).encrypt(encodedSecret);
}
async function decryptOverrides(encryptedData, secret) {
  const encodedSecret = base64url.decode(secret);
  if (encodedSecret.length !== 32) {
    throw new Error(
      "Invalid FLAGS_SECRET: must be a 256-bit base64url-encoded key (32 bytes)."
    );
  }
  const { payload } = await jwtDecrypt(encryptedData, encodedSecret);
  if (payload.pur !== "overrides" || !payload.o) {
    return void 0;
  }
  return payload.o;
}

// src/commands/flags/index.ts
var COMMAND_CONFIG23 = {
  ls: getCommandAliases(listSubcommand10),
  inspect: getCommandAliases(inspectSubcommand3),
  create: getCommandAliases(createSubcommand4),
  open: getCommandAliases(openSubcommand2),
  update: getCommandAliases(updateSubcommand3),
  set: getCommandAliases(setSubcommand2),
  split: getCommandAliases(splitSubcommand),
  rollout: getCommandAliases(rolloutSubcommand),
  rm: getCommandAliases(removeSubcommand8),
  archive: getCommandAliases(archiveSubcommand),
  disable: getCommandAliases(disableSubcommand),
  enable: getCommandAliases(enableSubcommand),
  "sdk-keys": getCommandAliases(sdkKeysSubcommand),
  prepare: getCommandAliases(prepareSubcommand),
  override: getCommandAliases(overrideSubcommand)
};
async function main17(client) {
  const telemetry2 = new FlagsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(flagsCommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG23
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("flags", subcommand);
    output_manager_default.print(help(flagsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: flagsCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "ls":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(listSubcommand10);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls7(client, args);
    case "inspect":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(inspectSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandInspect(subcommandOriginal);
      return inspect3(client, args);
    case "open":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(openSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandOpen(subcommandOriginal);
      return openFlag(client, args);
    case "create":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(createSubcommand4);
        return 2;
      }
      telemetry2.trackCliSubcommandCreate(subcommandOriginal);
      return create4(client, args);
    case "update":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(updateSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandUpdate(subcommandOriginal);
      return update2(client, args);
    case "set":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(setSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandSet(subcommandOriginal);
      return set2(client, args);
    case "split":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(splitSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSplit(subcommandOriginal);
      return split(client, args);
    case "rollout":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(rolloutSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRollout(subcommandOriginal);
      return rollout(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(removeSubcommand8);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm7(client, args);
    case "archive":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(archiveSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandArchive(subcommandOriginal);
      return archive(client, args);
    case "disable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(disableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDisable(subcommandOriginal);
      return disable3(client, args);
    case "enable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(enableSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandEnable(subcommandOriginal);
      return enable3(client, args);
    case "sdk-keys":
      telemetry2.trackCliSubcommandSdkKeys(subcommandOriginal);
      return sdkKeys(client);
    case "prepare":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(prepareSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandPrepare(subcommandOriginal);
      return emitDatafiles(client);
    case "override":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(overrideSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandOverride(subcommandOriginal);
      return override(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("flags", subcommandOriginal);
        printHelp(listSubcommand10);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls7(client, args);
  }
}

// src/commands/git/connect.ts
var import_chalk117 = __toESM(require_source(), 1);
import { join as join4 } from "path";

// src/util/telemetry/commands/git/connect.ts
var GitConnectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentGitUrl(name) {
    if (name) {
      this.trackCliArgument({
        arg: "gitUrl",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagConfirm(confirm) {
    if (confirm) {
      this.trackCliFlag("confirm");
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/git/connect.ts
async function connect(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(connectSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const { cwd } = client;
  const telemetry2 = new GitConnectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetry2.trackCliFlagConfirm(opts["--confirm"]);
  telemetry2.trackCliFlagYes(opts["--yes"]);
  if ("--confirm" in opts) {
    output_manager_default.warn("`--confirm` is deprecated, please use `--yes` instead");
    opts["--yes"] = opts["--confirm"];
  }
  const confirm = Boolean(opts["--yes"]);
  if (args.length > 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk117.default.cyan(
        `${getCommandName("project connect")}`
      )}`
    );
    return 2;
  }
  const repoArg = args[0];
  telemetry2.trackCliArgumentGitUrl(repoArg);
  const linkedProject = await ensureLink("git", client, client.cwd, {
    autoConfirm: confirm
  });
  if (typeof linkedProject === "number") {
    return linkedProject;
  }
  const { project, org } = linkedProject;
  const gitProviderLink = project.link;
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  const gitConfigPath = join4(cwd, ".git/config");
  const gitConfig = await parseGitConfig(gitConfigPath);
  if (repoArg) {
    const parsedUrlArg = parseRepoUrl(repoArg);
    if (!parsedUrlArg) {
      output_manager_default.error(
        `Failed to parse URL "${repoArg}". Please ensure the URL is valid.`
      );
      return 1;
    }
    if (gitConfig) {
      return await connectArgWithLocalGit({
        client,
        org,
        project,
        confirm,
        gitConfig,
        repoInfo: parsedUrlArg
      });
    }
    return await connectArg({
      client,
      confirm,
      org,
      project,
      repoInfo: parsedUrlArg
    });
  }
  if (!gitConfig) {
    output_manager_default.error(
      `No local Git repository found. Run ${import_chalk117.default.cyan(
        "`git clone <url>`"
      )} to clone a remote Git repository first.`
    );
    return 1;
  }
  const remoteUrls = pluckRemoteUrls(gitConfig);
  if (!remoteUrls) {
    output_manager_default.error(
      `No remote URLs found in your Git config. Make sure you've configured a remote repo in your local Git config. Run ${import_chalk117.default.cyan(
        "`git remote --help`"
      )} for more details.`
    );
    return 1;
  }
  const repoInfo = await selectAndParseRemoteUrl(client, remoteUrls);
  if (!repoInfo) {
    return Object.keys(remoteUrls).length > 1 ? 0 : 1;
  }
  const result = await checkExistsAndConnect({
    client,
    confirm,
    gitProviderLink,
    org,
    gitOrg: repoInfo.org,
    project,
    provider: repoInfo.provider,
    repo: repoInfo.repo,
    repoPath: `${repoInfo.org}/${repoInfo.repo}`
  });
  if (typeof result === "number") {
    return result;
  }
  return 0;
}
async function connectArg({
  client,
  confirm,
  org,
  project,
  repoInfo
}) {
  const { url: repoUrl } = repoInfo;
  const parsedRepoArg = parseRepoUrl(repoUrl);
  if (!parsedRepoArg) {
    output_manager_default.error(
      `Failed to parse URL "${repoUrl}". Please ensure the URL is valid.`
    );
    return 1;
  }
  const result = await checkExistsAndConnect({
    client,
    confirm,
    gitProviderLink: project.link,
    org,
    gitOrg: parsedRepoArg.org,
    project,
    provider: parsedRepoArg.provider,
    repo: parsedRepoArg.repo,
    repoPath: `${parsedRepoArg.org}/${parsedRepoArg.repo}`
  });
  if (typeof result === "number") {
    return result;
  }
  return 0;
}
async function connectArgWithLocalGit({
  client,
  org,
  project,
  confirm,
  gitConfig,
  repoInfo
}) {
  const remoteUrls = pluckRemoteUrls(gitConfig);
  if (remoteUrls) {
    const shouldConnect = await promptConnectArg({
      client,
      yes: confirm,
      repoInfo,
      remoteUrls
    });
    if (!shouldConnect) {
      return 1;
    }
    if (shouldConnect) {
      const result = await checkExistsAndConnect({
        client,
        confirm,
        gitProviderLink: project.link,
        org,
        gitOrg: repoInfo.org,
        project,
        provider: repoInfo.provider,
        repo: repoInfo.repo,
        repoPath: `${repoInfo.org}/${repoInfo.repo}`
      });
      if (typeof result === "number") {
        return result;
      }
    }
    return 0;
  }
  return await connectArg({ client, confirm, org, project, repoInfo });
}
async function promptConnectArg({
  client,
  yes,
  repoInfo: repoInfoFromArg,
  remoteUrls
}) {
  if (Object.keys(remoteUrls).length > 1) {
    output_manager_default.log("Found multiple Git repositories in your local Git config:");
    printRemoteUrls(remoteUrls);
  } else {
    const url = Object.values(remoteUrls)[0];
    const repoInfoFromGitConfig = parseRepoUrl(url);
    if (!repoInfoFromGitConfig) {
      output_manager_default.error(
        `Failed to parse URL "${url}". Please ensure the URL is valid.`
      );
      return false;
    }
    if (JSON.stringify(repoInfoFromGitConfig) === JSON.stringify(repoInfoFromArg)) {
      return true;
    }
    output_manager_default.log(
      `Found a repository in your local Git Config: ${import_chalk117.default.cyan(
        Object.values(remoteUrls)[0]
      )}`
    );
  }
  let shouldConnect = yes;
  if (!shouldConnect) {
    const { url: repoUrlFromArg } = repoInfoFromArg;
    shouldConnect = await client.input.confirm(
      `Do you still want to connect ${link_default(repoUrlFromArg)}?`,
      false
    );
    if (!shouldConnect) {
      output_manager_default.log("Canceled. Repo not connected.");
    }
  }
  return shouldConnect;
}

// src/commands/git/disconnect.ts
var import_chalk118 = __toESM(require_source(), 1);

// src/util/telemetry/commands/git/disconnect.ts
var GitDisconnectTelemetryClient = class extends TelemetryClient {
  trackCliFlagConfirm(confirm) {
    if (confirm) {
      this.trackCliFlag("confirm");
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/git/disconnect.ts
async function disconnect(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    disconnectSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const telemetry2 = new GitDisconnectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetry2.trackCliFlagConfirm(opts["--confirm"]);
  telemetry2.trackCliFlagYes(opts["--yes"]);
  if ("--confirm" in opts) {
    output_manager_default.warn("`--confirm` is deprecated, please use `--yes` instead");
    opts["--yes"] = opts["--confirm"];
  }
  if (args.length !== 0) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk118.default.cyan(
        `${getCommandName("project disconnect")}`
      )}`
    );
    return 2;
  }
  const autoConfirm = Boolean(parsedArgs.flags["--yes"]);
  const linkedProject = await ensureLink("git", client, client.cwd, {
    autoConfirm
  });
  if (typeof linkedProject === "number") {
    return linkedProject;
  }
  const { org, project } = linkedProject;
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  if (project.link) {
    const { org: linkOrg, repo } = project.link;
    output_manager_default.print(
      `Your Vercel project will no longer create deployments when you push to this repository.
`
    );
    const confirmDisconnect = autoConfirm || await client.input.confirm(
      `Are you sure you want to disconnect ${import_chalk118.default.cyan(
        `${linkOrg}/${repo}`
      )} from your project?`,
      false
    );
    if (confirmDisconnect) {
      await disconnectGitProvider(client, org, project.id);
      output_manager_default.log(`Disconnected ${import_chalk118.default.cyan(`${linkOrg}/${repo}`)}.`);
    } else {
      output_manager_default.log("Canceled");
    }
  } else {
    output_manager_default.error(
      `No Git repository connected. Run ${getCommandName(
        "project connect"
      )} to connect one.`
    );
    return 1;
  }
  return 0;
}

// src/util/telemetry/commands/git/index.ts
var GitTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandConnect(actual) {
    this.trackCliSubcommand({
      subcommand: "connect",
      value: actual
    });
  }
  trackCliSubcommandDisconnect(actual) {
    this.trackCliSubcommand({
      subcommand: "disconnect",
      value: actual
    });
  }
};

// src/commands/git/index.ts
var COMMAND_CONFIG24 = {
  connect: ["connect"],
  disconnect: ["disconnect"]
};
async function main18(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(gitCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new GitTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG24
  );
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("git", subcommand);
    output_manager_default.print(help(gitCommand, { columns: client.stderr.columns }));
    return 2;
  }
  switch (subcommand) {
    case "connect":
      telemetry2.trackCliSubcommandConnect(subcommandOriginal);
      return connect(client, args);
    case "disconnect":
      telemetry2.trackCliSubcommandDisconnect(subcommandOriginal);
      return disconnect(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG24));
      output_manager_default.print(help(gitCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/guidance/status.ts
var import_chalk119 = __toESM(require_source(), 1);
async function status(client) {
  const enabled = client.config.guidance?.enabled !== false;
  const status3 = enabled ? import_chalk119.default.green("Enabled") : import_chalk119.default.red("Disabled");
  output_manager_default.print("\n");
  output_manager_default.log(`${import_chalk119.default.bold("Guidance status")}: ${status3}`);
  output_manager_default.print("\n");
  return 0;
}

// src/commands/guidance/enable.ts
async function enable4(client) {
  client.config = {
    ...client.config,
    guidance: {
      ...client.config.guidance,
      enabled: true
    }
  };
  writeToConfigFile(client.config);
  await status(client);
  return 0;
}

// src/commands/guidance/disable.ts
async function disable4(client) {
  client.config = {
    ...client.config,
    guidance: {
      ...client.config.guidance,
      enabled: false
    }
  };
  writeToConfigFile(client.config);
  await status(client);
  return 0;
}

// src/util/telemetry/commands/guidance/index.ts
var GuidanceTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandStatus(actual) {
    this.trackCliSubcommand({
      subcommand: "status",
      value: actual
    });
  }
  trackCliSubcommandEnable(actual) {
    this.trackCliSubcommand({
      subcommand: "enable",
      value: actual
    });
  }
  trackCliSubcommandDisable(actual) {
    this.trackCliSubcommand({
      subcommand: "disable",
      value: actual
    });
  }
};

// src/commands/guidance/index.ts
var COMMAND_CONFIG25 = {
  status: getCommandAliases(statusSubcommand),
  enable: getCommandAliases(enableSubcommand2),
  disable: getCommandAliases(disableSubcommand2)
};
async function guidance(client) {
  if (!process.env.FF_GUIDANCE_MODE) {
    output_manager_default.error("The guidance subcommand does not exist");
    return 1;
  }
  const telemetryClient = new GuidanceTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(guidanceCommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, subcommandOriginal } = getSubcommand5(
    parsedArguments.args.slice(1),
    COMMAND_CONFIG25
  );
  const needHelp = parsedArguments.flags["--help"];
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        columns: client.stderr.columns,
        parent: guidanceCommand
      })
    );
  }
  if (!subcommand && needHelp) {
    telemetryClient.trackCliFlagHelp("guidance", subcommand);
    output_manager_default.print(help(guidanceCommand, { columns: client.stderr.columns }));
    return 2;
  }
  switch (subcommand) {
    case "status":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("guidance", subcommandOriginal);
        printHelp(statusSubcommand);
        return 2;
      }
      telemetryClient.trackCliSubcommandStatus(subcommandOriginal);
      return status(client);
    case "enable":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("guidance", subcommandOriginal);
        printHelp(enableSubcommand2);
        return 2;
      }
      telemetryClient.trackCliSubcommandEnable(subcommandOriginal);
      return enable4(client);
    case "disable":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("guidance", subcommandOriginal);
        printHelp(disableSubcommand2);
        return 2;
      }
      return disable4(client);
    default: {
      output_manager_default.print(help(guidanceCommand, { columns: client.stderr.columns }));
      return 2;
    }
  }
}

// src/commands/httpstat/index.ts
import { spawn as spawn3 } from "child_process";

// src/util/telemetry/commands/httpstat/index.ts
var HttpstatTelemetryClient = class extends TelemetryClient {
  trackCliArgumentPath(path3) {
    if (path3) {
      const value = path3.startsWith("/") ? "slash" : "no-slash";
      this.trackCliArgument({
        arg: "path",
        value
      });
    }
  }
  trackCliOptionDeployment(deploymentId) {
    if (deploymentId) {
      const value = deploymentId.startsWith("http://") || deploymentId.startsWith("https://") ? "url" : deploymentId.startsWith("dpl_") ? "dpl_" : "no-prefix";
      this.trackCliOption({
        option: "deployment",
        value
      });
    }
  }
  trackCliOptionProtectionBypass(secret) {
    if (secret) {
      this.trackCliOption({
        option: "protection-bypass",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/httpstat/index.ts
async function httpstat(client) {
  const telemetryClient = new HttpstatTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const setup = setupCurlLikeCommand(client, httpstatCommand, telemetryClient);
  if (typeof setup === "number") {
    return setup;
  }
  const { path: path3, deploymentFlag, protectionBypassFlag, toolFlags } = setup;
  const result = await getDeploymentUrlAndToken(client, "httpstat", path3, {
    deploymentFlag,
    protectionBypassFlag,
    autoConfirm: setup.yes
  });
  if (typeof result === "number") {
    return result;
  }
  const { fullUrl, deploymentProtectionToken } = result;
  const httpstatFlags = [...toolFlags];
  if (deploymentProtectionToken) {
    httpstatFlags.unshift(
      "-H",
      `x-vercel-protection-bypass: ${deploymentProtectionToken}`
    );
  }
  httpstatFlags.unshift(fullUrl);
  output_manager_default.debug(
    `Executing: httpstat ${httpstatFlags.map(requoteArgs).join(" ")}`
  );
  return new Promise((resolve8) => {
    const httpstatProcess = spawn3("httpstat", httpstatFlags, {
      stdio: "inherit",
      shell: false
    });
    httpstatProcess.on("error", (err) => {
      if ("code" in err && err.code === "ENOENT") {
        output_manager_default.error("httpstat command not found. Please install httpstat.");
        output_manager_default.log("");
        output_manager_default.log("Installation instructions:");
        output_manager_default.log("  macOS: brew install httpstat");
        output_manager_default.log("  pip: pip install httpstat");
        output_manager_default.log("  npm: npm install -g httpstat");
        output_manager_default.log("");
        output_manager_default.log(
          "Or visit: https://github.com/reorx/httpstat for more details"
        );
        resolve8(1);
      } else {
        output_manager_default.error(`Failed to execute httpstat: ${err.message}`);
        resolve8(1);
      }
    });
    httpstatProcess.on("close", (code2) => {
      resolve8(code2 ?? 1);
    });
  });
}

// src/commands/init/init.ts
var import_tar_fs = __toESM(require_tar_fs(), 1);
var import_chalk120 = __toESM(require_source(), 1);
import fs from "fs";
import path2 from "path";
var EXAMPLE_API = "https://examples.vercel.sh";
async function init(client, opts, args, telemetry2) {
  const [name, dir] = args;
  const force = opts["--force"];
  const examples = await fetchExampleList(client);
  if (!examples) {
    throw new Error("Could not fetch example list.");
  }
  const exampleList = examples.filter((x) => x.visible).map((x) => x.name);
  if (!name) {
    if (client.stdin.isTTY !== true) {
      output_manager_default.print("No framework provided");
      return 0;
    }
    const chosen = await chooseFromDropdown(
      client,
      "Select example:",
      exampleList
    );
    if (!chosen) {
      output_manager_default.log("Canceled");
      return 0;
    }
    return extractExample(client, chosen, dir, force);
  }
  if (exampleList.includes(name)) {
    telemetry2.trackCliArgumentExample(name, true);
    return extractExample(client, name, dir, force);
  }
  const oldExample = examples.find((x) => !x.visible && x.name === name);
  if (oldExample) {
    telemetry2.trackCliArgumentExample(name, true);
    return extractExample(client, name, dir, force, "v1");
  }
  telemetry2.trackCliArgumentExample(name, false);
  const found = await guess(client, exampleList, name);
  if (typeof found === "string") {
    return extractExample(client, found, dir, force);
  }
  output_manager_default.log("No changes made.");
  return 0;
}
async function fetchExampleList(client) {
  output_manager_default.spinner("Fetching examples");
  const url = `${EXAMPLE_API}/v2/list.json`;
  const body = await client.fetch(url);
  output_manager_default.stopSpinner();
  return body;
}
async function chooseFromDropdown(client, message, exampleList) {
  const choices = exampleList.map((name) => ({
    name,
    value: name,
    short: name
  }));
  return list(client, {
    message,
    choices
  });
}
async function extractExample(client, name, dir, force, ver = "v2") {
  const folder = prepareFolder(client.cwd, dir || name, force);
  output_manager_default.spinner(`Fetching ${name}`);
  const url = `${EXAMPLE_API}/${ver}/download/${name}.tar.gz`;
  return client.fetch(url, { json: false }).then(async (res) => {
    output_manager_default.stopSpinner();
    if (res.status !== 200) {
      throw new Error(`Could not get ${name}.tar.gz`);
    }
    await new Promise((resolve8, reject) => {
      const extractor = import_tar_fs.default.extract(folder);
      res.body.on("error", reject);
      extractor.on("error", reject);
      extractor.on("finish", resolve8);
      res.body.pipe(extractor);
    });
    const successLog = `Initialized "${import_chalk120.default.bold(
      name
    )}" example in ${import_chalk120.default.bold(humanizePath(folder))}.`;
    const folderRel = path2.relative(client.cwd, folder);
    const deployHint = folderRel === "" ? list_item_default(`To deploy, run ${getCommandName()}.`) : list_item_default(
      `To deploy, ${cmd(
        `cd ${folderRel}`
      )} and run ${getCommandName()}.`
    );
    output_manager_default.success(`${successLog}
${deployHint}`);
    return 0;
  }).catch((e2) => {
    output_manager_default.stopSpinner();
    throw e2;
  });
}
function prepareFolder(cwd, folder, force) {
  const dest = path2.join(cwd, folder);
  if (fs.existsSync(dest)) {
    if (!fs.lstatSync(dest).isDirectory()) {
      throw new Error(
        `Destination path "${import_chalk120.default.bold(
          folder
        )}" already exists and is not a directory.`
      );
    }
    if (!force && fs.readdirSync(dest).length !== 0) {
      throw new Error(
        `Destination path "${import_chalk120.default.bold(
          folder
        )}" already exists and is not an empty directory. You may use ${cmd(
          "--force"
        )} or ${cmd("-f")} to override it.`
      );
    }
  } else if (dest !== cwd) {
    try {
      fs.mkdirSync(dest);
    } catch (_e) {
      throw new Error(`Could not create directory "${import_chalk120.default.bold(folder)}".`);
    }
  }
  return dest;
}
async function guess(client, exampleList, name) {
  const GuessError = new Error(
    `No example found for ${import_chalk120.default.bold(name)}, run ${getCommandName(
      "init"
    )} to see the list of available examples.`
  );
  if (client.stdin.isTTY !== true) {
    throw GuessError;
  }
  const found = did_you_mean_default(name, exampleList, 0.7);
  if (typeof found === "string") {
    if (await client.input.confirm(`Did you mean ${import_chalk120.default.bold(found)}?`, false)) {
      return found;
    }
  } else {
    throw GuessError;
  }
}

// src/commands/init/index.ts
var import_error_utils9 = __toESM(require_dist(), 1);

// src/util/telemetry/commands/init/index.ts
var InitTelemetryClient = class extends TelemetryClient {
  trackCliArgumentExample(v, knownValue) {
    if (v) {
      this.trackCliArgument({
        arg: "example",
        value: knownValue ? v : this.redactedValue
      });
    }
  }
  trackCliArgumentDir(v) {
    if (v) {
      this.trackCliArgument({
        arg: "dir",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagForce(v) {
    if (v) {
      this.trackCliFlag("force");
    }
  }
};

// src/commands/init/index.ts
var COMMAND_CONFIG26 = {
  init: ["init"]
};
async function main19(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(initCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new InitTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("init");
    output_manager_default.print(help(initCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const args = getSubcommand5(parsedArgs.args.slice(1), COMMAND_CONFIG26).args;
  if (parsedArgs.args.length > 3) {
    output_manager_default.error("Too many arguments.");
    return 1;
  }
  telemetry2.trackCliArgumentDir(parsedArgs.args[2]);
  telemetry2.trackCliFlagForce(parsedArgs.flags["--force"]);
  try {
    return await init(client, parsedArgs.flags, args, telemetry2);
  } catch (err) {
    output_manager_default.prettyError(err);
    if ((0, import_error_utils9.isError)(err) && typeof err.stack === "string") {
      output_manager_default.debug(err.stack);
    }
    return 1;
  }
}

// src/commands/inspect/index.ts
var import_error_utils10 = __toESM(require_dist(), 1);
var import_chalk123 = __toESM(require_source(), 1);
var import_ms16 = __toESM(require_ms(), 1);
var import_title2 = __toESM(require_lib2(), 1);
import { URL as URL2 } from "url";

// src/util/output/builds.ts
var import_chalk121 = __toESM(require_source(), 1);
var import_bytes4 = __toESM(require_bytes(), 1);

// src/util/build-state.ts
var isReady = ({ readyState }) => readyState === "READY";
var isFailed = ({ readyState }) => readyState.endsWith("_ERROR") || readyState === "ERROR";

// src/util/output/builds.ts
var padding = 8;
var MAX_BUILD_GROUPS = 5;
var MAX_OUTPUTS_PER_GROUP = 5;
var hasOutput = (b) => Array.isArray(b.output) && b.output.length > 0;
var getCommonPath = (buildGroup) => {
  const commonPath = [];
  const splits = buildGroup.map(
    (build) => getDirPath(build.entrypoint).split("/")
  );
  const shortest = splits.reduce(
    (prevValue, currentValue) => Math.min(prevValue, currentValue.length),
    Infinity
  );
  for (let i = 0; i <= shortest; i++) {
    const first = splits[0][i];
    if (splits.every((pathParts) => pathParts[i] === first)) {
      commonPath.push(first);
      continue;
    }
    break;
  }
  return commonPath.join("/") || "/";
};
var styleBuild = (build, times, longestSource) => {
  const { entrypoint, id } = build;
  const time = typeof times[id] === "string" ? times[id] : "";
  let pathColor = import_chalk121.default.cyan;
  if (isFailed(build)) {
    pathColor = import_chalk121.default.red;
  }
  const entry = entrypoint.padEnd(longestSource + padding);
  const prefix = hasOutput(build) ? "\u250C" : "\u2576";
  return `${import_chalk121.default.grey(prefix)} ${pathColor(entry)}${time}`;
};
var styleHiddenBuilds = (commonPath, buildGroup, times, longestSource, isHidden = false) => {
  const { id } = buildGroup[0];
  const entry = commonPath.padEnd(longestSource + padding);
  const time = typeof times[id] === "string" ? times[id] : "";
  const prefix = isHidden === false && buildGroup.some(hasOutput) ? "\u250C" : "\u2576";
  let pathColor = import_chalk121.default.cyan;
  if (buildGroup.every(isFailed)) {
    pathColor = import_chalk121.default.red;
  }
  if (isHidden) {
    pathColor = import_chalk121.default.grey;
  }
  return `${import_chalk121.default.grey(prefix)} ${pathColor(entry)}${time}`;
};
var styleOutput = (output, readyState, isLast) => {
  const { type, path: path3, size, lambda } = output;
  const prefix = type === "lambda" ? "\u03BB " : "";
  const finalSize = size ? ` ${import_chalk121.default.grey(`(${(0, import_bytes4.default)(size)})`)}` : "";
  let color = import_chalk121.default.grey;
  let finalRegion = "";
  if (isReady({ readyState })) {
    color = import_chalk121.default;
  } else if (isFailed({ readyState })) {
    color = import_chalk121.default.red;
  }
  if (lambda) {
    const { deployedTo } = lambda;
    if (deployedTo && deployedTo.length > 0) {
      finalRegion = ` ${import_chalk121.default.grey(`[${deployedTo.join(", ")}]`)}`;
    }
  }
  const corner = isLast ? "\u2514\u2500\u2500" : "\u251C\u2500\u2500";
  const main31 = prefix + path3 + finalSize + finalRegion;
  return `${import_chalk121.default.grey(corner)} ${color(main31)}`;
};
var getDirPath = (path3, level = 0, highestLevel = null) => {
  const parts = path3.split("/").slice(0, -1);
  if (highestLevel === null || level === 0) {
    return parts.join("/");
  }
  const reverseLevel = highestLevel - level;
  return parts.slice(0, reverseLevel).join("/");
};
var sortByEntrypoint = (a, b) => {
  const aPath = getDirPath(a.entrypoint);
  const bPath = getDirPath(b.entrypoint);
  if (aPath === "") {
    return 1;
  }
  if (bPath === "") {
    return -1;
  }
  if (aPath > bPath) {
    return 1;
  }
  if (bPath > aPath) {
    return -1;
  }
  return 0;
};
var groupBuilds = (buildList, highestLevel, counter) => {
  const currentIndex = counter % buildList.length;
  const __level = Math.ceil(counter / buildList.length);
  const _level = (__level === 0 ? 1 : __level) - 1;
  const level = _level > highestLevel ? highestLevel : _level;
  const currentPath = getDirPath(
    buildList[currentIndex][0].entrypoint,
    level,
    highestLevel
  );
  const nextList = [];
  const currentGroup = [];
  for (let i = 0; i < buildList.length; i++) {
    const group = buildList[i];
    const path3 = getDirPath(group[0].entrypoint, level, highestLevel);
    if (path3 === currentPath) {
      currentGroup.push(...group);
    } else {
      nextList.push(group);
    }
  }
  if (currentIndex === 0) {
    nextList.unshift(currentGroup);
  } else {
    nextList.splice(currentIndex, 0, currentGroup);
  }
  return nextList;
};
var builds_default = (builds, times) => {
  let path3 = builds.sort(sortByEntrypoint).map((build) => [build]);
  const highestLevel = builds.reduce((prev, curr) => {
    const partCounter = curr.entrypoint.split("/").length - 1;
    return partCounter > prev ? partCounter : prev;
  }, 0);
  let counter = 0;
  while (path3.length > MAX_BUILD_GROUPS) {
    path3 = groupBuilds(path3, highestLevel, counter);
    counter++;
  }
  path3 = path3.reverse();
  const longestSource = builds.reduce((final2, current) => {
    const { length } = current.entrypoint;
    return length > final2 ? length : final2;
  }, 0);
  const final = [];
  let finalBuildsLength = path3.length;
  let lengthWithoutRootPaths = path3.length;
  const hiddenBuildGroup = [];
  path3 = (() => {
    const nextList = [];
    const rootList = [];
    for (const group of path3) {
      if (getCommonPath(group) === "/") {
        group.map((item) => rootList.push([item]));
      } else {
        nextList.push(group);
      }
    }
    lengthWithoutRootPaths = nextList.length;
    rootList.map((group) => nextList.push(group));
    return nextList;
  })();
  for (const buildGroup of path3) {
    const commonPath = getCommonPath(buildGroup);
    if (commonPath === "/") {
      if (lengthWithoutRootPaths <= MAX_BUILD_GROUPS && finalBuildsLength <= MAX_BUILD_GROUPS) {
        const item = buildGroup[0];
        final.push(`${styleBuild(item, times, longestSource)}
`);
        finalBuildsLength++;
      } else {
        hiddenBuildGroup.push(buildGroup[0]);
        continue;
      }
    } else if (buildGroup.length === 1) {
      const item = buildGroup[0];
      final.push(`${styleBuild(item, times, longestSource)}
`);
      finalBuildsLength++;
    } else {
      final.push(
        `${styleHiddenBuilds(
          `${commonPath}/*`,
          buildGroup,
          times,
          longestSource
        )}
`
      );
      finalBuildsLength++;
    }
    const outputs = [];
    for (const build of buildGroup) {
      if (Array.isArray(build.output)) {
        outputs.push(...build.output);
      }
    }
    outputs.slice(0, MAX_OUTPUTS_PER_GROUP).forEach(
      (output, index) => final.push(
        `${styleOutput(output, "READY", outputs.length === index + 1)}
`
      )
    );
    if (outputs.length > MAX_OUTPUTS_PER_GROUP) {
      final.push(
        import_chalk121.default.grey(
          `\u2514\u2500\u2500 ${outputs.length - MAX_OUTPUTS_PER_GROUP} output items hidden
`
        )
      );
    }
  }
  if (hiddenBuildGroup.length) {
    final.push(
      `${styleHiddenBuilds(
        `${hiddenBuildGroup.length} builds hidden`,
        hiddenBuildGroup,
        times,
        longestSource,
        true
      )}
`
    );
  }
  return {
    lines: final.length + 1,
    toPrint: `${final.join("")}`
  };
};

// src/util/output/routes.ts
var import_chalk122 = __toESM(require_source(), 1);
var longestProperty = (routes2, name) => {
  const longestItem = routes2.sort((a, b) => {
    const aName = a[name];
    const bName = b[name];
    const firstItem = typeof aName === "string" ? aName.length : 0;
    const secondItem = typeof bName === "string" ? bName.length : 0;
    return secondItem - firstItem;
  })[0];
  const val = longestItem[name];
  return typeof val === "string" ? val.length : 0;
};
function routes(routes2) {
  let toPrint = "";
  const longestSrc = longestProperty(routes2, "src");
  const longestDest = longestProperty(routes2, "dest");
  const padding2 = 6;
  const space = " ".repeat(padding2);
  const destSpace = " ".repeat(longestDest || 10);
  const arrow = import_chalk122.default.grey("->");
  for (const item of routes2) {
    if ("handle" in item) {
      toPrint += `${import_chalk122.default.grey("\u2576")} ${import_chalk122.default.cyan(item.handle)}`;
      continue;
    }
    const { src, dest, status: status3, headers } = item;
    const last = routes2.indexOf(item) === routes2.length - 1;
    const suffix = last ? "" : `
`;
    const finalSrc = import_chalk122.default.cyan(src.padEnd(longestSrc + padding2));
    const finalDest = dest ? `${arrow}${space}${dest}` : `  ${space}${destSpace}`;
    const finalStatus = status3 ? import_chalk122.default.grey(`[${status3}]`) : "";
    let finalHeaders = null;
    if (headers) {
      finalHeaders = `
`;
      const headerKeys = Object.keys(headers);
      for (const header of headerKeys) {
        const value = headers[header];
        const last2 = headerKeys.indexOf(header) === headerKeys.length - 1;
        const suffix2 = last2 ? "" : `
`;
        const prefix2 = import_chalk122.default.grey(last2 ? "\u2514\u2500\u2500" : "\u251C\u2500\u2500");
        finalHeaders += `${prefix2} ${header}: ${value}${suffix2}`;
      }
    }
    const prefix = import_chalk122.default.grey(finalHeaders ? "\u250C" : "\u2576");
    const fill = `${finalSrc}${finalDest}${space}${finalStatus}`;
    toPrint += `${prefix} ${fill}${finalHeaders || ""}${suffix}`;
  }
  return toPrint;
}

// src/util/telemetry/commands/inspect/index.ts
var InspectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrDeploymentId(idOrHost) {
    if (idOrHost) {
      this.trackCliArgument({
        arg: "deploymentIdOrHost",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionTimeout(timeout) {
    if (timeout) {
      this.trackCliOption({
        option: "timeout",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagLogs(logs2) {
    if (logs2) {
      this.trackCliFlag("logs");
    }
  }
  trackCliFlagWait(wait) {
    if (wait) {
      this.trackCliFlag("wait");
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
};

// src/commands/inspect/index.ts
async function inspect4(client) {
  const { print, error, warn } = output_manager_default;
  const telemetry2 = new InspectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(inspectCommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  if (parsedArguments.flags["--help"]) {
    telemetry2.trackCliFlagHelp("inspect");
    print(help(inspectCommand, { columns: client.stderr.columns }));
    return 2;
  }
  if (parsedArguments.args[0] === inspectCommand.name) {
    parsedArguments.args.shift();
  }
  let [deploymentIdOrHost] = parsedArguments.args;
  if (!deploymentIdOrHost) {
    const stdInput = await readStandardInput(client.stdin);
    if (stdInput) {
      deploymentIdOrHost = stdInput;
    }
  }
  if (!deploymentIdOrHost) {
    error(`${getCommandName("inspect <url>")} expects exactly one argument`);
    print(help(inspectCommand, { columns: client.stderr.columns }));
    return 1;
  }
  telemetry2.trackCliArgumentUrlOrDeploymentId(deploymentIdOrHost);
  telemetry2.trackCliOptionTimeout(parsedArguments.flags["--timeout"]);
  telemetry2.trackCliFlagLogs(parsedArguments.flags["--logs"]);
  telemetry2.trackCliFlagWait(parsedArguments.flags["--wait"]);
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  telemetry2.trackCliFlagJson(parsedArguments.flags["--json"]);
  const timeout = (0, import_ms16.default)(parsedArguments.flags["--timeout"] ?? "3m");
  if (timeout === void 0) {
    error(`Invalid timeout "${parsedArguments.flags["--timeout"]}"`);
    return 1;
  }
  let contextName = null;
  try {
    ({ contextName } = await getScope(client));
  } catch (err) {
    if ((0, import_error_utils10.isErrnoException)(err) && (err.code === "NOT_AUTHORIZED" || err.code === "TEAM_DELETED")) {
      error(err.message);
      return 1;
    }
    throw err;
  }
  const until = Date.now() + timeout;
  const wait = parsedArguments.flags["--wait"] ?? false;
  const withLogs = parsedArguments.flags["--logs"];
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const startTimestamp = Date.now();
  try {
    deploymentIdOrHost = new URL2(deploymentIdOrHost).hostname;
  } catch {
  }
  output_manager_default.spinner(
    `Fetching deployment "${deploymentIdOrHost}" in ${import_chalk123.default.bold(contextName)}`
  );
  let deployment = await getDeployment(client, contextName, deploymentIdOrHost);
  let abortController;
  if (withLogs && !asJson) {
    let promise;
    ({ abortController, promise } = displayBuildLogs(client, deployment, wait));
    if (wait) {
      promise.catch((error2) => warn(`Failed to read build logs: ${error2}`));
    } else {
      await promise;
    }
  }
  while (wait) {
    await sleep(250);
    deployment = await getDeployment(client, contextName, deploymentIdOrHost);
    if (!isDeploying(deployment.readyState)) {
      abortController?.abort();
      break;
    }
    if (Date.now() > until) {
      warn(`stopped waiting after ${(0, import_ms16.default)(timeout, { long: true })}`);
      abortController?.abort();
      break;
    }
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    await printJson({ deployment, contextName, client });
  } else if (withLogs) {
    print(`${import_chalk123.default.cyan("status")}	${stateString(deployment.readyState)}
`);
  } else {
    await printDetails({ deployment, contextName, client, startTimestamp });
  }
  return exitCode(deployment.readyState);
}
function stateString(s) {
  const CIRCLE = "\u25CF ";
  const sTitle = s && (0, import_title2.default)(s);
  switch (s) {
    case "INITIALIZING":
    case "BUILDING":
      return import_chalk123.default.yellow(CIRCLE) + sTitle;
    case "ERROR":
      return import_chalk123.default.red(CIRCLE) + sTitle;
    case "READY":
      return import_chalk123.default.green(CIRCLE) + sTitle;
    case "QUEUED":
      return import_chalk123.default.gray(CIRCLE) + sTitle;
    case "CANCELED":
      return import_chalk123.default.gray(CIRCLE) + sTitle;
    default:
      return import_chalk123.default.gray("UNKNOWN");
  }
}
async function printDetails({
  deployment,
  contextName,
  client,
  startTimestamp
}) {
  output_manager_default.log(
    `Fetched deployment "${import_chalk123.default.bold(deployment.url)}" in ${import_chalk123.default.bold(
      contextName
    )} ${elapsed(Date.now() - startTimestamp)}`
  );
  const {
    id,
    name,
    url,
    createdAt,
    routes: routes2,
    readyState,
    alias: aliases
  } = deployment;
  const { print, link } = output_manager_default;
  const { builds } = deployment.version === 2 ? await client.fetch(`/v11/deployments/${id}/builds`) : { builds: [] };
  print("\n");
  print(import_chalk123.default.bold("  General\n\n"));
  print(`    ${import_chalk123.default.cyan("id")}		${id}
`);
  print(`    ${import_chalk123.default.cyan("name")}	${name}
`);
  const customEnvironmentSlug = deployment.customEnvironment?.slug;
  const target = customEnvironmentSlug ?? deployment.target ?? "preview";
  print(`    ${import_chalk123.default.cyan("target")}	`);
  print(
    deployment.customEnvironment && deployment.team?.slug ? `${link(
      `${target}`,
      `https://vercel.com/${deployment.team.slug}/${name}/settings/environments/${deployment.customEnvironment.id}`,
      { fallback: () => target, color: false }
    )}
` : `${target}
`
  );
  print(`    ${import_chalk123.default.cyan("status")}	${stateString(readyState)}
`);
  print(`    ${import_chalk123.default.cyan("url")}		https://${url}
`);
  if (createdAt) {
    print(
      `    ${import_chalk123.default.cyan("created")}	${new Date(createdAt)} ${elapsed(
        Date.now() - createdAt,
        true
      )}
`
    );
  }
  print("\n\n");
  if (aliases !== void 0 && aliases.length > 0) {
    print(import_chalk123.default.bold("  Aliases\n\n"));
    let aliasList = "";
    for (const alias2 of aliases) {
      aliasList += `${import_chalk123.default.gray("\u2576")} https://${alias2}
`;
    }
    print(indent_default(aliasList, 4));
    print("\n\n");
  }
  if (builds.length > 0) {
    const times = {};
    for (const build of builds) {
      const { id: id2, createdAt: createdAt2, readyStateAt } = build;
      times[id2] = createdAt2 && readyStateAt ? elapsed(readyStateAt - createdAt2) : null;
    }
    print(import_chalk123.default.bold("  Builds\n\n"));
    print(indent_default(builds_default(builds, times).toPrint, 4));
    print("\n\n");
  }
  if (Array.isArray(routes2) && routes2.length > 0) {
    print(import_chalk123.default.bold("  Routes\n\n"));
    print(indent_default(routes(routes2), 4));
    print(`

`);
  }
}
async function printJson({
  deployment,
  contextName,
  client
}) {
  const {
    id,
    name,
    url,
    createdAt,
    routes: routes2,
    readyState,
    alias: aliases,
    target,
    customEnvironment
  } = deployment;
  const { builds } = deployment.version === 2 ? await client.fetch(`/v11/deployments/${id}/builds`) : { builds: [] };
  const jsonOutput = {
    id,
    name,
    url,
    target: customEnvironment?.slug ?? target ?? "preview",
    readyState,
    createdAt,
    ...aliases && aliases.length > 0 && { aliases },
    ...builds.length > 0 && { builds },
    ...Array.isArray(routes2) && routes2.length > 0 && { routes: routes2 },
    ...contextName && { contextName }
  };
  client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
}
function exitCode(state) {
  if (state === "ERROR" || state === "CANCELED") {
    return 1;
  }
  return 0;
}

// src/commands/integration/add-auto-provision.ts
var import_chalk127 = __toESM(require_source(), 1);
var import_error_utils11 = __toESM(require_dist(), 1);
var import_open12 = __toESM(require_open(), 1);

// src/util/integration/auto-provision-resource.ts
function isAutoProvisionFallback(error) {
  return typeof error === "object" && error !== null && "kind" in error && error.kind !== "provisioned" && "url" in error && "integration" in error && "product" in error;
}
async function autoProvisionResource(client, integrationSlug, productSlug, name, metadata, acceptedPolicies, billingPlanId, installationId, installationMetadata) {
  const endpoint = `/v1/integrations/integration/${encodeURIComponent(integrationSlug)}/marketplace/auto-provision/${encodeURIComponent(productSlug)}`;
  const body = {
    name,
    metadata,
    acceptedPolicies,
    source: "cli",
    ...billingPlanId ? { billingPlanId } : {},
    ...installationId ? { installationId } : {},
    ...installationMetadata && Object.keys(installationMetadata).length > 0 ? { installationMetadata } : {}
  };
  output_manager_default.debug(`Auto-provision request: POST ${endpoint}`);
  output_manager_default.debug(`Auto-provision body: ${JSON.stringify(body, null, 2)}`);
  try {
    const res = await client.fetch(endpoint, {
      method: "POST",
      json: false,
      body
    });
    if (res.ok) {
      return res.json();
    }
    throw new Error(`Auto-provision failed: ${res.status}`);
  } catch (error) {
    if (error instanceof APIError && error.status === 422 && isAutoProvisionFallback(error)) {
      output_manager_default.debug(`Auto-provision returned 422 fallback response`);
      return error;
    }
    output_manager_default.debug(`Auto-provision error: ${error}`);
    throw error;
  }
}

// src/util/integration/fetch-integration.ts
var import_chalk124 = __toESM(require_source(), 1);

// src/util/integration/fetch-marketplace-integrations-list.ts
async function fetchMarketplaceIntegrationsList(client) {
  return client.fetch(
    "/v2/integrations/integrations?integrationType=marketplace",
    {
      json: true
    }
  );
}

// src/util/integration/fetch-integration.ts
async function fetchIntegration(client, slug) {
  return client.fetch(`/v2/integrations/integration/${slug}`, {
    json: true
  });
}
function toDiscoverEntries(integrations) {
  const entries = [];
  for (const integration of integrations) {
    if (!integration.isMarketplace || !integration.canInstall) {
      continue;
    }
    const integrationTags = integration.tagIds ?? [];
    const products = integration.products ?? [];
    if (products.length === 0) {
      entries.push({
        name: integration.name,
        slug: integration.slug,
        provider: integration.name,
        description: integration.shortDescription ?? "",
        tags: integrationTags
      });
      continue;
    }
    for (const product of products) {
      const needsCompoundSlug = products.length > 1 || product.slug !== integration.slug;
      entries.push({
        name: product.name,
        slug: needsCompoundSlug ? `${integration.slug}/${product.slug}` : integration.slug,
        provider: integration.name,
        description: product.shortDescription ?? integration.shortDescription ?? "",
        tags: [...integrationTags, ...product.tags ?? []]
      });
    }
  }
  return entries;
}
function matchesSearchTerm(entry, term) {
  const lower = term.toLowerCase();
  return entry.name.toLowerCase().includes(lower) || entry.slug.toLowerCase().includes(lower) || entry.provider.toLowerCase().includes(lower) || entry.description.toLowerCase().includes(lower) || entry.tags.some((tag) => tag.toLowerCase().includes(lower));
}
async function resolveAndFetchIntegration(client, slug, telemetry2) {
  let directError;
  try {
    const integration = await fetchIntegration(client, slug);
    telemetry2.trackCliArgumentIntegration(slug, true);
    return integration;
  } catch (error) {
    directError = error;
  }
  output_manager_default.spinner("Searching marketplace integrations...", 500);
  let entries;
  try {
    const integrations = await fetchMarketplaceIntegrationsList(client);
    entries = toDiscoverEntries(integrations);
  } catch (discoverError) {
    output_manager_default.stopSpinner();
    output_manager_default.error(
      `Failed to get integration "${slug}": ${directError?.message ?? discoverError.message}`
    );
    telemetry2.trackCliArgumentIntegration(slug, false);
    return null;
  }
  output_manager_default.stopSpinner();
  const matches = entries.filter((entry) => matchesSearchTerm(entry, slug));
  if (!matches.length) {
    output_manager_default.error(
      `No integration found matching "${slug}". Run ${import_chalk124.default.cyan("vercel integration discover")} to browse available integrations.`
    );
    telemetry2.trackCliArgumentIntegration(slug, false);
    return null;
  }
  if (matches.length === 1) {
    const match = matches[0];
    if (client.stdin.isTTY === true) {
      const confirmed = await client.input.confirm(
        `Install ${import_chalk124.default.bold(match.name)} (${match.slug})?`,
        true
      );
      if (!confirmed) {
        telemetry2.trackCliArgumentIntegration(slug, false);
        return null;
      }
    }
    slug = match.slug;
  } else if (client.stdin.isTTY !== true) {
    output_manager_default.error(
      `Found ${matches.length} integrations matching "${slug}". Available integrations:
${matches.map((m) => `- ${m.slug}: ${m.description}`).join("\n")}`
    );
    telemetry2.trackCliArgumentIntegration(slug, false);
    return null;
  } else {
    slug = await client.input.select({
      message: `Found ${matches.length} integrations matching "${slug}". Pick one to install:`,
      choices: matches.map((m) => ({
        name: `${m.name} (${m.slug})${m.description ? ` - ${m.description}` : ""}`,
        value: m.slug
      }))
    });
  }
  let integrationSlug;
  let productSlug;
  const slashIndex = slug.indexOf("/");
  if (slashIndex !== -1) {
    integrationSlug = slug.substring(0, slashIndex);
    productSlug = slug.substring(slashIndex + 1);
  } else {
    integrationSlug = slug;
  }
  try {
    const integration = await fetchIntegration(client, integrationSlug);
    telemetry2.trackCliArgumentIntegration(integrationSlug, true);
    return { ...integration, productSlug };
  } catch (error) {
    output_manager_default.error(
      `Failed to get integration "${integrationSlug}": ${error.message}`
    );
    telemetry2.trackCliArgumentIntegration(integrationSlug, false);
    return null;
  }
}

// src/util/integration/fetch-installations.ts
async function fetchInstallations(client, integration) {
  const searchParams = new URLSearchParams();
  searchParams.set("view", "account");
  searchParams.set("installationType", "marketplace");
  searchParams.set("integrationIdOrSlug", integration.id);
  return client.fetch(
    `/v2/integrations/configurations?${searchParams}`,
    {
      json: true
    }
  );
}

// src/util/integration/accept-terms-via-browser.ts
var import_open10 = __toESM(require_open(), 1);

// src/util/integration/prompt-for-terms.ts
var MARKETPLACE_ADDENDUM_URL = "https://vercel.com/legal/integration-marketplace-end-users-addendum";
function getMarketplacePolicyLinks(integration) {
  const links = {
    marketplace_addendum: MARKETPLACE_ADDENDUM_URL
  };
  if (integration.privacyDocUri) {
    links.integration_privacy_policy = integration.privacyDocUri;
  }
  if (integration.eulaDocUri) {
    links.integration_eula = integration.eulaDocUri;
  }
  return links;
}
async function promptForTermAcceptance(client, integration) {
  if (client.isAgent) {
    output_manager_default.error(
      "Term acceptance cannot be performed by an AI agent. Run this command directly in your terminal."
    );
    return null;
  }
  if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Term acceptance requires an interactive terminal. Run this command in a TTY."
    );
    return null;
  }
  const addendumAccepted = await client.input.confirm(
    `Accept Vercel Marketplace End User Addendum? (${MARKETPLACE_ADDENDUM_URL})`,
    false
  );
  if (!addendumAccepted) {
    output_manager_default.error(
      "Vercel Marketplace End User Addendum must be accepted to continue."
    );
    return null;
  }
  const acceptedPolicies = {
    toc: (/* @__PURE__ */ new Date()).toISOString()
  };
  if (integration.privacyDocUri) {
    const accepted = await client.input.confirm(
      `Accept privacy policy? (${integration.privacyDocUri})`,
      false
    );
    if (!accepted) {
      output_manager_default.error("Privacy policy must be accepted to continue.");
      return null;
    }
    acceptedPolicies.privacy = (/* @__PURE__ */ new Date()).toISOString();
  }
  if (integration.eulaDocUri) {
    const accepted = await client.input.confirm(
      `Accept terms of service? (${integration.eulaDocUri})`,
      false
    );
    if (!accepted) {
      output_manager_default.error("Terms of service must be accepted to continue.");
      return null;
    }
    acceptedPolicies.eula = (/* @__PURE__ */ new Date()).toISOString();
  }
  return acceptedPolicies;
}

// src/util/integration/accept-terms-via-browser.ts
var POLL_INTERVAL_MS2 = 2e3;
var DEFAULT_TIMEOUT_MS = 5 * 60 * 1e3;
async function acceptTermsViaBrowser(client, integration, teamId, teamSlug, timeoutMs = DEFAULT_TIMEOUT_MS) {
  const url = new URL(
    `https://vercel.com/${encodeURIComponent(teamSlug)}/~/integrations/accept-terms/${encodeURIComponent(integration.slug)}`
  );
  url.searchParams.set("source", "cli");
  if (shouldEmitNonInteractiveCommandError(client)) {
    const tail = buildIntegrationCommandTailFromArgv(client.argv);
    const policyLinks = getMarketplacePolicyLinks(integration);
    outputActionRequired(
      client,
      {
        status: "action_required",
        reason: AGENT_REASON.INTEGRATION_TERMS_ACCEPTANCE_REQUIRED,
        message: `Accept marketplace terms for "${integration.name}" in your browser before this install can finish. Open verification_uri to start. This command does not wait for acceptance in non-interactive mode.`,
        verification_uri: url.href,
        policy_links: policyLinks,
        userActionRequired: true,
        hint: `Read policy_links, complete verification_uri in the browser, then retry install from next[]. Confirm with ${packageName} integration installations.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              tail,
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Retry install after terms are accepted (browser or dashboard)"
          }
        ]
      },
      1
    );
  }
  output_manager_default.log(
    "Opening browser for terms acceptance. Accept the terms to continue..."
  );
  output_manager_default.log(`Visit this URL if the browser does not open: ${url.href}`);
  try {
    await (0, import_open10.default)(url.href);
  } catch (err) {
    output_manager_default.debug(`Failed to open browser: ${err}`);
  }
  output_manager_default.spinner("Waiting for terms acceptance in browser...");
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    await sleep(POLL_INTERVAL_MS2);
    try {
      const installations = await fetchInstallations(client, integration);
      const teamInstallation = installations.find(
        (i) => i.ownerId === teamId && i.installationType === "marketplace"
      );
      if (teamInstallation) {
        output_manager_default.stopSpinner();
        output_manager_default.success("Terms accepted in browser.");
        return teamInstallation;
      }
    } catch (error) {
      output_manager_default.debug(`Polling error (will retry): ${error}`);
    }
  }
  output_manager_default.stopSpinner();
  output_manager_default.error(
    "Timed out waiting for terms acceptance. Please try again and accept terms in the browser within 5 minutes."
  );
  return null;
}

// src/util/integration/select-product.ts
async function selectProduct(client, products, productSlug) {
  if (productSlug) {
    const match = products.find((p) => p.slug === productSlug);
    if (!match) {
      const available = products.map((p) => p.slug).join(", ");
      output_manager_default.error(
        `Product "${productSlug}" not found. Available products: ${available}`
      );
      return;
    }
    return match;
  }
  if (products.length === 1) {
    return products[0];
  }
  return client.input.select({
    message: "Select a product",
    choices: products.map((p) => ({
      name: p.name,
      value: p,
      description: p.shortDescription
    }))
  });
}

// src/commands/integration-resource/claim.ts
var import_chalk125 = __toESM(require_source(), 1);
var import_open11 = __toESM(require_open(), 1);

// src/util/integration-resource/get-resources.ts
async function getResources(client) {
  const storesResponse = await client.fetch(
    `/v1/storage/stores`,
    {
      json: true
    }
  );
  return storesResponse.stores;
}

// src/util/integration-resource/create-claim-url.ts
var ClaimUrlError = class extends Error {
  constructor(message, code2, status3) {
    super(message);
    this.code = code2;
    this.status = status3;
  }
};
async function createClaimUrl(client, resource) {
  const installationId = resource.product?.integrationConfigurationId;
  if (!installationId) {
    throw new ClaimUrlError(
      `Resource "${resource.name}" is not associated with a marketplace installation and cannot be claimed.`,
      "not_marketplace"
    );
  }
  try {
    return await client.fetch(
      `/v1/integrations/installations/${encodeURIComponent(installationId)}/resources/${encodeURIComponent(resource.id)}/sandbox/claim-url`,
      {
        method: "POST",
        json: true,
        body: { source: "cli" }
      }
    );
  } catch (error) {
    throw mapClaimUrlError(error, resource);
  }
}
function mapClaimUrlError(error, resource) {
  if (!isAPIError(error)) {
    if (error instanceof Error) {
      return new ClaimUrlError(
        `Provider could not generate a claim URL: ${error.message}`,
        "unknown"
      );
    }
    return new ClaimUrlError(
      "Provider could not generate a claim URL.",
      "unknown"
    );
  }
  const status3 = error.status;
  const serverMessage = (error.serverMessage || error.message || "").toString();
  const messageLower = serverMessage.toLowerCase();
  if (status3 === 403) {
    return new ClaimUrlError(
      "You don't have permission to claim resources in this team.",
      "forbidden",
      status3
    );
  }
  if (status3 === 404) {
    if (messageLower.includes("installation")) {
      return new ClaimUrlError(
        "Integration installation not found. Run `vercel integration list`.",
        "installation_not_found",
        status3
      );
    }
    return new ClaimUrlError(
      `No resource named '${resource.name}' found.`,
      "resource_not_found",
      status3
    );
  }
  if (status3 === 400) {
    if (messageLower.includes("sandbox") || messageLower.includes("ownership")) {
      return new ClaimUrlError(
        `'${resource.name}' can no longer be claimed (likely already claimed). Run \`${packageName} integration list\` to refresh.`,
        "not_sandbox",
        status3
      );
    }
    if (messageLower.includes("marketplace") || messageLower.includes("integration")) {
      return new ClaimUrlError(
        "This integration doesn't support claiming.",
        "not_marketplace",
        status3
      );
    }
  }
  return new ClaimUrlError(
    `Provider could not generate a claim URL: ${serverMessage || "unknown error"}`,
    "partner_error",
    status3
  );
}

// src/util/integration-resource/poll-for-claim.ts
var import_ms17 = __toESM(require_ms(), 1);

// src/util/integration-resource/get-resource.ts
async function getResource(client, resourceId) {
  const { store } = await client.fetch(
    `/v1/storage/stores/${encodeURIComponent(resourceId)}`,
    { json: true }
  );
  return store;
}

// src/util/integration-resource/poll-for-claim.ts
var POLL_INTERVAL_MS3 = 2e3;
var DEFAULT_TIMEOUT_MS2 = 5 * 60 * 1e3;
var SANDBOX_CLAIM_IN_PROGRESS_MSG = "Claim still in progress \u2014 finish in your browser, then run `vercel integration list` to verify.";
async function pollForClaim(client, resourceId, options = {}) {
  const timeoutMs = options.timeoutMs ?? DEFAULT_TIMEOUT_MS2;
  let cancelled = false;
  let resolveCancelled;
  const cancelledSignal = new Promise((resolve8) => {
    resolveCancelled = resolve8;
  });
  const onSigint = () => {
    cancelled = true;
    resolveCancelled?.();
  };
  process.on("SIGINT", onSigint);
  try {
    output_manager_default.spinner("Waiting for claim to complete in browser...");
    const deadline = Date.now() + timeoutMs;
    while (Date.now() < deadline && !cancelled) {
      await Promise.race([sleep(POLL_INTERVAL_MS3), cancelledSignal]);
      if (cancelled)
        break;
      try {
        const updated = await getResource(client, resourceId);
        if (updated.ownership && updated.ownership !== "sandbox") {
          output_manager_default.stopSpinner();
          return { status: "claimed", resource: updated };
        }
      } catch (error) {
        output_manager_default.debug(`Polling error (will retry): ${error}`);
      }
    }
    output_manager_default.stopSpinner();
    if (cancelled) {
      output_manager_default.print("\n");
      output_manager_default.log(SANDBOX_CLAIM_IN_PROGRESS_MSG);
      return { status: "cancelled" };
    }
    output_manager_default.error(
      `Claim did not complete within ${(0, import_ms17.default)(timeoutMs, { long: true })}.`
    );
    output_manager_default.log(SANDBOX_CLAIM_IN_PROGRESS_MSG);
    return { status: "timeout" };
  } finally {
    process.off("SIGINT", onSigint);
  }
}

// src/util/integration-resource/claim-status.ts
function isSandboxResource(resource) {
  return resource.ownership === "sandbox";
}

// src/util/telemetry/commands/integration-resource/claim.ts
var IntegrationResourceClaimTelemetryClient = class extends TelemetryClient {
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
  trackCliFlagNoWait(v) {
    if (v) {
      this.trackCliFlag("no-wait");
    }
  }
};

// src/commands/integration-resource/claim.ts
async function claim(client, argv) {
  const telemetry2 = new IntegrationResourceClaimTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(claimSubcommand.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!parsedArguments.flags["--yes"];
  const noWait = !!parsedArguments.flags["--no-wait"];
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  telemetry2.trackCliFlagYes(skipConfirmation);
  telemetry2.trackCliFlagNoWait(noWait);
  if (parsedArguments.args.length > 1) {
    output_manager_default.error(
      "Too many arguments. Usage: `vercel integration-resource claim [name]`."
    );
    return 1;
  }
  const resourceNameArg = parsedArguments.args[0];
  telemetry2.trackCliArgumentResource(resourceNameArg);
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  output_manager_default.spinner("Retrieving resources\u2026", 500);
  let allResources;
  try {
    allResources = await getResources(client);
  } catch (error) {
    output_manager_default.stopSpinner();
    output_manager_default.error(`Failed to fetch resources: ${error.message}`);
    return 1;
  }
  output_manager_default.stopSpinner();
  let targetResource;
  if (resourceNameArg) {
    targetResource = allResources.find((r) => r.name === resourceNameArg);
    if (!targetResource) {
      output_manager_default.error(`No resource named '${resourceNameArg}' found.`);
      return 1;
    }
  } else {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "Missing resource name. Run `vercel integration list` to see available resources."
      );
      return 1;
    }
    const sandboxResources = allResources.filter(isSandboxResource);
    if (sandboxResources.length === 0) {
      output_manager_default.log("No sandbox resources to claim in the current project.");
      return 0;
    }
    if (sandboxResources.length === 1) {
      const only = sandboxResources[0];
      if (skipConfirmation) {
        targetResource = only;
      } else {
        const productLabel = only.product?.name ? ` (${only.product.name} sandbox)` : "";
        const confirmed = await client.input.confirm(
          `Claim ${import_chalk125.default.bold(only.name)}${productLabel}?`,
          true
        );
        if (!confirmed) {
          output_manager_default.log("Canceled");
          return 0;
        }
        targetResource = only;
      }
    } else {
      const choice = await client.input.select({
        message: "Which sandbox resource would you like to claim?",
        choices: sandboxResources.map((r) => ({
          name: r.product?.name ? `${r.name} (${r.product.name})` : r.name,
          value: r.id
        }))
      });
      targetResource = sandboxResources.find((r) => r.id === choice);
      if (!targetResource) {
        output_manager_default.error("No resource selected.");
        return 1;
      }
    }
  }
  return runClaimForResource(client, targetResource, { asJson, noWait });
}
async function runClaimForResource(client, targetResource, options = {}) {
  const { asJson, noWait, suppressActionRequired } = options;
  if (!isSandboxResource(targetResource)) {
    const alreadyClaimed = targetResource.ownership === "owned" || targetResource.ownership === "linked";
    output_manager_default.error(
      alreadyClaimed ? `'${targetResource.name}' can no longer be claimed (already claimed). Run \`${packageName} integration list\` to refresh.` : `'${targetResource.name}' is not a sandbox resource and cannot be claimed.`
    );
    return 1;
  }
  output_manager_default.spinner("Requesting claim URL\u2026", 500);
  let claimUrl;
  try {
    const response = await createClaimUrl(client, targetResource);
    claimUrl = response.claimUrl;
  } catch (error) {
    output_manager_default.stopSpinner();
    if (error instanceof ClaimUrlError) {
      output_manager_default.error(error.message);
      return 1;
    }
    output_manager_default.error(
      `A problem occurred while requesting the claim URL: ${error.message}`
    );
    return 1;
  }
  output_manager_default.stopSpinner();
  if (!suppressActionRequired && shouldEmitNonInteractiveCommandError(client)) {
    outputActionRequired(
      client,
      {
        status: "action_required",
        reason: AGENT_REASON.INTEGRATION_SANDBOX_CLAIM_REQUIRED,
        message: `Claim "${targetResource.name}" in your browser to finish provisioning. Open verification_uri to start. This command does not wait for the claim in non-interactive mode.`,
        verification_uri: claimUrl,
        userActionRequired: true,
        hint: `Open verification_uri in a browser, complete the provider's claim flow, then run \`${packageName} integration list\` to confirm \`ownership\` flipped from sandbox to linked.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration list",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "After claiming in the browser, verify ownership flipped to linked"
          }
        ]
      },
      1
    );
  }
  if (noWait) {
    if (asJson) {
      const json = {
        resource: { id: targetResource.id, name: targetResource.name },
        claimUrl,
        status: "url_generated"
      };
      client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
    } else {
      output_manager_default.log(
        `Visit this URL to claim ${import_chalk125.default.bold(targetResource.name)}: ${claimUrl}`
      );
      output_manager_default.log(
        `Re-run \`${packageName} integration list\` after claiming to verify.`
      );
    }
    return 0;
  }
  if (!client.stdin.isTTY) {
    if (asJson) {
      const json = {
        resource: { id: targetResource.id, name: targetResource.name },
        claimUrl,
        status: "url_generated"
      };
      client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
    } else {
      output_manager_default.log(
        `Visit this URL to claim ${import_chalk125.default.bold(targetResource.name)}: ${claimUrl}`
      );
      output_manager_default.log(
        `Re-run \`${packageName} integration list\` after claiming to verify.`
      );
    }
    return suppressActionRequired ? 0 : 1;
  }
  output_manager_default.log(`Opening browser to claim ${import_chalk125.default.bold(targetResource.name)}\u2026`);
  output_manager_default.log(`Visit this URL if the browser does not open: ${claimUrl}`);
  try {
    await (0, import_open11.default)(claimUrl);
  } catch (err) {
    output_manager_default.debug(`Failed to open browser: ${err}`);
  }
  const result = await pollForClaim(client, targetResource.id);
  if (result.status === "cancelled") {
    return 130;
  }
  if (result.status === "timeout") {
    if (asJson) {
      const json = {
        resource: { id: targetResource.id, name: targetResource.name },
        claimUrl,
        status: "timeout"
      };
      client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
    }
    return 1;
  }
  const claimed = result.resource;
  if (asJson) {
    const json = {
      resource: { id: claimed.id, name: claimed.name },
      claimUrl,
      status: "claimed"
    };
    client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
    return 0;
  }
  output_manager_default.success(`Claimed ${import_chalk125.default.bold(claimed.name)}.`);
  return 0;
}

// src/util/integration/build-sso-link.ts
function buildSSOLink(team, configurationId, resourceExternalId) {
  const url = new URL("/api/marketplace/sso", "https://vercel.com");
  url.searchParams.set("teamId", team.id);
  url.searchParams.set("integrationConfigurationId", configurationId);
  if (resourceExternalId) {
    url.searchParams.set("resource_id", resourceExternalId);
  }
  return url.href;
}

// src/util/integration/generate-resource-name.ts
var COLORS = [
  "gray",
  "red",
  "rose",
  "yellow",
  "amber",
  "green",
  "lime",
  "emerald",
  "blue",
  "cyan",
  "purple",
  "violet",
  "fuchsia",
  "orange",
  "pink",
  "indigo",
  "teal",
  "sky",
  "aero",
  "alizarin",
  "almond",
  "amethyst",
  "apricot",
  "aqua",
  "aquamarine",
  "aureolin",
  "beige",
  "bisque",
  "bistre",
  "bole",
  "bronze",
  "byzantine",
  "byzantium",
  "camel",
  "canary",
  "carmine",
  "celeste",
  "cerise",
  "cerulean",
  "champagne",
  "charcoal",
  "chestnut",
  "cinereous",
  "cinnabar",
  "citrine",
  "citron",
  "claret",
  "cobalt",
  "coffee",
  "copper",
  "coquelicot",
  "coral",
  "cordovan",
  "crimson",
  "cyclamen",
  "erin",
  "fulvous"
];
var NOUNS = [
  "apple",
  "ball",
  "car",
  "dog",
  "elephant",
  "flower",
  "garden",
  "house",
  "island",
  "jacket",
  "kite",
  "lamp",
  "mountain",
  "notebook",
  "ocean",
  "park",
  "queen",
  "river",
  "school",
  "tree",
  "umbrella",
  "village",
  "window",
  "xylophone",
  "yacht",
  "zebra",
  "book",
  "chair",
  "door",
  "grass",
  "anchor",
  "arrow",
  "basket",
  "battery",
  "bell",
  "blanket",
  "branch",
  "bridge",
  "brush",
  "bucket",
  "button",
  "cable",
  "candle",
  "canvas",
  "castle",
  "cave",
  "clock",
  "cloud",
  "coin",
  "compass",
  "crystal",
  "curtain",
  "cushion",
  "desert",
  "diamond",
  "drawer",
  "drum",
  "engine",
  "envelope",
  "feather",
  "fence",
  "ferry",
  "field",
  "flame",
  "flask",
  "forest",
  "fountain",
  "globe",
  "harbor",
  "helmet",
  "horizon",
  "kettle",
  "ladder",
  "lantern",
  "leaf",
  "lens",
  "lever",
  "lighthouse",
  "magnet",
  "marble",
  "mirror",
  "nest",
  "paddle",
  "pebble",
  "pendant",
  "pillar",
  "pillow",
  "planet",
  "plank",
  "pocket",
  "prism",
  "ribbon",
  "ridge",
  "saddle",
  "sail"
];
function randomElement(arr) {
  return arr[Math.floor(arr.length * Math.random())];
}
function generateRandomNameSuffix() {
  return `${randomElement(COLORS)}-${randomElement(NOUNS)}`;
}
function generateDefaultResourceName(productSlug, maxLength = 128) {
  const suffix = generateRandomNameSuffix();
  const fullName = `${productSlug}-${suffix}`;
  if (fullName.length <= maxLength) {
    return fullName;
  }
  return fullName.slice(0, maxLength).replace(/-+$/, "");
}
var DEFAULT_VALIDATION_RULE = {
  minLength: 1,
  maxLength: 128,
  pattern: /^[a-zA-Z0-9_-]*$/,
  patternErrorMessage: "Resource name can only contain letters, numbers, underscores, and hyphens"
};
var VALIDATION_RULES = {
  "aws-dsql": {
    minLength: 1,
    maxLength: 128,
    pattern: /^[a-zA-Z0-9_-]*$/,
    patternErrorMessage: "Resource name can only contain letters, numbers, underscores, and hyphens"
  },
  "aws-apg": {
    minLength: 1,
    maxLength: 50,
    pattern: /^[a-zA-Z][a-zA-Z0-9-]*$/,
    patternErrorMessage: "Resource name must start with a letter and can only contain letters, numbers, and hyphens",
    customValidation: (name) => {
      if (name.endsWith("-")) {
        return "Resource name cannot end with a hyphen";
      }
      if (name.includes("--")) {
        return "Resource name cannot contain consecutive hyphens";
      }
      return void 0;
    }
  },
  "aws-dynamodb": {
    minLength: 3,
    maxLength: 128,
    pattern: /^[a-zA-Z0-9_-]*$/,
    patternErrorMessage: "Resource name can only contain letters, numbers, underscores, and hyphens"
  }
};
function getValidationRuleForProduct(productSlug) {
  if (!productSlug) {
    return DEFAULT_VALIDATION_RULE;
  }
  return VALIDATION_RULES[productSlug.toLowerCase()] ?? DEFAULT_VALIDATION_RULE;
}
function validateResourceName(name, productSlug) {
  const rule = getValidationRuleForProduct(productSlug);
  if (!name || name.trim().length === 0) {
    return "Resource name cannot be empty";
  }
  if (name.length < rule.minLength) {
    return `Resource name must be at least ${rule.minLength} character${rule.minLength === 1 ? "" : "s"}`;
  }
  if (name.length > rule.maxLength) {
    return `Resource name cannot exceed ${rule.maxLength} characters`;
  }
  if (!rule.pattern.test(name)) {
    return rule.patternErrorMessage;
  }
  if (rule.customValidation) {
    const customError = rule.customValidation(name);
    if (customError) {
      return customError;
    }
  }
  return void 0;
}
function resolveResourceName(productSlug, resourceNameArg) {
  const rule = getValidationRuleForProduct(productSlug);
  const isAutoGenerated = resourceNameArg === void 0;
  const resourceName = resourceNameArg ?? generateDefaultResourceName(productSlug, rule.maxLength);
  const validationError = validateResourceName(resourceName, productSlug);
  if (validationError) {
    return {
      error: isAutoGenerated ? `${validationError}. Use --name to provide a valid resource name.` : validationError
    };
  }
  return { resourceName };
}

// src/util/telemetry/commands/integration/add.ts
var IntegrationAddTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIntegration(v, known) {
    if (v) {
      this.trackCliArgument({
        arg: "integration",
        value: known ? v : this.redactedValue
      });
    }
  }
  trackCliOptionName(v) {
    if (v) {
      this.trackCliOption({
        option: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionMetadata(v) {
    if (v?.length) {
      this.trackCliOption({
        option: "metadata",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionPlan(v) {
    if (v) {
      this.trackCliOption({
        option: "plan",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagNoConnect(v) {
    if (v) {
      this.trackCliFlag("no-connect");
    }
  }
  trackCliFlagNoEnvPull(v) {
    if (v) {
      this.trackCliFlag("no-env-pull");
    }
  }
  trackCliOptionEnvironment(v) {
    if (v?.length) {
      this.trackCliOption({
        option: "environment",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionInstallationId(v) {
    if (v) {
      this.trackCliOption({
        option: "installation-id",
        value: this.redactedValue
      });
    }
  }
  trackMarketplaceEvent(event, props) {
    this.trackCommandOutput({
      key: event,
      value: JSON.stringify(props)
    });
  }
  trackCliOptionPrefix(v) {
    if (v) {
      this.trackCliOption({
        option: "prefix",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagClaim(v) {
    if (v) {
      this.trackCliFlag("claim");
    }
  }
  trackCliFlagNoClaim(v) {
    if (v) {
      this.trackCliFlag("no-claim");
    }
  }
};

// src/util/integration/format-schema-help.ts
var import_chalk126 = __toESM(require_source(), 1);
function isHiddenOnCreate(prop) {
  return prop["ui:hidden"] === true || prop["ui:hidden"] === "create";
}
function getAllOptionValues(prop) {
  const raw = prop["ui:options"];
  if (!raw)
    return void 0;
  const values = raw.map(
    (opt) => typeof opt === "string" ? opt : opt.value
  );
  return values.length > 0 ? values : void 0;
}
function getVisibleOptions(prop) {
  const raw = prop["ui:options"];
  if (!raw)
    return void 0;
  const options = [];
  for (const opt of raw) {
    if (typeof opt === "string") {
      if (opt)
        options.push(opt);
    } else if (!opt.hidden && opt.value) {
      options.push(opt.value);
    }
  }
  return options.length > 0 ? options : void 0;
}
function generateExample(key, prop) {
  if (prop.type === "boolean") {
    return `-m ${key}=true`;
  }
  if (prop.type === "array") {
    const options2 = getVisibleOptions(prop);
    if (options2 && options2.length >= 2) {
      return `-m "${key}=${options2[0]},${options2[1]}"`;
    }
    if (options2 && options2.length === 1) {
      return `-m "${key}=${options2[0]}"`;
    }
    if (prop.items?.type === "number") {
      if (prop.default !== void 0) {
        return `-m ${key}=${prop.default}`;
      }
      return `-m ${key}=N,N`;
    }
    return `-m "${key}=value1,value2"`;
  }
  if (prop.type === "number") {
    if (prop.default !== void 0) {
      return `-m ${key}=${prop.default}`;
    }
    if (prop.minimum !== void 0) {
      return `-m ${key}=${prop.minimum}`;
    }
    return `-m ${key}=N`;
  }
  const options = getVisibleOptions(prop);
  if (options && options.length > 0) {
    return `-m ${key}=${options[0]}`;
  }
  return `-m ${key}=<value>`;
}
function isServerHandledRegion(prop) {
  const control = prop["ui:control"];
  return control === "vercel-region" || control === "multi-vercel-region";
}
function formatMetadataSchemaHelp(schema, integrationName, productSlug) {
  const lines = [];
  lines.push("");
  const header = productSlug ? `  Metadata options for "${integrationName}/${productSlug}":` : `  Metadata options for "${integrationName}":`;
  lines.push(import_chalk126.default.bold(header));
  lines.push("");
  const required = new Set(schema.required ?? []);
  const entries = Object.entries(schema.properties);
  if (entries.length === 0) {
    lines.push("    No metadata options available.");
    return lines.join("\n");
  }
  for (const [key, prop] of entries) {
    if (isHiddenOnCreate(prop)) {
      continue;
    }
    const isRequired = required.has(key);
    const requiredSuffix = isRequired && !isServerHandledRegion(prop) ? import_chalk126.default.red(" (required)") : "";
    const typeHint = prop.type === "boolean" ? import_chalk126.default.dim(" (true/false)") : prop.type === "array" ? import_chalk126.default.dim(" (comma-separated)") : "";
    lines.push(`    ${import_chalk126.default.cyan(key)}${requiredSuffix}${typeHint}`);
    if (prop.description) {
      lines.push(`      ${prop.description}`);
    }
    const visibleOptions = getVisibleOptions(prop);
    if (visibleOptions) {
      lines.push(`      Options: ${visibleOptions.join(", ")}`);
    }
    if (prop.minimum !== void 0 || prop.maximum !== void 0) {
      const range = [];
      if (prop.minimum !== void 0)
        range.push(`min: ${prop.minimum}`);
      if (prop.maximum !== void 0)
        range.push(`max: ${prop.maximum}`);
      lines.push(`      Range: ${range.join(", ")}`);
    }
    if (prop.default !== void 0) {
      lines.push(`      Default: ${prop.default}`);
    }
    const example = generateExample(key, prop);
    if (example) {
      lines.push(`      Example: ${import_chalk126.default.dim(example)}`);
    }
    lines.push("");
  }
  return lines.join("\n");
}
function mergeMetadataSchemas(productSchema, integrationSchema) {
  if (!productSchema && !integrationSchema)
    return void 0;
  if (!integrationSchema)
    return productSchema;
  if (!productSchema)
    return integrationSchema;
  return {
    type: "object",
    properties: {
      ...integrationSchema.properties,
      ...productSchema.properties
    },
    required: [
      .../* @__PURE__ */ new Set([
        ...integrationSchema.required ?? [],
        ...productSchema.required ?? []
      ])
    ]
  };
}

// src/util/integration/parse-metadata.ts
function parseMetadataFlags(rawMetadata, schema) {
  const metadata = {};
  const errors = [];
  if (!rawMetadata?.length) {
    return { metadata, errors };
  }
  for (const item of rawMetadata) {
    const eqIndex = item.indexOf("=");
    if (eqIndex === -1) {
      errors.push(`Invalid metadata format: "${item}". Expected KEY=VALUE`);
      continue;
    }
    const key = item.slice(0, eqIndex).trim();
    const value = item.slice(eqIndex + 1).trim();
    const propSchema = schema.properties[key];
    if (!propSchema) {
      errors.push(`Unknown metadata key: "${key}"`);
      continue;
    }
    if (propSchema.type === "boolean") {
      if (value === "true") {
        metadata[key] = true;
      } else if (value === "false") {
        metadata[key] = false;
      } else {
        errors.push(
          `Metadata "${key}" must be "true" or "false", got: "${value}"`
        );
      }
    } else if (propSchema.type === "number") {
      if (value === "") {
        errors.push(`Metadata "${key}" must be a number, got: ""`);
        continue;
      }
      const num = Number(value);
      if (Number.isNaN(num) || !Number.isFinite(num)) {
        errors.push(`Metadata "${key}" must be a number, got: "${value}"`);
        continue;
      }
      if (propSchema.minimum !== void 0 && num < propSchema.minimum) {
        errors.push(`Metadata "${key}" must be >= ${propSchema.minimum}`);
        continue;
      }
      if (propSchema.maximum !== void 0 && num > propSchema.maximum) {
        errors.push(`Metadata "${key}" must be <= ${propSchema.maximum}`);
        continue;
      }
      metadata[key] = num;
    } else if (propSchema.type === "array") {
      const items = value.split(",").map((v) => v.trim()).filter((v) => v.length > 0);
      const itemType = propSchema.items?.type;
      if (itemType === "number") {
        const nums = [];
        let hasError = false;
        for (const item2 of items) {
          if (item2 === "") {
            errors.push(`Metadata "${key}" contains invalid number: ""`);
            hasError = true;
            break;
          }
          const num = Number(item2);
          if (Number.isNaN(num) || !Number.isFinite(num)) {
            errors.push(`Metadata "${key}" contains invalid number: "${item2}"`);
            hasError = true;
            break;
          }
          if (propSchema.minimum !== void 0 && num < propSchema.minimum) {
            errors.push(
              `Metadata "${key}" contains number ${num} below minimum ${propSchema.minimum}`
            );
            hasError = true;
            break;
          }
          if (propSchema.maximum !== void 0 && num > propSchema.maximum) {
            errors.push(
              `Metadata "${key}" contains number ${num} above maximum ${propSchema.maximum}`
            );
            hasError = true;
            break;
          }
          nums.push(num);
        }
        if (!hasError) {
          metadata[key] = nums;
        }
      } else {
        const validValues = getAllOptionValues(propSchema);
        if (validValues) {
          const prevErrorCount = errors.length;
          for (const item2 of items) {
            if (!validValues.includes(item2)) {
              errors.push(
                `Metadata "${key}" contains invalid value: "${item2}". Must be one of: ${validValues.join(", ")}`
              );
            }
          }
          if (errors.length > prevErrorCount) {
            continue;
          }
        }
        metadata[key] = items;
      }
    } else {
      const validValues = getAllOptionValues(propSchema);
      if (validValues) {
        if (!validValues.includes(value)) {
          errors.push(
            `Metadata "${key}" must be one of: ${validValues.join(", ")}`
          );
          continue;
        }
      }
      metadata[key] = value;
    }
  }
  return { metadata, errors };
}
function validateRequiredMetadata(metadata, schema) {
  const errors = [];
  const required = schema.required ?? [];
  for (const key of required) {
    const propSchema = schema.properties[key];
    if (propSchema && isHiddenOnCreate(propSchema)) {
      continue;
    }
    if (propSchema && isServerHandledRegion(propSchema)) {
      continue;
    }
    if (metadata[key] === void 0 && propSchema?.default === void 0) {
      errors.push(`Required metadata missing: "${key}"`);
    }
  }
  return errors;
}
function validateAndPrintRequiredMetadata(metadata, schema) {
  const errors = validateRequiredMetadata(metadata, schema);
  for (const error of errors) {
    output_manager_default.error(error);
  }
  return errors.length === 0;
}

// src/commands/integration/add-auto-provision.ts
async function addAutoProvision(client, integrationSlugOrQuery, resourceNameArg, options = {}) {
  const commandName = options.commandName ?? "integration add";
  const telemetry2 = new IntegrationAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetry2.trackCliOptionName(resourceNameArg);
  telemetry2.trackCliOptionMetadata(options.metadata);
  telemetry2.trackCliFlagNoConnect(options.noConnect);
  telemetry2.trackCliFlagNoEnvPull(options.noEnvPull);
  telemetry2.trackCliOptionPlan(options.billingPlanId);
  telemetry2.trackCliOptionInstallationId(options.installationId);
  telemetry2.trackCliOptionEnvironment(options.environments);
  telemetry2.trackCliOptionPrefix(options.prefix);
  telemetry2.trackCliOptionFormat(options.asJson ? "json" : void 0);
  const { contextName, team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found");
    return 1;
  }
  client.config.currentTeam = team.id;
  const integration = await resolveAndFetchIntegration(
    client,
    integrationSlugOrQuery,
    telemetry2
  );
  if (!integration) {
    return 1;
  }
  if (integration.productSlug && !options.productSlug) {
    options.productSlug = integration.productSlug;
  }
  if (!integration.products?.length) {
    output_manager_default.error(
      `Integration "${integration.slug}" is not a Marketplace integration`
    );
    return 1;
  }
  if (!options.productSlug && integration.products.length > 1 && !client.stdin.isTTY) {
    const choices = integration.products.map((p) => `  ${integration.slug}/${p.slug}`).join("\n");
    output_manager_default.error(
      `Integration "${integration.slug}" has multiple products. Specify one with:

${choices}

Example: vercel ${commandName} ${integration.slug}/${integration.products[0].slug}`
    );
    return 1;
  }
  const [productResult, installationsResult] = await Promise.allSettled([
    selectProduct(client, integration.products, options.productSlug),
    fetchInstallations(client, integration)
  ]);
  if (productResult.status === "rejected") {
    output_manager_default.error(
      `Failed to select product: ${productResult.reason.message}`
    );
    return 1;
  }
  if (!productResult.value) {
    return 1;
  }
  if (installationsResult.status === "rejected") {
    output_manager_default.error(
      `Failed to get integration installations: ${installationsResult.reason.message}`
    );
    return 1;
  }
  const product = productResult.value;
  const installations = installationsResult.value;
  const baseProps = {
    integration_id: integration.id,
    integration_slug: integration.slug,
    integration_name: integration.name,
    product_id: product.id,
    product_slug: product.slug,
    team_id: team.id,
    source: "cli"
  };
  telemetry2.trackMarketplaceEvent(
    "marketplace_install_flow_started",
    baseProps
  );
  output_manager_default.log(
    `Installing ${import_chalk127.default.bold(product.name)} by ${import_chalk127.default.bold(integration.name)} under ${import_chalk127.default.bold(contextName)}`
  );
  output_manager_default.debug(`Selected product: ${product.slug} (id: ${product.id})`);
  output_manager_default.debug(
    `Product metadataSchema: ${JSON.stringify(product.metadataSchema, null, 2)}`
  );
  const teamInstallation = installations.find(
    (i) => i.ownerId === team.id && i.installationType === "marketplace"
  );
  let acceptedPolicies = {};
  let browserInstallationId;
  if (!teamInstallation) {
    if (
      // AI agent mode — cannot interact with terminal prompts
      client.isAgent || // Non-interactive terminal (CI/scripts) — no TTY for prompts
      !client.stdin.isTTY || // Server declares browser install required (e.g. needs device fingerprint)
      integration.capabilities?.requiresBrowserInstall
    ) {
      const browserInstallation = await acceptTermsViaBrowser(
        client,
        integration,
        team.id,
        contextName
      );
      if (!browserInstallation) {
        telemetry2.trackMarketplaceEvent("marketplace_install_flow_dropped", {
          ...baseProps,
          reason: "browser_terms_timeout"
        });
        return 1;
      }
      browserInstallationId = browserInstallation.id;
    } else {
      const policies = await promptForTermAcceptance(client, integration);
      if (!policies) {
        telemetry2.trackMarketplaceEvent("marketplace_install_flow_dropped", {
          ...baseProps,
          reason: "policy_declined"
        });
        return 1;
      }
      acceptedPolicies = policies;
    }
  }
  const integrationSchemaProps = integration.metadataSchema?.properties ?? {};
  const productSchemaProps = product.metadataSchema.properties;
  const mergedParsingSchema = mergeMetadataSchemas(product.metadataSchema, integration.metadataSchema) ?? product.metadataSchema;
  for (const key of Object.keys(integrationSchemaProps)) {
    if (key in productSchemaProps) {
      output_manager_default.debug(
        `Metadata key "${key}" exists in both product and installation schemas \u2014 product definition wins`
      );
    }
  }
  let metadata;
  let installationMetadata = {};
  if (options.metadata?.length) {
    output_manager_default.debug(
      `Parsing metadata from flags: ${JSON.stringify(options.metadata)}`
    );
    const { metadata: parsed, errors } = parseMetadataFlags(
      options.metadata,
      mergedParsingSchema
    );
    if (errors.length) {
      for (const error of errors) {
        output_manager_default.error(error);
      }
      telemetry2.trackMarketplaceEvent("marketplace_install_flow_dropped", {
        ...baseProps,
        reason: "metadata_parse_error"
      });
      return 1;
    }
    if (!validateAndPrintRequiredMetadata(parsed, mergedParsingSchema)) {
      telemetry2.trackMarketplaceEvent("marketplace_install_flow_dropped", {
        ...baseProps,
        reason: "metadata_validation_failed"
      });
      return 1;
    }
    const productMeta = {};
    const installMeta = {};
    for (const [key, value] of Object.entries(parsed)) {
      if (value === void 0)
        continue;
      const inProduct = key in productSchemaProps;
      const inInstallation = key in integrationSchemaProps;
      if (inProduct) {
        productMeta[key] = value;
      } else if (inInstallation) {
        installMeta[key] = value;
      } else {
        productMeta[key] = value;
      }
    }
    metadata = productMeta;
    installationMetadata = installMeta;
  } else {
    metadata = {};
  }
  const nameResult = resolveResourceName(product.slug, resourceNameArg);
  if ("error" in nameResult) {
    output_manager_default.error(nameResult.error);
    telemetry2.trackMarketplaceEvent("marketplace_install_flow_dropped", {
      ...baseProps,
      reason: "resource_name_invalid"
    });
    return 1;
  }
  const { resourceName } = nameResult;
  output_manager_default.debug(`Collected metadata: ${JSON.stringify(metadata)}`);
  output_manager_default.debug(`Resource name: ${resourceName}`);
  telemetry2.trackMarketplaceEvent("marketplace_checkout_plan_selected", {
    ...baseProps,
    billing_plan_id: options.billingPlanId ?? null,
    plan_selection_method: options.billingPlanId ? "cli_flag" : "server_default"
  });
  telemetry2.trackMarketplaceEvent(
    "marketplace_checkout_provisioning_started",
    baseProps
  );
  output_manager_default.spinner("Provisioning resource...");
  let result;
  try {
    result = await autoProvisionResource(
      client,
      integration.slug,
      product.slug,
      resourceName,
      metadata,
      acceptedPolicies,
      options.billingPlanId,
      browserInstallationId ?? options.installationId,
      installationMetadata
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    telemetry2.trackMarketplaceEvent(
      "marketplace_checkout_provisioning_failed",
      {
        ...baseProps,
        error_message: (0, import_error_utils11.errorToString)(error)
      }
    );
    output_manager_default.error((0, import_error_utils11.errorToString)(error));
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.debug(`Auto-provision result: ${JSON.stringify(result, null, 2)}`);
  if (result.kind === "unknown" && result.reason === "multiple_installations" && result.installations?.length) {
    const installationsList2 = result.installations.map((i) => {
      const parts = [i.id];
      if (i.type) {
        parts.push(`type=${i.type}`);
      }
      if (i.externalId) {
        parts.push(`externalId=${i.externalId}`);
      }
      if (i.status) {
        parts.push(`status=${i.status}`);
      }
      return parts.join(", ");
    }).map((line) => `  - ${line}`).join("\n");
    const slug = options.productSlug ? `${integration.slug}/${options.productSlug}` : integration.slug;
    telemetry2.trackMarketplaceEvent(
      "marketplace_install_flow_multiple_installations",
      {
        ...baseProps,
        installation_count: result.installations.length
      }
    );
    output_manager_default.error(
      `Multiple installations found for "${integration.slug}":
${installationsList2}

Re-run with --installation-id to select one, e.g.:
  vercel ${commandName} ${slug} --installation-id ${result.installations[0].id}`
    );
    return 1;
  }
  if (result.kind !== "provisioned") {
    const fallback = result;
    telemetry2.trackMarketplaceEvent("marketplace_install_flow_web_fallback", {
      ...baseProps,
      reason: fallback.reason ?? fallback.kind,
      auto_provision_result_kind: fallback.kind,
      auto_provision_result_reason: fallback.reason,
      auto_provision_error_message: fallback.error_message
    });
    output_manager_default.debug(`Fallback required - kind: ${fallback.kind}`);
    output_manager_default.debug(`Fallback URL from API: ${fallback.url}`);
    const projectLink = await getLinkedProjectField(
      client,
      options.noConnect,
      "name"
    );
    if (projectLink.exitCode !== void 0) {
      return projectLink.exitCode;
    }
    const missingRequired = (mergedParsingSchema.required ?? []).filter((key) => {
      if (key in metadata || key in installationMetadata)
        return false;
      const prop = mergedParsingSchema.properties[key];
      return prop && !isServerHandledRegion(prop);
    });
    if (missingRequired.length > 0) {
      const examples = missingRequired.map((key) => {
        const prop = mergedParsingSchema.properties[key];
        const options2 = prop ? getVisibleOptions(prop) : void 0;
        const exampleVal = options2?.[0] ?? "<value>";
        return `-m ${key}=${exampleVal}`;
      });
      output_manager_default.error(`Missing required metadata: ${missingRequired.join(", ")}.`);
      output_manager_default.log(
        `  Provide ${examples.join(" ")} to provision directly from the CLI.`
      );
      output_manager_default.log(
        `  Run \`vercel ${commandName} ${integration.slug} --help\` for all metadata options.`
      );
      return 1;
    }
    output_manager_default.log("Additional setup required. Opening browser...");
    const url = new URL(fallback.url);
    url.searchParams.set("defaultResourceName", resourceName);
    url.searchParams.set("source", "cli");
    if (Object.keys(metadata).length > 0) {
      url.searchParams.set("metadata", JSON.stringify(metadata));
    }
    if (Object.keys(installationMetadata).length > 0) {
      url.searchParams.set(
        "installationMetadata",
        JSON.stringify(installationMetadata)
      );
    }
    if (projectLink.value) {
      url.searchParams.set("projectSlug", projectLink.value);
    }
    if (options.billingPlanId) {
      url.searchParams.set("planId", options.billingPlanId);
    }
    if (options.environments?.length) {
      url.searchParams.set("environment", options.environments.join(","));
    }
    output_manager_default.debug(`Opening URL: ${url.href}`);
    (0, import_open12.default)(url.href).catch(
      (err) => output_manager_default.debug(`Failed to open browser: ${err}`)
    );
    return 1;
  }
  const provisioned = result;
  telemetry2.trackMarketplaceEvent(
    "marketplace_checkout_provisioning_completed",
    {
      ...baseProps,
      resource_id: provisioned.resource.id
    }
  );
  output_manager_default.debug(
    `Provisioned resource: ${JSON.stringify(provisioned.resource, null, 2)}`
  );
  output_manager_default.debug(
    `Installation: ${JSON.stringify(provisioned.installation, null, 2)}`
  );
  output_manager_default.debug(
    `Billing plan: ${JSON.stringify(provisioned.billingPlan, null, 2)}`
  );
  output_manager_default.success(
    `${product.name} successfully provisioned: ${import_chalk127.default.bold(resourceName)}`
  );
  const isSandbox = provisioned.resource.ownership === "sandbox";
  if (isSandbox && !options.noClaim) {
    telemetry2.trackCliFlagClaim(options.claim);
    telemetry2.trackCliFlagNoClaim(options.noClaim);
    const shouldRunClaim = options.claim || client.stdin.isTTY && !options.asJson;
    if (shouldRunClaim) {
      let runClaim = !!options.claim;
      if (!runClaim && client.stdin.isTTY) {
        runClaim = await client.input.confirm(
          `${import_chalk127.default.bold(resourceName)} is a sandbox resource. Claim it now?`,
          true
        );
      }
      if (runClaim) {
        try {
          const allResources = await getResources(client);
          const targetResource = allResources.find(
            (r) => r.id === provisioned.resource.id
          );
          if (targetResource) {
            await runClaimForResource(client, targetResource, {
              suppressActionRequired: true
            });
          } else {
            output_manager_default.warn(
              `Could not locate the newly provisioned resource. Run \`${packageName} integration-resource claim ${resourceName}\` to claim it.`
            );
          }
        } catch (error) {
          output_manager_default.warn(
            `Failed to start claim flow: ${error.message}. Run \`${packageName} integration-resource claim ${resourceName}\` to retry.`
          );
        }
      }
    } else {
      output_manager_default.log(
        `Sandbox resource \u2014 claim it with: ${import_chalk127.default.cyan(`vercel integration-resource claim ${resourceName}`)}`
      );
    }
  } else if (isSandbox && options.noClaim) {
    telemetry2.trackCliFlagNoClaim(options.noClaim);
    output_manager_default.log(
      `Sandbox resource \u2014 claim it later with: ${import_chalk127.default.cyan(`vercel integration-resource claim ${resourceName}`)}`
    );
  }
  const guideSlug = integration.products.length > 1 ? `${integration.slug}/${product.slug}` : integration.slug;
  const guideCommand = `vercel integration guide ${guideSlug}`;
  output_manager_default.log(
    indent_default(
      `Guide: Run ${import_chalk127.default.cyan(`\`${guideCommand}\``)} for getting started guides and code snippets`,
      4
    )
  );
  const setupResult = await postProvisionSetup(
    client,
    resourceName,
    provisioned.resource.id,
    contextName,
    {
      ...options,
      integrationSlug: integration.slug,
      installationId: provisioned.installation.id,
      onProjectConnected: (projectId) => {
        telemetry2.trackMarketplaceEvent("marketplace_project_connected", {
          ...baseProps,
          project_id: projectId,
          resource_id: provisioned.resource.id
        });
      },
      onProjectConnectFailed: (projectId, error) => {
        telemetry2.trackMarketplaceEvent("marketplace_project_connect_failed", {
          ...baseProps,
          project_id: projectId,
          resource_id: provisioned.resource.id,
          error_message: error.message
        });
      }
    }
  );
  if (options.asJson) {
    const warnings = [];
    if (setupResult.connectError) {
      warnings.push(
        `Failed to connect to project: ${setupResult.connectError}`
      );
    }
    if (setupResult.connected && !setupResult.envPulled && !options.noEnvPull) {
      warnings.push(ENV_PULL_FAILED_MESSAGE);
    }
    const jsonOutput = {
      resource: {
        id: provisioned.resource.id,
        name: provisioned.resource.name,
        status: provisioned.resource.status,
        externalResourceId: provisioned.resource.externalResourceId,
        ...provisioned.resource.ownership && {
          ownership: provisioned.resource.ownership
        }
      },
      integration: {
        id: provisioned.integration.id,
        slug: provisioned.integration.slug,
        name: provisioned.integration.name
      },
      product: {
        id: provisioned.product.id,
        slug: provisioned.product.slug,
        name: provisioned.product.name
      },
      installation: {
        id: provisioned.installation.id
      },
      billingPlan: provisioned.billingPlan ? {
        id: provisioned.billingPlan.id,
        name: provisioned.billingPlan.name,
        type: provisioned.billingPlan.type
      } : null,
      dashboardUrl: setupResult.dashboardUrl,
      ssoUrl: {
        integration: buildSSOLink(team, provisioned.installation.id),
        resource: buildSSOLink(
          team,
          provisioned.installation.id,
          provisioned.resource.externalResourceId
        )
      },
      project: setupResult.project ?? null,
      environments: setupResult.environments,
      envPulled: setupResult.envPulled,
      guideCommand,
      warnings
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
    return setupResult.connectError ? 0 : setupResult.exitCode;
  }
  return setupResult.exitCode;
}

// src/commands/integration/add.ts
async function add7(client, args, flags, commandName) {
  const resourceNameArg = flags["--name"];
  const metadataFlags = flags["--metadata"];
  const billingPlanId = flags["--plan"];
  const prefix = flags["--prefix"];
  if (prefix !== void 0 && !/^[a-zA-Z][a-zA-Z0-9_]*$/.test(prefix)) {
    output_manager_default.error(
      "Invalid --prefix value. It must start with a letter and contain only letters, digits, and underscores."
    );
    return 1;
  }
  const installationId = flags["--installation-id"];
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  if (args.length > 1) {
    output_manager_default.error("Cannot install more than one integration at a time");
    return 1;
  }
  const rawArg = args[0];
  if (!rawArg) {
    const message = "You must pass an integration slug";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message,
        hint: `Example: \`${packageName} integration add <integration-slug>\`. Run \`${packageName} integration discover\` to find slugs.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration discover",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "List available marketplace integrations and slugs"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration add neon",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Install after replacing neon with a slug from discover"
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  let integrationSlug;
  let productSlug;
  const slashIndex = rawArg.indexOf("/");
  if (slashIndex !== -1) {
    integrationSlug = rawArg.substring(0, slashIndex);
    productSlug = rawArg.substring(slashIndex + 1);
    if (!integrationSlug || !productSlug) {
      output_manager_default.error(
        "Invalid format. Expected: <integration-name>/<product-slug>"
      );
      return 1;
    }
  } else {
    integrationSlug = rawArg;
  }
  const environments = flags["--environment"];
  if (environments?.length) {
    const envValidation = validateEnvironments(environments);
    if (!envValidation.valid) {
      output_manager_default.error(
        `Invalid environment value: ${envValidation.invalid.map((e2) => `"${e2}"`).join(", ")}. Must be one of: ${VALID_ENVIRONMENTS.join(", ")}`
      );
      return 1;
    }
  }
  const claimFlag = flags["--claim"];
  const noClaimFlag = flags["--no-claim"];
  if (claimFlag && noClaimFlag) {
    output_manager_default.error("Cannot use both --claim and --no-claim.");
    return 1;
  }
  return await addAutoProvision(client, integrationSlug, resourceNameArg, {
    productSlug,
    metadata: metadataFlags,
    billingPlanId,
    installationId,
    noConnect: flags["--no-connect"],
    noEnvPull: flags["--no-env-pull"],
    environments,
    prefix,
    commandName,
    asJson,
    claim: claimFlag,
    noClaim: noClaimFlag
  });
}

// src/util/integration/format-product-help.ts
var import_chalk128 = __toESM(require_source(), 1);
function formatProductHelp(integrationSlug, products, commandName = "integration add") {
  const lines = [];
  lines.push("");
  lines.push(
    `  ${import_chalk128.default.dim("Available products for")} "${import_chalk128.default.bold(integrationSlug)}"${import_chalk128.default.dim(":")}`
  );
  lines.push("");
  const maxSlugLen = Math.max(...products.map((p) => p.slug.length));
  for (const product of products) {
    const paddedSlug = product.slug.padEnd(maxSlugLen);
    lines.push(`    ${import_chalk128.default.cyan(paddedSlug)}  ${product.name}`);
  }
  lines.push("");
  lines.push(`  ${import_chalk128.default.dim("Usage:")}`);
  lines.push("");
  lines.push(
    `    ${import_chalk128.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug}/<product-slug>`)}`
  );
  lines.push("");
  return lines.join("\n");
}

// src/util/integration/format-billing-plans-help.ts
var import_chalk129 = __toESM(require_source(), 1);
function formatBillingPlansHelp(productName, plans) {
  const enabledPlans = plans.filter((p) => !p.disabled);
  if (enabledPlans.length === 0) {
    return "";
  }
  const lines = [];
  lines.push("");
  lines.push(
    `  ${import_chalk129.default.dim("Available billing plans for")} "${import_chalk129.default.bold(productName)}"${import_chalk129.default.dim(":")}`
  );
  lines.push("");
  const maxIdLen = Math.max(...enabledPlans.map((p) => p.id.length));
  for (const plan of enabledPlans) {
    const paddedId = plan.id.padEnd(maxIdLen);
    const cost = plan.cost ? import_chalk129.default.dim(` (${plan.cost})`) : "";
    lines.push(`    ${import_chalk129.default.cyan(paddedId)}  ${plan.name}${cost}`);
  }
  lines.push("");
  lines.push(`  ${import_chalk129.default.dim("Usage:")}`);
  lines.push("");
  lines.push(`    ${import_chalk129.default.cyan(`--plan ${enabledPlans[0].id}`)}`);
  lines.push("");
  return lines.join("\n");
}

// src/util/integration/format-dynamic-examples.ts
var import_chalk130 = __toESM(require_source(), 1);
function formatDynamicExamples(integrationSlug, products, commandName = "integration add") {
  const lines = [];
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("Examples:")}`);
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Install ${integrationSlug}`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug}`)}`
  );
  if (products.length > 1) {
    const firstProduct = products[0];
    lines.push("");
    lines.push(`  ${import_chalk130.default.dim("-")} Install a specific product`);
    lines.push("");
    lines.push(
      `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug}/${firstProduct.slug}`)}`
    );
  }
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Install with a custom resource name`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --name my-resource`)}`
  );
  const metadataExample = buildMetadataExample(
    integrationSlug,
    products,
    commandName
  );
  if (metadataExample) {
    lines.push("");
    lines.push(`  ${import_chalk130.default.dim("-")} Install with metadata`);
    lines.push("");
    lines.push(`    ${import_chalk130.default.cyan(`$ ${metadataExample}`)}`);
  }
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Install with a specific billing plan`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --plan pro`)}`
  );
  lines.push("");
  lines.push(
    `  ${import_chalk130.default.dim("-")} Install and connect to specific environments only`
  );
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} -e production -e preview`)}`
  );
  lines.push("");
  lines.push(
    `  ${import_chalk130.default.dim("-")} Install without connecting to the current project`
  );
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --no-connect`)}`
  );
  lines.push("");
  lines.push(
    `  ${import_chalk130.default.dim("-")} Install without pulling environment variables`
  );
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --no-env-pull`)}`
  );
  lines.push("");
  lines.push(
    `  ${import_chalk130.default.dim("-")} Install with a prefix for environment variable names`
  );
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --prefix NEON2_`)}`
  );
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Output as JSON`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --format=json`)}`
  );
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Install using a specific installation`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --installation-id <id>`)}`
  );
  lines.push("");
  lines.push(
    `  ${import_chalk130.default.dim("-")} Claim a sandbox resource immediately after provisioning`
  );
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --claim`)}`
  );
  lines.push("");
  lines.push(`  ${import_chalk130.default.dim("-")} Skip the offer to claim a sandbox resource`);
  lines.push("");
  lines.push(
    `    ${import_chalk130.default.cyan(`$ ${packageName} ${commandName} ${integrationSlug} --no-claim`)}`
  );
  lines.push("");
  return lines.join("\n");
}
function buildMetadataExample(integrationSlug, products, commandName) {
  for (const product of products) {
    const schema = product.metadataSchema;
    if (!schema)
      continue;
    const flags = [];
    for (const [key, prop] of Object.entries(schema.properties)) {
      if (isHiddenOnCreate(prop)) {
        continue;
      }
      if (prop.type === "boolean") {
        flags.push(`-m ${key}=true`);
      } else if (prop.type === "array") {
        const visible = getVisibleOptions(prop);
        if (visible && visible.length > 0) {
          flags.push(`-m "${key}=${visible.slice(0, 2).join(",")}"`);
        }
      } else {
        const visible = getVisibleOptions(prop);
        if (visible && visible.length > 0) {
          flags.push(`-m ${key}=${visible[0]}`);
        }
      }
      if (flags.length >= 2)
        break;
    }
    if (flags.length > 0) {
      const slug = products.length > 1 ? `${integrationSlug}/${product.slug}` : integrationSlug;
      return `${packageName} ${commandName} ${slug} ${flags.join(" ")}`;
    }
  }
  return void 0;
}

// src/util/integration/fetch-billing-plans.ts
async function fetchBillingPlans(client, integration, product, metadata) {
  const searchParams = new URLSearchParams();
  searchParams.set("metadata", JSON.stringify(metadata));
  return client.fetch(
    `/v1/integrations/integration/${integration.id}/products/${product.id}/plans?${searchParams}`,
    {
      json: true
    }
  );
}

// src/commands/integration/add-help.ts
async function printAddDynamicHelp(client, rawArg, baseCommand, printHelp, commandName) {
  if (!rawArg) {
    return false;
  }
  const integrationSlug = rawArg.split("/")[0];
  const productSlug = rawArg.includes("/") ? rawArg.split("/")[1] : void 0;
  try {
    const integration = await fetchIntegration(client, integrationSlug);
    const products = integration.products ?? [];
    printHelp({ ...baseCommand, examples: [] });
    output_manager_default.print(formatDynamicExamples(integrationSlug, products, commandName));
    if (products.length > 1) {
      output_manager_default.print(formatProductHelp(integrationSlug, products, commandName));
    }
    for (const product of products) {
      const mergedSchema = mergeMetadataSchemas(
        product.metadataSchema,
        integration.metadataSchema
      );
      if (mergedSchema) {
        const metadataProductSlug = products.length > 1 ? product.slug : void 0;
        output_manager_default.print(
          formatMetadataSchemaHelp(
            mergedSchema,
            integrationSlug,
            metadataProductSlug
          )
        );
      }
    }
    const productsToShow = productSlug ? products.filter((p) => p.slug === productSlug) : products;
    for (const product of productsToShow) {
      try {
        const { plans } = await fetchBillingPlans(
          client,
          integration,
          product,
          {}
        );
        output_manager_default.print(formatBillingPlansHelp(product.name, plans));
      } catch (err) {
        output_manager_default.debug(
          `Failed to fetch billing plans for ${product.slug}: ${err}`
        );
      }
    }
    return true;
  } catch (err) {
    output_manager_default.debug(`Failed to fetch integration for dynamic help: ${err}`);
    return false;
  }
}

// src/util/telemetry/commands/install/index.ts
var InstallTelemetryClient = class extends TelemetryClient {
};

// src/commands/install/index.ts
async function install(client) {
  const flagsSpecification = getFlagsSpecification(addSubcommand6.options);
  let parsed;
  try {
    parsed = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags } = parsed;
  const telemetry2 = new InstallTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (flags["--help"]) {
    telemetry2.trackCliFlagHelp("install");
    const printed = await printAddDynamicHelp(
      client,
      args[1],
      installCommand,
      (c) => output_manager_default.print(help(c, { columns: client.stderr.columns })),
      "install"
    );
    if (!printed) {
      output_manager_default.print(help(installCommand, { columns: client.stderr.columns }));
    }
    return 0;
  }
  return add7(client, args.slice(1), flags, "install");
}

// src/util/telemetry/commands/integration/index.ts
var IntegrationTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandAcceptTerms(actual) {
    this.trackCliSubcommand({
      subcommand: "accept-terms",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandInstallations(actual) {
    this.trackCliSubcommand({
      subcommand: "installations",
      value: actual
    });
  }
  trackCliSubcommandDiscover(actual) {
    this.trackCliSubcommand({
      subcommand: "discover",
      value: actual
    });
  }
  trackCliSubcommandOpen(actual) {
    this.trackCliSubcommand({
      subcommand: "open",
      value: actual
    });
  }
  trackCliSubcommandBalance(actual) {
    this.trackCliSubcommand({
      subcommand: "balance",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandUpdate(actual) {
    this.trackCliSubcommand({
      subcommand: "update",
      value: actual
    });
  }
  trackCliSubcommandGuide(actual) {
    this.trackCliSubcommand({
      subcommand: "guide",
      value: actual
    });
  }
  trackCliSubcommandResource(actual) {
    this.trackCliSubcommand({
      subcommand: "resource",
      value: actual
    });
  }
};

// src/commands/integration/balance.ts
var import_chalk131 = __toESM(require_source(), 1);

// src/util/integration/fetch-installation-prepayment-info.ts
async function fetchInstallationPrepaymentInfo(client, installationId) {
  return await client.fetch(
    `/v1/integrations/installations/${installationId}/billing/balance`,
    {
      json: true
    }
  );
}
async function getBalanceInformation(client, installationId) {
  output_manager_default.spinner("Retrieving balance info\u2026", 500);
  try {
    const prepaymentInfo = await fetchInstallationPrepaymentInfo(
      client,
      installationId
    );
    output_manager_default.stopSpinner();
    if (!prepaymentInfo) {
      output_manager_default.error("No balance information found for this integration");
      return;
    }
    return prepaymentInfo;
  } catch (error) {
    output_manager_default.stopSpinner();
    output_manager_default.error(`Failed to fetch balance info: ${error.message}`);
    return;
  }
}

// src/util/integration/fetch-marketplace-integrations.ts
async function fetchMarketplaceIntegrations(client, slug) {
  const searchParams = new URLSearchParams();
  searchParams.set("view", "account");
  searchParams.set("installationType", "marketplace");
  searchParams.set("integrationIdOrSlug", slug);
  return await client.fetch(
    `/v2/integrations/configurations?${searchParams}`,
    {
      json: true
    }
  );
}
async function getFirstConfiguration(client, integrationSlug) {
  const configurations = await fetchMarketplaceIntegrations(
    client,
    integrationSlug
  );
  return configurations.length > 0 ? configurations[0] : void 0;
}

// src/util/telemetry/commands/integration/balance.ts
var IntegrationBalanceTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIntegration(v, known) {
    if (v) {
      this.trackCliArgument({
        arg: "name",
        value: known ? v : this.redactedValue
      });
    }
  }
};

// src/commands/integration/balance.ts
async function balance(client) {
  const telemetry2 = new IntegrationBalanceTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(balanceSubcommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(3), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  if (parsedArguments.args.length > 2) {
    output_manager_default.error("Cannot specify more than one integration at a time");
    return 1;
  }
  const integrationSlug = parsedArguments.args[1];
  if (!integrationSlug) {
    output_manager_default.error("You must pass an integration slug");
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  const installation = await getBalanceInstallationId(
    client,
    integrationSlug,
    telemetry2
  );
  if (installation === void 0) {
    return 1;
  }
  const installationId = installation.id;
  const resources = await getResourcesForInstallation(client, installationId);
  if (resources === void 0) {
    return 1;
  }
  const prepaymentInfo = await getBalanceInformation(client, installationId);
  if (prepaymentInfo === void 0) {
    return 1;
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonData = buildJsonOutput(
      prepaymentInfo,
      resources,
      integrationSlug
    );
    client.stdout.write(`${JSON.stringify(jsonData, null, 2)}
`);
    return 0;
  }
  outputBalanceInformation(prepaymentInfo, resources, integrationSlug);
  return 0;
}
async function getBalanceInstallationId(client, integrationSlug, telemetry2) {
  let knownIntegrationSlug = false;
  output_manager_default.spinner("Retrieving installation\u2026", 500);
  try {
    const installation = await getFirstConfiguration(client, integrationSlug);
    if (!installation) {
      output_manager_default.stopSpinner();
      output_manager_default.error("No installations found for this integration");
      return;
    }
    knownIntegrationSlug = true;
    return installation;
  } catch (error) {
    output_manager_default.stopSpinner();
    output_manager_default.error(`Failed to fetch installations: ${error.message}`);
    return;
  } finally {
    telemetry2.trackCliArgumentIntegration(
      integrationSlug,
      knownIntegrationSlug
    );
  }
}
async function getResourcesForInstallation(client, installationId) {
  output_manager_default.spinner("Retrieving resources\u2026", 500);
  try {
    const resources = (await getResources(client)).filter(
      (resource) => resource.product?.integrationConfigurationId === installationId
    );
    output_manager_default.stopSpinner();
    return resources;
  } catch (error) {
    output_manager_default.stopSpinner();
    output_manager_default.error(`Failed to fetch resources: ${error.message}`);
    return;
  }
}
function buildJsonOutput(prepaymentInfo, resources, integrationSlug) {
  const balances = prepaymentInfo.balances.map((balance2) => {
    const resourceName = balance2.resourceId ? resources.find((r) => r.externalResourceId === balance2.resourceId)?.name ?? balance2.resourceId : "installation";
    return {
      resourceName,
      resourceId: balance2.resourceId ?? null,
      amountInCents: balance2.currencyValueInCents,
      amount: formattedCurrency(balance2.currencyValueInCents)
    };
  });
  const thresholds = prepaymentInfo.thresholds.map((threshold) => {
    const resourceName = threshold.resourceId ? resources.find((r) => r.externalResourceId === threshold.resourceId)?.name ?? threshold.resourceId : "installation";
    return {
      resourceName,
      resourceId: threshold.resourceId ?? null,
      minimumAmountInCents: threshold.minimumAmountInCents,
      minimumAmount: formattedCurrency(threshold.minimumAmountInCents),
      purchaseAmountInCents: threshold.purchaseAmountInCents,
      purchaseAmount: formattedCurrency(threshold.purchaseAmountInCents)
    };
  });
  return {
    integration: integrationSlug,
    balances,
    thresholds
  };
}
function outputBalanceInformation(prepaymentInfo, resources, integrationSlug) {
  const hasBalances = prepaymentInfo.balances.length > 0;
  const hasThresholds = prepaymentInfo.thresholds.length > 0;
  if (!hasBalances) {
    output_manager_default.log("No balances found for this integration");
  }
  if (!hasThresholds) {
    output_manager_default.log("No thresholds found for this integration");
  }
  if (!hasBalances && !hasThresholds) {
    return 0;
  }
  const mappings = {};
  for (const balance2 of prepaymentInfo.balances) {
    const resourceName = balance2.resourceId ? resources.find((r) => r.externalResourceId === balance2.resourceId)?.name ?? balance2.resourceId : "installation";
    mappings[balance2.resourceId ?? "installation"] = { balance: balance2, resourceName };
  }
  for (const threshold of prepaymentInfo.thresholds) {
    const mapping = mappings[threshold.resourceId ?? "installation"];
    if (mapping) {
      mapping.threshold = threshold;
    } else {
      const resourceName = threshold.resourceId ? resources.find((r) => r.externalResourceId === threshold.resourceId)?.name ?? threshold.resourceId : "installation";
      mappings[threshold.resourceId ?? "installation"] = {
        threshold,
        resourceName
      };
    }
  }
  output_manager_default.log(
    `${import_chalk131.default.bold(`Balances and thresholds for ${integrationSlug}`)}:`
  );
  for (const key in mappings) {
    const mapping = mappings[key];
    const name = mapping.resourceName;
    const balance2 = mapping.balance;
    const threshold = mapping.threshold;
    output_manager_default.log(`\u25CF ${name}`);
    if (balance2) {
      output_manager_default.log(
        `    Balance: ${formattedCurrency(balance2.currencyValueInCents)}`
      );
    }
    if (threshold) {
      output_manager_default.log(
        `    Threshold: Spend ${formattedCurrency(threshold.purchaseAmountInCents)} if balance goes below ${formattedCurrency(threshold.minimumAmountInCents)}`
      );
    }
  }
}
function formattedCurrency(amountInCents) {
  return Intl.NumberFormat("en-US", {
    style: "currency",
    currency: "USD"
  }).format(amountInCents / 100);
}

// src/util/telemetry/commands/integration-resource/index.ts
var IntegrationResourceTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCreateThreshold(actual) {
    this.trackCliSubcommand({
      subcommand: "create-threshold",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandDisconnect(actual) {
    this.trackCliSubcommand({
      subcommand: "disconnect",
      value: actual
    });
  }
  trackCliSubcommandClaim(actual) {
    this.trackCliSubcommand({
      subcommand: "claim",
      value: actual
    });
  }
  trackCliSubcommandConnect(actual) {
    this.trackCliSubcommand({
      subcommand: "connect",
      value: actual
    });
  }
};

// src/commands/integration-resource/connect.ts
var import_chalk132 = __toESM(require_source(), 1);

// src/util/telemetry/commands/integration-resource/connect.ts
var IntegrationResourceConnectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentProject(v) {
    if (v) {
      this.trackCliArgument({
        arg: "project",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(v) {
    if (v?.length) {
      this.trackCliOption({
        option: "environment",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionPrefix(v) {
    if (v) {
      this.trackCliOption({
        option: "prefix",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/integration-resource/connect.ts
async function connect2(client, argv) {
  const telemetry2 = new IntegrationResourceConnectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(connectSubcommand2.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!parsedArguments.flags["--yes"];
  const prefix = parsedArguments.flags["--prefix"];
  const environmentsFlag = parsedArguments.flags["--environment"];
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  telemetry2.trackCliFlagYes(skipConfirmation);
  telemetry2.trackCliOptionPrefix(prefix);
  telemetry2.trackCliOptionEnvironment(environmentsFlag);
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  if (parsedArguments.args.length < 1) {
    output_manager_default.error("You must specify a resource. See `--help` for details.");
    return 1;
  }
  if (parsedArguments.args.length > 2) {
    output_manager_default.error(
      "Too many arguments. Usage: `vercel integration resource connect <resource> [project]`."
    );
    return 1;
  }
  if (prefix !== void 0 && !/^[a-zA-Z][a-zA-Z0-9_]*$/.test(prefix)) {
    output_manager_default.error(
      "Invalid --prefix value. It must start with a letter and contain only letters, digits, and underscores."
    );
    return 1;
  }
  let environments;
  if (environmentsFlag?.length) {
    const envValidation = validateEnvironments(environmentsFlag);
    if (!envValidation.valid) {
      output_manager_default.error(
        `Invalid environment value: ${envValidation.invalid.map((e2) => `"${e2}"`).join(", ")}. Must be one of: ${VALID_ENVIRONMENTS.join(", ")}`
      );
      return 1;
    }
    environments = environmentsFlag;
  } else {
    environments = [...VALID_ENVIRONMENTS];
  }
  const resourceName = parsedArguments.args[0];
  const specifiedProject = parsedArguments.args[1];
  telemetry2.trackCliArgumentResource(resourceName);
  telemetry2.trackCliArgumentProject(specifiedProject);
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  output_manager_default.spinner("Retrieving resource\u2026", 500);
  const resources = await getResources(client);
  const targetedResource = resources.find(
    (resource) => resource.name === resourceName
  );
  output_manager_default.stopSpinner();
  if (!targetedResource) {
    output_manager_default.error(`No resource ${import_chalk132.default.bold(resourceName)} found.`);
    return 1;
  }
  let projectName = specifiedProject;
  if (!projectName) {
    projectName = await getLinkedProject(client).then((result) => {
      if (result.status === "linked") {
        return result.project.name;
      }
      return;
    });
    if (!projectName) {
      output_manager_default.error(
        "No project linked. Either use `vc link` to link a project, or specify the project name."
      );
      return 1;
    }
  }
  const alreadyConnected = targetedResource.projectsMetadata?.find(
    (p) => p.name === projectName
  );
  if (alreadyConnected) {
    output_manager_default.error(
      `Project ${import_chalk132.default.bold(projectName)} is already connected to resource ${import_chalk132.default.bold(targetedResource.name)}.`
    );
    output_manager_default.log(
      `To change environments or env var prefix, disconnect first: \`vercel integration resource disconnect ${targetedResource.name} ${projectName}\``
    );
    return 1;
  }
  output_manager_default.spinner("Resolving project\u2026", 500);
  const project = await getProjectByNameOrId(client, projectName);
  output_manager_default.stopSpinner();
  if (project instanceof ProjectNotFound) {
    output_manager_default.error(`No project ${import_chalk132.default.bold(projectName)} found.`);
    return 1;
  }
  if (!skipConfirmation && client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Connecting a resource requires confirmation. Re-run with --yes.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation) {
    output_manager_default.log(
      `The resource ${import_chalk132.default.bold(targetedResource.name)} will be connected to project ${import_chalk132.default.bold(projectName)} (environments: ${environments.join(", ")}).`
    );
    const confirmed = await client.input.confirm(
      `${import_chalk132.default.cyan("Connect?")}`,
      true
    );
    if (!confirmed) {
      output_manager_default.log("Canceled");
      return 0;
    }
  }
  try {
    output_manager_default.spinner("Connecting resource\u2026", 500);
    await connectResourceToProject(
      client,
      project.id,
      targetedResource.id,
      environments,
      prefix !== void 0 ? { envVarPrefix: prefix } : void 0
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    if (isAPIError(error) && error.status === 400) {
      const conflict = error.serverMessage?.match(
        /existing environment variable with name ([A-Za-z_][A-Za-z0-9_]*)/
      );
      if (conflict) {
        const varName = conflict[1];
        const envRmCmd = environments.length === 1 ? `vercel env rm ${varName} ${environments[0]}` : `vercel env rm ${varName}`;
        output_manager_default.error(
          `Cannot connect: env var ${import_chalk132.default.bold(varName)} already exists on project ${import_chalk132.default.bold(projectName)} in one of the target environments (${environments.join(", ")}).`
        );
        if (prefix === void 0) {
          output_manager_default.log(
            `Re-run with \`--prefix <PREFIX>_\` to namespace the new variables, or remove the existing one with \`${envRmCmd}\`.`
          );
        } else {
          output_manager_default.log(
            `Prefix \`${prefix}\` did not avoid the collision. Try a different prefix or run \`${envRmCmd}\`.`
          );
        }
        return 1;
      }
    }
    output_manager_default.error(
      `A problem occurred while connecting: ${error.message}`
    );
    return 1;
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    client.stdout.write(
      `${JSON.stringify(
        {
          resource: targetedResource.name,
          connected: true,
          project: projectName,
          environments
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  output_manager_default.success(
    `Connected ${import_chalk132.default.bold(targetedResource.name)} to ${import_chalk132.default.bold(projectName)} (${environments.join(", ")})`
  );
  return 0;
}

// src/commands/integration-resource/create-threshold.ts
var import_chalk133 = __toESM(require_source(), 1);

// src/util/telemetry/commands/integration-resource/create-threshold.ts
var IntegrationResourceCreateThresholdTelemetryClient = class extends TelemetryClient {
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentMinimum(v) {
    if (v) {
      this.trackCliArgument({
        arg: "minimum",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentSpend(v) {
    if (v) {
      this.trackCliArgument({
        arg: "spend",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentLimit(v) {
    if (v) {
      this.trackCliArgument({
        arg: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/util/integration-resource/update-threshold.ts
async function updateThreshold(client, installationId, resourceId, billingPlanId, minimumAmountInCents, purchaseAmountInCents, maximumAmountPerPeriodInCents, metadata) {
  return await client.fetch(
    `/v1/integrations/installations/${installationId}/resources/${resourceId}/billing/threshold`,
    {
      method: "POST",
      json: true,
      body: {
        billingPlanId,
        minimumAmountInCents,
        purchaseAmountInCents,
        maximumAmountPerPeriodInCents,
        metadata: JSON.stringify(metadata)
      }
    }
  );
}

// src/util/integration/update-installation-threshold.ts
async function updateInstallationThreshold(client, installationId, billingPlanId, minimumAmountInCents, purchaseAmountInCents, maximumAmountPerPeriodInCents, metadata) {
  await client.fetch(
    `/v1/integrations/installations/${installationId}/billing/threshold/batch`,
    {
      method: "POST",
      json: false,
      headers: { "content-type": "application/json; charset=utf-8" },
      body: JSON.stringify([
        {
          billingPlanId,
          minimumAmountInCents,
          purchaseAmountInCents,
          maximumAmountPerPeriodInCents,
          metadata: JSON.stringify(metadata)
        }
      ])
    }
  );
}

// src/commands/integration-resource/create-threshold.ts
async function createThreshold(client, argv) {
  const telemetry2 = new IntegrationResourceCreateThresholdTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(
    createThresholdSubcommand.options
  );
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  let args;
  try {
    args = parseCreateThresholdArguments(parsedArguments.args, telemetry2);
  } catch (error) {
    output_manager_default.error(error.message);
    return 1;
  }
  const skipConfirmWithYes = parsedArguments.flags["--yes"];
  telemetry2.trackCliFlagYes(skipConfirmWithYes);
  const { resourceName, minimum, spend, limit } = args;
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  output_manager_default.spinner("Retrieving resource\u2026", 500);
  const resources = await getResources(client);
  const targetedResource = resources.find(
    (resource) => resource.name === resourceName
  );
  output_manager_default.stopSpinner();
  if (!targetedResource) {
    output_manager_default.error(`The resource ${import_chalk133.default.bold(resourceName)} was not found.`);
    return 1;
  }
  if (!targetedResource.product?.integrationConfigurationId) {
    output_manager_default.error(
      `The resource ${import_chalk133.default.bold(resourceName)} does not have an integration configuration.`
    );
    return 1;
  }
  if (targetedResource.billingPlan?.type !== "prepayment") {
    output_manager_default.error(
      `The resource ${import_chalk133.default.bold(resourceName)} is not a prepayment-based resource.`
    );
    return 1;
  }
  const minimumSpend = parseFloat(targetedResource.billingPlan.minimumAmount ?? "0.50") * 100;
  if (minimumSpend > spend) {
    output_manager_default.error(
      `The spend amount $${spend / 100} is below your billing plan's minimum amount of $${minimumSpend / 100}.`
    );
    return 1;
  }
  const maximumSpend = parseFloat(targetedResource.billingPlan.maximumAmount ?? "1000000") * 100;
  if (targetedResource.billingPlan.maximumAmount !== void 0 && maximumSpend < spend) {
    output_manager_default.error(
      `The spend amount $${spend / 100} is above your billing plan's maximum amount of $${maximumSpend / 100}.`
    );
    return 1;
  }
  const prepaymentInfo = await getBalanceInformation(
    client,
    targetedResource.product.integrationConfigurationId
  );
  if (prepaymentInfo === void 0) {
    return 1;
  }
  if (targetedResource.billingPlan.scope !== "resource") {
    output_manager_default.log(
      `The resource ${import_chalk133.default.bold(resourceName)} uses an installation-level balance.`
    );
    return await updateThresholdForInstallation({
      client,
      resource: targetedResource,
      prepaymentInfo,
      minimum,
      spend,
      limit,
      skipConfirmWithYes: Boolean(skipConfirmWithYes)
    });
  }
  return await updateThresholdForResource({
    client,
    resource: targetedResource,
    prepaymentInfo,
    minimum,
    spend,
    limit,
    skipConfirmWithYes: Boolean(skipConfirmWithYes)
  });
}
function parseCreateThresholdArguments(passedArgs, telemetry2) {
  if (passedArgs.length < 4) {
    throw new Error("Missing arguments. See `--help` for details.");
  }
  if (passedArgs.length > 4) {
    throw new Error("Too many arguments. See `--help` for details.");
  }
  const args = passedArgs.slice(0, 4);
  telemetry2.trackCliArgumentResource(args[0]);
  telemetry2.trackCliArgumentMinimum(args[1]);
  telemetry2.trackCliArgumentSpend(args[2]);
  telemetry2.trackCliArgumentLimit(args[3]);
  const resourceName = args[0];
  const minimum = Math.round(Number.parseFloat(args[1]) * 100);
  const spend = Math.round(Number.parseFloat(args[2]) * 100);
  const limit = Math.round(Number.parseFloat(args[3]) * 100);
  if (isNaN(minimum)) {
    throw new Error(
      'Minimum is an invalid number format. Minimum must be a positive number (ex. "5.75")'
    );
  }
  if (isNaN(spend)) {
    throw new Error(
      'Spend is an invalid number format. Spend must be a positive number (ex. "10.99").'
    );
  }
  if (isNaN(limit)) {
    throw new Error(
      'Limit is an invalid number format. Limit must be a positive number (ex. "1000").'
    );
  }
  if (minimum < 0) {
    throw new Error("Minimum cannot be negative.");
  }
  if (spend < 0) {
    throw new Error("Spend cannot be negative.");
  }
  if (limit < 0) {
    throw new Error("Limit cannot be negative.");
  }
  if (minimum > spend) {
    throw new Error("Minimum cannot be greater than spend.");
  }
  if (minimum > limit) {
    throw new Error("Minimum cannot be greater than limit.");
  }
  if (limit < spend) {
    throw new Error("Limit cannot be less than spend.");
  }
  return { resourceName, minimum, spend, limit };
}
async function updateThresholdForResource(props) {
  const existingThreshold = props.prepaymentInfo.thresholds.find(
    (threshold) => threshold.resourceId === props.resource.externalResourceId
  );
  const existingBalance = props.prepaymentInfo.balances.find(
    (balance2) => balance2.resourceId === props.resource.externalResourceId
  );
  return handleUpdateThreshold({
    client: props.client,
    resource: props.resource,
    minimum: props.minimum,
    spend: props.spend,
    limit: props.limit,
    existingThreshold,
    existingBalance,
    skipConfirmWithYes: props.skipConfirmWithYes,
    isInstallationLevel: false
  });
}
async function updateThresholdForInstallation(props) {
  const existingThreshold = props.prepaymentInfo.thresholds.find(
    (threshold) => threshold.resourceId === void 0
  );
  const existingBalance = props.prepaymentInfo.balances.find(
    (balance2) => balance2.resourceId === void 0
  );
  return handleUpdateThreshold({
    client: props.client,
    resource: props.resource,
    minimum: props.minimum,
    spend: props.spend,
    limit: props.limit,
    existingThreshold,
    existingBalance,
    skipConfirmWithYes: props.skipConfirmWithYes,
    isInstallationLevel: true
  });
}
async function handleUpdateThreshold(props) {
  if (props.resource.billingPlan?.type !== "prepayment") {
    output_manager_default.log(
      `The resource ${import_chalk133.default.bold(props.resource.name)} is not a prepayment-based resource.`
    );
    return 0;
  }
  if (!props.resource.product?.integrationConfigurationId) {
    output_manager_default.log(
      `The resource ${import_chalk133.default.bold(props.resource.name)} does not have an integration configuration.`
    );
    return 0;
  }
  if (!props.skipConfirmWithYes && !props.client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  const entityTextReference = props.isInstallationLevel ? `installation ${import_chalk133.default.bold(props.resource.product?.name)}` : `resource ${import_chalk133.default.bold(props.resource.name)}`;
  if (props.existingThreshold) {
    const shouldOverwriteThreshold = props.skipConfirmWithYes || await props.client.input.confirm(
      `The ${entityTextReference} already has a threshold. (minimum: $${props.existingThreshold.minimumAmountInCents / 100}, spend: $${props.existingThreshold.purchaseAmountInCents / 100}, limit: ${props.existingThreshold.maximumAmountPerPeriodInCents ? `$${props.existingThreshold.maximumAmountPerPeriodInCents / 100}` : "none set"}). Do you want to overwrite it?`,
      true
    );
    if (!shouldOverwriteThreshold) {
      output_manager_default.log("Aborting\u2026");
      return 0;
    }
  }
  if (props.existingBalance && props.existingBalance.currencyValueInCents < props.minimum) {
    const shouldOverwriteBalance = props.skipConfirmWithYes || await props.client.input.confirm(
      `The minimum threshold is higher than the current balance of $${props.existingBalance.currencyValueInCents / 100}. Are you sure?`,
      true
    );
    if (!shouldOverwriteBalance) {
      output_manager_default.log("Aborting\u2026");
      return 0;
    }
  }
  const thresholdCreationConfirmed = props.skipConfirmWithYes || await props.client.input.confirm(
    `Are you sure you want to create a threshold for the ${entityTextReference} with minimum $${props.minimum / 100}, spend $${props.spend / 100}, and limit $${props.limit / 100}?`,
    true
  );
  if (!thresholdCreationConfirmed) {
    output_manager_default.log("Aborting\u2026");
    return 0;
  }
  const metadata = JSON.parse(props.existingThreshold?.metadata ?? "{}");
  output_manager_default.spinner("Creating threshold\u2026", 500);
  if (props.isInstallationLevel) {
    await updateInstallationThreshold(
      props.client,
      props.resource.product.integrationConfigurationId,
      props.resource.billingPlan.id,
      props.minimum,
      props.spend,
      props.limit,
      metadata
    );
  } else {
    await updateThreshold(
      props.client,
      props.resource.product.integrationConfigurationId,
      props.resource.id,
      props.resource.billingPlan.id,
      props.minimum,
      props.spend,
      props.limit,
      metadata
    );
  }
  output_manager_default.stopSpinner();
  output_manager_default.success(`Threshold for ${entityTextReference} created successfully.`);
  return 0;
}

// src/commands/integration-resource/disconnect.ts
var import_chalk134 = __toESM(require_source(), 1);

// src/util/integration-resource/disconnect-resource-from-project.ts
async function disconnectResourceFromProject(client, resource, connection) {
  return client.fetch(
    `/v1/storage/stores/${resource.id}/connections/${connection.id}`,
    {
      json: true,
      method: "DELETE"
    }
  );
}
async function disconnectResourceFromAllProjects(client, resource) {
  return client.fetch(`/v1/storage/stores/${resource.id}/connections`, {
    json: true,
    method: "DELETE"
  });
}

// src/util/telemetry/commands/integration-resource/disconnect.ts
var IntegrationResourceDisconnectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentProject(v) {
    if (v) {
      this.trackCliArgument({
        arg: "project",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/util/integration-resource/types.ts
var CancelledError = class extends Error {
};
var FailedError = class extends Error {
};

// src/commands/integration-resource/disconnect.ts
async function disconnect2(client, argv) {
  const telemetry2 = new IntegrationResourceDisconnectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(
    disconnectSubcommand2.options
  );
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!parsedArguments.flags["--yes"];
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  const isMissingResource = parsedArguments.args.length < 1;
  if (isMissingResource) {
    output_manager_default.error("You must specify a resource. See `--help` for details.");
    return 1;
  }
  const hasTooManyArguments = parsedArguments.args.length > 2;
  if (hasTooManyArguments) {
    output_manager_default.error(
      "Cannot specify more than one project at a time. Use `--all` to disconnect the specified resource from all projects."
    );
    return 1;
  }
  const shouldDisconnectAll = parsedArguments.flags["--all"];
  const isProjectSpecified = parsedArguments.args.length === 2;
  if (isProjectSpecified && shouldDisconnectAll) {
    output_manager_default.error("Cannot specify a project while using the `--all` flag.");
    return 1;
  }
  const resourceName = parsedArguments.args[0];
  let specifiedProject;
  if (isProjectSpecified) {
    specifiedProject = parsedArguments.args[1];
  }
  telemetry2.trackCliArgumentResource(resourceName);
  telemetry2.trackCliArgumentProject(specifiedProject);
  telemetry2.trackCliFlagYes(skipConfirmation);
  telemetry2.trackCliFlagAll(shouldDisconnectAll);
  output_manager_default.spinner("Retrieving resource\u2026", 500);
  const resources = await getResources(client);
  const targetedResource = resources.find(
    (resource) => resource.name === resourceName
  );
  output_manager_default.stopSpinner();
  if (!targetedResource) {
    output_manager_default.error(`No resource ${import_chalk134.default.bold(resourceName)} found.`);
    return 1;
  }
  if (parsedArguments.flags["--all"]) {
    try {
      await handleDisconnectAllProjects(
        client,
        targetedResource,
        !!parsedArguments.flags["--yes"],
        asJson
      );
    } catch (error) {
      if (error instanceof CancelledError) {
        output_manager_default.log(error.message);
        return 0;
      }
      if (error instanceof FailedError) {
        output_manager_default.error(error.message);
        return 1;
      }
      throw error;
    }
    if (asJson) {
      const projects = targetedResource.projectsMetadata?.map((project) => project.name) ?? [];
      client.stdout.write(
        `${JSON.stringify({ resource: targetedResource.name, disconnected: true, projects }, null, 2)}
`
      );
    }
    return 0;
  }
  if (!specifiedProject) {
    specifiedProject = await getLinkedProject(client).then((result) => {
      if (result.status === "linked") {
        return result.project.name;
      }
      return;
    });
    if (!specifiedProject) {
      output_manager_default.error(
        "No project linked. Either use `vc link` to link a project, or specify the project name."
      );
      return 1;
    }
  }
  return await handleDisconnectProject(
    client,
    targetedResource,
    specifiedProject,
    skipConfirmation,
    asJson
  );
}
async function handleDisconnectProject(client, resource, projectName, skipConfirmation, asJson) {
  const project = resource.projectsMetadata?.find(
    (project2) => projectName === project2.name
  );
  if (!project) {
    output_manager_default.error(
      `Project ${import_chalk134.default.bold(projectName)} is not connected to resource ${import_chalk134.default.bold(resource.name)}.`
    );
    output_manager_default.log(
      `Run \`vercel integration list\` to see which projects are connected to each resource.`
    );
    return 1;
  }
  if (!skipConfirmation && client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Disconnecting a resource requires confirmation. Re-run with --yes.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!skipConfirmation && !await confirmDisconnectProject(client, resource, project)) {
    output_manager_default.log("Canceled");
    return 0;
  }
  try {
    output_manager_default.spinner("Disconnecting resource\u2026", 500);
    await disconnectResourceFromProject(client, resource, project);
  } catch (error) {
    output_manager_default.error(
      `A problem occurred while disconnecting: ${error.message}`
    );
    return 1;
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    client.stdout.write(
      `${JSON.stringify({ resource: resource.name, disconnected: true, projects: [projectName] }, null, 2)}
`
    );
    return 0;
  }
  output_manager_default.success(
    `Disconnected ${import_chalk134.default.bold(project.name)} from ${import_chalk134.default.bold(resource.name)}`
  );
  return 0;
}
async function handleDisconnectAllProjects(client, resource, skipConfirmation, asJson = false) {
  if (resource.projectsMetadata?.length === 0) {
    output_manager_default.log(`${import_chalk134.default.bold(resource.name)} has no projects to disconnect.`);
    return;
  }
  if (!skipConfirmation && client.nonInteractive) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Disconnecting all projects requires confirmation. Re-run with --yes.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
    throw new FailedError(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
  }
  if (!skipConfirmation && !client.stdin.isTTY) {
    throw new FailedError(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
  }
  if (!skipConfirmation && !await confirmDisconnectAllProjects(client, resource)) {
    throw new CancelledError("Canceled");
  }
  try {
    output_manager_default.spinner("Disconnecting projects from resource\u2026", 500);
    await disconnectResourceFromAllProjects(client, resource);
    if (asJson) {
      output_manager_default.stopSpinner();
    } else {
      output_manager_default.success(
        `Disconnected all projects from ${import_chalk134.default.bold(resource.name)}`
      );
    }
  } catch (error) {
    throw new FailedError(
      `A problem occurred while disconnecting all projects: ${error.message}`
    );
  }
  return;
}
async function confirmDisconnectProject(client, resource, project) {
  output_manager_default.log(
    `The resource ${import_chalk134.default.bold(resource.name)} will be disconnected from project ${import_chalk134.default.bold(project.name)}.`
  );
  return client.input.confirm(`${import_chalk134.default.red("Are you sure?")}`, false);
}
async function confirmDisconnectAllProjects(client, resource) {
  output_manager_default.log("The following projects will be disconnected:");
  if (!resource.projectsMetadata) {
    return false;
  }
  for (const project of resource.projectsMetadata) {
    output_manager_default.print(`  ${project.name}
`);
  }
  return client.input.confirm(import_chalk134.default.red("Are you sure?"), false);
}

// src/commands/integration-resource/remove-resource.ts
var import_chalk135 = __toESM(require_source(), 1);

// src/util/integration-resource/delete-resource.ts
async function deleteResource(client, resource) {
  return client.fetch(`/v1/storage/stores/integration/${resource.id}`, {
    json: true,
    method: "DELETE"
  });
}

// src/util/telemetry/commands/integration-resource/remove.ts
var IntegrationResourceRemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagDisconnectAll(v) {
    if (v) {
      this.trackCliFlag("disconnect-all");
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/integration-resource/remove-resource.ts
var suggestRemoveOpts = {
  prependGlobalFlags: true,
  excludeFlags: ["--yes", "-y"]
};
async function remove4(client, argv) {
  const telemetry2 = new IntegrationResourceRemoveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(removeSubcommand9.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!parsedArguments.flags["--yes"];
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  const isMissingResource = parsedArguments.args.length < 1;
  if (isMissingResource) {
    const message = "You must specify a resource. See `--help` for details.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message,
        hint: `Put global flags first, then \`integration-resource remove <resource> --yes\`. Use \`--disconnect-all\` to detach projects before delete.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration-resource remove <resource> --yes",
              packageName,
              suggestRemoveOpts
            ),
            when: "Replace <resource> with the store name (angle brackets are not part of the command)"
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  const hasTooManyArguments = parsedArguments.args.length > 1;
  if (hasTooManyArguments) {
    const message = "Cannot specify more than one resource at a time.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message,
        hint: `One resource per invocation: \`${packageName} [global flags] integration-resource remove <resource> [--disconnect-all] --yes\`.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration-resource remove <resource> --disconnect-all --yes",
              packageName,
              suggestRemoveOpts
            ),
            when: "Single resource name in place of <resource>"
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  const disconnectAll = !!parsedArguments.flags["--disconnect-all"];
  const resourceName = parsedArguments.args[0];
  telemetry2.trackCliArgumentResource(resourceName);
  telemetry2.trackCliFlagDisconnectAll(disconnectAll);
  telemetry2.trackCliFlagYes(skipConfirmation);
  output_manager_default.spinner("Retrieving resource\u2026", 500);
  const resources = await getResources(client);
  const targetedResource = resources.find(
    (resource) => resource.name === resourceName
  );
  output_manager_default.stopSpinner();
  if (!targetedResource) {
    output_manager_default.error(`No resource ${import_chalk135.default.bold(resourceName)} found.`);
    return 1;
  }
  if (disconnectAll) {
    try {
      await handleDisconnectAllProjects(
        client,
        targetedResource,
        skipConfirmation,
        asJson
      );
    } catch (error) {
      if (error instanceof CancelledError) {
        output_manager_default.log(error.message);
        return 0;
      }
      if (error instanceof FailedError) {
        output_manager_default.error(error.message);
        return 1;
      }
      throw error;
    }
  }
  return await handleDeleteResource(client, targetedResource, {
    skipConfirmation,
    skipProjectCheck: disconnectAll,
    asJson
  });
}
async function handleDeleteResource(client, resource, options) {
  const hasProjects = resource.projectsMetadata && resource.projectsMetadata?.length > 0;
  if (!options?.skipProjectCheck && hasProjects) {
    output_manager_default.error(
      `Cannot delete resource ${import_chalk135.default.bold(resource.name)} while it has connected projects. Please disconnect any projects using this resource first or use the \`--disconnect-all\` flag.`
    );
    return 1;
  }
  if (!options?.skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  if (!options?.skipConfirmation && !await confirmDeleteResource(client, resource)) {
    output_manager_default.log("Canceled");
    return 0;
  }
  try {
    output_manager_default.spinner("Deleting resource\u2026", 500);
    await deleteResource(client, resource);
  } catch (error) {
    output_manager_default.error(
      `A problem occurred when attempting to delete ${import_chalk135.default.bold(resource.name)}: ${error.message}`
    );
    return 1;
  }
  if (options?.asJson) {
    output_manager_default.stopSpinner();
    client.stdout.write(
      `${JSON.stringify({ resource: resource.name, removed: true }, null, 2)}
`
    );
    return 0;
  }
  output_manager_default.success(`${import_chalk135.default.bold(resource.name)} successfully deleted.`);
  return 0;
}
async function confirmDeleteResource(client, resource) {
  output_manager_default.log(`${import_chalk135.default.bold(resource.name)} will be deleted permanently.`);
  return client.input.confirm(`${import_chalk135.default.red("Are you sure?")}`, false);
}

// src/commands/integration-resource/index.ts
var COMMAND_CONFIG27 = {
  remove: getCommandAliases(removeSubcommand9),
  disconnect: getCommandAliases(disconnectSubcommand2),
  connect: getCommandAliases(connectSubcommand2),
  "create-threshold": getCommandAliases(createThresholdSubcommand),
  claim: getCommandAliases(claimSubcommand)
};
async function dispatchResourceSubcommand(client, subArgs, needHelp, options) {
  const telemetry2 = new IntegrationResourceTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const {
    subcommand,
    subcommandOriginal,
    args: innerArgs
  } = getSubcommand5(subArgs, COMMAND_CONFIG27);
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp(options.helpBreadcrumb);
    output_manager_default.print(
      help(integrationResourceCommand, { columns: client.stderr.columns })
    );
    return 0;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        columns: client.stderr.columns,
        parent: options.parentForChildHelp
      })
    );
  }
  switch (subcommand) {
    case "create-threshold": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp(options.helpBreadcrumb, subcommandOriginal);
        printHelp(createThresholdSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandCreateThreshold(subcommandOriginal);
      return createThreshold(client, innerArgs);
    }
    case "remove": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp(options.helpBreadcrumb, subcommandOriginal);
        printHelp(removeSubcommand9);
        return 0;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return remove4(client, innerArgs);
    }
    case "disconnect": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp(options.helpBreadcrumb, subcommandOriginal);
        printHelp(disconnectSubcommand2);
        return 0;
      }
      telemetry2.trackCliSubcommandDisconnect(subcommandOriginal);
      return disconnect2(client, innerArgs);
    }
    case "connect": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp(options.helpBreadcrumb, subcommandOriginal);
        printHelp(connectSubcommand2);
        return 0;
      }
      telemetry2.trackCliSubcommandConnect(subcommandOriginal);
      return connect2(client, innerArgs);
    }
    case "claim": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp(options.helpBreadcrumb, subcommandOriginal);
        printHelp(claimSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandClaim(subcommandOriginal);
      return claim(client, innerArgs);
    }
    default: {
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG27));
      return 2;
    }
  }
}
async function main20(client) {
  const { args, flags } = parseArguments(
    client.argv.slice(2),
    getFlagsSpecification(integrationResourceCommand.options),
    { permissive: true }
  );
  return dispatchResourceSubcommand(client, args.slice(1), !!flags["--help"], {
    helpBreadcrumb: "integration-resource",
    parentForChildHelp: integrationResourceCommand
  });
}

// src/commands/integration/list.ts
var import_chalk136 = __toESM(require_source(), 1);
var import_title3 = __toESM(require_lib2(), 1);

// src/util/telemetry/commands/integration/list.ts
var IntegrationListTelemetryClient = class extends TelemetryClient {
  trackCliArgumentProject(v) {
    if (v) {
      this.trackCliArgument({
        arg: "project",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionIntegration(v, known) {
    if (v) {
      this.trackCliOption({
        option: "integration",
        value: known ? v : this.redactedValue
      });
    }
  }
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
};

// src/commands/integration/list.ts
async function list9(client) {
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(listSubcommand11.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(3), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new IntegrationListTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetry2.trackCliArgumentProject(parsedArguments.args[1]);
  telemetry2.trackCliFlagAll(parsedArguments.flags["--all"]);
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const validationResult = validateLsArgs({
    commandName: "integration list [project]",
    args: parsedArguments.args,
    maxArgs: 2,
    exitCode: 1
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  let project;
  if (parsedArguments.args.length === 2) {
    if (parsedArguments.flags["--all"]) {
      output_manager_default.error("Cannot specify a project when using the `--all` flag.");
      return 1;
    }
    project = { name: parsedArguments.args[1] };
  }
  const { contextName, team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  if (!project && !parsedArguments.flags["--all"]) {
    project = await getLinkedProject(client).then((result) => {
      if (result.status === "linked") {
        return result.project;
      }
      return;
    });
    if (!project) {
      output_manager_default.error(
        "No project linked. Either use `vc link` to link a project, or the `--all` flag to list all resources."
      );
      return 1;
    }
  }
  let resources;
  try {
    output_manager_default.spinner("Retrieving resources\u2026", 500);
    resources = await getResources(client);
  } catch (error) {
    output_manager_default.error(`Failed to fetch resources: ${error.message}`);
    return 1;
  }
  const filterIntegration = parsedArguments.flags["--integration"]?.toLocaleLowerCase();
  function resourceIsFromMarketplace(resource) {
    return resource.type === "integration";
  }
  let knownIntegration = false;
  function filterOnIntegration(resource) {
    if (!filterIntegration)
      return true;
    const match = filterIntegration === resource.product?.slug;
    if (match)
      knownIntegration = true;
    return match;
  }
  function filterOnProject(resource) {
    return !project || !!resource.projectsMetadata?.find(
      (metadata) => metadata.projectId === project?.id || metadata.name === project?.name
    );
  }
  function filterOnFlags(resource) {
    return filterOnIntegration(resource) && filterOnProject(resource);
  }
  const results = resources.filter(resourceIsFromMarketplace).filter(filterOnFlags).map((resource) => {
    return {
      id: resource.id,
      name: resource.name,
      status: resource.status,
      product: resource.product?.name,
      integration: resource.product?.slug,
      configurationId: resource.product?.integrationConfigurationId,
      projects: resource.projectsMetadata?.map((project2) => project2.name),
      isSandbox: isSandboxResource(resource)
    };
  });
  const sandboxCount = results.filter((r) => r.isSandbox).length;
  telemetry2.trackCliOptionIntegration(
    parsedArguments.flags["--integration"],
    knownIntegration
  );
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonResources = results.map((result) => {
      const obj = {
        id: result.id,
        name: result.name,
        status: result.status,
        product: result.product,
        installationId: result.configurationId,
        projects: result.projects
      };
      if (result.isSandbox) {
        obj.claim_status = "sandbox";
      }
      return obj;
    });
    client.stdout.write(
      `${JSON.stringify({ resources: jsonResources }, null, 2)}
`
    );
    return 0;
  }
  if (results.length === 0) {
    output_manager_default.log("No resources found.");
    return 0;
  }
  const headerMessage = project ? `Integration resources for project ${import_chalk136.default.bold(project.name)} in ${import_chalk136.default.bold(contextName)}:` : `Integrations in ${import_chalk136.default.bold(contextName)}:`;
  output_manager_default.log(
    `${headerMessage}
${table(
      [
        ["Name", "Status", "Product", "Integration", "Projects"].map(
          (header) => import_chalk136.default.bold(import_chalk136.default.cyan(header))
        ),
        ...results.map((result) => [
          resourceLink(contextName, result) ?? import_chalk136.default.gray("\u2013"),
          resourceStatus(result.status ?? "\u2013", result.isSandbox),
          result.product ?? import_chalk136.default.gray("\u2013"),
          integrationLink(result, team) ?? import_chalk136.default.gray("\u2013"),
          import_chalk136.default.grey(
            result.projects?.length ? result.projects.join(", ") : "\u2013"
          )
        ])
      ],
      { hsep: 8 }
    )}`
  );
  if (sandboxCount > 0) {
    const noun = sandboxCount === 1 ? "resource" : "resources";
    output_manager_default.log(`${sandboxCount} sandbox ${noun} can be claimed.`);
    output_manager_default.print(
      `  Run \`${packageName} integration-resource claim <name>\` to claim one.
`
    );
  }
  return 0;
}
function resourceStatus(status3, isSandbox = false) {
  const CIRCLE = "\u25CF ";
  const statusTitleCase = (0, import_title3.default)(status3);
  const sandboxTag = isSandbox ? ` ${import_chalk136.default.yellow("[SANDBOX]")}` : "";
  switch (status3) {
    case "initializing":
      return import_chalk136.default.yellow(CIRCLE) + statusTitleCase + sandboxTag;
    case "error":
      return import_chalk136.default.red(CIRCLE) + statusTitleCase + sandboxTag;
    case "available":
      return import_chalk136.default.green(CIRCLE) + statusTitleCase + sandboxTag;
    case "suspended":
      return import_chalk136.default.white(CIRCLE) + statusTitleCase + sandboxTag;
    case "limits-exceeded-suspended":
      return `${import_chalk136.default.white(CIRCLE)}Limits exceeded${sandboxTag}`;
    default:
      return import_chalk136.default.gray(statusTitleCase) + sandboxTag;
  }
}
function resourceLink(orgSlug, resource) {
  if (!resource.name) {
    return;
  }
  const projectUrl = `https://vercel.com/${orgSlug}/~`;
  return output_manager_default.link(
    resource.name,
    `${projectUrl}/stores/integration/${resource.id}`,
    { fallback: () => resource.name ?? "\u2013", color: false }
  );
}
function integrationLink(integration, team) {
  if (!integration.integration) {
    return;
  }
  if (!integration.configurationId) {
    return integration.integration;
  }
  const boldName = import_chalk136.default.bold(integration.integration);
  const integrationDeepLink = buildSSOLink(team, integration.configurationId);
  return output_manager_default.link(boldName, integrationDeepLink, {
    fallback: () => boldName,
    color: false
  });
}

// src/commands/integration/open-integration.ts
var import_chalk137 = __toESM(require_source(), 1);
var import_open13 = __toESM(require_open(), 1);

// src/util/telemetry/commands/integration/open.ts
var IntegrationOpenTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(v, known) {
    this.trackCliArgument({
      arg: "name",
      value: known ? v : this.redactedValue
    });
  }
  trackCliArgumentResource(v) {
    if (v) {
      this.trackCliArgument({
        arg: "resource",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/integration/open-integration.ts
async function openIntegration(client, subArgs) {
  const telemetry2 = new IntegrationOpenTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(openSubcommand3.options);
  let parsedArguments;
  try {
    parsedArguments = parseArguments(subArgs, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArguments;
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  if (args.length > 2) {
    output_manager_default.error(
      "Too many arguments. Usage: integration open <name> [resource]"
    );
    return 1;
  }
  const integrationSlug = args[0];
  const resourceName = args[1];
  if (!integrationSlug) {
    output_manager_default.error("You must pass an integration name");
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found");
    return 1;
  }
  client.config.currentTeam = team.id;
  let configuration;
  let knownIntegrationSlug = false;
  try {
    configuration = await getFirstConfiguration(client, integrationSlug);
    knownIntegrationSlug = !!configuration;
  } catch (error) {
    output_manager_default.error(
      `Failed to fetch configuration for ${import_chalk137.default.bold(`"${integrationSlug}"`)}: ${error.message}`
    );
    return 1;
  } finally {
    telemetry2.trackCliArgumentName(integrationSlug, knownIntegrationSlug);
    if (resourceName) {
      telemetry2.trackCliArgumentResource(resourceName);
    }
  }
  if (!configuration) {
    output_manager_default.error(
      `No configuration found for ${import_chalk137.default.bold(`"${integrationSlug}"`)}.`
    );
    return 1;
  }
  const configurationId = configuration.id;
  if (resourceName) {
    let resources;
    try {
      resources = await getResources(client);
    } catch (error) {
      output_manager_default.error(`Failed to fetch resources: ${error.message}`);
      return 1;
    }
    const resource = resources.find(
      (r) => r.name === resourceName && r.product?.integrationConfigurationId === configurationId
    );
    if (!resource) {
      output_manager_default.error(
        `Resource ${import_chalk137.default.bold(`"${resourceName}"`)} not found for integration ${import_chalk137.default.bold(`"${integrationSlug}"`)}.`
      );
      return 1;
    }
    const link2 = buildSSOLink(
      team,
      configurationId,
      resource.externalResourceId
    );
    outputLink(client, link2, asJson, resourceName, true);
    return 0;
  }
  const link = buildSSOLink(team, configurationId);
  outputLink(client, link, asJson, integrationSlug, false);
  return 0;
}
function outputLink(client, link, json, name, isResource) {
  if (json) {
    client.stdout.write(`${JSON.stringify({ url: link }, null, 2)}
`);
  } else if (client.stdout.isTTY) {
    const label = isResource ? `Opening the ${import_chalk137.default.bold(name)} resource dashboard...` : `Opening the ${import_chalk137.default.bold(name)} dashboard...`;
    output_manager_default.print(label);
    (0, import_open13.default)(link);
  } else {
    client.stdout.write(`${link}
`);
  }
}

// src/commands/integration/remove-integration.ts
var import_chalk138 = __toESM(require_source(), 1);

// src/util/integration/remove-integration.ts
async function removeIntegration(client, configuration) {
  return client.fetch(
    `/v2/integrations/installations/${configuration.id}/uninstall`,
    {
      json: true,
      body: {},
      method: "POST"
    }
  );
}

// src/util/telemetry/commands/integration/remove.ts
var IntegrationRemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIntegration(v, known) {
    if (v) {
      this.trackCliArgument({
        arg: "integration",
        value: known ? v : this.redactedValue
      });
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/integration/remove-integration.ts
async function remove5(client, argv) {
  const telemetry2 = new IntegrationRemoveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(removeSubcommand10.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const skipConfirmation = !!parsedArguments.flags["--yes"];
  telemetry2.trackCliFlagYes(skipConfirmation);
  telemetry2.trackCliOptionFormat(parsedArguments.flags["--format"]);
  if (asJson && !skipConfirmation) {
    output_manager_default.error("--format=json requires --yes to skip confirmation prompts");
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found.");
    return 1;
  }
  client.config.currentTeam = team.id;
  const isMissingIntegrationSlug = parsedArguments.args.length < 1;
  if (isMissingIntegrationSlug) {
    const message = "You must specify an integration. See `--help` for details.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message,
        hint: `Put global flags (\`--cwd\`, \`--non-interactive\`, etc.) first, then \`integration remove <slug> --yes\`. Replace \`<slug>\` with your integration; non-interactive removal requires \`--yes\`.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration installations",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "List integration slugs for this team"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration remove <slug> --yes",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Substitute your integration slug for <slug> (angle brackets are not part of the command)"
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  const hasTooManyArguments = parsedArguments.args.length > 1;
  if (hasTooManyArguments) {
    const message = "Cannot specify more than one integration at a time.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message,
        hint: `Use one slug only: \`${packageName} [global flags] integration remove <slug> --yes\`. Global flags belong before \`integration\`.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration remove <slug> --yes",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Single slug in place of <slug>; keep one integration remove invocation"
          }
        ]
      },
      1
    );
    output_manager_default.error(message);
    return 1;
  }
  const integrationName = parsedArguments.args[0];
  output_manager_default.spinner("Retrieving integration\u2026", 500);
  const integrationConfiguration = await getFirstConfiguration(
    client,
    integrationName
  );
  output_manager_default.stopSpinner();
  if (!integrationConfiguration) {
    output_manager_default.error(`No integration ${import_chalk138.default.bold(integrationName)} found.`);
    telemetry2.trackCliArgumentIntegration(integrationName, false);
    return 1;
  }
  telemetry2.trackCliArgumentIntegration(integrationName, true);
  if (!skipConfirmation && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  }
  const userDidNotConfirm = !skipConfirmation && !await confirmIntegrationRemoval(
    client,
    integrationConfiguration.slug,
    team
  );
  if (userDidNotConfirm) {
    output_manager_default.log("Canceled");
    return 0;
  }
  try {
    output_manager_default.spinner("Uninstalling integration\u2026", 1e3);
    await removeIntegration(client, integrationConfiguration);
  } catch (error) {
    if (isAPIError(error) && error.status === 403 && error.serverMessage.includes("resources")) {
      let resourceNames = [];
      try {
        const searchParams = new URLSearchParams();
        searchParams.set("teamId", team.id);
        searchParams.set(
          "integrationConfigurationId",
          integrationConfiguration.id
        );
        searchParams.set("skip-metadata", "true");
        const { stores } = await client.fetch(
          `/v1/storage/stores?${searchParams}`,
          { json: true }
        );
        resourceNames = stores.map((s) => s.name);
      } catch {
      }
      const emitStructuredHasResources = asJson || shouldEmitNonInteractiveCommandError(client);
      if (emitStructuredHasResources) {
        output_manager_default.stopSpinner();
        const approvalHint = "Remove each resource with --disconnect-all (non-interactive: include --yes). Get user approval before destructive resource removal.";
        const resourceTail = asJson ? "--disconnect-all --yes --format=json" : "--disconnect-all --yes";
        const integrationRemoveTail = asJson ? `--yes --format=json` : "--yes";
        const suggestNextOpts = {
          prependGlobalFlags: true,
          excludeFlags: ["--yes", "-y"]
        };
        const payload = {
          status: "error",
          reason: AGENT_REASON.HAS_RESOURCES,
          integration: integrationName,
          removed: false,
          error: "has_resources",
          message: `Cannot uninstall ${integrationName} because it still has resources.`,
          resources: resourceNames,
          hint: client.isAgent ? `${approvalHint} Agents must obtain explicit user approval before running integration-resource remove.` : approvalHint,
          userActionRequired: client.isAgent ? true : void 0,
          next: resourceNames.map((name) => ({
            command: buildCommandWithGlobalFlags(
              client.argv,
              `integration-resource remove ${name} ${resourceTail}`,
              packageName,
              suggestNextOpts
            ),
            when: `Disconnect and remove resource ${name}; substitute the real resource name if different`
          })),
          retry: buildCommandWithGlobalFlags(
            client.argv,
            `integration remove ${integrationName} ${integrationRemoveTail}`,
            packageName,
            suggestNextOpts
          )
        };
        client.stdout.write(`${JSON.stringify(payload, null, 2)}
`);
        return 1;
      }
      output_manager_default.error(
        `Cannot uninstall ${import_chalk138.default.bold(integrationName)} because it still has resources.`
      );
      if (resourceNames.length > 0) {
        output_manager_default.log("");
        output_manager_default.log("Resources that must be removed first:");
        for (const name of resourceNames) {
          output_manager_default.log(`  ${import_chalk138.default.gray("-")} ${name}`);
        }
        output_manager_default.log("");
      }
      if (client.isAgent) {
        output_manager_default.log(
          "AGENT: You must get user approval before running any resource removal commands."
        );
      }
      output_manager_default.log(
        `Remove and disconnect all resources first with: ${import_chalk138.default.cyan(`${packageName} integration-resource remove <resource-name> --disconnect-all`)}`
      );
      output_manager_default.log(
        `Then retry: ${import_chalk138.default.cyan(`${packageName} integration remove ${integrationName}`)}`
      );
      return 1;
    }
    output_manager_default.error(
      import_chalk138.default.red(
        `Failed to remove ${import_chalk138.default.bold(integrationName)}: ${error.message}`
      )
    );
    return 1;
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    client.stdout.write(
      `${JSON.stringify({ integration: integrationName, removed: true }, null, 2)}
`
    );
    return 0;
  }
  output_manager_default.success(`${import_chalk138.default.bold(integrationName)} successfully removed.`);
  return 0;
}
async function confirmIntegrationRemoval(client, integration, team) {
  output_manager_default.log(
    `The ${import_chalk138.default.bold(integration)} integration will be removed permanently from team ${import_chalk138.default.bold(team.name)}.`
  );
  return client.input.confirm(`${import_chalk138.default.red("Are you sure?")}`, false);
}

// src/commands/integration/update-integration.ts
var import_chalk139 = __toESM(require_source(), 1);

// src/util/integration/patch-integration-configuration.ts
async function patchIntegrationConfiguration(client, configurationId, body) {
  return client.fetch(
    `/v1/integrations/configuration/${encodeURIComponent(configurationId)}`,
    {
      method: "PATCH",
      body,
      json: true
    }
  );
}

// src/util/telemetry/commands/integration/update.ts
var IntegrationUpdateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIntegration(v, known) {
    if (v) {
      this.trackCliArgument({
        arg: "integration",
        value: known ? v : this.redactedValue
      });
    }
  }
  trackCliOptionPlan(v) {
    if (v) {
      this.trackCliOption({
        option: "plan",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionAuthorizationId(v) {
    if (v) {
      this.trackCliOption({
        option: "authorization-id",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionProjects(v) {
    if (v?.length) {
      this.trackCliOption({
        option: "projects",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionInstallationId(v) {
    if (v) {
      this.trackCliOption({
        option: "installation-id",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionFormat(v) {
    if (v) {
      this.trackCliOption({
        option: "format",
        value: v
      });
    }
  }
};

// src/commands/integration/update-integration.ts
function selectConfiguration(configurations, integrationSlug, installationId) {
  if (configurations.length === 0) {
    return {
      ok: false,
      reason: AGENT_REASON.NOT_FOUND,
      message: `No integration "${integrationSlug}" found.`
    };
  }
  if (installationId) {
    const found = configurations.find((c) => c.id === installationId);
    if (!found) {
      const known = configurations.map((c) => c.id).join(", ");
      return {
        ok: false,
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: `No installation matching --installation-id ${installationId}. Known configuration IDs: ${known}`
      };
    }
    return { ok: true, configuration: found };
  }
  if (configurations.length > 1) {
    const list15 = configurations.map((c) => c.id).join(", ");
    return {
      ok: false,
      reason: AGENT_REASON.INVALID_ARGUMENTS,
      message: `Multiple installations found for "${integrationSlug}": ${list15}. Re-run with --installation-id <id> to select one. Run \`${packageName} integration installations\` to list installation IDs for this team.`
    };
  }
  return { ok: true, configuration: configurations[0] };
}
function buildProjectsBody(raw) {
  if (!raw || raw.length === 0) {
    return {
      error: 'Expected at least one --projects value (use "all" or project IDs).'
    };
  }
  if (raw.length === 1 && raw[0] === "all") {
    return { projects: "all" };
  }
  if (raw.includes("all")) {
    return {
      error: 'Cannot mix "all" with specific project IDs. Use only --projects all, or repeat --projects for each project ID.'
    };
  }
  return { projects: raw };
}
function emitUpdateCliError(client, message, reason, exitCode2, extra) {
  outputAgentError(
    client,
    {
      status: "error",
      reason,
      message,
      ...extra
    },
    exitCode2
  );
  output_manager_default.error(message);
}
async function update3(client, argv) {
  const telemetry2 = new IntegrationUpdateTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(updateSubcommand4.options);
  try {
    parsedArguments = parseArguments(argv, flagsSpecification);
  } catch (error) {
    const msg = error instanceof Error ? error.message : String(error);
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg
      },
      1
    );
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    emitUpdateCliError(
      client,
      formatResult.error,
      AGENT_REASON.INVALID_ARGUMENTS,
      1
    );
    return 1;
  }
  const preferJson = formatResult.jsonOutput || Boolean(client.nonInteractive);
  const plan = parsedArguments.flags["--plan"];
  const projectsRaw = parsedArguments.flags["--projects"];
  const authorizationId = parsedArguments.flags["--authorization-id"];
  const installationId = parsedArguments.flags["--installation-id"];
  if (parsedArguments.args.length < 1) {
    const hadSubcommandOptsFirst = projectsRaw !== void 0 && projectsRaw.length > 0 || plan !== void 0;
    const msg = hadSubcommandOptsFirst ? `Put the integration slug immediately after \`update\`, before \`--projects\`, \`--plan\`, or other subcommand options. Example: \`${packageName} integration update neon --projects all\`.` : `You must specify an integration slug after \`update\`. Example: \`${packageName} integration update neon --projects all\` or \`${packageName} integration update neon --plan <plan-id>\`.`;
    emitUpdateCliError(client, msg, AGENT_REASON.MISSING_ARGUMENTS, 1, {
      hint: hadSubcommandOptsFirst ? `You used \`--projects\` or \`--plan\` without a preceding integration name. Use \`${packageName} integration update <integration> --projects \u2026\`. Global flags such as \`--cwd\` may appear anywhere.` : `Run \`${packageName} integration installations\` to list slugs and installation IDs for your team.`,
      next: [
        {
          command: buildCommandWithGlobalFlags(
            client.argv,
            "integration update neon --projects all",
            packageName,
            { prependGlobalFlags: true }
          ),
          when: "Correct order: integration slug first, then --projects (replace neon)"
        },
        {
          command: buildCommandWithGlobalFlags(
            client.argv,
            "integration installations",
            packageName,
            { prependGlobalFlags: true }
          ),
          when: "Discover which integration slug to use"
        }
      ]
    });
    return 1;
  }
  if (parsedArguments.args.length > 1) {
    emitUpdateCliError(
      client,
      "Cannot specify more than one integration at a time.",
      AGENT_REASON.INVALID_ARGUMENTS,
      2
    );
    return 2;
  }
  const integrationSlug = parsedArguments.args[0];
  const hasPlan = plan !== void 0;
  const projectsOutcome = projectsRaw !== void 0 ? buildProjectsBody(projectsRaw) : null;
  if (hasPlan && projectsRaw !== void 0 && projectsRaw.length > 0) {
    emitUpdateCliError(
      client,
      "Pass either --plan or --projects, not both. See `vercel integration update --help`.",
      AGENT_REASON.INVALID_ARGUMENTS,
      1
    );
    return 1;
  }
  if (!hasPlan && !projectsOutcome) {
    emitUpdateCliError(
      client,
      "Nothing to update. Pass --plan to change billing plan, or --projects to change project access.",
      AGENT_REASON.MISSING_ARGUMENTS,
      1,
      {
        hint: `Example: \`${packageName} integration update ${integrationSlug} --projects all\``
      }
    );
    return 1;
  }
  if (projectsOutcome && "error" in projectsOutcome) {
    emitUpdateCliError(
      client,
      projectsOutcome.error,
      AGENT_REASON.INVALID_ARGUMENTS,
      1
    );
    return 1;
  }
  const hasProjects = Boolean(projectsOutcome && !("error" in projectsOutcome));
  telemetry2.trackCliOptionPlan(plan);
  telemetry2.trackCliOptionAuthorizationId(authorizationId);
  telemetry2.trackCliOptionProjects(projectsRaw);
  telemetry2.trackCliOptionInstallationId(installationId);
  telemetry2.trackCliOptionFormat(
    parsedArguments.flags["--format"]
  );
  const { team } = await getScope(client);
  if (!team) {
    emitUpdateCliError(
      client,
      "Team not found.",
      AGENT_REASON.MISSING_SCOPE,
      1,
      {
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "teams switch",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Switch to a team that has this integration"
          }
        ]
      }
    );
    return 1;
  }
  client.config.currentTeam = team.id;
  output_manager_default.spinner("Retrieving integration\u2026", 500);
  const configurations = await fetchMarketplaceIntegrations(
    client,
    integrationSlug
  );
  output_manager_default.stopSpinner();
  const selected = selectConfiguration(
    configurations,
    integrationSlug,
    installationId
  );
  if (!selected.ok) {
    emitUpdateCliError(client, selected.message, selected.reason, 1);
    telemetry2.trackCliArgumentIntegration(integrationSlug, false);
    return 1;
  }
  telemetry2.trackCliArgumentIntegration(integrationSlug, true);
  const body = hasPlan ? {
    billingPlanId: plan,
    ...authorizationId ? { authorizationId } : {}
  } : projectsOutcome;
  try {
    output_manager_default.spinner("Updating integration\u2026", 1e3);
    await patchIntegrationConfiguration(
      client,
      selected.configuration.id,
      body
    );
    output_manager_default.stopSpinner();
  } catch (error) {
    output_manager_default.stopSpinner();
    const apiMsg = isAPIError(error) ? error.serverMessage || error.message : error.message;
    emitUpdateCliError(
      client,
      `Failed to update ${integrationSlug}: ${apiMsg}`,
      AGENT_REASON.API_ERROR,
      1
    );
    return 1;
  }
  if (preferJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          integration: integrationSlug,
          configurationId: selected.configuration.id,
          updated: true,
          ...hasPlan ? { billingPlanId: plan } : {},
          ...hasProjects ? { projects: body.projects } : {}
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  output_manager_default.success(
    `${import_chalk139.default.bold(integrationSlug)} installation updated successfully.`
  );
  return 0;
}

// src/commands/integration/discover.ts
var import_chalk140 = __toESM(require_source(), 1);

// src/util/telemetry/commands/integration/discover.ts
var IntegrationDiscoverTelemetryClient = class extends TelemetryClient {
  trackCliFlagJson(v) {
    if (v) {
      this.trackCliFlag("json");
    }
  }
  trackCliArgumentQuery(v) {
    if (v) {
      this.trackCliArgument({
        arg: "query",
        value: this.redactedValue
      });
    }
  }
};

// src/util/integration/fetch-integration-categories.ts
async function fetchIntegrationCategories(client) {
  return client.fetch("/v2/integrations/categories", {
    json: true
  });
}

// src/commands/integration/discover.ts
var KNOWN_PROTOCOL_TYPES = /* @__PURE__ */ new Set([
  "storage",
  "ai",
  "observability",
  "messaging",
  "compute"
]);
function resolveTags(productTags, integrationTagIds, categoryTitleById) {
  const result = /* @__PURE__ */ new Set();
  const allTags = [...integrationTagIds ?? [], ...productTags ?? []];
  for (const tag of allTags) {
    if (tag.startsWith("tag_")) {
      const title4 = categoryTitleById.get(tag);
      result.add(title4 ?? tag.substring(4));
    } else if (!KNOWN_PROTOCOL_TYPES.has(tag)) {
      result.add(tag.charAt(0).toUpperCase() + tag.slice(1));
    }
  }
  return [...result];
}
async function discover(client, args) {
  let parsedArguments = null;
  const flagsSpecification = getFlagsSpecification(discoverSubcommand.options);
  try {
    parsedArguments = parseArguments(args, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new IntegrationDiscoverTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArguments.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel integration discover [query]`"
    );
    return 1;
  }
  const query = parsedArguments.args[0] ?? "";
  telemetry2.trackCliArgumentQuery(query);
  const formatResult = validateJsonOutput(parsedArguments.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliFlagJson(parsedArguments.flags["--json"]);
  output_manager_default.spinner("Fetching marketplace integrations...", 500);
  let integrations;
  let categories = [];
  try {
    const [integrationsResult, categoriesResult] = await Promise.allSettled([
      fetchMarketplaceIntegrationsList(client),
      fetchIntegrationCategories(client)
    ]);
    if (integrationsResult.status === "rejected") {
      throw integrationsResult.reason;
    }
    integrations = integrationsResult.value;
    if (categoriesResult.status === "fulfilled") {
      categories = categoriesResult.value;
    } else {
      output_manager_default.warn(
        `Failed to fetch integration categories. Continuing without categories: ${categoriesResult.reason instanceof Error ? categoriesResult.reason.message : String(categoriesResult.reason)}`
      );
    }
  } catch (error) {
    output_manager_default.error(
      `Failed to fetch marketplace integrations: ${error.message}`
    );
    return 1;
  }
  const categoryTitleById = new Map(
    categories.map((category) => [category.id, category.title])
  );
  const results = [];
  for (const integration of integrations) {
    if (!integration.isMarketplace || !integration.canInstall) {
      continue;
    }
    const products = integration.products ?? [];
    if (products.length === 0) {
      results.push({
        name: integration.name,
        slug: integration.slug,
        provider: integration.name,
        description: integration.shortDescription ?? "",
        tags: resolveTags(void 0, integration.tagIds, categoryTitleById)
      });
    } else {
      for (const product of products) {
        const needsCompoundSlug = products.length > 1 || product.slug !== integration.slug;
        results.push({
          name: product.name,
          slug: needsCompoundSlug ? `${integration.slug}/${product.slug}` : integration.slug,
          provider: integration.name,
          description: product.shortDescription ?? integration.shortDescription ?? "",
          tags: resolveTags(
            product.tags,
            integration.tagIds,
            categoryTitleById
          )
        });
      }
    }
  }
  output_manager_default.stopSpinner();
  const filtered = query ? results.filter((product) => matchesSearchTerm2(product, query)) : results;
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify(
        {
          products: filtered
        },
        null,
        2
      )}
`
    );
    return 0;
  }
  if (filtered.length === 0) {
    if (query) {
      output_manager_default.log(`No marketplace products matching "${query}" found.`);
    } else {
      output_manager_default.log("No marketplace products found.");
    }
    return 0;
  }
  const useCompactFormat = client.stderr.columns > 0 && client.stderr.columns < 120;
  const formattedOutput = useCompactFormat ? formatCompactList(filtered) : formatTable2(filtered);
  output_manager_default.log("Available marketplace products:\n" + formattedOutput);
  return 0;
}
function formatTable2(products) {
  return table(
    [
      ["Product Name", "Slug", "Provider", "Description"].map(
        (header) => import_chalk140.default.bold(import_chalk140.default.cyan(header))
      ),
      ...products.map((product) => [
        product.name,
        product.slug,
        product.provider,
        product.description || import_chalk140.default.gray("-")
      ])
    ],
    { hsep: 4 }
  );
}
function matchesSearchTerm2(product, term) {
  const lower = term.toLowerCase();
  return product.name.toLowerCase().includes(lower) || product.slug.toLowerCase().includes(lower) || product.provider.toLowerCase().includes(lower) || product.description.toLowerCase().includes(lower) || product.tags.some((tag) => tag.toLowerCase().includes(lower));
}
function formatCompactList(products) {
  return products.map((product) => {
    return [
      `${import_chalk140.default.bold(product.name)} (${product.slug})`,
      `  Provider: ${product.provider}`,
      `  Description: ${product.description || "-"}`
    ].join("\n");
  }).join("\n\n");
}

// src/commands/integration/guide.ts
var import_chalk141 = __toESM(require_source(), 1);

// src/util/telemetry/commands/integration/guide.ts
var IntegrationGuideTelemetryClient = class extends TelemetryClient {
  trackCliArgumentIntegration(v, known) {
    if (v) {
      this.trackCliArgument({
        arg: "integration",
        value: known ? v : this.redactedValue
      });
    }
  }
  trackCliOptionFramework(v) {
    if (v) {
      this.trackCliOption({
        option: "framework",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/integration/guide.ts
async function guide(client, subArgs) {
  const flagsSpecification = getFlagsSpecification(guideSubcommand.options);
  let parsedArguments;
  try {
    parsedArguments = parseArguments(subArgs, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new IntegrationGuideTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  const frameworkFlag = parsedArguments.flags["--framework"];
  telemetry2.trackCliOptionFramework(frameworkFlag);
  const rawArg = parsedArguments.args[0];
  if (!rawArg) {
    output_manager_default.error(
      "You must specify an integration. Usage: vercel integration guide <integration>[/<product>]"
    );
    return 1;
  }
  const integrationSlug = rawArg.split("/")[0];
  const productSlug = rawArg.includes("/") ? rawArg.split("/")[1] : void 0;
  output_manager_default.spinner("Fetching integration details\u2026", 500);
  let integration;
  try {
    integration = await fetchIntegration(client, integrationSlug);
  } catch (error) {
    telemetry2.trackCliArgumentIntegration(integrationSlug);
    output_manager_default.error(
      `Failed to fetch integration "${integrationSlug}": ${error.message}`
    );
    return 1;
  }
  telemetry2.trackCliArgumentIntegration(integrationSlug, true);
  const products = integration.products ?? [];
  if (products.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.log(`No products found for integration "${integrationSlug}".`);
    return 0;
  }
  let product;
  if (productSlug) {
    product = products.find((p) => p.slug === productSlug);
    if (!product) {
      output_manager_default.stopSpinner();
      output_manager_default.error(
        `Product "${productSlug}" not found for integration "${integrationSlug}".`
      );
      if (products.length > 1) {
        output_manager_default.log("Available products:");
        for (const p of products) {
          output_manager_default.log(`  ${import_chalk141.default.cyan(p.slug)}  ${p.name}`);
        }
      }
      return 1;
    }
  } else if (products.length === 1) {
    product = products[0];
  } else {
    output_manager_default.stopSpinner();
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        `"${integrationSlug}" has multiple products. Specify one with: vercel integration guide ${integrationSlug}/<product>`
      );
      output_manager_default.log("Available products:");
      for (const p of products) {
        output_manager_default.log(`  ${import_chalk141.default.cyan(p.slug)}  ${p.name}`);
      }
      return 1;
    }
    product = await client.input.select({
      message: `"${integrationSlug}" has multiple products. Select one:`,
      choices: products.map((p) => ({
        name: `${p.name} (${p.slug})`,
        value: p
      }))
    });
  }
  output_manager_default.stopSpinner();
  const guides = product.guides ?? [];
  const snippets = product.snippets ?? [];
  const resourceLinks = product.resourceLinks ?? [];
  if (guides.length === 0 && snippets.length === 0) {
    output_manager_default.log(`No guides or snippets available for "${product.name}" yet.`);
    if (resourceLinks.length > 0) {
      printResourceLinks(client, resourceLinks);
    }
    return 0;
  }
  let selectedGuide;
  if (guides.length > 0) {
    if (frameworkFlag) {
      selectedGuide = guides.find((g) => g.framework === frameworkFlag);
      if (!selectedGuide) {
        output_manager_default.error(
          `Framework "${frameworkFlag}" not found for "${product.name}".`
        );
        output_manager_default.log("Available frameworks:");
        for (const g of guides) {
          output_manager_default.log(`  ${import_chalk141.default.cyan(g.framework)}  ${g.title}`);
        }
        return 1;
      }
    } else if (guides.length === 1) {
      selectedGuide = guides[0];
    } else if (client.stdin.isTTY) {
      selectedGuide = await client.input.select({
        message: "Select a framework guide:",
        choices: guides.map((g) => ({
          name: g.title,
          value: g
        }))
      });
    } else {
      selectedGuide = guides[0];
    }
  }
  printGuide(client, product, selectedGuide, snippets, resourceLinks);
  return 0;
}
function printGuide(client, product, guide2, snippets, resourceLinks) {
  const lines = [];
  lines.push(`# ${product.name}`);
  lines.push("");
  lines.push(product.shortDescription);
  lines.push("");
  if (guide2) {
    lines.push(`## Getting Started with ${guide2.title}`);
    lines.push("");
    for (let i = 0; i < guide2.steps.length; i++) {
      const step = guide2.steps[i];
      lines.push(`### Step ${i + 1}: ${step.title}`);
      lines.push("");
      lines.push(step.content);
      lines.push("");
    }
  }
  if (snippets.length > 0) {
    lines.push("## Code Snippets");
    lines.push("");
    for (const snippet of snippets) {
      lines.push(`### ${snippet.name}`);
      lines.push("");
      lines.push(`\`\`\`${snippet.language}`);
      lines.push(snippet.content);
      lines.push("```");
      lines.push("");
    }
  }
  if (resourceLinks.length > 0) {
    appendResourceLinks(resourceLinks, lines);
  }
  client.stdout.write(`${lines.join("\n")}
`);
}
function appendResourceLinks(links, lines) {
  lines.push("## Resources");
  lines.push("");
  for (const link of links) {
    lines.push(`- [${link.title}](${link.href})`);
  }
  lines.push("");
}
function printResourceLinks(client, links) {
  const lines = [];
  appendResourceLinks(links, lines);
  client.stdout.write(`${lines.join("\n")}
`);
}

// src/util/integration/fetch-all-marketplace-installations.ts
async function fetchAllMarketplaceInstallations(client) {
  const searchParams = new URLSearchParams();
  searchParams.set("view", "account");
  searchParams.set("installationType", "marketplace");
  return client.fetch(
    `/v2/integrations/configurations?${searchParams}`,
    { json: true }
  );
}

// src/commands/integration/installations-list.ts
var import_chalk142 = __toESM(require_source(), 1);
function pickIntegrationKey(row) {
  const integration = row.integration;
  if (integration && typeof integration.slug === "string") {
    return integration.slug;
  }
  if (typeof row.integrationId === "string") {
    return row.integrationId;
  }
  return "-";
}
async function installationsList(client, argv) {
  const spec = getFlagsSpecification(installationsSubcommand.options);
  let parsed;
  try {
    parsed = parseArguments(argv, spec);
  } catch (e2) {
    printError(e2);
    return 1;
  }
  const fr = validateJsonOutput(parsed.flags);
  if (!fr.valid) {
    output_manager_default.error(fr.error);
    return 1;
  }
  if (parsed.args.length > 0) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel integration installations [--integration <slug>] [--format json]`"
    );
    return 1;
  }
  const filterSlug = parsed.flags["--integration"];
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("No team context. Run `vercel switch` or use --scope.");
    return 1;
  }
  try {
    const rows = await fetchAllMarketplaceInstallations(client);
    const filtered = filterSlug ? rows.filter((r) => {
      const key = pickIntegrationKey(r);
      return key === filterSlug || String(r.integrationId) === filterSlug;
    }) : rows;
    if (fr.jsonOutput) {
      client.stdout.write(
        `${JSON.stringify({ installations: filtered }, null, 2)}
`
      );
      return 0;
    }
    if (filtered.length === 0) {
      output_manager_default.log("No marketplace installations found.");
      return 0;
    }
    const headers = ["Installation", "Integration", "Owner"].map(
      (h) => import_chalk142.default.cyan(h)
    );
    const lines = [
      headers,
      ...filtered.map((r) => {
        const id = String(r.id ?? r.integrationConfigurationId ?? "-");
        const integ = pickIntegrationKey(r);
        const owner = String(r.ownerId ?? "-");
        return [id, integ, owner];
      })
    ];
    output_manager_default.print(`
${table(lines, { hsep: 2 }).replace(/^/gm, "  ")}
`);
    return 0;
  } catch (err) {
    if (isAPIError(err)) {
      output_manager_default.error(
        err.serverMessage || `Could not list installations (${err.status}). If your API version requires a specific integration, pass --integration <slug>.`
      );
      return 1;
    }
    throw err;
  }
}

// src/commands/integration/accept-terms.ts
var import_chalk143 = __toESM(require_source(), 1);

// src/util/integration/marketplace-install-integration.ts
async function installMarketplaceIntegration(client, integrationIdOrSlug, acceptedPolicies) {
  return client.fetch(
    `/v2/integrations/integration/${encodeURIComponent(integrationIdOrSlug)}/marketplace/install`,
    {
      method: "POST",
      json: true,
      body: { acceptedPolicies, source: "cli" }
    }
  );
}

// src/commands/integration/accept-terms.ts
function logPolicyLinksSummary(links) {
  output_manager_default.log(`Marketplace addendum: ${links.marketplace_addendum}`);
  if (links.integration_privacy_policy) {
    output_manager_default.log(`Privacy policy: ${links.integration_privacy_policy}`);
  }
  if (links.integration_eula) {
    output_manager_default.log(`Terms of service: ${links.integration_eula}`);
  }
}
async function acceptTerms(client, argv) {
  const spec = getFlagsSpecification(acceptTermsSubcommand.options);
  let parsed;
  try {
    parsed = parseArguments(argv, spec);
  } catch (e2) {
    printError(e2);
    return 1;
  }
  const formatResult = validateJsonOutput(parsed.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const preferJson = formatResult.jsonOutput || shouldEmitNonInteractiveCommandError(client);
  if (parsed.args.length < 1) {
    const msg = `Missing integration slug. Example: \`${packageName} integration accept-terms <integration>\``;
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message: msg,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "integration discover",
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Find integration slugs"
          }
        ]
      },
      1
    );
    output_manager_default.error(msg);
    return 1;
  }
  if (parsed.args.length > 1) {
    const msg = "Specify only one integration at a time.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg
      },
      1
    );
    output_manager_default.error(msg);
    return 1;
  }
  const integrationSlug = parsed.args[0];
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Team not found. Run `vercel switch` or use --scope.");
    return 1;
  }
  client.config.currentTeam = team.id;
  let integration;
  try {
    integration = await fetchIntegration(client, integrationSlug);
  } catch (error) {
    const msg = isAPIError(error) ? error.serverMessage || error.message : error.message;
    output_manager_default.error(`Failed to load integration "${integrationSlug}": ${msg}`);
    return 1;
  }
  if (integration.capabilities?.requiresBrowserInstall) {
    const msg = `Integration "${integration.slug}" requires accepting terms in the browser (or device attestation). Use \`${packageName} integration add ${integration.slug}\` instead.`;
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg,
        policy_links: getMarketplacePolicyLinks(integration),
        hint: "The dashboard accept-terms flow is required for this integration.",
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `integration add ${integration.slug}`,
              packageName,
              { prependGlobalFlags: true }
            ),
            when: "Open browser / complete required install steps"
          }
        ]
      },
      1
    );
    output_manager_default.error(msg);
    return 1;
  }
  const installations = await fetchInstallations(client, integration);
  const existing = installations.find(
    (i) => i.ownerId === team.id && i.installationType === "marketplace"
  );
  const policyLinks = getMarketplacePolicyLinks(integration);
  if (existing) {
    if (preferJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            integration: integration.slug,
            installationId: existing.id,
            alreadyInstalled: true,
            policy_links: policyLinks
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(
      `Integration ${import_chalk143.default.bold(integration.slug)} is already installed for this team.`
    );
    return 0;
  }
  let acceptedPolicies;
  if (shouldEmitNonInteractiveCommandError(client)) {
    const msg = "Marketplace terms cannot be accepted in non-interactive mode. Run this command in an interactive terminal as a human.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INTEGRATION_TERMS_ACCEPTANCE_REQUIRED,
        message: msg,
        policy_links: getMarketplacePolicyLinks(integration),
        hint: `Open the policy links, then run \`${packageName} integration accept-terms ${integration.slug}\` interactively.`
      },
      1
    );
    output_manager_default.error(msg);
    return 1;
  }
  acceptedPolicies = await promptForTermAcceptance(client, integration);
  if (!acceptedPolicies) {
    return 1;
  }
  try {
    const result = await installMarketplaceIntegration(
      client,
      integration.id,
      acceptedPolicies
    );
    if (preferJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            integration: integration.slug,
            installationId: result.id,
            installed: true,
            policy_links: policyLinks
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.success(
      `Terms accepted. Integration ${import_chalk143.default.bold(integration.slug)} is installed for this team.`
    );
    logPolicyLinksSummary(policyLinks);
    return 0;
  } catch (error) {
    if (isAPIError(error) && error.status === 409) {
      if (preferJson) {
        client.stdout.write(
          `${JSON.stringify(
            {
              integration: integration.slug,
              alreadyInstalled: true,
              policy_links: policyLinks
            },
            null,
            2
          )}
`
        );
        return 0;
      }
      output_manager_default.log(
        `Integration ${import_chalk143.default.bold(integration.slug)} is already installed for this team.`
      );
      return 0;
    }
    const msg = isAPIError(error) ? error.serverMessage || error.message : error.message;
    output_manager_default.error(`Failed to accept terms for "${integration.slug}": ${msg}`);
    return 1;
  }
}

// src/commands/integration/index.ts
var COMMAND_CONFIG28 = {
  add: getCommandAliases(addSubcommand6),
  "accept-terms": getCommandAliases(acceptTermsSubcommand),
  open: getCommandAliases(openSubcommand3),
  list: getCommandAliases(listSubcommand11),
  installations: getCommandAliases(installationsSubcommand),
  discover: getCommandAliases(discoverSubcommand),
  guide: getCommandAliases(guideSubcommand),
  balance: getCommandAliases(balanceSubcommand),
  remove: getCommandAliases(removeSubcommand10),
  resource: getCommandAliases(resourceSubcommand),
  update: getCommandAliases(updateSubcommand4)
};
async function main21(client) {
  const telemetry2 = new IntegrationTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { args, flags } = parseArguments(
    client.argv.slice(2),
    getFlagsSpecification(integrationCommand.options),
    { permissive: true }
  );
  const {
    subcommand,
    subcommandOriginal,
    args: subArgs
  } = getSubcommand5(args.slice(1), COMMAND_CONFIG28);
  const needHelp = flags["--help"];
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        columns: client.stderr.columns,
        parent: integrationCommand
      })
    );
  }
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("integration");
    output_manager_default.print(
      help(integrationCommand, {
        columns: client.stderr.columns
      })
    );
    return 0;
  }
  switch (subcommand) {
    case "add": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        const printed = await printAddDynamicHelp(
          client,
          subArgs[0],
          addSubcommand6,
          (cmd2) => printHelp(cmd2),
          "integration add"
        );
        if (!printed) {
          printHelp(addSubcommand6);
        }
        return 0;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      const addFlagsSpec = getFlagsSpecification(addSubcommand6.options);
      let addParsedArgs;
      try {
        addParsedArgs = parseArguments(subArgs, addFlagsSpec);
      } catch (error) {
        printError(error);
        return 1;
      }
      return add7(
        client,
        addParsedArgs.args,
        addParsedArgs.flags,
        "integration add"
      );
    }
    case "accept-terms": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(acceptTermsSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandAcceptTerms(subcommandOriginal);
      return acceptTerms(client, subArgs);
    }
    case "list": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(listSubcommand11);
        return 0;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return list9(client);
    }
    case "installations": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(installationsSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandInstallations(subcommandOriginal);
      return installationsList(client, subArgs);
    }
    case "discover": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(discoverSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandDiscover(subcommandOriginal);
      return discover(client, subArgs);
    }
    case "guide": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(guideSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandGuide(subcommandOriginal);
      return guide(client, subArgs);
    }
    case "balance": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(balanceSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandBalance(subcommandOriginal);
      return balance(client);
    }
    case "open": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(openSubcommand3);
        return 0;
      }
      telemetry2.trackCliSubcommandOpen(subcommandOriginal);
      return openIntegration(client, subArgs);
    }
    case "remove": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(removeSubcommand10);
        return 0;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return remove5(client, subArgs);
    }
    case "resource": {
      if (subArgs.length === 0 && needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(resourceSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandResource(subcommandOriginal);
      const nestedParent = {
        ...resourceSubcommand,
        name: "integration resource"
      };
      return dispatchResourceSubcommand(client, subArgs, !!needHelp, {
        helpBreadcrumb: "integration resource",
        parentForChildHelp: nestedParent
      });
    }
    case "update": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("integration", subcommandOriginal);
        printHelp(updateSubcommand4);
        return 0;
      }
      telemetry2.trackCliSubcommandUpdate(subcommandOriginal);
      return update3(client, subArgs);
    }
    default: {
      const validSubcommands = Object.keys(COMMAND_CONFIG28).join(" | ");
      const missingSubcommand = subArgs.length === 0;
      const message = missingSubcommand ? `Please specify a valid subcommand: ${validSubcommands}` : `Unknown subcommand "${subArgs[0]}". Valid subcommands: ${validSubcommands}`;
      outputAgentError(
        client,
        {
          status: "error",
          reason: missingSubcommand ? AGENT_REASON.MISSING_ARGUMENTS : AGENT_REASON.INVALID_ARGUMENTS,
          message,
          hint: missingSubcommand ? `Pass a subcommand after \`integration\`, for example \`${packageName} integration installations\` or \`${packageName} integration list\`. Global flags such as \`--cwd\` may appear anywhere.` : `Check the spelling or run \`${packageName} integration --help\` for the list of subcommands.`,
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "integration --help",
                packageName,
                { prependGlobalFlags: true }
              ),
              when: "Show all integration subcommands and options"
            },
            ...missingSubcommand ? [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "integration installations",
                  packageName,
                  { prependGlobalFlags: true }
                ),
                when: "List marketplace installations for the current team"
              }
            ] : []
          ]
        },
        2
      );
      output_manager_default.error(message);
      return 2;
    }
  }
}

// src/commands/logout/index.ts
var import_error_utils13 = __toESM(require_dist(), 1);

// src/util/telemetry/commands/logout/index.ts
var LogoutTelemetryClient = class extends TelemetryClient {
};

// src/commands/logout/future.ts
var import_error_utils12 = __toESM(require_dist(), 1);
async function logout(client) {
  const { authConfig } = client;
  if (!authConfig.token) {
    output_manager_default.note(
      `Not currently logged in, so ${getCommandName("logout")} did nothing`
    );
    return 0;
  }
  output_manager_default.spinner("Logging out\u2026", 200);
  const revocationResponse = await revocationRequest({
    token: authConfig.token
  });
  output_manager_default.debug(`'Revocation response:', ${await revocationResponse.clone().text()}`);
  const [revocationError] = await processRevocationResponse(revocationResponse);
  let logoutError = false;
  if (revocationError) {
    output_manager_default.error(revocationError.message);
    output_manager_default.debug(revocationError.cause);
    output_manager_default.error("Failed during logout");
    logoutError = true;
  }
  try {
    client.updateConfig({ currentTeam: void 0 });
    client.writeToConfigFile();
    client.emptyAuthConfig();
    client.persistAuthConfig();
    output_manager_default.debug("Configuration has been deleted");
    if (!logoutError) {
      output_manager_default.success("Logged out!");
      return 0;
    }
  } catch (err) {
    output_manager_default.debug((0, import_error_utils12.errorToString)(err));
    output_manager_default.error("Failed during logout");
  }
  return 1;
}

// src/commands/logout/index.ts
async function logout2(client) {
  const { authConfig, config } = client;
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(logoutCommand.options);
  const telemetry2 = new LogoutTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("logout");
    output_manager_default.print(help(logoutCommand, { columns: client.stderr.columns }));
    return 0;
  }
  if ("refreshToken" in authConfig) {
    return await logout(client);
  }
  output_manager_default.debug("Falling back to legacy logout");
  if (!authConfig.token) {
    output_manager_default.note(
      `Not currently logged in, so ${getCommandName("logout")} did nothing`
    );
    return 0;
  }
  output_manager_default.spinner("Logging out\u2026", 200);
  let exitCode2 = 0;
  try {
    await client.fetch("/v3/user/tokens/current", {
      method: "DELETE",
      useCurrentTeam: false
    });
  } catch (err) {
    if (isAPIError(err)) {
      if (err.status === 403) {
        output_manager_default.debug("Token is invalid so it cannot be revoked");
      } else if (err.status !== 200) {
        output_manager_default.debug(err?.message ?? "");
        exitCode2 = 1;
      }
    }
  }
  delete config.currentTeam;
  delete authConfig.token;
  delete authConfig.userId;
  try {
    if (!authConfig.skipWrite) {
      writeToConfigFile(config);
      persistAuthConfig(authConfig, config);
    }
    output_manager_default.debug("Configuration has been deleted");
  } catch (err) {
    output_manager_default.debug((0, import_error_utils13.errorToString)(err));
    exitCode2 = 1;
  }
  if (exitCode2 === 0) {
    output_manager_default.log("Logged out!");
  } else {
    output_manager_default.error("Failed during logout");
  }
  return exitCode2;
}

// src/commands/logs/index.ts
var import_error_utils14 = __toESM(require_dist(), 1);
var import_chalk144 = __toESM(require_source(), 1);
var import_format21 = __toESM(require_format(), 1);

// src/util/logs-v2.ts
var import_ms18 = __toESM(require_ms(), 1);
var LOG_LEVEL_SEVERITY = {
  info: 0,
  warning: 1,
  error: 2,
  fatal: 3
};
function getLogLevelSeverity(level) {
  return LOG_LEVEL_SEVERITY[level] ?? -1;
}
function getDisplayLog(logs2, requestedLevels) {
  if (logs2.length === 0) {
    return void 0;
  }
  const matchingLogs = requestedLevels && requestedLevels.length > 0 ? logs2.filter((log) => requestedLevels.includes(log.level)) : logs2;
  const candidates = matchingLogs.length > 0 ? matchingLogs : logs2;
  return candidates.reduce(
    (selected, current) => getLogLevelSeverity(current.level) > getLogLevelSeverity(selected.level) ? current : selected
  );
}
function parseRelativeTime(input) {
  const now = Date.now();
  const msValue = (0, import_ms18.default)(input);
  if (typeof msValue === "number") {
    return now - msValue;
  }
  const date = new Date(input);
  if (!isNaN(date.getTime())) {
    return date.getTime();
  }
  throw new Error(`Invalid time format: ${input}`);
}
async function fetchRequestLogs(client, options) {
  const {
    projectId,
    ownerId,
    deploymentId,
    environment,
    level,
    statusCode,
    source,
    since,
    until,
    search: search2,
    requestId,
    branch,
    page = 0
  } = options;
  const now = Date.now();
  const defaultStartDate = now - 24 * 60 * 60 * 1e3;
  const query = new URLSearchParams();
  query.set("projectId", projectId);
  query.set("ownerId", ownerId);
  query.set("page", String(page));
  query.set(
    "startDate",
    String(since ? parseRelativeTime(since) : defaultStartDate)
  );
  query.set("endDate", String(until ? parseRelativeTime(until) : now));
  if (deploymentId) {
    query.set("deploymentId", deploymentId);
  }
  if (environment) {
    query.set("environment", environment);
  }
  if (level && level.length > 0) {
    query.set("level", level.join(","));
  }
  if (statusCode) {
    query.set("statusCode", statusCode);
  }
  if (source && source.length > 0) {
    query.set("source", source.join(","));
  }
  if (search2) {
    query.set("search", search2);
  }
  if (requestId) {
    query.set("requestId", requestId);
  }
  if (branch) {
    query.set("branch", branch);
  }
  const baseUrl = client.apiUrl === "https://api.vercel.com" ? "https://vercel.com" : client.apiUrl;
  const url = `${baseUrl}/api/logs/request-logs?${query.toString()}`;
  const data = await client.fetch(url);
  const logs2 = (data.rows || []).map((row) => {
    const requestLogs = (row.logs || []).map((log) => ({
      level: log.level || "info",
      message: log.message || "",
      messageTruncated: log.messageTruncated
    }));
    const displayLog = getDisplayLog(requestLogs, options.level);
    const firstEvent = row.events?.[0];
    return {
      id: row.requestId || "",
      timestamp: row.timestamp ? new Date(row.timestamp).getTime() : Date.now(),
      deploymentId: row.deploymentId || "",
      projectId: options.projectId,
      level: displayLog?.level || "info",
      message: displayLog?.message || "",
      messageTruncated: displayLog?.messageTruncated,
      source: firstEvent?.source || "static",
      domain: row.domain || "",
      requestMethod: row.requestMethod || "",
      requestPath: row.requestPath || "",
      responseStatusCode: row.statusCode || 0,
      environment: row.environment || "production",
      branch: row.branch,
      cache: row.cache,
      traceId: row.traceId,
      logs: requestLogs
    };
  });
  return {
    logs: logs2,
    pagination: {
      hasMore: data.hasMoreRows ?? false
    }
  };
}
async function* fetchAllRequestLogs(client, options) {
  let page = 0;
  let remaining = options.limit ?? 100;
  let hasMore = true;
  while (hasMore && remaining > 0) {
    const response = await fetchRequestLogs(client, {
      ...options,
      page
    });
    if (!response.logs || response.logs.length === 0) {
      break;
    }
    for (const log of response.logs) {
      yield log;
      remaining--;
      if (remaining <= 0) {
        return;
      }
    }
    hasMore = response.pagination?.hasMore ?? false;
    page++;
  }
}

// src/util/telemetry/commands/logs/index.ts
var LogsTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrDeploymentId(v) {
    if (v) {
      this.trackCliArgument({
        arg: "urlOrDeploymentId",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionDeployment(v) {
    if (v) {
      this.trackCliOption({
        option: "deployment",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEnvironment(v) {
    if (v) {
      const allowed = ["production", "preview"].includes(v) ? v : this.redactedValue;
      this.trackCliOption({
        option: "environment",
        value: allowed
      });
    }
  }
  trackCliOptionLevel(v) {
    if (v && v.length > 0) {
      const allowedLevels = ["error", "warning", "info", "fatal"];
      const sanitized = v.every((l) => allowedLevels.includes(l)) ? v.join(",") : this.redactedValue;
      this.trackCliOption({
        option: "level",
        value: sanitized
      });
    }
  }
  trackCliOptionStatusCode(v) {
    if (v) {
      this.trackCliOption({
        option: "status-code",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSource(v) {
    if (v && v.length > 0) {
      const allowedSources = [
        "serverless",
        "edge-function",
        "edge-middleware",
        "static"
      ];
      const sanitized = v.every((s) => allowedSources.includes(s)) ? v.join(",") : this.redactedValue;
      this.trackCliOption({
        option: "source",
        value: sanitized
      });
    }
  }
  trackCliOptionSince(v) {
    if (v) {
      this.trackCliOption({
        option: "since",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionUntil(v) {
    if (v) {
      this.trackCliOption({
        option: "until",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionLimit(v) {
    if (typeof v === "number") {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagJson(v) {
    if (v) {
      this.trackCliFlag("json");
    }
  }
  trackCliFlagFollow(v) {
    if (v) {
      this.trackCliFlag("follow");
    }
  }
  trackCliFlagNoFollow(v) {
    if (v) {
      this.trackCliFlag("no-follow");
    }
  }
  trackCliOptionQuery(v) {
    if (v) {
      this.trackCliOption({
        option: "query",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSearch(v) {
    if (v) {
      this.trackCliOption({
        option: "search",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionRequestId(v) {
    if (v) {
      this.trackCliOption({
        option: "request-id",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagExpand(v) {
    if (v) {
      this.trackCliFlag("expand");
    }
  }
  trackCliOptionBranch(v) {
    if (v) {
      this.trackCliOption({
        option: "branch",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagNoBranch(v) {
    if (v) {
      this.trackCliFlag("no-branch");
    }
  }
};

// src/commands/logs/index.ts
async function getLatestDeploymentByBranch(client, projectId, branch) {
  const branchMetaKeys = [
    "githubCommitRef",
    "gitlabCommitRef",
    "bitbucketCommitRef"
  ];
  for (const metaKey of branchMetaKeys) {
    const query = new URLSearchParams();
    query.set("projectId", projectId);
    query.set("limit", "1");
    query.set("state", "READY");
    query.set(`meta-${metaKey}`, branch);
    const { deployments } = await client.fetch(
      `/v6/deployments?${query}`
    );
    if (deployments.length > 0) {
      return {
        id: deployments[0].uid,
        url: deployments[0].url
      };
    }
  }
  return null;
}
var TIME_ONLY_FORMAT = "HH:mm:ss.SS";
var DATE_TIME_FORMAT = "MMM DD HH:mm:ss.SS";
function table2({
  columns,
  rows,
  tableWidth,
  formatHeader,
  formatRow
}) {
  const zeroPad = [0, 0];
  const maxWidths = columns.map((col) => {
    const headerWidth = col.label.length;
    const maxContent = Math.max(
      headerWidth,
      ...rows.map((row) => col.getValue(row).length)
    );
    return maxContent;
  });
  const colPaddings = columns.map(
    (col) => col.padding ?? zeroPad
  );
  const finalWidths = [];
  let usedWidth = 0;
  let stretchIndex = -1;
  for (let i = 0; i < columns.length; i++) {
    const col = columns[i];
    const padding2 = colPaddings[i][0] + colPaddings[i][1];
    if (col.width === "stretch") {
      stretchIndex = i;
      finalWidths.push(0);
    } else if (typeof col.width === "number") {
      finalWidths.push(col.width);
      usedWidth += col.width + padding2;
    } else {
      finalWidths.push(maxWidths[i]);
      usedWidth += maxWidths[i] + padding2;
    }
  }
  usedWidth += (columns.length - 1) * 2;
  if (stretchIndex >= 0) {
    const stretchPadding = colPaddings[stretchIndex][0] + colPaddings[stretchIndex][1];
    finalWidths[stretchIndex] = Math.max(
      10,
      tableWidth - usedWidth - stretchPadding
    );
  }
  const pad = (value, width) => {
    if (value.length > width) {
      return value.slice(0, width - 1) + "\u2026";
    }
    return value.padEnd(width);
  };
  const headerStr = columns.map((col, i) => {
    const padded = pad(col.label, finalWidths[i]);
    return " ".repeat(colPaddings[i][0]) + padded + " ".repeat(colPaddings[i][1]);
  }).join("  ");
  const header = formatHeader ? formatHeader(headerStr) : headerStr;
  const formattedRows = rows.map((row) => {
    const rowStr = columns.map((col, i) => {
      const value = col.getValue(row);
      const padded = pad(value, finalWidths[i]);
      const formatted = col.format ? col.format(padded, row) : padded;
      return " ".repeat(colPaddings[i][0]) + formatted + " ".repeat(colPaddings[i][1]);
    }).join("  ");
    return formatRow ? formatRow(rowStr, row) : rowStr;
  });
  return { header, rows: formattedRows };
}
function logsSpanMultipleDays(logs2) {
  if (logs2.length === 0)
    return false;
  const firstDay = new Date(logs2[0].timestamp).toDateString();
  return logs2.some((log) => new Date(log.timestamp).toDateString() !== firstDay);
}
function parseLevels(levels) {
  if (!levels)
    return [];
  if (typeof levels === "string")
    return [levels];
  return levels;
}
function parseSources(sources) {
  if (!sources)
    return [];
  if (typeof sources === "string")
    return [sources];
  return sources;
}
function isNonLiveTerminalDeployment(deployment) {
  return deployment.readyState === "ERROR" || deployment.readyState === "CANCELED";
}
function getInspectCommand(deployment, contextName, { plain = false } = {}) {
  const scopeOption = contextName ? ` --scope ${contextName}` : "";
  const command = `inspect https://${deployment.url}${scopeOption}`;
  return plain ? `\`${getCommandNamePlain(command)}\`` : getCommandName(command);
}
function printNonLiveDeploymentError(deployment, contextName) {
  const inspectCommand2 = getInspectCommand(deployment, contextName);
  output_manager_default.error(
    `Logs are unavailable because deployment ${import_chalk144.default.bold(
      deployment.id
    )} never reached READY and ended in ${deployment.readyState}.
Run ${inspectCommand2} for deployment details.`
  );
}
async function resolveLogsTarget(client, { contextName, deploymentOption, projectOption }) {
  if (deploymentOption) {
    output_manager_default.spinner(`Resolving deployment "${deploymentOption}"`, 1e3);
    let deployment;
    try {
      deployment = await getDeployment(client, contextName, deploymentOption);
    } catch (err) {
      if (err instanceof DeploymentNotFound) {
        output_manager_default.error(
          `Deployment not found: ${deploymentOption} under ${import_chalk144.default.bold(
            contextName
          )}`
        );
        return { exitCode: 1 };
      }
      if (err instanceof InvalidDeploymentId) {
        output_manager_default.error(`Invalid deployment ID: ${deploymentOption}`);
        return { exitCode: 1 };
      }
      throw err;
    } finally {
      output_manager_default.stopSpinner();
    }
    if (!deployment.projectId) {
      output_manager_default.error("Deployment is not associated with a project.");
      return { exitCode: 1 };
    }
    output_manager_default.spinner(`Fetching project "${deployment.projectId}"`, 1e3);
    const project = await getProjectByNameOrId(client, deployment.projectId);
    output_manager_default.stopSpinner();
    if (project instanceof ProjectNotFound) {
      output_manager_default.error(
        `Project not found: ${deployment.projectId} under ${import_chalk144.default.bold(
          contextName
        )}`
      );
      return { exitCode: 1 };
    }
    if (projectOption) {
      output_manager_default.spinner(`Fetching project "${projectOption}"`, 1e3);
      const explicitProject = await getProjectByNameOrId(client, projectOption);
      output_manager_default.stopSpinner();
      if (explicitProject instanceof ProjectNotFound) {
        output_manager_default.error(
          `Project not found: ${projectOption} under ${import_chalk144.default.bold(contextName)}`
        );
        return { exitCode: 1 };
      }
      if (explicitProject.id !== project.id) {
        output_manager_default.error(
          `The deployment "${deploymentOption}" does not belong to "${projectOption}" project. Remove either the deployment selection or the ${import_chalk144.default.bold(
            "--project"
          )} option.`
        );
        return { exitCode: 1 };
      }
    }
    return {
      projectId: project.id,
      projectSlug: project.name,
      orgSlug: contextName,
      ownerId: project.accountId,
      deployment
    };
  }
  if (projectOption) {
    output_manager_default.spinner(`Fetching project "${projectOption}"`, 1e3);
    const project = await getProjectByNameOrId(client, projectOption);
    output_manager_default.stopSpinner();
    if (project instanceof ProjectNotFound) {
      output_manager_default.error(
        `Project not found: ${projectOption} under ${import_chalk144.default.bold(contextName)}`
      );
      return { exitCode: 1 };
    }
    return {
      projectId: project.id,
      projectSlug: project.name,
      orgSlug: contextName,
      ownerId: project.accountId
    };
  }
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return { exitCode: link.exitCode };
  }
  if (link.status === "not_linked") {
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName(
        "link"
      )} to begin, or specify a project with ${import_chalk144.default.bold("--project")}.`
    );
    return { exitCode: 1 };
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  return {
    projectId: link.project.id,
    projectSlug: link.project.name,
    orgSlug: link.org.slug,
    ownerId: link.org.id
  };
}
async function logs(client) {
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(logsCommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    output_manager_default.print(help(logsCommand, { columns: client.stderr.columns }));
    return 1;
  }
  const telemetry2 = new LogsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArguments.flags["--help"]) {
    telemetry2.trackCliFlagHelp("logs");
    output_manager_default.print(help(logsCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const subArgs = parsedArguments.args.slice(1);
  const [deploymentArgument] = subArgs;
  const projectOption = parsedArguments.flags["--project"];
  const deploymentFlag = parsedArguments.flags["--deployment"];
  const environmentOption = parsedArguments.flags["--environment"];
  let deploymentOption = deploymentFlag;
  if (deploymentArgument) {
    let deploymentIdOrHost = deploymentArgument;
    try {
      deploymentIdOrHost = new URL(deploymentArgument).hostname;
    } catch {
    }
    deploymentOption = deploymentIdOrHost;
  }
  const levelOption = parsedArguments.flags["--level"];
  const statusCodeOption = parsedArguments.flags["--status-code"];
  const sourceOption = parsedArguments.flags["--source"];
  const sinceOption = parsedArguments.flags["--since"];
  const untilOption = parsedArguments.flags["--until"];
  const limitOption = parsedArguments.flags["--limit"];
  const jsonOption = parsedArguments.flags["--json"];
  const queryOption = parsedArguments.flags["--query"];
  const searchOption = parsedArguments.flags["--search"];
  const requestIdOption = parsedArguments.flags["--request-id"];
  const expandOption = parsedArguments.flags["--expand"];
  const branchFlagValue = parsedArguments.flags["--branch"];
  const noFollowFlagValue = parsedArguments.flags["--no-follow"];
  const followOption = parsedArguments.flags["--follow"];
  telemetry2.trackCliArgumentUrlOrDeploymentId(deploymentArgument);
  telemetry2.trackCliOptionProject(projectOption);
  telemetry2.trackCliOptionDeployment(deploymentFlag);
  telemetry2.trackCliOptionEnvironment(environmentOption);
  telemetry2.trackCliOptionLevel(levelOption);
  telemetry2.trackCliOptionStatusCode(statusCodeOption);
  telemetry2.trackCliOptionSource(sourceOption);
  telemetry2.trackCliOptionSince(sinceOption);
  telemetry2.trackCliOptionUntil(untilOption);
  telemetry2.trackCliOptionLimit(limitOption);
  telemetry2.trackCliFlagJson(jsonOption);
  telemetry2.trackCliFlagFollow(followOption);
  telemetry2.trackCliFlagNoFollow(noFollowFlagValue);
  telemetry2.trackCliOptionQuery(queryOption);
  telemetry2.trackCliOptionSearch(searchOption);
  telemetry2.trackCliOptionRequestId(requestIdOption);
  telemetry2.trackCliFlagExpand(expandOption);
  telemetry2.trackCliOptionBranch(branchFlagValue);
  if (followOption) {
    const incompatibleFlags = [
      { flag: "--environment", value: environmentOption },
      { flag: "--level", value: levelOption },
      { flag: "--status-code", value: statusCodeOption },
      { flag: "--source", value: sourceOption },
      { flag: "--since", value: sinceOption },
      { flag: "--until", value: untilOption },
      { flag: "--limit", value: limitOption },
      { flag: "--query", value: queryOption },
      { flag: "--search", value: searchOption },
      { flag: "--request-id", value: requestIdOption }
    ];
    const usedIncompatible = incompatibleFlags.filter((f) => f.value !== void 0 && f.value !== null).map((f) => import_chalk144.default.bold(f.flag));
    if (usedIncompatible.length > 0) {
      output_manager_default.error(
        `The ${import_chalk144.default.bold("--follow")} flag does not support filtering. Remove: ${usedIncompatible.join(", ")}`
      );
      return 1;
    }
  }
  let contextName;
  try {
    ({ contextName } = await getScope(client));
  } catch (err) {
    if ((0, import_error_utils14.isErrnoException)(err) && (err.code === "NOT_AUTHORIZED" || err.code === "TEAM_DELETED")) {
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
  const logsTarget = await resolveLogsTarget(client, {
    contextName,
    deploymentOption,
    projectOption
  });
  if ("exitCode" in logsTarget) {
    return logsTarget.exitCode;
  }
  const { projectId, projectSlug, orgSlug, ownerId, deployment } = logsTarget;
  let deploymentId = deployment?.id;
  if (deployment && isNonLiveTerminalDeployment(deployment)) {
    const inspectContextName = detectExplicitScope(client) ? contextName : void 0;
    const inspectCommand2 = getInspectCommand(deployment, inspectContextName, {
      plain: true
    });
    if (jsonOption) {
      client.stdout.write(
        `${JSON.stringify({
          type: "deployment_error",
          message: `Logs are unavailable because deployment ${deployment.id} never reached READY and ended in ${deployment.readyState}. Run ${inspectCommand2} for deployment details.`
        })}
`
      );
    } else {
      printNonLiveDeploymentError(deployment, inspectContextName);
    }
    return 1;
  }
  const noBranchFlagValue = parsedArguments.flags["--no-branch"];
  let branchOption;
  if (typeof branchFlagValue === "string") {
    branchOption = branchFlagValue;
  } else if (!noBranchFlagValue && !deploymentId) {
    try {
      const gitMeta = await createGitMeta(client.cwd);
      if (gitMeta?.commitRef) {
        branchOption = gitMeta.commitRef;
        output_manager_default.debug(`Detected git branch: ${branchOption}`);
      }
    } catch {
    }
  }
  if (followOption) {
    if (!deploymentId) {
      if (noBranchFlagValue) {
        output_manager_default.error(
          `The ${import_chalk144.default.bold("--follow")} flag requires a deployment. Specify one with ${import_chalk144.default.bold("--deployment")} or remove ${import_chalk144.default.bold("--no-branch")} to auto-detect from the current git branch.`
        );
        return 1;
      }
      if (!branchOption) {
        output_manager_default.error(
          `The ${import_chalk144.default.bold("--follow")} flag requires a deployment. Specify one with ${import_chalk144.default.bold("--deployment")} or run from within a git repository.`
        );
        return 1;
      }
      output_manager_default.spinner(
        `Finding latest deployment for branch "${branchOption}"`,
        1e3
      );
      const branchDeployment = await getLatestDeploymentByBranch(
        client,
        projectId,
        branchOption
      );
      output_manager_default.stopSpinner();
      if (!branchDeployment) {
        output_manager_default.error(
          `No deployments found for branch "${branchOption}". Deploy this branch first or specify a deployment with ${import_chalk144.default.bold("--deployment")}.`
        );
        return 1;
      }
      deploymentId = branchDeployment.id;
      output_manager_default.debug(
        `Found deployment ${deploymentId} for branch ${branchOption}`
      );
    }
    if (!jsonOption) {
      output_manager_default.print(
        `Streaming logs for deployment ${import_chalk144.default.bold(deploymentId)} starting from ${import_chalk144.default.bold((0, import_format21.default)(Date.now(), TIME_ONLY_FORMAT))}

`
      );
    }
    const abortController = new AbortController();
    return await displayRuntimeLogs(
      client,
      {
        deploymentId,
        projectId,
        parse: !jsonOption
      },
      abortController
    );
  }
  if (environmentOption && !["production", "preview"].includes(environmentOption)) {
    output_manager_default.error(
      `Invalid environment: ${environmentOption}. Must be "production" or "preview".`
    );
    return 1;
  }
  const validLevels = ["error", "warning", "info", "fatal"];
  const levels = parseLevels(levelOption);
  for (const level of levels) {
    if (!validLevels.includes(level)) {
      output_manager_default.error(
        `Invalid log level: ${level}. Must be one of: ${validLevels.join(", ")}.`
      );
      return 1;
    }
  }
  const validSources = [
    "serverless",
    "edge-function",
    "edge-middleware",
    "static"
  ];
  const sources = parseSources(sourceOption);
  for (const source of sources) {
    if (!validSources.includes(source)) {
      output_manager_default.error(
        `Invalid source: ${source}. Must be one of: ${validSources.join(", ")}.`
      );
      return 1;
    }
  }
  const limit = limitOption ?? 100;
  output_manager_default.spinner("Fetching logs...", 1e3);
  const terminalWidth = client.stderr.isTTY ? client.stderr.columns || 120 : 120;
  const logs2 = [];
  try {
    for await (const log of fetchAllRequestLogs(client, {
      projectId,
      ownerId,
      deploymentId,
      environment: environmentOption,
      level: levels.length > 0 ? levels : void 0,
      statusCode: statusCodeOption,
      source: sources.length > 0 ? sources : void 0,
      since: sinceOption,
      until: untilOption,
      limit,
      search: searchOption ?? queryOption,
      requestId: requestIdOption,
      branch: branchOption
    })) {
      output_manager_default.stopSpinner();
      if (jsonOption) {
        client.stdout.write(JSON.stringify(log) + "\n");
      } else {
        logs2.push(log);
      }
    }
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  if (!jsonOption) {
    const branchSuffix = branchOption ? ` on branch ${import_chalk144.default.cyan(branchOption)}` : "";
    if (logs2.length === 0) {
      output_manager_default.print(
        import_chalk144.default.dim(
          `No logs found for ${formatProject(orgSlug, projectSlug)}${branchSuffix}
`
        )
      );
    } else {
      const showDate = logsSpanMultipleDays(logs2);
      const timeFormat = showDate ? DATE_TIME_FORMAT : TIME_ONLY_FORMAT;
      const rowData = logs2.map((log) => {
        const statusCode = log.responseStatusCode;
        return {
          time: (0, import_format21.default)(log.timestamp, timeFormat),
          host: log.domain || "",
          level: log.level,
          path: `${getSourceIcon(log.source)} ${log.requestMethod} ${log.requestPath}`,
          status: !statusCode || statusCode <= 0 ? "---" : String(statusCode),
          statusCode,
          message: log.message?.replace(/\n/g, " ").trim() || "",
          messageTruncated: log.messageTruncated,
          logs: log.logs
        };
      });
      const baseColumns = [
        {
          label: "TIME",
          getValue: (row) => row.time,
          format: (padded) => import_chalk144.default.dim(padded)
        },
        {
          label: "HOST",
          getValue: (row) => row.host,
          format: (padded) => import_chalk144.default.dim(padded)
        },
        {
          label: "LEVEL",
          getValue: (row) => row.level,
          format: (padded, row) => colorizeLevel(padded, row.level)
        },
        {
          label: "",
          padding: [0, 3],
          getValue: (row) => row.path
        }
      ];
      const columns = expandOption ? baseColumns : [
        ...baseColumns,
        {
          label: "STATUS",
          getValue: (row) => row.status,
          format: (padded, row) => row.statusCode <= 0 ? import_chalk144.default.gray(padded) : colorizeStatus(padded, row.statusCode)
        },
        {
          label: "MESSAGE",
          width: "stretch",
          getValue: (row) => row.message || "(no message)",
          format: (padded, row) => row.message ? colorizeMessage(padded, row.level) : import_chalk144.default.dim(padded)
        }
      ];
      const formatted = table2({
        columns,
        rows: rowData,
        tableWidth: terminalWidth,
        formatHeader: (header) => import_chalk144.default.dim(header),
        formatRow: expandOption ? (rowStr, row) => {
          if (row.logs.length > 0) {
            const renderedLogs = row.logs.map((log) => {
              const message = log.message.replace(/\n/g, " ").trim();
              const safeMessage = message || "(no message)";
              const truncatedIndicator = log.messageTruncated ? import_chalk144.default.gray("\u2026") : "";
              return `${colorizeMessage(safeMessage, log.level)}${truncatedIndicator}`;
            }).join("\n");
            return `${rowStr}
${renderedLogs}
`;
          }
          return rowStr + "\n";
        } : void 0
      });
      output_manager_default.print(formatted.header + "\n");
      for (const row of formatted.rows) {
        output_manager_default.print(row + "\n");
      }
      output_manager_default.print(
        import_chalk144.default.gray(
          `Fetched ${logs2.length} logs for ${formatProject(orgSlug, projectSlug)}${branchSuffix}
`
        )
      );
    }
  }
  return 0;
}
function colorizeLevel(formatted, level) {
  switch (level) {
    case "fatal":
      return import_chalk144.default.red.bold(formatted);
    case "error":
      return import_chalk144.default.red(formatted);
    case "warning":
      return import_chalk144.default.yellow(formatted);
    default:
      return import_chalk144.default.dim(formatted);
  }
}
function colorizeStatus(formatted, statusCode) {
  if (statusCode >= 500) {
    return import_chalk144.default.red(formatted);
  } else if (statusCode >= 400) {
    return import_chalk144.default.yellow(formatted);
  } else if (statusCode >= 300) {
    return import_chalk144.default.cyan(formatted);
  } else if (statusCode >= 200) {
    return import_chalk144.default.green(formatted);
  }
  return import_chalk144.default.gray(formatted);
}
function getSourceIcon(source) {
  switch (source) {
    case "serverless":
    case "lambda":
      return "\u03BB";
    case "edge-function":
    case "edge-middleware":
    case "middleware":
      return "\u03B5";
    case "static":
    case "external":
    case "redirect":
      return "\u25C7";
    default:
      return " ";
  }
}
function colorizeMessage(message, level) {
  switch (level) {
    case "fatal":
    case "error":
      return import_chalk144.default.red(message);
    case "warning":
      return import_chalk144.default.yellow(message);
    default:
      return import_chalk144.default.dim(message);
  }
}

// src/commands/mcp/mcp.ts
import { execSync } from "child_process";
var MCP_ENDPOINT = "https://mcp.vercel.com";
function getAvailableClients() {
  return [
    "Claude Code",
    "Claude.ai and Claude for desktop",
    "Cursor",
    "VS Code with Copilot"
  ];
}
function safeExecSync(command, options = {}) {
  try {
    return execSync(command, {
      stdio: "pipe",
      encoding: "utf8",
      ...options
    });
  } catch (error) {
    return { error: error.message, stderr: error.stderr?.toString() || "" };
  }
}
async function getProjectSpecificUrl(client) {
  try {
    const linkedProject = await getLinkedProject(client);
    if (linkedProject.status !== "linked" || !linkedProject.org || !linkedProject.project) {
      return null;
    }
    const { org, project } = linkedProject;
    return {
      url: `${MCP_ENDPOINT}/${org.slug}/${project.name}`,
      projectName: project.name
    };
  } catch (_error) {
    return null;
  }
}
async function mcp(client, opts = {}) {
  const isProjectSpecific = opts.project ?? client.argv.includes("--project");
  if (!client.nonInteractive) {
    output_manager_default.print("\u{1F680} Vercel MCP Setup \u2014 Automated\n");
  }
  if (isProjectSpecific) {
    if (!client.nonInteractive) {
      output_manager_default.print("\u{1F517} Setting up project-specific MCP access...\n");
    }
    const projectInfo = await getProjectSpecificUrl(client);
    if (!projectInfo) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "not_linked",
            message: "No linked project found. Link your project first for project-specific MCP.",
            next: [{ command: `${packageName} link` }]
          },
          1
        );
      }
      output_manager_default.print(
        "\u274C No linked project found. Please link your project first:\n"
      );
      output_manager_default.print("   vercel link\n");
      return 1;
    }
    if (!client.nonInteractive) {
      output_manager_default.print(`\u2705 Project-specific URL: ${projectInfo.url}
`);
      output_manager_default.print(
        "This URL will automatically provide project and team context.\n\n"
      );
    }
  }
  const availableClients = getAvailableClients();
  let selectedClients;
  if (opts.clients && opts.clients.length > 0) {
    const invalid = opts.clients.filter(
      (name) => !availableClients.includes(name)
    );
    if (invalid.length > 0) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "invalid_clients",
            message: `Invalid client(s): ${invalid.join(
              ", "
            )}. Valid options: ${availableClients.join(", ")}.`
          },
          1
        );
      }
      output_manager_default.error(
        `Invalid client(s): ${invalid.join(
          ", "
        )}. Valid options: ${availableClients.join(", ")}.`
      );
      return 1;
    }
    selectedClients = opts.clients;
    if (!client.nonInteractive) {
      output_manager_default.print(
        `Using clients from --clients: ${selectedClients.join(", ")}

`
      );
    }
  } else {
    selectedClients = await client.input.checkbox({
      message: "Select MCP clients to set up:",
      choices: availableClients.map((name) => ({
        name,
        value: name,
        short: name
      }))
    });
  }
  if (!Array.isArray(selectedClients) || selectedClients.length === 0) {
    if (client.nonInteractive) {
      client.stdout.write(
        `${JSON.stringify(
          {
            status: "ok",
            summary: [],
            message: "No clients selected."
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.print("\nNo clients selected. Exiting.\n");
    return 0;
  }
  const summary = [];
  if (!client.nonInteractive) {
    output_manager_default.print("\nStarting setup...\n");
  }
  for (const clientName of selectedClients) {
    if (!client.nonInteractive) {
      output_manager_default.print(`\u{1F527} Setting up ${clientName}...
`);
    }
    if (clientName === "Claude Code") {
      const mcpUrl = isProjectSpecific ? (await getProjectSpecificUrl(client))?.url : MCP_ENDPOINT;
      const mcpName = isProjectSpecific ? `vercel-${(await getProjectSpecificUrl(client))?.projectName}` : "vercel";
      const result = safeExecSync(
        `claude mcp add --transport http ${mcpName} ${mcpUrl}`
      );
      if (typeof result === "object" && "error" in result) {
        if (result.stderr?.includes("already exists")) {
          summary.push("\u2705 Claude Code: Vercel MCP already configured");
          if (!client.nonInteractive) {
            output_manager_default.print(
              "\u2139\uFE0F  Vercel MCP is already configured in Claude Code\n"
            );
            output_manager_default.print("\u2500".repeat(50) + "\n");
          }
        } else {
          summary.push("\u274C Claude Code: Failed to add MCP server");
          if (!client.nonInteractive) {
            output_manager_default.print("\u{1F4A1} Manual setup required:\n");
            output_manager_default.print(
              `   claude mcp add --transport http ${mcpName} ${mcpUrl}
`
            );
            output_manager_default.print(
              "   # Or use the /mcp command in Claude Code to authenticate\n"
            );
            output_manager_default.print("\u2500".repeat(50) + "\n");
          }
        }
      } else {
        summary.push("\u2705 Claude Code: Vercel MCP added successfully");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2705 Successfully added Vercel MCP to Claude Code\n");
          output_manager_default.print(
            "\u2139\uFE0F You may need to authenticate using the /mcp command in Claude Code\n"
          );
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
      }
    } else if (clientName === "Claude.ai and Claude for desktop") {
      if (!client.nonInteractive) {
        output_manager_default.print(
          "\u{1F4A1} Manual setup required for Claude.ai and Claude for desktop\n"
        );
        output_manager_default.print("   1. Open Settings in the sidebar\n");
        output_manager_default.print(
          "   2. Navigate to Connectors and select Add custom connector\n"
        );
        output_manager_default.print("   3. Configure the connector:\n");
        if (isProjectSpecific) {
          const projectInfo = await getProjectSpecificUrl(client);
          const projectName = projectInfo?.projectName || "project";
          output_manager_default.print(`      \u2022 Name: Vercel ${projectName}
`);
          output_manager_default.print(`      \u2022 URL: ${projectInfo?.url}
`);
        } else {
          output_manager_default.print("      \u2022 Name: Vercel\n");
          output_manager_default.print(`      \u2022 URL: ${MCP_ENDPOINT}
`);
        }
        output_manager_default.print("   4. Complete the authentication flow\n");
      }
      summary.push("\u2139\uFE0F  Claude.ai/Desktop: Manual setup required");
      if (!client.nonInteractive) {
        output_manager_default.print("\u2500".repeat(50) + "\n");
      }
    } else if (clientName === "Cursor") {
      const cursorCheck = safeExecSync(
        process.platform === "darwin" ? "ls /Applications/Cursor.app" : process.platform === "win32" ? "where cursor" : "which cursor"
      );
      if (typeof cursorCheck === "object" && "error" in cursorCheck) {
        if (!client.nonInteractive) {
          output_manager_default.print("\u26A0\uFE0F Cursor not detected. Please install Cursor first.\n");
          output_manager_default.print("   Download from: https://cursor.sh\n");
          output_manager_default.print("\n");
        }
        summary.push("\u26A0\uFE0F Cursor: Not installed");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
        continue;
      }
      const mcpUrl = isProjectSpecific ? (await getProjectSpecificUrl(client))?.url : MCP_ENDPOINT;
      const serverName = isProjectSpecific ? `vercel-${(await getProjectSpecificUrl(client))?.projectName}` : "vercel";
      const cursorConfigPath = process.platform === "darwin" ? `${process.env.HOME}/Library/Application Support/Cursor/User/settings.json` : process.platform === "win32" ? `${process.env.APPDATA}/Cursor/User/settings.json` : `${process.env.HOME}/.config/Cursor/User/settings.json`;
      const cursorMcpPath = process.platform === "darwin" ? `${process.env.HOME}/.cursor/mcp.json` : process.platform === "win32" ? `${process.env.APPDATA}/Cursor/mcp.json` : `${process.env.HOME}/.cursor/mcp.json`;
      let cursorAlreadyConfigured = false;
      try {
        const fs2 = __require("fs");
        if (fs2.existsSync(cursorMcpPath)) {
          const configContent = fs2.readFileSync(cursorMcpPath, "utf8");
          const config2 = JSON.parse(configContent);
          const mcpServers = config2.mcpServers || {};
          cursorAlreadyConfigured = Object.values(mcpServers).some(
            (server) => server.url === mcpUrl || server.url === MCP_ENDPOINT
          );
        }
        if (!cursorAlreadyConfigured && fs2.existsSync(cursorConfigPath)) {
          const configContent = fs2.readFileSync(cursorConfigPath, "utf8");
          const config2 = JSON.parse(configContent);
          const mcpServers = config2["mcp.servers"] || {};
          cursorAlreadyConfigured = Object.values(mcpServers).some(
            (server) => server.url === mcpUrl || server.url === MCP_ENDPOINT
          );
        }
      } catch (_error) {
      }
      if (cursorAlreadyConfigured) {
        summary.push("\u2705 Cursor: Vercel MCP already configured");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2139\uFE0F  Vercel MCP is already configured in Cursor\n");
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
        continue;
      }
      const config = {
        url: mcpUrl,
        name: serverName
      };
      const configJson = JSON.stringify(config);
      const encodedConfig = Buffer.from(configJson).toString("base64");
      const oneClickUrl = `cursor://anysphere.cursor-deeplink/mcp/install?name=${serverName}&config=${encodedConfig}`;
      try {
        if (process.platform === "darwin") {
          execSync(`open '${oneClickUrl}'`);
        } else if (process.platform === "win32") {
          execSync(`start ${oneClickUrl}`);
        } else {
          execSync(`xdg-open '${oneClickUrl}'`);
        }
        summary.push("\u2705 Cursor: One-click installer opened");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2139\uFE0F  Follow the prompts in Cursor to complete setup\n");
        }
      } catch (_error) {
        summary.push("\u26A0\uFE0F Cursor: Deep link may not have worked");
        if (!client.nonInteractive) {
          output_manager_default.print("\u26A0\uFE0F Could not open Cursor automatically\n");
          output_manager_default.print("\u{1F4A1} Manual setup:\n");
          output_manager_default.print("   1. Open Cursor\n");
          output_manager_default.print("   2. Go to Settings (Cmd+, / Ctrl+,)\n");
          output_manager_default.print("   3. Navigate to MCP section\n");
          output_manager_default.print('   4. Click "Add Server"\n');
          output_manager_default.print("   5. Enter the following details:\n");
          output_manager_default.print(`      \u2022 Name: ${serverName}
`);
          output_manager_default.print(`      \u2022 URL: ${mcpUrl}
`);
          output_manager_default.print(
            '   6. Click "Add" and follow the authorization prompts\n'
          );
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
      }
    } else if (clientName === "VS Code with Copilot") {
      const copilotCheck = safeExecSync(
        "code --list-extensions | grep -i copilot"
      );
      if (typeof copilotCheck === "object" && "error" in copilotCheck) {
        if (!client.nonInteractive) {
          output_manager_default.print(
            "\u26A0\uFE0F GitHub Copilot not detected. MCP functionality may be limited.\n"
          );
          output_manager_default.print("   1. Open VS Code\n");
          output_manager_default.print("   2. Go to Extensions (Cmd+Shift+X / Ctrl+Shift+X)\n");
          output_manager_default.print('   3. Search for "GitHub Copilot"\n');
          output_manager_default.print(
            "   4. Install and authenticate with your GitHub account\n"
          );
          output_manager_default.print("   5. Restart VS Code\n");
          output_manager_default.print("\n");
        }
      }
      const mcpUrl = isProjectSpecific ? (await getProjectSpecificUrl(client))?.url : MCP_ENDPOINT;
      const serverName = isProjectSpecific ? `vercel-${(await getProjectSpecificUrl(client))?.projectName}` : "vercel";
      const vscodeConfigPath = process.platform === "darwin" ? `${process.env.HOME}/Library/Application Support/Code/User/settings.json` : process.platform === "win32" ? `${process.env.APPDATA}/Code/User/settings.json` : `${process.env.HOME}/.config/Code/User/settings.json`;
      const vscodeMcpPath = process.platform === "darwin" ? `${process.env.HOME}/Library/Application Support/Code/User/mcp.json` : process.platform === "win32" ? `${process.env.APPDATA}/Code/User/mcp.json` : `${process.env.HOME}/.config/Code/User/mcp.json`;
      let vscodeAlreadyConfigured = false;
      try {
        const fs2 = __require("fs");
        if (fs2.existsSync(vscodeMcpPath)) {
          const configContent = fs2.readFileSync(vscodeMcpPath, "utf8");
          const config2 = JSON.parse(configContent);
          const mcpServers = config2.servers || {};
          vscodeAlreadyConfigured = Object.values(mcpServers).some(
            (server) => server.url === mcpUrl || server.url === MCP_ENDPOINT
          );
        }
        if (!vscodeAlreadyConfigured && fs2.existsSync(vscodeConfigPath)) {
          const configContent = fs2.readFileSync(vscodeConfigPath, "utf8");
          const config2 = JSON.parse(configContent);
          const mcpServers = config2["mcp.servers"] || {};
          vscodeAlreadyConfigured = Object.values(mcpServers).some(
            (server) => server.url === mcpUrl || server.url === MCP_ENDPOINT
          );
        }
      } catch (_error) {
      }
      if (vscodeAlreadyConfigured) {
        summary.push("\u2705 VS Code: Vercel MCP already configured");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2139\uFE0F  Vercel MCP is already configured in VS Code\n");
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
        continue;
      }
      const config = {
        name: serverName,
        url: mcpUrl
      };
      const encodedConfig = encodeURIComponent(JSON.stringify(config));
      const oneClickUrl = `vscode:mcp/install?${encodedConfig}`;
      try {
        if (process.platform === "darwin") {
          execSync(`open '${oneClickUrl}'`);
        } else if (process.platform === "win32") {
          execSync(`start ${oneClickUrl}`);
        } else {
          execSync(`xdg-open '${oneClickUrl}'`);
        }
        summary.push("\u2705 VS Code: One-click installer opened");
        if (!client.nonInteractive) {
          output_manager_default.print("\u2139\uFE0F  Follow the prompts in VS Code to complete setup\n");
        }
      } catch (_error) {
        summary.push("\u274C VS Code: Failed to open one-click installer");
        if (!client.nonInteractive) {
          output_manager_default.print("\u{1F4A1} Manual setup instructions:\n");
          output_manager_default.print("   1. Open VS Code\n");
          output_manager_default.print(
            "   2. Press Cmd+Shift+P (Mac) or Ctrl+Shift+P (Windows/Linux)\n"
          );
          output_manager_default.print('   3. Type "MCP: Add Server" and press Enter\n');
          output_manager_default.print("   4. Select HTTP\n");
          output_manager_default.print("   5. Enter the following details:\n");
          output_manager_default.print(`      \u2022 URL: ${mcpUrl}
`);
          output_manager_default.print(`      \u2022 Name: ${serverName}
`);
          output_manager_default.print(
            "   6. Select Global or Workspace depending on your needs\n"
          );
          output_manager_default.print("   7. Click Add\n");
          output_manager_default.print("   8. Follow the authorization steps\n");
          output_manager_default.print("\u2500".repeat(50) + "\n");
        }
      }
    }
  }
  if (client.nonInteractive) {
    const jsonOutput = {
      status: "ok",
      summary,
      hint: "Restart your MCP client(s) to apply changes.",
      next: [
        {
          command: `${packageName} link`,
          when: "To link a project for project-specific MCP (optional)"
        }
      ]
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
    return 0;
  }
  output_manager_default.print("\u{1F4CA} Setup Summary\n");
  output_manager_default.print("\u2500".repeat(50) + "\n");
  summary.forEach((line) => output_manager_default.print(`${line}
`));
  output_manager_default.print("\u2728 Setup complete! Restart your clients if needed.\n");
  return 0;
}

// src/commands/mcp/constants.ts
var MCP_CLIENTS = [
  { slug: "claude-code", display: "Claude Code" },
  { slug: "claude-desktop", display: "Claude.ai and Claude for desktop" },
  { slug: "cursor", display: "Cursor" },
  { slug: "vscode-copilot", display: "VS Code with Copilot" }
];
var MCP_CLIENT_DISPLAY_NAMES = MCP_CLIENTS.map((c) => c.display);

// src/commands/mcp/index.ts
function buildSuggestedMcpCommand(client, clientsValue) {
  const args = client.argv.slice(2);
  const preserved = [];
  let hasNonInteractive = false;
  for (let i = 1; i < args.length; i++) {
    const arg = args[i];
    if (arg === "--non-interactive") {
      hasNonInteractive = true;
      continue;
    }
    if (arg === "--clients") {
      i++;
      while (i < args.length && !args[i].startsWith("-")) {
        i++;
      }
      i--;
      continue;
    }
    if (arg.startsWith("--clients=")) {
      continue;
    }
    preserved.push(arg);
  }
  const singleQuotedClients = `'${clientsValue}'`;
  preserved.push("--clients", singleQuotedClients);
  if (hasNonInteractive) {
    preserved.push("--non-interactive");
  }
  const suffix = preserved.join(" ");
  return suffix ? `${packageName} mcp ${suffix}` : `${packageName} mcp`;
}
function parseAndValidateClients(clientsFlag) {
  if (!clientsFlag || !clientsFlag.trim())
    return [];
  const requested = clientsFlag.split(",").map((s) => s.trim()).filter(Boolean);
  const normalized = requested.map((name) => {
    const lower = name.toLowerCase();
    const match = MCP_CLIENT_DISPLAY_NAMES.find((c) => c.toLowerCase() === lower);
    return match ?? name;
  });
  return [...new Set(normalized)];
}
async function main22(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(mcpCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    output_manager_default.print(help(mcpCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const project = !!parsedArgs.flags["--project"];
  const clientsFlag = parsedArgs.flags["--clients"];
  const clients = parseAndValidateClients(clientsFlag);
  if (client.nonInteractive) {
    if (clients.length === 0) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_clients",
          message: "In non-interactive mode --clients is required. Specify a comma-separated list of MCP clients to set up.",
          next: [
            {
              command: buildSuggestedMcpCommand(
                client,
                "Cursor,VS Code with Copilot"
              )
            }
          ]
        },
        1
      );
      return 1;
    }
    const invalid = clients.filter(
      (c) => !MCP_CLIENT_DISPLAY_NAMES.some((v) => v === c)
    );
    if (invalid.length > 0) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_clients",
          message: `Invalid client(s): ${invalid.join(
            ", "
          )}. Valid options: ${MCP_CLIENT_DISPLAY_NAMES.join(", ")}.`,
          next: [
            {
              command: buildSuggestedMcpCommand(
                client,
                "Cursor,VS Code with Copilot"
              )
            }
          ]
        },
        1
      );
      return 1;
    }
  }
  if (project) {
    client.argv.push("--project");
  }
  try {
    return await mcp(client, { project, clients });
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
}

// src/util/telemetry/commands/metrics/index.ts
var MetricsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandQuery(v) {
    if (v) {
      this.trackCliSubcommand({
        subcommand: "query",
        value: v
      });
    }
  }
  trackCliSubcommandSchema(v) {
    if (v) {
      this.trackCliSubcommand({
        subcommand: "schema",
        value: v
      });
    }
  }
  trackCliArgumentMetricId(v) {
    if (v) {
      this.trackCliArgument({
        arg: "metric-id",
        value: v
      });
    }
  }
  trackCliOptionAggregation(v) {
    if (v) {
      this.trackCliOption({
        option: "aggregation",
        value: v
      });
    }
  }
  trackCliOptionGroupBy(v) {
    if (v && v.length > 0) {
      this.trackCliOption({
        option: "group-by",
        value: v.join(",")
      });
    }
  }
  trackCliOptionLimit(v) {
    if (typeof v === "number") {
      this.trackCliOption({
        option: "limit",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionFilter(v) {
    if (v) {
      this.trackCliOption({
        option: "filter",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSince(v) {
    if (v) {
      this.trackCliOption({
        option: "since",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionUntil(v) {
    if (v) {
      this.trackCliOption({
        option: "until",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionGranularity(v) {
    if (v) {
      this.trackCliOption({
        option: "granularity",
        value: v
      });
    }
  }
  trackCliFlagAll(v) {
    if (v) {
      this.trackCliFlag("all");
    }
  }
};

// src/commands/metrics/index.ts
var COMMAND_CONFIG29 = {
  schema: getCommandAliases(schemaSubcommand)
};
async function metrics(client) {
  const telemetry2 = new MetricsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(metricsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG29
  );
  const positionalMetric = subArgs[0] === "query" ? subArgs[1] : subArgs[0];
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("metrics", subcommand);
    output_manager_default.print(help(metricsCommand, { columns: client.stderr.columns }));
    return 0;
  }
  function printSubHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: metricsCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "schema": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("metrics", subcommandOriginal);
        printSubHelp(schemaSubcommand);
        return 0;
      }
      telemetry2.trackCliSubcommandSchema(subcommandOriginal);
      const schemaFn = (await import("./chunks/schema-F2RCHWJK.js")).default;
      return schemaFn(client, telemetry2);
    }
    default: {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("metrics", subcommandOriginal);
        output_manager_default.print(help(metricsCommand, { columns: client.stderr.columns }));
        return 0;
      }
      if (!positionalMetric) {
        output_manager_default.print(help(metricsCommand, { columns: client.stderr.columns }));
        return 2;
      }
      const queryFn = (await import("./chunks/query-N6O4SKZE.js")).default;
      return queryFn(client, telemetry2);
    }
  }
}

// src/commands/microfrontends/pull.ts
var import_chalk145 = __toESM(require_source(), 1);
import { join as join5 } from "path";
var import_fs_extra = __toESM(require_lib(), 1);
var VERCEL_DIR = ".vercel";
var VERCEL_DIR_MICROFRONTENDS = "microfrontends.json";
async function pull2(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(pullSubcommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const link = await ensureLink("microfrontends", client, client.cwd, {
    autoConfirm
  });
  if (typeof link === "number") {
    return link;
  }
  const { project, org, repoRoot } = link;
  let currentDirectory;
  if (repoRoot) {
    currentDirectory = join5(repoRoot, project.rootDirectory || "");
  } else {
    currentDirectory = client.cwd;
  }
  client.config.currentTeam = org.type === "team" ? org.id : void 0;
  const { contextName } = await getScope(client);
  output_manager_default.spinner(
    `Fetching microfrontends configuration in ${import_chalk145.default.bold(contextName)}`
  );
  let rawConfig;
  const dpl = parsedArgs.flags["--dpl"];
  try {
    if (dpl) {
      const { config } = await client.fetch(`/v1/microfrontends/${dpl}/config`, {
        method: "GET"
      });
      rawConfig = config;
    } else {
      const projectId = project.id;
      const { config } = await client.fetch(`/v1/microfrontends/projects/${projectId}/production-mfe-config`, {
        method: "GET"
      });
      rawConfig = config;
    }
    const sanitizedConfig = {
      ...rawConfig,
      applications: Object.fromEntries(
        Object.entries(rawConfig.applications).map(([name, app]) => [
          name,
          {
            ...app,
            projectId: void 0
            // remove projectId
          }
        ])
      )
    };
    output_manager_default.stopSpinner();
    const path3 = join5(currentDirectory, VERCEL_DIR, VERCEL_DIR_MICROFRONTENDS);
    await (0, import_fs_extra.outputJSON)(path3, sanitizedConfig, {
      spaces: 2
    });
    const microfrontendsStamp = stamp_default();
    output_manager_default.print(
      `${prependEmoji(
        `Downloaded microfrontends configuration to ${import_chalk145.default.bold(
          humanizePath(
            join5(currentDirectory, VERCEL_DIR, VERCEL_DIR_MICROFRONTENDS)
          )
        )} ${import_chalk145.default.gray(microfrontendsStamp())}`,
        emoji("success")
      )}
`
    );
    return 0;
  } catch (error) {
    output_manager_default.stopSpinner();
    printError(error);
    return 1;
  }
}

// src/commands/microfrontends/create-group.ts
var import_chalk146 = __toESM(require_source(), 1);
import { writeFile as writeFile4 } from "fs/promises";
import { existsSync } from "fs";
import { join as join6 } from "path";

// src/commands/microfrontends/utils.ts
async function ensureMicrofrontendsContext(client, options) {
  const link = await ensureLink("microfrontends", client, client.cwd, {
    autoConfirm: options?.autoConfirm
  });
  if (typeof link === "number") {
    return link;
  }
  const { project, org, repoRoot } = link;
  if (org.type !== "team") {
    output_manager_default.error("Microfrontends are only available for teams.");
    return 1;
  }
  client.config.currentTeam = org.id;
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Microfrontends are only available for teams.");
    return 1;
  }
  return { project, org, team, repoRoot };
}
async function fetchMicrofrontendsGroups(client, teamId) {
  output_manager_default.spinner("Fetching microfrontends groups\u2026");
  const response = await client.fetch(
    `/v1/microfrontends/groups?teamId=${teamId}`,
    { method: "GET" }
  );
  output_manager_default.stopSpinner();
  return response;
}
function validateDefaultRoute(path3) {
  if (!path3 || !path3.startsWith("/")) {
    return "Route must start with /";
  }
  if (/\s/.test(path3)) {
    return "Route must not contain spaces";
  }
  if (path3.includes("?") || path3.includes("#")) {
    return "Route must not contain query strings or fragments";
  }
  try {
    new URL(path3, "http://n");
  } catch {
    return "Route is not a valid URL path";
  }
  return true;
}
function validateRoutingPath(path3) {
  if (!path3 || !path3.startsWith("/")) {
    return "Path must start with /. See https://vercel.com/docs/microfrontends/path-routing for supported syntax.";
  }
  if (/\s/.test(path3)) {
    return "Path must not contain spaces. See https://vercel.com/docs/microfrontends/path-routing for supported syntax.";
  }
  return true;
}

// src/commands/microfrontends/create-group.ts
var MAX_GROUP_NAME_LENGTH = 48;
async function createGroup(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    createGroupSubcommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Microfrontends are only available for teams.");
    return 1;
  }
  const teamSlug = team.slug;
  output_manager_default.log(
    "A microfrontends group allows multiple projects to be composed into one application with a seamless user experience."
  );
  output_manager_default.log(
    `Creating a new microfrontends group for team ${import_chalk146.default.bold(teamSlug)}.`
  );
  output_manager_default.log("");
  const groupsResponse = await fetchMicrofrontendsGroups(client, team.id);
  const { groups, maxMicrofrontendsGroupsPerTeam, maxMicrofrontendsPerGroup } = groupsResponse;
  const plan = team.billing.plan;
  const isProTrialPlan = team.billing.plan === "pro" && team.billing.status === "trialing";
  const isLimitedPlan = isProTrialPlan || plan === "hobby";
  if (groups.length >= maxMicrofrontendsGroupsPerTeam) {
    if (isLimitedPlan) {
      const planName = isProTrialPlan ? "Pro Trial" : "Hobby";
      const url = `https://vercel.com/${teamSlug}/~/settings/billing`;
      output_manager_default.log(
        `You've reached the microfrontends group limit for ${planName}. Upgrade to Pro to create more groups.`
      );
      output_manager_default.log(`Upgrade: ${output_manager_default.link(url, url, { fallback: false })}`);
      return 1;
    }
    output_manager_default.error(
      `Maximum number of microfrontends groups (${maxMicrofrontendsGroupsPerTeam}) reached.`
    );
    return 1;
  }
  const existingMfeProjectCount = groups.reduce(
    (count, g) => count + g.projects.length,
    0
  );
  const freeProjects = 2;
  if (isLimitedPlan && existingMfeProjectCount + 1 > freeProjects) {
    const planName = isProTrialPlan ? "Pro Trial" : "Hobby";
    const url = `https://vercel.com/${teamSlug}/~/settings/billing`;
    output_manager_default.log(
      `You've reached the microfrontends project limit for ${planName}. Upgrade to Pro to add more projects.`
    );
    output_manager_default.log(`Upgrade: ${output_manager_default.link(url, url, { fallback: false })}`);
    return 1;
  }
  const nameFlag = parsedArgs.flags["--name"];
  const projectFlags = parsedArgs.flags["--project"];
  const defaultAppFlag = parsedArgs.flags["--default-app"];
  const defaultRouteFlag = parsedArgs.flags["--default-route"];
  const projectDefaultRouteFlags = parsedArgs.flags["--project-default-route"];
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const isNonInteractive = client.nonInteractive || client.argv.includes("--non-interactive");
  if (isNonInteractive) {
    if (!nameFlag) {
      output_manager_default.error(
        "Missing required flag --name. Use --name in non-interactive mode."
      );
      return 1;
    }
    if (!projectFlags || projectFlags.length === 0) {
      output_manager_default.error(
        "Missing required flag --project. Use --project in non-interactive mode."
      );
      return 1;
    }
    if (projectFlags.length > 1 && !defaultAppFlag) {
      output_manager_default.error(
        "Missing required flag --default-app when using multiple --project values in non-interactive mode."
      );
      return 1;
    }
  }
  const projectsToAddCount = projectFlags?.length ?? 1;
  const wouldAffectBilling = existingMfeProjectCount + projectsToAddCount > freeProjects;
  if (wouldAffectBilling) {
    if (isNonInteractive) {
      const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
      const interactiveCmd = getCommandNamePlain(
        `microfrontends create-group ${flags.filter((f) => f !== "--non-interactive").join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: "error",
          reason: "purchase_requires_user",
          message: "Creating a microfrontends group affects billing and cannot be performed non-interactively. Agents must not make billing changes on behalf of a user. The user must run this command interactively in a terminal to review billing details and confirm, or complete the action in the Vercel dashboard.",
          next: [
            {
              command: interactiveCmd,
              when: "user runs this command interactively (remove --non-interactive)"
            }
          ]
        },
        1
      );
      return 1;
    }
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "This command must be run interactively because it affects billing."
      );
      return 1;
    }
  }
  let groupName;
  if (nameFlag) {
    const validation = validateGroupName(nameFlag, groups);
    if (validation !== true) {
      output_manager_default.error(validation);
      return 1;
    }
    groupName = nameFlag;
  } else {
    groupName = await client.input.text({
      message: "Group name:",
      validate: (val) => validateGroupName(val, groups)
    });
  }
  let selectedProjects;
  if (projectFlags) {
    selectedProjects = [];
    for (const name of projectFlags) {
      try {
        const project = await getProjectByNameOrId(client, name, team.id);
        if (project instanceof ProjectNotFound) {
          output_manager_default.error(`Project "${name}" not found.`);
          return 1;
        }
        selectedProjects.push(project);
      } catch {
        output_manager_default.error(`Project "${name}" not found.`);
        return 1;
      }
    }
  } else {
    output_manager_default.spinner("Fetching projects\u2026");
    const firstPage = await client.fetch(`/v9/projects?limit=100&teamId=${team.id}`, { method: "GET" });
    output_manager_default.stopSpinner();
    const allProjects = firstPage.projects;
    const hasMoreProjects = firstPage.pagination.next !== null;
    const availableProjects = allProjects.filter(
      (p) => !isProjectInMicrofrontendsGroup(p, groups)
    );
    selectedProjects = [];
    let addMore = true;
    while (addMore) {
      let project;
      if (hasMoreProjects) {
        await client.input.text({
          message: selectedProjects.length === 0 ? "Type a project name to add:" : "Type another project name to add:",
          validate: async (val) => {
            if (!val) {
              return "Project name cannot be empty";
            }
            const result = await getProjectByNameOrId(client, val, team.id);
            if (result instanceof ProjectNotFound) {
              return "Project not found";
            }
            if (isProjectInMicrofrontendsGroup(result, groups)) {
              return `Project "${val}" is already in a microfrontends group`;
            }
            if (selectedProjects.some((s) => s.id === result.id)) {
              return `Project "${val}" is already selected`;
            }
            project = result;
            return true;
          }
        });
      } else {
        if (availableProjects.length === 0) {
          if (selectedProjects.length === 0) {
            output_manager_default.error(
              "No available projects. All projects are already in a microfrontends group."
            );
            return 1;
          }
          break;
        }
        const remaining = availableProjects.filter(
          (p) => !selectedProjects.some((s) => s.id === p.id)
        );
        if (remaining.length === 0) {
          break;
        }
        const projectId = await client.input.select({
          message: selectedProjects.length === 0 ? "Select a project to add:" : "Select another project to add:",
          choices: remaining.map((p) => ({ name: p.name, value: p.id }))
        });
        project = availableProjects.find((p) => p.id === projectId);
      }
      if (project) {
        selectedProjects.push(project);
      }
      addMore = await client.input.confirm("Add another project?", false);
    }
    if (selectedProjects.length === 0) {
      output_manager_default.error("At least one project must be selected.");
      return 1;
    }
  }
  if (selectedProjects.length > maxMicrofrontendsPerGroup) {
    output_manager_default.error(
      `Cannot add more than ${maxMicrofrontendsPerGroup} projects to a group.`
    );
    return 1;
  }
  let defaultApp;
  if (defaultAppFlag) {
    const found = selectedProjects.find((p) => p.name === defaultAppFlag);
    if (!found) {
      output_manager_default.error(
        `Default app "${defaultAppFlag}" must be one of the selected projects.`
      );
      return 1;
    }
    defaultApp = found;
  } else if (selectedProjects.length === 1) {
    defaultApp = selectedProjects[0];
  } else {
    const defaultAppId = await client.input.select({
      message: "Select the default application:",
      choices: selectedProjects.map((p) => ({ name: p.name, value: p.id }))
    });
    defaultApp = selectedProjects.find((p) => p.id === defaultAppId);
  }
  let defaultRoute;
  if (defaultRouteFlag) {
    const validation = validateDefaultRoute(defaultRouteFlag);
    if (validation !== true) {
      output_manager_default.error(validation);
      return 1;
    }
    defaultRoute = defaultRouteFlag;
  } else {
    defaultRoute = "/";
  }
  const otherProjects = selectedProjects.filter((p) => p.id !== defaultApp.id);
  const parsedProjectDefaultRoutes = parseProjectDefaultRouteFlags(
    projectDefaultRouteFlags,
    selectedProjects,
    defaultApp
  );
  if (typeof parsedProjectDefaultRoutes === "string") {
    output_manager_default.error(parsedProjectDefaultRoutes);
    return 1;
  }
  const otherApplications = [];
  for (const project of otherProjects) {
    const routeFromFlag = parsedProjectDefaultRoutes.get(project.id);
    let route;
    if (routeFromFlag) {
      route = routeFromFlag;
    } else if (isNonInteractive) {
      output_manager_default.error(
        `Missing required flag --project-default-route for "${project.name}". Use --project-default-route=${project.name}=/<path> in non-interactive mode.`
      );
      return 1;
    } else {
      route = await client.input.text({
        message: `Default route for "${project.name}":`,
        validate: validateDefaultRoute
      });
    }
    otherApplications.push({ projectId: project.id, defaultRoute: route });
  }
  const newProjectCount = selectedProjects.length;
  const totalAfter = existingMfeProjectCount + newProjectCount;
  output_manager_default.log("");
  output_manager_default.log(import_chalk146.default.bold("Billing"));
  output_manager_default.log(`  ${import_chalk146.default.bold("Team:")}     ${teamSlug}`);
  output_manager_default.log(
    `  ${import_chalk146.default.bold("Adding:")}   ${newProjectCount} microfrontends project${newProjectCount > 1 ? "s" : ""}`
  );
  let projectFee;
  if (totalAfter <= freeProjects) {
    projectFee = import_chalk146.default.green("Free") + ` (first ${freeProjects} included)`;
  } else if (existingMfeProjectCount >= freeProjects) {
    projectFee = import_chalk146.default.yellow(`$250.00/month x ${newProjectCount}`) + ` project${newProjectCount > 1 ? "s" : ""}`;
  } else {
    const freeRemaining = freeProjects - existingMfeProjectCount;
    const paidProjects = newProjectCount - freeRemaining;
    projectFee = import_chalk146.default.green(`${freeRemaining} free`) + ", " + import_chalk146.default.yellow(`$250.00/month x ${paidProjects}`);
  }
  output_manager_default.log(`  ${import_chalk146.default.bold("Project fee:")}  ${projectFee}`);
  output_manager_default.log(
    `  ${import_chalk146.default.bold("Request fee:")}  ${import_chalk146.default.yellow("$2.00")} per million microfrontends routed requests`
  );
  output_manager_default.log("");
  if (totalAfter <= freeProjects) {
    output_manager_default.log(
      `This group is within the free tier, so no project fee will be charged to ${import_chalk146.default.bold(teamSlug)}'s bill. Request fees still apply for microfrontends routed requests.`
    );
  } else {
    const newPaidProjects = Math.min(
      newProjectCount,
      totalAfter - freeProjects
    );
    output_manager_default.log(
      `By proceeding, ${import_chalk146.default.bold(teamSlug)} will be charged for ${newPaidProjects} additional paid project${newPaidProjects > 1 ? "s" : ""} on the team's monthly bill.`
    );
  }
  output_manager_default.log("");
  if (!autoConfirm && !client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use --yes to confirm group creation in non-interactive mode."
    );
    return 1;
  }
  const confirmed = autoConfirm || await client.input.confirm("Create microfrontends group?", true);
  if (!confirmed) {
    output_manager_default.log("Aborted.");
    return 0;
  }
  const createStamp = stamp_default();
  output_manager_default.spinner("Creating microfrontends group\u2026");
  try {
    await client.fetch(`/v1/microfrontends/group?teamId=${team.id}`, {
      method: "POST",
      body: {
        groupName,
        defaultApp: {
          projectId: defaultApp.id,
          defaultRoute
        },
        otherApplications
      }
    });
  } catch (error) {
    output_manager_default.stopSpinner();
    if (isAPIError(error) && error.status === 403) {
      output_manager_default.error(
        "You must be an Owner to create or modify microfrontends groups."
      );
      return 1;
    }
    printError(error);
    return 1;
  }
  output_manager_default.stopSpinner();
  const settingsUrl = `https://vercel.com/${teamSlug}/${defaultApp.name}/settings/microfrontends`;
  output_manager_default.success(
    `Microfrontends group "${groupName}" created ${import_chalk146.default.gray(createStamp())}`
  );
  output_manager_default.log(
    `View microfrontends group: ${output_manager_default.link(settingsUrl, settingsUrl, { fallback: false })}`
  );
  const link = await getLinkedProject(client, client.cwd);
  const linkedProject = link.status === "linked" ? link.project : void 0;
  if (linkedProject && linkedProject.id === defaultApp.id) {
    const repoRoot = link.status === "linked" ? link.repoRoot : void 0;
    const projectDir = repoRoot ? join6(repoRoot, linkedProject.rootDirectory || "") : client.cwd;
    const configPath = join6(projectDir, "microfrontends.json");
    if (!existsSync(configPath)) {
      output_manager_default.log("");
      output_manager_default.log(
        `No changes to production traffic will take effect until a ${import_chalk146.default.bold("microfrontends.json")} is added to the default app. This configuration file specifies the path routing for the microfrontends group.`
      );
      const shouldCreate = client.stdin.isTTY && !isNonInteractive && await client.input.confirm("Create a microfrontends.json now?", true);
      if (shouldCreate) {
        const routingPaths = {};
        for (const project of otherProjects) {
          output_manager_default.log(
            `Enter routing paths for ${import_chalk146.default.bold(project.name)} (comma-separated, e.g. /docs, /docs/*)`
          );
          const pathsInput = await client.input.text({
            message: `Paths for ${project.name}:`,
            validate: (val) => {
              if (!val.trim()) {
                return "At least one path is required";
              }
              const paths = val.split(",").map((p) => p.trim());
              for (const p of paths) {
                const result = validateRoutingPath(p);
                if (result !== true) {
                  return `Invalid path "${p}": ${result}`;
                }
              }
              return true;
            }
          });
          routingPaths[project.name] = pathsInput.split(",").map((p) => p.trim());
        }
        const productionAlias = defaultApp.targets?.production?.alias?.[0];
        const config = generateMicrofrontendsConfig(
          defaultApp.name,
          routingPaths,
          productionAlias
        );
        await writeFile4(configPath, config + "\n", "utf-8");
        output_manager_default.success(`Created ${import_chalk146.default.bold("microfrontends.json")}`);
        output_manager_default.log(
          "Create a preview deployment to test microfrontends before deploying to production."
        );
      }
    }
  } else {
    output_manager_default.log(
      `Next step: Add a ${import_chalk146.default.bold("microfrontends.json")} to your default app. See ${output_manager_default.link("https://vercel.com/docs/microfrontends/path-routing", "https://vercel.com/docs/microfrontends/path-routing", { fallback: false })}`
    );
  }
  return 0;
}
function generateMicrofrontendsConfig(defaultAppName, routingPaths, productionAlias) {
  const fallback = productionAlias || "...your-production-domain.vercel.app";
  const applications = {
    [defaultAppName]: {
      development: {
        fallback
      }
    }
  };
  for (const [name, paths] of Object.entries(routingPaths)) {
    applications[name] = {
      routing: [{ paths }]
    };
  }
  return JSON.stringify(
    { $schema: "https://openapi.vercel.sh/microfrontends.json", applications },
    null,
    2
  );
}
function validateGroupName(name, existingGroups) {
  if (!name || name.trim().length === 0) {
    return "Group name cannot be empty.";
  }
  if (name.length > MAX_GROUP_NAME_LENGTH) {
    return `Group name must be ${MAX_GROUP_NAME_LENGTH} characters or less.`;
  }
  if (existingGroups.some((g) => g.group.name === name)) {
    return `A group named "${name}" already exists.`;
  }
  return true;
}
function isProjectInMicrofrontendsGroup(project, groups) {
  return groups.some((g) => g.projects.some((p) => p.id === project.id));
}
function parseProjectDefaultRouteFlags(projectDefaultRouteFlags, selectedProjects, defaultApp) {
  const routeByProjectId = /* @__PURE__ */ new Map();
  if (!projectDefaultRouteFlags || projectDefaultRouteFlags.length === 0) {
    return routeByProjectId;
  }
  for (const entry of projectDefaultRouteFlags) {
    const separatorIndex = entry.indexOf("=");
    if (separatorIndex <= 0 || separatorIndex === entry.length - 1) {
      return `Invalid --project-default-route value "${entry}". Use "<project>=<route>", for example "docs=/docs".`;
    }
    const projectIdentifier = entry.slice(0, separatorIndex).trim();
    const route = entry.slice(separatorIndex + 1).trim();
    if (!projectIdentifier || !route) {
      return `Invalid --project-default-route value "${entry}". Use "<project>=<route>", for example "docs=/docs".`;
    }
    const project = selectedProjects.find(
      (p) => p.name === projectIdentifier || p.id === projectIdentifier
    );
    if (!project) {
      return `Invalid --project-default-route value "${entry}". Project "${projectIdentifier}" is not one of the selected projects.`;
    }
    if (project.id === defaultApp.id) {
      return `Invalid --project-default-route value "${entry}". Use --default-route for the default app "${defaultApp.name}".`;
    }
    const routeValidation = validateDefaultRoute(route);
    if (routeValidation !== true) {
      return `Invalid --project-default-route value "${entry}": ${routeValidation}`;
    }
    if (routeByProjectId.has(project.id)) {
      return `Duplicate --project-default-route for project "${project.name}".`;
    }
    routeByProjectId.set(project.id, route);
  }
  return routeByProjectId;
}

// src/commands/microfrontends/add-to-group.ts
var import_chalk147 = __toESM(require_source(), 1);
async function addToGroup(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    addToGroupSubcommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const context = await ensureMicrofrontendsContext(client);
  if (typeof context === "number") {
    return context;
  }
  const { project, team } = context;
  const teamSlug = team.slug;
  const groupsResponse = await fetchMicrofrontendsGroups(client, team.id);
  const { groups, maxMicrofrontendsPerGroup } = groupsResponse;
  const existingGroup = groups.find(
    (g) => g.projects.some((p) => p.id === project.id)
  );
  if (existingGroup) {
    output_manager_default.error(
      `Project "${project.name}" is already part of the microfrontends group "${existingGroup.group.name}". A project cannot be in more than one microfrontends group.`
    );
    return 1;
  }
  if (groups.length === 0) {
    output_manager_default.error(
      "No microfrontends groups exist. Create one first with `vercel microfrontends create-group`."
    );
    return 1;
  }
  const existingMfeProjectCount = groups.reduce(
    (count, g) => count + g.projects.length,
    0
  );
  const freeProjects = 2;
  const totalAfter = existingMfeProjectCount + 1;
  const plan = team.billing.plan;
  const isProTrialPlan = team.billing.plan === "pro" && team.billing.status === "trialing";
  const isLimitedPlan = isProTrialPlan || plan === "hobby";
  if (isLimitedPlan && totalAfter > freeProjects) {
    const planName = isProTrialPlan ? "Pro Trial" : "Hobby";
    const url = `https://vercel.com/${teamSlug}/~/settings/billing`;
    output_manager_default.log(
      `You've reached the microfrontends project limit for ${planName}. Upgrade to Pro to add more projects.`
    );
    output_manager_default.log(`Upgrade: ${output_manager_default.link(url, url, { fallback: false })}`);
    return 1;
  }
  output_manager_default.log(
    `Adding project ${import_chalk147.default.bold(project.name)} to a microfrontends group on ${import_chalk147.default.bold(teamSlug)}.`
  );
  output_manager_default.log("");
  const groupFlag = parsedArgs.flags["--group"];
  const defaultRouteFlag = parsedArgs.flags["--default-route"];
  const wouldAffectBilling = totalAfter > freeProjects;
  if (wouldAffectBilling) {
    if (client.nonInteractive) {
      const settingsUrl2 = `https://vercel.com/${teamSlug}/${project.name}/settings/microfrontends`;
      const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
      const interactiveCmd = getCommandNamePlain(
        `microfrontends add-to-group ${flags.filter((f) => f !== "--non-interactive").join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: "error",
          reason: "purchase_requires_user",
          message: "Adding a project to a microfrontends group affects billing and cannot be performed non-interactively. Agents must not make billing changes on behalf of a user. The user must run this command interactively in a terminal to review billing details and confirm, or complete the action in the Vercel dashboard.",
          next: [
            {
              command: openUrlInBrowserCommand(settingsUrl2),
              when: "user opens Microfrontends settings in the browser"
            },
            {
              command: interactiveCmd,
              when: "user runs this command interactively (remove --non-interactive)"
            }
          ]
        },
        1
      );
    }
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "This command must be run interactively because it affects billing."
      );
      return 1;
    }
  }
  let selectedGroup;
  if (groupFlag) {
    const found = groups.find(
      (g) => g.group.name === groupFlag || g.group.id === groupFlag
    );
    if (!found) {
      output_manager_default.error(`Microfrontends group "${groupFlag}" not found.`);
      return 1;
    }
    selectedGroup = found;
  } else {
    const groupId = await client.input.select({
      message: "Select a microfrontends group:",
      choices: groups.map((g) => ({ name: g.group.name, value: g.group.id }))
    });
    selectedGroup = groups.find((g) => g.group.id === groupId);
  }
  if (selectedGroup.projects.length >= maxMicrofrontendsPerGroup) {
    if (isLimitedPlan) {
      const planName = isProTrialPlan ? "Pro Trial" : "Hobby";
      const url = `https://vercel.com/${teamSlug}/~/settings/billing`;
      output_manager_default.log(
        `You've reached the microfrontends project limit for ${planName}. Upgrade to Pro to add more projects.`
      );
      output_manager_default.log(`Upgrade: ${output_manager_default.link(url, url, { fallback: false })}`);
      return 1;
    }
    output_manager_default.error(
      `Group "${selectedGroup.group.name}" has reached the maximum number of projects (${maxMicrofrontendsPerGroup}).`
    );
    return 1;
  }
  let defaultRoute;
  if (defaultRouteFlag) {
    const validation = validateDefaultRoute(defaultRouteFlag);
    if (validation !== true) {
      output_manager_default.error(validation);
      return 1;
    }
    defaultRoute = defaultRouteFlag;
  } else if (client.stdin.isTTY) {
    output_manager_default.log(
      "Specify a default route for the project. This path will be used for deployment screenshots and the default link to the project."
    );
    defaultRoute = await client.input.text({
      message: "Default route (e.g. /docs):",
      validate: validateDefaultRoute
    });
  }
  output_manager_default.log("");
  output_manager_default.log(import_chalk147.default.bold("Billing"));
  output_manager_default.log(`  ${import_chalk147.default.bold("Team:")}  ${teamSlug}`);
  let projectFee;
  if (totalAfter <= freeProjects) {
    projectFee = import_chalk147.default.green("Free") + ` (first ${freeProjects} included)`;
  } else if (existingMfeProjectCount >= freeProjects) {
    projectFee = import_chalk147.default.yellow("$250.00/month");
  } else {
    projectFee = import_chalk147.default.yellow("$250.00/month") + ` (${freeProjects} free projects used)`;
  }
  output_manager_default.log(`  ${import_chalk147.default.bold("Project fee:")}  ${projectFee}`);
  output_manager_default.log(
    `  ${import_chalk147.default.bold("Request fee:")}  ${import_chalk147.default.yellow("$2.00")} per million microfrontends routed requests`
  );
  output_manager_default.log("");
  if (totalAfter <= freeProjects) {
    output_manager_default.log(
      `Adding "${project.name}" is within the free tier, so no project fee will be charged to ${import_chalk147.default.bold(teamSlug)}'s bill. Request fees still apply for microfrontends routed requests.`
    );
  } else {
    output_manager_default.log(
      `By proceeding, ${import_chalk147.default.bold(teamSlug)} will be charged for this project on the team's monthly bill.`
    );
  }
  output_manager_default.log("");
  const addConfirmed = await client.input.confirm(
    `Add "${project.name}" to "${selectedGroup.group.name}"?`,
    true
  );
  if (!addConfirmed) {
    output_manager_default.log("Aborted.");
    return 0;
  }
  const addStamp = stamp_default();
  output_manager_default.spinner("Adding project to microfrontends group\u2026");
  try {
    await client.fetch(
      `/v10/projects/${project.id}/microfrontends?teamId=${teamSlug}`,
      {
        method: "PATCH",
        body: {
          microfrontendsGroupId: selectedGroup.group.id,
          isDefaultApp: false,
          defaultRoute,
          enabled: true
        }
      }
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    if (isAPIError(error) && error.status === 403) {
      output_manager_default.error(
        "You must be an Owner to create or modify microfrontends groups."
      );
      return 1;
    }
    printError(error);
    return 1;
  }
  output_manager_default.stopSpinner();
  const settingsUrl = `https://vercel.com/${teamSlug}/${project.name}/settings/microfrontends`;
  output_manager_default.success(
    `Project "${project.name}" added to group "${selectedGroup.group.name}" ${import_chalk147.default.gray(addStamp())}`
  );
  output_manager_default.log(
    `View microfrontends group: ${output_manager_default.link(settingsUrl, settingsUrl, { fallback: false })}`
  );
  output_manager_default.log(
    `Next step: Add routing paths for this project in your microfrontends.json configuration. See ${output_manager_default.link("https://vercel.com/docs/microfrontends/path-routing", "https://vercel.com/docs/microfrontends/path-routing", { fallback: false })}`
  );
  return 0;
}

// src/commands/microfrontends/remove-from-group.ts
var import_chalk148 = __toESM(require_source(), 1);
async function removeFromGroup(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    removeFromGroupSubcommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const context = await ensureMicrofrontendsContext(client, { autoConfirm });
  if (typeof context === "number") {
    return context;
  }
  const { project, team } = context;
  const teamSlug = team.slug;
  const groupsResponse = await fetchMicrofrontendsGroups(client, team.id);
  const { groups } = groupsResponse;
  const projectGroup = groups.find(
    (g) => g.projects.some((p) => p.id === project.id)
  );
  if (!projectGroup) {
    output_manager_default.error(
      `Project "${project.name}" is not part of any microfrontends group.`
    );
    return 1;
  }
  const projectEntry = projectGroup.projects.find((p) => p.id === project.id);
  if (projectEntry?.microfrontends?.isDefaultApp) {
    output_manager_default.error(
      `Project "${project.name}" is the default app for group "${projectGroup.group.name}" and cannot be removed. To remove it, first change the default app in the dashboard.`
    );
    return 1;
  }
  if (client.nonInteractive && !autoConfirm) {
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: `Removing "${project.name}" from microfrontends group "${projectGroup.group.name}" requires confirmation. Use --yes to confirm.`,
        next: [
          {
            command: buildCommandWithYes(client.argv),
            when: "to confirm removal"
          }
        ]
      },
      1
    );
    return 1;
  }
  output_manager_default.log(
    `Removing project ${import_chalk148.default.bold(project.name)} from microfrontends group ${import_chalk148.default.bold(projectGroup.group.name)} on ${import_chalk148.default.bold(teamSlug)}.`
  );
  output_manager_default.log(
    `After removal, "${project.name}" will no longer be a child app in the group and will not be part of the composed application.`
  );
  const config = projectGroup.config;
  const isReferencedInConfig = !!config?.applications?.[project.name];
  if (isReferencedInConfig) {
    output_manager_default.log("");
    output_manager_default.warn(
      `The microfrontends.json configuration still contains an entry for "${project.name}".`
    );
    output_manager_default.warn(
      `Removing this project will cause other microfrontends projects in the group to fail deploying until the configuration is updated.`
    );
    if (!autoConfirm) {
      if (!client.stdin.isTTY) {
        output_manager_default.error(
          "Cannot remove a project that is still referenced in microfrontends.json in non-interactive mode."
        );
        return 1;
      }
      output_manager_default.log("");
      const configConfirmed = await client.input.confirm(
        `Remove "${project.name}" even though it is still referenced in microfrontends.json?`,
        false
      );
      if (!configConfirmed) {
        output_manager_default.log("Aborted.");
        return 0;
      }
    }
  }
  output_manager_default.log("");
  if (!autoConfirm) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "This command must be run interactively to confirm removal."
      );
      return 1;
    }
    const confirmed = await client.input.confirm(
      `Remove "${project.name}" from "${projectGroup.group.name}"?`,
      false
    );
    if (!confirmed) {
      output_manager_default.log("Aborted.");
      return 0;
    }
  }
  const removeStamp = stamp_default();
  output_manager_default.spinner("Removing project from microfrontends group\u2026");
  try {
    await client.fetch(
      `/v10/projects/${project.id}/microfrontends?teamId=${teamSlug}`,
      {
        method: "PATCH",
        body: {
          enabled: false
        }
      }
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    if (isAPIError(error) && error.status === 403) {
      output_manager_default.error(
        "You must be an Owner to create or modify microfrontends groups."
      );
      return 1;
    }
    printError(error);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success(
    `Project "${project.name}" removed from group "${projectGroup.group.name}" ${import_chalk148.default.gray(removeStamp())}`
  );
  if (isReferencedInConfig) {
    output_manager_default.log(
      `Remember to remove "${project.name}" from your microfrontends.json configuration.`
    );
  }
  return 0;
}

// src/commands/microfrontends/delete-group.ts
var import_chalk149 = __toESM(require_source(), 1);
async function deleteGroup(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    deleteGroupSubcommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const groupFlag = parsedArgs.flags["--group"];
  if (client.nonInteractive && !autoConfirm) {
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: "Deleting a microfrontends group requires confirmation. Use --yes and --group to confirm.",
        next: [
          {
            command: buildCommandWithYes(client.argv),
            when: "to confirm deletion"
          }
        ]
      },
      1
    );
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Microfrontends are only available for teams.");
    return 1;
  }
  const groupsResponse = await fetchMicrofrontendsGroups(client, team.id);
  const { groups } = groupsResponse;
  if (groups.length === 0) {
    output_manager_default.error("No microfrontends groups exist. There is nothing to delete.");
    return 1;
  }
  if (!client.stdin.isTTY && !groupFlag) {
    output_manager_default.error(
      "Missing required flag --group. Use --group to specify the microfrontends group, or run interactively."
    );
    return 1;
  }
  let selectedGroup;
  if (groupFlag) {
    const found = groups.find(
      (g) => g.group.name === groupFlag || g.group.id === groupFlag
    );
    if (!found) {
      output_manager_default.error(`Microfrontends group "${groupFlag}" not found.`);
      return 1;
    }
    selectedGroup = found;
  } else {
    const link = await getLinkedProject(client, client.cwd);
    const linkedProject = link.status === "linked" ? link.project : void 0;
    const linkedGroup = linkedProject ? groups.find(
      (g) => g.projects.some(
        (p) => p.id === linkedProject.id && p.microfrontends?.isDefaultApp
      )
    ) : void 0;
    if (linkedGroup && client.stdin.isTTY) {
      const useLinkedGroup = await client.input.confirm(
        `Delete microfrontends group "${linkedGroup.group.name}" (linked to ${import_chalk149.default.bold(linkedProject.name)})?`,
        true
      );
      if (useLinkedGroup) {
        selectedGroup = linkedGroup;
      } else if (groups.length === 1) {
        output_manager_default.log("Aborted.");
        return 0;
      } else {
        const remaining = groups.filter(
          (g) => g.group.id !== linkedGroup.group.id
        );
        const groupId = await client.input.select({
          message: "Select a microfrontends group to delete:",
          choices: remaining.map((g) => ({
            name: g.group.name,
            value: g.group.id
          }))
        });
        selectedGroup = groups.find((g) => g.group.id === groupId);
      }
    } else {
      const groupId = await client.input.select({
        message: "Select a microfrontends group to delete:",
        choices: groups.map((g) => ({
          name: g.group.name,
          value: g.group.id
        }))
      });
      selectedGroup = groups.find((g) => g.group.id === groupId);
    }
  }
  const groupName = selectedGroup.group.name;
  const projectCount = selectedGroup.projects.length;
  output_manager_default.log("");
  output_manager_default.log(
    `This will delete the microfrontends group ${import_chalk149.default.bold(groupName)} and all of its settings.`
  );
  if (projectCount > 0) {
    output_manager_default.log(
      `${projectCount} project${projectCount > 1 ? "s" : ""} will be removed from the group.`
    );
  }
  output_manager_default.log(import_chalk149.default.red("This action is not reversible."));
  output_manager_default.log("");
  if (!autoConfirm) {
    if (!client.stdin.isTTY) {
      output_manager_default.error(
        "This command must be run interactively to confirm deletion."
      );
      return 1;
    }
    const typedName = await client.input.text({
      message: `Type ${import_chalk149.default.bold(groupName)} to confirm deletion:`,
      validate: (val) => {
        if (val !== groupName) {
          return `You must type "${groupName}" to confirm.`;
        }
        return true;
      }
    });
    if (typedName !== groupName) {
      output_manager_default.log("Aborted.");
      return 0;
    }
  }
  const deleteStamp = stamp_default();
  output_manager_default.spinner("Deleting microfrontends group\u2026");
  try {
    await client.fetch(
      `/v2/teams/${team.id}/microfrontends/${selectedGroup.group.id}?teamId=${team.id}`,
      {
        method: "DELETE"
      }
    );
  } catch (error) {
    output_manager_default.stopSpinner();
    if (isAPIError(error) && error.status === 403) {
      output_manager_default.error(
        "You must be an Owner to create or modify microfrontends groups."
      );
      return 1;
    }
    printError(error);
    return 1;
  }
  output_manager_default.stopSpinner();
  output_manager_default.success(
    `Microfrontends group "${groupName}" deleted ${import_chalk149.default.gray(deleteStamp())}`
  );
  return 0;
}

// src/commands/microfrontends/inspect-group.ts
import { existsSync as existsSync2 } from "fs";
import { join as join7 } from "path";
var import_chalk150 = __toESM(require_source(), 1);
async function inspectGroup(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    inspectGroupSubcommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(
      "error" in formatResult ? formatResult.error : "Invalid output format"
    );
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const groupFlag = parsedArgs.flags["--group"];
  const configFileNameFlag = parsedArgs.flags["--config-file-name"];
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("Microfrontends are only available for teams.");
    return 1;
  }
  const groupsResponse = await fetchMicrofrontendsGroups(client, team.id);
  const { groups } = groupsResponse;
  if (groups.length === 0) {
    output_manager_default.error("No microfrontends groups exist.");
    return 1;
  }
  if (!groupFlag && client.nonInteractive) {
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        action: AGENT_ACTION.MISSING_ARGUMENTS,
        message: "Missing required flag --group. Use --group and --format=json in non-interactive mode.",
        next: [
          {
            command: 'vercel microfrontends inspect-group --group="<group-name-or-id>" --format=json',
            when: "to inspect a specific microfrontends group non-interactively"
          }
        ]
      },
      1
    );
    return 1;
  }
  if (!client.stdin.isTTY && !groupFlag) {
    output_manager_default.error(
      "Missing required flag --group. Use --group to specify the microfrontends group, or run interactively."
    );
    return 1;
  }
  let selectedGroup;
  if (groupFlag) {
    const found = groups.find(
      (g) => g.group.name === groupFlag || g.group.id === groupFlag || g.group.slug === groupFlag
    );
    if (!found) {
      output_manager_default.error(`Microfrontends group "${groupFlag}" not found.`);
      return 1;
    }
    selectedGroup = found;
  } else {
    const groupId = await client.input.select({
      message: "Select a microfrontends group to inspect:",
      choices: groups.map((g) => ({ name: g.group.name, value: g.group.id }))
    });
    selectedGroup = groups.find((g) => g.group.id === groupId);
  }
  const localRepoContext = await getLocalRepoContext(client);
  output_manager_default.spinner("Fetching project metadata\u2026");
  const projects = await Promise.all(
    selectedGroup.projects.map(
      (project) => enrichGroupProject(
        client,
        team.id,
        selectedGroup,
        project,
        localRepoContext
      )
    )
  );
  output_manager_default.stopSpinner();
  const defaultProject = projects.find((p) => p.isDefaultApp);
  const configuredConfigFileName = resolveConfiguredMicrofrontendsConfigFileName(configFileNameFlag);
  if (configFileNameFlag && !configuredConfigFileName) {
    output_manager_default.error(
      "Invalid --config-file-name. Value must end with .json or .jsonc."
    );
    return 1;
  }
  const configFile = resolveLocalConfigFilePath(
    projects,
    localRepoContext,
    configuredConfigFileName
  );
  const json = {
    group: {
      id: selectedGroup.group.id,
      slug: selectedGroup.group.slug,
      name: selectedGroup.group.name,
      fallbackEnvironment: selectedGroup.group.fallbackEnvironment ?? null
    },
    projectCount: projects.length,
    defaultApp: defaultProject?.name ?? defaultProject?.id ?? null,
    configFile,
    config: {
      exists: !!selectedGroup.config,
      applications: Object.keys(selectedGroup.config?.applications ?? {})
    },
    projects
  };
  if (asJson) {
    client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
    return 0;
  }
  const groupTable = table([
    ["Group", json.group.name],
    ["ID", json.group.id],
    ["Slug", json.group.slug],
    ["Projects", String(json.projectCount)],
    ["Default app", json.defaultApp ?? "(none)"],
    ["Fallback environment", json.group.fallbackEnvironment ?? "(none)"],
    ["Local config file", json.configFile ?? "(not found)"],
    [
      "Config applications",
      json.config.applications.length > 0 ? json.config.applications.join(", ") : "(none)"
    ]
  ]);
  output_manager_default.log("");
  output_manager_default.log(groupTable);
  output_manager_default.log("");
  output_manager_default.log("Projects");
  output_manager_default.log("");
  for (const project of projects) {
    const rows = [
      [
        "Project",
        project.isDefaultApp ? `${project.name} (default app)` : project.name
      ],
      ["Default route", project.defaultRoute ?? "(none)"],
      ["Production domain", project.productionDomain ?? "(none)"],
      ["Framework", project.framework ?? "(unknown)"],
      [
        "Git org/repo",
        `${project.git.org ?? "(unknown)"}/${project.git.repo ?? "(unknown)"}`
      ],
      ["Root directory", project.git.rootDirectory ?? "(unknown)"]
    ];
    if (project.packageName) {
      rows.push(["Package name", project.packageName]);
    }
    if (!project.inGroupConfig) {
      rows.push(["In config", (0, import_chalk150.red)("no")]);
    }
    const projectTable = table(rows);
    output_manager_default.log(projectTable);
    output_manager_default.log("");
  }
  for (const project of projects) {
    if (project.projectFetchStatus !== "ok") {
      output_manager_default.warn(
        `Project metadata for "${project.id}" is incomplete (status: ${project.projectFetchStatus}).`
      );
    }
  }
  return 0;
}
async function enrichGroupProject(client, teamId, group, groupProject, localRepoContext) {
  let fullProject;
  let projectFetchStatus = "ok";
  try {
    const maybeProject = await getProjectByNameOrId(
      client,
      groupProject.id,
      teamId
    );
    if (maybeProject instanceof ProjectNotFound) {
      projectFetchStatus = "not_found";
    } else {
      fullProject = maybeProject;
    }
  } catch {
    projectFetchStatus = "error";
  }
  const name = groupProject.name ?? fullProject?.name ?? groupProject.id;
  const inGroupConfig = Object.prototype.hasOwnProperty.call(
    group.config?.applications ?? {},
    name
  );
  const rootDirectory = fullProject?.rootDirectory ?? null;
  const gitRepo = fullProject?.link?.repo ?? null;
  const gitOrg = fullProject?.link?.org ?? null;
  const productionDomain = fullProject?.targets?.production?.alias?.[0] ?? fullProject?.targets?.production?.url ?? null;
  const framework = fullProject?.framework ?? null;
  const packageName2 = await resolvePackageNameFromLocalRepo(
    localRepoContext,
    gitOrg,
    gitRepo,
    rootDirectory
  );
  return {
    id: groupProject.id,
    name,
    isDefaultApp: !!groupProject.microfrontends?.isDefaultApp,
    enabled: !!groupProject.microfrontends?.enabled,
    defaultRoute: groupProject.microfrontends?.defaultRoute ?? null,
    productionDomain,
    framework,
    git: {
      org: gitOrg,
      repo: gitRepo,
      rootDirectory
    },
    packageName: packageName2,
    inGroupConfig,
    projectFetchStatus
  };
}
async function getLocalRepoContext(client) {
  try {
    const link = await getLinkedProject(client, client.cwd);
    if (link.status !== "linked") {
      return {
        repoRoot: null,
        linkedProjectId: null,
        linkedRepoOrg: null,
        linkedRepoName: null
      };
    }
    return {
      repoRoot: link.repoRoot ?? null,
      linkedProjectId: link.project.id,
      linkedRepoOrg: link.project.link?.org ?? null,
      linkedRepoName: link.project.link?.repo ?? null
    };
  } catch {
    return {
      repoRoot: null,
      linkedProjectId: null,
      linkedRepoOrg: null,
      linkedRepoName: null
    };
  }
}
function isProjectInLocalRepo(localRepoContext, gitOrg, gitRepo) {
  if (!localRepoContext.repoRoot) {
    return false;
  }
  if (!localRepoContext.linkedRepoOrg || !localRepoContext.linkedRepoName) {
    return false;
  }
  return localRepoContext.linkedRepoOrg === gitOrg && localRepoContext.linkedRepoName === gitRepo;
}
async function resolvePackageNameFromLocalRepo(localRepoContext, gitOrg, gitRepo, rootDirectory) {
  if (!isProjectInLocalRepo(localRepoContext, gitOrg, gitRepo)) {
    return null;
  }
  const projectDir = join7(localRepoContext.repoRoot, rootDirectory || "");
  const packageJsonPath = join7(projectDir, "package.json");
  const pkg = await readJSONFile(packageJsonPath);
  if (!pkg || pkg instanceof Error) {
    return null;
  }
  return typeof pkg.name === "string" ? pkg.name : null;
}
function resolveLocalConfigFilePath(projects, localRepoContext, configuredConfigFileName) {
  const defaultProject = projects.find((project) => project.isDefaultApp);
  if (!defaultProject) {
    return null;
  }
  if (!isProjectInLocalRepo(
    localRepoContext,
    defaultProject.git.org,
    defaultProject.git.repo
  )) {
    return null;
  }
  const projectDir = join7(
    localRepoContext.repoRoot,
    defaultProject.git.rootDirectory || ""
  );
  const configuredName = configuredConfigFileName ?? null;
  if (configuredName) {
    const configuredPath = join7(projectDir, configuredName);
    if (existsSync2(configuredPath)) {
      return configuredPath;
    }
  }
  const jsonPath = join7(projectDir, "microfrontends.json");
  const jsoncPath = join7(projectDir, "microfrontends.jsonc");
  if (existsSync2(jsonPath)) {
    return jsonPath;
  }
  if (existsSync2(jsoncPath)) {
    return jsoncPath;
  }
  return null;
}
function resolveConfiguredMicrofrontendsConfigFileName(configured) {
  if (!configured) {
    return null;
  }
  const normalized = configured.trim();
  if (!normalized.endsWith(".json") && !normalized.endsWith(".jsonc")) {
    return null;
  }
  return normalized.replace(/^\/+/, "");
}

// src/util/telemetry/commands/microfrontends/index.ts
var MicrofrontendsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandPull(actual) {
    this.trackCliSubcommand({
      subcommand: "pull",
      value: actual
    });
  }
  trackCliSubcommandCreateGroup(actual) {
    this.trackCliSubcommand({
      subcommand: "create-group",
      value: actual
    });
  }
  trackCliSubcommandAddToGroup(actual) {
    this.trackCliSubcommand({
      subcommand: "add-to-group",
      value: actual
    });
  }
  trackCliSubcommandRemoveFromGroup(actual) {
    this.trackCliSubcommand({
      subcommand: "remove-from-group",
      value: actual
    });
  }
  trackCliSubcommandDeleteGroup(actual) {
    this.trackCliSubcommand({
      subcommand: "delete-group",
      value: actual
    });
  }
  trackCliSubcommandInspectGroup(actual) {
    this.trackCliSubcommand({
      subcommand: "inspect-group",
      value: actual
    });
  }
};

// src/commands/microfrontends/index.ts
var COMMAND_CONFIG30 = {
  "create-group": getCommandAliases(createGroupSubcommand),
  "add-to-group": getCommandAliases(addToGroupSubcommand),
  "remove-from-group": getCommandAliases(removeFromGroupSubcommand),
  "delete-group": getCommandAliases(deleteGroupSubcommand),
  "inspect-group": getCommandAliases(inspectGroupSubcommand),
  pull: getCommandAliases(pullSubcommand)
};
async function main23(client) {
  const telemetry2 = new MicrofrontendsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    microfrontendsCommand.options
  );
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const { subcommand, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG30
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("microfrontends");
    output_manager_default.print(
      help(microfrontendsCommand, { columns: client.stderr.columns })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: microfrontendsCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  switch (subcommand) {
    case "create-group":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(createGroupSubcommand);
      }
      telemetry2.trackCliSubcommandCreateGroup(subcommandOriginal);
      return createGroup(client);
    case "add-to-group":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(addToGroupSubcommand);
      }
      telemetry2.trackCliSubcommandAddToGroup(subcommandOriginal);
      return addToGroup(client);
    case "remove-from-group":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(removeFromGroupSubcommand);
      }
      telemetry2.trackCliSubcommandRemoveFromGroup(subcommandOriginal);
      return removeFromGroup(client);
    case "delete-group":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(deleteGroupSubcommand);
      }
      telemetry2.trackCliSubcommandDeleteGroup(subcommandOriginal);
      return deleteGroup(client);
    case "inspect-group":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(inspectGroupSubcommand);
      }
      telemetry2.trackCliSubcommandInspectGroup(subcommandOriginal);
      return inspectGroup(client);
    case "pull":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("microfrontends", subcommandOriginal);
        return printHelp(pullSubcommand);
      }
      telemetry2.trackCliSubcommandPull(subcommandOriginal);
      return pull2(client);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG30));
      output_manager_default.print(
        help(microfrontendsCommand, { columns: client.stderr.columns })
      );
      return 2;
  }
}

// src/commands/oauth-apps/index.ts
var COMMAND_CONFIG31 = {
  listRequests: getCommandAliases(listRequestsSubcommand),
  register: getCommandAliases(registerSubcommand),
  dismiss: getCommandAliases(dismissSubcommand),
  install: getCommandAliases(installSubcommand),
  remove: getCommandAliases(removeSubcommand11)
};
var KNOWN_OAUTH_APPS_SUBCOMMANDS = /* @__PURE__ */ new Set([
  "listRequests",
  "register",
  "dismiss",
  "install",
  "remove"
]);
function suggestVercelCommand(client, commandTail) {
  const globals = getGlobalFlagsFromArgv(client.argv);
  if (globals.length === 0) {
    return `${packageName} ${commandTail}`;
  }
  return `${packageName} ${globals.join(" ")} ${commandTail}`;
}
function suggestOauthAppsCommand(client, oauthAppsTail) {
  return suggestVercelCommand(client, `oauth-apps ${oauthAppsTail}`);
}
function shellSingleQuoteArg(value) {
  if (value === "") {
    return `''`;
  }
  if (!value.includes("'")) {
    return `'${value}'`;
  }
  return `"${value.replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\$/g, "\\$").replace(/`/g, "\\`").replace(/\r?\n/g, "\\n")}"`;
}
var OPTION_REQUIRES_ARGUMENT_RE = /^option requires argument:\s*(\S+)/i;
function snapshotRegisterFlagsFromArgs(registerArgs) {
  const redirectUris = [];
  let name;
  let slug;
  let description;
  let outputFormatJson;
  for (let i = 0; i < registerArgs.length; i++) {
    const a = registerArgs[i];
    if (a === "--name") {
      const v = registerArgs[i + 1];
      if (v !== void 0 && !v.startsWith("-")) {
        name = v;
        i++;
      }
    } else if (a.startsWith("--name=")) {
      name = a.slice("--name=".length);
    } else if (a === "--slug") {
      const v = registerArgs[i + 1];
      if (v !== void 0 && !v.startsWith("-")) {
        slug = v;
        i++;
      }
    } else if (a.startsWith("--slug=")) {
      slug = a.slice("--slug=".length);
    } else if (a === "--description") {
      const v = registerArgs[i + 1];
      if (v !== void 0 && !v.startsWith("-")) {
        description = v;
        i++;
      }
    } else if (a.startsWith("--description=")) {
      description = a.slice("--description=".length);
    } else if (a === "--redirect-uri") {
      const v = registerArgs[i + 1];
      if (v !== void 0 && !v.startsWith("-")) {
        redirectUris.push(v);
        i++;
      }
    } else if (a.startsWith("--redirect-uri=")) {
      redirectUris.push(a.slice("--redirect-uri=".length));
    } else if (a === "--json") {
      outputFormatJson = true;
    } else if (a === "--format") {
      const v = registerArgs[i + 1];
      if (v !== void 0 && v.toLowerCase() === "json") {
        outputFormatJson = true;
        i++;
      }
    } else if (a.startsWith("--format=")) {
      const v = a.slice("--format=".length);
      if (v.toLowerCase() === "json") {
        outputFormatJson = true;
      }
    }
  }
  return { name, slug, description, redirectUris, outputFormatJson };
}
function buildRegisterCommandTailForSuggestion(s) {
  const parts = ["register"];
  parts.push(
    s.name !== void 0 ? `--name ${shellSingleQuoteArg(s.name)}` : "--name <display-name>"
  );
  parts.push(
    s.slug !== void 0 ? `--slug ${shellSingleQuoteArg(s.slug)}` : "--slug <slug>"
  );
  if (s.description !== void 0) {
    parts.push(`--description ${shellSingleQuoteArg(s.description)}`);
  }
  for (const u of s.redirectUris) {
    parts.push(`--redirect-uri ${shellSingleQuoteArg(u)}`);
  }
  if (s.outputFormatJson) {
    parts.push("--format json");
  }
  return parts.join(" ");
}
function registerOptionErrorWhenLine(snap, erroredFlag) {
  const needName = snap.name === void 0;
  const needSlug = snap.slug === void 0;
  if (!needName && needSlug && erroredFlag === "--slug") {
    return "Substitute <slug> only; `--name` was copied from your command. Keep the slug value immediately after `--slug`, before globals like `--cwd`.";
  }
  if (needName && needSlug) {
    return "Replace <display-name> and <slug>; keep the slug immediately after `--slug` when adding more flags.";
  }
  if (needSlug) {
    return "Replace <slug>; keep the slug immediately after `--slug` when adding more flags.";
  }
  if (needName) {
    return "Replace <display-name>.";
  }
  return "Fix flag values and retry.";
}
function tryEmitRegisterOptionRequiresArgumentError(client, err, registerArgs) {
  if (!shouldEmitNonInteractiveCommandError(client)) {
    return false;
  }
  if (!(err instanceof Error) || !OPTION_REQUIRES_ARGUMENT_RE.test(err.message)) {
    return false;
  }
  const flag = err.message.match(OPTION_REQUIRES_ARGUMENT_RE)?.[1] ?? "option";
  const snap = snapshotRegisterFlagsFromArgs(registerArgs);
  const tail = buildRegisterCommandTailForSuggestion(snap);
  const isSlug = flag === "--slug";
  const message = isSlug ? "Missing value for `--slug`: the next token starts with `--`, so it was not treated as the slug." : `\`${flag}\` requires a value before the next \`--\` flag.`;
  const hint = isSlug ? `Put the slug immediately after \`--slug\`, before globals like \`--cwd\`. Example: \`${packageName} oauth-apps register --name <display-name> --slug <slug> --cwd <path> --non-interactive\`. The slug is a URL-safe id you choose (lowercase letters, numbers, hyphens).` : `Reorder or quote values so each flag is followed by its argument. Example: \`${packageName} oauth-apps register --name <display-name> --slug <slug>\`.`;
  outputAgentError(
    client,
    {
      status: "error",
      reason: AGENT_REASON.INVALID_ARGUMENTS,
      message,
      hint,
      next: [
        {
          command: suggestOauthAppsCommand(client, tail),
          when: registerOptionErrorWhenLine(snap, flag)
        }
      ]
    },
    1
  );
  output_manager_default.error(message);
  return true;
}
function emitOauthAppsApiError(client, message, jsonOutputFlag) {
  const payload = {
    status: "error",
    reason: AGENT_REASON.API_ERROR,
    message
  };
  if (shouldEmitNonInteractiveCommandError(client)) {
    outputAgentError(client, payload, 1);
    return 1;
  }
  if (jsonOutputFlag) {
    client.stdout.write(`${JSON.stringify(payload, null, 2)}
`);
    return 1;
  }
  output_manager_default.error(message);
  return 1;
}
async function ensureTeamScopeForOauthAppsApi(client, registerContext) {
  const { team } = await getScope(client);
  if (team) {
    client.config.currentTeam = team.id;
    return true;
  }
  if (registerContext) {
    const { name, slug } = registerContext;
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_SCOPE,
        message: "Registering a Vercel App requires a team. Switch scope with `vercel teams switch` or pass `--scope <team>`.",
        hint: "OAuth apps are owned by a team; personal (user-only) scope cannot create them.",
        next: [
          {
            command: suggestVercelCommand(client, "teams switch"),
            when: "Pick the team that will own the app"
          },
          {
            command: suggestOauthAppsCommand(
              client,
              `register --name ${shellSingleQuoteArg(name)} --slug ${shellSingleQuoteArg(slug)}`
            ),
            when: "Retry register after selecting a team"
          }
        ]
      },
      1
    );
    output_manager_default.error(
      "Registering a Vercel App requires a team. Use `vercel teams switch` or `--scope <team>`."
    );
    return false;
  }
  outputAgentError(
    client,
    {
      status: "error",
      reason: AGENT_REASON.MISSING_SCOPE,
      message: "OAuth app commands require a team. Switch scope with `vercel teams switch` or pass `--scope <team>`.",
      hint: "Installations and requests are scoped to a team; personal (user-only) scope cannot use these APIs.",
      next: [
        {
          command: suggestVercelCommand(client, "teams switch"),
          when: "Pick the team for OAuth app actions"
        },
        {
          command: suggestOauthAppsCommand(client, "list-requests"),
          when: "List pending installation requests after selecting a team"
        }
      ]
    },
    1
  );
  output_manager_default.error(
    "OAuth app commands require a team. Use `vercel teams switch` or `--scope <team>`."
  );
  return false;
}
async function main24(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(oauthAppsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const { subcommand, args } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG31
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    output_manager_default.print(help(oauthAppsCommand, { columns: client.stderr.columns }));
    return 0;
  }
  if (subcommand === void 0 && args.length > 0 && !args[0].startsWith("-")) {
    output_manager_default.error(
      `Invalid oauth-apps subcommand "${args[0]}". Use: dismiss | install | list-requests | register | remove`
    );
    return 1;
  }
  if (typeof subcommand === "string" && !KNOWN_OAUTH_APPS_SUBCOMMANDS.has(subcommand)) {
    output_manager_default.error(
      `Invalid oauth-apps subcommand "${subcommand}". Use: dismiss | install | list-requests | register | remove`
    );
    return 1;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: oauthAppsCommand,
        columns: client.stderr.columns
      })
    );
    return 0;
  }
  switch (subcommand) {
    case "dismiss": {
      if (needHelp) {
        return printHelp(dismissSubcommand);
      }
      if (!await ensureTeamScopeForOauthAppsApi(client)) {
        return 1;
      }
      const spec = getFlagsSpecification(dismissSubcommand.options);
      let p;
      try {
        p = parseArguments(args, spec);
      } catch (e2) {
        printError(e2);
        return 1;
      }
      const appId = p.args[0];
      const fr = validateJsonOutput(p.flags);
      if (!fr.valid) {
        output_manager_default.error(fr.error);
        return 1;
      }
      if (!appId) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Missing app client id. Usage: `vercel oauth-apps dismiss <appId>`",
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "oauth-apps dismiss <appId> --yes"
                ),
                when: "Dismiss an installation request"
              }
            ]
          },
          1
        );
        output_manager_default.error("Missing app client id.");
        return 1;
      }
      if (client.nonInteractive && !p.flags["--yes"]) {
        outputActionRequired(
          client,
          {
            status: "action_required",
            reason: AGENT_REASON.CONFIRMATION_REQUIRED,
            message: "Re-run with --yes to dismiss this installation request.",
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  `oauth-apps dismiss ${appId} --yes`
                ),
                when: "Confirm dismiss"
              }
            ]
          },
          1
        );
        return 1;
      }
      if (!client.nonInteractive && !p.flags["--yes"]) {
        const ok = await client.input.confirm(
          `Dismiss installation request for ${appId}?`,
          false
        );
        if (!ok) {
          output_manager_default.log("Canceled.");
          return 0;
        }
      }
      try {
        await client.fetch(
          `/v1/oauth-apps/installation-requests/${encodeURIComponent(appId)}`,
          {
            method: "DELETE"
          }
        );
        if (fr.jsonOutput) {
          client.stdout.write(
            `${JSON.stringify({ dismissed: appId }, null, 2)}
`
          );
        } else {
          output_manager_default.log(`Dismissed installation request for ${appId}`);
        }
        return 0;
      } catch (err) {
        if (isAPIError(err)) {
          const msg = err.serverMessage || `API error (${err.status})`;
          return emitOauthAppsApiError(client, msg, fr.jsonOutput);
        }
        throw err;
      }
    }
    case "register": {
      if (needHelp) {
        return printHelp(registerSubcommand);
      }
      const spec = getFlagsSpecification(registerSubcommand.options);
      let p;
      try {
        p = parseArguments(args, spec);
      } catch (e2) {
        if (tryEmitRegisterOptionRequiresArgumentError(client, e2, args)) {
          return 1;
        }
        printError(e2);
        return 1;
      }
      const fr = validateJsonOutput(p.flags);
      if (!fr.valid) {
        output_manager_default.error(fr.error);
        return 1;
      }
      const name = p.flags["--name"];
      const slug = p.flags["--slug"];
      const redirectUris = p.flags["--redirect-uri"] ?? [];
      const description = p.flags["--description"];
      if (!name) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Missing --name",
            hint: `Provide a display name (3\u2013200 characters, letters, numbers, spaces, hyphens, underscores). Example: \`${packageName} oauth-apps register --name <display-name> --slug <slug>\`.`,
            next: [
              {
                command: suggestOauthAppsCommand(
                  client,
                  "register --name <display-name> --slug <slug>"
                ),
                when: "Replace placeholders; add --redirect-uri for each callback URL if needed"
              }
            ]
          },
          1
        );
        output_manager_default.error("Missing --name");
        return 1;
      }
      if (!slug) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Missing --slug",
            hint: `You choose the slug when you register the app\u2014it is not assigned by Vercel. Use a unique URL-safe id (at least 3 characters; lowercase letters, numbers, and hyphens only), often derived from the app name (e.g. \`acme-dashboard\`). Example: \`${packageName} oauth-apps register --name <display-name> --slug <slug>\`.`,
            next: [
              {
                command: suggestOauthAppsCommand(
                  client,
                  `register --name ${shellSingleQuoteArg(name)} --slug <slug>`
                ),
                when: "Substitute <slug> with your chosen identifier; add --redirect-uri for each OAuth callback URL if needed"
              }
            ]
          },
          1
        );
        output_manager_default.error("Missing --slug");
        return 1;
      }
      if (!await ensureTeamScopeForOauthAppsApi(client, { name, slug })) {
        return 1;
      }
      const body = { name, slug };
      if (redirectUris.length > 0) {
        body.redirectUris = redirectUris;
      }
      if (description !== void 0) {
        body.description = description;
      }
      try {
        const app = await client.fetch("/v1/oauth-apps", {
          method: "POST",
          body
        });
        if (fr.jsonOutput) {
          client.stdout.write(`${JSON.stringify(app, null, 2)}
`);
        } else {
          output_manager_default.log(
            `Registered Vercel App "${app.name}" (${app.clientId}).` + (app.redirectUris?.length ? ` Redirect URIs: ${app.redirectUris.join(", ")}.` : "")
          );
        }
        return 0;
      } catch (err) {
        if (isAPIError(err)) {
          const msg = err.serverMessage || `API error (${err.status})`;
          return emitOauthAppsApiError(client, msg, fr.jsonOutput);
        }
        throw err;
      }
    }
    case "install": {
      if (needHelp) {
        return printHelp(installSubcommand);
      }
      if (!await ensureTeamScopeForOauthAppsApi(client)) {
        return 1;
      }
      const spec = getFlagsSpecification(installSubcommand.options);
      let p;
      try {
        p = parseArguments(args, spec);
      } catch (e2) {
        printError(e2);
        return 1;
      }
      const fr = validateJsonOutput(p.flags);
      if (!fr.valid) {
        output_manager_default.error(fr.error);
        return 1;
      }
      const clientId = p.flags["--client-id"];
      const permissions = p.flags["--permission"] ?? [];
      const projectsRaw = p.flags["--projects"];
      if (!clientId) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Missing --client-id",
            hint: `The OAuth client ID (\`cl_...\`) is issued when a Vercel App is registered (\`${packageName} oauth-apps register --name <display-name> --slug <slug>\`, or the Vercel Dashboard developer flow) or supplied by the app author. Pending installs for your team may list it; run \`${packageName} oauth-apps list-requests\` (use \`--format=json\` in scripts).`,
            next: [
              {
                command: suggestOauthAppsCommand(
                  client,
                  "list-requests --format=json"
                ),
                when: "See pending OAuth app requests for this team (may include client identifiers)"
              },
              {
                command: suggestOauthAppsCommand(
                  client,
                  "install --client-id <client-id> --permission <scope>"
                ),
                when: "Replace <client-id> and <scope> (e.g. read:project); repeat --permission for each scope"
              }
            ]
          },
          1
        );
        output_manager_default.error("Missing --client-id");
        return 1;
      }
      if (permissions.length === 0) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Provide at least one --permission (repeatable)",
            hint: `Each \`--permission\` grants an OAuth scope the app needs (for example \`read:project\`). Check the app\u2019s documentation for required scopes.`,
            next: [
              {
                command: suggestOauthAppsCommand(
                  client,
                  `install --client-id ${clientId} --permission <scope>`
                ),
                when: "Replace <scope> (e.g. read:project); repeat --permission for each scope"
              }
            ]
          },
          1
        );
        output_manager_default.error("Provide at least one --permission");
        return 1;
      }
      const body = {
        clientId,
        permissions
      };
      if (projectsRaw !== void 0) {
        const projectIds = projectsRaw.trim() === "*" ? ["*"] : projectsRaw.split(",").map((s) => s.trim()).filter(Boolean);
        if (projectIds.length) {
          body.resources = { projectIds };
        }
      }
      try {
        const res = await client.fetch("/v1/oauth-apps/installations", { method: "POST", body });
        if (fr.jsonOutput) {
          client.stdout.write(`${JSON.stringify(res, null, 2)}
`);
        } else {
          output_manager_default.log("App installed successfully.");
        }
        return 0;
      } catch (err) {
        if (isAPIError(err)) {
          const msg = err.serverMessage || `API error (${err.status})`;
          return emitOauthAppsApiError(client, msg, fr.jsonOutput);
        }
        throw err;
      }
    }
    case "remove": {
      if (needHelp) {
        return printHelp(removeSubcommand11);
      }
      if (!await ensureTeamScopeForOauthAppsApi(client)) {
        return 1;
      }
      const spec = getFlagsSpecification(removeSubcommand11.options);
      let p;
      try {
        p = parseArguments(args, spec);
      } catch (e2) {
        printError(e2);
        return 1;
      }
      const installationId = p.args[0];
      const fr = validateJsonOutput(p.flags);
      if (!fr.valid) {
        output_manager_default.error(fr.error);
        return 1;
      }
      if (!installationId) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: "Missing installation id"
          },
          1
        );
        output_manager_default.error("Missing installation id");
        return 1;
      }
      if (client.nonInteractive && !p.flags["--yes"]) {
        outputActionRequired(
          client,
          {
            status: "action_required",
            reason: AGENT_REASON.CONFIRMATION_REQUIRED,
            message: "Re-run with --yes to uninstall.",
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  `oauth-apps remove ${installationId} --yes`
                ),
                when: "Confirm uninstall"
              }
            ]
          },
          1
        );
        return 1;
      }
      if (!client.nonInteractive && !p.flags["--yes"]) {
        const ok = await client.input.confirm(
          `Uninstall Vercel App installation ${installationId}?`,
          false
        );
        if (!ok) {
          output_manager_default.log("Canceled.");
          return 0;
        }
      }
      try {
        await client.fetch(
          `/v1/oauth-apps/installations/${encodeURIComponent(installationId)}`,
          { method: "DELETE" }
        );
        if (fr.jsonOutput) {
          client.stdout.write(
            `${JSON.stringify({ uninstalled: installationId }, null, 2)}
`
          );
        } else {
          output_manager_default.log(`Uninstalled ${installationId}`);
        }
        return 0;
      } catch (err) {
        if (isAPIError(err)) {
          const msg = err.serverMessage || `API error (${err.status})`;
          return emitOauthAppsApiError(client, msg, fr.jsonOutput);
        }
        throw err;
      }
    }
    default: {
      if (needHelp) {
        return printHelp(listRequestsSubcommand);
      }
      if (!await ensureTeamScopeForOauthAppsApi(client)) {
        return 1;
      }
      const spec = getFlagsSpecification(listRequestsSubcommand.options);
      let p;
      try {
        p = parseArguments(args, spec);
      } catch (e2) {
        printError(e2);
        return 1;
      }
      const fr = validateJsonOutput(p.flags);
      if (!fr.valid) {
        output_manager_default.error(fr.error);
        return 1;
      }
      try {
        const data = await client.fetch("/v1/oauth-apps/installation-requests");
        if (fr.jsonOutput) {
          client.stdout.write(
            `${JSON.stringify({ installationRequests: data.installationRequests }, null, 2)}
`
          );
          return 0;
        }
        if (!data.installationRequests?.length) {
          output_manager_default.log("No pending installation requests.");
          return 0;
        }
        for (const r of data.installationRequests) {
          output_manager_default.log(
            `${r.app.name} (${r.app.id}) \u2014 requested by ${r.requester.name}`
          );
        }
        return 0;
      } catch (err) {
        if (isAPIError(err)) {
          const msg = err.serverMessage || `API error (${err.status})`;
          return emitOauthAppsApiError(client, msg, fr.jsonOutput);
        }
        throw err;
      }
    }
  }
}

// src/commands/open/index.ts
var import_open15 = __toESM(require_open(), 1);

// src/util/telemetry/commands/open/index.ts
var OpenTelemetryClient = class extends TelemetryClient {
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
  trackCliCommandOpen(value) {
    this.trackCliCommand({
      command: "open",
      value
    });
  }
};

// src/commands/open/index.ts
async function openCommandHandler(client) {
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(openCommand.options);
  const telemetry2 = new OpenTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("open");
    output_manager_default.print(help(openCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const link = await ensureLink("open", client, client.cwd, {
    autoConfirm
  });
  if (typeof link === "number") {
    return link;
  }
  if (link.status !== "linked" || !link.org || !link.project) {
    if (!client.nonInteractive) {
      output_manager_default.error("This command requires a linked project. Please run:");
      output_manager_default.print("  vercel link\n");
    }
    return 1;
  }
  const { org, project } = link;
  const projectUrl = `https://vercel.com/${org.slug}/${project.name}`;
  output_manager_default.log(`Opening ${projectUrl} in your browser...`);
  await (0, import_open15.default)(projectUrl);
  return 0;
}

// src/commands/project/add.ts
var import_chalk151 = __toESM(require_source(), 1);
var import_ms19 = __toESM(require_ms(), 1);

// src/util/telemetry/commands/project/add.ts
var ProjectAddTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(name) {
    if (name) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/project/add.ts
async function add8(client, argv) {
  const telemetryClient = new ProjectAddTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand7.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk151.default.cyan(
        `${getCommandName("project add <name>")}`
      )}`
    );
    if (args.length > 1) {
      const example = import_chalk151.default.cyan(
        `${getCommandName(`project add "${args.join(" ")}"`)}`
      );
      output_manager_default.log(
        `If your project name  has spaces, make sure to wrap it in quotes. Example: 
  ${example} `
      );
    }
    return 1;
  }
  const start = Date.now();
  const [name] = args;
  telemetryClient.trackCliArgumentName(name);
  try {
    await createProject(client, { name });
  } catch (err) {
    if (isAPIError(err) && err.code === "too_many_projects") {
      output_manager_default.prettyError(err);
      return 1;
    }
    if (isAPIError(err) && err.status === 409) {
    } else {
      throw err;
    }
  }
  const elapsed2 = (0, import_ms19.default)(Date.now() - start);
  const { contextName } = await getScope(client);
  output_manager_default.log(
    `${import_chalk151.default.cyan("Success!")} Project ${import_chalk151.default.bold(
      name.toLowerCase()
    )} added (${import_chalk151.default.bold(contextName)}) ${import_chalk151.default.gray(`[${elapsed2}]`)}`
  );
  return 0;
}

// src/commands/project/access-summary.ts
var import_chalk152 = __toESM(require_source(), 1);
async function accessSummary(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    accessSummarySubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project access-summary [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  let summary;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project access-summary",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    summary = await client.fetch(
      `/v1/projects/${encodeURIComponent(project.id)}/members/summary`
    );
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, {
      variant: "access-summary"
    });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(summary, null, 2)}
`);
    return 0;
  }
  const entries = Object.entries(summary).sort(
    ([a], [b]) => a.localeCompare(b)
  );
  if (entries.length === 0) {
    output_manager_default.log("No summary data returned.");
    return 0;
  }
  const rows = [
    ["Role", "Count"].map((h) => (0, import_chalk152.gray)(h)),
    ...entries.map(([role, count]) => [role, String(count)])
  ];
  client.stderr.write(`${table(rows, { hsep: 3 })}
`);
  return 0;
}

// src/commands/project/checks.ts
var import_chalk153 = __toESM(require_source(), 1);

// src/commands/project/checks-add.ts
import { readFileSync as readFileSync3 } from "fs";
import { resolve as resolve7 } from "path";
var REQUIRES = ["build-ready", "deployment-url", "none"];
var BLOCKING_STAGES = [
  "build-start",
  "deployment-start",
  "deployment-alias",
  "deployment-promotion",
  "none"
];
function emitValidationError(client, message, exitCode2, extra) {
  outputAgentError(
    client,
    {
      status: "error",
      reason: AGENT_REASON.INVALID_ARGUMENTS,
      message,
      ...extra
    },
    exitCode2
  );
}
async function checksAdd(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification([...checksAddFlags]);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    emitValidationError(
      client,
      "Invalid number of arguments. Usage: `vercel project checks add [name]`",
      2
    );
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project checks add [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    if (client.nonInteractive) {
      emitValidationError(client, formatResult.error, 1);
    }
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || Boolean(client.nonInteractive);
  const filePath = parsedArgs.flags["--file"];
  const checkName = parsedArgs.flags["--check-name"];
  const requires = parsedArgs.flags["--requires"];
  const blocksFlag = parsedArgs.flags["--blocks"];
  const timeout = parsedArgs.flags["--timeout"];
  const targetsRaw = parsedArgs.flags["--targets"];
  const sourceRaw = parsedArgs.flags["--source"];
  let body;
  if (typeof filePath === "string" && filePath.length > 0) {
    try {
      const raw = readFileSync3(resolve7(client.cwd, filePath), "utf8");
      const parsed = JSON.parse(raw);
      if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
        emitValidationError(client, "`--file` must contain a JSON object.", 1);
        output_manager_default.error("`--file` must contain a JSON object.");
        return 1;
      }
      body = parsed;
    } catch (err) {
      const msg = err instanceof Error ? err.message : String(err);
      emitValidationError(client, `Failed to read \`--file\`: ${msg}`, 1);
      output_manager_default.error(`Failed to read \`--file\`: ${msg}`);
      return 1;
    }
  } else {
    if (typeof checkName !== "string" || !checkName.trim()) {
      emitValidationError(
        client,
        "Provide `--check-name` and `--requires`, or a `--file` with the full JSON body.",
        1,
        {
          hint: 'See `vercel project checks --help` and the REST API "Create a check" documentation.',
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "project checks add <name> --file <path>"
              ),
              when: "Create from a JSON file (replace paths)"
            }
          ]
        }
      );
      output_manager_default.error(
        "Provide `--check-name` and `--requires`, or a `--file` with the full JSON body. See `vercel project checks add --help`."
      );
      return 1;
    }
    if (typeof requires !== "string" || !requires.trim()) {
      emitValidationError(
        client,
        "`--requires` is required when using `--check-name` (build-ready, deployment-url, or none).",
        1
      );
      output_manager_default.error(
        "`--requires` is required when using `--check-name` (build-ready, deployment-url, or none)."
      );
      return 1;
    }
    if (!REQUIRES.includes(requires)) {
      emitValidationError(
        client,
        `\`--requires\` must be one of: ${REQUIRES.join(", ")}`,
        1
      );
      output_manager_default.error(`\`--requires\` must be one of: ${REQUIRES.join(", ")}`);
      return 1;
    }
    body = {
      name: checkName.trim(),
      requires: requires.trim()
    };
    if (typeof blocksFlag === "string" && blocksFlag.length > 0) {
      if (!BLOCKING_STAGES.includes(
        blocksFlag
      )) {
        emitValidationError(
          client,
          `\`--blocks\` must be one of: ${BLOCKING_STAGES.join(", ")}`,
          1
        );
        output_manager_default.error(
          `\`--blocks\` must be one of: ${BLOCKING_STAGES.join(", ")}`
        );
        return 1;
      }
      body.blocks = blocksFlag;
    }
    if (typeof timeout === "number") {
      body.timeout = timeout;
    }
    if (typeof targetsRaw === "string" && targetsRaw.trim()) {
      body.targets = targetsRaw.split(",").map((s) => s.trim()).filter(Boolean);
    }
    if (typeof sourceRaw === "string" && sourceRaw.trim()) {
      try {
        body.source = JSON.parse(sourceRaw);
      } catch {
        emitValidationError(client, "`--source` must be valid JSON.", 1);
        output_manager_default.error("`--source` must be valid JSON.");
        return 1;
      }
    }
  }
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project checks add",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const result = await client.fetch(
      `/v2/projects/${encodeURIComponent(project.id)}/checks`,
      {
        method: "POST",
        body
      }
    );
    if (asJson) {
      const id2 = String(result.id ?? "");
      if (client.nonInteractive) {
        client.stdout.write(
          `${JSON.stringify(
            {
              status: AGENT_STATUS.OK,
              check: result,
              projectId: project.id,
              projectName: project.name,
              message: id2 ? `Created deployment check ${id2} on ${project.name}.` : `Created deployment check on ${project.name}.`,
              next: [
                {
                  command: buildCommandWithGlobalFlags(
                    client.argv,
                    "project checks"
                  ),
                  when: "List checks for this project"
                },
                {
                  command: buildCommandWithGlobalFlags(
                    client.argv,
                    "project checks remove <check_id>"
                  ),
                  when: "Remove a check by id if needed"
                }
              ]
            },
            null,
            2
          )}
`
        );
      } else {
        client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
      }
      return 0;
    }
    const id = String(result.id ?? "");
    output_manager_default.log(
      id ? `Created deployment check ${id} on ${project.name}.` : `Created deployment check on ${project.name}.`
    );
    return 0;
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "checks" });
    printError(err);
    return 1;
  }
}

// src/commands/project/checks-remove.ts
async function checksRemove(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification([...checksRemoveFlags]);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length < 1 || parsedArgs.args.length > 2) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message: "Check id is required. Example: `vercel project checks remove chk_abc123`",
        hint: "Run `project checks` to list check ids, then pass the id to `project checks remove` (optional project name as second argument).",
        next: [
          {
            command: buildCommandWithGlobalFlags(client.argv, "project checks"),
            when: "List deployment checks and their ids"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project checks remove <check_id>"
            ),
            when: "Remove a check after replacing <check_id> with an id from the list"
          }
        ]
      },
      1
    );
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project checks remove <checkId> [project-name]`"
    );
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: formatResult.error
        },
        1
      );
    }
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || Boolean(client.nonInteractive);
  const checkId = parsedArgs.args[0]?.trim();
  if (!checkId) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message: "Check id is required (from `vercel project checks`). Example: `vercel project checks remove chk_abc123`",
        next: [
          {
            command: buildCommandWithGlobalFlags(client.argv, "project checks"),
            when: "List deployment checks and their ids"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project checks remove <check_id>"
            ),
            when: "Remove a check after replacing <check_id>"
          }
        ]
      },
      1
    );
    output_manager_default.error("A check id is required (from `vercel project checks`).");
    return 1;
  }
  const projectNameOrId = parsedArgs.args[1];
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project checks remove",
      projectNameOrId,
      forReadOnlyCommand: true
    });
    const result = await client.fetch(
      `/v2/projects/${encodeURIComponent(project.id)}/checks/${encodeURIComponent(checkId)}`,
      { method: "DELETE" }
    );
    if (asJson) {
      if (client.nonInteractive) {
        client.stdout.write(
          `${JSON.stringify(
            {
              status: AGENT_STATUS.OK,
              checkId,
              projectId: project.id,
              projectName: project.name,
              result,
              message: `Removed check ${checkId} from ${project.name}.`,
              next: [
                {
                  command: buildCommandWithGlobalFlags(
                    client.argv,
                    "project checks"
                  ),
                  when: "List remaining checks"
                }
              ]
            },
            null,
            2
          )}
`
        );
      } else {
        client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
      }
      return 0;
    }
    output_manager_default.log(`Removed check ${checkId} from ${project.name}.`);
    return 0;
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "checks" });
    printError(err);
    return 1;
  }
}

// src/commands/project/checks.ts
var BLOCKING_STAGES2 = [
  "build-start",
  "deployment-start",
  "deployment-alias",
  "deployment-promotion",
  "none"
];
async function checks(client, argv) {
  if (argv[0] === "add") {
    return checksAdd(client, argv.slice(1));
  }
  if (argv[0] === "remove" || argv[0] === "rm") {
    return checksRemove(client, argv.slice(1));
  }
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(checksSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (typeof parsedArgs.flags["--file"] === "string" || typeof parsedArgs.flags["--check-name"] === "string" || typeof parsedArgs.flags["--requires"] === "string") {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: "To create a check, run `vercel project checks add [name]` with `--file` or `--check-name` / `--requires`.",
        hint: "Use `project checks add`, not `project checks`, when passing create flags.",
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project checks add <name>"
            ),
            when: "Create a check (replace <name> or omit if linked)"
          }
        ]
      },
      2
    );
    output_manager_default.error(
      "To create a check, run `vercel project checks add [name]` with `--file` or `--check-name` / `--requires`. See `vercel project checks --help`."
    );
    return 2;
  }
  if (parsedArgs.args.length > 1) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: "Invalid number of arguments. Usage: `vercel project checks [name]`"
      },
      2
    );
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project checks [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: formatResult.error
        },
        1
      );
    }
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || Boolean(client.nonInteractive);
  const blocksFlag = parsedArgs.flags["--blocks"];
  if (typeof blocksFlag === "string") {
    if (!BLOCKING_STAGES2.includes(blocksFlag)) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: `\`--blocks\` must be one of: ${BLOCKING_STAGES2.join(", ")}`
        },
        1
      );
      output_manager_default.error(
        `\`--blocks\` must be one of: ${BLOCKING_STAGES2.join(", ")}`
      );
      return 1;
    }
  }
  let result;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project checks",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const query = new URLSearchParams();
    if (typeof blocksFlag === "string") {
      query.set("blocks", blocksFlag);
    }
    const qs = query.toString();
    result = await client.fetch(
      `/v2/projects/${encodeURIComponent(project.id)}/checks${qs ? `?${qs}` : ""}`
    );
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "checks" });
    printError(err);
    return 1;
  }
  const list15 = result.checks ?? [];
  if (asJson) {
    if (client.nonInteractive) {
      const payload = {
        status: AGENT_STATUS.OK,
        checks: list15,
        pagination: result.pagination,
        message: list15.length === 0 ? "No checks configured for this project." : `${list15.length} deployment check(s) configured.`,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project checks add <name>"
            ),
            when: "Add a deployment check (replace <name> or omit if linked)"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project checks remove <check_id>"
            ),
            when: "Remove a check after replacing <check_id> with an id from checks"
          }
        ]
      };
      client.stdout.write(`${JSON.stringify(payload, null, 2)}
`);
    } else {
      client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
    }
    return 0;
  }
  if (list15.length === 0) {
    output_manager_default.log("No checks configured for this project.");
    return 0;
  }
  const rows = [
    ["ID", "Name", "Blocking", "Target"].map((h) => (0, import_chalk153.gray)(h)),
    ...list15.map((c) => [
      String(c.id ?? c.checkId ?? ""),
      String(c.name ?? ""),
      String(c.blocks ?? ""),
      String(
        Array.isArray(c.environments) ? c.environments.join(",") : c.target ?? ""
      )
    ])
  ];
  client.stderr.write(`${table(rows, { hsep: 3 })}
`);
  return 0;
}

// src/commands/project/inspect.ts
var import_chalk154 = __toESM(require_source(), 1);
var import_frameworks = __toESM(require_frameworks(), 1);

// src/util/telemetry/commands/project/inspect.ts
var ProjectInspectTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(value) {
    if (value) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/project/inspect.ts
async function inspect5(client, argv) {
  const telemetry2 = new ProjectInspectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(inspectSubcommand4.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  const name = args[0];
  telemetry2.trackCliArgumentName(name);
  telemetry2.trackCliFlagYes(parsedArgs.flags["--yes"]);
  if (args.length !== 0 && args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk154.default.cyan(
        `${getCommandName("project inspect <name>")}`
      )}`
    );
    return 2;
  }
  const inspectStamp = stamp_default();
  const project = await getProjectByCwdOrLink({
    autoConfirm: parsedArgs.flags["--yes"],
    client,
    commandName: "project inspect",
    projectNameOrId: name
  });
  const org = await getTeamById(client, project.accountId);
  const projectSlugLink = formatProject(org.slug, project.name);
  output_manager_default.log(`Found Project ${projectSlugLink} ${import_chalk154.default.gray(inspectStamp())}`);
  output_manager_default.print("\n");
  output_manager_default.print(import_chalk154.default.bold("  General\n\n"));
  output_manager_default.print(`    ${import_chalk154.default.cyan("ID")}				${project.id}
`);
  output_manager_default.print(`    ${import_chalk154.default.cyan("Name")}			${project.name}
`);
  output_manager_default.print(`    ${import_chalk154.default.cyan("Owner")}			${org.name}
`);
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Created At")}			${formatDate(project.createdAt)}
`
  );
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Root Directory")}		${project.rootDirectory ?? "."}
`
  );
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Node.js Version")}		${project.nodeVersion}
`
  );
  const framework = import_frameworks.frameworkList.find((f) => f.slug === project.framework);
  output_manager_default.print("\n");
  output_manager_default.print(import_chalk154.default.bold("  Framework Settings\n\n"));
  output_manager_default.print(`    ${import_chalk154.default.cyan("Framework Preset")}		${framework?.name}
`);
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Build Command")}		${project.buildCommand ?? import_chalk154.default.dim(framework?.settings?.buildCommand.placeholder ?? "None")}
`
  );
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Output Directory")}		${project.outputDirectory ?? import_chalk154.default.dim(framework?.settings?.outputDirectory.placeholder ?? "None")}
`
  );
  output_manager_default.print(
    `    ${import_chalk154.default.cyan("Install Command")}		${project.installCommand ?? import_chalk154.default.dim(framework?.settings?.installCommand.placeholder ?? "None")}
`
  );
  output_manager_default.print("\n");
  return 0;
}

// src/commands/project/list.ts
var import_ms20 = __toESM(require_ms(), 1);
var import_chalk155 = __toESM(require_source(), 1);

// src/util/telemetry/commands/project/list.ts
var ProjectListTelemetryClient = class extends TelemetryClient {
  trackCliFlagUpdateRequired(updateRequired) {
    if (updateRequired) {
      this.trackCliFlag("update-required");
    }
  }
  trackCliOptionNext(next) {
    if (next) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
  trackCliOptionFilter(filter) {
    if (filter) {
      this.trackCliOption({
        option: "filter",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/project/list.ts
var TABLE_HEADERS = [
  "Project Name",
  "Latest Production URL",
  "Updated",
  "Node Version"
];
var PAGINATION_FLAGS_TO_EXCLUDE = [
  "_",
  "--next",
  "-N",
  "-d",
  "-y",
  "--json",
  "--format"
];
var BASE_PROJECTS_URL = "/v9/projects?limit=20";
async function list10(client, argv) {
  const telemetryClient = new ProjectListTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand12.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  if (args.length !== 0) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk155.default.cyan(
        `${getCommandName("project ls")}`
      )}`
    );
    return 2;
  }
  const start = Date.now();
  const { contextName } = await getScope(client, {
    resolveLocalScope: true
  });
  output_manager_default.spinner(`Fetching projects in ${import_chalk155.default.bold(contextName)}`);
  const flags = processFlags(opts, telemetryClient);
  if ("error" in flags) {
    output_manager_default.stopSpinner();
    output_manager_default.error(flags.error);
    return 1;
  }
  const projectsUrl = buildProjectsUrl(flags);
  const {
    projects: projectList,
    pagination
  } = await client.fetch(projectsUrl, {
    method: "GET"
  });
  output_manager_default.stopSpinner();
  const elapsed2 = (0, import_ms20.default)(Date.now() - start);
  if (flags.json) {
    outputJson4(client, projectList, {
      pagination,
      contextName,
      elapsed: elapsed2,
      deprecated: flags.deprecated
    });
  } else {
    outputTable(projectList, {
      contextName,
      elapsed: elapsed2,
      deprecated: flags.deprecated,
      opts,
      pagination
    });
  }
  return 0;
}
function processFlags(opts, telemetryClient) {
  const deprecated = opts["--update-required"] || false;
  const next = opts["--next"];
  const filter = opts["--filter"];
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    return { error: formatResult.error };
  }
  const json = formatResult.jsonOutput;
  telemetryClient.trackCliFlagUpdateRequired(deprecated);
  telemetryClient.trackCliOptionNext(next);
  telemetryClient.trackCliOptionFormat(opts["--format"]);
  telemetryClient.trackCliFlagJson(opts["--json"]);
  telemetryClient.trackCliOptionFilter(filter);
  return { deprecated, next, json, filter };
}
function buildProjectsUrl(flags) {
  let url = BASE_PROJECTS_URL;
  if (flags.deprecated) {
    url += `&deprecated=${flags.deprecated}`;
  }
  if (flags.next) {
    url += `&until=${flags.next}`;
  }
  if (flags.filter) {
    url += `&search=${encodeURIComponent(flags.filter)}`;
  }
  return url;
}
function createProjectJson(project, deprecated) {
  return {
    name: project.name,
    id: project.id,
    latestProductionUrl: getLatestProdUrl(project),
    updatedAt: project.updatedAt,
    nodeVersion: project.nodeVersion ?? null,
    deprecated
  };
}
function outputJson4(client, projectList, metadata) {
  const jsonOutput = {
    projects: projectList.map(
      (project) => createProjectJson(project, metadata.deprecated)
    ),
    pagination: metadata.pagination,
    contextName: metadata.contextName,
    elapsed: metadata.elapsed
  };
  client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
}
function outputTable(projectList, options) {
  const { contextName, elapsed: elapsed2, deprecated, opts, pagination } = options;
  output_manager_default.log(
    `${projectList.length > 0 ? "Projects" : "No projects"} found under ${import_chalk155.default.bold(contextName)} ${deprecated ? "that are using a deprecated Node.js version" : "\b"} ${import_chalk155.default.gray(`[${elapsed2}]`)}`
  );
  if (projectList.length > 0) {
    printProjectsTable(projectList);
    printPaginationInstructions(opts, pagination);
  }
}
function printProjectsTable(projectList) {
  const tablePrint = table(
    [
      TABLE_HEADERS.map((header) => import_chalk155.default.bold(import_chalk155.default.cyan(header))),
      ...projectList.flatMap((project) => [
        [
          import_chalk155.default.bold(project.name),
          getLatestProdUrl(project),
          import_chalk155.default.gray((0, import_ms20.default)(Date.now() - project.updatedAt)),
          project.nodeVersion ?? ""
        ]
      ])
    ],
    { hsep: 3 }
  ).replace(/^/gm, "  ");
  output_manager_default.print(`
${tablePrint}

`);
}
function printPaginationInstructions(opts, pagination) {
  if (pagination && pagination.count === 20) {
    const flags = getCommandFlags(opts, PAGINATION_FLAGS_TO_EXCLUDE);
    const nextCmd = `project ls${flags} --next ${pagination.next}`;
    output_manager_default.log(`To display the next page, run ${getCommandName(nextCmd)}`);
  }
}
function getLatestProdUrl(project) {
  const alias2 = project.targets?.production?.alias?.[0];
  if (alias2)
    return `https://${alias2}`;
  return "--";
}

// src/commands/project/members.ts
var import_chalk156 = __toESM(require_source(), 1);
async function members(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(membersSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project members [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const limit = parsedArgs.flags["--limit"];
  if (typeof limit === "number" && (limit < 1 || limit > 100)) {
    output_manager_default.error("`--limit` must be a number between 1 and 100.");
    return 1;
  }
  let result;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project members",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const query = new URLSearchParams();
    if (parsedArgs.flags["--search"]) {
      query.set("search", String(parsedArgs.flags["--search"]));
    }
    if (typeof limit === "number") {
      query.set("limit", String(limit));
    }
    const qs = query.toString();
    const path3 = `/v1/projects/${encodeURIComponent(project.id)}/members${qs ? `?${qs}` : ""}`;
    result = await client.fetch(path3);
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "members" });
    printError(err);
    return 1;
  }
  const projectMembers = result.members || [];
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify({ members: projectMembers, pagination: result.pagination }, null, 2)}
`
    );
    return 0;
  }
  const rows = [
    ["uid", "Identity", "Role", "Team Role"].map((str) => (0, import_chalk156.gray)(str)),
    ...projectMembers.map((member) => [
      member.uid,
      member.username || member.email || "",
      member.computedProjectRole || member.role || "",
      member.teamRole || ""
    ])
  ];
  client.stderr.write(`${table(rows, { hsep: 3 })}
`);
  return 0;
}

// src/commands/project/access-groups.ts
var import_chalk157 = __toESM(require_source(), 1);
async function accessGroups(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    accessGroupsSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project access-groups [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const limit = parsedArgs.flags["--limit"];
  if (typeof limit === "number" && (limit < 1 || limit > 100)) {
    output_manager_default.error("`--limit` must be a number between 1 and 100.");
    return 1;
  }
  let result;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project access-groups",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const query = new URLSearchParams();
    query.set("projectId", project.id);
    if (parsedArgs.flags["--search"]) {
      query.set("search", String(parsedArgs.flags["--search"]));
    }
    if (typeof limit === "number") {
      query.set("limit", String(limit));
    }
    if (typeof parsedArgs.flags["--next"] === "number") {
      query.set("next", String(parsedArgs.flags["--next"]));
    }
    result = await client.fetch(
      `/v1/access-groups?${query.toString()}`
    );
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, { variant: "access-groups" });
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
    return 0;
  }
  const rows = [
    ["id", "Name", "Role"].map((str) => (0, import_chalk157.gray)(str)),
    ...(result.accessGroups || []).map((group) => [
      group.id,
      group.name,
      group.role || ""
    ])
  ];
  client.stderr.write(`${table(rows, { hsep: 3 })}
`);
  return 0;
}

// src/commands/project/rename.ts
var import_chalk158 = __toESM(require_source(), 1);
var import_ms21 = __toESM(require_ms(), 1);

// src/util/telemetry/commands/project/rename.ts
var ProjectRenameTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(name) {
    if (name) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliArgumentNewName(newName) {
    if (newName) {
      this.trackCliArgument({
        arg: "new-name",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/project/rename.ts
async function rename(client, argv) {
  const telemetryClient = new ProjectRenameTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(renameSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  if (args.length !== 2) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk158.default.cyan(
        `${getCommandName("project rename <name> <new-name>")}`
      )}`
    );
    return 2;
  }
  const [projectNameOrId, newName] = args;
  telemetryClient.trackCliArgumentName(projectNameOrId);
  telemetryClient.trackCliArgumentNewName(newName);
  const project = await getProjectByNameOrId(client, projectNameOrId);
  if (project instanceof ProjectNotFound) {
    output_manager_default.error("No such project exists");
    return 1;
  }
  const start = Date.now();
  let renamedProject;
  try {
    renamedProject = await client.fetch(
      `/v9/projects/${encodeURIComponent(project.id)}`,
      {
        method: "PATCH",
        body: { name: newName }
      }
    );
  } catch (err) {
    if (isAPIError(err) && err.status === 409) {
      output_manager_default.error(`A project named "${newName}" already exists`);
      return 1;
    }
    if (isAPIError(err) && err.status === 403) {
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
  const elapsed2 = (0, import_ms21.default)(Date.now() - start);
  output_manager_default.log(
    `${import_chalk158.default.cyan("Success!")} Project ${import_chalk158.default.bold(
      project.name
    )} renamed to ${import_chalk158.default.bold(renamedProject.name)} ${import_chalk158.default.gray(
      `[${elapsed2}]`
    )}`
  );
  return 0;
}

// src/commands/project/rm.ts
var import_chalk159 = __toESM(require_source(), 1);
var import_ms22 = __toESM(require_ms(), 1);

// src/util/telemetry/commands/project/rm.ts
var ProjectRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(name) {
    if (name) {
      this.trackCliArgument({
        arg: "name",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/project/rm.ts
var e = encodeURIComponent;
async function rm8(client, argv) {
  const telemetryClient = new ProjectRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand12.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk159.default.cyan(
        `${getCommandName("project rm <name>")}`
      )}`
    );
    return 1;
  }
  const name = args[0];
  telemetryClient.trackCliArgumentName(name);
  const start = Date.now();
  const yes = await readConfirmation3(client, name);
  if (!yes) {
    output_manager_default.log("User abort");
    return 0;
  }
  try {
    await client.fetch(`/v2/projects/${e(name)}`, {
      method: "DELETE"
    });
  } catch (err) {
    if (isAPIError(err) && err.status === 404) {
      output_manager_default.error("No such project exists");
      return 1;
    }
    if (isAPIError(err) && err.status === 403) {
      output_manager_default.error(err.message);
      return 1;
    }
  }
  const elapsed2 = (0, import_ms22.default)(Date.now() - start);
  output_manager_default.log(
    `${import_chalk159.default.cyan("Success!")} Project ${import_chalk159.default.bold(name)} removed ${import_chalk159.default.gray(
      `[${elapsed2}]`
    )}`
  );
  return 0;
}
async function readConfirmation3(client, projectName) {
  output_manager_default.print(
    prependEmoji(
      `The project ${import_chalk159.default.bold(projectName)} will be removed permanently.
It will also delete everything under the project including deployments.
`,
      emoji("warning")
    )
  );
  return await client.input.confirm(
    `${import_chalk159.default.bold.red("Are you sure?")}`,
    false
  );
}

// src/commands/project/token.ts
var import_chalk160 = __toESM(require_source(), 1);
async function getOidcToken(client, argv) {
  const flagsSpecification = getFlagsSpecification(tokenSubcommand2.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags } = parsedArgs;
  const formatResult = validateJsonOutput(flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  client.nonInteractive = true;
  if (args.length > 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk160.default.cyan(
        `${getCommandName("project token <name>")}`
      )}`
    );
    return 1;
  }
  const [name] = args;
  const project = await getProjectByCwdOrLink({
    autoConfirm: Boolean(flags["--yes"]),
    nonInteractive: true,
    client,
    commandName: "project token",
    projectNameOrId: name
  });
  try {
    const res = await client.fetch(
      `/projects/${project.id}/token`,
      {
        method: "POST",
        accountId: project.accountId,
        body: JSON.stringify({
          source: "vercel-cli"
        }),
        headers: {
          "Content-Type": "application/json"
        }
      }
    );
    if (asJson) {
      client.stdout.write(`${JSON.stringify({ token: res.token }, null, 2)}
`);
    } else {
      client.stdout.write(`${res.token}
`);
    }
    return 0;
  } catch (err) {
    if (isAPIError(err) && err.status === 404) {
      output_manager_default.error("No such project exists");
      return 1;
    }
    if (isAPIError(err)) {
      output_manager_default.error(err.message);
      return 1;
    }
    output_manager_default.error(`An unexpected error occurred!
${err}`);
    return 1;
  }
}

// src/commands/project/speed-insights.ts
async function speedInsights(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    speedInsightsSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project speed-insights [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project speed-insights",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const query = new URLSearchParams({ projectId: project.id });
    const result = await client.fetch(
      `/speed-insights/toggle?${query.toString()}`,
      {
        method: "POST",
        json: true,
        body: { value: true }
      }
    );
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            enabled: result.value,
            projectId: project.id,
            projectName: project.name
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(`Speed Insights is enabled for ${project.name}.`);
    return 0;
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, {
      variant: "speed-insights"
    });
    printError(err);
    return 1;
  }
}

// src/commands/project/web-analytics.ts
async function webAnalytics(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    webAnalyticsSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel project web-analytics [name]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  try {
    const project = await getProjectByCwdOrLink({
      client,
      commandName: "project web-analytics",
      projectNameOrId: parsedArgs.args[0],
      forReadOnlyCommand: true
    });
    const query = new URLSearchParams({ projectId: project.id });
    const result = await client.fetch(
      `/web/insights/toggle?${query.toString()}`,
      {
        method: "POST",
        json: true,
        body: { value: true }
      }
    );
    if (asJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            enabled: result.value,
            projectId: project.id,
            projectName: project.name
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(`Web Analytics is enabled for ${project.name}.`);
    return 0;
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, {
      variant: "web-analytics"
    });
    printError(err);
    return 1;
  }
}

// src/commands/project/protection.ts
var import_chalk161 = __toESM(require_source(), 1);
var PROTECTION_ACTIONS = ["enable", "disable"];
var DEFAULT_SKEW_PROTECTION_MAX_AGE = 2592e3;
var ENABLED_DEPLOYMENT_TYPE = "prod_deployment_urls_and_all_previews";
function parseSkewMaxAgeSeconds(value) {
  const trimmed = value.trim();
  if (trimmed === "" || !/^\d+$/.test(trimmed)) {
    return {
      ok: false,
      message: "Invalid --skew-max-age: expected a positive integer (seconds), e.g. 604800."
    };
  }
  const n = Number(trimmed);
  if (!Number.isSafeInteger(n) || n <= 0) {
    return {
      ok: false,
      message: "Invalid --skew-max-age: value must be a positive integer within safe range."
    };
  }
  return { ok: true, seconds: n };
}
var PROTECTION_KEYS = [
  "passwordProtection",
  "ssoProtection",
  "skewProtectionMaxAge",
  "customerSupportCodeVisibility",
  "gitForkProtection",
  "protectionBypass"
];
function isProtectionAction(v) {
  return !!v && PROTECTION_ACTIONS.includes(v);
}
async function protection(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(
    protectionSubcommand.options
  );
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: error instanceof Error ? error.message : String(error)
      },
      1
    );
    printError(error);
    return 1;
  }
  const actionArg = parsedArgs.args[0];
  const action = isProtectionAction(actionArg) ? actionArg : void 0;
  if (!action && parsedArgs.args.length > 1) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: "Invalid arguments. Usage: `vercel project protection [name]`",
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "project protection"
            ),
            when: "Show deployment protection for the linked project"
          }
        ]
      },
      2
    );
    output_manager_default.error(
      "Invalid arguments. Usage: `vercel project protection [name]`"
    );
    return 2;
  }
  if (action && parsedArgs.args.length > 2) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: `Invalid arguments. Usage: \`vercel project protection ${action} [name]\``,
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action}`
            ),
            when: "Retry with at most one project name"
          }
        ]
      },
      2
    );
    output_manager_default.error(
      `Invalid arguments. Usage: \`vercel project protection ${action} [name]\``
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: formatResult.error
      },
      1
    );
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const preferJson = formatResult.jsonOutput || Boolean(client.nonInteractive);
  const skewMaxAgeFlag = parsedArgs.flags["--skew-max-age"];
  if (action === "disable" && skewMaxAgeFlag !== void 0) {
    const msg = "`--skew-max-age` can only be used with `project protection enable`.";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg,
        hint: "Use `project protection disable ... --skew` to turn skew protection off."
      },
      2
    );
    output_manager_default.error(msg);
    return 2;
  }
  let enableSkewMaxAgeSeconds;
  if (action === "enable" && skewMaxAgeFlag !== void 0) {
    const parsed = parseSkewMaxAgeSeconds(skewMaxAgeFlag);
    if (!parsed.ok) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: parsed.message,
          hint: "Pass a positive integer number of seconds (e.g. 604800 for 7 days).",
          next: [
            {
              command: buildCommandWithGlobalFlags(
                client.argv,
                "project protection enable --skew --skew-max-age 604800"
              ),
              when: "Example with a valid max age in seconds"
            }
          ]
        },
        1
      );
      output_manager_default.error(parsed.message);
      return 1;
    }
    enableSkewMaxAgeSeconds = parsed.seconds;
  }
  const ssoSelected = Boolean(parsedArgs.flags["--sso"]);
  const passwordSelected = Boolean(parsedArgs.flags["--password"]);
  const customerSupportCodeVisibilitySelected = Boolean(
    parsedArgs.flags["--customer-support-code-visibility"]
  );
  const skewSelected = Boolean(parsedArgs.flags["--skew"]);
  const bypassSelected = Boolean(parsedArgs.flags["--protection-bypass"]);
  const protectionBypassSecret = parsedArgs.flags["--protection-bypass-secret"];
  const gitForkProtectionSelected = Boolean(
    parsedArgs.flags["--git-fork-protection"]
  );
  if (action && !ssoSelected && !passwordSelected && !customerSupportCodeVisibilitySelected && !skewSelected && !bypassSelected && !gitForkProtectionSelected) {
    const msg = "No protection selected. Pass --sso, --password, --customer-support-code-visibility, --skew, --protection-bypass, or --git-fork-protection. Usage: `vercel project protection enable|disable [name] --sso|--password|--customer-support-code-visibility|--skew|--protection-bypass|--git-fork-protection`";
    outputAgentError(
      client,
      {
        status: "error",
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message: msg,
        hint: "Use `project protection enable|disable` with a protection flag (e.g. --sso, --password, --customer-support-code-visibility, --skew, --protection-bypass, or --git-fork-protection).",
        next: [
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --sso`
            ),
            when: "Example: same action with SSO protection selected"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --password`
            ),
            when: "Example: same action with password protection selected"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --customer-support-code-visibility`
            ),
            when: "Example: same action with customer support code visibility selected"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --skew`
            ),
            when: "Example: same action with skew protection selected"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --protection-bypass`
            ),
            when: "Example: same action with automation protection bypass"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              `project protection ${action} --git-fork-protection`
            ),
            when: "Example: same action with Git fork protection selected"
          }
        ]
      },
      2
    );
    output_manager_default.error(msg);
    return 2;
  }
  let project;
  try {
    project = await getProjectByCwdOrLink({
      client,
      commandName: "project protection",
      projectNameOrId: action ? parsedArgs.args[1] : parsedArgs.args[0],
      forReadOnlyCommand: !action
    });
  } catch (err) {
    exitWithNonInteractiveError(client, err, 1, {
      variant: "protection"
    });
    printError(err);
    return 1;
  }
  if (action) {
    const skewProtectionMaxAge = action === "enable" ? enableSkewMaxAgeSeconds ?? DEFAULT_SKEW_PROTECTION_MAX_AGE : 0;
    if (bypassSelected) {
      if (action === "disable" && !protectionBypassSecret) {
        const secretMsg = "Disabling protection bypass requires --protection-bypass-secret <secret>.";
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.MISSING_ARGUMENTS,
            message: secretMsg,
            hint: "Pass the existing automation bypass secret to revoke it.",
            next: [
              {
                command: buildCommandWithGlobalFlags(
                  client.argv,
                  "project protection disable --protection-bypass --protection-bypass-secret <secret>"
                ),
                when: "Replace <secret> with the bypass secret to revoke"
              }
            ]
          },
          2
        );
        output_manager_default.error(secretMsg);
        return 2;
      }
      try {
        const bypassBody = action === "enable" ? {
          generate: protectionBypassSecret ? { secret: protectionBypassSecret } : {}
        } : {
          revoke: {
            secret: protectionBypassSecret,
            regenerate: false
          }
        };
        await client.fetch(
          `/v1/projects/${encodeURIComponent(project.id)}/protection-bypass`,
          {
            method: "PATCH",
            body: bypassBody
          }
        );
      } catch (err) {
        exitWithNonInteractiveError(client, err, 1, { variant: "protection" });
        printError(err);
        return 1;
      }
    }
    const patchBody = {};
    if (ssoSelected) {
      patchBody.ssoProtection = action === "enable" ? { deploymentType: ENABLED_DEPLOYMENT_TYPE } : null;
    }
    if (passwordSelected) {
      patchBody.passwordProtection = action === "enable" ? { deploymentType: ENABLED_DEPLOYMENT_TYPE } : null;
    }
    if (customerSupportCodeVisibilitySelected) {
      patchBody.customerSupportCodeVisibility = action === "enable";
    }
    if (skewSelected) {
      patchBody.skewProtectionMaxAge = skewProtectionMaxAge;
    }
    if (gitForkProtectionSelected) {
      patchBody.gitForkProtection = action === "enable";
    }
    if (Object.keys(patchBody).length > 0) {
      try {
        await client.fetch(`/v9/projects/${encodeURIComponent(project.id)}`, {
          method: "PATCH",
          body: patchBody
        });
      } catch (err) {
        exitWithNonInteractiveError(client, err, 1, { variant: "protection" });
        printError(err);
        return 1;
      }
    }
    if (preferJson) {
      client.stdout.write(
        `${JSON.stringify(
          {
            action,
            projectId: project.id,
            projectName: project.name,
            ssoProtection: ssoSelected ? action === "enable" : void 0,
            passwordProtection: passwordSelected ? action === "enable" : void 0,
            customerSupportCodeVisibility: customerSupportCodeVisibilitySelected ? action === "enable" : void 0,
            skewProtection: skewSelected ? action === "enable" : void 0,
            ...skewSelected ? action === "enable" ? { skewProtectionMaxAge } : { skewProtectionMaxAge: 0 } : {},
            protectionBypass: bypassSelected ? action === "enable" : void 0,
            gitForkProtection: gitForkProtectionSelected ? action === "enable" : void 0
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.log(
      `${import_chalk161.default.bold("Deployment protection")} ${action === "enable" ? "enabled" : "disabled"} for ${import_chalk161.default.cyan(project.name)}`
    );
    return 0;
  }
  const raw = project;
  const slice = {};
  for (const key of PROTECTION_KEYS) {
    if (key in raw) {
      slice[key] = raw[key];
    }
  }
  if (preferJson) {
    client.stdout.write(
      `${JSON.stringify({ projectId: project.id, name: project.name, ...slice }, null, 2)}
`
    );
    return 0;
  }
  output_manager_default.log(
    `${import_chalk161.default.bold("Protection settings")} for ${import_chalk161.default.cyan(project.name)} (${project.id})`
  );
  if (Object.keys(slice).length === 0) {
    output_manager_default.log("No deployment protection fields returned for this project.");
    return 0;
  }
  for (const [k, v] of Object.entries(slice)) {
    output_manager_default.log(`${import_chalk161.default.cyan(`${k}:`)} ${JSON.stringify(v)}`);
  }
  return 0;
}

// src/util/telemetry/commands/project/index.ts
var ProjectTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandInspect(actual) {
    this.trackCliSubcommand({
      subcommand: "inspect",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandAccessSummary(actual) {
    this.trackCliSubcommand({
      subcommand: "access-summary",
      value: actual
    });
  }
  trackCliSubcommandChecks(actual) {
    this.trackCliSubcommand({
      subcommand: "checks",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandRename(actual) {
    this.trackCliSubcommand({
      subcommand: "rename",
      value: actual
    });
  }
  trackCliSubcommandToken(actual) {
    this.trackCliSubcommand({
      subcommand: "token",
      value: actual
    });
  }
  trackCliSubcommandMembers(actual) {
    this.trackCliSubcommand({
      subcommand: "members",
      value: actual
    });
  }
  trackCliSubcommandAccessGroups(actual) {
    this.trackCliSubcommand({
      subcommand: "access-groups",
      value: actual
    });
  }
  trackCliSubcommandProtection(actual) {
    this.trackCliSubcommand({
      subcommand: "protection",
      value: actual
    });
  }
  trackCliSubcommandWebAnalytics(actual) {
    this.trackCliSubcommand({
      subcommand: "web-analytics",
      value: actual
    });
  }
  trackCliSubcommandSpeedInsights(actual) {
    this.trackCliSubcommand({
      subcommand: "speed-insights",
      value: actual
    });
  }
};

// src/commands/project/index.ts
var COMMAND_CONFIG32 = {
  inspect: getCommandAliases(inspectSubcommand4),
  list: getCommandAliases(listSubcommand12),
  members: getCommandAliases(membersSubcommand),
  accessGroups: getCommandAliases(accessGroupsSubcommand),
  add: getCommandAliases(addSubcommand7),
  "access-summary": getCommandAliases(accessSummarySubcommand),
  checks: getCommandAliases(checksSubcommand),
  protection: getCommandAliases(protectionSubcommand),
  rename: getCommandAliases(renameSubcommand),
  remove: getCommandAliases(removeSubcommand12),
  token: getCommandAliases(tokenSubcommand2),
  speedInsights: getCommandAliases(speedInsightsSubcommand),
  webAnalytics: getCommandAliases(webAnalyticsSubcommand)
};
async function main25(client) {
  const telemetry2 = new ProjectTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const flagsSpecification = getFlagsSpecification(projectCommand.options);
  let parsedArgs;
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  let { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG32
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("project");
    output_manager_default.print(help(projectCommand, { columns: client.stderr.columns }));
    return 0;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: projectCommand, columns: client.stderr.columns })
    );
    return 0;
  }
  if (!parsedArgs.args[1]) {
    subcommand = "list";
  }
  let exitCode2;
  switch (subcommand) {
    case "inspect":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(inspectSubcommand4);
      }
      telemetry2.trackCliSubcommandInspect(subcommandOriginal);
      exitCode2 = await inspect5(client, args);
      break;
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(listSubcommand12);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      exitCode2 = await list10(client, args);
      break;
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(addSubcommand7);
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      exitCode2 = await add8(client, args);
      break;
    case "access-summary":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(accessSummarySubcommand);
      }
      telemetry2.trackCliSubcommandAccessSummary(subcommandOriginal);
      exitCode2 = await accessSummary(client, args);
      break;
    case "checks":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(checksSubcommand);
      }
      telemetry2.trackCliSubcommandChecks(
        args[0] === "add" ? "checks add" : args[0] === "remove" || args[0] === "rm" ? "checks remove" : subcommandOriginal
      );
      exitCode2 = await checks(client, args);
      break;
    case "members":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(membersSubcommand);
      }
      telemetry2.trackCliSubcommandMembers(subcommandOriginal);
      exitCode2 = await members(client, args);
      break;
    case "accessGroups":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(accessGroupsSubcommand);
      }
      telemetry2.trackCliSubcommandAccessGroups(subcommandOriginal);
      exitCode2 = await accessGroups(client, args);
      break;
    case "protection":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(protectionSubcommand);
      }
      telemetry2.trackCliSubcommandProtection(
        args[0] === "enable" ? "protection enable" : args[0] === "disable" ? "protection disable" : subcommandOriginal
      );
      exitCode2 = await protection(client, args);
      break;
    case "webAnalytics":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(webAnalyticsSubcommand);
      }
      telemetry2.trackCliSubcommandWebAnalytics(subcommandOriginal);
      exitCode2 = await webAnalytics(client, args);
      break;
    case "speedInsights":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(speedInsightsSubcommand);
      }
      telemetry2.trackCliSubcommandSpeedInsights(subcommandOriginal);
      exitCode2 = await speedInsights(client, args);
      break;
    case "token":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(tokenSubcommand2);
      }
      telemetry2.trackCliSubcommandToken(subcommandOriginal);
      exitCode2 = await getOidcToken(client, args);
      break;
    case "rename":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(renameSubcommand);
      }
      telemetry2.trackCliSubcommandRename(subcommandOriginal);
      exitCode2 = await rename(client, args);
      break;
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("project", subcommandOriginal);
        return printHelp(removeSubcommand12);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      exitCode2 = await rm8(client, args);
      break;
    default: {
      const fallback = await tryOpenApiFallback(
        client,
        parsedArgs.args.slice(1),
        resolveOpenApiTagForProjectsCli
      );
      if (fallback !== null) {
        exitCode2 = fallback;
        break;
      }
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG32));
      output_manager_default.print(help(projectCommand, { columns: client.stderr.columns }));
      return 2;
    }
  }
  if (exitCode2 === 0) {
    await autoInstallVercelPlugin(client);
  }
  return exitCode2;
}

// src/commands/promote/index.ts
var import_ms25 = __toESM(require_ms(), 1);
var import_error_utils15 = __toESM(require_dist(), 1);

// src/commands/promote/request-promote.ts
var import_chalk165 = __toESM(require_source(), 1);

// src/util/projects/get-project-by-deployment.ts
var import_chalk162 = __toESM(require_source(), 1);
async function getProjectByDeployment({
  client,
  deployId
}) {
  const { config } = client;
  const { contextName } = await getScope(client);
  if (!isValidName(deployId)) {
    throw new Error(
      `The provided argument "${deployId}" is not a valid deployment ID or URL`
    );
  }
  let deployment;
  let team;
  try {
    output_manager_default?.spinner(
      `Fetching deployment "${deployId}" in ${import_chalk162.default.bold(contextName)}\u2026`
    );
    const [teamResult, deploymentResult] = await Promise.allSettled([
      config.currentTeam ? getTeamById(client, config.currentTeam) : void 0,
      getDeployment(client, contextName, deployId)
    ]);
    if (teamResult.status === "rejected") {
      throw new Error(
        `Failed to retrieve team information: ${teamResult.reason}`
      );
    }
    if (deploymentResult.status === "rejected") {
      throw new Error(deploymentResult.reason);
    }
    team = teamResult.value;
    deployment = deploymentResult.value;
    output_manager_default?.log(
      `Fetching deployment "${deployId}" in ${import_chalk162.default.bold(contextName)}\u2026`
    );
    if (deployment.team?.id) {
      if (!team || deployment.team.id !== team.id) {
        const err = new Error(
          team ? `Deployment doesn't belong to current team ${import_chalk162.default.bold(
            contextName
          )}` : `Deployment belongs to a different team`
        );
        err.code = "ERR_INVALID_TEAM";
        throw err;
      }
    } else if (team) {
      const err = new Error(
        `Deployment doesn't belong to current team ${import_chalk162.default.bold(contextName)}`
      );
      err.code = "ERR_INVALID_TEAM";
      throw err;
    }
    if (!deployment.projectId) {
      throw new Error("Deployment is not associated to a project");
    }
    const project = await getProjectByNameOrId(client, deployment.projectId);
    if (project instanceof ProjectNotFound) {
      throw project;
    }
    return {
      contextName,
      deployment,
      project
    };
  } finally {
    output_manager_default?.stopSpinner();
  }
}

// src/commands/promote/request-promote.ts
var import_ms24 = __toESM(require_ms(), 1);

// src/commands/promote/status.ts
var import_chalk164 = __toESM(require_source(), 1);
var import_ms23 = __toESM(require_ms(), 1);

// src/util/alias/render-alias-status.ts
var import_chalk163 = __toESM(require_source(), 1);
function renderAliasStatus(status3) {
  if (status3 === "completed") {
    return import_chalk163.default.green(status3);
  }
  if (status3 === "failed") {
    return import_chalk163.default.red(status3);
  }
  if (status3 === "skipped") {
    return import_chalk163.default.gray(status3);
  }
  return import_chalk163.default.yellow(status3);
}

// src/commands/rolling-release/request-rolling-release.ts
async function requestRollingRelease({
  client,
  projectId,
  teamId
}) {
  const { rollingRelease: rollingRelease2 } = await client.fetch(
    `/v1/projects/${projectId}/rolling-release?teamId=${teamId}`
  );
  return rollingRelease2;
}

// src/commands/promote/status.ts
async function promoteStatus({
  client,
  contextName,
  deployment,
  project,
  timeout = "3m"
}) {
  const recentThreshold = Date.now() - (0, import_ms23.default)("3m");
  const promoteTimeout = Date.now() + (0, import_ms23.default)(timeout);
  let counter = 0;
  let spinnerMessage = deployment ? "Promote in progress" : `Checking promotion status of ${project.name}`;
  if (!contextName) {
    ({ contextName } = await getScope(client));
  }
  try {
    output_manager_default.spinner(`${spinnerMessage}\u2026`);
    for (; ; ) {
      const projectCheck = await getProjectByNameOrId(
        client,
        project.id,
        project.accountId,
        true
      );
      if (projectCheck instanceof ProjectNotFound) {
        throw projectCheck;
      }
      if (projectCheck.rollingRelease) {
        output_manager_default.log(`Rolling Releases enabled \u2026`);
        const rr = await requestRollingRelease({
          client,
          projectId: project.id,
          teamId: project.accountId
        });
        if (rr.activeStage) {
          output_manager_default.stopSpinner();
          output_manager_default.log(
            `Rolling Release serving traffic at initial percentage ${rr.activeStage.targetPercentage}`
          );
          return 0;
        }
        continue;
      }
      const {
        jobStatus,
        requestedAt,
        toDeploymentId,
        type
      } = projectCheck.lastAliasRequest ?? {};
      if (!jobStatus || jobStatus !== "in-progress" && jobStatus !== "pending") {
        output_manager_default.stopSpinner();
        output_manager_default.log(`${spinnerMessage}\u2026`);
      }
      if (!jobStatus || !requestedAt || !toDeploymentId || requestedAt < recentThreshold) {
        output_manager_default.log("No deployment promotion in progress");
        return 0;
      }
      if (jobStatus === "skipped" && type === "promote") {
        output_manager_default.log("Promote deployment was skipped");
        return 0;
      }
      if (jobStatus === "succeeded") {
        return await renderJobSucceeded({
          client,
          contextName,
          performingPromote: !!deployment,
          requestedAt,
          project,
          toDeploymentId
        });
      }
      if (jobStatus === "failed") {
        return await renderJobFailed({
          client,
          contextName,
          deployment,
          project,
          toDeploymentId
        });
      }
      if (jobStatus !== "pending" && jobStatus !== "in-progress") {
        output_manager_default.log(`Unknown promote deployment status "${jobStatus}"`);
        return 1;
      }
      if (requestedAt < recentThreshold || Date.now() >= promoteTimeout) {
        output_manager_default.log(
          `The promotion exceeded its deadline - rerun ${import_chalk164.default.bold(
            `${packageName} promote ${toDeploymentId}`
          )} to try again`
        );
        return 1;
      }
      if (counter++ === 0 && !deployment) {
        spinnerMessage += ` requested at ${formatDate(requestedAt)}`;
      }
      output_manager_default.spinner(`${spinnerMessage}\u2026`);
      await sleep(250);
    }
  } finally {
    output_manager_default.stopSpinner();
  }
}
async function renderJobFailed({
  client,
  contextName,
  deployment,
  project,
  toDeploymentId
}) {
  try {
    const name = (deployment || await getDeployment(client, contextName, toDeploymentId))?.url;
    output_manager_default.error(
      `Failed to remap all aliases to the requested deployment ${name} (${toDeploymentId})`
    );
  } catch (_e) {
    output_manager_default.error(
      `Failed to remap all aliases to the requested deployment ${toDeploymentId}`
    );
  }
  let nextTimestamp;
  for (; ; ) {
    let url = `/v9/projects/${project.id}/promote/aliases?failedOnly=true&limit=20`;
    if (nextTimestamp) {
      url += `&until=${nextTimestamp}`;
    }
    const { aliases, pagination } = await client.fetch(url);
    for (const { alias: alias2, status: status3 } of aliases) {
      output_manager_default.log(
        `  ${renderAliasStatus(status3).padEnd(11)}  ${alias2.alias} (${alias2.deploymentId})`
      );
    }
    if (pagination?.next) {
      nextTimestamp = pagination.next;
    } else {
      break;
    }
  }
  return 1;
}
async function renderJobSucceeded({
  client,
  contextName,
  performingPromote,
  project,
  requestedAt,
  toDeploymentId
}) {
  let deploymentInfo = "";
  try {
    const deployment = await getDeployment(client, contextName, toDeploymentId);
    deploymentInfo = `${import_chalk164.default.bold(deployment.url)} (${toDeploymentId})`;
  } catch (err) {
    output_manager_default.debug(
      `Failed to get deployment url for ${toDeploymentId}: ${err?.toString() || err}`
    );
    deploymentInfo = import_chalk164.default.bold(toDeploymentId);
  }
  const duration = performingPromote ? elapsed(Date.now() - requestedAt) : "";
  output_manager_default.log(
    `Success! ${import_chalk164.default.bold(
      project.name
    )} was promoted to ${deploymentInfo} ${duration}`
  );
  return 0;
}

// src/commands/promote/request-promote.ts
async function requestPromote({
  client,
  deployId,
  timeout,
  yes
}) {
  const { contextName, deployment, project } = await getProjectByDeployment({
    client,
    deployId
  });
  let promoteByCreation = false;
  if (deployment.target !== "production") {
    if (yes) {
      promoteByCreation = true;
    } else {
      const question = "This deployment is not a production deployment and cannot be directly promoted. A new deployment will be built using your production environment. Are you sure you want to continue?";
      promoteByCreation = await client.input.confirm(question, false);
      if (!promoteByCreation) {
        output_manager_default.error("Canceled");
        return 0;
      }
    }
  }
  if (promoteByCreation) {
    const newDeployment = await client.fetch(
      `/v13/deployments?teamId=${deployment.ownerId}`,
      {
        body: {
          deploymentId: deployment.id,
          name: project.name,
          target: "production",
          meta: {
            action: "promote"
          }
        },
        accountId: deployment.ownerId,
        method: "POST"
      }
    );
    output_manager_default.log(
      `Successfully created new deployment of ${import_chalk165.default.bold(project.name)} at ${newDeployment.inspectorUrl}`
    );
    return 0;
  }
  const res = await client.fetch(
    `/v10/projects/${project.id}/promote/${deployment.id}`,
    {
      body: {},
      // required
      json: false,
      method: "POST"
    }
  );
  if (res.status === 202) {
    output_manager_default.log(
      "Promotion has been queued and will begin when the active rolling release completes successfully."
    );
    return 0;
  }
  if (timeout !== void 0 && (0, import_ms24.default)(timeout) === 0) {
    output_manager_default.log(
      `Successfully requested promote of ${import_chalk165.default.bold(project.name)} to ${deployment.url} (${deployment.id})`
    );
    output_manager_default.log(`To check promote status, run ${getCommandName("promote")}.`);
    return 0;
  }
  return await promoteStatus({
    client,
    contextName,
    deployment,
    project,
    timeout
  });
}

// src/util/telemetry/commands/promote/index.ts
var PromoteTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrDeploymentId(value) {
    if (value) {
      this.trackCliArgument({
        arg: "urlOrDeploymentId",
        value: this.redactedValue
      });
    }
  }
  trackCliSubcommandStatus() {
    this.trackCliSubcommand({
      subcommand: "status",
      value: "status"
    });
  }
  trackCliOptionTimeout(time) {
    if (time) {
      this.trackCliOption({
        option: "timeout",
        value: "[TIME]"
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/promote/index.ts
var promote_default = async (client) => {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(promoteCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const telemetry2 = new PromoteTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const needHelp = parsedArgs.flags["--help"];
  if (!parsedArgs.args[1] && needHelp) {
    telemetry2.trackCliFlagHelp("promote");
    output_manager_default.print(help(promoteCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const yes = parsedArgs.flags["--yes"] ?? false;
  telemetry2.trackCliFlagYes(parsedArgs.flags["--yes"]);
  const timeout = parsedArgs.flags["--timeout"];
  if (timeout && (0, import_ms25.default)(timeout) === void 0) {
    output_manager_default.error(`Invalid timeout "${timeout}"`);
    return 1;
  }
  telemetry2.trackCliOptionTimeout(parsedArgs.flags["--timeout"]);
  const actionOrDeployId = parsedArgs.args[1] || "status";
  try {
    if (actionOrDeployId === "status") {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("promote", "status");
        output_manager_default.print(
          help(statusSubcommand2, {
            columns: client.stderr.columns,
            parent: promoteCommand
          })
        );
        return 2;
      }
      telemetry2.trackCliSubcommandStatus();
      const project = await getProjectByCwdOrLink({
        autoConfirm: parsedArgs.flags["--yes"],
        client,
        commandName: "promote",
        projectNameOrId: parsedArgs.args[2]
      });
      return await promoteStatus({
        client,
        project,
        timeout
      });
    }
    telemetry2.trackCliArgumentUrlOrDeploymentId(actionOrDeployId);
    return await requestPromote({
      client,
      deployId: actionOrDeployId,
      timeout,
      yes
    });
  } catch (err) {
    if ((0, import_error_utils15.isErrnoException)(err)) {
      if (err.code === "ERR_CANCELED") {
        return 0;
      }
      if (err.code === "ERR_INVALID_CWD" || err.code === "ERR_LINK_PROJECT") {
        return 1;
      }
    }
    output_manager_default.prettyError(err);
    return 1;
  }
};

// src/commands/redeploy/index.ts
var import_chalk167 = __toESM(require_source(), 1);
var import_client = __toESM(require_dist2(), 1);

// src/util/deploy/get-deployment-by-id-or-url.ts
var import_chalk166 = __toESM(require_source(), 1);
async function getDeploymentByIdOrURL({
  client,
  contextName,
  deployIdOrUrl
}) {
  const { config } = client;
  if (!isValidName(deployIdOrUrl)) {
    throw new Error(
      `The provided argument "${deployIdOrUrl}" is not a valid deployment ID or URL`
    );
  }
  let deployment;
  let team;
  try {
    output_manager_default.spinner(
      `Fetching deployment "${deployIdOrUrl}" in ${import_chalk166.default.bold(contextName)}\u2026`
    );
    const [teamResult, deploymentResult] = await Promise.allSettled([
      config.currentTeam ? getTeamById(client, config.currentTeam) : void 0,
      getDeployment(client, contextName, deployIdOrUrl)
    ]);
    if (teamResult.status === "rejected") {
      throw new Error(
        `Failed to retrieve team information: ${teamResult.reason}`
      );
    }
    if (deploymentResult.status === "rejected") {
      throw new Error(deploymentResult.reason.message);
    }
    team = teamResult.value;
    deployment = deploymentResult.value;
    output_manager_default.log(
      `Fetching deployment "${deployIdOrUrl}" in ${import_chalk166.default.bold(contextName)}\u2026`
    );
  } finally {
    output_manager_default.stopSpinner();
  }
  if (deployment.team?.id) {
    if (!team || deployment.team.id !== team.id) {
      const err = new Error(
        team ? `Deployment doesn't belong to current team ${import_chalk166.default.bold(
          contextName
        )}` : `Deployment belongs to a different team`
      );
      err.code = "ERR_INVALID_TEAM";
      throw err;
    }
  } else if (team) {
    const err = new Error(
      `Deployment doesn't belong to current team ${import_chalk166.default.bold(contextName)}`
    );
    err.code = "ERR_INVALID_TEAM";
    throw err;
  }
  return deployment;
}

// src/commands/redeploy/index.ts
var import_error_utils16 = __toESM(require_dist(), 1);

// src/util/telemetry/commands/redeploy/index.ts
var RedeployTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrDeploymentId(value) {
    if (value) {
      this.trackCliArgument({
        arg: "urlOrDeploymentId",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagNoWait(noWait) {
    if (noWait) {
      this.trackCliFlag("no-wait");
    }
  }
  trackCliOptionTarget(target) {
    if (target) {
      this.trackCliArgument({
        arg: "target",
        value: this.redactedTargetName(target)
      });
    }
  }
};

// src/commands/redeploy/index.ts
async function redeploy(client) {
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(redeployCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new RedeployTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("redeploy");
    output_manager_default.print(help(redeployCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const deployIdOrUrl = parsedArgs.args[1];
  if (!deployIdOrUrl) {
    output_manager_default.error(
      `Missing required deployment id or url: ${getCommandName(
        `redeploy <deployment-id-or-url>`
      )}`
    );
    return 1;
  }
  telemetry2.trackCliArgumentUrlOrDeploymentId(deployIdOrUrl);
  telemetry2.trackCliFlagNoWait(parsedArgs.flags["--no-wait"]);
  telemetry2.trackCliOptionTarget(parsedArgs.flags["--target"]);
  const { contextName } = await getScope(client);
  const noWait = !!parsedArgs.flags["--no-wait"];
  const targetArgument = parsedArgs.flags["--target"];
  try {
    const fromDeployment = await getDeploymentByIdOrURL({
      client,
      contextName,
      deployIdOrUrl
    });
    let target;
    let customEnvironmentSlugOrId;
    if (!targetArgument) {
      target = fromDeployment.target ?? void 0;
      customEnvironmentSlugOrId = fromDeployment.customEnvironment?.id;
    } else if (targetArgument === "staging" || targetArgument === "production") {
      target = targetArgument;
    } else if (targetArgument === "preview") {
      target = void 0;
    } else if (targetArgument) {
      customEnvironmentSlugOrId = targetArgument;
      target = void 0;
    } else {
      target = fromDeployment.target;
    }
    let customEnvironment;
    if (fromDeployment?.projectId && customEnvironmentSlugOrId) {
      const customEnvironments = await getCustomEnvironments(
        client,
        fromDeployment.projectId
      );
      customEnvironment = pickCustomEnvironment(
        customEnvironments,
        customEnvironmentSlugOrId
      );
    }
    if (customEnvironmentSlugOrId && !customEnvironment) {
      output_manager_default.error(
        `The provided argument "${targetArgument}" is not a valid target environment.`
      );
      return 1;
    }
    const deployStamp = stamp_default();
    output_manager_default.spinner(`Redeploying project ${fromDeployment.id}`, 0);
    let deployment = await client.fetch(`/v13/deployments?forceNew=1`, {
      body: {
        deploymentId: fromDeployment.id,
        meta: {
          action: "redeploy"
        },
        name: fromDeployment.name,
        target,
        customEnvironmentSlugOrId
      },
      method: "POST"
    });
    output_manager_default.stopSpinner();
    const previewUrl = `https://${deployment.url}`;
    let isProdDeployment = target === "production";
    if (customEnvironmentSlugOrId && customEnvironment) {
      isProdDeployment = customEnvironment.type === "production";
    }
    printAlignedLabel("Inspect", import_chalk167.default.cyan(deployment.inspectorUrl));
    printAlignedLabel(
      isProdDeployment ? "Production" : "Preview",
      import_chalk167.default.cyan(previewUrl),
      isProdDeployment ? { gutter: "\u25B2" } : {}
    );
    if (!client.stdout.isTTY) {
      client.stdout.write(`https://${deployment.url}`);
    }
    if (!noWait) {
      output_manager_default.spinner(
        deployment.readyState === "QUEUED" ? "Queued\u2026" : "Building\u2026",
        0
      );
      let project;
      let rollingRelease2;
      if (deployment.projectId && deployment.projectId != "") {
        project = await getProjectByNameOrId(client, deployment.projectId);
        rollingRelease2 = project?.rollingRelease;
      }
      if (deployment.readyState === "READY" && deployment.aliasAssigned && !rollingRelease2) {
        output_manager_default.spinner("Completing\u2026", 0);
      } else {
        try {
          const clientOptions = {
            agent: client.agent,
            apiUrl: client.apiUrl,
            debug: output_manager_default.debugEnabled,
            path: "",
            // unused by checkDeploymentStatus()
            teamId: fromDeployment.team?.id,
            token: client.authConfig.token,
            userAgent: ua_default
          };
          for await (const event of (0, import_client.checkDeploymentStatus)(
            deployment,
            clientOptions
          )) {
            if (event.type === "building") {
              output_manager_default.spinner("Building\u2026", 0);
            } else if (event.type === "ready" && rollingRelease2) {
              output_manager_default.spinner("Releasing\u2026", 0);
              output_manager_default.stopSpinner();
              deployment = event.payload;
              break;
            } else if (event.type === "ready" && (event.payload.checksState ? event.payload.checksState === "completed" : true)) {
              output_manager_default.spinner("Completing\u2026", 0);
            } else if (event.type === "checks-running") {
              output_manager_default.spinner("Running Checks\u2026", 0);
            } else if (event.type === "alias-assigned" || event.type === "checks-conclusion-failed") {
              output_manager_default.stopSpinner();
              if (event.type === "alias-assigned" && !Array.isArray(event.payload) && event.payload.target === "production" && event.payload.alias && event.payload.alias.length > 0) {
                const primaryDomain = event.payload.alias[0];
                const prodUrl = `https://${primaryDomain}`;
                printAlignedLabel("Aliased", import_chalk167.default.cyan(prodUrl), {
                  gutter: "\u25B2"
                });
              }
              deployment = event.payload;
              break;
            } else if (event.type === "canceled") {
              output_manager_default.stopSpinner();
              output_manager_default.print("The deployment has been canceled.\n");
              return 1;
            } else if (event.type === "error") {
              output_manager_default.stopSpinner();
              const now = new Now({
                client,
                currentTeam: fromDeployment.team?.id
              });
              const error = await now.handleDeploymentError(event.payload, {
                env: {}
              });
              throw error;
            }
          }
        } catch (err) {
          output_manager_default.prettyError(err);
          process.exit(1);
        }
      }
    }
    return printDeploymentStatus(
      client,
      deployment,
      deployStamp,
      noWait,
      false
    );
  } catch (err) {
    output_manager_default.prettyError(err);
    if ((0, import_error_utils16.isErrnoException)(err) && err.code === "ERR_INVALID_TEAM") {
      output_manager_default.error(
        `Use ${import_chalk167.default.bold("vc switch")} to change your current team`
      );
    }
    return 1;
  }
}

// src/commands/redirects/list.ts
var import_chalk168 = __toESM(require_source(), 1);
var import_pluralize9 = __toESM(require_pluralize(), 1);

// src/commands/redirects/shared.ts
async function parseSubcommandArgs3(argv, command) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(command.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  return parsedArgs;
}
function validateRequiredArgs(args, required) {
  for (let i = 0; i < required.length; i++) {
    if (!args[i]) {
      return `Missing required argument: ${required[i]}`;
    }
  }
  return null;
}
async function ensureProjectLink3(client) {
  const link = await getLinkedProject(client);
  if (link.status === "error") {
    return link.exitCode;
  } else if (link.status === "not_linked") {
    if (client.nonInteractive) {
      const linkCmd = getCommandNamePlain("link");
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          message: `Your codebase isn't linked to a project on Vercel. Run ${linkCmd} to begin.`,
          next: [{ command: linkCmd }]
        },
        1
      );
    }
    output_manager_default.error(
      `Your codebase isn't linked to a project on Vercel. Run ${getCommandName("link")} to begin.`
    );
    return 1;
  }
  client.config.currentTeam = link.org.type === "team" ? link.org.id : void 0;
  return link;
}
async function confirmAction2(client, skipConfirmation, message, details) {
  if (skipConfirmation)
    return true;
  if (details) {
    output_manager_default.print(`  ${details}
`);
  }
  return await client.input.confirm(message, false);
}
function isValidUrl(url) {
  if (url.startsWith("/")) {
    return true;
  }
  try {
    const parsed = new URL(url);
    return parsed.protocol === "http:" || parsed.protocol === "https:";
  } catch {
    return false;
  }
}
var REDIRECTS_SUBCOMMAND_EXCLUSIVE_FLAGS = /* @__PURE__ */ new Set([
  "--status",
  "--case-sensitive",
  "--preserve-query-params",
  "--name",
  "--overwrite"
]);
function getArgsAfterRedirectsSubcommand(fullArgs, subcommand) {
  const idx = fullArgs.indexOf(subcommand);
  return idx >= 0 ? fullArgs.slice(idx + 1) : [];
}
function getRedirectGlobalFlagsOnly(afterSubcommandArgs) {
  const out = [];
  for (let i = 0; i < afterSubcommandArgs.length; i++) {
    const a = afterSubcommandArgs[i];
    if (!a.startsWith("-"))
      continue;
    let name = a;
    const hasEq = a.includes("=");
    if (hasEq) {
      name = a.slice(0, a.indexOf("="));
    }
    if (REDIRECTS_SUBCOMMAND_EXCLUSIVE_FLAGS.has(name)) {
      if (!hasEq && (name === "--status" || name === "--name") && i + 1 < afterSubcommandArgs.length && !afterSubcommandArgs[i + 1].startsWith("-")) {
        i++;
      }
      continue;
    }
    if (!GLOBAL_CLI_FLAG_NAMES.has(name)) {
      continue;
    }
    out.push(a);
    if (!hasEq && globalCliFlagTakesValue(name)) {
      if (i + 1 < afterSubcommandArgs.length && !afterSubcommandArgs[i + 1].startsWith("-")) {
        out.push(afterSubcommandArgs[++i]);
      }
    }
  }
  return out;
}
function getRedirectPromoteSuggestionFlags(afterSubcommandArgs) {
  const parts = getRedirectGlobalFlagsOnly(afterSubcommandArgs);
  if (!parts.some((p) => p === "--yes" || p === "-y")) {
    parts.push("--yes");
  }
  return parts;
}
function buildRedirectsSuggestionFlags(fullArgs, subcommand, options = {}) {
  const after = getArgsAfterRedirectsSubcommand(fullArgs, subcommand);
  const flagParts = after.filter((a) => a.startsWith("-"));
  if (options.ensureYes !== false && !flagParts.some((a) => a === "--yes" || a === "-y")) {
    flagParts.push("--yes");
  }
  return flagParts;
}

// src/util/redirects/get-redirects.ts
async function getRedirects(client, projectId, options = {}) {
  const { teamId, search: search2, page, perPage = 50, versionId, diff: diff2 } = options;
  const params = new URLSearchParams();
  params.set("projectId", projectId);
  if (teamId) {
    params.set("teamId", teamId);
  }
  if (versionId) {
    params.set("versionId", versionId);
  }
  if (diff2) {
    params.set("diff", diff2 === true ? "true" : diff2);
  } else {
    params.set("per_page", perPage.toString());
    if (search2) {
      params.set("q", search2);
    }
    if (page) {
      params.set("page", page.toString());
    }
  }
  const url = `/v1/bulk-redirects?${params}`;
  return await client.fetch(url);
}

// src/util/redirects/get-redirect-versions.ts
async function getRedirectVersions(client, projectId, teamId) {
  const params = new URLSearchParams();
  params.set("projectId", projectId);
  if (teamId) {
    params.set("teamId", teamId);
  }
  const url = `/v1/bulk-redirects/versions?${params}`;
  return await client.fetch(url);
}

// src/commands/redirects/list.ts
async function list11(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, listSubcommand13);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const { flags } = parsed;
  const teamId = org.type === "team" ? org.id : void 0;
  const search2 = flags["--search"];
  const page = flags["--page"];
  const perPage = flags["--per-page"];
  const staging = flags["--staging"];
  const versionIdFlag = flags["--version"];
  let versionId;
  let versionName;
  let useDiff = false;
  if (staging) {
    output_manager_default.spinner("Fetching staging version");
    const { versions } = await getRedirectVersions(client, project.id, teamId);
    const stagingVersion = versions.find((v) => v.isStaging);
    if (!stagingVersion) {
      output_manager_default.error(
        `No staging version found for ${import_chalk168.default.bold(project.name)}. Run ${import_chalk168.default.cyan(
          "vercel redirects list-versions"
        )} to see available versions.`
      );
      return 1;
    }
    versionId = stagingVersion.id;
    versionName = stagingVersion.name || stagingVersion.id;
    if (!search2 && !page) {
      useDiff = true;
    }
  }
  if (versionIdFlag) {
    if (staging) {
      output_manager_default.error("Cannot use both --staging and --version flags together");
      return 1;
    }
    output_manager_default.spinner("Fetching version");
    const { versions } = await getRedirectVersions(client, project.id, teamId);
    const version = versions.find(
      (v) => v.id === versionIdFlag || v.name === versionIdFlag
    );
    if (!version) {
      output_manager_default.error(
        `Version "${versionIdFlag}" not found. Run ${import_chalk168.default.cyan(
          "vercel redirects list-versions"
        )} to see available versions.`
      );
      return 1;
    }
    versionId = version.id;
    versionName = version.name || version.id;
  }
  const lsStamp = stamp_default();
  let spinnerMessage = `Fetching redirects for ${import_chalk168.default.bold(project.name)}`;
  if (versionName) {
    spinnerMessage += ` (version: ${versionName})`;
  }
  if (search2) {
    spinnerMessage += ` matching "${search2}"`;
  }
  output_manager_default.spinner(spinnerMessage);
  const { redirects, pagination } = await getRedirects(client, project.id, {
    teamId,
    search: search2,
    page,
    perPage,
    versionId,
    diff: useDiff
  });
  if (useDiff) {
    const added = redirects.filter((r) => r.action === "+");
    const removed = redirects.filter((r) => r.action === "-");
    const unchanged = redirects.filter((r) => !r.action);
    output_manager_default.log(
      `Changes in staging version ${import_chalk168.default.bold(versionName || "")} ${import_chalk168.default.gray(lsStamp())}`
    );
    if (added.length === 0 && removed.length === 0) {
      output_manager_default.log("\n  No changes from production version\n");
    } else {
      if (added.length > 0) {
        output_manager_default.print(`
  ${import_chalk168.default.bold(import_chalk168.default.green(`Added (${added.length}):`))}
`);
        output_manager_default.print(formatRedirectsTable(added, "+"));
      }
      if (removed.length > 0) {
        output_manager_default.print(`
  ${import_chalk168.default.bold(import_chalk168.default.red(`Removed (${removed.length}):`))}
`);
        output_manager_default.print(formatRedirectsTable(removed, "-"));
      }
      if (unchanged.length > 0) {
        output_manager_default.print(
          `
  ${import_chalk168.default.gray(`${unchanged.length} redirect${unchanged.length === 1 ? "" : "s"} unchanged`)}
`
        );
      }
      output_manager_default.print("\n");
    }
  } else {
    let resultMessage = `${(0, import_pluralize9.default)("Redirect", redirects.length, true)} found for ${import_chalk168.default.bold(
      project.name
    )}`;
    if (versionName) {
      resultMessage += ` ${import_chalk168.default.gray(`(version: ${versionName})`)}`;
    }
    if (search2) {
      resultMessage += ` matching "${search2}"`;
    }
    if (pagination) {
      resultMessage += ` ${import_chalk168.default.gray(`(page ${pagination.page} of ${pagination.numPages})`)}`;
    }
    resultMessage += ` ${import_chalk168.default.gray(lsStamp())}`;
    output_manager_default.log(resultMessage);
    if (client.nonInteractive && redirects.length === 0 && !staging && !versionIdFlag) {
      output_manager_default.log(
        `  ${getCommandNamePlain("redirects list")} shows production redirects only. If you added redirects but do not see them here, they may still be staged only\u2014run ${getCommandNamePlain("redirects list --staging")} to view staged changes.`
      );
    }
    if (redirects.length > 0) {
      output_manager_default.print(formatRedirectsTable(redirects));
      output_manager_default.print("\n");
    }
  }
  if (pagination && pagination.page < pagination.numPages) {
    const nextPage = pagination.page + 1;
    let command = `redirects list --page ${nextPage}`;
    if (search2) {
      command += ` --search "${search2}"`;
    }
    if (perPage) {
      command += ` --per-page ${perPage}`;
    }
    output_manager_default.log(`To display the next page, run ${getCommandName(command)}`);
  }
  return 0;
}
function formatRedirectsTable(redirects, actionSymbol) {
  const rows = redirects.map((redirect) => {
    const status3 = redirect.statusCode || (redirect.permanent ? 308 : 307);
    const prefix = actionSymbol || "";
    const colorFn = actionSymbol === "+" ? import_chalk168.default.green : actionSymbol === "-" ? import_chalk168.default.red : (s) => s;
    return [
      colorFn(`${prefix} ${redirect.source}`),
      colorFn(`${redirect.destination}`),
      colorFn(status3.toString())
    ];
  });
  return formatTable(
    ["Source", "Destination", "Status"],
    ["l", "l", "l"],
    [{ rows }]
  );
}

// src/commands/redirects/list-versions.ts
var import_chalk169 = __toESM(require_source(), 1);
var import_ms26 = __toESM(require_ms(), 1);
var import_pluralize10 = __toESM(require_pluralize(), 1);
async function listVersions(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, listVersionsSubcommand);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const lsStamp = stamp_default();
  output_manager_default.spinner(`Fetching redirect versions for ${import_chalk169.default.bold(project.name)}`);
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const sortedVersions = sortVersions(versions).slice(0, 20);
  output_manager_default.log(
    `${(0, import_pluralize10.default)("Version", sortedVersions.length, true)} found for ${import_chalk169.default.bold(
      project.name
    )} ${import_chalk169.default.gray(lsStamp())}`
  );
  if (sortedVersions.length > 0) {
    output_manager_default.print(formatVersionsTable(sortedVersions));
    output_manager_default.print("\n");
  }
  return 0;
}
function sortVersions(versions) {
  return versions.slice().sort((a, b) => {
    if (a.isStaging && !b.isStaging)
      return -1;
    if (!a.isStaging && b.isStaging)
      return 1;
    if (a.isLive && !b.isLive)
      return -1;
    if (!a.isLive && b.isLive)
      return 1;
    return b.lastModified - a.lastModified;
  });
}
function formatVersionsTable(versions) {
  const now = Date.now();
  const rows = versions.map((version) => {
    const age = (0, import_ms26.default)(now - version.lastModified);
    let status3 = "";
    if (version.isStaging) {
      status3 = import_chalk169.default.yellow("Staging");
    } else if (version.isLive) {
      status3 = import_chalk169.default.green("Live");
    } else {
      status3 = import_chalk169.default.gray("Previous");
    }
    const name = version.name || import_chalk169.default.gray("(unnamed)");
    const redirectCountStr = version.redirectCount !== void 0 && version.redirectCount !== null ? version.redirectCount.toString() : import_chalk169.default.gray("-");
    return [
      status3,
      name,
      version.id,
      redirectCountStr,
      version.createdBy || import_chalk169.default.gray("(unknown)"),
      import_chalk169.default.gray(age + " ago")
    ];
  });
  return formatTable(
    ["Status", "Name", "ID", "Redirects", "Created By", "Age"],
    ["l", "l", "l", "r", "l", "l"],
    [{ rows }]
  );
}

// src/commands/redirects/add.ts
var import_chalk170 = __toESM(require_source(), 1);

// src/util/redirects/put-redirects.ts
async function putRedirects(client, projectId, redirects, teamId, name) {
  const url = `/v1/bulk-redirects`;
  const body = {
    projectId,
    redirects
  };
  if (teamId) {
    body.teamId = teamId;
  }
  if (name) {
    body.name = name;
  }
  return await client.fetch(url, {
    method: "PUT",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify(body)
  });
}

// src/util/redirects/update-redirect-version.ts
async function updateRedirectVersion(client, projectId, versionId, action, teamId) {
  const params = new URLSearchParams();
  params.set("projectId", projectId);
  if (teamId) {
    params.set("teamId", teamId);
  }
  const url = `/v1/bulk-redirects/versions?${params}`;
  return await client.fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      id: versionId,
      action
    })
  });
}

// src/commands/redirects/add.ts
async function add9(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, addSubcommand8);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const existingStagingVersion = versions.find((v) => v.isStaging);
  const { args, flags } = parsed;
  const skipPrompts = flags["--yes"] || client.nonInteractive;
  if (client.nonInteractive && (!args[0] || !args[1])) {
    const sourcePart = args[0] || "<source>";
    const destPart = args[1] !== void 0 && args[1] !== "" ? args[1] : "<destination>";
    const flagParts = buildRedirectsSuggestionFlags(
      client.argv.slice(2),
      "add"
    );
    const cmd2 = getCommandNamePlain(
      `redirects add ${sourcePart} ${destPart} ${flagParts.join(" ")}`.trim()
    );
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        action: AGENT_ACTION.MISSING_ARGUMENTS,
        message: `In non-interactive mode source and destination are required. Run: ${cmd2}`,
        next: [{ command: cmd2, when: "to add a redirect" }]
      },
      1
    );
    return 1;
  }
  let source;
  if (args[0]) {
    source = args[0];
    if (!isValidUrl(source)) {
      output_manager_default.error(
        "Source must be a relative path (starting with /) or an absolute URL"
      );
      return 1;
    }
  } else {
    if (skipPrompts) {
      output_manager_default.error(
        "Source and destination are required when using --yes. Use: vercel redirects add <source> <destination> --yes"
      );
      return 1;
    }
    output_manager_default.log("Add a new redirect\n");
    source = await client.input.text({
      message: "What is the source URL?",
      validate: (val) => {
        if (!val) {
          return "Source URL cannot be empty";
        }
        if (!isValidUrl(val)) {
          return "Must be a relative path (starting with /) or an absolute URL";
        }
        return true;
      }
    });
  }
  let destination;
  if (args[1]) {
    destination = args[1];
    if (!isValidUrl(destination)) {
      output_manager_default.error(
        "Destination must be a relative path (starting with /) or an absolute URL"
      );
      return 1;
    }
  } else {
    if (skipPrompts) {
      output_manager_default.error(
        "Source and destination are required when using --yes. Use: vercel redirects add <source> <destination> --yes"
      );
      return 1;
    }
    if (!args[0]) {
      output_manager_default.log("Add a new redirect\n");
    }
    destination = await client.input.text({
      message: "What is the destination URL?",
      validate: (val) => {
        if (!val) {
          return "Destination URL cannot be empty";
        }
        if (!isValidUrl(val)) {
          return "Must be a relative path (starting with /) or an absolute URL";
        }
        return true;
      }
    });
  }
  let statusCode;
  if (flags["--status"]) {
    statusCode = flags["--status"];
    if (![301, 302, 307, 308].includes(statusCode)) {
      output_manager_default.error("Status code must be 301, 302, 307, or 308");
      return 1;
    }
  } else if (skipPrompts) {
    statusCode = 307;
  } else {
    statusCode = await client.input.select({
      message: "Select the status code:",
      choices: [
        {
          name: "307 - Temporary Redirect (preserves request method)",
          value: 307
        },
        {
          name: "308 - Permanent Redirect (preserves request method)",
          value: 308
        },
        {
          name: "301 - Moved Permanently (cached by browsers)",
          value: 301
        },
        {
          name: "302 - Found (temporary redirect, not cached)",
          value: 302
        }
      ]
    });
  }
  let caseSensitive;
  if (flags["--case-sensitive"] !== void 0) {
    caseSensitive = flags["--case-sensitive"];
  } else if (skipPrompts) {
    caseSensitive = false;
  } else {
    caseSensitive = await client.input.confirm(
      "Should the redirect be case sensitive?",
      false
    );
  }
  let preserveQueryParams;
  if (flags["--preserve-query-params"] !== void 0) {
    preserveQueryParams = flags["--preserve-query-params"];
  } else if (skipPrompts) {
    preserveQueryParams = false;
  } else {
    preserveQueryParams = await client.input.confirm(
      "Should query parameters be preserved?",
      false
    );
  }
  let versionName;
  if (flags["--name"]) {
    versionName = flags["--name"];
    if (versionName && versionName.length > 256) {
      output_manager_default.error("Name must be 256 characters or less");
      return 1;
    }
  } else if (skipPrompts) {
    versionName = void 0;
  } else {
    const provideName = await client.input.confirm(
      "Do you want to provide a name for this version?",
      false
    );
    if (provideName) {
      versionName = await client.input.text({
        message: "Version name (max 256 characters):",
        validate: (val) => {
          if (val && val.length > 256) {
            return "Name must be 256 characters or less";
          }
          return true;
        }
      });
      if (!versionName) {
        versionName = void 0;
      }
    }
  }
  const addStamp = stamp_default();
  output_manager_default.spinner("Adding redirect");
  const { alias: alias2, version } = await putRedirects(
    client,
    project.id,
    [
      {
        source,
        destination,
        statusCode,
        caseSensitive,
        preserveQueryParams
      }
    ],
    teamId,
    versionName
  );
  if (client.nonInteractive) {
    output_manager_default.stopSpinner();
    const testUrl = alias2 ? source.startsWith("/") ? `https://${alias2}${source}` : `https://${alias2}` : void 0;
    const fullArgs = client.argv.slice(2);
    const afterAdd = getArgsAfterRedirectsSubcommand(fullArgs, "add");
    const globalFlags = getRedirectGlobalFlagsOnly(afterAdd);
    const flagsSuffix = globalFlags.length > 0 ? ` ${globalFlags.join(" ")}` : "";
    const listStagingCmd = getCommandNamePlain(
      `redirects list --staging${flagsSuffix}`.trim()
    );
    const promoteFlagParts = getRedirectPromoteSuggestionFlags(afterAdd);
    const promoteCmd = getCommandNamePlain(
      `redirects promote ${version.id} ${promoteFlagParts.join(" ")}`.trim()
    );
    const jsonOutput = {
      status: AGENT_STATUS.OK,
      redirect: {
        source,
        destination,
        statusCode,
        caseSensitive,
        preserveQueryParams
      },
      version: { id: version.id, name: version.name || version.id },
      ...alias2 && { alias: alias2, testUrl },
      /** Redirect exists only in staging until promote; production list stays unchanged. */
      stagingOnly: true,
      inProduction: false,
      message: `Redirect was added to a staging version only\u2014not live in production yet. \`vercel redirects list\` (without --staging) shows production redirects only, so you may see 0 until you promote. Use ${listStagingCmd} to see staged redirects including this one, then ${promoteCmd} to promote this staging version to production.`,
      next: [
        {
          command: listStagingCmd,
          when: "To list staged redirects (includes this redirect)"
        },
        {
          command: promoteCmd,
          when: "To promote this staging version to production"
        }
      ]
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
    return 0;
  }
  output_manager_default.log(`${import_chalk170.default.cyan("\u2713")} Redirect added ${import_chalk170.default.gray(addStamp())}`);
  output_manager_default.print(`
  ${import_chalk170.default.bold("Redirect:")}
`);
  output_manager_default.print(`    ${import_chalk170.default.cyan(source)} \u2192 ${import_chalk170.default.cyan(destination)}
`);
  output_manager_default.print(`    Status: ${statusCode}
`);
  output_manager_default.print(`    Case sensitive: ${caseSensitive ? "Yes" : "No"}
`);
  output_manager_default.print(
    `    Preserve query params: ${preserveQueryParams ? "Yes" : "No"}
`
  );
  if (alias2) {
    const testUrl = source.startsWith("/") ? `https://${alias2}${source}` : `https://${alias2}`;
    output_manager_default.print(
      `
  ${import_chalk170.default.bold("Test your changes:")} ${import_chalk170.default.cyan(testUrl)}
`
    );
  }
  const newVersionName = version.name || version.id;
  output_manager_default.print(`  ${import_chalk170.default.bold("New staging version:")} ${newVersionName}

`);
  if (!existingStagingVersion) {
    const shouldPromote = await client.input.confirm(
      "This is the only staged change. Do you want to promote it to production now?",
      false
    );
    if (shouldPromote) {
      const promoteStamp = stamp_default();
      output_manager_default.spinner("Promoting to production");
      await updateRedirectVersion(
        client,
        project.id,
        version.id,
        "promote",
        teamId
      );
      output_manager_default.log(
        `${import_chalk170.default.cyan("\u2713")} Version promoted to production ${import_chalk170.default.gray(promoteStamp())}`
      );
    }
  } else {
    output_manager_default.warn(
      `There are other staged changes. Please review all changes with ${import_chalk170.default.cyan("vercel redirects list --staging")} before promoting to production.`
    );
  }
  return 0;
}

// src/commands/redirects/upload.ts
var import_chalk171 = __toESM(require_source(), 1);
import { readFileSync as readFileSync4 } from "fs";
import { basename as basename2 } from "path";
import FormData from "form-data";

// src/commands/redirects/validate-redirects.ts
import { statSync as statSync2 } from "fs";
var MAX_FILE_SIZE = 50 * 1024 * 1024;
var ALLOWED_EXTENSIONS = [".csv", ".json"];
var MAX_REDIRECTS = 1e6;
var MAX_URL_LENGTH = 2048;
var VALID_STATUS_CODES = [301, 302, 303, 307, 308];
function validateUploadFile(filePath, options = {}) {
  const maxSize = options.maxFileSize ?? MAX_FILE_SIZE;
  const allowedExts = options.allowedExtensions ?? ALLOWED_EXTENSIONS;
  try {
    const stats = statSync2(filePath);
    if (!stats.isFile()) {
      return { valid: false, error: `Path "${filePath}" is not a file` };
    }
    if (stats.size > maxSize) {
      const sizeMB = Math.round(maxSize / (1024 * 1024));
      return { valid: false, error: `File must be below ${sizeMB}MB` };
    }
    const hasValidExtension = allowedExts.some(
      (ext) => filePath.toLowerCase().endsWith(ext)
    );
    if (!hasValidExtension) {
      return {
        valid: false,
        error: `File must be a .csv or .json file`
      };
    }
    return { valid: true };
  } catch (err) {
    if (err.code === "ENOENT") {
      return { valid: false, error: `File "${filePath}" not found` };
    }
    return { valid: false, error: `Error accessing file: ${err.message}` };
  }
}
function validateRedirect(redirect) {
  if (!redirect.source) {
    return { valid: false, error: "Redirect source is required" };
  }
  if (redirect.source.length > MAX_URL_LENGTH) {
    return { valid: false, error: "Source URL is too long" };
  }
  if (!isValidUrl(redirect.source)) {
    return {
      valid: false,
      error: "Source must be a relative path or a valid URL"
    };
  }
  if (!redirect.destination) {
    return { valid: false, error: "Redirect destination is required" };
  }
  if (redirect.destination.length > MAX_URL_LENGTH) {
    return { valid: false, error: "Destination URL is too long" };
  }
  try {
    new URL(redirect.destination, "https://vercel.com");
  } catch {
    return { valid: false, error: "Destination must be a valid URL" };
  }
  if (redirect.statusCode) {
    if (!VALID_STATUS_CODES.includes(redirect.statusCode)) {
      return {
        valid: false,
        error: `Invalid status code. Must be one of: ${VALID_STATUS_CODES.join(", ")}`
      };
    }
  }
  return { valid: true };
}
function validateRedirectsArray(redirects) {
  if (!Array.isArray(redirects)) {
    return {
      valid: false,
      error: "JSON file must contain an array of redirects"
    };
  }
  if (redirects.length === 0) {
    return { valid: false, error: "No redirects provided" };
  }
  if (redirects.length > MAX_REDIRECTS) {
    return {
      valid: false,
      error: `Too many redirects. Maximum allowed: ${MAX_REDIRECTS}`
    };
  }
  for (let i = 0; i < redirects.length; i++) {
    const result = validateRedirect(redirects[i]);
    if (!result.valid) {
      return {
        valid: false,
        error: `Redirect ${i + 1}: ${result.error}`
      };
    }
  }
  return { valid: true };
}
function validateCSVStructure(content) {
  const lines = content.trim().split("\n");
  if (lines.length < 2) {
    return {
      valid: false,
      error: "CSV must have a header and at least one redirect"
    };
  }
  const header = lines[0].toLowerCase();
  const hasSource = header.includes("source");
  const hasDestination = header.includes("destination");
  if (!hasSource || !hasDestination) {
    return {
      valid: false,
      error: 'CSV must have "source" and "destination" columns'
    };
  }
  return { valid: true };
}
function validateVersionName(name) {
  if (!name) {
    return { valid: false, error: "Name is required" };
  }
  if (name.length > 256) {
    return { valid: false, error: "Name must be 256 characters or less" };
  }
  return { valid: true };
}

// src/commands/redirects/upload.ts
async function upload(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, uploadSubcommand);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const { args, flags } = parsed;
  const skipPrompts = flags["--yes"] || false;
  const filePath = args[0];
  if (!filePath) {
    if (client.nonInteractive) {
      const flagParts = buildRedirectsSuggestionFlags(
        client.argv.slice(2),
        "upload"
      );
      const cmd2 = getCommandNamePlain(
        `redirects upload <file> ${flagParts.join(" ")}`.trim()
      );
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `File path is required. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to upload redirects from a CSV or JSON file"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error("File path is required. Use: vercel redirects upload <file>");
    return 1;
  }
  if (client.nonInteractive && !skipPrompts) {
    const cmd2 = buildCommandWithYes(client.argv);
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: `In non-interactive mode use --yes to confirm upload. Run: ${cmd2}`,
        next: [{ command: cmd2, when: "to confirm upload" }]
      },
      1
    );
    return 1;
  }
  const overwrite = flags["--overwrite"] || false;
  const fileValidation = validateUploadFile(filePath);
  if (!fileValidation.valid) {
    output_manager_default.error(fileValidation.error);
    return 1;
  }
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const existingStagingVersion = versions.find((v) => v.isStaging);
  if (!skipPrompts) {
    const fileName = basename2(filePath);
    const fileType = filePath.endsWith(".csv") ? "CSV" : "JSON";
    const message = overwrite ? `Upload ${fileType} file "${fileName}" and replace all existing redirects?` : `Upload ${fileType} file "${fileName}"?`;
    const confirmed = await client.input.confirm(message, true);
    if (!confirmed) {
      output_manager_default.log("Upload cancelled");
      return 0;
    }
  }
  let versionName;
  if (!skipPrompts) {
    const provideName = await client.input.confirm(
      "Do you want to provide a name for this version?",
      false
    );
    if (provideName) {
      versionName = await client.input.text({
        message: "Version name (max 256 characters):",
        validate: (val) => {
          if (val && val.length > 256) {
            return "Name must be 256 characters or less";
          }
          return true;
        }
      });
      const { valid, error } = validateVersionName(versionName);
      if (!valid) {
        output_manager_default.error(error);
        return 1;
      }
    }
  }
  const uploadStamp = stamp_default();
  output_manager_default.spinner("Uploading redirects");
  try {
    let result;
    const url = "/v1/bulk-redirects";
    if (filePath.endsWith(".csv")) {
      const csvContent = readFileSync4(filePath);
      const fileName = basename2(filePath);
      const csvValidation = validateCSVStructure(csvContent.toString());
      if (!csvValidation.valid) {
        output_manager_default.error(`Invalid CSV: ${csvValidation.error}`);
        return 1;
      }
      const form = new FormData();
      form.append("teamId", teamId || org.id);
      form.append("projectId", project.id);
      form.append("overwrite", String(overwrite));
      if (versionName) {
        form.append("name", versionName);
      }
      form.append("bulkRedirectsFile", csvContent, {
        filename: fileName,
        contentType: "text/csv"
      });
      result = await client.fetch(url, {
        method: "PUT",
        headers: form.getHeaders(),
        body: form
      });
    } else {
      const content = readFileSync4(filePath, "utf8");
      let redirects2;
      try {
        redirects2 = JSON.parse(content);
      } catch (_err) {
        output_manager_default.error("Invalid JSON file format");
        return 1;
      }
      const redirectsValidation = validateRedirectsArray(redirects2);
      if (!redirectsValidation.valid) {
        output_manager_default.error(redirectsValidation.error);
        return 1;
      }
      const body = {
        projectId: project.id,
        redirects: redirects2,
        overwrite
      };
      if (teamId) {
        body.teamId = teamId;
      }
      if (versionName) {
        body.versionName = versionName;
      }
      result = await client.fetch(url, {
        method: "PUT",
        body
      });
    }
    if (client.nonInteractive) {
      output_manager_default.stopSpinner();
      const afterUpload = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "upload"
      );
      const promoteFlagParts = getRedirectPromoteSuggestionFlags(afterUpload);
      const promoteCmd = getCommandNamePlain(
        `redirects promote ${result.version.id} ${promoteFlagParts.join(" ")}`.trim()
      );
      const jsonOutput = {
        status: AGENT_STATUS.OK,
        version: {
          id: result.version.id,
          name: result.version.name || result.version.id,
          ...result.version.redirectCount !== void 0 && {
            redirectCount: result.version.redirectCount
          }
        },
        ...result.alias && {
          alias: result.alias,
          testUrl: `https://${result.alias}`
        },
        ...!existingStagingVersion && {
          next: [
            {
              command: promoteCmd,
              when: "To promote this staging version to production"
            }
          ]
        },
        ...existingStagingVersion && {
          hint: `Review staged changes with ${getCommandNamePlain("redirects list --staging")} before promoting.`
        }
      };
      client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
      return 0;
    }
    output_manager_default.log(
      `${import_chalk171.default.cyan("\u2713")} Redirects uploaded ${import_chalk171.default.gray(uploadStamp())}`
    );
    output_manager_default.spinner("Fetching diff");
    const { redirects } = await getRedirects(client, project.id, {
      teamId,
      versionId: result.version.id,
      diff: "only"
    });
    const redirectCount = redirects.length;
    output_manager_default.print(`
  ${import_chalk171.default.bold("Summary:")}
`);
    output_manager_default.print(
      `    Uploaded ${redirectCount} redirect${redirectCount === 1 ? "" : "s"}
`
    );
    if (redirectCount > 0) {
      const added = redirects.filter((r) => r.action === "+");
      const deleted = redirects.filter((r) => r.action === "-");
      const edited = redirects.filter((r) => r.action === "~");
      output_manager_default.print(`
  ${import_chalk171.default.bold("Changes:")}
`);
      if (added.length > 0) {
        output_manager_default.print(`    ${import_chalk171.default.green(`Added: ${added.length}`)}
`);
      }
      if (deleted.length > 0) {
        output_manager_default.print(`    ${import_chalk171.default.red(`Deleted: ${deleted.length}`)}
`);
      }
      if (edited.length > 0) {
        output_manager_default.print(`    ${import_chalk171.default.yellow(`Modified: ${edited.length}`)}
`);
      }
      output_manager_default.print(`
  ${import_chalk171.default.bold("Redirect changes:")}
`);
      const displayRedirects = redirects.slice(0, 100);
      const rows = displayRedirects.map((redirect) => {
        const status3 = redirect.statusCode || (redirect.permanent ? 308 : 307);
        const action = redirect.action || "+";
        let colorFn;
        let actionSymbol;
        switch (action) {
          case "+":
            colorFn = import_chalk171.default.green;
            actionSymbol = "+";
            break;
          case "-":
            colorFn = import_chalk171.default.red;
            actionSymbol = "-";
            break;
          case "~":
            colorFn = import_chalk171.default.yellow;
            actionSymbol = "~";
            break;
          default:
            colorFn = (s) => s;
            actionSymbol = " ";
        }
        return [
          colorFn(`${actionSymbol} ${redirect.source}`),
          colorFn(redirect.destination),
          colorFn(status3.toString())
        ];
      });
      output_manager_default.print(
        formatTable(
          ["Source", "Destination", "Status"],
          ["l", "l", "l"],
          [{ rows }]
        )
      );
      if (redirectCount > 100) {
        output_manager_default.print(
          `
  ${import_chalk171.default.gray(`... and ${redirectCount - 100} more redirect${redirectCount - 100 === 1 ? "" : "s"}`)}
`
        );
      }
    }
    if (result.alias) {
      const testUrl = `https://${result.alias}`;
      output_manager_default.print(
        `
  ${import_chalk171.default.bold("Test your changes:")} ${import_chalk171.default.cyan(testUrl)}
`
      );
    }
    const newVersionName = result.version.name || result.version.id;
    output_manager_default.print(
      `  ${import_chalk171.default.bold("New staging version:")} ${newVersionName}

`
    );
    if (existingStagingVersion) {
      output_manager_default.warn(
        `There are other staged changes. Please review all changes with ${import_chalk171.default.cyan("vercel redirects list --staging")} before promoting to production.`
      );
    } else if (!skipPrompts && !client.nonInteractive) {
      const shouldPromote = await client.input.confirm(
        "This is the only staged change. Do you want to promote it to production now?",
        false
      );
      if (shouldPromote) {
        const promoteStamp = stamp_default();
        output_manager_default.spinner("Promoting to production");
        await updateRedirectVersion(
          client,
          project.id,
          result.version.id,
          "promote",
          teamId
        );
        output_manager_default.log(
          `${import_chalk171.default.cyan("\u2713")} Version promoted to production ${import_chalk171.default.gray(promoteStamp())}`
        );
      }
    } else if (!existingStagingVersion && client.nonInteractive) {
      output_manager_default.print(
        `  Run ${import_chalk171.default.cyan(`vercel redirects promote ${result.version.id} --yes`)} to promote this staging version to production.

`
      );
    }
    return 0;
  } catch (error) {
    output_manager_default.error(`Failed to upload redirects: ${error.message}`);
    return 1;
  }
}

// src/commands/redirects/remove.ts
var import_chalk172 = __toESM(require_source(), 1);

// src/util/redirects/delete-redirects.ts
async function deleteRedirects(client, projectId, sources, teamId) {
  const params = new URLSearchParams();
  params.set("projectId", projectId);
  if (teamId) {
    params.set("teamId", teamId);
  }
  const url = `/v1/bulk-redirects?${params}`;
  return await client.fetch(url, {
    method: "DELETE",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      redirects: sources
    })
  });
}

// src/commands/redirects/remove.ts
async function remove6(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, removeSubcommand13);
  if (typeof parsed === "number")
    return parsed;
  const error = validateRequiredArgs(parsed.args, ["source"]);
  if (error) {
    if (client.nonInteractive) {
      const flagParts = buildRedirectsSuggestionFlags(
        client.argv.slice(2),
        "remove"
      );
      const cmd2 = getCommandNamePlain(
        `redirects remove <source> ${flagParts.join(" ")}`.trim()
      );
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `${error} Run: ${cmd2}`,
          next: [{ command: cmd2, when: "to remove a redirect" }]
        },
        1
      );
    }
    output_manager_default.error(error);
    return 1;
  }
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const existingStagingVersion = versions.find((v) => v.isStaging);
  const [source] = parsed.args;
  output_manager_default.spinner("Fetching redirect information");
  const { redirects } = await getRedirects(client, project.id, { teamId });
  const redirectToRemove = redirects.find((r) => r.source === source);
  if (!redirectToRemove) {
    if (client.nonInteractive) {
      const afterRemove = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "remove"
      );
      const globalFlags = getRedirectGlobalFlagsOnly(afterRemove);
      const listCmd2 = getCommandNamePlain(
        `redirects list ${globalFlags.join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.REDIRECT_NOT_FOUND,
          message: `Redirect with source "${source}" not found. Run ${listCmd2} to see available redirects.`,
          next: [{ command: listCmd2 }]
        },
        1
      );
    }
    output_manager_default.error(
      `Redirect with source "${source}" not found. Run ${import_chalk172.default.cyan(
        "vercel redirects list"
      )} to see available redirects.`
    );
    return 1;
  }
  if (client.nonInteractive && !parsed.flags["--yes"]) {
    const cmd2 = buildCommandWithYes(client.argv);
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: `In non-interactive mode use --yes to confirm removal. Run: ${cmd2}`,
        next: [{ command: cmd2, when: "to confirm removal" }]
      },
      1
    );
    return 1;
  }
  output_manager_default.print(`
  ${import_chalk172.default.bold("Removing redirect:")}
`);
  output_manager_default.print(
    `    ${import_chalk172.default.cyan(redirectToRemove.source)} \u2192 ${import_chalk172.default.cyan(redirectToRemove.destination)}
`
  );
  const status3 = redirectToRemove.statusCode || (redirectToRemove.permanent ? 308 : 307);
  output_manager_default.print(`    Status: ${status3}

`);
  const confirmed = await confirmAction2(
    client,
    parsed.flags["--yes"],
    `Remove this redirect?`,
    `This will create a new staging version without this redirect.`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const removeStamp = stamp_default();
  output_manager_default.spinner(`Removing redirect for ${import_chalk172.default.bold(source)}`);
  const { alias: alias2, version } = await deleteRedirects(
    client,
    project.id,
    [source],
    teamId
  );
  if (client.nonInteractive) {
    output_manager_default.stopSpinner();
    const testUrl = alias2 ? source.startsWith("/") ? `https://${alias2}${source}` : `https://${alias2}` : void 0;
    const afterRemove = getArgsAfterRedirectsSubcommand(
      client.argv.slice(2),
      "remove"
    );
    const promoteFlagParts = getRedirectPromoteSuggestionFlags(afterRemove);
    const promoteCmd = getCommandNamePlain(
      `redirects promote ${version.id} ${promoteFlagParts.join(" ")}`.trim()
    );
    const jsonOutput = {
      status: AGENT_STATUS.OK,
      removed: { source },
      version: { id: version.id, name: version.name || version.id },
      ...alias2 && { alias: alias2, testUrl },
      ...!existingStagingVersion && {
        next: [
          {
            command: promoteCmd,
            when: "To promote this staging version to production"
          }
        ]
      },
      ...existingStagingVersion && {
        hint: `Review staged changes with ${getCommandNamePlain("redirects list --staging")} before promoting.`
      }
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
    return 0;
  }
  output_manager_default.log(
    `${import_chalk172.default.cyan("\u2713")} Redirect removed ${import_chalk172.default.gray(removeStamp())}`
  );
  if (alias2) {
    const testUrl = source.startsWith("/") ? `https://${alias2}${source}` : `https://${alias2}`;
    output_manager_default.print(
      `
  ${import_chalk172.default.bold("Test your changes:")} ${import_chalk172.default.cyan(testUrl)}
`
    );
    output_manager_default.print(
      `  This URL should no longer redirect to the above destination.
`
    );
  }
  const versionName = version.name || version.id;
  output_manager_default.print(`  ${import_chalk172.default.bold("New staging version:")} ${versionName}

`);
  if (!existingStagingVersion) {
    const shouldPromote = await client.input.confirm(
      "This is the only staged change. Do you want to promote it to production now?",
      false
    );
    if (shouldPromote) {
      const promoteStamp = stamp_default();
      output_manager_default.spinner("Promoting to production");
      await updateRedirectVersion(
        client,
        project.id,
        version.id,
        "promote",
        teamId
      );
      output_manager_default.log(
        `${import_chalk172.default.cyan("\u2713")} Version promoted to production ${import_chalk172.default.gray(promoteStamp())}`
      );
    }
  } else {
    output_manager_default.warn(
      `There are other staged changes. Review them with ${import_chalk172.default.cyan("vercel redirects list --staging")} before promoting to production.`
    );
  }
  return 0;
}

// src/commands/redirects/promote.ts
var import_chalk173 = __toESM(require_source(), 1);
async function promote(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, promoteSubcommand);
  if (typeof parsed === "number")
    return parsed;
  const error = validateRequiredArgs(parsed.args, ["version-id"]);
  if (error) {
    if (client.nonInteractive) {
      const afterPromote = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "promote"
      );
      const listVersionsCmd = getCommandNamePlain(
        `redirects list-versions ${getRedirectGlobalFlagsOnly(afterPromote).join(" ")}`.trim()
      );
      const promoteFlagParts = getRedirectPromoteSuggestionFlags(afterPromote);
      const promoteCmd = getCommandNamePlain(
        `redirects promote <version-id> ${promoteFlagParts.join(" ")}`.trim()
      );
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `${error} Run ${listVersionsCmd} to list version IDs and names, then ${promoteCmd} (replace <version-id> with a staging version to promote).`,
          next: [
            { command: listVersionsCmd, when: "To list redirect version IDs" },
            {
              command: promoteCmd,
              when: "To promote a staging version (substitute version-id)"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(error);
    return 1;
  }
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const [versionIdentifier] = parsed.args;
  output_manager_default.spinner(`Fetching redirect versions for ${import_chalk173.default.bold(project.name)}`);
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const version = versions.find(
    (v) => v.id === versionIdentifier || v.name === versionIdentifier
  );
  if (!version) {
    if (client.nonInteractive) {
      const afterPromote = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "promote"
      );
      const listVersionsCmd = getCommandNamePlain(
        `redirects list-versions ${getRedirectGlobalFlagsOnly(afterPromote).join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_FOUND,
          message: `Version with ID or name "${versionIdentifier}" not found.`,
          next: [
            {
              command: listVersionsCmd,
              when: "To see available redirect versions (IDs and names)"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Version with ID or name "${versionIdentifier}" not found. Run ${import_chalk173.default.cyan(
        "vercel redirects list-versions"
      )} to see available versions.`
    );
    return 1;
  }
  if (version.isLive) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: `Version ${version.name || version.id} is already live. Nothing to promote.`
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Version ${import_chalk173.default.bold(version.name || version.id)} is already live.`
    );
    return 1;
  }
  if (!version.isStaging) {
    if (client.nonInteractive) {
      const afterPromote = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "promote"
      );
      const listVersionsCmd = getCommandNamePlain(
        `redirects list-versions ${getRedirectGlobalFlagsOnly(afterPromote).join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: `Version ${version.name || version.id} is not staged. Only staging versions can be promoted to production.`,
          next: [
            {
              command: listVersionsCmd,
              when: "To see which version is currently staged"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Version ${import_chalk173.default.bold(
        version.name || version.id
      )} is not staged. Only staging versions can be promoted to production.
Run ${import_chalk173.default.cyan(
        "vercel redirects list-versions"
      )} to see which version is currently staged.`
    );
    return 1;
  }
  const versionName = version.name || version.id;
  output_manager_default.spinner("Fetching changes");
  const { redirects: diffRedirects } = await getRedirects(client, project.id, {
    teamId,
    versionId: version.id,
    diff: true
  });
  const changedRedirects = diffRedirects.filter(
    (r) => r.action === "+" || r.action === "-"
  );
  if (changedRedirects.length > 0) {
    output_manager_default.print(`
${import_chalk173.default.bold("Changes to be promoted:")}

`);
    const displayRedirects = changedRedirects.slice(0, 20);
    for (const redirect of displayRedirects) {
      const status3 = redirect.statusCode || (redirect.permanent ? 308 : 307);
      const symbol = redirect.action === "+" ? import_chalk173.default.green("+") : import_chalk173.default.red("-");
      output_manager_default.print(
        `  ${symbol} ${redirect.source} \u2192 ${redirect.destination} (${status3})
`
      );
    }
    if (changedRedirects.length > 20) {
      output_manager_default.print(
        import_chalk173.default.gray(`
  ... and ${changedRedirects.length - 20} more changes
`)
      );
    }
    output_manager_default.print("\n");
  } else {
    output_manager_default.print(
      `
${import_chalk173.default.gray("No changes detected from current production version.")}

`
    );
  }
  if (client.nonInteractive && !parsed.flags["--yes"]) {
    const cmd2 = buildCommandWithYes(client.argv);
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: `In non-interactive mode use --yes to confirm promote. Run: ${cmd2}`,
        next: [{ command: cmd2, when: "to confirm promote to production" }]
      },
      1
    );
    return 1;
  }
  const confirmed = await confirmAction2(
    client,
    parsed.flags["--yes"],
    `Promote version ${import_chalk173.default.bold(versionName)} to production?`,
    `This will make it the live version for ${import_chalk173.default.bold(project.name)}.`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const updateStamp = stamp_default();
  output_manager_default.spinner(`Promoting version ${import_chalk173.default.bold(versionName)} to production`);
  const { version: newVersion } = await updateRedirectVersion(
    client,
    project.id,
    version.id,
    "promote",
    teamId
  );
  output_manager_default.log(
    `${import_chalk173.default.cyan("\u2713")} Version ${import_chalk173.default.bold(
      newVersion.name || newVersion.id
    )} promoted to production ${import_chalk173.default.gray(updateStamp())}`
  );
  return 0;
}

// src/commands/redirects/restore.ts
var import_chalk174 = __toESM(require_source(), 1);
async function restore(client, argv) {
  const parsed = await parseSubcommandArgs3(argv, restoreSubcommand);
  if (typeof parsed === "number")
    return parsed;
  const error = validateRequiredArgs(parsed.args, ["version-id"]);
  if (error) {
    if (client.nonInteractive) {
      const afterRestore = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "restore"
      );
      const globalFlags = getRedirectGlobalFlagsOnly(afterRestore);
      const listVersionsCmd = getCommandNamePlain(
        `redirects list-versions ${globalFlags.join(" ")}`.trim()
      );
      const restoreFlagParts = getRedirectPromoteSuggestionFlags(afterRestore);
      const restoreCmd = getCommandNamePlain(
        `redirects restore <version-id> ${restoreFlagParts.join(" ")}`.trim()
      );
      outputActionRequired(
        client,
        {
          status: AGENT_STATUS.ACTION_REQUIRED,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          action: AGENT_ACTION.MISSING_ARGUMENTS,
          message: `${error} Run ${listVersionsCmd} to list version IDs and names, then ${restoreCmd} (replace <version-id> with a non-live, non-staging version).`,
          next: [
            {
              command: listVersionsCmd,
              when: "To list redirect version IDs to restore"
            },
            {
              command: restoreCmd,
              when: "To restore a previous version (substitute version-id)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(error);
    return 1;
  }
  const link = await ensureProjectLink3(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const [versionIdentifier] = parsed.args;
  output_manager_default.spinner(`Fetching redirect versions for ${import_chalk174.default.bold(project.name)}`);
  const { versions } = await getRedirectVersions(client, project.id, teamId);
  const version = versions.find(
    (v) => v.id === versionIdentifier || v.name === versionIdentifier
  );
  if (!version) {
    if (client.nonInteractive) {
      const afterRestore = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "restore"
      );
      const globalFlags = getRedirectGlobalFlagsOnly(afterRestore);
      const listCmd2 = getCommandNamePlain(
        `redirects list-versions ${globalFlags.join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.VERSION_NOT_FOUND,
          message: `Version with ID or name "${versionIdentifier}" not found. Run ${listCmd2} to see available versions.`,
          next: [{ command: listCmd2 }]
        },
        1
      );
    }
    output_manager_default.error(
      `Version with ID or name "${versionIdentifier}" not found. Run ${import_chalk174.default.cyan(
        "vercel redirects list-versions"
      )} to see available versions.`
    );
    return 1;
  }
  if (version.isLive) {
    if (client.nonInteractive) {
      const afterRestore = getArgsAfterRedirectsSubcommand(
        client.argv.slice(2),
        "restore"
      );
      const globalFlags = getRedirectGlobalFlagsOnly(afterRestore);
      const listCmd2 = getCommandNamePlain(
        `redirects list-versions ${globalFlags.join(" ")}`.trim()
      );
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.VERSION_ALREADY_LIVE,
          message: `Version ${version.name || version.id} is currently live. You cannot restore the live version. Run ${listCmd2} to see previous versions you can restore.`,
          next: [{ command: listCmd2 }]
        },
        1
      );
    }
    output_manager_default.error(
      `Version ${import_chalk174.default.bold(
        version.name || version.id
      )} is currently live. You cannot restore the live version.
Run ${import_chalk174.default.cyan(
        "vercel redirects list-versions"
      )} to see previous versions you can restore.`
    );
    return 1;
  }
  if (version.isStaging) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.VERSION_IS_STAGING,
          message: `Version ${version.name || version.id} is staged. You can only restore previous (non-staging, non-live) versions.`
        },
        1
      );
    }
    output_manager_default.error(
      `Version ${import_chalk174.default.bold(
        version.name || version.id
      )} is staged. You can only restore previous versions.`
    );
    return 1;
  }
  const versionName = version.name || version.id;
  output_manager_default.spinner("Fetching changes");
  const { redirects: diffRedirects } = await getRedirects(client, project.id, {
    teamId,
    versionId: version.id,
    diff: true
  });
  const changedRedirects = diffRedirects.filter(
    (r) => r.action === "+" || r.action === "-"
  );
  if (changedRedirects.length > 0) {
    output_manager_default.print(`
${import_chalk174.default.bold("Changes to be restored:")}

`);
    const displayRedirects = changedRedirects.slice(0, 20);
    for (const redirect of displayRedirects) {
      const status3 = redirect.statusCode || (redirect.permanent ? 308 : 307);
      const symbol = redirect.action === "+" ? import_chalk174.default.green("+") : import_chalk174.default.red("-");
      output_manager_default.print(
        `  ${symbol} ${redirect.source} \u2192 ${redirect.destination} (${status3})
`
      );
    }
    if (changedRedirects.length > 20) {
      output_manager_default.print(
        import_chalk174.default.gray(`
  ... and ${changedRedirects.length - 20} more changes
`)
      );
    }
    output_manager_default.print("\n");
  } else {
    output_manager_default.print(
      `
${import_chalk174.default.gray("No changes detected from current production version.")}

`
    );
  }
  if (client.nonInteractive && !parsed.flags["--yes"]) {
    const cmd2 = buildCommandWithYes(client.argv);
    outputActionRequired(
      client,
      {
        status: AGENT_STATUS.ACTION_REQUIRED,
        reason: AGENT_REASON.CONFIRMATION_REQUIRED,
        action: AGENT_ACTION.CONFIRMATION_REQUIRED,
        message: `In non-interactive mode use --yes to confirm restore. Run: ${cmd2}`,
        next: [{ command: cmd2, when: "to confirm restore to production" }]
      },
      1
    );
    return 1;
  }
  const confirmed = await confirmAction2(
    client,
    parsed.flags["--yes"],
    `Restore version ${import_chalk174.default.bold(versionName)}?`,
    `This will make it the live version for ${import_chalk174.default.bold(project.name)}.`
  );
  if (!confirmed) {
    output_manager_default.log("Canceled");
    return 0;
  }
  const updateStamp = stamp_default();
  output_manager_default.spinner(`Restoring version ${import_chalk174.default.bold(versionName)}`);
  const { version: newVersion } = await updateRedirectVersion(
    client,
    project.id,
    version.id,
    "restore",
    teamId
  );
  output_manager_default.log(
    `${import_chalk174.default.cyan("\u2713")} Version ${import_chalk174.default.bold(
      newVersion.name || newVersion.id
    )} restored to production ${import_chalk174.default.gray(updateStamp())}`
  );
  return 0;
}

// src/util/telemetry/commands/redirects/index.ts
var RedirectsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
  trackCliSubcommandListVersions(actual) {
    this.trackCliSubcommand({
      subcommand: "list-versions",
      value: actual
    });
  }
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandUpload(actual) {
    this.trackCliSubcommand({
      subcommand: "upload",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandPromote(actual) {
    this.trackCliSubcommand({
      subcommand: "promote",
      value: actual
    });
  }
  trackCliSubcommandRestore(actual) {
    this.trackCliSubcommand({
      subcommand: "restore",
      value: actual
    });
  }
};

// src/commands/redirects/index.ts
var COMMAND_CONFIG33 = {
  list: getCommandAliases(listSubcommand13),
  "list-versions": getCommandAliases(listVersionsSubcommand),
  add: getCommandAliases(addSubcommand8),
  upload: getCommandAliases(uploadSubcommand),
  remove: getCommandAliases(removeSubcommand13),
  promote: getCommandAliases(promoteSubcommand),
  restore: getCommandAliases(restoreSubcommand)
};
async function main26(client) {
  const telemetry2 = new RedirectsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(redirectsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG33
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("redirects");
    output_manager_default.print(help(redirectsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: redirectsCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(listSubcommand13);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return list11(client, args);
    case "list-versions":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(listVersionsSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandListVersions(subcommandOriginal);
      return listVersions(client, args);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(addSubcommand8);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add9(client, args);
    case "upload":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(uploadSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandUpload(subcommandOriginal);
      return upload(client, args);
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(removeSubcommand13);
        return 2;
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return remove6(client, args);
    case "promote":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(promoteSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandPromote(subcommandOriginal);
      return promote(client, args);
    case "restore":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("redirects", subcommandOriginal);
        printHelp(restoreSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRestore(subcommandOriginal);
      return restore(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG33));
      output_manager_default.print(help(redirectsCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/remove/index.ts
var import_chalk175 = __toESM(require_source(), 1);
var import_ms27 = __toESM(require_ms(), 1);
var import_pluralize11 = __toESM(require_pluralize(), 1);

// src/util/url.ts
var normalizeURL2 = (u) => {
  if (u.slice(-1) === "/") {
    u = u.slice(0, -1);
  }
  u = u.replace(/^https:\/\//i, "");
  return u;
};

// src/util/projects/remove-project.ts
async function removeProject(client, projectNameOrId) {
  try {
    await client.fetch(`/projects/${encodeURIComponent(projectNameOrId)}`, {
      method: "DELETE"
    });
  } catch (error) {
    if (isAPIError(error) && error.status === 404) {
      return new ProjectNotFound(projectNameOrId);
    }
    throw error;
  }
}

// src/util/deploy/get-deployments-by-project-id.ts
import { URLSearchParams as URLSearchParams4 } from "url";
async function getDeploymentsByProjectId(client, projectId, options = { from: null, limit: 100, continue: false }, total = 0) {
  const limit = options.limit || 100;
  const query = new URLSearchParams4();
  query.set("projectId", projectId);
  query.set("limit", limit.toString());
  if (options.from) {
    query.set("from", options.from.toString());
  }
  const { deployments: legacyDeployments } = await client.fetch(
    `/v6/deployments?${query}`
  );
  const deployments = legacyDeployments.map((depl) => {
    return {
      aliasAssigned: depl.aliasAssigned,
      aliasError: depl.aliasError,
      buildingAt: depl.buildingAt,
      checksConclusion: depl.checksConclusion,
      checksState: depl.checksState,
      createdAt: depl.created,
      creator: {
        uid: depl.creator.uid,
        username: depl.creator.username
      },
      id: depl.uid,
      inspectorUrl: depl.inspectorUrl,
      meta: depl.meta,
      name: depl.name,
      public: true,
      ready: depl.ready,
      readyState: depl.state,
      regions: [],
      source: depl.source,
      status: depl.state,
      target: depl.target,
      type: depl.type,
      url: depl.url,
      version: 2
    };
  });
  total += deployments.length;
  if (options.max && total >= options.max) {
    return deployments;
  }
  if (options.continue && deployments.length === limit) {
    const nextFrom = deployments[deployments.length - 1].createdAt;
    const nextOptions = Object.assign({}, options, { from: nextFrom });
    deployments.push(
      ...await getDeploymentsByProjectId(
        client,
        projectId,
        nextOptions,
        total
      )
    );
  }
  return deployments;
}

// src/util/telemetry/commands/remove/index.ts
var RemoveTelemetryClient = class extends TelemetryClient {
  trackCliArgumentNameOrDeploymentId(values) {
    if (values) {
      this.trackCliArgument({
        arg: "nameOrDeploymentId",
        value: this.redactedArgumentsLength(values)
      });
    }
  }
  trackCliFlagHard(flag) {
    if (flag) {
      this.trackCliFlag("hard");
    }
  }
  trackCliFlagSafe(flag) {
    if (flag) {
      this.trackCliFlag("safe");
    }
  }
  trackCliFlagYes(flag) {
    if (flag) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/remove/index.ts
async function remove7(client) {
  const telemetryClient = new RemoveTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(removeCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error2) {
    printError(error2);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetryClient.trackCliFlagHelp("remove");
    output_manager_default.print(help(removeCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const ids = parsedArgs.args.slice(1);
  const hard = parsedArgs.flags["--hard"];
  const skipConfirmation = parsedArgs.flags["--yes"];
  const safe = parsedArgs.flags["--safe"];
  telemetryClient.trackCliArgumentNameOrDeploymentId(ids);
  telemetryClient.trackCliFlagSafe(safe);
  telemetryClient.trackCliFlagHard(hard);
  telemetryClient.trackCliFlagYes(skipConfirmation);
  const {
    config: { currentTeam }
  } = client;
  const { success, error, log } = output_manager_default;
  if (ids.length < 1) {
    error(`${getCommandName("rm")} expects at least one argument`);
    output_manager_default.print(help(removeCommand, { columns: client.stderr.columns }));
    return 1;
  }
  const invalidName = ids.find((name) => !isValidName(name));
  if (invalidName) {
    error(
      `The provided argument "${invalidName}" is not a valid deployment or project`
    );
    return 1;
  }
  const { contextName } = await getScope(client);
  output_manager_default.spinner(
    `Fetching deployment(s) ${ids.map((id) => `"${id}"`).join(" ")} in ${import_chalk175.default.bold(contextName)}`
  );
  let aliases;
  let projects;
  let deployments;
  const findStart = Date.now();
  try {
    const searchFilter = (d) => ids.some(
      (id) => d && !(d instanceof NowError) && (d.id === id || d.name === id || d.url === normalizeURL2(id))
    );
    const [deploymentList, projectList] = await Promise.all([
      Promise.all(
        ids.map(async (idOrHost) => {
          if (!contextName) {
            throw new Error("Context name is not defined");
          }
          return getDeployment(client, contextName, idOrHost).catch((err) => err);
        })
      ),
      Promise.all(
        ids.map(async (idOrName) => getProjectByNameOrId(client, idOrName))
      )
    ]);
    deployments = deploymentList.filter((d) => searchFilter(d));
    projects = projectList.filter((d) => searchFilter(d));
    if (safe) {
      const projectDeployments = await Promise.all(
        projects.map((project) => {
          return getDeploymentsByProjectId(client, project.id, {
            max: 201,
            continue: true
          });
        })
      );
      const to = Math.min(projectDeployments.length, 201);
      for (let i = 0; i < to; i++) {
        for (const pDepl of projectDeployments[i]) {
          const depl = pDepl;
          depl.aliases = [];
          deployments.push(depl);
        }
      }
      projects = [];
    } else {
      deployments = deployments.filter(
        (d) => !projects.some((p) => p.name === d.name)
      );
    }
    aliases = await Promise.all(
      deployments.map(async (depl) => {
        const { aliases: aliases2 } = await getAliases(client, depl.id);
        return aliases2;
      })
    );
  } finally {
    output_manager_default.stopSpinner();
  }
  deployments = deployments.filter((match, i) => {
    if (safe && aliases[i].length > 0) {
      return false;
    }
    match.aliases = aliases[i];
    return true;
  });
  if (deployments.length === 0 && projects.length === 0) {
    const safeUnaliased = parsedArgs.flags["--safe"] ? "unaliased" : "any";
    const stylizedIds = ids.map((id) => import_chalk175.default.bold(`"${id}"`)).join(", ");
    const commandName = getCommandName("projects ls");
    log(
      `Could not find ${safeUnaliased} deployments or projects matching ${stylizedIds}. Run ${commandName} to list.`
    );
    return 1;
  }
  log(
    `Found ${deploymentsAndProjects(deployments, projects)} for removal in ${import_chalk175.default.bold(contextName)} ${elapsed(Date.now() - findStart)}`
  );
  if (deployments.length > 200) {
    output_manager_default.warn(
      "Only 200 deployments can get deleted at once. Please continue 10 minutes after deletion to remove the rest."
    );
  }
  if (!skipConfirmation) {
    const confirmation = (await readConfirmation4(deployments, projects)).toLowerCase();
    if (confirmation !== "y" && confirmation !== "yes") {
      output_manager_default.log("Canceled");
      return 1;
    }
  }
  const now = new Now({
    client,
    currentTeam
  });
  const start = Date.now();
  await Promise.all([
    ...deployments.map((depl) => now.remove(depl.id, { hard })),
    ...projects.map((project) => removeProject(client, project.id))
  ]);
  success(
    `Removed ${deploymentsAndProjects(deployments, projects)} ${elapsed(Date.now() - start)}`
  );
  deployments.forEach((depl) => {
    output_manager_default.print(`${import_chalk175.default.gray("-")} ${import_chalk175.default.bold(depl.url)}
`);
  });
  projects.forEach((project) => {
    output_manager_default.print(`${import_chalk175.default.gray("-")} ${import_chalk175.default.bold(project.name)}
`);
  });
  return 0;
}
function readConfirmation4(deployments, projects) {
  return new Promise((resolve8) => {
    if (deployments.length > 0) {
      output_manager_default.log(
        `The following ${(0, import_pluralize11.default)(
          "deployment",
          deployments.length,
          deployments.length > 1
        )} will be permanently removed:`
      );
      const deploymentTable = table(
        deployments.map((depl) => {
          const time = import_chalk175.default.gray(`${(0, import_ms27.default)(Date.now() - depl.createdAt)} ago`);
          const url = depl.url ? import_chalk175.default.underline(`https://${depl.url}`) : "";
          return [`  ${depl.id}`, url, time];
        }),
        { align: ["l", "r", "l"], hsep: 6 }
      );
      output_manager_default.print(`${deploymentTable}
`);
    }
    for (const depl of deployments) {
      for (const { alias: alias2 } of depl.aliases) {
        output_manager_default.warn(
          `${import_chalk175.default.underline(`https://${alias2}`)} is an alias for ${import_chalk175.default.bold(depl.url)} and will be removed`
        );
      }
    }
    if (projects.length > 0) {
      output_manager_default.print(
        `The following ${(0, import_pluralize11.default)(
          "project",
          projects.length,
          projects.length > 1
        )} will be permanently removed, including all ${projects.length > 1 ? "their" : "its"} deployments and aliases:
`
      );
      for (const project of projects) {
        output_manager_default.print(`${import_chalk175.default.gray("-")} ${import_chalk175.default.bold(project.name)}
`);
      }
    }
    output_manager_default.print(
      `${import_chalk175.default.bold.red("> Are you sure?")} ${import_chalk175.default.gray("(y/N) ")}`
    );
    process.stdin.on("data", (d) => {
      process.stdin.pause();
      resolve8(d.toString().trim());
    }).resume();
  });
}
function deploymentsAndProjects(deployments, projects, conjunction = "and") {
  if (!projects || projects.length === 0) {
    return `${(0, import_pluralize11.default)("deployment", deployments.length, true)}`;
  }
  if (!deployments || deployments.length === 0) {
    return `${(0, import_pluralize11.default)("project", projects.length, true)}`;
  }
  return `${(0, import_pluralize11.default)("deployment", deployments.length, true)} ${conjunction} ${(0, import_pluralize11.default)("project", projects.length, true)}`;
}

// src/commands/rollback/index.ts
var import_error_utils17 = __toESM(require_dist(), 1);
var import_ms30 = __toESM(require_ms(), 1);

// src/commands/rollback/request-rollback.ts
var import_chalk177 = __toESM(require_source(), 1);
var import_ms29 = __toESM(require_ms(), 1);

// src/commands/rollback/status.ts
var import_chalk176 = __toESM(require_source(), 1);
var import_ms28 = __toESM(require_ms(), 1);
async function rollbackStatus({
  client,
  contextName,
  deployment,
  project,
  timeout = "3m"
}) {
  const recentThreshold = Date.now() - (0, import_ms28.default)("3m");
  const rollbackTimeout = Date.now() + (0, import_ms28.default)(timeout);
  let counter = 0;
  let spinnerMessage = deployment ? "Rollback in progress" : `Checking rollback status of ${project.name}`;
  if (!contextName) {
    ({ contextName } = await getScope(client));
  }
  try {
    output_manager_default.spinner(`${spinnerMessage}\u2026`);
    for (; ; ) {
      const projectCheck = await getProjectByNameOrId(
        client,
        project.id,
        project.accountId,
        true
      );
      if (projectCheck instanceof ProjectNotFound) {
        throw projectCheck;
      }
      const {
        jobStatus,
        requestedAt,
        toDeploymentId,
        type
      } = projectCheck.lastAliasRequest ?? {};
      if (!jobStatus || jobStatus !== "in-progress" && jobStatus !== "pending") {
        output_manager_default.stopSpinner();
        output_manager_default.log(`${spinnerMessage}\u2026`);
      }
      if (!jobStatus || !requestedAt || !toDeploymentId || requestedAt < recentThreshold) {
        output_manager_default.log("No deployment rollback in progress");
        return 0;
      }
      if (jobStatus === "skipped" && type === "rollback") {
        output_manager_default.log("Rollback was skipped");
        return 0;
      }
      if (jobStatus === "succeeded") {
        return await renderJobSucceeded2({
          client,
          contextName,
          performingRollback: !!deployment,
          requestedAt,
          project,
          toDeploymentId
        });
      }
      if (jobStatus === "failed") {
        return await renderJobFailed2({
          client,
          contextName,
          deployment,
          project,
          toDeploymentId
        });
      }
      if (jobStatus !== "pending" && jobStatus !== "in-progress") {
        output_manager_default.log(`Unknown rollback status "${jobStatus}"`);
        return 1;
      }
      if (requestedAt < recentThreshold || Date.now() >= rollbackTimeout) {
        output_manager_default.log(
          `The rollback exceeded its deadline - rerun ${import_chalk176.default.bold(
            `${packageName} rollback ${toDeploymentId}`
          )} to try again`
        );
        return 1;
      }
      if (counter++ === 0 && !deployment) {
        spinnerMessage += ` requested at ${formatDate(requestedAt)}`;
      }
      output_manager_default.spinner(`${spinnerMessage}\u2026`);
      await sleep(250);
    }
  } finally {
    output_manager_default.stopSpinner();
  }
}
async function renderJobFailed2({
  client,
  contextName,
  deployment,
  project,
  toDeploymentId
}) {
  try {
    const name = (deployment || await getDeployment(client, contextName, toDeploymentId))?.url;
    output_manager_default.error(
      `Failed to remap all aliases to the requested deployment ${name} (${toDeploymentId})`
    );
  } catch (_e) {
    output_manager_default.error(
      `Failed to remap all aliases to the requested deployment ${toDeploymentId}`
    );
  }
  let nextTimestamp;
  for (; ; ) {
    let url = `/v9/projects/${project.id}/rollback/aliases?failedOnly=true&limit=20`;
    if (nextTimestamp) {
      url += `&until=${nextTimestamp}`;
    }
    const { aliases, pagination } = await client.fetch(url);
    for (const { alias: alias2, status: status3 } of aliases) {
      output_manager_default.log(
        `  ${renderAliasStatus(status3).padEnd(11)}  ${alias2.alias} (${alias2.deploymentId})`
      );
    }
    if (pagination?.next) {
      nextTimestamp = pagination.next;
    } else {
      break;
    }
  }
  return 1;
}
async function renderJobSucceeded2({
  client,
  contextName,
  performingRollback,
  project,
  requestedAt,
  toDeploymentId
}) {
  let deploymentInfo = "";
  try {
    const deployment = await getDeployment(client, contextName, toDeploymentId);
    deploymentInfo = `${import_chalk176.default.bold(deployment.url)} (${toDeploymentId})`;
  } catch (err) {
    output_manager_default.debug(
      `Failed to get deployment url for ${toDeploymentId}: ${err?.toString() || err}`
    );
    deploymentInfo = import_chalk176.default.bold(toDeploymentId);
  }
  const duration = performingRollback ? elapsed(Date.now() - requestedAt) : "";
  output_manager_default.log(
    `Success! ${import_chalk176.default.bold(
      project.name
    )} was rolled back to ${deploymentInfo} ${duration}`
  );
  return 0;
}

// src/commands/rollback/request-rollback.ts
async function requestRollback({
  client,
  deployId,
  timeout
}) {
  const { contextName, deployment, project } = await getProjectByDeployment({
    client,
    deployId
  });
  await client.fetch(`/v9/projects/${project.id}/rollback/${deployment.id}`, {
    body: {},
    // required
    method: "POST"
  });
  if (timeout !== void 0 && (0, import_ms29.default)(timeout) === 0) {
    output_manager_default.log(
      `Successfully requested rollback of ${import_chalk177.default.bold(project.name)} to ${deployment.url} (${deployment.id})`
    );
    output_manager_default.log(`To check rollback status, run ${getCommandName("rollback")}.`);
    return 0;
  }
  return await rollbackStatus({
    client,
    contextName,
    deployment,
    project,
    timeout
  });
}

// src/util/telemetry/commands/rollback/index.ts
var RollbackTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrlOrDeploymentId(value) {
    if (value) {
      this.trackCliArgument({
        arg: "urlOrDeploymentId",
        value: this.redactedValue
      });
    }
  }
  trackCliSubcommandStatus() {
    this.trackCliSubcommand({
      subcommand: "status",
      value: "status"
    });
  }
  trackCliOptionTimeout(time) {
    if (time) {
      this.trackCliOption({
        option: "timeout",
        value: "[TIME]"
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/rollback/index.ts
var rollback_default = async (client) => {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(rollbackCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new RollbackTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  telemetry2.trackCliOptionTimeout(parsedArgs.flags["--timeout"]);
  telemetry2.trackCliFlagYes(parsedArgs.flags["--yes"]);
  const needHelp = parsedArgs.flags["--help"];
  if (!parsedArgs.args[1] && needHelp) {
    telemetry2.trackCliFlagHelp("rollback");
    output_manager_default.print(help(rollbackCommand, { columns: client.stderr.columns }));
    return 2;
  }
  const timeout = parsedArgs.flags["--timeout"];
  if (timeout && (0, import_ms30.default)(timeout) === void 0) {
    output_manager_default.error(`Invalid timeout "${timeout}"`);
    return 1;
  }
  const actionOrDeployId = parsedArgs.args[1] || "status";
  try {
    if (actionOrDeployId === "status") {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("rollback", "status");
        output_manager_default.print(
          help(statusSubcommand3, {
            columns: client.stderr.columns,
            parent: rollbackCommand
          })
        );
        return 2;
      }
      telemetry2.trackCliSubcommandStatus();
      const project = await getProjectByCwdOrLink({
        autoConfirm: parsedArgs.flags["--yes"],
        client,
        commandName: "rollback",
        projectNameOrId: parsedArgs.args[2]
      });
      return await rollbackStatus({
        client,
        project,
        timeout
      });
    }
    return await requestRollback({
      client,
      deployId: actionOrDeployId,
      timeout
    });
  } catch (err) {
    if ((0, import_error_utils17.isErrnoException)(err)) {
      if (err.code === "ERR_CANCELED") {
        return 0;
      }
      if (err.code === "ERR_INVALID_CWD" || err.code === "ERR_LINK_PROJECT") {
        return 1;
      }
    }
    output_manager_default.prettyError(err);
    return 1;
  }
};

// src/commands/rolling-release/start-rolling-release.ts
async function startRollingRelease({
  client,
  dpl,
  projectId,
  teamId,
  yes
}) {
  const { deployment } = await getProjectByDeployment({
    client,
    deployId: dpl
  });
  let promoteByCreation = false;
  if (deployment.target !== "production") {
    if (yes) {
      promoteByCreation = true;
    } else if (client.nonInteractive) {
      output_manager_default.error(
        "This deployment is not a production deployment. In non-interactive mode use --yes to create a new production deployment and start the rolling release."
      );
      return 1;
    } else {
      const question = "This deployment is not a production deployment and cannot be directly promoted. A new deployment will be built using your production environment. Are you sure you want to continue?";
      promoteByCreation = await client.input.confirm(question, false);
      if (!promoteByCreation) {
        output_manager_default.error("Canceled");
        return 0;
      }
    }
  }
  if (promoteByCreation) {
    const newDeployment = await client.fetch(
      `/v13/deployments?teamId=${deployment.ownerId}`,
      {
        body: {
          deploymentId: deployment.id,
          name: projectId,
          target: "production",
          meta: {
            action: "promote"
          }
        },
        accountId: deployment.ownerId,
        method: "POST"
      }
    );
    output_manager_default.log(
      `Successfully created new deployment at ${newDeployment.inspectorUrl}`
    );
    return 0;
  }
  await client.fetch(
    `/v10/projects/${projectId}/promote/${deployment.id}?teamId=${teamId}`,
    {
      body: {},
      // required
      json: false,
      method: "POST"
    }
  );
  output_manager_default.log("Successfully started rolling release");
  return 0;
}

// src/commands/rolling-release/configure-rolling-release.ts
var import_ms31 = __toESM(require_ms(), 1);
function parseDuration(value) {
  if (!value) {
    return void 0;
  }
  if (/^\d+(s|ms)$/i.test(value)) {
    return void 0;
  }
  const milliseconds = (0, import_ms31.default)(/^\d+$/.test(value) ? `${value}m` : value);
  if (milliseconds === void 0 || milliseconds <= 0) {
    return void 0;
  }
  const minutes = Math.round(milliseconds / 6e4);
  if (minutes <= 0) {
    return void 0;
  }
  return minutes;
}
function parseStageFlags(stageFlags, advancementType) {
  const stages = [];
  for (const stageValue of stageFlags) {
    const parts = stageValue.split(",");
    const percentage = parseInt(parts[0], 10);
    if (isNaN(percentage) || percentage < 1 || percentage > 99) {
      return {
        stages: null,
        error: `Invalid stage percentage "${parts[0]}". Must be a number between 1 and 99.`
      };
    }
    if (parts.length > 1) {
      if (advancementType === "manual-approval") {
        return {
          stages: null,
          error: 'Duration must not be provided for stages when advancement type is "manual-approval".'
        };
      }
      const duration = parseDuration(parts[1]);
      if (duration === void 0) {
        return {
          stages: null,
          error: `Invalid duration "${parts[1]}". Use minutes (e.g. "5m"), hours (e.g. "1h"), or days (e.g. "1d"). Seconds are not supported.`
        };
      }
      stages.push({ targetPercentage: percentage, duration });
    } else {
      if (advancementType === "automatic") {
        return {
          stages: null,
          error: 'Duration is required for each stage when advancement type is "automatic". Use the format "PERCENTAGE,DURATION" (e.g. "10,5m").'
        };
      }
      stages.push({ targetPercentage: percentage });
    }
  }
  for (let i = 1; i < stages.length; i++) {
    if (stages[i].targetPercentage <= stages[i - 1].targetPercentage) {
      return {
        stages: null,
        error: "Stage percentages must be in ascending order."
      };
    }
  }
  return { stages, error: null };
}
async function interactiveConfigure(client) {
  const action = await client.input.select({
    message: "Would you like to enable or disable rolling releases?",
    choices: [
      { name: "Enable", value: "enable" },
      { name: "Disable", value: "disable" }
    ]
  });
  if (action === "disable") {
    return { config: void 0 };
  }
  const advancementType = await client.input.select({
    message: "How should stages advance?",
    choices: [
      {
        name: "automatic - Stages advance automatically after a set duration",
        value: "automatic"
      },
      {
        name: "manual-approval - Each stage requires manual approval to advance",
        value: "manual-approval"
      }
    ]
  });
  const stages = [];
  let stageNumber = 1;
  let addMore = true;
  while (addMore) {
    const percentageStr = await client.input.text({
      message: `Enter traffic percentage for stage ${stageNumber} (1-99):`,
      validate: (val) => {
        const num = parseInt(val, 10);
        if (isNaN(num) || num < 1 || num > 99) {
          return "Percentage must be a number between 1 and 99.";
        }
        if (stages.length > 0 && num <= stages[stages.length - 1].targetPercentage) {
          return `Percentage must be greater than the previous stage (${stages[stages.length - 1].targetPercentage}%).`;
        }
        return true;
      }
    });
    const percentage = parseInt(percentageStr, 10);
    let duration;
    if (advancementType === "automatic") {
      const durationStr = await client.input.text({
        message: `Enter duration for this stage (e.g. 5m, 1h):`,
        validate: (val) => {
          const parsed = parseDuration(val);
          if (parsed === void 0) {
            return 'Invalid duration. Use minutes (e.g. "5m"), hours (e.g. "1h"), or days (e.g. "1d"). Seconds are not supported.';
          }
          return true;
        }
      });
      duration = parseDuration(durationStr);
    }
    stages.push({ targetPercentage: percentage, duration });
    stageNumber++;
    addMore = await client.input.confirm("Add another stage?", false);
  }
  output_manager_default.log("");
  output_manager_default.log("Rolling release configuration:");
  output_manager_default.log(`  Advancement: ${advancementType}`);
  stages.forEach((stage, i) => {
    const durationText = stage.duration !== void 0 ? ` for ${stage.duration} minutes` : "";
    output_manager_default.log(`  Stage ${i + 1}: ${stage.targetPercentage}%${durationText}`);
  });
  output_manager_default.log(`  Stage ${stages.length + 1}: 100% (final)`);
  output_manager_default.log("");
  const confirmed = await client.input.confirm(
    "Apply this configuration?",
    true
  );
  if (!confirmed) {
    output_manager_default.log("Configuration cancelled.");
    return { exitCode: 0 };
  }
  return {
    config: {
      enabled: true,
      advancementType,
      stages: [...stages, { targetPercentage: 100 }]
    }
  };
}
async function buildConfigurePayload({
  client,
  cfgString,
  enableFlag,
  disableFlag,
  advancementType,
  stageFlags
}) {
  if (cfgString !== void 0) {
    if (cfgString === "disable") {
      return { config: void 0 };
    }
    try {
      const cfg = JSON.parse(cfgString);
      return { config: cfg };
    } catch {
      output_manager_default.error("Invalid JSON provided for --cfg option.");
      return { exitCode: 1 };
    }
  }
  if (disableFlag) {
    if (enableFlag) {
      output_manager_default.error("--enable and --disable are mutually exclusive.");
      return { exitCode: 1 };
    }
    return { config: void 0 };
  }
  if (enableFlag) {
    if (advancementType !== "automatic" && advancementType !== "manual-approval") {
      output_manager_default.error(
        '--advancement-type is required when using --enable. Must be "automatic" or "manual-approval".'
      );
      return { exitCode: 1 };
    }
    if (!stageFlags || stageFlags.length === 0) {
      output_manager_default.error("At least one --stage is required when using --enable.");
      return { exitCode: 1 };
    }
    const parsed = parseStageFlags(stageFlags, advancementType);
    if (parsed.error !== null) {
      output_manager_default.error(parsed.error);
      return { exitCode: 1 };
    }
    return {
      config: {
        enabled: true,
        advancementType,
        stages: [...parsed.stages, { targetPercentage: 100 }]
      }
    };
  }
  const hasAnyFlags = advancementType !== void 0 || stageFlags !== void 0 && stageFlags.length > 0;
  if (hasAnyFlags) {
    output_manager_default.error(
      "--enable or --disable is required when using --advancement-type or --stage flags."
    );
    return { exitCode: 1 };
  }
  if (client.nonInteractive) {
    output_manager_default.error(
      "In non-interactive mode use --enable or --disable with --advancement-type and --stage as needed. Example: vercel rolling-release configure --enable --advancement-type=automatic --stage=10,5m --stage=50,10m"
    );
    return { exitCode: 1 };
  }
  if (client.stdin.isTTY) {
    return interactiveConfigure(client);
  }
  output_manager_default.error(
    "Missing configuration flags. Use --enable/--disable with --advancement-type and --stage, or run interactively in a terminal."
  );
  return { exitCode: 1 };
}
async function configureRollingRelease({
  client,
  projectId,
  teamId,
  rollingReleaseConfig
}) {
  const body = {
    ...rollingReleaseConfig,
    enabled: Boolean(rollingReleaseConfig)
  };
  await client.fetch(
    `/v1/projects/${projectId}/rolling-release/config?teamId=${teamId}`,
    {
      body,
      json: true,
      method: "PATCH"
    }
  );
  if (rollingReleaseConfig) {
    output_manager_default.log("Successfully configured rolling releases.");
  } else {
    output_manager_default.log("Successfully disabled rolling releases.");
  }
  return 0;
}

// src/commands/rolling-release/approve-rolling-release.ts
async function approveRollingRelease({
  client,
  projectId,
  teamId,
  activeStageIndex,
  dpl
}) {
  const { deployment } = await getProjectByDeployment({
    client,
    deployId: dpl
  });
  await client.fetch(
    `/v1/projects/${projectId}/rolling-release/approve-stage?teamId=${teamId}`,
    {
      method: "POST",
      json: true,
      body: {
        activeStageIndex,
        nextStageIndex: activeStageIndex + 1,
        canaryDeploymentId: deployment.id
      }
    }
  );
  output_manager_default.log(
    "Successfully requested approval of rolling release to next stage."
  );
  return 0;
}

// src/commands/rolling-release/abort-rolling-release.ts
async function abortRollingRelease({
  client,
  projectId,
  dpl,
  teamId
}) {
  const { deployment } = await getProjectByDeployment({
    client,
    deployId: dpl
  });
  await client.fetch(
    `/v9/projects/${projectId}/rollback/${deployment.id}?teamId=${teamId}`,
    {
      body: {},
      // required
      method: "POST"
    }
  );
  output_manager_default.log("Successfully aborted the active rolling release.");
  return 0;
}

// src/commands/rolling-release/complete-rolling-release.ts
async function completeRollingRelease({
  client,
  projectId,
  teamId,
  dpl
}) {
  const { deployment } = await getProjectByDeployment({
    client,
    deployId: dpl
  });
  await client.fetch(
    `/v1/projects/${projectId}/rolling-release/complete?teamId=${teamId}`,
    {
      body: { canaryDeploymentId: deployment.id },
      // required
      json: true,
      method: "POST"
    }
  );
  output_manager_default.log(
    "Successfully requested completion of the active rolling release."
  );
  return 0;
}

// src/util/telemetry/commands/rolling-release/index.ts
var RollingReleaseTelemetryClient = class extends TelemetryClient {
  trackCliOptionAction(value) {
    this.trackCliOption({
      option: "action",
      value: value ?? ""
    });
  }
  trackCliOptionName(name) {
    if (name) {
      this.trackCliOption({
        option: "name",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionCfg(value) {
    this.trackCliOption({
      option: "cfg",
      value: value ?? ""
    });
  }
  trackCliOptionDpl(value) {
    this.trackCliOption({
      option: "dpl",
      value: value ?? ""
    });
  }
  trackCliOptionCurrentStageIndex(value) {
    this.trackCliOption({
      option: "currentStageIndex",
      value: value ?? ""
    });
  }
  trackCliSubcommandConfigure(actual) {
    this.trackCliSubcommand({
      subcommand: "configure",
      value: actual
    });
  }
  trackCliSubcommandStart(actual) {
    this.trackCliSubcommand({
      subcommand: "start",
      value: actual
    });
  }
  trackCliSubcommandApprove(actual) {
    this.trackCliSubcommand({
      subcommand: "approve",
      value: actual
    });
  }
  trackCliSubcommandAbort(actual) {
    this.trackCliSubcommand({
      subcommand: "abort",
      value: actual
    });
  }
  trackCliSubcommandComplete(actual) {
    this.trackCliSubcommand({
      subcommand: "complete",
      value: actual
    });
  }
  trackCliSubcommandFetch(actual) {
    this.trackCliSubcommand({
      subcommand: "fetch",
      value: actual
    });
  }
  trackCliFlagEnable(value) {
    if (value) {
      this.trackCliFlag("enable");
    }
  }
  trackCliFlagDisable(value) {
    if (value) {
      this.trackCliFlag("disable");
    }
  }
  trackCliOptionAdvancementType(value) {
    if (value) {
      this.trackCliOption({
        option: "advancement-type",
        value
      });
    }
  }
  trackCliOptionStage(value) {
    if (value) {
      this.trackCliOption({
        option: "stage",
        value: value.join(",")
      });
    }
  }
};

// src/commands/rolling-release/index.ts
var COMMAND_CONFIG34 = {
  configure: getCommandAliases(configureSubcommand),
  start: getCommandAliases(startSubcommand),
  approve: getCommandAliases(approveSubcommand),
  abort: getCommandAliases(abortSubcommand),
  complete: getCommandAliases(completeSubcommand),
  fetch: getCommandAliases(fetchSubcommand)
};
function buildDeploymentSuggestionCommands(client, subcmd) {
  const args = client.argv.slice(2);
  const preservedParts = [];
  let hasNonInteractive = false;
  for (let i = 2; i < args.length; i++) {
    const arg = args[i];
    if (arg === "--non-interactive") {
      hasNonInteractive = true;
      continue;
    }
    if (arg.startsWith("--cwd=")) {
      const cwdPath = arg.slice(6);
      if (cwdPath) {
        preservedParts.push("--cwd", cwdPath);
      }
      continue;
    }
    if (arg === "--cwd") {
      if (i + 1 < args.length) {
        preservedParts.push("--cwd", args[i + 1]);
        i++;
      }
      continue;
    }
    preservedParts.push(arg);
  }
  const preservedSuffix = preservedParts.join(" ");
  const listCommand = preservedSuffix ? `${packageName} ls ${preservedSuffix}` : `${packageName} ls`;
  const base = preservedSuffix ? `${packageName} rolling-release ${subcmd} ${preservedSuffix}` : `${packageName} rolling-release ${subcmd}`;
  const defaultSuffix = subcmd === "approve" ? "--dpl dpl_123 --currentStageIndex=0" : "--dpl dpl_123";
  const subcommandCommand = hasNonInteractive ? `${base} ${defaultSuffix} --non-interactive` : `${base} ${defaultSuffix}`;
  return { listCommand, subcommandCommand };
}
function outputRollingReleaseErrorWithDeploymentSuggestions(client, subcmd, reason, message) {
  const { listCommand, subcommandCommand } = buildDeploymentSuggestionCommands(
    client,
    subcmd
  );
  outputAgentError(
    client,
    {
      status: "error",
      reason,
      message,
      next: [{ command: listCommand }, { command: subcommandCommand }]
    },
    1
  );
}
async function rollingRelease(client) {
  const telemetry2 = new RollingReleaseTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const {
    subcommand,
    args: subcommandArgs,
    subcommandOriginal
  } = getSubcommand5(client.argv.slice(3), COMMAND_CONFIG34);
  const needHelp = client.argv.includes("--help") || client.argv.includes("-h");
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("rolling-release");
    output_manager_default.print(
      help(rollingReleaseCommand, { columns: client.stderr.columns })
    );
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: rollingReleaseCommand,
        columns: client.stderr.columns
      })
    );
  }
  try {
    const link = await getLinkedProject(client);
    if (link.status === "error") {
      return link.exitCode;
    }
    if (link.status === "not_linked") {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "not_linked",
            message: "No project found for rolling releases. Link your project first.",
            next: [{ command: `${packageName} link` }]
          },
          1
        );
      }
      output_manager_default.error(
        "No project found. Please run `vc link` to link your project first."
      );
      return 1;
    }
    const { project, org } = link;
    client.config.currentTeam = org.type === "team" ? org.id : void 0;
    let subcommandFlags;
    switch (subcommand) {
      case "configure": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(configureSubcommand);
          return 2;
        }
        subcommandFlags = parseArguments(
          subcommandArgs,
          getFlagsSpecification(configureSubcommand.options)
        );
        const cfgString = subcommandFlags.flags["--cfg"];
        const enableFlag = subcommandFlags.flags["--enable"];
        const disableFlag = subcommandFlags.flags["--disable"];
        const advancementType = subcommandFlags.flags["--advancement-type"];
        const stageFlags = subcommandFlags.flags["--stage"];
        telemetry2.trackCliFlagEnable(enableFlag);
        telemetry2.trackCliFlagDisable(disableFlag);
        telemetry2.trackCliOptionAdvancementType(advancementType);
        telemetry2.trackCliOptionStage(stageFlags);
        const configResult = await buildConfigurePayload({
          client,
          cfgString,
          enableFlag,
          disableFlag,
          advancementType,
          stageFlags
        });
        if (configResult.exitCode !== void 0) {
          return configResult.exitCode;
        }
        await configureRollingRelease({
          client,
          projectId: project.id,
          teamId: org.id,
          rollingReleaseConfig: configResult.config
        });
        break;
      }
      case "start": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(startSubcommand);
          return 2;
        }
        subcommandFlags = parseArguments(
          subcommandArgs,
          getFlagsSpecification(startSubcommand.options)
        );
        const dpl = subcommandFlags.flags["--dpl"];
        if (dpl === void 0) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "start",
              "missing_flags",
              "Starting a rolling release in non-interactive mode requires the --dpl flag."
            );
          }
          output_manager_default.error("starting a rolling release requires --dpl option.");
          return 1;
        }
        try {
          await startRollingRelease({
            client,
            dpl,
            projectId: project.id,
            teamId: project.accountId,
            yes: subcommandFlags.flags["--yes"] ?? false
          });
        } catch (err) {
          if (client.nonInteractive && isAPIError(err)) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "start",
              "api_error",
              err.message || "Starting the rolling release failed for this deployment."
            );
            return 1;
          }
          throw err;
        }
        break;
      }
      case "approve": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(approveSubcommand);
          return 2;
        }
        subcommandFlags = parseArguments(
          subcommandArgs,
          getFlagsSpecification(approveSubcommand.options)
        );
        const dpl = subcommandFlags.flags["--dpl"];
        const currentStageIndex = subcommandFlags.flags["--currentStageIndex"];
        const activeStageIndex = parseInt(currentStageIndex ?? "");
        if (!dpl) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "approve",
              "missing_flags",
              "Approving a rolling release in non-interactive mode requires --dpl and --currentStageIndex."
            );
          }
          output_manager_default.error("Missing required flag --dpl");
          return 1;
        }
        if (currentStageIndex === void 0) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "approve",
              "missing_flags",
              "Approving a rolling release in non-interactive mode requires --currentStageIndex."
            );
          }
          output_manager_default.error("Missing required flag --currentStageIndex");
          return 1;
        }
        if (isNaN(activeStageIndex)) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "approve",
              "invalid_flag",
              "--currentStageIndex must be a valid number."
            );
          }
          output_manager_default.error("--currentStageIndex must be a valid number.");
          return 1;
        }
        await approveRollingRelease({
          client,
          projectId: project.id,
          teamId: org.id,
          activeStageIndex,
          dpl
        });
        break;
      }
      case "abort": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(abortSubcommand);
          return 2;
        }
        subcommandFlags = parseArguments(
          subcommandArgs,
          getFlagsSpecification(abortSubcommand.options)
        );
        const dpl = subcommandFlags.flags["--dpl"];
        if (!dpl) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "abort",
              "missing_flags",
              "Aborting a rolling release in non-interactive mode requires the --dpl flag."
            );
          }
          output_manager_default.error("Missing required flag --dpl");
          return 1;
        }
        try {
          await abortRollingRelease({
            client,
            projectId: project.id,
            teamId: org.id,
            dpl
          });
        } catch (err) {
          if (client.nonInteractive && isAPIError(err)) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "abort",
              "api_error",
              err.message || "Aborting the rolling release failed for this deployment."
            );
            return 1;
          }
          throw err;
        }
        break;
      }
      case "complete": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(completeSubcommand);
          return 2;
        }
        subcommandFlags = parseArguments(
          subcommandArgs,
          getFlagsSpecification(completeSubcommand.options)
        );
        const dpl = subcommandFlags.flags["--dpl"];
        if (!dpl) {
          if (client.nonInteractive) {
            outputRollingReleaseErrorWithDeploymentSuggestions(
              client,
              "complete",
              "missing_flags",
              "Completing a rolling release in non-interactive mode requires the --dpl flag."
            );
          }
          output_manager_default.error("Missing required flag --dpl");
          return 1;
        }
        await completeRollingRelease({
          client,
          projectId: project.id,
          teamId: org.id,
          dpl
        });
        break;
      }
      case "fetch": {
        if (needHelp) {
          telemetry2.trackCliFlagHelp("rolling-release", subcommandOriginal);
          printHelp(fetchSubcommand);
          return 2;
        }
        const result = await requestRollingRelease({
          client,
          projectId: project.id,
          teamId: org.id
        });
        output_manager_default.log(JSON.stringify(result, null, 2));
        break;
      }
      default: {
        output_manager_default.debug(`Invalid subcommand: ${subcommand}`);
        output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG34));
        output_manager_default.print(
          help(rollingReleaseCommand, { columns: client.stderr.columns })
        );
        return 2;
      }
    }
    return 0;
  } catch (err) {
    if (client.nonInteractive && isAPIError(err)) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "api_error",
          message: err.message || "Rolling release command failed."
        },
        1
      );
    }
    printError(err);
    return 1;
  }
}

// src/commands/routes/list.ts
var import_chalk178 = __toESM(require_source(), 1);
var import_pluralize12 = __toESM(require_pluralize(), 1);
async function list12(client, argv) {
  const parsed = await parseSubcommandArgs(argv, listSubcommand14, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const { flags } = parsed;
  const teamId = org.type === "team" ? org.id : void 0;
  const search2 = flags["--search"];
  const filter = flags["--filter"];
  const production = flags["--production"];
  const versionIdFlag = flags["--version-id"];
  const diffFlag = flags["--diff"];
  const expand = flags["--expand"];
  if (filter) {
    const validFilters = [
      "rewrite",
      "redirect",
      "set_status",
      "transform"
    ];
    if (!validFilters.includes(filter)) {
      const msg = `Invalid filter type "${filter}". Valid types: ${validFilters.join(", ")}`;
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "invalid_arguments",
          message: msg,
          next: [{ command: withGlobalFlags(client, "routes list") }]
        });
        process.exit(1);
      }
      output_manager_default.error(msg);
      return 1;
    }
  }
  if (production && versionIdFlag) {
    const msg = "Cannot use both --production and --version-id flags together";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "invalid_arguments",
        message: msg,
        next: [{ command: withGlobalFlags(client, "routes list") }]
      });
      process.exit(1);
    }
    output_manager_default.error(msg);
    return 1;
  }
  if (production && diffFlag) {
    const msg = "Cannot use both --production and --diff flags together. --diff compares staged changes against production.";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: "error",
        reason: "invalid_arguments",
        message: msg,
        next: [{ command: withGlobalFlags(client, "routes list --diff") }]
      });
      process.exit(1);
    }
    output_manager_default.error(msg);
    return 1;
  }
  let versionId;
  let versionName;
  let useDiff = false;
  if (production) {
    output_manager_default.spinner("Fetching production version");
    const { versions } = await getRouteVersions(client, project.id, {
      teamId
    });
    const productionVersion = versions.find((v) => !v.isStaging);
    if (!productionVersion) {
      const msg = `No production version found for ${project.name}.`;
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "not_found",
          message: msg,
          next: [{ command: withGlobalFlags(client, "routes list") }]
        });
        process.exit(1);
      }
      output_manager_default.error(
        `No production version found for ${import_chalk178.default.bold(project.name)}.`
      );
      return 1;
    }
    versionId = productionVersion.id;
    versionName = productionVersion.id;
  }
  if (diffFlag && !versionIdFlag) {
    output_manager_default.spinner("Fetching staging version");
    const { versions } = await getRouteVersions(client, project.id, {
      teamId
    });
    const stagingVersion = versions.find((v) => v.isStaging);
    if (!stagingVersion) {
      const msg = `No staged changes to diff. Run ${getCommandName("routes add")} or ${getCommandName("routes edit")} to make changes.`;
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "not_found",
          message: msg,
          next: [
            { command: withGlobalFlags(client, "routes list") },
            { command: withGlobalFlags(client, "routes add") }
          ]
        });
        process.exit(1);
      }
      output_manager_default.error(
        `No staged changes to diff. Run ${import_chalk178.default.cyan(
          getCommandName("routes add")
        )} or ${import_chalk178.default.cyan(getCommandName("routes edit"))} to make changes.`
      );
      return 1;
    }
    versionId = stagingVersion.id;
    versionName = stagingVersion.id;
    useDiff = true;
  }
  if (versionIdFlag) {
    output_manager_default.spinner("Fetching version");
    const { versions } = await getRouteVersions(client, project.id, {
      teamId
    });
    const result = findVersionById(versions, versionIdFlag);
    if (result.error || !result.version) {
      const msg = result.error ?? "Version not found";
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "not_found",
          message: msg,
          next: [{ command: withGlobalFlags(client, "routes list-versions") }]
        });
        process.exit(1);
      }
      output_manager_default.error(msg);
      return 1;
    }
    versionId = result.version.id;
    versionName = result.version.id;
    if (diffFlag) {
      useDiff = true;
    }
  }
  const lsStamp = stamp_default();
  let spinnerMessage = `Fetching routes for ${import_chalk178.default.bold(project.name)}`;
  if (versionName) {
    spinnerMessage += ` (version: ${versionName})`;
  }
  if (search2) {
    spinnerMessage += ` matching "${search2}"`;
  }
  if (filter) {
    spinnerMessage += ` filtered by ${filter}`;
  }
  output_manager_default.spinner(spinnerMessage);
  const { routes: routes2, version, limit } = await getRoutes(client, project.id, {
    teamId,
    search: search2,
    filter,
    versionId,
    diff: useDiff
  });
  if (useDiff) {
    const added = routes2.filter((r) => r.action === "+");
    const removed = routes2.filter((r) => r.action === "-");
    const modified = routes2.filter((r) => r.action === "~");
    const unchanged = routes2.filter((r) => !r.action);
    output_manager_default.log(
      `Changes in staging version ${import_chalk178.default.bold(versionName || "")} ${import_chalk178.default.gray(lsStamp())}`
    );
    if (added.length === 0 && removed.length === 0 && modified.length === 0) {
      output_manager_default.log("\n  No changes from production version\n");
    } else {
      if (added.length > 0) {
        output_manager_default.print(
          `
  ${import_chalk178.default.bold(import_chalk178.default.green(`Added (${added.length}):`))}
`
        );
        output_manager_default.print(formatRoutesTable(added, "+"));
      }
      if (modified.length > 0) {
        output_manager_default.print(
          `
  ${import_chalk178.default.bold(import_chalk178.default.yellow(`Modified (${modified.length}):`))}
`
        );
        output_manager_default.print(formatRoutesTable(modified, "~"));
      }
      if (removed.length > 0) {
        output_manager_default.print(
          `
  ${import_chalk178.default.bold(import_chalk178.default.red(`Removed (${removed.length}):`))}
`
        );
        output_manager_default.print(formatRoutesTable(removed, "-"));
      }
      if (unchanged.length > 0) {
        output_manager_default.print(
          `
  ${import_chalk178.default.gray(`${unchanged.length} route${unchanged.length === 1 ? "" : "s"} unchanged`)}
`
        );
      }
      output_manager_default.print("\n");
    }
    if (version?.alias) {
      output_manager_default.log(
        `Test your changes: ${import_chalk178.default.cyan(`https://${version.alias}`)}`
      );
    }
  } else {
    let resultMessage = `${(0, import_pluralize12.default)("Route", routes2.length, true)} found for ${import_chalk178.default.bold(
      project.name
    )}`;
    if (versionName) {
      resultMessage += ` ${import_chalk178.default.gray(`(version: ${versionName})`)}`;
    }
    if (search2) {
      resultMessage += ` matching "${search2}"`;
    }
    if (filter) {
      resultMessage += ` filtered by ${filter}`;
    }
    resultMessage += ` ${import_chalk178.default.gray(lsStamp())}`;
    output_manager_default.log(resultMessage);
    if (routes2.length > 0) {
      if (expand) {
        output_manager_default.print(formatExpandedRoutes(routes2));
      } else {
        output_manager_default.print(formatRoutesTable(routes2));
      }
      output_manager_default.print("\n");
    }
    if (limit && limit.maxRoutes > 0) {
      const utilization = limit.currentRoutes / limit.maxRoutes;
      if (utilization >= 0.8) {
        const usageColor = utilization >= 1 ? import_chalk178.default.red : import_chalk178.default.yellow;
        output_manager_default.print(
          `  ${usageColor(`Routes Usage: ${limit.currentRoutes}/${limit.maxRoutes}`)}

`
        );
      }
    }
  }
  return 0;
}
function formatRoutesTable(routes2, actionSymbol) {
  const rows = routes2.map((rule, index) => {
    const typeLabels = getRouteTypeLabel(rule);
    const status3 = rule.enabled === false ? import_chalk178.default.red("Disabled") : import_chalk178.default.green("Enabled");
    const prefix = actionSymbol || "";
    const colorFn = actionSymbol === "+" ? import_chalk178.default.green : actionSymbol === "-" ? import_chalk178.default.red : actionSymbol === "~" ? import_chalk178.default.yellow : (s) => s;
    const position = actionSymbol ? "" : `${index + 1}`;
    return [
      colorFn(`${prefix} ${position}`).trim(),
      colorFn(
        truncate(rule.name, 30) + (rule.staged ? import_chalk178.default.yellow(" (draft)") : "")
      ),
      colorFn(truncate(rule.route.src, 40)),
      colorFn(typeLabels),
      status3
    ];
  });
  return formatTable(
    ["#", "Name", "Path Pattern", "Actions", "Status"],
    ["r", "l", "l", "l", "l"],
    [{ rows }]
  );
}
function truncate(str, maxLength) {
  if (str.length <= maxLength)
    return str;
  return str.slice(0, maxLength - 3) + "...";
}
function formatExpandedRoutes(routes2) {
  const lines = [""];
  routes2.forEach((rule, index) => {
    const typeLabels = getRouteTypeLabel(rule);
    const syntaxLabel = getSrcSyntaxLabel(rule);
    const statusText = rule.enabled === false ? import_chalk178.default.red("Disabled") : import_chalk178.default.green("Enabled");
    const stagedText = rule.staged ? import_chalk178.default.yellow(" (staged)") : "";
    lines.push(
      `  ${import_chalk178.default.bold(`${index + 1}. ${rule.name}`)}${stagedText}  ${import_chalk178.default.gray(`[${rule.id}]`)}`
    );
    lines.push(`     ${import_chalk178.default.cyan("Status:")}  ${statusText}`);
    lines.push(`     ${import_chalk178.default.cyan("Type:")}    ${typeLabels}`);
    if (rule.description) {
      lines.push(`     ${import_chalk178.default.cyan("Desc:")}    ${rule.description}`);
    }
    lines.push(`     ${import_chalk178.default.cyan("Source:")}  ${rule.route.src}`);
    lines.push(`     ${import_chalk178.default.cyan("Syntax:")}  ${syntaxLabel}`);
    if (rule.route.dest) {
      lines.push(`     ${import_chalk178.default.cyan("Dest:")}    ${rule.route.dest}`);
    }
    if (rule.route.status) {
      lines.push(`     ${import_chalk178.default.cyan("Code:")}    ${rule.route.status}`);
    }
    if (rule.route.headers && Object.keys(rule.route.headers).length > 0) {
      lines.push(`     ${import_chalk178.default.cyan("Response Headers:")}`);
      for (const [key, value] of Object.entries(rule.route.headers)) {
        lines.push(`       ${key}: ${value}`);
      }
    }
    if (rule.route.transforms && rule.route.transforms.length > 0) {
      lines.push(`     ${import_chalk178.default.cyan("Transforms:")}`);
      for (const transform of rule.route.transforms) {
        lines.push(`       ${formatTransform(transform)}`);
      }
    }
    if (rule.route.has && rule.route.has.length > 0) {
      lines.push(`     ${import_chalk178.default.cyan("Has conditions:")}`);
      for (const condition of rule.route.has) {
        lines.push(`       ${formatCondition(condition)}`);
      }
    }
    if (rule.route.missing && rule.route.missing.length > 0) {
      lines.push(`     ${import_chalk178.default.cyan("Does not have conditions:")}`);
      for (const condition of rule.route.missing) {
        lines.push(`       ${formatCondition(condition)}`);
      }
    }
    lines.push("");
  });
  return lines.join("\n");
}

// src/commands/routes/list-versions.ts
var import_chalk179 = __toESM(require_source(), 1);
var import_ms32 = __toESM(require_ms(), 1);
async function listVersions2(client, argv) {
  const parsed = await parseSubcommandArgs(
    argv,
    listVersionsSubcommand2,
    client
  );
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const { flags } = parsed;
  const teamId = org.type === "team" ? org.id : void 0;
  const count = flags["--count"];
  if (count !== void 0) {
    if (count < 1 || count > 100) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: "error",
          reason: "invalid_arguments",
          message: "Count must be between 1 and 100",
          next: [
            {
              command: withGlobalFlags(
                client,
                "routes list-versions --count 20"
              )
            }
          ]
        });
        process.exit(1);
      }
      output_manager_default.error("Count must be between 1 and 100");
      return 1;
    }
  }
  const lsStamp = stamp_default();
  output_manager_default.spinner(`Fetching route versions for ${import_chalk179.default.bold(project.name)}`);
  const { versions } = await getRouteVersions(client, project.id, {
    teamId,
    count
  });
  output_manager_default.log(
    `Route versions for ${import_chalk179.default.bold(project.name)} ${import_chalk179.default.gray(lsStamp())}`
  );
  if (versions.length === 0) {
    output_manager_default.log("\n  No versions found\n");
    return 0;
  }
  output_manager_default.print(formatVersionsTable2(versions));
  output_manager_default.print("\n");
  return 0;
}
function formatVersionsTable2(versions) {
  const rows = versions.map((version) => {
    let status3 = "";
    if (version.isStaging) {
      status3 = import_chalk179.default.yellow("Staging");
    } else if (version.isLive) {
      status3 = import_chalk179.default.green("Live");
    } else {
      status3 = import_chalk179.default.gray("Previous");
    }
    const id = version.id.slice(0, 12);
    const routeCount = version.ruleCount !== void 0 ? version.ruleCount.toString() : "-";
    const age = getRelativeTime(version.lastModified);
    return [status3, id, routeCount, age];
  });
  return formatTable(
    ["Status", "ID", "Routes", "Age"],
    ["l", "l", "r", "l"],
    [{ rows }]
  );
}
function getRelativeTime(timestamp) {
  const now = Date.now();
  const diff2 = now - timestamp;
  if (diff2 < 1e3) {
    return "just now";
  }
  return (0, import_ms32.default)(diff2, { long: true }) + " ago";
}

// src/commands/routes/inspect.ts
var import_chalk180 = __toESM(require_source(), 1);
function withGlobalFlags7(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
async function inspect6(client, argv) {
  const parsed = await parseSubcommandArgs(argv, inspectSubcommand5, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const { flags, args } = parsed;
  const teamId = org.type === "team" ? org.id : void 0;
  const showDiff = flags["--diff"];
  const identifier = args[0] === "inspect" ? args[1] : args[0];
  if (!identifier) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "Route name or ID is required. Pass a single route name or ID as the first argument after inspect.",
          next: [
            {
              command: withGlobalFlags7(client, "routes inspect <name-or-id>"),
              when: "replace <name-or-id> with route name or ID from routes list"
            },
            {
              command: withGlobalFlags7(client, "routes list"),
              when: "to list routes and copy an exact name or id"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `Missing route name or ID. Usage: ${import_chalk180.default.cyan(getCommandName("routes inspect <name-or-id>"))}`
    );
    return 1;
  }
  const inspectStamp = stamp_default();
  output_manager_default.spinner(
    `Searching for route "${identifier}" in ${import_chalk180.default.bold(project.name)}`
  );
  const { routes: routes2 } = await getRoutes(client, project.id, {
    teamId,
    search: identifier
  });
  const exactMatch = routes2.find(
    (r) => r.id === identifier || r.name.toLowerCase() === identifier.toLowerCase()
  );
  let route;
  if (exactMatch) {
    route = exactMatch;
  } else if (routes2.length === 0) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_FOUND,
          message: `No route found matching "${identifier}".`,
          next: [
            {
              command: withGlobalFlags7(client, "routes list"),
              when: "to list routes and pick an exact name or id"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.error(
      `No route found matching "${identifier}". Run ${import_chalk180.default.cyan(
        getCommandName("routes list")
      )} to see all routes.`
    );
    return 1;
  } else if (routes2.length === 1) {
    route = routes2[0];
  } else {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.AMBIGUOUS_ROUTE,
          message: `Multiple routes match "${identifier}" (${routes2.length} matches). Pass an exact route name or ID; non-interactive mode cannot prompt for a choice.`,
          next: [
            {
              command: withGlobalFlags7(client, "routes list"),
              when: "to see route names and ids, then inspect with a unique id"
            },
            {
              command: withGlobalFlags7(client, "routes inspect <name-or-id>"),
              when: "retry with a single unique name or id"
            }
          ]
        },
        1
      );
      return 1;
    }
    output_manager_default.log(
      `Found ${routes2.length} routes matching "${identifier}" ${import_chalk180.default.gray(inspectStamp())}`
    );
    const selectedId = await client.input.select({
      message: "Select a route to inspect:",
      choices: routes2.map((r) => ({
        value: r.id,
        name: `${r.name} ${import_chalk180.default.gray(`(${r.route.src})`)}`
      }))
    });
    if (!selectedId) {
      output_manager_default.error("No route selected");
      return 1;
    }
    const selected = routes2.find((r) => r.id === selectedId);
    if (!selected) {
      output_manager_default.error("Selected route not found");
      return 1;
    }
    route = selected;
  }
  output_manager_default.log(
    `Route found in ${import_chalk180.default.bold(project.name)} ${import_chalk180.default.gray(inspectStamp())}`
  );
  if (showDiff) {
    return await showRouteDiff(client, project.id, teamId, route);
  }
  output_manager_default.print(formatRouteDetails(route));
  return 0;
}
async function showRouteDiff(client, projectId, teamId, stagingRoute) {
  const { versions } = await getRouteVersions(client, projectId, { teamId });
  const productionVersion = versions.find((v) => v.isLive);
  if (!productionVersion) {
    output_manager_default.log("No production version found. Showing current route.");
    output_manager_default.print(formatRouteDetails(stagingRoute));
    return 0;
  }
  const { routes: productionRoutes } = await getRoutes(client, projectId, {
    teamId,
    versionId: productionVersion.id
  });
  const productionRoute = productionRoutes.find((r) => r.id === stagingRoute.id);
  if (!productionRoute) {
    output_manager_default.print(
      `
  ${import_chalk180.default.bold(stagingRoute.name)} ${import_chalk180.default.green("(new)")}
`
    );
    output_manager_default.print(`  ${import_chalk180.default.gray(stagingRoute.id)}
`);
    output_manager_default.print(
      `  ${import_chalk180.default.green("This route does not exist in production yet.")}
`
    );
    output_manager_default.print(formatRouteDetails(stagingRoute));
    return 0;
  }
  output_manager_default.print(formatRouteDiff(stagingRoute, productionRoute));
  return 0;
}
function diffField(label, oldVal, newVal) {
  if (oldVal === newVal) {
    if (!newVal)
      return "";
    return `  ${import_chalk180.default.cyan(label)} ${newVal}`;
  }
  if (!oldVal && newVal) {
    return `${import_chalk180.default.green("+")} ${import_chalk180.default.cyan(label)} ${newVal}`;
  }
  if (oldVal && !newVal) {
    return `${import_chalk180.default.red("-")} ${import_chalk180.default.cyan(label)} ${import_chalk180.default.strikethrough(oldVal)}`;
  }
  return `${import_chalk180.default.yellow("~")} ${import_chalk180.default.cyan(label)} ${import_chalk180.default.red(oldVal)} \u2192 ${import_chalk180.default.green(newVal)}`;
}
function formatRouteDiff(staging, production) {
  const lines = [""];
  const hasChanges = JSON.stringify(normalizeForComparison(staging)) !== JSON.stringify(normalizeForComparison(production));
  if (!hasChanges) {
    lines.push(`  ${import_chalk180.default.bold(staging.name)}`);
    lines.push(`  ${import_chalk180.default.gray(staging.id)}`);
    lines.push("");
    lines.push(`  ${import_chalk180.default.gray("No staged changes for this route.")}`);
    lines.push("");
    lines.push(import_chalk180.default.bold("  Route Configuration"));
    return lines.join("\n") + "\n" + formatRouteDetails(staging);
  }
  const typeLabels = getRouteTypeLabel(staging);
  const syntaxLabel = getSrcSyntaxLabel(staging);
  lines.push(`  ${import_chalk180.default.bold(staging.name)} ${import_chalk180.default.yellow("(modified)")}`);
  lines.push(`  ${import_chalk180.default.gray(staging.id)}`);
  lines.push("");
  if (production.description !== staging.description) {
    if (!production.description && staging.description) {
      lines.push(
        `${import_chalk180.default.green("+")} ${import_chalk180.default.cyan("Description:")}  ${staging.description}`
      );
    } else if (production.description && !staging.description) {
      lines.push(
        `${import_chalk180.default.red("-")} ${import_chalk180.default.cyan("Description:")}  ${import_chalk180.default.strikethrough(production.description)}`
      );
    } else {
      lines.push(
        `${import_chalk180.default.yellow("~")} ${import_chalk180.default.cyan("Description:")}  ${import_chalk180.default.red(production.description)} \u2192 ${import_chalk180.default.green(staging.description)}`
      );
    }
    lines.push("");
  } else if (staging.description) {
    lines.push(`  ${staging.description}`);
    lines.push("");
  }
  const prodStatusText = production.enabled === false ? "Disabled" : "Enabled";
  const stagingStatusText = staging.enabled === false ? "Disabled" : "Enabled";
  lines.push(
    diffField("Status:", prodStatusText, stagingStatusText) || `  ${import_chalk180.default.cyan("Status:")}      ${stagingStatusText}`
  );
  lines.push(`  ${import_chalk180.default.cyan("Type:")}        ${typeLabels}`);
  lines.push("");
  lines.push(import_chalk180.default.bold("  Route Configuration"));
  const srcLine = diffField("Source:", production.route.src, staging.route.src);
  lines.push(srcLine || `  ${import_chalk180.default.cyan("Source:")}      ${staging.route.src}`);
  lines.push(`  ${import_chalk180.default.cyan("Syntax:")}      ${syntaxLabel}`);
  const destLine = diffField(
    "Destination:",
    production.route.dest,
    staging.route.dest
  );
  if (destLine)
    lines.push(destLine);
  else if (staging.route.dest)
    lines.push(`  ${import_chalk180.default.cyan("Destination:")} ${staging.route.dest}`);
  const prodStatus = production.route.status ? String(production.route.status) : void 0;
  const stagingStatus = staging.route.status ? String(staging.route.status) : void 0;
  const statusLine = diffField("HTTP Status:", prodStatus, stagingStatus);
  if (statusLine)
    lines.push(statusLine);
  else if (staging.route.status)
    lines.push(`  ${import_chalk180.default.cyan("HTTP Status:")} ${staging.route.status}`);
  const prodHeaders = production.route.headers ?? {};
  const stagingHeaders = staging.route.headers ?? {};
  const allHeaderKeys = /* @__PURE__ */ new Set([
    ...Object.keys(prodHeaders),
    ...Object.keys(stagingHeaders)
  ]);
  if (allHeaderKeys.size > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Response Headers"));
    for (const key of allHeaderKeys) {
      const prodVal = prodHeaders[key];
      const stagingVal = stagingHeaders[key];
      if (prodVal === stagingVal) {
        lines.push(`  ${import_chalk180.default.cyan(key + ":")} ${stagingVal}`);
      } else if (!prodVal) {
        lines.push(
          `${import_chalk180.default.green("+")} ${import_chalk180.default.cyan(key + ":")} ${stagingVal}`
        );
      } else if (!stagingVal) {
        lines.push(
          `${import_chalk180.default.red("-")} ${import_chalk180.default.cyan(key + ":")} ${import_chalk180.default.strikethrough(prodVal)}`
        );
      } else {
        lines.push(
          `${import_chalk180.default.yellow("~")} ${import_chalk180.default.cyan(key + ":")} ${import_chalk180.default.red(prodVal)} \u2192 ${import_chalk180.default.green(stagingVal)}`
        );
      }
    }
  }
  const prodTransforms = production.route.transforms ?? [];
  const stagingTransforms = staging.route.transforms ?? [];
  if (prodTransforms.length > 0 || stagingTransforms.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Transforms"));
    const prodKeys = prodTransforms.map(transformKey);
    const stagingKeys = stagingTransforms.map(transformKey);
    for (let i = 0; i < stagingTransforms.length; i++) {
      const key = stagingKeys[i];
      const prodIdx = prodKeys.indexOf(key);
      if (prodIdx === -1) {
        lines.push(
          `${import_chalk180.default.green("+")} ${formatTransform(stagingTransforms[i])}`
        );
      } else {
        const prodArgs = stringifyArgs(prodTransforms[prodIdx].args);
        const stagingArgs = stringifyArgs(stagingTransforms[i].args);
        if (prodArgs !== stagingArgs) {
          lines.push(
            `${import_chalk180.default.yellow("~")} ${formatTransform(stagingTransforms[i])} ${import_chalk180.default.gray(`(was: ${prodArgs})`)}`
          );
        } else {
          lines.push(`  ${formatTransform(stagingTransforms[i])}`);
        }
      }
    }
    for (let i = 0; i < prodTransforms.length; i++) {
      const key = prodKeys[i];
      if (!stagingKeys.includes(key)) {
        lines.push(
          `${import_chalk180.default.red("-")} ${import_chalk180.default.strikethrough(formatTransformPlain(prodTransforms[i]))}`
        );
      }
    }
  }
  const prodHas = production.route.has ?? [];
  const stagingHas = staging.route.has ?? [];
  if (prodHas.length > 0 || stagingHas.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Has Conditions"));
    diffConditions(lines, prodHas, stagingHas);
  }
  const prodMissing = production.route.missing ?? [];
  const stagingMissing = staging.route.missing ?? [];
  if (prodMissing.length > 0 || stagingMissing.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Does Not Have Conditions"));
    diffConditions(lines, prodMissing, stagingMissing);
  }
  lines.push("");
  return lines.join("\n");
}
function normalizeForComparison(rule) {
  return {
    name: rule.name,
    description: rule.description,
    enabled: rule.enabled,
    route: rule.route
  };
}
function transformKey(t) {
  const key = typeof t.target.key === "string" ? t.target.key : JSON.stringify(t.target.key);
  return `${t.type}:${t.op}:${key}`;
}
function stringifyArgs(args) {
  if (args === void 0)
    return "";
  if (typeof args === "string")
    return args;
  if (Array.isArray(args))
    return args.join(", ");
  return JSON.stringify(args);
}
function formatTransformPlain(transform) {
  const typeLabel = TRANSFORM_TYPE_LABELS[transform.type] ?? transform.type;
  const key = typeof transform.target.key === "string" ? transform.target.key : JSON.stringify(transform.target.key);
  const parts = [`[${typeLabel}]`, transform.op, key];
  if (transform.args !== void 0 && transform.op !== "delete") {
    const argsStr = Array.isArray(transform.args) ? transform.args.join(", ") : transform.args;
    parts.push(`= ${argsStr}`);
  }
  return parts.join(" ");
}
function conditionKey(c) {
  return JSON.stringify({ type: c.type, key: c.key, value: c.value });
}
function diffConditions(lines, prodConditions, stagingConditions) {
  const prodKeys = prodConditions.map(conditionKey);
  const stagingKeys = stagingConditions.map(conditionKey);
  for (const cond of stagingConditions) {
    const key = conditionKey(cond);
    if (prodKeys.includes(key)) {
      lines.push(`  ${formatCondition(cond)}`);
    } else {
      lines.push(`${import_chalk180.default.green("+")} ${formatCondition(cond)}`);
    }
  }
  for (const cond of prodConditions) {
    const key = conditionKey(cond);
    if (!stagingKeys.includes(key)) {
      lines.push(`${import_chalk180.default.red("-")} ${formatCondition(cond)}`);
    }
  }
}
function formatRouteDetails(rule) {
  const lines = [""];
  const typeLabels = getRouteTypeLabel(rule);
  const syntaxLabel = getSrcSyntaxLabel(rule);
  const statusText = rule.enabled === false ? import_chalk180.default.red("Disabled") : import_chalk180.default.green("Enabled");
  lines.push(`  ${import_chalk180.default.bold(rule.name)}`);
  lines.push(`  ${import_chalk180.default.gray(rule.id)}`);
  lines.push("");
  if (rule.description) {
    lines.push(`  ${rule.description}`);
    lines.push("");
  }
  lines.push(`  ${import_chalk180.default.cyan("Status:")}      ${statusText}`);
  if (rule.staged === true) {
    lines.push(
      `  ${import_chalk180.default.cyan("State:")}       ${import_chalk180.default.yellow("Staged (not yet published)")}`
    );
  } else if (rule.staged === false) {
    lines.push(`  ${import_chalk180.default.cyan("State:")}       ${import_chalk180.default.green("Published")}`);
  }
  lines.push(`  ${import_chalk180.default.cyan("Type:")}        ${typeLabels}`);
  lines.push("");
  lines.push(import_chalk180.default.bold("  Route Configuration"));
  lines.push(`  ${import_chalk180.default.cyan("Source:")}      ${rule.route.src}`);
  lines.push(`  ${import_chalk180.default.cyan("Syntax:")}      ${syntaxLabel}`);
  if (rule.route.dest) {
    lines.push(`  ${import_chalk180.default.cyan("Destination:")} ${rule.route.dest}`);
  }
  if (rule.route.status) {
    lines.push(`  ${import_chalk180.default.cyan("HTTP Status:")} ${rule.route.status}`);
  }
  const responseHeaderSets = Object.entries(rule.route.headers ?? {});
  const allTransforms = rule.route.transforms ?? [];
  const responseHeaderTransforms = allTransforms.filter(
    (t) => t.type === "response.headers"
  );
  const requestHeaderTransforms = allTransforms.filter(
    (t) => t.type === "request.headers"
  );
  const requestQueryTransforms = allTransforms.filter(
    (t) => t.type === "request.query"
  );
  if (responseHeaderSets.length > 0 || responseHeaderTransforms.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Response Headers"));
    for (const [key, value] of responseHeaderSets) {
      lines.push(`  ${import_chalk180.default.yellow("set")} ${import_chalk180.default.cyan(key)} = ${value}`);
    }
    for (const t of responseHeaderTransforms) {
      lines.push(`  ${formatTransform(t, false)}`);
    }
  }
  if (requestHeaderTransforms.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Request Headers"));
    for (const t of requestHeaderTransforms) {
      lines.push(`  ${formatTransform(t, false)}`);
    }
  }
  if (requestQueryTransforms.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Request Query"));
    for (const t of requestQueryTransforms) {
      lines.push(`  ${formatTransform(t, false)}`);
    }
  }
  if (rule.route.has && rule.route.has.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Has Conditions"));
    for (const condition of rule.route.has) {
      lines.push(`  ${formatCondition(condition)}`);
    }
  }
  if (rule.route.missing && rule.route.missing.length > 0) {
    lines.push("");
    lines.push(import_chalk180.default.bold("  Does Not Have Conditions"));
    for (const condition of rule.route.missing) {
      lines.push(`  ${formatCondition(condition)}`);
    }
  }
  lines.push("");
  return lines.join("\n");
}

// src/commands/routes/add.ts
var import_chalk181 = __toESM(require_source(), 1);

// src/util/routes/add-route.ts
async function addRoute(client, projectId, routeInput, options = {}) {
  const { teamId, position } = options;
  const query = new URLSearchParams();
  if (teamId)
    query.set("teamId", teamId);
  const queryString = query.toString();
  const url = `/v1/projects/${projectId}/routes${queryString ? `?${queryString}` : ""}`;
  const body = {
    route: routeInput
  };
  if (position) {
    body.position = position;
  }
  return await client.fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify(body)
  });
}

// src/commands/routes/add.ts
function withGlobalFlags8(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
function shellQuoteArg(value) {
  if (/[\s"'\\]/.test(value)) {
    return `"${value.replace(/\\/g, "\\\\").replace(/"/g, '\\"')}"`;
  }
  return value;
}
function buildRoutesAddFullFlagsSuggestion(client, parsed) {
  const { args, flags } = parsed;
  const parts = ["routes", "add"];
  let nameArg = args[0];
  if (nameArg === "add" && args[1] && !args[1].startsWith("-")) {
    nameArg = args[1];
  }
  if (nameArg && !nameArg.startsWith("-")) {
    parts.push(shellQuoteArg(nameArg));
  } else {
    parts.push("<name>");
  }
  if (flags["--src"]) {
    parts.push("--src", shellQuoteArg(String(flags["--src"])));
  } else {
    parts.push("--src", "<path>");
  }
  if (flags["--src-syntax"]) {
    parts.push("--src-syntax", String(flags["--src-syntax"]));
  }
  if (flags["--action"]) {
    parts.push("--action", String(flags["--action"]));
  } else {
    parts.push("--action", "rewrite");
  }
  const action = flags["--action"];
  if (flags["--dest"]) {
    parts.push("--dest", shellQuoteArg(String(flags["--dest"])));
  } else if (!action || action === "rewrite" || action === "redirect") {
    parts.push("--dest", "<dest>");
  }
  if (flags["--status"] !== void 0 && flags["--status"] !== null) {
    parts.push("--status", String(flags["--status"]));
  }
  parts.push("--yes");
  return withGlobalFlags8(client, parts.join(" "));
}
async function add10(client, argv) {
  const parsed = await parseSubcommandArgs(argv, addSubcommand9, client);
  if (typeof parsed === "number")
    return parsed;
  const link = await ensureProjectLink(client);
  if (typeof link === "number")
    return link;
  const { project, org } = link;
  const teamId = org.type === "team" ? org.id : void 0;
  const { args, flags } = parsed;
  const isAgentMode = client.nonInteractive;
  const skipPrompts = flags["--yes"] || isAgentMode;
  const telemetry2 = new RoutesAddTelemetryClient({
    opts: { store: client.telemetryEventStore }
  });
  telemetry2.trackCliFlagYes(skipPrompts);
  telemetry2.trackCliFlagDisabled(flags["--disabled"]);
  telemetry2.trackCliArgumentName(args[0]);
  telemetry2.trackCliOptionSrc(flags["--src"]);
  telemetry2.trackCliOptionSrcSyntax(
    flags["--src-syntax"]
  );
  telemetry2.trackCliOptionAction(flags["--action"]);
  telemetry2.trackCliOptionDest(flags["--dest"]);
  telemetry2.trackCliOptionStatus(flags["--status"]);
  telemetry2.trackCliOptionPosition(flags["--position"]);
  telemetry2.trackCliOptionDescription(
    flags["--description"]
  );
  telemetry2.trackCliOptionSetResponseHeader(
    flags["--set-response-header"]
  );
  telemetry2.trackCliOptionAppendResponseHeader(
    flags["--append-response-header"]
  );
  telemetry2.trackCliOptionDeleteResponseHeader(
    flags["--delete-response-header"]
  );
  telemetry2.trackCliOptionSetRequestHeader(
    flags["--set-request-header"]
  );
  telemetry2.trackCliOptionAppendRequestHeader(
    flags["--append-request-header"]
  );
  telemetry2.trackCliOptionDeleteRequestHeader(
    flags["--delete-request-header"]
  );
  telemetry2.trackCliOptionSetRequestQuery(
    flags["--set-request-query"]
  );
  telemetry2.trackCliOptionAppendRequestQuery(
    flags["--append-request-query"]
  );
  telemetry2.trackCliOptionDeleteRequestQuery(
    flags["--delete-request-query"]
  );
  telemetry2.trackCliOptionHas(flags["--has"]);
  telemetry2.trackCliOptionMissing(flags["--missing"]);
  telemetry2.trackCliOptionAi(flags["--ai"]);
  const aiPrompt = flags["--ai"];
  if (isAgentMode && !aiPrompt && !flags["--src"]) {
    const fullFlagsCmd = buildRoutesAddFullFlagsSuggestion(client, parsed);
    outputAgentError(
      client,
      {
        status: AGENT_STATUS.ERROR,
        reason: AGENT_REASON.MISSING_ARGUMENTS,
        message: "In non-interactive mode pass either full route flags (name, --src, --action, --dest, --yes) or --ai <description> with --yes. For --src use a URL path pattern that starts with a forward slash / (e.g. /about, /api/:path*). With --src-syntax regex you may use a regex such as ^/api/.*. Run vercel routes add --help for options.",
        next: [
          {
            command: withGlobalFlags8(
              client,
              "routes add --ai <description> --yes"
            ),
            when: "AI generates the route and creates it without prompts when generation succeeds (replace <description>)"
          },
          {
            command: fullFlagsCmd,
            when: "add missing --src/--dest (or other placeholders) to this command"
          }
        ],
        hint: "Requires a CLI with the routes command. --src is a URL path: leading character must be / (forward slash). --ai --yes is non-interactive when the API returns a route; if it fails twice, use full flags or rephrase."
      },
      1
    );
  }
  if (aiPrompt) {
    const conflictingFlags = [
      "--src",
      "--src-syntax",
      "--action",
      "--dest",
      "--status",
      "--has",
      "--missing",
      "--set-response-header",
      "--append-response-header",
      "--delete-response-header",
      "--set-request-header",
      "--append-request-header",
      "--delete-request-header",
      "--set-request-query",
      "--append-request-query",
      "--delete-request-query",
      "--description",
      "--disabled",
      "--position"
    ];
    const usedConflicts = conflictingFlags.filter((f) => flags[f] !== void 0);
    if (usedConflicts.length > 0) {
      output_manager_default.error(
        `Cannot use --ai with ${usedConflicts.join(", ")}. Use --ai alone to generate a route from a description.`
      );
      return 1;
    }
    return await handleAIAdd2(
      client,
      project.id,
      teamId,
      aiPrompt,
      skipPrompts,
      parsed
    );
  }
  const { versions } = await getRouteVersions(client, project.id, { teamId });
  const existingStagingVersion = versions.find((v) => v.isStaging);
  let name;
  const nameArg = args[0];
  if (nameArg) {
    if (nameArg.length > MAX_NAME_LENGTH) {
      output_manager_default.error(`Route name must be ${MAX_NAME_LENGTH} characters or less.`);
      return 1;
    }
    name = nameArg;
  } else if (skipPrompts) {
    if (isAgentMode) {
      const fullFlagsCmd = buildRoutesAddFullFlagsSuggestion(client, parsed);
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "In non-interactive mode route name is required as the first argument.",
          next: [
            {
              command: fullFlagsCmd,
              when: "replace <name> and any remaining placeholders"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Route name is required when using --yes. Usage: ${getCommandName('routes add "Route Name" --src "/path" --action rewrite --dest "/destination" --yes')}`
    );
    return 1;
  } else {
    const hasAnyFlags = flags["--src"] !== void 0 || flags["--dest"] !== void 0 || flags["--status"] !== void 0;
    if (!hasAnyFlags) {
      const mode = await client.input.select({
        message: "How would you like to create this route?",
        choices: [
          {
            name: "Describe what you want (AI-powered)",
            value: "ai"
          },
          { name: "Build manually (step by step)", value: "manual" }
        ]
      });
      if (mode === "ai") {
        telemetry2.trackCliOptionAi("interactive");
        return await handleAIAdd2(
          client,
          project.id,
          teamId,
          void 0,
          skipPrompts,
          parsed
        );
      }
    }
    output_manager_default.log("Add a new route\n");
    name = await client.input.text({
      message: "Route name:",
      validate: (val) => {
        if (!val)
          return "Route name is required";
        if (val.length > MAX_NAME_LENGTH)
          return `Name must be ${MAX_NAME_LENGTH} characters or less`;
        return true;
      }
    });
  }
  let src;
  let syntax = "regex";
  if (flags["--src-syntax"]) {
    const syntaxArg = flags["--src-syntax"];
    if (!VALID_SYNTAXES.includes(syntaxArg)) {
      output_manager_default.error(
        `Invalid syntax: "${syntaxArg}". Valid options: ${VALID_SYNTAXES.join(", ")}.`
      );
      return 1;
    }
    syntax = syntaxArg;
  }
  if (flags["--src"]) {
    src = stripQuotes(flags["--src"]);
  } else if (skipPrompts) {
    if (isAgentMode) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "In non-interactive mode --src is required. It must be a URL path pattern starting with forward slash / (e.g. /old-page, /api/:path*). Not a Windows path and not backslash. Use --src-syntax regex only if you need a regex pattern.",
          next: [
            {
              command: buildRoutesAddFullFlagsSuggestion(client, parsed),
              when: "set --src to a path starting with /"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Source path is required when using --yes. Usage: ${getCommandName('routes add "Name" --src "/path" --action rewrite --dest "/dest" --yes')}`
    );
    return 1;
  } else {
    const syntaxChoice = await client.input.select({
      message: "How do you want to specify the path?",
      choices: [
        {
          name: "Path pattern (e.g., /api/:version/users/:id)",
          value: "path-to-regexp"
        },
        { name: "Exact match (e.g., /about)", value: "equals" },
        { name: "Regular expression (e.g., ^/api/(.*)$)", value: "regex" }
      ]
    });
    syntax = syntaxChoice;
    const syntaxHelp = syntax === "path-to-regexp" ? "Use :param for parameters, :param* for optional wildcard" : syntax === "equals" ? "Enter the exact path to match" : "Enter a regular expression pattern";
    src = await client.input.text({
      message: `Path pattern (${syntaxHelp}):`,
      validate: (val) => {
        if (!val)
          return "Path pattern is required";
        if (syntax !== "regex" && !val.startsWith("/")) {
          return "Path must start with / and be a valid URL path";
        }
        return true;
      }
    });
  }
  if (syntax !== "regex" && !src.startsWith("/")) {
    const pathMsg = "--src must start with a forward slash / (URL path root). Examples: /about, /api/:path*. Backslash \\ is wrong. If you need a regex, pass --src-syntax regex and a pattern like ^/api/.*";
    if (isAgentMode) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: pathMsg,
          next: [
            {
              command: buildRoutesAddFullFlagsSuggestion(client, parsed),
              when: "prefix --src with / or use --src-syntax regex"
            }
          ],
          hint: "Route source is matched against request URL paths; they always begin with /."
        },
        1
      );
    }
    output_manager_default.error(
      "Path must start with / (forward slash). Use --src-syntax regex for regex patterns."
    );
    return 1;
  }
  const actionFlag = flags["--action"];
  const dest = flags["--dest"] ? stripQuotes(flags["--dest"]) : void 0;
  const status3 = flags["--status"];
  const actionError = validateActionFlags(actionFlag, dest, status3);
  if (actionError) {
    if (isAgentMode) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: actionError,
          next: [
            {
              command: buildRoutesAddFullFlagsSuggestion(client, parsed),
              when: "fix flags per message (e.g. add --dest for rewrite, --status for redirect)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(actionError);
    return 1;
  }
  let headers = {};
  let transforms = [];
  try {
    const transformFlags = extractTransformFlags(flags);
    const collected = collectHeadersAndTransforms(transformFlags);
    headers = collected.headers;
    transforms = collected.transforms;
  } catch (e2) {
    output_manager_default.error(
      `Invalid transform format. ${e2 instanceof Error ? e2.message : ""}`
    );
    return 1;
  }
  const hasAnyAction = dest || status3 || Object.keys(headers).length > 0 || transforms.length > 0;
  if (!hasAnyAction && skipPrompts) {
    if (isAgentMode) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_ARGUMENTS,
          message: "In non-interactive mode at least one action is required. Use --action with --dest or --status, or header/transform flags.",
          next: [
            {
              command: buildRoutesAddFullFlagsSuggestion(client, parsed),
              when: "example rewrite action; adjust --action/--dest/--status as needed"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      "At least one action is required when using --yes. Use --action with --dest/--status, or header/transform flags."
    );
    return 1;
  }
  if (!hasAnyAction && !skipPrompts) {
    const addConditions = await client.input.confirm(
      "Add conditions (has/does not have)?",
      false
    );
    if (addConditions) {
      await collectInteractiveConditions(client, flags);
    }
    const availableActions = [...ALL_ACTION_CHOICES];
    let actionCount = 0;
    for (; ; ) {
      const choices = [...availableActions];
      if (actionCount > 0) {
        choices.push({ name: "Done", value: "done" });
      }
      const actionType = await client.input.select({
        message: actionCount === 0 ? "What action should this route perform?" : "Add another action:",
        choices: choices.map((c) => ({ name: c.name, value: c.value }))
      });
      if (actionType === "done")
        break;
      await collectActionDetails(client, actionType, flags);
      actionCount++;
      const selectedChoice = ALL_ACTION_CHOICES.find(
        (c) => c.value === actionType
      );
      if (selectedChoice?.exclusive) {
        const exclusiveValues = ALL_ACTION_CHOICES.filter((c) => c.exclusive).map(
          (c) => c.value
        );
        for (const val of exclusiveValues) {
          const idx = availableActions.findIndex((a) => a.value === val);
          if (idx !== -1)
            availableActions.splice(idx, 1);
        }
      }
      if (availableActions.length === 0)
        break;
    }
    const addDescription = await client.input.confirm(
      "Add a description?",
      false
    );
    if (addDescription) {
      const desc = await client.input.text({
        message: "Description:",
        validate: (val) => val && val.length > MAX_DESCRIPTION_LENGTH ? `Description must be ${MAX_DESCRIPTION_LENGTH} characters or less` : true
      });
      if (desc) {
        Object.assign(flags, { "--description": desc });
      }
    }
    const customPosition = await client.input.confirm(
      "Set route position? (default: end)",
      false
    );
    if (customPosition) {
      const positionChoice = await client.input.select({
        message: "Position:",
        choices: [
          { name: "Start - First route (highest priority)", value: "start" },
          { name: "End - Last route (lowest priority)", value: "end" },
          { name: "Before specific route", value: "before" },
          { name: "After specific route", value: "after" }
        ]
      });
      if (positionChoice === "before" || positionChoice === "after") {
        const refId = await client.input.text({
          message: `Route ID to place ${positionChoice}:`,
          validate: (val) => val ? true : "Route ID is required"
        });
        Object.assign(flags, { "--position": `${positionChoice}:${refId}` });
      } else {
        Object.assign(flags, { "--position": positionChoice });
      }
    }
  }
  const finalDest = flags["--dest"] ? stripQuotes(flags["--dest"]) : void 0;
  const finalStatus = flags["--status"];
  try {
    const finalTransformFlags = extractTransformFlags(flags);
    const collected = collectHeadersAndTransforms(finalTransformFlags);
    headers = collected.headers;
    transforms = collected.transforms;
  } catch (e2) {
    output_manager_default.error(
      `Invalid transform format. ${e2 instanceof Error ? e2.message : ""}`
    );
    return 1;
  }
  let hasConditions = [];
  let missingConditions = [];
  const hasFlags = flags["--has"];
  const missingFlags = flags["--missing"];
  try {
    if (hasFlags) {
      hasConditions = parseConditions(hasFlags);
    }
    if (missingFlags) {
      missingConditions = parseConditions(missingFlags);
    }
  } catch (e2) {
    output_manager_default.error(e2 instanceof Error ? e2.message : "Invalid condition format");
    return 1;
  }
  const totalConditions = hasConditions.length + missingConditions.length;
  if (totalConditions > MAX_CONDITIONS) {
    output_manager_default.error(
      `Too many conditions: ${totalConditions}. Maximum is ${MAX_CONDITIONS}.`
    );
    return 1;
  }
  const description = flags["--description"];
  if (description && description.length > MAX_DESCRIPTION_LENGTH) {
    output_manager_default.error(
      `Description must be ${MAX_DESCRIPTION_LENGTH} characters or less.`
    );
    return 1;
  }
  let position;
  const positionFlag = flags["--position"];
  if (positionFlag) {
    try {
      position = parsePosition(positionFlag);
    } catch (e2) {
      output_manager_default.error(`${e2 instanceof Error ? e2.message : "Invalid position"}`);
      return 1;
    }
  }
  const isRedirect = finalDest && finalStatus && REDIRECT_STATUS_CODES.includes(finalStatus);
  telemetry2.trackCliFlagHasConditions(hasConditions.length > 0);
  telemetry2.trackCliFlagMissingConditions(missingConditions.length > 0);
  telemetry2.trackCliFlagResponseHeaders(Object.keys(headers).length > 0);
  telemetry2.trackCliFlagRequestTransforms(transforms.length > 0);
  if (isRedirect) {
    telemetry2.trackCliActionType("redirect");
  } else if (finalDest) {
    telemetry2.trackCliActionType("rewrite");
  } else if (finalStatus) {
    telemetry2.trackCliActionType("set-status");
  } else if (Object.keys(headers).length > 0 || transforms.length > 0) {
    telemetry2.trackCliActionType("modify");
  }
  const routeInput = {
    name,
    description,
    enabled: !flags["--disabled"],
    srcSyntax: syntax,
    route: {
      src,
      ...finalDest && { dest: finalDest },
      ...finalStatus && { status: finalStatus },
      ...Object.keys(headers).length > 0 && { headers },
      ...transforms.length > 0 && { transforms },
      ...hasConditions.length > 0 && { has: hasConditions },
      ...missingConditions.length > 0 && { missing: missingConditions }
    }
  };
  populateRouteEnv(routeInput.route);
  const addStamp = stamp_default();
  output_manager_default.spinner(`Adding route "${name}"`);
  try {
    const { route, version } = await addRoute(client, project.id, routeInput, {
      teamId,
      position
    });
    if (isAgentMode) {
      output_manager_default.stopSpinner();
      const jsonOutput = {
        status: "ok",
        route: {
          id: route.id,
          name: route.name,
          src,
          ...finalDest && { dest: finalDest },
          ...finalStatus && { status: finalStatus }
        },
        version: {
          id: version.id,
          ...version.alias && { alias: version.alias }
        },
        ...!existingStagingVersion && {
          next: [
            {
              command: withGlobalFlags8(client, "routes publish --yes"),
              when: "to promote this version to production"
            }
          ]
        },
        ...existingStagingVersion && {
          hint: "Review staged changes with vercel routes list --diff before promoting."
        }
      };
      client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
      return 0;
    }
    output_manager_default.log(
      `${import_chalk181.default.cyan("Created")} route "${name}" ${import_chalk181.default.gray(addStamp())}`
    );
    output_manager_default.print(`
  ${import_chalk181.default.bold("Route:")} ${route.name}
`);
    output_manager_default.print(`  ${import_chalk181.default.gray("ID:")} ${route.id}
`);
    output_manager_default.print(`  ${import_chalk181.default.gray("Path:")} ${src}
`);
    if (finalDest) {
      if (isRedirect) {
        output_manager_default.print(
          `  ${import_chalk181.default.gray("Redirect:")} ${finalDest} (${finalStatus})
`
        );
      } else {
        output_manager_default.print(`  ${import_chalk181.default.gray("Rewrite:")} ${finalDest}
`);
      }
    } else if (finalStatus) {
      output_manager_default.print(`  ${import_chalk181.default.gray("Status:")} ${finalStatus}
`);
    }
    if (Object.keys(headers).length > 0) {
      output_manager_default.print(
        `  ${import_chalk181.default.gray("Headers:")} ${Object.keys(headers).length} header(s)
`
      );
    }
    if (transforms.length > 0) {
      output_manager_default.print(
        `  ${import_chalk181.default.gray("Transforms:")} ${transforms.length} transform(s)
`
      );
    }
    if (hasConditions.length > 0 || missingConditions.length > 0) {
      output_manager_default.print(
        `  ${import_chalk181.default.gray("Conditions:")} ${hasConditions.length} has, ${missingConditions.length} missing
`
      );
    }
    if (version.alias) {
      let testPath = "/";
      if (syntax === "equals") {
        testPath = src;
      } else if (syntax === "path-to-regexp") {
        testPath = src.replace(/:\w+\*/g, "test").replace(/:\w+/g, "test");
      } else if (src.startsWith("/")) {
        testPath = "/";
      }
      output_manager_default.print(
        `
  ${import_chalk181.default.bold("Test your changes:")} ${import_chalk181.default.cyan(`https://${version.alias}${testPath}`)}
`
      );
    }
    output_manager_default.print(`
  ${import_chalk181.default.bold("Staging version:")} ${version.id}
`);
    await offerAutoPromote(
      client,
      project.id,
      version,
      !!existingStagingVersion,
      { teamId, skipPrompts }
    );
    return 0;
  } catch (e2) {
    const error = e2;
    if (isAgentMode) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.ROUTE_CREATE_FAILED,
          message: "Route creation failed for this project. See hint for next steps.",
          hint: error.message || "Use `vercel routes list --diff` to inspect staged routes, then adjust flags and retry.",
          next: [
            {
              command: withGlobalFlags8(client, "routes list --diff"),
              when: "to inspect staged state"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(error.message || "Failed to create route");
    return 1;
  }
}
async function handleAIAdd2(client, projectId, teamId, aiPrompt, skipPrompts, parsedForSuggestion) {
  const { versions } = await getRouteVersions(client, projectId, { teamId });
  const existingStagingVersion = versions.find((v) => v.isStaging);
  let prompt = aiPrompt;
  let currentGenerated;
  let nonInteractiveGenerationRetries = 0;
  for (; ; ) {
    if (!prompt) {
      prompt = await client.input.text({
        message: "Describe the route you want to create:",
        validate: (val) => {
          if (!val)
            return "A description is required";
          if (val.length > 2e3)
            return "Description must be 2000 characters or less";
          return true;
        }
      });
    }
    output_manager_default.spinner("Generating route...");
    let errorMessage;
    try {
      const result = await generateRoute(
        client,
        projectId,
        { prompt },
        { teamId }
      );
      if (result.error) {
        errorMessage = result.error;
      } else if (!result.route) {
        errorMessage = "Could not generate a route from that description. Try rephrasing.";
      } else {
        currentGenerated = result.route;
      }
    } catch (e2) {
      const error = e2;
      errorMessage = error.message || "Failed to generate route";
    }
    if (currentGenerated) {
      break;
    }
    if (client.nonInteractive || skipPrompts || !client.stdin.isTTY) {
      if (skipPrompts && nonInteractiveGenerationRetries < 1) {
        nonInteractiveGenerationRetries++;
        output_manager_default.stopSpinner();
        output_manager_default.spinner("Generating route... (retry)");
        continue;
      }
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.ROUTE_GENERATION_FAILED,
          message: errorMessage || "Could not generate a route after retry. Rephrase --ai description or use full route flags.",
          next: [
            {
              command: withGlobalFlags8(
                client,
                "routes add --ai <description> --yes"
              ),
              when: "retry with a clearer description (replace <description>)"
            },
            {
              command: parsedForSuggestion ? buildRoutesAddFullFlagsSuggestion(client, parsedForSuggestion) : withGlobalFlags8(
                client,
                "routes add <name> --src <path> --action rewrite --dest <dest> --yes"
              ),
              when: "add with explicit flags if AI keeps failing"
            }
          ],
          hint: "Non-interactive --ai runs up to two generation attempts then exits with JSON."
        },
        1
      );
    }
    output_manager_default.error(errorMessage);
    if (skipPrompts || !client.stdin.isTTY) {
      return 1;
    }
    const retry = await client.input.select({
      message: "What would you like to do?",
      choices: [
        { name: "Try again with a different description", value: "retry" },
        { name: "Cancel", value: "cancel" }
      ]
    });
    if (retry === "cancel") {
      output_manager_default.log("Cancelled.");
      return 0;
    }
    prompt = void 0;
  }
  printGeneratedRoutePreview(currentGenerated);
  if (skipPrompts) {
    return await createFromGenerated2(
      client,
      projectId,
      teamId,
      currentGenerated,
      existingStagingVersion,
      skipPrompts
    );
  }
  if (!client.stdin.isTTY) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.CONFIRMATION_REQUIRED,
          message: "Route creation from AI preview requires a TTY to confirm, or use full flags with --yes non-interactively.",
          next: [
            {
              command: parsedForSuggestion ? buildRoutesAddFullFlagsSuggestion(client, parsedForSuggestion) : withGlobalFlags8(
                client,
                "routes add <name> --src <path> --action rewrite --dest <dest> --yes"
              ),
              when: "non-interactive add without AI preview"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `Cannot interactively confirm route creation in a non-TTY environment. Use full route flags with ${getCommandName("routes add <name> --src ... --yes")}, or run in a TTY.`
    );
    return 1;
  }
  for (; ; ) {
    const choice = await client.input.select({
      message: "What would you like to do?",
      choices: [
        { name: "Create this route", value: "create" },
        { name: "Edit with AI (describe changes)", value: "ai-edit" },
        { name: "Edit manually", value: "manual" },
        { name: "Discard", value: "discard" }
      ],
      pageSize: 4,
      loop: false
    });
    if (choice === "create") {
      return await createFromGenerated2(
        client,
        projectId,
        teamId,
        currentGenerated,
        existingStagingVersion,
        skipPrompts
      );
    }
    if (choice === "ai-edit") {
      const editPrompt = await client.input.text({
        message: "Describe what you'd like to change:",
        validate: (val) => {
          if (!val)
            return "A description is required";
          if (val.length > 2e3)
            return "Description must be 2000 characters or less";
          return true;
        }
      });
      output_manager_default.spinner("Updating route...");
      try {
        const editResult = await generateRoute(
          client,
          projectId,
          {
            prompt: editPrompt,
            currentRoute: convertRouteToCurrentRoute(currentGenerated)
          },
          { teamId }
        );
        if (editResult.error) {
          output_manager_default.error(editResult.error);
          output_manager_default.log("Keeping previous route:");
          printGeneratedRoutePreview(currentGenerated);
          continue;
        }
        if (!editResult.route) {
          output_manager_default.error("Could not apply changes. Try rephrasing.");
          output_manager_default.log("Keeping previous route:");
          printGeneratedRoutePreview(currentGenerated);
          continue;
        }
        currentGenerated = editResult.route;
        printGeneratedRoutePreview(currentGenerated);
      } catch (e2) {
        const error = e2;
        output_manager_default.error(error.message || "Failed to update route");
        output_manager_default.log("Keeping previous route:");
        printGeneratedRoutePreview(currentGenerated);
      }
      continue;
    }
    if (choice === "manual") {
      const routeInput = generatedRouteToAddInput(currentGenerated);
      const tempRule = {
        name: routeInput.name,
        description: routeInput.description,
        enabled: true,
        srcSyntax: routeInput.srcSyntax,
        route: routeInput.route
      };
      await runInteractiveEditLoop(client, tempRule);
      populateRouteEnv(tempRule.route);
      const addStamp = stamp_default();
      output_manager_default.spinner(`Adding route "${tempRule.name}"`);
      try {
        const { route, version } = await addRoute(
          client,
          projectId,
          {
            name: tempRule.name,
            description: tempRule.description,
            srcSyntax: tempRule.srcSyntax,
            route: tempRule.route
          },
          { teamId }
        );
        output_manager_default.log(
          `${import_chalk181.default.cyan("Created")} route "${route.name}" ${import_chalk181.default.gray(addStamp())}`
        );
        output_manager_default.print(`
  ${import_chalk181.default.bold("Route:")} ${route.name}
`);
        output_manager_default.print(`  ${import_chalk181.default.gray("ID:")} ${route.id}
`);
        output_manager_default.print(`
  ${import_chalk181.default.bold("Staging version:")} ${version.id}
`);
        await offerAutoPromote(
          client,
          projectId,
          version,
          !!existingStagingVersion,
          { teamId, skipPrompts }
        );
        return 0;
      } catch (e2) {
        const error = e2;
        output_manager_default.error(error.message || "Failed to create route");
        return 1;
      }
    }
    output_manager_default.log("Discarded.");
    return 0;
  }
}
async function createFromGenerated2(client, projectId, teamId, generated, existingStagingVersion, skipPrompts) {
  const routeInput = generatedRouteToAddInput(generated);
  populateRouteEnv(routeInput.route);
  const addStamp = stamp_default();
  output_manager_default.spinner(`Adding route "${routeInput.name}"`);
  try {
    const { route, version } = await addRoute(client, projectId, routeInput, {
      teamId
    });
    if (client.nonInteractive) {
      output_manager_default.stopSpinner();
      const jsonOutput = {
        status: "ok",
        route: {
          id: route.id,
          name: route.name,
          src: routeInput.route.src
        },
        version: {
          id: version.id,
          ...version.alias && { alias: version.alias }
        },
        ...!existingStagingVersion && {
          next: [
            {
              command: withGlobalFlags8(client, "routes publish --yes"),
              when: "to promote this version to production"
            }
          ]
        },
        ...existingStagingVersion && {
          hint: "Review staged changes with vercel routes list --diff before promoting."
        }
      };
      client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
      return 0;
    }
    output_manager_default.log(
      `${import_chalk181.default.cyan("Created")} route "${route.name}" ${import_chalk181.default.gray(addStamp())}`
    );
    output_manager_default.print(`
  ${import_chalk181.default.bold("Route:")} ${route.name}
`);
    output_manager_default.print(`  ${import_chalk181.default.gray("ID:")} ${route.id}
`);
    output_manager_default.print(`
  ${import_chalk181.default.bold("Staging version:")} ${version.id}
`);
    await offerAutoPromote(
      client,
      projectId,
      version,
      !!existingStagingVersion,
      { teamId, skipPrompts }
    );
    return 0;
  } catch (e2) {
    const error = e2;
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.ROUTE_CREATE_FAILED,
          message: "Route creation failed for this project. See hint for next steps.",
          hint: error.message || "Use `vercel routes list --diff` to inspect staged routes, then adjust flags and retry.",
          next: [
            {
              command: withGlobalFlags8(client, "routes list --diff"),
              when: "to inspect staged state"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(error.message || "Failed to create route");
    return 1;
  }
}

// src/commands/routes/index.ts
var COMMAND_CONFIG35 = {
  list: getCommandAliases(listSubcommand14),
  "list-versions": getCommandAliases(listVersionsSubcommand2),
  inspect: getCommandAliases(inspectSubcommand5),
  add: getCommandAliases(addSubcommand9),
  edit: getCommandAliases(editSubcommand),
  delete: getCommandAliases(deleteSubcommand),
  enable: getCommandAliases(enableSubcommand3),
  disable: getCommandAliases(disableSubcommand3),
  reorder: getCommandAliases(reorderSubcommand),
  export: getCommandAliases(exportSubcommand),
  publish: getCommandAliases(publishSubcommand2),
  restore: getCommandAliases(restoreSubcommand2),
  "discard-staging": getCommandAliases(discardSubcommand2)
};
async function main27(client) {
  const telemetry2 = new RoutesTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(routesCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const subArgs = parsedArgs.args.slice(1);
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    subArgs,
    COMMAND_CONFIG35
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("routes");
    output_manager_default.print(help(routesCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        parent: routesCommand,
        columns: client.stderr.columns
      })
    );
  }
  switch (subcommand) {
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(listSubcommand14);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return list12(client, args);
    case "list-versions":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(listVersionsSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandListVersions(subcommandOriginal);
      return listVersions2(client, args);
    case "inspect":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(inspectSubcommand5);
        return 2;
      }
      telemetry2.trackCliSubcommandInspect(subcommandOriginal);
      return inspect6(client, args);
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(addSubcommand9);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add10(client, args);
    case "edit":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(editSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandEdit(subcommandOriginal);
      return (await import("./chunks/edit-LWDFUOJX.js")).default(client, args);
    case "delete":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(deleteSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandDelete(subcommandOriginal);
      return (await import("./chunks/delete-53LWNN3R.js")).default(client, args);
    case "enable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(enableSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandEnable(subcommandOriginal);
      return (await import("./chunks/enable-DBP5ARSO.js")).default(client, args);
    case "disable":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(disableSubcommand3);
        return 2;
      }
      telemetry2.trackCliSubcommandDisable(subcommandOriginal);
      return (await import("./chunks/disable-PB4E5RT2.js")).default(client, args);
    case "reorder":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(reorderSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandReorder(subcommandOriginal);
      return (await import("./chunks/reorder-TGFBUZO6.js")).default(client, args);
    case "export":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(exportSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandExport(subcommandOriginal);
      return (await import("./chunks/export-LNTUARTL.js")).default(client, args);
    case "publish":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(publishSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandPublish(subcommandOriginal);
      return (await import("./chunks/publish-BLJ4Z47H.js")).default(client, args);
    case "restore":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(restoreSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandRestore(subcommandOriginal);
      return (await import("./chunks/restore-RQB3RYLR.js")).default(client, args);
    case "discard-staging":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("routes", subcommandOriginal);
        printHelp(discardSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandDiscardStaging(subcommandOriginal);
      return (await import("./chunks/discard-XJPKRCEX.js")).default(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG35));
      output_manager_default.print(help(routesCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/sandbox/index.ts
function getFlagValue(args, names) {
  let value;
  for (let i = 0; i < args.length; i++) {
    const arg = args[i];
    const [option, inlineValue] = arg.split("=", 2);
    if (!names.includes(option)) {
      continue;
    }
    if (inlineValue !== void 0) {
      value = inlineValue;
      continue;
    }
    if (i + 1 < args.length) {
      value = args[i + 1];
      i++;
    }
  }
  return value;
}
async function sandbox(client) {
  const argv = client.argv.slice(2);
  const commandIndex = argv.indexOf("sandbox");
  const rootArgs = commandIndex === -1 ? argv : argv.slice(0, commandIndex);
  const sandboxArgs = commandIndex === -1 ? [] : argv.slice(commandIndex + 1);
  const scope = getFlagValue(rootArgs, ["--scope", "-S"]);
  const team = getFlagValue(rootArgs, ["--team", "-T"]);
  const token2 = getFlagValue(rootArgs, ["--token", "-t"]);
  const forwardedArgs = [
    ...scope ? ["--scope", scope] : team ? ["--team", team] : [],
    ...sandboxArgs
  ];
  const originalCwd = process.cwd();
  const originalAuthToken = process.env.VERCEL_AUTH_TOKEN;
  try {
    if (token2) {
      process.env.VERCEL_AUTH_TOKEN = token2;
    } else if (!process.env.VERCEL_AUTH_TOKEN && process.env.VERCEL_TOKEN) {
      process.env.VERCEL_AUTH_TOKEN = process.env.VERCEL_TOKEN;
    } else if (!process.env.VERCEL_AUTH_TOKEN && client.authConfig.token) {
      process.env.VERCEL_AUTH_TOKEN = client.authConfig.token;
    }
    process.chdir(client.cwd);
    const { createApp } = await import("sandbox");
    await createApp({
      appName: "vercel sandbox",
      withoutAuth: false
    }).run(forwardedArgs);
    return 0;
  } catch (error) {
    printError(error);
    return 1;
  } finally {
    process.chdir(originalCwd);
    if (typeof originalAuthToken === "string") {
      process.env.VERCEL_AUTH_TOKEN = originalAuthToken;
    } else {
      delete process.env.VERCEL_AUTH_TOKEN;
    }
  }
}

// src/commands/skills/index.ts
var import_chalk182 = __toESM(require_source(), 1);
var import_frameworks2 = __toESM(require_frameworks(), 1);
var import_fs_detectors = __toESM(require_dist3(), 1);
import { access as access2, readFile as readFile6 } from "fs/promises";
import { join as join8 } from "path";
import { homedir } from "os";
import { spawn as spawn4 } from "child_process";

// src/util/telemetry/commands/skills/index.ts
var SkillsTelemetryClient = class extends TelemetryClient {
  trackCliFlagJson(v) {
    if (v) {
      this.trackCliFlag("json");
    }
  }
  trackCliFlagYes(v) {
    if (v) {
      this.trackCliFlag("yes");
    }
  }
  trackCliArgumentQuery(v) {
    if (v) {
      this.trackCliArgument({
        arg: "query",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/skills/index.ts
var SKILLS_API = "https://skills.sh/api/search";
var FETCH_TIMEOUT_MS = 1e4;
var MIN_INSTALLS = 100;
var MAX_RESULTS = 8;
var MAX_FRAMEWORK_RESULTS = 4;
var MAX_DEP_QUERIES = 5;
var NOTABLE_DEPS = /* @__PURE__ */ new Set([
  "prisma",
  "@prisma/client",
  "drizzle-orm",
  "mongoose",
  "typeorm",
  "sequelize",
  "tailwindcss",
  "next-auth",
  "@auth/core",
  "@clerk/nextjs",
  "lucia",
  "@supabase/supabase-js",
  "@trpc/server",
  "graphql",
  "@apollo/client",
  "stripe",
  "zustand",
  "@reduxjs/toolkit",
  "@tanstack/react-query",
  "playwright",
  "@playwright/test",
  "cypress",
  "vitest",
  "jest",
  "sanity",
  "payload",
  "contentful",
  "turborepo",
  "docker-compose",
  "firebase",
  "@sentry/nextjs",
  "zod",
  "convex",
  "upstash"
]);
async function searchSkills(query) {
  try {
    const url = `${SKILLS_API}?q=${encodeURIComponent(query)}`;
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS);
    try {
      const res = await fetch(url, { signal: controller.signal });
      if (!res.ok) {
        output_manager_default.debug(`Skills API returned ${res.status} for query "${query}"`);
        return [];
      }
      const data = await res.json();
      return data.skills ?? [];
    } finally {
      clearTimeout(timeout);
    }
  } catch (err) {
    output_manager_default.debug(`Skills search failed for "${query}": ${err}`);
    return [];
  }
}
async function detectProjectFramework(cwd) {
  try {
    const fs2 = new import_fs_detectors.LocalFileSystemDetector(cwd);
    const detected = await (0, import_fs_detectors.detectFrameworks)({ fs: fs2, frameworkList: import_frameworks2.frameworkList });
    if (detected.length > 0) {
      return { name: detected[0].name, slug: detected[0].slug || "" };
    }
  } catch {
  }
  return null;
}
async function readPackageDeps(cwd) {
  try {
    const raw = await readFile6(join8(cwd, "package.json"), "utf8");
    const pkg = JSON.parse(raw);
    const allDeps = {
      ...pkg.dependencies,
      ...pkg.devDependencies
    };
    const depNames = Object.keys(allDeps);
    const notable = depNames.filter((d) => NOTABLE_DEPS.has(d));
    const language = depNames.includes("typescript") ? "typescript" : "javascript";
    return { deps: notable, language };
  } catch {
    return { deps: [], language: "unknown" };
  }
}
function cleanDepName(dep) {
  return dep.replace(/^@/, "").replace(/\//g, "-");
}
async function isSkillInstalled(name, cwd) {
  const sanitized = name.toLowerCase().replace(/\s+/g, "-");
  const dirs = [
    join8(cwd, ".agents", "skills", sanitized),
    join8(homedir(), ".agents", "skills", sanitized)
  ];
  for (const dir of dirs) {
    try {
      await access2(dir);
      return true;
    } catch {
    }
  }
  return false;
}
function formatInstalls(n) {
  if (n >= 1e3)
    return `${(n / 1e3).toFixed(1)}k`;
  return String(n);
}
async function skills(client) {
  let parsedArgs;
  try {
    parsedArgs = parseArguments(
      client.argv.slice(2),
      getFlagsSpecification(skillsCommand.options)
    );
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new SkillsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("skills");
    output_manager_default.print(help(skillsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  telemetry2.trackCliFlagJson(parsedArgs.flags["--json"]);
  telemetry2.trackCliFlagYes(parsedArgs.flags["--yes"]);
  telemetry2.trackCliArgumentQuery(parsedArgs.args[1]);
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const query = parsedArgs.args[1];
  const { cwd } = client;
  const yes = parsedArgs.flags["--yes"] ?? false;
  try {
    if (query) {
      return await directSearch(client, query, asJson, cwd);
    }
    return await autoDetect(client, cwd, asJson, yes);
  } catch (err) {
    printError(err);
    return 1;
  }
}
async function directSearch(client, query, asJson, cwd) {
  output_manager_default.spinner("Searching skills...");
  const results = await searchSkills(query);
  output_manager_default.stopSpinner();
  const filtered = results.filter((s) => s.installs >= MIN_INSTALLS).slice(0, MAX_RESULTS);
  const { exitCode: exitCode2 } = await displayResults(
    client,
    filtered,
    asJson,
    `Search: "${query}"`,
    cwd
  );
  return exitCode2;
}
async function autoDetect(client, cwd, asJson, yes) {
  output_manager_default.spinner("Detecting project...");
  const [framework, { deps, language }] = await Promise.all([
    detectProjectFramework(cwd),
    readPackageDeps(cwd)
  ]);
  if (!framework && deps.length === 0) {
    output_manager_default.stopSpinner();
    output_manager_default.log(
      "Could not detect a framework or notable dependencies in this directory."
    );
    output_manager_default.log(
      `Try searching directly: ${import_chalk182.default.cyan("vercel skills <query>")}`
    );
    return 0;
  }
  const detectedParts = [];
  if (framework)
    detectedParts.push(framework.name);
  if (language !== "unknown")
    detectedParts.push(language);
  if (deps.length > 0)
    detectedParts.push(deps.join(", "));
  output_manager_default.stopSpinner();
  output_manager_default.log(`Detected: ${import_chalk182.default.bold(detectedParts.join(" + "))}`);
  output_manager_default.spinner("Searching for relevant skills...");
  const queries = [];
  if (framework) {
    const slug = (framework.slug || framework.name).toLowerCase().replace(/-\d+$/, "");
    queries.push({ term: slug, tier: "framework" });
    const base = slug.replace(/(kit|js)$/, "");
    if (base && base !== slug) {
      queries.push({ term: base, tier: "framework" });
    }
    output_manager_default.debug(`Framework queries: ${queries.map((q) => q.term).join(", ")}`);
  }
  for (const dep of deps.slice(0, MAX_DEP_QUERIES)) {
    queries.push({ term: cleanDepName(dep), tier: "dependency" });
  }
  output_manager_default.debug(
    `Total queries: ${queries.map((q) => `${q.term} (${q.tier})`).join(", ")}`
  );
  const searchResults = await Promise.allSettled(
    queries.map(async (q) => ({
      tier: q.tier,
      skills: await searchSkills(q.term)
    }))
  );
  const frameworkSkills = /* @__PURE__ */ new Map();
  const depSkills = /* @__PURE__ */ new Map();
  for (const result of searchResults) {
    if (result.status !== "fulfilled")
      continue;
    const { tier, skills: resultSkills } = result.value;
    const bucket = tier === "framework" ? frameworkSkills : depSkills;
    for (const skill of resultSkills) {
      if (skill.installs < MIN_INSTALLS)
        continue;
      const existing = bucket.get(skill.skillId);
      if (!existing || skill.installs > existing.installs) {
        bucket.set(skill.skillId, skill);
      }
    }
  }
  const sortByInstalls = (a, b) => b.installs - a.installs;
  const topFramework = Array.from(frameworkSkills.values()).sort(sortByInstalls).slice(0, MAX_FRAMEWORK_RESULTS);
  for (const fw of topFramework) {
    depSkills.delete(fw.skillId);
  }
  const topDeps = Array.from(depSkills.values()).sort(sortByInstalls).slice(0, MAX_RESULTS - topFramework.length);
  const ranked = [...topFramework, ...topDeps];
  output_manager_default.debug(
    `Results: ${topFramework.length} framework + ${topDeps.length} dependency skills`
  );
  output_manager_default.stopSpinner();
  const {
    exitCode: exitCode2,
    results: displayedResults,
    installedChecks
  } = await displayResults(
    client,
    ranked,
    asJson,
    `Detected: ${detectedParts.join(" + ")}`,
    cwd
  );
  if (exitCode2 !== 0 || asJson)
    return exitCode2;
  const installable = displayedResults.map((s, i) => ({ ...s, installed: installedChecks[i] })).filter((s) => !s.installed);
  if (installable.length === 0) {
    output_manager_default.log(import_chalk182.default.green("All recommended skills are already installed."));
    return 0;
  }
  let toInstall;
  if (yes) {
    toInstall = installable;
  } else if (!client.stdin.isTTY) {
    output_manager_default.error(
      "Confirmation required. Use `--yes` to skip the confirmation prompt."
    );
    return 1;
  } else {
    const selected = await client.input.checkbox({
      message: "Select skills to install",
      choices: installable.map((s) => ({
        name: `${s.skillId} ${import_chalk182.default.gray(`(${s.source})`)}`,
        value: s,
        checked: true
      }))
    });
    if (!Array.isArray(selected) || selected.length === 0) {
      output_manager_default.log("No skills selected.");
      return 0;
    }
    toInstall = selected;
  }
  output_manager_default.log(
    `
Installing ${toInstall.length} skill${toInstall.length > 1 ? "s" : ""}...
`
  );
  const bySource = /* @__PURE__ */ new Map();
  for (const skill of toInstall) {
    const existing = bySource.get(skill.source) ?? [];
    existing.push(skill.skillId);
    bySource.set(skill.source, existing);
  }
  const succeeded = [];
  const failed = [];
  for (const [source, skillIds] of bySource) {
    const label = skillIds.length === 1 ? skillIds[0] : skillIds.join(", ");
    output_manager_default.spinner(`Installing ${label} from ${source}...`);
    const args = ["skills", "add", source];
    for (const id of skillIds) {
      args.push("--skill", id);
    }
    args.push("-y");
    output_manager_default.debug(`> npx ${args.join(" ")}`);
    const code2 = await runCommand("npx", args);
    output_manager_default.stopSpinner();
    if (code2 !== 0) {
      failed.push(...skillIds);
    } else {
      succeeded.push(...skillIds);
    }
  }
  if (succeeded.length > 0) {
    output_manager_default.success(
      `Installed ${succeeded.length} skill${succeeded.length > 1 ? "s" : ""}: ${succeeded.join(", ")}`
    );
  }
  if (failed.length > 0) {
    output_manager_default.error(`Failed to install: ${failed.join(", ")}`);
  }
  return 0;
}
async function displayResults(client, results, asJson, context, cwd) {
  const empty = { exitCode: 0, results, installedChecks: [] };
  if (results.length === 0) {
    output_manager_default.log("No skills found.");
    output_manager_default.log(
      `Try searching directly: ${import_chalk182.default.cyan("vercel skills <query>")}`
    );
    return empty;
  }
  const installedChecks = await Promise.all(
    results.map((s) => isSkillInstalled(s.skillId, cwd))
  );
  if (asJson) {
    output_manager_default.stopSpinner();
    const skillsWithStatus = results.map((s, i) => ({
      ...s,
      installed: installedChecks[i]
    }));
    client.stdout.write(
      `${JSON.stringify({ context, skills: skillsWithStatus }, null, 2)}
`
    );
    return { exitCode: 0, results, installedChecks };
  }
  const tableData = [
    ["Skill", "Installs", "Source", ""].map((h) => import_chalk182.default.bold(import_chalk182.default.cyan(h))),
    ...results.map((s, i) => [
      s.skillId,
      formatInstalls(s.installs),
      import_chalk182.default.gray(s.source),
      installedChecks[i] ? import_chalk182.default.green("installed") : ""
    ])
  ];
  output_manager_default.log(`
${table(tableData, { hsep: 4 })}`);
  return { exitCode: 0, results, installedChecks };
}
function runCommand(cmd2, args) {
  return new Promise((resolve8) => {
    const child = spawn4(cmd2, args, {
      stdio: "pipe"
    });
    child.on("close", (code2) => resolve8(code2 ?? 1));
    child.on("error", () => resolve8(1));
  });
}

// src/commands/target/list.ts
var import_ms33 = __toESM(require_ms(), 1);
var import_chalk183 = __toESM(require_source(), 1);
function formatBranchMatcher(branchMatcher) {
  if (branchMatcher?.type === "equals") {
    return branchMatcher.pattern;
  } else if (branchMatcher?.type === "startsWith") {
    return `${branchMatcher.pattern}${import_chalk183.default.dim("*")}`;
  } else if (branchMatcher?.type === "endsWith") {
    return `${import_chalk183.default.dim("*")}${branchMatcher.pattern}`;
  }
  return import_chalk183.default.dim("No branch configuration");
}
var TYPE_MAP = {
  production: "Production",
  preview: "Preview",
  development: "Development"
};
var BRANCH_TRACKING_MAP = {
  production: (project) => project.link?.productionBranch ?? "main",
  preview: (_, env) => env.slug === "preview" ? import_chalk183.default.dim("All unassigned git branches") : formatBranchMatcher(env.branchMatcher),
  development: () => import_chalk183.default.dim("Accessible via CLI")
};
async function list13(client, argv) {
  const { cwd } = client;
  const telemetry2 = new TelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand15.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const validationResult = validateLsArgs({
    commandName: "target ls",
    args: parsedArgs.args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  const autoConfirm = !!parsedArgs.flags["--yes"];
  const link = await ensureLink(targetCommand.name, client, cwd, {
    autoConfirm
  });
  if (typeof link === "number") {
    return link;
  }
  const start = Date.now();
  const projectSlugLink = formatProject(link.org.slug, link.project.name);
  output_manager_default.spinner(`Fetching custom environments for ${projectSlugLink}`);
  const url = `/projects/${encodeURIComponent(
    link.project.id
  )}/custom-environments`;
  let { environments: result } = await client.fetch(url, {
    method: "GET",
    accountId: link.org.id
  });
  output_manager_default.stopSpinner();
  const elapsed2 = (0, import_ms33.default)(Date.now() - start);
  result = withDefaultEnvironmentsIncluded(result);
  if (asJson) {
    const jsonOutput = {
      targets: result.map((target) => ({
        id: target.id,
        slug: target.slug,
        type: target.type,
        description: target.description,
        branchMatcher: target.branchMatcher,
        createdAt: target.createdAt,
        updatedAt: target.updatedAt
      }))
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    output_manager_default.log(
      `${result.length} Environment${result.length === 1 ? "" : "s"} found under ${projectSlugLink} ${import_chalk183.default.gray(`[${elapsed2}]`)}`
    );
    const tablePrint = table(
      [
        ["Target Name", "Branch Tracking", "Type", "Updated"].map(
          (header) => import_chalk183.default.bold(import_chalk183.default.cyan(header))
        ),
        ...result.flatMap((target) => {
          return [
            [
              formatEnvironment(link.org.slug, link.project.name, target),
              BRANCH_TRACKING_MAP[target.type](link.project, target),
              TYPE_MAP[target.type],
              import_chalk183.default.gray(
                target.updatedAt > 0 ? (0, import_ms33.default)(Date.now() - target.updatedAt) : "-"
              )
            ]
          ];
        })
      ],
      { hsep: 3 }
    ).replace(/^/gm, "  ");
    output_manager_default.print(`
${tablePrint}

`);
  }
  return 0;
}
function withDefaultEnvironmentsIncluded(environments) {
  return [
    {
      id: "production",
      slug: "production",
      createdAt: 0,
      updatedAt: 0,
      type: "production",
      description: "",
      domains: []
    },
    {
      id: "preview",
      slug: "preview",
      createdAt: 0,
      updatedAt: 0,
      type: "preview",
      description: "",
      domains: []
    },
    {
      id: "development",
      slug: "development",
      createdAt: 0,
      updatedAt: 0,
      type: "development",
      description: "",
      domains: []
    },
    ...environments.slice().sort((a, b) => a.slug.localeCompare(b.slug))
  ];
}

// src/util/telemetry/commands/target/index.ts
var TargetTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandList(subcommandActual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: subcommandActual
    });
  }
};

// src/commands/target/index.ts
var COMMAND_CONFIG36 = {
  ls: getCommandAliases(listSubcommand15)
};
async function main28(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(targetCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const { telemetryEventStore } = client;
  const telemetry2 = new TargetTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  parsedArgs.args = parsedArgs.args.slice(1);
  const subcommand = parsedArgs.args[0];
  const args = parsedArgs.args.slice(1);
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("target");
    output_manager_default.print(help(targetCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: targetCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "ls":
    case "list":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("target", "list");
        printHelp(listSubcommand15);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommand);
      return await list13(client, args);
    default:
      output_manager_default.error(getInvalidSubcommand(COMMAND_CONFIG36));
      output_manager_default.print(help(targetCommand, { columns: client.stderr.columns }));
      return 2;
  }
}

// src/commands/teams/list.ts
var import_chalk184 = __toESM(require_source(), 1);

// src/util/telemetry/commands/teams/list.ts
var TeamsListTelemetryClient = class extends TelemetryClient {
  trackCliOptionNext(value) {
    if (value && value > 0) {
      this.trackCliOption({
        option: "next",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionUntil(value) {
    if (value) {
      this.trackCliOption({
        option: "until",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionSince(value) {
    if (value) {
      this.trackCliOption({
        option: "since",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionCount(value) {
    if (value && value > 0) {
      this.trackCliOption({
        option: "count",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/teams/list.ts
async function list14(client, argv) {
  const { config, telemetryEventStore } = client;
  const telemetry2 = new TeamsListTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand16.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const validationResult = validateLsArgs({
    commandName: "teams ls",
    args: parsedArgs.args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  const next = parsedArgs.flags["--next"];
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionNext(next);
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  telemetry2.trackCliOptionCount(parsedArgs.flags["--count"]);
  telemetry2.trackCliOptionUntil(parsedArgs.flags["--until"]);
  telemetry2.trackCliOptionSince(parsedArgs.flags["--since"]);
  if (typeof next !== "undefined" && !Number.isInteger(next)) {
    output_manager_default.error("Please provide a number for flag `--next`");
    return 1;
  }
  output_manager_default.spinner("Fetching teams");
  const { teams: teams2, pagination } = await getTeams(client, {
    next,
    apiVersion: 2
  });
  let { currentTeam } = config;
  output_manager_default.spinner("Fetching user information");
  const user = await getUser(client);
  const accountIsCurrent = !currentTeam && user.version !== "northstar";
  if (accountIsCurrent) {
    currentTeam = user.id;
  }
  const teamList = teams2.map(({ id, slug, name }) => ({
    id,
    name,
    value: slug,
    isCurrent: id === currentTeam
  }));
  if (user.version !== "northstar") {
    teamList.unshift({
      id: user.id,
      name: user.email,
      value: user.username || user.email,
      isCurrent: accountIsCurrent
    });
  }
  if (!accountIsCurrent) {
    const index = teamList.findIndex((choice2) => choice2.id === currentTeam);
    const choice = teamList.splice(index, 1)[0];
    teamList.unshift(choice);
  }
  output_manager_default.stopSpinner();
  if (asJson) {
    const jsonOutput = {
      teams: teamList.map((team) => ({
        id: team.id,
        slug: team.value,
        name: team.name,
        current: team.isCurrent
      })),
      pagination
    };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    client.stdout.write("\n");
    const teamTable = table(
      [
        ["id", "Team name"].map((str) => (0, import_chalk184.gray)(str)),
        ...teamList.map((team) => [team.value, team.name])
      ],
      { hsep: 5 }
    );
    client.stderr.write(
      currentTeam ? teamTable.split("\n").map((line, i) => {
        const prefix = i > 0 ? teamList[i - 1].isCurrent ? chars_default.tick : " " : " ";
        return `${prefix} ${line}`;
      }).join("\n") : teamTable
    );
    client.stderr.write("\n");
    if (pagination?.count === 20) {
      const flags = getCommandFlags(parsedArgs.flags, [
        "--next",
        "-N",
        "-d",
        "--format"
      ]);
      const nextCmd = `${packageName} teams ls${flags} --next ${pagination.next}`;
      client.stdout.write("\n");
      output_manager_default.log(`To display the next page run ${cmd(nextCmd)}`);
    }
  }
  return 0;
}

// src/commands/teams/add.ts
var import_chalk186 = __toESM(require_source(), 1);

// src/commands/teams/invite.ts
var import_chalk185 = __toESM(require_source(), 1);

// src/util/input/regexes.ts
var email = /.+@.+\..+$/;

// src/util/teams/invite-user-to-team.ts
async function inviteUserToTeam(client, teamId, email2) {
  const body = await client.fetch(
    `/teams/${encodeURIComponent(teamId)}/members`,
    {
      method: "POST",
      body: { email: email2 }
    }
  );
  return body;
}

// src/commands/teams/invite.ts
var import_error_utils18 = __toESM(require_dist(), 1);

// src/util/telemetry/commands/teams/invite.ts
var TeamsInviteTelemetryClient = class extends TelemetryClient {
  trackCliArgumentEmail(values) {
    if (values) {
      this.trackCliArgument({
        arg: "email",
        value: this.redactedArgumentsLength(values)
      });
    }
  }
};

// src/commands/teams/invite.ts
function withGlobalFlags9(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
var validateEmail = (data) => email.test(data.trim()) || data.length === 0;
var domains = Array.from(
  /* @__PURE__ */ new Set([
    "aol.com",
    "gmail.com",
    "google.com",
    "yahoo.com",
    "ymail.com",
    "hotmail.com",
    "live.com",
    "outlook.com",
    "inbox.com",
    "mail.com",
    "gmx.com",
    "icloud.com"
  ])
);
async function invite(client, argv, { introMsg = "", noopMsg = "No changes made" } = {}) {
  const { config, telemetryEventStore } = client;
  const { currentTeam: currentTeamId } = config;
  const telemetry2 = new TeamsInviteTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(inviteSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const { args: emails } = parsedArgs;
  if (client.nonInteractive && emails.length === 0) {
    const fullArgs = client.argv.slice(2);
    const inviteIdx = fullArgs.indexOf("invite");
    const afterInvite = inviteIdx >= 0 ? fullArgs.slice(inviteIdx + 1) : [];
    const flagParts = getSameSubcommandSuggestionFlags(afterInvite);
    const cmd2 = getCommandNamePlain(
      `teams invite <email> ${flagParts.join(" ")}`.trim()
    );
    outputActionRequired(
      client,
      {
        status: "action_required",
        reason: "missing_arguments",
        action: "missing_arguments",
        message: `In non-interactive mode at least one email is required. Run: ${cmd2}`,
        next: [
          {
            command: cmd2,
            when: "to invite teammates (replace <email> with a teammate email)"
          }
        ]
      },
      1
    );
    return 1;
  }
  output_manager_default.spinner("Fetching teams");
  const teams2 = await getTeams(client);
  const currentTeam = teams2.find((team) => team.id === currentTeamId);
  output_manager_default.spinner("Fetching user information");
  const user = await getUser(client);
  domains.push(user.email.split("@")[1]);
  if (!currentTeam) {
    const err = `You can't run this command under ${param(
      user.username || user.email
    )}.
Please select a team scope using ${getCommandName(
      `switch`
    )} or use ${cmd("--scope")}`;
    if (client.nonInteractive) {
      const switchCmd = withGlobalFlags9(client, "teams switch <slug>");
      outputAgentError(
        client,
        {
          status: "error",
          reason: "team_scope_required",
          message: `Team scope is required for teams invite. Run ${switchCmd} or use --scope.`,
          next: [
            {
              command: switchCmd,
              when: "to select a team scope (replace <slug> with your team slug)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(err);
    return 1;
  }
  output_manager_default.log(
    introMsg || `Inviting team members to ${import_chalk185.default.bold(currentTeam.name)}`
  );
  telemetry2.trackCliArgumentEmail(emails);
  if (emails.length > 0) {
    for (const email3 of emails) {
      if (email.test(email3)) {
        output_manager_default.spinner(email3);
        const elapsed2 = stamp_default();
        let userInfo = null;
        try {
          const res = await inviteUserToTeam(client, currentTeam.id, email3);
          userInfo = res.username;
        } catch (err) {
          if (isAPIError(err) && err.code === "user_not_found") {
            if (client.nonInteractive) {
              const fullArgs = client.argv.slice(2);
              const inviteIdx = fullArgs.indexOf("invite");
              const afterInvite = inviteIdx >= 0 ? fullArgs.slice(inviteIdx + 1) : [];
              const flagParts = getSameSubcommandSuggestionFlags(afterInvite);
              const retryCmd = getCommandNamePlain(
                `teams invite <email> ${flagParts.join(" ")}`.trim()
              );
              outputAgentError(
                client,
                {
                  status: "error",
                  reason: "user_not_found",
                  message: `No user exists with the email address "${email3}".`,
                  next: [
                    {
                      command: retryCmd
                    }
                  ]
                },
                1
              );
            }
            output_manager_default.error(`No user exists with the email address "${email3}".`);
            return 1;
          }
          throw err;
        }
        output_manager_default.log(
          `${import_chalk185.default.cyan(chars_default.tick)} ${email3}${userInfo ? ` (${userInfo})` : ""} ${elapsed2()}`
        );
      } else {
        output_manager_default.log(`${import_chalk185.default.red(`\u2716 ${email3}`)} ${import_chalk185.default.gray("[invalid]")}`);
      }
    }
    return 0;
  }
  const inviteUserPrefix = "Invite User".padEnd(14);
  const sentEmailPrefix = "Sent Email".padEnd(14);
  let hasError = false;
  let email2;
  do {
    email2 = "";
    try {
      email2 = await client.input.text({
        message: `- ${inviteUserPrefix}`,
        validate: validateEmail
      });
    } catch (err) {
      if (!(0, import_error_utils18.isError)(err) || err.message !== "USER_ABORT") {
        throw err;
      }
    }
    let elapsed2;
    if (email2) {
      elapsed2 = stamp_default();
      output_manager_default.spinner(inviteUserPrefix + email2);
      try {
        const { username } = await inviteUserToTeam(
          client,
          currentTeam.id,
          email2
        );
        email2 = `${email2}${username ? ` (${username})` : ""} ${elapsed2()}`;
        emails.push(email2);
        output_manager_default.log(`${import_chalk185.default.cyan(chars_default.tick)} ${sentEmailPrefix}${email2}`);
        if (hasError) {
          hasError = false;
          process.stderr.write(eraseLines(emails.length + 2));
          output_manager_default.log(
            introMsg || `Inviting team members to ${import_chalk185.default.bold(currentTeam.name)}`
          );
          for (const email3 of emails) {
            output_manager_default.log(`${import_chalk185.default.cyan(chars_default.tick)} ${inviteUserPrefix}${email3}`);
          }
        }
      } catch (err) {
        output_manager_default.stopSpinner();
        process.stderr.write(eraseLines(emails.length + 2));
        output_manager_default.error((0, import_error_utils18.errorToString)(err));
        hasError = true;
        for (const email3 of emails) {
          output_manager_default.log(`${import_chalk185.default.cyan(chars_default.tick)} ${sentEmailPrefix}${email3}`);
        }
      }
    }
  } while (email2 !== "");
  output_manager_default.stopSpinner();
  process.stderr.write(eraseLines(emails.length + 2));
  const n = emails.length;
  if (emails.length === 0) {
    output_manager_default.log(noopMsg);
  } else {
    output_manager_default.success(`Invited ${n} teammate${n > 1 ? "s" : ""}`);
    for (const email3 of emails) {
      output_manager_default.log(`${import_chalk185.default.cyan(chars_default.tick)} ${inviteUserPrefix}${email3}`);
    }
  }
  return 0;
}

// src/util/teams/create-team.ts
async function createTeam(client, { slug }, opts) {
  const fetchOpts = {
    method: "POST",
    body: { slug },
    useCurrentTeam: false,
    bailOn429: true
  };
  if (opts?.signal) {
    fetchOpts.signal = opts.signal;
  }
  const body = await client.fetch(`/teams`, fetchOpts);
  return body;
}

// src/util/teams/patch-team.ts
async function patchTeam(client, teamId, payload) {
  const body = await client.fetch(
    `/teams/${encodeURIComponent(teamId)}`,
    {
      method: "PATCH",
      body: payload
    }
  );
  if (body && typeof body === "object" && "ok" in body) {
    throw new Error("PATCH /teams returned non-JSON response");
  }
  return body;
}

// src/commands/teams/add.ts
var import_error_utils19 = __toESM(require_dist(), 1);
var validateSlug = (value) => /^[a-z]+[a-z0-9_-]*$/.test(value);
var validateName = (value) => /^[ a-zA-Z0-9_-]+$/.test(value);
var teamUrlPrefix = "Team URL".padEnd(14) + import_chalk186.default.gray("vercel.com/");
var teamNamePrefix = "Team Name".padEnd(14);
var CREATE_TEAM_TIMEOUT_MS = 3e4;
var TIMEOUT_HINT = "The request took too long and was cancelled. The team URL may already be taken, or the server may be slow. Try a different slug (e.g. your-company-name) or try again later. Run with `--debug` to see where the request is getting stuck.";
function createTeamWithTimeout(client, slug) {
  const controller = new AbortController();
  const timeoutId = setTimeout(
    () => controller.abort(),
    CREATE_TEAM_TIMEOUT_MS
  );
  output_manager_default.debug(
    `Creating team with slug "${slug}" (timeout: ${CREATE_TEAM_TIMEOUT_MS / 1e3}s, apiUrl: ${client.apiUrl})`
  );
  const promise = createTeam(client, { slug }, { signal: controller.signal }).then((result) => {
    clearTimeout(timeoutId);
    return result;
  }).catch((err) => {
    clearTimeout(timeoutId);
    throw err;
  });
  return Promise.race([
    promise,
    new Promise(
      (_, reject) => setTimeout(
        () => reject(new Error("Request timed out.")),
        CREATE_TEAM_TIMEOUT_MS + 500
      )
    )
  ]);
}
function formatCreateTeamError(err, slug) {
  const isAbort = (0, import_error_utils19.isError)(err) && err.name === "AbortError";
  const isTimeoutMsg = (0, import_error_utils19.isError)(err) && err.message === "Request timed out.";
  if (isAbort || isTimeoutMsg) {
    return TIMEOUT_HINT;
  }
  if (isAPIError(err) && err.status === 429) {
    const retryMs = err.retryAfterMs;
    const retrySec = typeof retryMs === "number" && retryMs >= 0 ? Math.ceil(retryMs / 1e3) : 0;
    const formatWait = (sec) => {
      if (sec < 60)
        return `${sec} seconds`;
      if (sec < 3600)
        return `${sec} seconds (${Math.ceil(sec / 60)} minutes)`;
      return `${sec} seconds (${(sec / 3600).toFixed(1)} hours)`;
    };
    const waitHint = retrySec > 0 ? ` Try again in ${formatWait(retrySec)}.` : " Wait a minute or two, then try again.";
    return "Rate limited (429 Too Many Requests). The Vercel API limits how often you can create teams." + waitHint + " (The API sends when the rate limit window resets, often a fixed time like the next hour or midnight UTC.) Dashboard: https://vercel.com/account.";
  }
  if (isAPIError(err) && err.status === 400) {
    const msg = (err.serverMessage || err.message || "").toLowerCase();
    const slugHint = msg.includes("slug") || msg.includes("cannot be used") ? `That team URL (${import_chalk186.default.cyan(`vercel.com/${slug}`)}) is not available. It may already be taken. ` : "";
    const paymentHint = msg.includes("payment") ? "A payment method is required to create a team. " : "";
    const prefix = slugHint || paymentHint;
    return prefix ? `${prefix}${prefix.trim() ? "\n" : ""}${err.serverMessage || err.message}` : err.serverMessage || err.message;
  }
  return (0, import_error_utils19.errorToString)(err);
}
function formatCreateTeamErrorPlain(err, slug) {
  const isAbort = (0, import_error_utils19.isError)(err) && err.name === "AbortError";
  const isTimeoutMsg = (0, import_error_utils19.isError)(err) && err.message === "Request timed out.";
  if (isAbort || isTimeoutMsg) {
    return TIMEOUT_HINT;
  }
  if (isAPIError(err) && err.status === 429) {
    return formatCreateTeamError(err, slug);
  }
  if (isAPIError(err) && err.status === 400) {
    const msg = (err.serverMessage || err.message || "").toLowerCase();
    const slugHint = msg.includes("slug") || msg.includes("cannot be used") ? `That team URL (vercel.com/${slug}) is not available. It may already be taken. ` : "";
    const paymentHint = msg.includes("payment") ? "A payment method is required to create a team. " : "";
    const prefix = slugHint || paymentHint;
    return prefix ? `${prefix}${prefix.trim() ? "\n" : ""}${err.serverMessage || err.message}` : err.serverMessage || err.message;
  }
  return (0, import_error_utils19.errorToString)(err);
}
var VERCEL_ACCOUNT_BILLING_URL = "https://vercel.com/account/billing";
function createTeamErrorReason(err) {
  if (isAPIError(err) && err.status === 400) {
    const msg = (err.serverMessage || err.message || "").toLowerCase();
    if (msg.includes("payment"))
      return "payment_required";
    if (msg.includes("slug") || msg.includes("cannot be used"))
      return "slug_unavailable";
  }
  if (isAPIError(err) && err.status === 429)
    return "rate_limited";
  if ((0, import_error_utils19.isError)(err) && (err.name === "AbortError" || err.message === "Request timed out.")) {
    return "timeout";
  }
  return "team_creation_failed";
}
async function add11(client, argv = []) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand10.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const slugFlag = parsedArgs.flags["--slug"];
  const nameFlag = parsedArgs.flags["--name"];
  if (client.nonInteractive) {
    const missing = [];
    if (!slugFlag || !slugFlag.trim())
      missing.push("--slug");
    if (!nameFlag || !nameFlag.trim())
      missing.push("--name");
    if (missing.length > 0) {
      const fullArgs = client.argv.slice(2);
      const addIdx = fullArgs.indexOf("add");
      const afterAdd = addIdx >= 0 ? fullArgs.slice(addIdx + 1) : [];
      const flagParts = getSameSubcommandSuggestionFlags(afterAdd);
      const cmd2 = getCommandNamePlain(
        `teams add --slug <slug> --name <name> ${flagParts.join(" ")}`.trim()
      );
      const requiredPlain = missing.join(" and ");
      const verb = missing.length === 1 ? "is" : "are";
      outputActionRequired(
        client,
        {
          status: "action_required",
          reason: "missing_arguments",
          action: "missing_arguments",
          message: `In non-interactive mode ${requiredPlain} ${verb} required. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to create a team non-interactively"
            }
          ]
        },
        1
      );
    }
    const slug2 = slugFlag.trim().toLowerCase();
    const name2 = nameFlag.trim();
    if (!validateSlug(slug2)) {
      const msg = `Invalid ${param("--slug")}: must start with a letter and contain only lowercase letters, numbers, hyphens, and underscores (e.g. ${param("acme")})`;
      const msgPlain = "Invalid --slug: must start with a letter and contain only lowercase letters, numbers, hyphens, and underscores (e.g. acme)";
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "invalid_slug",
            message: msgPlain,
            next: [
              {
                command: getCommandNamePlain(
                  "teams add --slug <slug> --name <name>"
                )
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(msg);
      return 1;
    }
    if (!validateName(name2)) {
      const msg = `Invalid ${param("--name")}: only letters, numbers, spaces, hyphens, and underscores allowed`;
      const msgPlain = "Invalid --name: only letters, numbers, spaces, hyphens, and underscores allowed";
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "invalid_name",
            message: msgPlain
          },
          1
        );
      }
      output_manager_default.error(msg);
      return 1;
    }
    output_manager_default.spinner(teamUrlPrefix + slug2);
    let team2;
    try {
      team2 = await createTeamWithTimeout(client, slug2);
    } catch (err) {
      output_manager_default.stopSpinner();
      if (client.nonInteractive) {
        let message = formatCreateTeamErrorPlain(err, slug2);
        const reason = createTeamErrorReason(err);
        const next = [];
        if (reason === "payment_required") {
          message += ` Add a payment method at ${VERCEL_ACCOUNT_BILLING_URL}, then retry.`;
          next.push({
            command: openUrlInBrowserCommand(VERCEL_ACCOUNT_BILLING_URL),
            when: "To open the billing page in your browser"
          });
        }
        outputAgentError(
          client,
          {
            status: "error",
            reason,
            message,
            ...next.length > 0 && { next }
          },
          1
        );
      }
      output_manager_default.error(formatCreateTeamError(err, slug2));
      return 1;
    }
    output_manager_default.stopSpinner();
    output_manager_default.spinner(teamNamePrefix + name2);
    try {
      const res2 = await patchTeam(client, team2.id, { name: name2 });
      team2 = Object.assign(team2, res2);
    } catch (err) {
      output_manager_default.stopSpinner();
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "team_update_failed",
            message: (0, import_error_utils19.errorToString)(err)
          },
          1
        );
      }
      output_manager_default.error((0, import_error_utils19.errorToString)(err));
      return 1;
    }
    output_manager_default.stopSpinner();
    client.config.currentTeam = team2.id;
    writeToConfigFile(client.config);
    output_manager_default.success(
      `Team ${import_chalk186.default.bold(team2.name)} (${import_chalk186.default.cyan(`vercel.com/${slug2}`)}) created.`
    );
    return 0;
  }
  let slug = slugFlag?.trim().toLowerCase();
  let team;
  let elapsed2;
  output_manager_default.log(
    `Pick a team identifier for its URL (e.g.: ${import_chalk186.default.cyan(
      "`vercel.com/acme`"
    )})`
  );
  do {
    try {
      slug = await client.input.text({
        message: `- ${teamUrlPrefix}`,
        validate: validateSlug,
        default: slug
      });
    } catch (err) {
      if ((0, import_error_utils19.isError)(err) && err.message === "USER_ABORT") {
        output_manager_default.log("Canceled");
        return 0;
      }
      throw err;
    }
    elapsed2 = stamp_default();
    output_manager_default.spinner(teamUrlPrefix + slug);
    try {
      team = await createTeamWithTimeout(client, slug);
    } catch (err) {
      output_manager_default.stopSpinner();
      output_manager_default.print(eraseLines(2));
      output_manager_default.error(formatCreateTeamError(err, slug));
      slug = void 0;
    }
  } while (!team);
  output_manager_default.stopSpinner();
  process.stdout.write(eraseLines(2));
  output_manager_default.success(`Team created ${elapsed2()}`);
  output_manager_default.log(`${import_chalk186.default.cyan(`${chars_default.tick} `) + teamUrlPrefix + slug}
`);
  output_manager_default.log("Pick a display name for your team");
  let name = nameFlag?.trim();
  try {
    name = await client.input.text({
      message: `- ${teamNamePrefix}`,
      validate: validateName,
      default: name
    });
  } catch (err) {
    if ((0, import_error_utils19.isError)(err) && err.message === "USER_ABORT") {
      output_manager_default.log("No name specified");
      return 2;
    }
    throw err;
  }
  elapsed2 = stamp_default();
  output_manager_default.spinner(teamNamePrefix + name);
  const res = await patchTeam(client, team.id, { name });
  output_manager_default.stopSpinner();
  process.stdout.write(eraseLines(2));
  team = Object.assign(team, res);
  output_manager_default.success(`Team name saved ${elapsed2()}`);
  output_manager_default.log(`${import_chalk186.default.cyan(`${chars_default.tick} `) + teamNamePrefix + team.name}
`);
  output_manager_default.spinner("Saving");
  client.config.currentTeam = team.id;
  writeToConfigFile(client.config);
  output_manager_default.stopSpinner();
  await invite(client, [], {
    introMsg: "Invite your teammates! When done, press enter on an empty field",
    noopMsg: `You can invite teammates later by running ${getCommandName(
      `teams invite`
    )}`
  });
  return 0;
}

// src/commands/teams/switch.ts
var import_chalk187 = __toESM(require_source(), 1);

// src/util/telemetry/commands/teams/switch.ts
var TeamsSwitchTelemetryClient = class extends TelemetryClient {
  trackCliArgumentName(slug) {
    if (slug) {
      this.trackCliArgument({
        arg: "slug",
        value: this.redactedValue
      });
    }
  }
};

// src/commands/teams/switch.ts
function withGlobalFlags10(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
var updateCurrentTeam = (config, team) => {
  if (team) {
    config.currentTeam = team.id;
  } else {
    delete config.currentTeam;
  }
  writeToConfigFile(config);
};
async function warnIfStaleLinkExists(client, newOrgId) {
  let link = null;
  try {
    link = await getLinkFromDir(
      getVercelDirectory(client.cwd)
    );
  } catch (_error) {
    link = null;
  }
  if (!link || link.orgId === newOrgId) {
    return;
  }
  output_manager_default.warn(
    `This directory is linked to a project under a different team/scope. Commands like \`deploy\` will still use the linked project's team. Run \`vc link\` to re-link.`
  );
}
async function change(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(switchSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  let {
    args: [desiredSlug]
  } = parsedArgs;
  if (client.nonInteractive && !desiredSlug) {
    const fullArgs = client.argv.slice(2);
    const switchIdx = fullArgs.findIndex((a) => a === "switch" || a === "change");
    const afterSwitch = switchIdx >= 0 ? fullArgs.slice(switchIdx + 1) : fullArgs;
    const afterPositional = afterSwitch.length > 0 && !afterSwitch[0].startsWith("-") ? afterSwitch.slice(1) : afterSwitch;
    const flagTail = getSameSubcommandSuggestionFlags(afterPositional);
    const cmd2 = getCommandNamePlain(
      `teams switch <slug> ${flagTail.join(" ")}`.trim()
    );
    outputActionRequired(
      client,
      {
        status: "action_required",
        reason: "missing_arguments",
        action: "missing_arguments",
        message: `In non-interactive mode a team slug (or username for personal scope) is required. Run: ${cmd2}`,
        next: [
          {
            command: cmd2,
            when: "to switch scope (replace <slug> with team slug)"
          },
          {
            command: withGlobalFlags10(client, "teams list"),
            when: "to list teams and slugs"
          }
        ]
      },
      1
    );
    return 1;
  }
  const { config, telemetryEventStore } = client;
  const telemetry2 = new TeamsSwitchTelemetryClient({
    opts: {
      store: telemetryEventStore
    }
  });
  telemetry2.trackCliArgumentName(desiredSlug);
  output_manager_default.spinner("Fetching teams information");
  const [user, teams2] = await Promise.all([getUser(client), getTeams(client)]);
  const defaultTeamId = user.version === "northstar" ? user.defaultTeamId : void 0;
  const currentTeamId = config.currentTeam || defaultTeamId;
  const personalScopeSelected = !currentTeamId;
  const currentTeam = currentTeamId ? teams2.find((team) => team.id === currentTeamId) : void 0;
  if (!personalScopeSelected && !currentTeam) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "current_team_invalid",
          message: "You are not a member of the current team anymore. Switch to a valid team or personal scope.",
          next: [
            {
              command: withGlobalFlags10(client, "teams list"),
              when: "to list teams and slugs you can switch to"
            },
            {
              command: withGlobalFlags10(client, "login"),
              when: "to re-authenticate if your session or team membership changed"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`You are not a member of the current team anymore.`);
    return 1;
  }
  if (!desiredSlug) {
    const teamChoices = teams2.slice(0).sort((a, b) => {
      return a.name.toLowerCase().localeCompare(b.name.toLowerCase());
    }).map((team) => {
      let title4 = `${team.name} (${team.slug})`;
      const selected = team.id === currentTeam?.id;
      if (selected) {
        title4 += ` ${import_chalk187.default.bold("(current)")}`;
      }
      if (team.limited) {
        title4 += ` ${emoji("locked")}`;
      }
      return {
        name: title4,
        value: team.slug,
        short: team.slug,
        selected
      };
    });
    let suffix = personalScopeSelected ? ` ${import_chalk187.default.bold("(current)")}` : "";
    if (user.limited) {
      suffix += ` ${emoji("locked")}`;
    }
    const personalAccountChoice = user.version === "northstar" ? [] : [
      { separator: "Personal Account" },
      {
        name: `${user.name || user.email} (${user.username})${suffix}`,
        value: user.username,
        short: user.username,
        selected: personalScopeSelected
      }
    ];
    const choices = [
      ...personalAccountChoice,
      { separator: "Teams" },
      ...teamChoices
    ];
    output_manager_default.stopSpinner();
    if (client.nonInteractive) {
      const fullArgs = client.argv.slice(2);
      const switchIdx = fullArgs.findIndex(
        (a) => a === "switch" || a === "change"
      );
      const afterSwitch = switchIdx >= 0 ? fullArgs.slice(switchIdx + 1) : fullArgs;
      const afterPositional = afterSwitch.length > 0 && !afterSwitch[0].startsWith("-") ? afterSwitch.slice(1) : afterSwitch;
      const flagTail = getSameSubcommandSuggestionFlags(afterPositional);
      const cmd2 = getCommandNamePlain(
        `teams switch <slug> ${flagTail.join(" ")}`.trim()
      );
      outputActionRequired(
        client,
        {
          status: "action_required",
          reason: "missing_arguments",
          action: "missing_arguments",
          message: `In non-interactive mode a team slug (or username for personal scope) is required. Run: ${cmd2}`,
          next: [
            {
              command: cmd2,
              when: "to switch scope (replace <slug> with team slug)"
            },
            {
              command: withGlobalFlags10(client, "teams list"),
              when: "to list teams and slugs"
            }
          ]
        },
        1
      );
      return 1;
    }
    desiredSlug = await list(client, {
      message: "Switch to:",
      choices,
      eraseFinalAnswer: true
    });
  }
  if (!desiredSlug) {
    output_manager_default.log("No changes made.");
    return 0;
  }
  if (desiredSlug === user.username || desiredSlug === user.email) {
    if (user.version === "northstar") {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "personal_scope_not_allowed",
            message: "You cannot set your Personal Account as the scope in this account type."
          },
          1
        );
      }
      output_manager_default.error("You cannot set your Personal Account as the scope.");
      return 1;
    }
    if (personalScopeSelected) {
      output_manager_default.log("No changes made");
      return 0;
    }
    if (user.limited) {
      await client.reauthenticate({
        scope: user.username,
        teamId: null
      });
    }
    updateCurrentTeam(config);
    output_manager_default.success(
      `Your account (${import_chalk187.default.bold(user.username)}) is now active!`
    );
    await warnIfStaleLinkExists(client, user.id);
    return 0;
  }
  const newTeam = teams2.find((team) => team.slug === desiredSlug);
  if (!newTeam) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "scope_not_accessible",
          message: `You do not have permission to access scope "${desiredSlug}".`,
          next: [
            {
              command: withGlobalFlags10(client, "teams list")
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `You do not have permission to access scope ${import_chalk187.default.bold(desiredSlug)}.`
    );
    return 1;
  }
  if (newTeam.slug === currentTeam?.slug) {
    output_manager_default.log("No changes made");
    return 0;
  }
  if (newTeam.limited) {
    const samlEnabled = newTeam.saml?.connection?.state === "active";
    await client.reauthenticate({
      teamId: newTeam.id,
      scope: newTeam.slug,
      enforced: samlEnabled && newTeam.saml?.enforced === true
    });
  }
  updateCurrentTeam(config, newTeam);
  output_manager_default.success(
    `The team ${import_chalk187.default.bold(newTeam.name)} (${newTeam.slug}) is now active!`
  );
  await warnIfStaleLinkExists(client, newTeam.id);
  return 0;
}

// src/commands/teams/request.ts
function withGlobalFlags11(client, commandTemplate) {
  const flags = getGlobalFlagsOnlyFromArgs(client.argv.slice(2));
  return getCommandNamePlain(`${commandTemplate} ${flags.join(" ")}`.trim());
}
function optionRoot(flagToken) {
  if (flagToken.startsWith("--")) {
    return flagToken.includes("=") ? `--${flagToken.slice(2).split("=")[0]}` : flagToken;
  }
  return flagToken;
}
function teamsRequestNextCommand(client) {
  const fullArgs = client.argv.slice(2);
  const reqIdx = fullArgs.findIndex(
    (a) => a === "request" || a === "access-request"
  );
  const afterRequest = reqIdx >= 0 ? fullArgs.slice(reqIdx + 1) : [];
  const globalParts = getGlobalFlagsOnlyFromArgs(fullArgs);
  const subParts = getSameSubcommandSuggestionFlags(afterRequest);
  const seen = /* @__PURE__ */ new Set();
  for (const g of globalParts) {
    seen.add(optionRoot(g));
  }
  const extras = [];
  for (let i = 0; i < subParts.length; ) {
    const a = subParts[i];
    const root = optionRoot(a);
    if (seen.has(root)) {
      i += 1;
      if (!a.includes("=") && i < subParts.length && !subParts[i].startsWith("-")) {
        i += 1;
      }
      continue;
    }
    seen.add(root);
    extras.push(a);
    i += 1;
    if (!a.includes("=") && i < subParts.length && !subParts[i].startsWith("-")) {
      extras.push(subParts[i]);
      i += 1;
    }
  }
  const base = getCommandNameWithGlobalFlags(
    "teams request [userId]",
    client.argv
  );
  if (extras.length === 0) {
    return base;
  }
  return getCommandNamePlain(`${base} ${extras.join(" ")}`.trim());
}
function apiFailureReason(err) {
  if (typeof err.code === "string" && err.code) {
    return err.code;
  }
  if (typeof err.code === "number" && Number.isFinite(err.code)) {
    return String(err.code);
  }
  return `http_${err.status}`;
}
async function request(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(requestSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    if (client.nonInteractive) {
      const cmdStr = teamsRequestNextCommand(client);
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: "Invalid number of arguments. At most one optional userId is allowed.",
          next: [
            {
              command: cmdStr,
              when: "to check join-request status (omit userId for the current user)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      "Invalid number of arguments. Usage: `vercel teams request [userId]`"
    );
    return 2;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: formatResult.error
        },
        1
      );
    }
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const { currentTeam: teamId } = client.config;
  if (!teamId) {
    const switchCmd = withGlobalFlags11(client, "teams switch <slug>");
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "team_scope_required",
          message: `Team scope is required for teams request. Run ${switchCmd} or use --scope.`,
          next: [
            {
              command: switchCmd,
              when: "to select a team scope (replace <slug> with your team slug)"
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(
      `This command requires a team scope. Run ${cmd("teams switch")} first.`
    );
    return 1;
  }
  const userId = parsedArgs.args[0];
  const path3 = userId ? `/v1/teams/${encodeURIComponent(teamId)}/request/${encodeURIComponent(userId)}` : `/v1/teams/${encodeURIComponent(teamId)}/request`;
  let data;
  try {
    data = await client.fetch(path3);
  } catch (err) {
    if (client.nonInteractive && isAPIError(err)) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: apiFailureReason(err),
          message: err.serverMessage || err.message,
          next: [
            {
              command: teamsRequestNextCommand(client),
              when: "to retry after resolving the error (same flags and optional userId)"
            }
          ]
        },
        1
      );
    }
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(data, null, 2)}
`);
    return 0;
  }
  output_manager_default.log(
    `Team: ${String(data.teamName ?? "")} (${String(data.teamSlug ?? "")})`
  );
  output_manager_default.log(`Confirmed: ${String(data.confirmed ?? "")}`);
  if (data.accessRequestedAt != null) {
    output_manager_default.log(`Access requested at: ${String(data.accessRequestedAt)}`);
  }
  if (data.joinedFrom && typeof data.joinedFrom === "object") {
    output_manager_default.log(`Joined from: ${JSON.stringify(data.joinedFrom)}`);
  }
  return 0;
}

// src/commands/teams/members.ts
var import_chalk188 = __toESM(require_source(), 1);
async function members2(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(membersSubcommand2.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  const next = parsedArgs.flags["--next"];
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  if (typeof next !== "undefined" && !Number.isInteger(next)) {
    output_manager_default.error("Please provide a number for flag `--next`");
    return 1;
  }
  const teamId = client.config.currentTeam;
  if (!teamId) {
    output_manager_default.error(
      "Team scope is required. Run `vercel teams switch <slug>` or pass `--scope`."
    );
    return 1;
  }
  const query = new URLSearchParams({ limit: "20" });
  if (next) {
    query.set("next", String(next));
  }
  const result = await client.fetch(
    `/v2/teams/${teamId}/members?${query}`
  );
  const members3 = result.members || [];
  const { pagination } = result;
  if (asJson) {
    client.stdout.write(
      `${JSON.stringify({ members: members3, pagination }, null, 2)}
`
    );
    return 0;
  }
  const rows = [
    ["uid", "Identity", "Role"].map((str) => (0, import_chalk188.gray)(str)),
    ...members3.map((member) => [
      member.uid,
      member.username || member.email || "",
      member.role || ""
    ])
  ];
  client.stderr.write(`${table(rows, { hsep: 3 })}
`);
  if (pagination?.count === 20) {
    const flags = getCommandFlags(parsedArgs.flags, ["--next", "-N"]);
    const nextCmd = `${packageName} teams members${flags} --next ${pagination.next}`;
    output_manager_default.log(`To display the next page run ${cmd(nextCmd)}`);
  }
  return 0;
}

// src/commands/teams/sso.ts
var import_chalk189 = __toESM(require_source(), 1);
async function sso(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(ssoSubcommand.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const { team } = await getScope(client);
  if (!team) {
    output_manager_default.error("No team context. Run `vercel switch` or use --scope.");
    return 1;
  }
  try {
    const full = await client.fetch(
      `/teams/${encodeURIComponent(team.id)}`
    );
    const payload = {
      teamId: full.id,
      slug: full.slug,
      name: full.name,
      saml: full.saml ?? null
    };
    if (formatResult.jsonOutput) {
      client.stdout.write(`${JSON.stringify(payload, null, 2)}
`);
      return 0;
    }
    output_manager_default.log(`${import_chalk189.default.bold("Team")} ${full.name} (${import_chalk189.default.cyan(full.slug)})`);
    if (full.saml) {
      output_manager_default.log(
        `${import_chalk189.default.cyan("SAML enforced:")} ${String(full.saml.enforced)}`
      );
      if (full.saml.connection?.state) {
        output_manager_default.log(
          `${import_chalk189.default.cyan("Connection state:")} ${full.saml.connection.state}`
        );
      }
    } else {
      output_manager_default.log(
        "No SAML configuration on this team (or not visible to your token)."
      );
    }
    return 0;
  } catch (err) {
    if (isAPIError(err)) {
      output_manager_default.error(err.serverMessage || `API error (${err.status})`);
      return 1;
    }
    throw err;
  }
}

// src/util/telemetry/commands/teams/index.ts
var TeamsTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandInvite(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "invite",
        value: actual
      });
    }
  }
  trackCliSubcommandAdd(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "add",
        value: actual
      });
    }
  }
  trackCliSubcommandSwitch(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "switch",
        value: actual
      });
    }
  }
  trackCliSubcommandList(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "list",
        value: actual
      });
    }
  }
  trackCliSubcommandRequest(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "request",
        value: actual
      });
    }
  }
  trackCliSubcommandSso(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "sso",
        value: actual
      });
    }
  }
  trackCliSubcommandMembers(actual) {
    if (actual) {
      this.trackCliSubcommand({
        subcommand: "members",
        value: actual
      });
    }
  }
};

// src/commands/teams/index.ts
var COMMAND_CONFIG37 = {
  list: ["ls", "list"],
  switch: ["switch", "change"],
  add: ["create", "add"],
  invite: ["invite"],
  request: ["request", "access-request"],
  sso: ["sso"],
  members: ["members", "member"]
};
async function teams(client) {
  const telemetry2 = new TeamsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(teamsCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "invalid_arguments",
          message: error instanceof Error ? error.message : String(error)
        },
        1
      );
    }
    printError(error);
    return 1;
  }
  if (parsedArgs.args[0] === "switch") {
    parsedArgs.args.unshift("teams");
  }
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG37
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("teams", subcommand);
    output_manager_default.print(help(teamsCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: teamsCommand, columns: client.stderr.columns })
    );
  }
  switch (subcommand) {
    case "list": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(listSubcommand16);
        return 2;
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return list14(client, args);
    }
    case "switch": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(switchSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSwitch(subcommandOriginal);
      return change(client, args);
    }
    case "add": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(addSubcommand10);
        return 2;
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add11(client, args);
    }
    case "invite": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(inviteSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandInvite(subcommandOriginal);
      return invite(client, args);
    }
    case "request": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(requestSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandRequest(subcommandOriginal);
      return request(client, args);
    }
    case "sso": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(ssoSubcommand);
        return 2;
      }
      telemetry2.trackCliSubcommandSso(subcommandOriginal);
      return sso(client, args);
    }
    case "members": {
      if (needHelp) {
        telemetry2.trackCliFlagHelp("teams", subcommandOriginal);
        printHelp(membersSubcommand2);
        return 2;
      }
      telemetry2.trackCliSubcommandMembers(subcommandOriginal);
      return members2(client, args);
    }
    default: {
      const fallback = await tryOpenApiFallback(
        client,
        parsedArgs.args.slice(1),
        resolveOpenApiTagForTeamsCli
      );
      if (fallback !== null) {
        return fallback;
      }
      output_manager_default.error(
        "Please specify a valid subcommand: add | ls | switch | invite | request | sso | members"
      );
      output_manager_default.print(help(teamsCommand, { columns: client.stderr.columns }));
      return 2;
    }
  }
}

// src/commands/tokens/ls.ts
var import_chalk190 = __toESM(require_source(), 1);
async function ls8(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand17.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const validation = validateJsonOutput(parsedArgs.flags);
  if (!validation.valid) {
    output_manager_default.error(validation.error);
    return 1;
  }
  const asJson = validation.jsonOutput;
  const limit = parsedArgs.flags["--limit"];
  if (typeof limit === "number" && (limit < 1 || limit > 100)) {
    output_manager_default.error("`--limit` must be between 1 and 100.");
    return 1;
  }
  const params = new URLSearchParams();
  if (typeof limit === "number") {
    params.set("limit", String(limit));
  }
  const qs = params.toString();
  const path3 = `/v6/user/tokens${qs ? `?${qs}` : ""}`;
  const result = await client.fetch(path3, {
    useCurrentTeam: false
  });
  const tokens = result.tokens || [];
  if (asJson) {
    client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
    return 0;
  }
  if (tokens.length === 0) {
    output_manager_default.log("No tokens found.");
    return 0;
  }
  const rows = [
    ["id", "name", "type", "active"].map((h) => (0, import_chalk190.gray)(h)),
    ...tokens.map((t) => [
      t.id ?? "",
      t.name ?? "",
      t.type ?? "",
      t.active === void 0 ? "" : String(t.active)
    ])
  ];
  client.stderr.write(`${table(rows, { hsep: 2 })}
`);
  return 0;
}

// src/commands/tokens/add.ts
var import_open17 = __toESM(require_open(), 1);
import { KNOWN_AGENTS as KNOWN_AGENTS2 } from "@vercel/detect-agent";
var VERCEL_ACCOUNT_TOKENS_URL = "https://vercel.com/account/tokens";
var CLASSIC_TOKEN_GUIDANCE = `Creating a new token requires a classic personal access token. Sessions from "vercel login" use OAuth and cannot call the create-token API.

What to do:
  \u2022 Open the dashboard and create a classic token: Account \u2192 Settings \u2192 Tokens (${VERCEL_ACCOUNT_TOKENS_URL})
  \u2022 Authenticate the CLI with that token: set VERCEL_TOKEN to the token value, or use --token <token>
  \u2022 Run this command again with the same arguments.`;
var CLASSIC_TOKEN_AGENT_HINT = `The Vercel API only allows creating personal tokens when the CLI is authenticated with a classic personal access token (dashboard: Account \u2192 Settings \u2192 Tokens). OAuth login cannot mint new tokens. After creating a classic token, set VERCEL_TOKEN or pass --token, then re-run the same command.`;
var USER_SCOPE_GUIDANCE = `Your token is classic but does not include full user (account) scope, so the API cannot create a new personal token.

Common cases:
  \u2022 Team-only, project-only, or limited product tokens (for example some "vcp_" tokens) cannot mint new tokens.
  \u2022 Create a new classic token in the dashboard (Account \u2192 Settings \u2192 Tokens) with access to your personal account / full token permissions.
  \u2022 Then set VERCEL_TOKEN or use --token with that value and run this command again.`;
var USER_SCOPE_AGENT_HINT = `Creating a personal token requires a classic token that includes full user account scope. Team-scoped, project-scoped, or narrow product tokens are rejected with HTTP 403. Create a classic personal access token at ${VERCEL_ACCOUNT_TOKENS_URL} with account-level access, set VERCEL_TOKEN or --token, then re-run.`;
function normalizeApiMessage(message) {
  return message.replace(/\s*\(\d{3}\)\s*$/, "").trim();
}
function getSanitizedRerunCommand(client) {
  const rerunArgs = stripSensitiveAuthArgs(client.argv.slice(2));
  return getCommandNamePlain(rerunArgs.join(" ").trim());
}
function isClassicTokenRequiredForCreateError(err) {
  if (!isAPIError(err) || err.status !== 403) {
    return false;
  }
  const raw = err.serverMessage || err.message;
  return normalizeApiMessage(raw) === "Only user authentication tokens can be used to create new tokens.";
}
function isTokenUserScopeRequiredError(err) {
  if (!isAPIError(err) || err.status !== 403) {
    return false;
  }
  const raw = normalizeApiMessage(err.serverMessage || err.message);
  return raw.startsWith(
    'To create a token you must be authenticated to scope "'
  );
}
function tokensAddRecoveryNext(client, rerun, rerunWhen) {
  return [
    {
      command: openUrlInBrowserCommand(VERCEL_ACCOUNT_TOKENS_URL),
      when: "Open the Vercel dashboard to create a classic personal access token"
    },
    {
      command: `export VERCEL_TOKEN='<class_access_token>'`,
      when: "Paste your classic token from the dashboard (replace <class_access_token>); on Windows use set VERCEL_TOKEN=<class_access_token> in cmd or $env:VERCEL_TOKEN in PowerShell"
    },
    {
      command: rerun,
      when: rerunWhen
    }
  ];
}
async function openTokensDashboardInBrowser(client) {
  const isCursorAgent = client.agentName === KNOWN_AGENTS2.CURSOR || client.agentName === KNOWN_AGENTS2.CURSOR_CLI;
  const shouldSkipBrowser = Boolean(process.env.CI) && !isCursorAgent;
  if (shouldSkipBrowser) {
    return;
  }
  try {
    const p = await (0, import_open17.default)(VERCEL_ACCOUNT_TOKENS_URL);
    p?.on?.("error", () => {
    });
  } catch {
  }
}
async function add12(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(addSubcommand11.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args } = parsedArgs;
  const name = args[0]?.trim();
  if (!name) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_arguments",
          message: 'Token name is required. Example: `vercel tokens add "My token"`'
        },
        1
      );
    }
    output_manager_default.error(
      'Token name is required. Example: `vercel tokens add "My token"`'
    );
    return 1;
  }
  if (args.length > 1) {
    output_manager_default.error("Too many arguments. Pass a single token name.");
    return 1;
  }
  const validation = validateJsonOutput(parsedArgs.flags);
  if (!validation.valid) {
    output_manager_default.error(validation.error);
    return 1;
  }
  const asJson = validation.jsonOutput;
  const projectId = parsedArgs.flags["--project"];
  const body = { name };
  if (typeof projectId === "string" && projectId.length > 0) {
    body.projectId = projectId;
  }
  let result;
  try {
    result = await client.fetch("/v3/user/tokens", {
      method: "POST",
      body,
      useCurrentTeam: false
    });
  } catch (err) {
    if (isClassicTokenRequiredForCreateError(err)) {
      await openTokensDashboardInBrowser(client);
      const rerun = getSanitizedRerunCommand(client);
      if (shouldEmitNonInteractiveCommandError(client)) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.CLASSIC_TOKEN_REQUIRED,
            message: "Creating tokens requires a classic personal access token; OAuth login cannot create new tokens.",
            userActionRequired: true,
            verification_uri: VERCEL_ACCOUNT_TOKENS_URL,
            hint: CLASSIC_TOKEN_AGENT_HINT,
            next: tokensAddRecoveryNext(
              client,
              rerun,
              "Re-run after the token is set (or use --token <class_access_token> instead of export)"
            )
          },
          1
        );
      }
      output_manager_default.error(CLASSIC_TOKEN_GUIDANCE);
      return 1;
    }
    if (isTokenUserScopeRequiredError(err)) {
      await openTokensDashboardInBrowser(client);
      const rerun = getSanitizedRerunCommand(client);
      const apiMessage = normalizeApiMessage(
        isAPIError(err) ? err.serverMessage || err.message : String(err)
      );
      if (shouldEmitNonInteractiveCommandError(client)) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: AGENT_REASON.TOKEN_USER_SCOPE_REQUIRED,
            message: apiMessage,
            userActionRequired: true,
            verification_uri: VERCEL_ACCOUNT_TOKENS_URL,
            hint: USER_SCOPE_AGENT_HINT,
            next: tokensAddRecoveryNext(
              client,
              rerun,
              "Re-run with a classic token that has full user account scope (or pass --token <class_access_token>)"
            )
          },
          1
        );
      }
      output_manager_default.error(USER_SCOPE_GUIDANCE);
      output_manager_default.error("");
      output_manager_default.error(apiMessage);
      return 1;
    }
    printError(err);
    return 1;
  }
  if (asJson) {
    client.stdout.write(`${JSON.stringify(result, null, 2)}
`);
    return 0;
  }
  output_manager_default.success(
    "Token created. Save the value below \u2014 it will not be shown again."
  );
  if (result.bearerToken) {
    output_manager_default.log(result.bearerToken);
  }
  if (result.token?.id) {
    output_manager_default.log(`id: ${result.token.id}`);
  }
  return 0;
}

// src/commands/tokens/rm.ts
async function rm9(client, argv) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand14.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const id = parsedArgs.args[0];
  if (!id) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "missing_arguments",
        message: "Token id is required. Example: `vercel tokens rm tok_abc123`",
        hint: "Run `tokens ls` to list ids, then pass the id to `tokens rm`.",
        next: [
          {
            command: buildCommandWithGlobalFlags(client.argv, "tokens ls"),
            when: "List personal access token ids for the current account"
          },
          {
            command: buildCommandWithGlobalFlags(
              client.argv,
              "tokens rm <token_id>"
            ),
            when: "Remove a token after replacing <token_id> with an id from the list"
          }
        ]
      },
      1
    );
    output_manager_default.error(
      "Token id is required. Example: `vercel tokens rm tok_abc123`"
    );
    return 1;
  }
  if (parsedArgs.args.length > 1) {
    output_manager_default.error("Too many arguments. Pass a single token id.");
    return 1;
  }
  const validation = validateJsonOutput(parsedArgs.flags);
  if (!validation.valid) {
    output_manager_default.error(validation.error);
    return 1;
  }
  const asJson = validation.jsonOutput;
  await client.fetch(`/v3/user/tokens/${encodeURIComponent(id)}`, {
    method: "DELETE",
    useCurrentTeam: false
  });
  if (asJson) {
    client.stdout.write(`${JSON.stringify({ ok: true, id }, null, 2)}
`);
    return 0;
  }
  output_manager_default.success(`Removed token ${id}`);
  return 0;
}

// src/util/telemetry/commands/tokens/index.ts
var TokensTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandAdd(actual) {
    this.trackCliSubcommand({
      subcommand: "add",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
};

// src/commands/tokens/index.ts
var COMMAND_CONFIG38 = {
  add: ["add", "create"],
  remove: ["rm", "remove", "delete"],
  ls: ["ls", "list"]
};
async function main29(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(tokensCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new TokensTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG38
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("tokens");
    output_manager_default.print(help(tokensCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: tokensCommand, columns: client.stderr.columns })
    );
    return 2;
  }
  switch (subcommand) {
    case "add":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("tokens", subcommandOriginal);
        return printHelp(addSubcommand11);
      }
      telemetry2.trackCliSubcommandAdd(subcommandOriginal);
      return add12(client, args);
    case "remove":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("tokens", subcommandOriginal);
        return printHelp(removeSubcommand14);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm9(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("tokens", subcommandOriginal);
        return printHelp(listSubcommand17);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls8(client, args);
  }
}

// src/commands/telemetry/status.ts
var import_chalk191 = __toESM(require_source(), 1);
async function status2(client) {
  const enabled = client.config.telemetry?.enabled !== false;
  const status3 = enabled ? import_chalk191.default.green("Enabled") : import_chalk191.default.red("Disabled");
  output_manager_default.print("\n");
  output_manager_default.log(`${import_chalk191.default.bold("Telemetry status")}: ${status3}
`);
  const learnMoreMessage = `

Learn more: ${import_chalk191.default.cyan("https://vercel.com/docs/cli/about-telemetry")}`;
  if (enabled) {
    output_manager_default.log(`You have opted in to Vercel CLI telemetry${learnMoreMessage}`);
  } else {
    output_manager_default.log("You have opted out of Vercel CLI telemetry");
    output_manager_default.log(
      `No data will be collected from your machine${learnMoreMessage}`
    );
  }
  return 0;
}

// src/commands/telemetry/enable.ts
async function enable5(client) {
  client.config = {
    ...client.config,
    telemetry: {
      ...client.config.telemetry,
      enabled: true
    }
  };
  writeToConfigFile(client.config);
  await status2(client);
  return 0;
}

// src/commands/telemetry/disable.ts
async function disable5(client) {
  client.config = {
    ...client.config,
    telemetry: {
      ...client.config.telemetry,
      enabled: false
    }
  };
  writeToConfigFile(client.config);
  await status2(client);
  return 0;
}

// src/commands/telemetry/flush.ts
var import_node_fetch = __toESM(require_lib3(), 1);
async function flush(_client, args) {
  const url = process.env.VERCEL_TELEMETRY_BRIDGE_URL || "https://telemetry.vercel.com/api/vercel-cli/v1/events";
  const { headers, body } = JSON.parse(args[0]);
  try {
    const res = await (0, import_node_fetch.default)(url, {
      method: "POST",
      headers,
      body: JSON.stringify(body)
    });
    const status3 = res.status;
    const cliTracked = res.headers.get("x-vercel-cli-tracked") || "";
    const wasRecorded = cliTracked === "1";
    if (status3 === 204) {
      if (wasRecorded) {
        process.stderr.write("Telemetry event tracked");
      } else {
        process.stderr.write("Telemetry event ignored");
      }
    } else {
      process.stderr.write(
        `Failed to send telemetry events. Unexpected response from telemetry server: ${status3}`
      );
    }
    return 0;
  } catch (error) {
    if (error instanceof Error) {
      process.stderr.write(`Failed to send telemetry events. ${error.message}`);
    }
    return 1;
  }
}

// src/util/telemetry/commands/telemetry/index.ts
var TelemetryTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandStatus(actual) {
    this.trackCliSubcommand({
      subcommand: "status",
      value: actual
    });
  }
  trackCliSubcommandEnable(actual) {
    this.trackCliSubcommand({
      subcommand: "enable",
      value: actual
    });
  }
  trackCliSubcommandDisable(_) {
  }
  trackCliSubcommandFlush(_) {
  }
};

// src/commands/telemetry/index.ts
var import_chalk192 = __toESM(require_source(), 1);
var COMMAND_CONFIG39 = {
  status: getCommandAliases(statusSubcommand4),
  enable: getCommandAliases(enableSubcommand4),
  disable: getCommandAliases(disableSubcommand4),
  flush: getCommandAliases(flushSubcommand)
};
async function telemetry(client) {
  const telemetryClient = new TelemetryTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArguments;
  const flagsSpecification = getFlagsSpecification(telemetryCommand.options);
  try {
    parsedArguments = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArguments.args.slice(1),
    COMMAND_CONFIG39
  );
  const needHelp = parsedArguments.flags["--help"];
  function printHelp(command) {
    output_manager_default.print(
      help(command, {
        columns: client.stderr.columns,
        parent: telemetryCommand
      })
    );
  }
  if (!subcommand && needHelp) {
    telemetryClient.trackCliFlagHelp("telemetry", subcommand);
    output_manager_default.print(help(telemetryCommand, { columns: client.stderr.columns }));
    return 2;
  }
  switch (subcommand) {
    case "status":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("telemetry", subcommandOriginal);
        printHelp(statusSubcommand4);
        return 2;
      }
      telemetryClient.trackCliSubcommandStatus(subcommandOriginal);
      return status2(client);
    case "flush":
      return flush(client, args);
    case "enable":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("telemetry", subcommandOriginal);
        printHelp(enableSubcommand4);
        return 2;
      }
      telemetryClient.trackCliSubcommandEnable(subcommandOriginal);
      return enable5(client);
    case "disable":
      if (needHelp) {
        telemetryClient.trackCliFlagHelp("telemetry", subcommandOriginal);
        printHelp(disableSubcommand4);
        return 2;
      }
      return disable5(client);
    default: {
      const errorMessage = parsedArguments.args.length !== 2 ? "Invalid number of arguments" : "Invalid subcommand";
      output_manager_default.print(
        `${import_chalk192.default.red("Error")}: ${errorMessage}. See help instructions for usage:
`
      );
      output_manager_default.print(help(telemetryCommand, { columns: client.stderr.columns }));
      return 2;
    }
  }
}

// src/util/telemetry/commands/traces/index.ts
var TracesTelemetryClient = class extends TelemetryClient {
  trackCliArgumentRequestId(requestId) {
    if (requestId) {
      this.trackCliArgument({
        arg: "requestId",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
  trackCliOptionProject(project) {
    if (project) {
      this.trackCliOption({
        option: "project",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagOpen(open15) {
    if (open15) {
      this.trackCliFlag("open");
    }
  }
  trackCliOptionView(view) {
    if (view) {
      this.trackCliOption({
        option: "view",
        value: view
      });
    }
  }
};

// src/commands/traces/get.ts
var import_open18 = __toESM(require_open(), 1);

// src/commands/traces/fetch-trace.ts
async function fetchTrace({
  client,
  teamId,
  projectId,
  requestId
}) {
  const search2 = new URLSearchParams({ teamId, projectId, requestId });
  const url = `/v1/projects/traces?${search2.toString()}`;
  const response = await client.fetch(url);
  return { trace: response.trace };
}

// src/commands/traces/analyze.ts
var MAX_TREE_DEPTH = 256;
var SPAN_STATUS_ERROR = 1;
var ATTR_HINT_KEYS = [
  "http.url",
  "http.target",
  "db.statement",
  "http.status_code"
];
function tupleToUs(tuple) {
  if (!tuple || tuple.length !== 2) {
    return null;
  }
  const [seconds, nanoseconds] = tuple;
  if (typeof seconds !== "number" || typeof nanoseconds !== "number") {
    return null;
  }
  return seconds * 1e6 + nanoseconds / 1e3;
}
function getDurationUs(span) {
  const us = tupleToUs(span.duration);
  if (us === null) {
    return null;
  }
  return us < 0 ? 0 : us;
}
function getStartTimeUs(span) {
  return tupleToUs(span.startTime);
}
function readAttrString(attributes, key) {
  const value = attributes?.[key];
  if (typeof value === "string") {
    return value;
  }
  if (typeof value === "number") {
    return String(value);
  }
  return void 0;
}
function resolveRootSpan(trace2) {
  if (trace2.spans.length === 0) {
    return void 0;
  }
  if (trace2.rootSpanId) {
    const match = trace2.spans.find((span) => span.spanId === trace2.rootSpanId);
    if (match) {
      return match;
    }
  }
  return trace2.spans[0];
}
function compareByStart(a, b) {
  const startA = getStartTimeUs(a);
  const startB = getStartTimeUs(b);
  if (startA === null && startB === null) {
    return 0;
  }
  if (startA === null) {
    return 1;
  }
  if (startB === null) {
    return -1;
  }
  return startA - startB;
}
function analyze(trace2) {
  const root = resolveRootSpan(trace2);
  const rootDurationUs = root ? getDurationUs(root) : null;
  const rootStartUs = root ? getStartTimeUs(root) : null;
  const spansById = /* @__PURE__ */ new Map();
  for (const span of trace2.spans) {
    spansById.set(span.spanId, span);
  }
  const childrenByParent = /* @__PURE__ */ new Map();
  for (const span of trace2.spans) {
    if (!span.parentSpanId) {
      continue;
    }
    if (!spansById.has(span.parentSpanId)) {
      continue;
    }
    const list15 = childrenByParent.get(span.parentSpanId);
    if (list15) {
      list15.push(span);
    } else {
      childrenByParent.set(span.parentSpanId, [span]);
    }
  }
  for (const list15 of childrenByParent.values()) {
    list15.sort(compareByStart);
  }
  const spanInfo = /* @__PURE__ */ new Map();
  for (const span of trace2.spans) {
    const durationUs = getDurationUs(span);
    const children = childrenByParent.get(span.spanId) ?? [];
    let childTotalUs = 0;
    for (const child of children) {
      const cd = getDurationUs(child);
      if (cd !== null) {
        childTotalUs += cd;
      }
    }
    const selfTimeUs = durationUs === null ? null : Math.max(0, durationUs - childTotalUs);
    let startOffsetUs = null;
    if (rootStartUs !== null) {
      const startUs = getStartTimeUs(span);
      if (startUs !== null) {
        startOffsetUs = Math.max(0, startUs - rootStartUs);
      }
    }
    spanInfo.set(span.spanId, {
      span,
      durationUs,
      selfTimeUs,
      startOffsetUs
    });
  }
  const visited = /* @__PURE__ */ new Set();
  const treeOrder = [];
  let truncatedAtDepth = false;
  if (root) {
    truncatedAtDepth = walkTree(root, 0, childrenByParent, visited, treeOrder) || truncatedAtDepth;
  }
  const orphanOrder = [];
  const orphanRoots = trace2.spans.filter(
    (span) => !visited.has(span.spanId) && (!span.parentSpanId || !spansById.has(span.parentSpanId))
  ).sort(compareByStart);
  for (const orphan of orphanRoots) {
    truncatedAtDepth = walkTree(orphan, 0, childrenByParent, visited, orphanOrder) || truncatedAtDepth;
  }
  for (const span of trace2.spans) {
    if (!visited.has(span.spanId)) {
      orphanOrder.push({ span, depth: 0 });
      visited.add(span.spanId);
    }
  }
  const errorSpans = trace2.spans.filter(
    (span) => span.status?.code === SPAN_STATUS_ERROR
  );
  const byName = /* @__PURE__ */ new Map();
  for (const span of trace2.spans) {
    const name = span.name || "<unnamed>";
    const durationUs = getDurationUs(span) ?? 0;
    const entry = byName.get(name);
    if (entry) {
      entry.count += 1;
      entry.totalUs += durationUs;
    } else {
      byName.set(name, { count: 1, totalUs: durationUs });
    }
  }
  const repeatedOps = [];
  for (const [name, { count, totalUs }] of byName) {
    if (count >= 2) {
      repeatedOps.push({
        name,
        count,
        totalUs,
        perCallUs: totalUs / count
      });
    }
  }
  repeatedOps.sort((a, b) => b.totalUs - a.totalUs);
  return {
    trace: trace2,
    root,
    rootDurationUs,
    rootStartUs,
    spanInfo,
    treeOrder,
    orphanOrder,
    errorSpans,
    repeatedOps,
    truncatedAtDepth
  };
}
function walkTree(span, depth, childrenByParent, visited, out) {
  if (visited.has(span.spanId)) {
    return false;
  }
  visited.add(span.spanId);
  if (depth >= MAX_TREE_DEPTH) {
    out.push({ span, depth });
    return true;
  }
  out.push({ span, depth });
  let truncated = false;
  const children = childrenByParent.get(span.spanId) ?? [];
  for (const child of children) {
    truncated = walkTree(child, depth + 1, childrenByParent, visited, out) || truncated;
  }
  return truncated;
}

// src/commands/traces/render-markdown.ts
var UNKNOWN = "<unknown>";
var UNNAMED = "<unnamed>";
var ATTR_VALUE_MAX_LEN = 80;
var TRACE_ID_MAX_LEN = 13;
function renderMarkdown(trace2, options) {
  const analysis = analyze(trace2);
  const sections = [];
  sections.push(renderHeader(analysis, options));
  if (analysis.errorSpans.length > 0) {
    sections.push(renderErrors(analysis));
  }
  if (analysis.repeatedOps.length > 0) {
    sections.push(renderRepeatedOps(analysis));
  }
  sections.push(renderTree(analysis));
  return `${sections.join("\n\n")}
`;
}
function renderHeader(analysis, options) {
  const { trace: trace2, root, rootDurationUs, errorSpans } = analysis;
  const attributes = root?.attributes;
  const method = readAttrString(attributes, "http.method");
  const target = readAttrString(attributes, "http.target");
  const status3 = readAttrString(attributes, "http.status_code");
  const endpoint = formatEndpoint(method, target, status3);
  const duration = formatDurationUs(rootDurationUs);
  const spans = formatSpansLine(trace2.spans.length, errorSpans.length);
  const lines = [
    `# Trace ${ellipsis(trace2.traceId, TRACE_ID_MAX_LEN)}`,
    "",
    `- **Trace id:** ${trace2.traceId}`,
    `- **Request id:** ${options.requestId}`,
    `- **Endpoint:** ${endpoint}`,
    `- **Duration:** ${duration}`,
    `- **Spans:** ${spans}`
  ];
  return lines.join("\n");
}
function renderErrors(analysis) {
  const lines = [`## Errors (${analysis.errorSpans.length})`, ""];
  for (const span of analysis.errorSpans) {
    const name = `\`${span.name || UNNAMED}\``;
    const message = span.status?.message;
    const header = message ? `${name} \u2014 \`${message}\`` : name;
    lines.push(`- ${header}`);
    for (const hint of collectAttrHints(span.attributes)) {
      lines.push(`  - \`${hint.key}\`: ${hint.value}`);
    }
  }
  return lines.join("\n");
}
function renderRepeatedOps(analysis) {
  const { repeatedOps, rootDurationUs } = analysis;
  const showPctColumn = rootDurationUs !== null && rootDurationUs > 0;
  const header = ["Operation", "Count", "Total", "Per call"];
  const align = ["---", "---:", "---:", "---:"];
  if (showPctColumn) {
    header.push("% of root");
    align.push("---:");
  }
  const lines = [
    "## Repeated operations",
    "",
    `| ${header.join(" | ")} |`,
    `| ${align.join(" | ")} |`
  ];
  for (const op of repeatedOps) {
    const cells = [
      `\`${op.name}\``,
      String(op.count),
      formatDurationUs(op.totalUs),
      formatDurationUs(op.perCallUs)
    ];
    if (showPctColumn && rootDurationUs !== null) {
      cells.push(formatPct(op.totalUs, rootDurationUs));
    }
    lines.push(`| ${cells.join(" | ")} |`);
  }
  return lines.join("\n");
}
function renderTree(analysis) {
  const lines = ["## Span tree", ""];
  if (analysis.treeOrder.length === 0 && analysis.orphanOrder.length === 0) {
    lines.push("_No spans._");
    return lines.join("\n");
  }
  for (const node of analysis.treeOrder) {
    lines.push(formatTreeRow(node, analysis));
  }
  if (analysis.orphanOrder.length > 0) {
    lines.push("", "### Orphan spans", "");
    for (const node of analysis.orphanOrder) {
      lines.push(formatTreeRow(node, analysis));
    }
  }
  if (analysis.truncatedAtDepth) {
    lines.push("", `_Tree truncated at depth ${MAX_TREE_DEPTH}._`);
  }
  return lines.join("\n");
}
function formatTreeRow(node, analysis) {
  const { span, depth } = node;
  const info = analysis.spanInfo.get(span.spanId);
  const indent = "  ".repeat(depth);
  const parts = [];
  const durationUs = info?.durationUs ?? null;
  parts.push(`\`${formatDurationUs(durationUs)}\``);
  if (analysis.rootDurationUs !== null && analysis.rootDurationUs > 0) {
    parts.push(formatPct(durationUs, analysis.rootDurationUs));
  }
  if (analysis.rootStartUs !== null) {
    const offset = info?.startOffsetUs ?? null;
    parts.push(formatOffset(offset));
  }
  parts.push(`\`${span.name || UNNAMED}\``);
  if (span.status?.code === SPAN_STATUS_ERROR) {
    const message = span.status.message;
    parts.push(message ? `[error: ${message}]` : "[error]");
  }
  const [hint] = collectAttrHints(span.attributes);
  if (hint) {
    parts.push(`\u2014 \`${hint.key}\`: ${hint.value}`);
  }
  return `${indent}- ${parts.join(" ")}`;
}
function formatEndpoint(method, target, status3) {
  const left = method && target ? `${method} ${target}` : method ?? target;
  const path3 = left ? `\`${left}\`` : UNKNOWN;
  if (status3) {
    return `${path3} \u2192 ${status3}`;
  }
  return path3;
}
function formatSpansLine(count, errors) {
  if (errors === 0) {
    return String(count);
  }
  const label = errors === 1 ? "error" : "errors";
  return `${count} (${errors} ${label})`;
}
function formatDurationUs(us) {
  if (us === null) {
    return UNKNOWN;
  }
  if (us < 1e3) {
    return `${Math.round(us)}\u03BCs`;
  }
  if (us < 1e6) {
    return `${(us / 1e3).toFixed(1)}ms`;
  }
  return `${(us / 1e6).toFixed(1)}s`;
}
function formatOffset(us) {
  if (us === null) {
    return `+${UNKNOWN}`;
  }
  return `+${formatDurationUs(us)}`;
}
function formatPct(us, rootUs) {
  if (us === null) {
    return UNKNOWN;
  }
  if (rootUs <= 0) {
    return UNKNOWN;
  }
  const pct = us / rootUs * 100;
  if (pct > 0 && pct < 1) {
    return "<1%";
  }
  return `${Math.round(pct)}%`;
}
function collectAttrHints(attributes) {
  if (!attributes) {
    return [];
  }
  const out = [];
  for (const key of ATTR_HINT_KEYS) {
    const value = readAttrString(attributes, key);
    if (value !== void 0) {
      out.push({ key, value: ellipsis(value, ATTR_VALUE_MAX_LEN) });
    }
  }
  return out;
}

// src/commands/traces/scope-resolver.ts
var MISSING_BOTH_MESSAGE = "No linked project found. Run `vercel link`, pass --cwd to a linked dir, or use --scope <team> and --project <name>.";
function resolveScope({
  flags = {},
  linkedProject
}) {
  const flagScope = flags.scope?.trim() || void 0;
  const flagProject = flags.project?.trim() || void 0;
  if (linkedProject.status === "linked") {
    return {
      teamId: flagScope ?? linkedProject.org.id,
      projectId: flagProject ?? linkedProject.project.id
    };
  }
  if (flagScope && flagProject) {
    return { teamId: flagScope, projectId: flagProject };
  }
  return { message: MISSING_BOTH_MESSAGE };
}

// src/commands/traces/get.ts
var VIEW_OPTIONS = ["timeline", "tree", "gantt"];
function isView(value) {
  return VIEW_OPTIONS.includes(value);
}
function buildDashboardUrl({
  teamSlug,
  projectName,
  traceId,
  view
}) {
  const base = `https://vercel.com/${encodeURIComponent(teamSlug)}/${encodeURIComponent(projectName)}/logs/traces/${encodeURIComponent(traceId)}`;
  if (view && view !== "timeline") {
    return `${base}?view=${encodeURIComponent(view)}`;
  }
  return base;
}
async function resolveDashboardScope({
  client,
  scopeFlag,
  projectFlag,
  teamId,
  linkedProject
}) {
  let teamSlug;
  if (scopeFlag) {
    if (scopeFlag.startsWith("team_")) {
      const team = await getTeamById(client, scopeFlag);
      teamSlug = team.slug;
    } else {
      teamSlug = scopeFlag;
    }
  } else if (linkedProject.status === "linked") {
    teamSlug = linkedProject.org.slug;
  } else {
    return {
      error: "Unable to resolve team slug for the dashboard URL.",
      reason: AGENT_REASON.NOT_LINKED
    };
  }
  let projectName;
  if (projectFlag) {
    const project = await getProjectByNameOrId(client, projectFlag, teamId);
    if (project instanceof ProjectNotFound) {
      return {
        error: `Project not found: ${projectFlag}`,
        reason: AGENT_REASON.NOT_FOUND
      };
    }
    projectName = project.name;
  } else if (linkedProject.status === "linked") {
    projectName = linkedProject.project.name;
  } else {
    return {
      error: "Unable to resolve project name for the dashboard URL.",
      reason: AGENT_REASON.NOT_LINKED
    };
  }
  return { teamSlug, projectName };
}
async function get3(client, telemetry2) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(getSubcommand2.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const positional = parsedArgs.args.slice(1);
  const requestId = positional[0] === getSubcommand2.name ? positional[1] : positional[0];
  const json = parsedArgs.flags["--json"];
  const scopeFlag = parsedArgs.flags["--scope"];
  const projectFlag = parsedArgs.flags["--project"];
  const openFlag2 = parsedArgs.flags["--open"];
  const viewFlag = parsedArgs.flags["--view"];
  telemetry2.trackCliArgumentRequestId(requestId);
  telemetry2.trackCliFlagJson(json);
  telemetry2.trackCliOptionProject(projectFlag);
  telemetry2.trackCliFlagOpen(openFlag2);
  telemetry2.trackCliOptionView(viewFlag);
  if (json && openFlag2) {
    const msg = "`--json` and `--open` cannot be used together.";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: AGENT_STATUS.ERROR,
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg
      });
    }
    output_manager_default.error(msg);
    return 1;
  }
  if (viewFlag && !openFlag2) {
    const msg = "`--view` can only be used with `--open`.";
    if (client.nonInteractive) {
      outputAgentError(client, {
        status: AGENT_STATUS.ERROR,
        reason: AGENT_REASON.INVALID_ARGUMENTS,
        message: msg
      });
    }
    output_manager_default.error(msg);
    return 1;
  }
  let view;
  if (viewFlag !== void 0) {
    if (!isView(viewFlag)) {
      const msg = `\`--view\` must be one of: ${VIEW_OPTIONS.join(", ")}. Received: ${viewFlag}`;
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.INVALID_ARGUMENTS,
          message: msg
        });
      }
      output_manager_default.error(msg);
      return 1;
    }
    view = viewFlag;
  }
  if (!requestId) {
    output_manager_default.print(
      help(getSubcommand2, {
        parent: tracesCommand,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  let teamId;
  let projectId;
  let linkedProject;
  if (scopeFlag && projectFlag && !openFlag2) {
    teamId = scopeFlag;
    projectId = projectFlag;
  } else {
    linkedProject = await getLinkedProject(client);
    if (linkedProject.status === "error") {
      if (scopeFlag && projectFlag) {
        linkedProject = { status: "not_linked", org: null, project: null };
      } else {
        return linkedProject.exitCode;
      }
    }
    const scope = resolveScope({
      flags: { scope: scopeFlag, project: projectFlag },
      linkedProject
    });
    if ("message" in scope) {
      output_manager_default.error(scope.message);
      return 1;
    }
    teamId = scope.teamId;
    projectId = scope.projectId;
  }
  output_manager_default.spinner("Fetching trace\u2026");
  let trace2;
  try {
    ({ trace: trace2 } = await fetchTrace({
      client,
      teamId,
      projectId,
      requestId
    }));
  } catch (err) {
    output_manager_default.stopSpinner();
    printError(err);
    return 1;
  }
  output_manager_default.stopSpinner();
  if (openFlag2) {
    if (!linkedProject) {
      const msg = "Unable to resolve project link for the dashboard URL.";
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.NOT_LINKED,
          message: msg
        });
      }
      output_manager_default.error(msg);
      return 1;
    }
    const resolved = await resolveDashboardScope({
      client,
      scopeFlag,
      projectFlag,
      teamId,
      linkedProject
    });
    if ("error" in resolved) {
      if (client.nonInteractive) {
        outputAgentError(client, {
          status: AGENT_STATUS.ERROR,
          reason: resolved.reason,
          message: resolved.error
        });
      }
      output_manager_default.error(resolved.error);
      return 1;
    }
    const url = buildDashboardUrl({
      teamSlug: resolved.teamSlug,
      projectName: resolved.projectName,
      traceId: trace2.traceId,
      view
    });
    output_manager_default.log(`Opening ${url} in your browser...`);
    await (0, import_open18.default)(url);
    return 0;
  }
  if (json) {
    client.stdout.write(`${JSON.stringify(trace2, null, 2)}
`);
    return 0;
  }
  client.stdout.write(renderMarkdown(trace2, { requestId }));
  output_manager_default.log("Run with --json for full trace data.");
  return 0;
}

// src/commands/traces/index.ts
var COMMAND_CONFIG40 = {
  get: getCommandAliases(getSubcommand2)
};
async function traces(client) {
  const telemetry2 = new TracesTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(tracesCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (err) {
    printError(err);
    return 1;
  }
  const { subcommand, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG40
  );
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("traces", subcommandOriginal);
    const isGet = subcommand === getSubcommand2.name;
    output_manager_default.print(
      help(isGet ? getSubcommand2 : tracesCommand, {
        parent: isGet ? tracesCommand : void 0,
        columns: client.stderr.columns
      })
    );
    return 2;
  }
  return get3(client, telemetry2);
}

// src/util/telemetry/commands/upgrade/index.ts
var UpgradeTelemetryClient = class extends TelemetryClient {
  trackCliFlagDryRun(dryRun) {
    if (dryRun) {
      this.trackCliFlag("dry-run");
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
  trackCliFlagEnableAuto(enableAuto) {
    if (enableAuto) {
      this.trackCliFlag("enable-auto");
    }
  }
  trackCliFlagDisableAuto(disableAuto) {
    if (disableAuto) {
      this.trackCliFlag("disable-auto");
    }
  }
};

// src/commands/upgrade/index.ts
async function upgrade(client) {
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(upgradeCommand.options);
  const telemetry2 = new UpgradeTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("upgrade");
    output_manager_default.print(help(upgradeCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const dryRun = parsedArgs.flags["--dry-run"];
  const enableAuto = parsedArgs.flags["--enable-auto"];
  const disableAuto = parsedArgs.flags["--disable-auto"];
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliFlagDryRun(dryRun);
  telemetry2.trackCliFlagEnableAuto(enableAuto);
  telemetry2.trackCliFlagDisableAuto(disableAuto);
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  telemetry2.trackCliFlagJson(parsedArgs.flags["--json"]);
  if (enableAuto && disableAuto) {
    output_manager_default.error("Cannot use --enable-auto and --disable-auto together");
    return 1;
  }
  if (enableAuto || disableAuto) {
    const enabled = Boolean(enableAuto);
    setAutoUpdate(client, enabled);
    output_manager_default.success(
      `Automatic CLI updates ${enabled ? "enabled" : "disabled"}.`
    );
    return 0;
  }
  if (dryRun || asJson) {
    const updateCommand = await getUpdateCommand();
    const global = await isGlobal();
    if (asJson) {
      const jsonOutput = {
        currentVersion: pkg_default.version,
        installationType: global ? "global" : "local",
        upgradeCommand: updateCommand,
        autoUpdatesEnabled: isAutoUpdateEnabled(client.config)
      };
      client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
    } else {
      output_manager_default.print(`Current version: ${pkg_default.version}
`);
      output_manager_default.print(`Installation type: ${global ? "global" : "local"}
`);
      output_manager_default.print(`Upgrade command: ${updateCommand}
`);
      output_manager_default.print(
        `Automatic updates: ${isAutoUpdateEnabled(client.config) ? "Enabled" : "Disabled"}
`
      );
    }
    return 0;
  }
  return executeUpgrade();
}

// src/commands/usage/index.ts
var import_chalk196 = __toESM(require_source(), 1);
var import_jsonlines = __toESM(require_jsonlines(), 1);

// src/util/telemetry/commands/usage/index.ts
var UsageTelemetryClient = class extends TelemetryClient {
  trackCliOptionFrom(from) {
    if (from) {
      this.trackCliOption({
        option: "from",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionTo(to) {
    if (to) {
      this.trackCliOption({
        option: "to",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagJson(json) {
    if (json) {
      this.trackCliFlag("json");
    }
  }
  trackCliOptionBreakdown(breakdown) {
    if (breakdown) {
      this.trackCliOption({
        option: "breakdown",
        value: breakdown
      });
    }
  }
  trackCliOptionGroupBy(groupBy) {
    if (groupBy) {
      this.trackCliOption({
        option: "group-by",
        value: groupBy
      });
    }
  }
};

// src/commands/usage/index.ts
var import_error_utils20 = __toESM(require_dist(), 1);

// src/util/billing/period-utils.ts
import { DateTime } from "luxon";
var LA_TIMEZONE = "America/Los_Angeles";
function toISOOrThrow(dt) {
  if (!dt.isValid) {
    throw new Error(`Invalid DateTime: ${dt.invalidReason}`);
  }
  const iso = dt.toISO();
  if (!iso) {
    throw new Error(`Failed to convert DateTime to ISO`);
  }
  return iso;
}
function getDefaultFromDateTime() {
  return DateTime.now().setZone(LA_TIMEZONE).startOf("month");
}
function getDefaultToDateTime() {
  return DateTime.utc();
}
function getDefaultFromDate() {
  const dt = getDefaultFromDateTime();
  return toISOOrThrow(dt.toUTC());
}
function getDefaultToDate() {
  const dt = getDefaultToDateTime();
  return toISOOrThrow(dt.toUTC());
}
function getDefaultFromDateDisplay() {
  const dt = getDefaultFromDateTime();
  return dt.toFormat("yyyy-MM-dd");
}
function getDefaultToDateDisplay() {
  const dt = getDefaultToDateTime();
  return dt.toFormat("yyyy-MM-dd");
}
function parseBillingDate(dateStr, isEndDate = false) {
  if (dateStr.includes("T")) {
    const dt = DateTime.fromISO(dateStr);
    if (!dt.isValid) {
      throw new Error(
        `Invalid date: "${dateStr}". Expected ISO 8601 format (YYYY-MM-DD or YYYY-MM-DDTHH:mm:ss)`
      );
    }
    return dateStr;
  }
  const laDateTime = DateTime.fromISO(dateStr, { zone: LA_TIMEZONE }).startOf(
    "day"
  );
  if (!laDateTime.isValid) {
    throw new Error(
      `Invalid date: "${dateStr}". Expected ISO 8601 format (YYYY-MM-DD)`
    );
  }
  const finalDateTime = isEndDate ? laDateTime.plus({ days: 1 }) : laDateTime;
  return toISOOrThrow(finalDateTime.toUTC());
}
function getISOWeekNumber(dateStr) {
  const date = new Date(dateStr);
  date.setUTCDate(date.getUTCDate() + 4 - (date.getUTCDay() || 7));
  const yearStart = new Date(Date.UTC(date.getUTCFullYear(), 0, 1));
  const weekNo = Math.ceil(
    ((date.getTime() - yearStart.getTime()) / 864e5 + 1) / 7
  );
  return weekNo;
}
function getISOWeekYear(dateStr) {
  const date = new Date(dateStr);
  date.setUTCDate(date.getUTCDate() + 4 - (date.getUTCDay() || 7));
  return date.getUTCFullYear();
}
function getPeriodKey(chargePeriodStart, breakdownPeriod) {
  if (!chargePeriodStart) {
    return "Unknown";
  }
  switch (breakdownPeriod) {
    case "daily":
      return chargePeriodStart.substring(0, 10);
    case "weekly": {
      const weekYear = getISOWeekYear(chargePeriodStart);
      const weekNum = getISOWeekNumber(chargePeriodStart);
      return `${weekYear}-W${weekNum.toString().padStart(2, "0")}`;
    }
    case "monthly":
      return chargePeriodStart.substring(0, 7);
    default:
      return chargePeriodStart.substring(0, 10);
  }
}
var VALID_BREAKDOWN_PERIODS = [
  "daily",
  "weekly",
  "monthly"
];
function isValidBreakdownPeriod(value) {
  return value !== void 0 && VALID_BREAKDOWN_PERIODS.includes(value);
}

// src/util/billing/group-by-utils.ts
var VALID_GROUP_BY_DIMENSIONS = [
  "project",
  "region"
];
function isValidGroupByDimension(value) {
  return value !== void 0 && VALID_GROUP_BY_DIMENSIONS.includes(value);
}

// src/commands/usage/output-aggregated.ts
var import_chalk193 = __toESM(require_source(), 1);
function outputAggregated({ data, startTime }) {
  const { print, log } = output_manager_default;
  log(
    `Usage for ${import_chalk193.default.bold(data.contextName)} ${elapsed(Date.now() - startTime)}`
  );
  log("");
  const periodSuffix = data.usingDefaults ? " (current month)" : "";
  log(
    `${import_chalk193.default.gray("Period:")} ${data.fromDisplay} to ${data.toDisplay}${periodSuffix}`
  );
  log(`${import_chalk193.default.gray("Charges processed:")} ${data.chargeCount}`);
  log(`${import_chalk193.default.gray("Pricing unit:")} ${data.pricingUnit}`);
  log("");
  const sortedServices = [...data.services.entries()].sort(
    (a, b) => b[1].billedCost - a[1].billedCost
  );
  if (sortedServices.length === 0) {
    log("No usage data found for this period.");
    return;
  }
  const quantityHeader = data.pricingUnit === "USD" ? "Usage (USD)" : data.pricingUnit;
  const headers = ["Service", quantityHeader, "Effective Cost", "Billed Cost"];
  const rows = sortedServices.map(([name, svc]) => [
    name,
    formatQuantity(svc.pricingQuantity, svc.pricingUnit),
    formatCurrency(svc.effectiveCost),
    formatCurrency(svc.billedCost)
  ]);
  rows.push([
    import_chalk193.default.bold("Total"),
    import_chalk193.default.bold(
      formatQuantity(data.grandTotals.pricingQuantity, data.pricingUnit)
    ),
    import_chalk193.default.bold(formatCurrency(data.grandTotals.effectiveCost)),
    import_chalk193.default.bold(formatCurrency(data.grandTotals.billedCost))
  ]);
  const tablePrint = table(
    [headers.map((h) => import_chalk193.default.bold(import_chalk193.default.cyan(h))), ...rows],
    { hsep: 4, align: ["l", "r", "r", "r"] }
  ).replace(/^/gm, "  ");
  print(`
${tablePrint}

`);
  log(
    `${import_chalk193.default.gray("Amount due:")} ${import_chalk193.default.bold(formatCurrency(data.grandTotals.billedCost))}`
  );
}

// src/commands/usage/output-breakdown.ts
var import_chalk194 = __toESM(require_source(), 1);
function getPeriodLabel(period) {
  switch (period) {
    case "daily":
      return "Daily";
    case "weekly":
      return "Weekly";
    case "monthly":
      return "Monthly";
    default:
      return "Period";
  }
}
function outputBreakdown({
  data,
  breakdownPeriod,
  startTime
}) {
  const { print, log } = output_manager_default;
  const periodLabel = getPeriodLabel(breakdownPeriod);
  log(
    `${periodLabel} Usage for ${import_chalk194.default.bold(data.contextName)} ${elapsed(Date.now() - startTime)}`
  );
  log("");
  const periodSuffix = data.usingDefaults ? " (current month)" : "";
  log(
    `${import_chalk194.default.gray("Period:")} ${data.fromDisplay} to ${data.toDisplay}${periodSuffix}`
  );
  log(`${import_chalk194.default.gray("Charges processed:")} ${data.chargeCount}`);
  log(`${import_chalk194.default.gray("Pricing unit:")} ${data.pricingUnit}`);
  log("");
  const sortedServices = [...data.services.entries()].sort(
    (a, b) => b[1].billedCost - a[1].billedCost
  );
  if (sortedServices.length === 0) {
    log("No usage data found for this period.");
    return;
  }
  const quantityHeader = data.pricingUnit === "USD" ? "Usage (USD)" : data.pricingUnit;
  const sortedPeriods = [...data.periodUsage.keys()].sort();
  for (const periodKey of sortedPeriods) {
    const periodData = data.periodUsage.get(periodKey);
    const sortedPeriodServices = [...periodData.services.entries()].sort(
      (a, b) => b[1].billedCost - a[1].billedCost
    );
    log(
      `${import_chalk194.default.bold(import_chalk194.default.cyan(periodKey))} (Total: ${formatQuantity(periodData.totalPricingQuantity, data.pricingUnit)}, ${formatCurrency(periodData.totalBilledCost)})`
    );
    const headers = ["Service", quantityHeader, "Billed Cost"];
    const rows = sortedPeriodServices.map(([name, svc]) => [
      name,
      formatQuantity(svc.pricingQuantity, svc.pricingUnit),
      formatCurrency(svc.billedCost)
    ]);
    const tablePrint = table(
      [headers.map((h) => import_chalk194.default.bold(import_chalk194.default.gray(h))), ...rows],
      { hsep: 4, align: ["l", "r", "r"] }
    ).replace(/^/gm, "  ");
    print(`${tablePrint}
`);
  }
}

// src/commands/usage/output-group-by.ts
var import_chalk195 = __toESM(require_source(), 1);
function getDimensionLabel(dimension) {
  switch (dimension) {
    case "project":
      return "Project";
    case "region":
      return "Region";
    default:
      return "Group";
  }
}
function outputGroupBy({
  data,
  groupByDimension,
  startTime
}) {
  const { print, log } = output_manager_default;
  const dimensionLabel = getDimensionLabel(groupByDimension);
  log(
    `Usage by ${dimensionLabel} for ${import_chalk195.default.bold(data.contextName)} ${elapsed(Date.now() - startTime)}`
  );
  log("");
  const periodSuffix = data.usingDefaults ? " (current month)" : "";
  log(
    `${import_chalk195.default.gray("Period:")} ${data.fromDisplay} to ${data.toDisplay}${periodSuffix}`
  );
  log(`${import_chalk195.default.gray("Charges processed:")} ${data.chargeCount}`);
  log(`${import_chalk195.default.gray("Pricing unit:")} ${data.pricingUnit}`);
  log("");
  const sortedGroups = [...data.groupByUsage.entries()].sort(
    (a, b) => b[1].totalBilledCost - a[1].totalBilledCost
  );
  if (sortedGroups.length === 0) {
    log("No usage data found for this period.");
    return;
  }
  const quantityHeader = data.pricingUnit === "USD" ? "Usage (USD)" : data.pricingUnit;
  for (const [groupName, groupData] of sortedGroups) {
    log(
      `${import_chalk195.default.bold(import_chalk195.default.cyan(groupName))} (Total: ${formatQuantity(groupData.totalPricingQuantity, data.pricingUnit)}, ${formatCurrency(groupData.totalBilledCost)})`
    );
    const sortedServices = [...groupData.services.entries()].sort(
      (a, b) => b[1].billedCost - a[1].billedCost
    );
    const headers = ["Service", quantityHeader, "Billed Cost"];
    const rows = sortedServices.map(([name, svc]) => [
      name,
      formatQuantity(svc.pricingQuantity, svc.pricingUnit),
      formatCurrency(svc.billedCost)
    ]);
    const tablePrint = table(
      [headers.map((h) => import_chalk195.default.bold(import_chalk195.default.gray(h))), ...rows],
      { hsep: 4, align: ["l", "r", "r"] }
    ).replace(/^/gm, "  ");
    print(`${tablePrint}
`);
  }
  log("");
  log(
    `${import_chalk195.default.gray("Amount due:")} ${import_chalk195.default.bold(formatCurrency(data.grandTotals.billedCost))}`
  );
}

// src/commands/usage/output-json.ts
function outputJson5(client, {
  data,
  fromDate,
  toDate,
  breakdownPeriod,
  groupByDimension
}) {
  const sortedServices = [...data.services.entries()].sort(
    (a, b) => b[1].billedCost - a[1].billedCost
  );
  const sortedPeriods = [...data.periodUsage.keys()].sort();
  const jsonOutput = {
    period: {
      from: fromDate,
      to: toDate
    },
    context: data.contextName,
    pricingUnit: data.pricingUnit
  };
  if (breakdownPeriod) {
    jsonOutput.breakdown = {
      period: breakdownPeriod,
      data: sortedPeriods.map((periodKey) => {
        const periodData = data.periodUsage.get(periodKey);
        const sortedPeriodServices = [...periodData.services.entries()].sort(
          (a, b) => b[1].billedCost - a[1].billedCost
        );
        return {
          periodKey,
          services: sortedPeriodServices.map(([name, svc]) => ({
            name,
            pricingQuantity: svc.pricingQuantity,
            pricingUnit: svc.pricingUnit,
            effectiveCost: svc.effectiveCost,
            billedCost: svc.billedCost
          })),
          totals: {
            pricingQuantity: periodData.totalPricingQuantity,
            effectiveCost: periodData.totalEffectiveCost,
            billedCost: periodData.totalBilledCost
          }
        };
      })
    };
  }
  if (groupByDimension) {
    const sortedGroups = [...data.groupByUsage.entries()].sort(
      (a, b) => b[1].totalBilledCost - a[1].totalBilledCost
    );
    jsonOutput.groupBy = {
      dimension: groupByDimension,
      data: sortedGroups.map(([groupName, groupData]) => {
        const sortedGroupServices = [...groupData.services.entries()].sort(
          (a, b) => b[1].billedCost - a[1].billedCost
        );
        return {
          name: groupName,
          services: sortedGroupServices.map(([name, svc]) => ({
            name,
            pricingQuantity: svc.pricingQuantity,
            pricingUnit: svc.pricingUnit,
            effectiveCost: svc.effectiveCost,
            billedCost: svc.billedCost
          })),
          totals: {
            pricingQuantity: groupData.totalPricingQuantity,
            effectiveCost: groupData.totalEffectiveCost,
            billedCost: groupData.totalBilledCost
          }
        };
      })
    };
  }
  jsonOutput.services = sortedServices.map(([name, svc]) => ({
    name,
    pricingQuantity: svc.pricingQuantity,
    pricingUnit: svc.pricingUnit,
    effectiveCost: svc.effectiveCost,
    billedCost: svc.billedCost
  }));
  jsonOutput.totals = {
    pricingQuantity: data.grandTotals.pricingQuantity,
    effectiveCost: data.grandTotals.effectiveCost,
    billedCost: data.grandTotals.billedCost
  };
  jsonOutput.chargeCount = data.chargeCount;
  client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
}

// src/commands/usage/index.ts
async function usage(client) {
  const { print, error, debug, spinner } = output_manager_default;
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(usageCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (err) {
    printError(err);
    return 1;
  }
  const telemetry2 = new UsageTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("usage");
    print(help(usageCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  const fromFlag = parsedArgs.flags["--from"];
  const toFlag = parsedArgs.flags["--to"];
  if (Boolean(fromFlag) !== Boolean(toFlag)) {
    error(
      "Both --from and --to must be specified or neither for the current month"
    );
    return 1;
  }
  const usingDefaults = !fromFlag && !toFlag;
  let fromDate;
  let toDate;
  try {
    fromDate = fromFlag ? parseBillingDate(fromFlag, false) : getDefaultFromDate();
    toDate = toFlag ? parseBillingDate(toFlag, true) : getDefaultToDate();
  } catch (err) {
    error(err.message);
    return 1;
  }
  const fromDisplay = fromFlag ?? getDefaultFromDateDisplay();
  const toDisplay = toFlag ?? getDefaultToDateDisplay();
  const breakdownFlag = parsedArgs.flags["--breakdown"];
  let breakdownPeriod;
  if (breakdownFlag) {
    if (!isValidBreakdownPeriod(breakdownFlag)) {
      error(
        `Invalid breakdown period: "${breakdownFlag}". Valid options are: ${VALID_BREAKDOWN_PERIODS.join(", ")}`
      );
      return 1;
    }
    breakdownPeriod = breakdownFlag;
  }
  const groupByFlag = parsedArgs.flags["--group-by"];
  let groupByDimension;
  if (groupByFlag) {
    if (!isValidGroupByDimension(groupByFlag)) {
      error(
        `Invalid group-by dimension: "${groupByFlag}". Valid options are: ${VALID_GROUP_BY_DIMENSIONS.join(", ")}`
      );
      return 1;
    }
    groupByDimension = groupByFlag;
  }
  if (breakdownPeriod && groupByDimension) {
    error(
      "--breakdown and --group-by cannot be used together. Use one or the other."
    );
    return 1;
  }
  telemetry2.trackCliOptionFrom(fromFlag);
  telemetry2.trackCliOptionTo(toFlag);
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  telemetry2.trackCliOptionBreakdown(breakdownFlag);
  telemetry2.trackCliOptionGroupBy(groupByFlag);
  if (fromFlag) {
    debug(`Date conversion: ${fromFlag} -> ${fromDate}`);
  }
  if (toFlag) {
    debug(`Date conversion: ${toFlag} (end of day) -> ${toDate}`);
  }
  let contextName;
  let teamId;
  try {
    const scope = await getScope(client);
    contextName = scope.contextName;
    teamId = scope.team?.id;
  } catch (err) {
    if ((0, import_error_utils20.isErrnoException)(err) && (err.code === "NOT_AUTHORIZED" || err.code === "TEAM_DELETED")) {
      error(err.message);
      return 1;
    }
    throw err;
  }
  const start = Date.now();
  if (!asJson) {
    spinner(`Fetching usage data for ${import_chalk196.default.bold(contextName)}`);
  }
  debug(`Fetching charges from ${fromDate} to ${toDate}`);
  const query = new URLSearchParams({
    from: fromDate,
    to: toDate
  });
  if (teamId) {
    query.set("teamId", teamId);
  }
  try {
    const response = await client.fetch(`/v1/billing/charges?${query}`, {
      json: false,
      useCurrentTeam: false
    });
    if (!response.ok) {
      const errorText = await response.text();
      error(`Failed to fetch usage data: ${response.status} ${errorText}`);
      return 1;
    }
    const usageData = await processCharges(
      response,
      breakdownPeriod,
      groupByDimension,
      contextName,
      fromDisplay,
      toDisplay,
      usingDefaults
    );
    if (asJson) {
      outputJson5(client, {
        data: usageData,
        fromDate,
        toDate,
        breakdownPeriod,
        groupByDimension
      });
      return 0;
    }
    if (groupByDimension) {
      outputGroupBy({
        data: usageData,
        groupByDimension,
        startTime: start
      });
    } else if (breakdownPeriod) {
      outputBreakdown({
        data: usageData,
        breakdownPeriod,
        startTime: start
      });
    } else {
      outputAggregated({
        data: usageData,
        startTime: start
      });
    }
    return 0;
  } catch (err) {
    output_manager_default.prettyError(err);
    return 1;
  }
}
function getGroupKey(charge, dimension) {
  if (dimension === "project") {
    return charge.Tags?.ProjectName || charge.Tags?.ProjectId || "(unattributed)";
  }
  return charge.RegionName || charge.RegionId || "(global)";
}
async function processCharges(response, breakdownPeriod, groupByDimension, contextName, fromDisplay, toDisplay, usingDefaults) {
  const services = /* @__PURE__ */ new Map();
  const periodUsage = /* @__PURE__ */ new Map();
  const groupByUsage = /* @__PURE__ */ new Map();
  let grandPricingQuantity = 0;
  let grandEffective = 0;
  let grandBilled = 0;
  let chargeCount = 0;
  let pricingUnit = "MIUs";
  await new Promise((resolve8, reject) => {
    const stream = response.body.pipe(import_jsonlines.default.parse());
    stream.on("data", (charge) => {
      chargeCount++;
      if (chargeCount === 1 && charge.PricingUnit) {
        pricingUnit = charge.PricingUnit;
      }
      const serviceName = charge.ServiceName || "Unknown";
      const quantity = charge.PricingQuantity || 0;
      const effective = charge.EffectiveCost || 0;
      const billed = charge.BilledCost || 0;
      grandPricingQuantity += quantity;
      grandEffective += effective;
      grandBilled += billed;
      const existing = services.get(serviceName) || {
        pricingQuantity: 0,
        effectiveCost: 0,
        billedCost: 0,
        pricingUnit: charge.PricingUnit || pricingUnit
      };
      services.set(serviceName, {
        pricingQuantity: existing.pricingQuantity + quantity,
        effectiveCost: existing.effectiveCost + effective,
        billedCost: existing.billedCost + billed,
        pricingUnit: existing.pricingUnit
      });
      if (breakdownPeriod) {
        const periodKey = getPeriodKey(
          charge.ChargePeriodStart,
          breakdownPeriod
        );
        if (!periodUsage.has(periodKey)) {
          periodUsage.set(periodKey, {
            services: /* @__PURE__ */ new Map(),
            totalPricingQuantity: 0,
            totalEffectiveCost: 0,
            totalBilledCost: 0
          });
        }
        const periodData = periodUsage.get(periodKey);
        periodData.totalPricingQuantity += quantity;
        periodData.totalEffectiveCost += effective;
        periodData.totalBilledCost += billed;
        const periodService = periodData.services.get(serviceName) || {
          pricingQuantity: 0,
          effectiveCost: 0,
          billedCost: 0,
          pricingUnit: charge.PricingUnit || pricingUnit
        };
        periodData.services.set(serviceName, {
          pricingQuantity: periodService.pricingQuantity + quantity,
          effectiveCost: periodService.effectiveCost + effective,
          billedCost: periodService.billedCost + billed,
          pricingUnit: periodService.pricingUnit
        });
      }
      if (groupByDimension) {
        const groupKey = getGroupKey(charge, groupByDimension);
        if (!groupByUsage.has(groupKey)) {
          groupByUsage.set(groupKey, {
            services: /* @__PURE__ */ new Map(),
            totalPricingQuantity: 0,
            totalEffectiveCost: 0,
            totalBilledCost: 0
          });
        }
        const groupData = groupByUsage.get(groupKey);
        groupData.totalPricingQuantity += quantity;
        groupData.totalEffectiveCost += effective;
        groupData.totalBilledCost += billed;
        const groupService = groupData.services.get(serviceName) || {
          pricingQuantity: 0,
          effectiveCost: 0,
          billedCost: 0,
          pricingUnit: charge.PricingUnit || pricingUnit
        };
        groupData.services.set(serviceName, {
          pricingQuantity: groupService.pricingQuantity + quantity,
          effectiveCost: groupService.effectiveCost + effective,
          billedCost: groupService.billedCost + billed,
          pricingUnit: groupService.pricingUnit
        });
      }
    });
    stream.on("end", resolve8);
    stream.on("error", reject);
    response.body.on("error", reject);
  });
  return {
    contextName,
    fromDisplay,
    toDisplay,
    usingDefaults,
    pricingUnit,
    chargeCount,
    services,
    periodUsage,
    groupByUsage,
    grandTotals: {
      pricingQuantity: grandPricingQuantity,
      effectiveCost: grandEffective,
      billedCost: grandBilled
    }
  };
}

// src/commands/webhooks/ls.ts
var import_ms34 = __toESM(require_ms(), 1);
var import_chalk197 = __toESM(require_source(), 1);
var import_pluralize13 = __toESM(require_pluralize(), 1);

// src/util/webhooks/get-webhooks.ts
async function getWebhooks(client) {
  const response = await client.fetch("/v1/webhooks");
  if (Array.isArray(response)) {
    return { webhooks: response };
  }
  return response;
}

// src/util/telemetry/commands/webhooks/ls.ts
var WebhooksLsTelemetryClient = class extends TelemetryClient {
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/webhooks/ls.ts
async function ls9(client, argv) {
  const telemetry2 = new WebhooksLsTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(listSubcommand19.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const validationResult = validateLsArgs({
    commandName: "webhooks ls",
    args,
    maxArgs: 0,
    exitCode: 2
  });
  if (validationResult !== 0) {
    return validationResult;
  }
  telemetry2.trackCliOptionFormat(opts["--format"]);
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || client.nonInteractive;
  const { contextName } = await getScope(client);
  const lsStamp = stamp_default();
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Fetching Webhooks under ${import_chalk197.default.bold(contextName)}`);
  }
  const { webhooks } = await getWebhooks(client);
  if (asJson) {
    output_manager_default.stopSpinner();
    const webhooksList = webhooks.map((webhook) => ({
      id: webhook.id,
      url: webhook.url,
      events: webhook.events,
      projectIds: webhook.projectIds,
      createdAt: webhook.createdAt,
      updatedAt: webhook.updatedAt
    }));
    const jsonOutput = client.nonInteractive ? {
      status: AGENT_STATUS.OK,
      webhooks: webhooksList,
      message: `${webhooks.length} webhook(s) found.`,
      next: [
        {
          command: getCommandNamePlain("webhooks get <id>"),
          when: "Inspect a webhook"
        }
      ]
    } : { webhooks: webhooksList };
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    output_manager_default.log(
      `${(0, import_pluralize13.default)("Webhook", webhooks.length, true)} found under ${import_chalk197.default.bold(
        contextName
      )} ${import_chalk197.default.gray(lsStamp())}`
    );
    if (webhooks.length > 0) {
      output_manager_default.print(
        formatWebhooksTable(webhooks).replace(/^(.*)/gm, `${" ".repeat(1)}$1`)
      );
      output_manager_default.print("\n\n");
    }
  }
  return 0;
}
function formatWebhooksTable(webhooks) {
  const current = Date.now();
  const rows = webhooks.map((webhook) => {
    const age = webhook.createdAt ? (0, import_ms34.default)(current - webhook.createdAt) : "-";
    const eventsDisplay = webhook.events.length > 2 ? `${webhook.events.slice(0, 2).join(", ")} +${webhook.events.length - 2}` : webhook.events.join(", ");
    return [webhook.id, webhook.url, eventsDisplay, import_chalk197.default.gray(age)];
  });
  return formatTable(
    ["ID", "URL", "Events", "Age"],
    ["l", "l", "l", "l"],
    [{ rows }]
  );
}

// src/commands/webhooks/get.ts
var import_chalk198 = __toESM(require_source(), 1);

// src/util/webhooks/get-webhook.ts
async function getWebhook(client, webhookId) {
  return await client.fetch(
    `/v1/webhooks/${encodeURIComponent(webhookId)}`
  );
}

// src/util/telemetry/commands/webhooks/get.ts
var WebhooksGetTelemetryClient = class extends TelemetryClient {
  trackCliArgumentId(id) {
    if (id) {
      this.trackCliArgument({
        arg: "id",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionFormat(format4) {
    if (format4) {
      this.trackCliOption({
        option: "format",
        value: format4
      });
    }
  }
};

// src/commands/webhooks/get.ts
async function get4(client, argv) {
  const telemetry2 = new WebhooksGetTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(getSubcommand4.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const [webhookId] = args;
  const getStamp = stamp_default();
  if (!webhookId) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_id",
          message: "Webhook ID is required. Provide ID as the first argument.",
          next: [
            { command: getCommandNamePlain("webhooks ls") },
            { command: getCommandNamePlain("webhooks get <id>") }
          ]
        },
        1
      );
    }
    output_manager_default.error(`${getCommandName(`webhooks get <id>`)} expects one argument`);
    return 1;
  }
  telemetry2.trackCliArgumentId(webhookId);
  telemetry2.trackCliOptionFormat(opts["--format"]);
  const formatResult = validateJsonOutput(opts);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput || client.nonInteractive;
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk198.default.cyan(
        `${getCommandName("webhooks get <id>")}`
      )}`
    );
    return 1;
  }
  output_manager_default.debug(`Fetching webhook info`);
  const { contextName } = await getScope(client);
  if (!client.nonInteractive) {
    output_manager_default.spinner(
      `Fetching Webhook ${webhookId} under ${import_chalk198.default.bold(contextName)}`
    );
  }
  let webhook;
  try {
    webhook = await getWebhook(client, webhookId);
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err) && err.status === 404) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "webhook_not_found",
            message: `Webhook not found: ${webhookId}.`,
            next: [{ command: getCommandNamePlain("webhooks ls") }]
          },
          1
        );
      }
      output_manager_default.error(`Webhook not found: ${webhookId}`);
      output_manager_default.log(`Run ${getCommandName(`webhooks ls`)} to see your webhooks.`);
      return 1;
    }
    throw err;
  }
  if (asJson) {
    output_manager_default.stopSpinner();
    const jsonOutput = client.nonInteractive ? {
      status: "ok",
      webhook,
      message: `Webhook ${webhookId} found.`,
      next: [
        {
          command: getCommandNamePlain(`webhooks rm ${webhookId} --yes`),
          when: "Remove this webhook"
        }
      ]
    } : webhook;
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else {
    output_manager_default.log(
      `Webhook ${webhookId} found under ${import_chalk198.default.bold(contextName)} ${import_chalk198.default.gray(
        getStamp()
      )}`
    );
    output_manager_default.print("\n");
    output_manager_default.print(import_chalk198.default.bold("  General\n\n"));
    output_manager_default.print(`    ${import_chalk198.default.cyan("ID")}			${webhook.id}
`);
    output_manager_default.print(`    ${import_chalk198.default.cyan("URL")}			${webhook.url}
`);
    output_manager_default.print(
      `    ${import_chalk198.default.cyan("Created At")}		${formatDate(webhook.createdAt)}
`
    );
    output_manager_default.print(
      `    ${import_chalk198.default.cyan("Updated At")}		${formatDate(webhook.updatedAt)}
`
    );
    output_manager_default.print("\n");
    output_manager_default.print(import_chalk198.default.bold("  Events\n\n"));
    for (const event of webhook.events) {
      output_manager_default.print(`    - ${event}
`);
    }
    if (webhook.projectIds && webhook.projectIds.length > 0) {
      output_manager_default.print("\n");
      output_manager_default.print(import_chalk198.default.bold("  Projects\n\n"));
      if (webhook.projectsMetadata && webhook.projectsMetadata.length > 0) {
        for (const project of webhook.projectsMetadata) {
          output_manager_default.print(`    - ${project.name} (${project.id})
`);
        }
      } else {
        for (const projectId of webhook.projectIds) {
          output_manager_default.print(`    - ${projectId}
`);
        }
      }
    }
    output_manager_default.print("\n");
  }
  return 0;
}

// src/commands/webhooks/create.ts
var import_chalk199 = __toESM(require_source(), 1);

// src/util/webhooks/create-webhook.ts
async function createWebhook(client, payload) {
  return await client.fetch("/v1/webhooks", {
    method: "POST",
    body: payload
  });
}

// src/util/telemetry/commands/webhooks/create.ts
var WebhooksCreateTelemetryClient = class extends TelemetryClient {
  trackCliArgumentUrl(url) {
    if (url) {
      this.trackCliArgument({
        arg: "url",
        value: this.redactedValue
      });
    }
  }
  trackCliOptionEvent(events) {
    if (events && events.length > 0) {
      this.trackCliOption({
        option: "event",
        value: String(events.length)
      });
    }
  }
  trackCliOptionProject(projects) {
    if (projects && projects.length > 0) {
      this.trackCliOption({
        option: "project",
        value: String(projects.length)
      });
    }
  }
};

// src/util/webhooks/get-webhook-events.ts
var cachedEvents = null;
async function getWebhookEvents() {
  if (cachedEvents) {
    return cachedEvents;
  }
  const cache = new OpenApiCache();
  const loaded = await cache.load();
  if (!loaded) {
    output_manager_default.debug("Failed to load OpenAPI spec for webhook events");
    return [];
  }
  const endpoints = cache.getEndpoints();
  const createWebhookEndpoint = endpoints.find(
    (e2) => e2.path === "/v1/webhooks" && e2.method === "POST"
  );
  if (!createWebhookEndpoint) {
    output_manager_default.debug("Could not find POST /v1/webhooks endpoint in OpenAPI spec");
    return [];
  }
  const bodyFields = cache.getBodyFields(createWebhookEndpoint);
  const eventsField = bodyFields.find((f) => f.name === "events");
  if (!eventsField?.enumValues) {
    output_manager_default.debug("Could not find events enum in webhook endpoint");
    return [];
  }
  cachedEvents = eventsField.enumValues.filter(
    (v) => typeof v === "string"
  );
  return cachedEvents;
}
async function validateWebhookEvents(events) {
  const validEvents = await getWebhookEvents();
  if (validEvents.length === 0) {
    return [];
  }
  const validSet = new Set(validEvents);
  return events.filter((e2) => !validSet.has(e2));
}

// src/commands/webhooks/create.ts
var WEBHOOKS_EVENTS_DOCS_URL = "https://vercel.com/docs/webhooks/webhooks-api";
async function create5(client, argv) {
  const telemetry2 = new WebhooksCreateTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(createSubcommand5.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  let [url] = args;
  if (!url) {
    if (client.nonInteractive) {
      const argv2 = client.argv.slice(2);
      const subcommandParts = argv2.slice(0, 2);
      const rest = argv2.slice(2);
      const subcommandWithPlaceholder = `${subcommandParts.join(" ")} <url>${rest.length ? ` ${rest.join(" ")}` : ""}`;
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_URL,
          message: "Webhook URL is required. Provide URL as the first argument.",
          next: [
            {
              command: getCommandNamePlain(subcommandWithPlaceholder)
            }
          ]
        },
        1
      );
    }
    url = await client.input.text({
      message: "Webhook URL:",
      validate: (val) => {
        if (!val)
          return "URL is required";
        try {
          const urlObj = new URL(val);
          if (!["http:", "https:"].includes(urlObj.protocol)) {
            return "Webhook URL must use http or https protocol";
          }
        } catch {
          return "Invalid URL";
        }
        return true;
      }
    });
  } else {
    try {
      const urlObj = new URL(url);
      if (!["http:", "https:"].includes(urlObj.protocol)) {
        output_manager_default.error("Webhook URL must use http or https protocol");
        return 1;
      }
    } catch {
      output_manager_default.error(`Invalid URL: ${url}`);
      return 1;
    }
  }
  const projectIds = opts["--project"];
  const eventFlags = opts["--event"];
  let events;
  if (!eventFlags || eventFlags.length === 0) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: AGENT_STATUS.ERROR,
          reason: AGENT_REASON.MISSING_EVENTS,
          message: "At least one event is required. Use --event <event> (can be repeated).",
          next: [
            {
              command: getCommandNamePlain(
                "webhooks create <url> --event <event>"
              )
            }
          ]
        },
        1
      );
    }
    const availableEvents = await getWebhookEvents() ?? [];
    if (availableEvents.length === 0) {
      output_manager_default.error(
        "Could not fetch available webhook events. Please specify events using --event flags."
      );
      return 1;
    }
    events = await client.input.checkbox({
      message: "Select events:",
      choices: availableEvents.map((event) => ({
        name: event,
        value: event
      })),
      validate: (selected) => {
        if (selected.length === 0)
          return "At least one event is required";
        return true;
      }
    });
  } else {
    const invalidEvents = await validateWebhookEvents(eventFlags);
    if (invalidEvents.length > 0) {
      if (client.nonInteractive) {
        const suggested = buildCreateCommandWithEventPlaceholder(
          client.argv,
          url
        );
        outputAgentError(
          client,
          {
            status: AGENT_STATUS.ERROR,
            reason: AGENT_REASON.INVALID_ARGUMENTS,
            message: `Invalid event type${invalidEvents.length > 1 ? "s" : ""}: ${invalidEvents.join(", ")}. Use a valid event name (e.g. deployment.created).`,
            hint: `See available events: ${WEBHOOKS_EVENTS_DOCS_URL}`,
            next: [{ command: suggested, when: "use a valid --event value" }]
          },
          1
        );
        return 1;
      }
      output_manager_default.error(
        `Invalid event type${invalidEvents.length > 1 ? "s" : ""}: ${invalidEvents.join(", ")}`
      );
      return 1;
    }
    events = eventFlags;
  }
  telemetry2.trackCliArgumentUrl(url);
  telemetry2.trackCliOptionEvent(events);
  telemetry2.trackCliOptionProject(projectIds);
  const { contextName } = await getScope(client);
  const createStamp = stamp_default();
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Creating webhook under ${import_chalk199.default.bold(contextName)}`);
  }
  try {
    const webhook = await createWebhook(client, {
      url,
      events,
      projectIds
    });
    if (client.nonInteractive) {
      const json = {
        status: AGENT_STATUS.OK,
        webhook: {
          id: webhook.id,
          url: webhook.url,
          events: webhook.events,
          ...webhook.projectIds?.length ? { projectIds: webhook.projectIds } : {}
        },
        message: `Webhook ${webhook.id} created.`,
        next: [
          {
            command: getCommandNamePlain(`webhooks get ${webhook.id}`),
            when: "Inspect the webhook"
          }
        ]
      };
      if (webhook.secret)
        json.secret = webhook.secret;
      client.stdout.write(`${JSON.stringify(json, null, 2)}
`);
      return 0;
    }
    output_manager_default.stopSpinner();
    output_manager_default.success(
      `Webhook created: ${import_chalk199.default.bold(webhook.id)} ${createStamp()}`
    );
    output_manager_default.print("\n");
    output_manager_default.print(import_chalk199.default.bold("  Webhook Details\n\n"));
    output_manager_default.print(`    ${import_chalk199.default.cyan("ID".padEnd(10))}${webhook.id}
`);
    output_manager_default.print(`    ${import_chalk199.default.cyan("URL".padEnd(10))}${webhook.url}
`);
    output_manager_default.print(
      `    ${import_chalk199.default.cyan("Events".padEnd(10))}${webhook.events.join(", ")}
`
    );
    if (webhook.projectIds && webhook.projectIds.length > 0) {
      output_manager_default.print(
        `    ${import_chalk199.default.cyan("Projects".padEnd(10))}${webhook.projectIds.join(", ")}
`
      );
    }
    output_manager_default.print("\n");
    output_manager_default.warn(
      `Save this secret - it will not be shown again: ${import_chalk199.default.bold(webhook.secret)}`
    );
    output_manager_default.print("\n");
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err)) {
      if (client.nonInteractive) {
        const reason = err.code === "invalid_url" ? AGENT_REASON.INVALID_URL : err.code === "invalid_event" ? AGENT_REASON.INVALID_EVENT : AGENT_REASON.API_ERROR;
        const suggested = reason === AGENT_REASON.INVALID_EVENT ? buildCreateCommandWithEventPlaceholder(client.argv, url) : void 0;
        outputAgentError(
          client,
          {
            status: AGENT_STATUS.ERROR,
            reason,
            message: err.message,
            hint: reason === AGENT_REASON.INVALID_EVENT ? `See available events: ${WEBHOOKS_EVENTS_DOCS_URL}` : void 0,
            next: suggested !== void 0 ? [{ command: suggested, when: "use a valid --event value" }] : void 0
          },
          1
        );
        return 1;
      }
      if (err.code === "invalid_url") {
        output_manager_default.error(`Invalid webhook URL: ${url}`);
        return 1;
      }
      if (err.code === "invalid_event") {
        output_manager_default.error(`Invalid event type. Please check the event names.`);
        return 1;
      }
      output_manager_default.error(err.message);
      return 1;
    }
    throw err;
  }
}
function buildCreateCommandWithEventPlaceholder(fullArgv, url) {
  const argv = fullArgv.slice(2);
  const out = ["webhooks", "create", url];
  let i = 3;
  let hadEvent = false;
  while (i < argv.length) {
    if (argv[i] === "--event" || argv[i] === "-e") {
      hadEvent = true;
      i += 2;
      continue;
    }
    out.push(argv[i]);
    i++;
  }
  if (hadEvent) {
    out.push("--event", "<event>");
  }
  return getCommandNamePlain(out.join(" "));
}

// src/commands/webhooks/rm.ts
var import_chalk200 = __toESM(require_source(), 1);

// src/util/webhooks/delete-webhook.ts
async function deleteWebhook(client, webhookId) {
  await client.fetch(`/v1/webhooks/${encodeURIComponent(webhookId)}`, {
    method: "DELETE"
  });
}

// src/util/telemetry/commands/webhooks/rm.ts
var WebhooksRmTelemetryClient = class extends TelemetryClient {
  trackCliArgumentId(id) {
    if (id) {
      this.trackCliArgument({
        arg: "id",
        value: this.redactedValue
      });
    }
  }
  trackCliFlagYes(yes) {
    if (yes) {
      this.trackCliFlag("yes");
    }
  }
};

// src/commands/webhooks/rm.ts
function webhookCommandWithGlobalFlags(baseSubcommand, argv) {
  const globalFlags = getGlobalFlagsOnlyFromArgs(argv.slice(2));
  const full = globalFlags.length ? `${baseSubcommand} ${globalFlags.join(" ")}` : baseSubcommand;
  return getCommandNamePlain(full);
}
async function rm10(client, argv) {
  const telemetry2 = new WebhooksRmTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(removeSubcommand15.options);
  try {
    parsedArgs = parseArguments(argv, flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  const { args, flags: opts } = parsedArgs;
  const [webhookId] = args;
  telemetry2.trackCliArgumentId(webhookId);
  telemetry2.trackCliFlagYes(opts["--yes"]);
  if (!webhookId) {
    if (client.nonInteractive) {
      outputAgentError(
        client,
        {
          status: "error",
          reason: "missing_id",
          message: "Webhook ID is required. Provide ID as the first argument.",
          next: [
            {
              command: webhookCommandWithGlobalFlags(
                "webhooks ls",
                client.argv
              )
            },
            {
              command: webhookCommandWithGlobalFlags(
                "webhooks rm <id> --yes",
                client.argv
              )
            }
          ]
        },
        1
      );
    }
    output_manager_default.error(`${getCommandName(`webhooks rm <id>`)} expects one argument`);
    return 1;
  }
  const skipConfirmation = opts["--yes"] || false;
  if (client.nonInteractive && !skipConfirmation) {
    outputAgentError(
      client,
      {
        status: "error",
        reason: "confirmation_required",
        message: "Removing a webhook requires confirmation. Re-run with --yes.",
        next: [{ command: buildCommandWithYes(client.argv) }]
      },
      1
    );
  }
  const { contextName } = await getScope(client);
  if (args.length !== 1) {
    output_manager_default.error(
      `Invalid number of arguments. Usage: ${import_chalk200.default.cyan(
        `${getCommandName("webhooks rm <id>")}`
      )}`
    );
    return 1;
  }
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Fetching webhook ${webhookId}`);
  }
  let webhook;
  try {
    webhook = await getWebhook(client, webhookId);
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err) && err.status === 404) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "webhook_not_found",
            message: `Webhook not found: ${webhookId}.`,
            next: [
              {
                command: webhookCommandWithGlobalFlags(
                  "webhooks ls",
                  client.argv
                )
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(`Webhook not found: ${webhookId}`);
      output_manager_default.log(`Run ${getCommandName(`webhooks ls`)} to see your webhooks.`);
      return 1;
    }
    throw err;
  }
  output_manager_default.stopSpinner();
  if (!skipConfirmation && !await client.input.confirm(
    `Are you sure you want to remove webhook ${param(webhookId)} (${webhook.url})?`,
    false
  )) {
    if (!client.nonInteractive)
      output_manager_default.log("Canceled");
    return 0;
  }
  const removeStamp = stamp_default();
  if (!client.nonInteractive) {
    output_manager_default.spinner(`Removing webhook under ${import_chalk200.default.bold(contextName)}`);
  }
  try {
    await deleteWebhook(client, webhookId);
    output_manager_default.stopSpinner();
    if (client.nonInteractive) {
      client.stdout.write(
        `${JSON.stringify(
          {
            status: "ok",
            webhook: { id: webhookId, url: webhook.url },
            message: `Webhook ${webhookId} removed.`
          },
          null,
          2
        )}
`
      );
      return 0;
    }
    output_manager_default.success(`Webhook ${import_chalk200.default.bold(webhookId)} removed ${removeStamp()}`);
    return 0;
  } catch (err) {
    output_manager_default.stopSpinner();
    if (isAPIError(err) && err.status === 404) {
      if (client.nonInteractive) {
        outputAgentError(
          client,
          {
            status: "error",
            reason: "webhook_not_found",
            message: `Webhook not found: ${webhookId}.`,
            next: [
              {
                command: webhookCommandWithGlobalFlags(
                  "webhooks ls",
                  client.argv
                )
              }
            ]
          },
          1
        );
      }
      output_manager_default.error(`Webhook not found: ${webhookId}`);
      return 1;
    }
    throw err;
  }
}

// src/util/telemetry/commands/webhooks/index.ts
var WebhooksTelemetryClient = class extends TelemetryClient {
  trackCliSubcommandCreate(actual) {
    this.trackCliSubcommand({
      subcommand: "create",
      value: actual
    });
  }
  trackCliSubcommandGet(actual) {
    this.trackCliSubcommand({
      subcommand: "get",
      value: actual
    });
  }
  trackCliSubcommandRemove(actual) {
    this.trackCliSubcommand({
      subcommand: "remove",
      value: actual
    });
  }
  trackCliSubcommandList(actual) {
    this.trackCliSubcommand({
      subcommand: "list",
      value: actual
    });
  }
};

// src/commands/webhooks/index.ts
var COMMAND_CONFIG41 = {
  create: ["create", "add"],
  get: ["get", "inspect"],
  ls: ["ls", "list"],
  rm: ["rm", "remove", "delete"]
};
async function main30(client) {
  let parsedArgs;
  const flagsSpecification = getFlagsSpecification(webhooksCommand.options);
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification, {
      permissive: true
    });
  } catch (error) {
    printError(error);
    return 1;
  }
  const telemetry2 = new WebhooksTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  const { subcommand, args, subcommandOriginal } = getSubcommand5(
    parsedArgs.args.slice(1),
    COMMAND_CONFIG41
  );
  const needHelp = parsedArgs.flags["--help"];
  if (!subcommand && needHelp) {
    telemetry2.trackCliFlagHelp("webhooks");
    output_manager_default.print(help(webhooksCommand, { columns: client.stderr.columns }));
    return 2;
  }
  function printHelp(command) {
    output_manager_default.print(
      help(command, { parent: webhooksCommand, columns: client.stderr.columns })
    );
    return 2;
  }
  switch (subcommand) {
    case "create":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("webhooks", subcommandOriginal);
        return printHelp(createSubcommand5);
      }
      telemetry2.trackCliSubcommandCreate(subcommandOriginal);
      return create5(client, args);
    case "get":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("webhooks", subcommandOriginal);
        return printHelp(getSubcommand4);
      }
      telemetry2.trackCliSubcommandGet(subcommandOriginal);
      return get4(client, args);
    case "rm":
      if (needHelp) {
        telemetry2.trackCliFlagHelp("webhooks", subcommandOriginal);
        return printHelp(removeSubcommand15);
      }
      telemetry2.trackCliSubcommandRemove(subcommandOriginal);
      return rm10(client, args);
    default:
      if (needHelp) {
        telemetry2.trackCliFlagHelp("webhooks", subcommandOriginal);
        return printHelp(listSubcommand19);
      }
      telemetry2.trackCliSubcommandList(subcommandOriginal);
      return ls9(client, args);
  }
}

// src/commands/whoami/index.ts
var import_chalk201 = __toESM(require_source(), 1);

// src/util/telemetry/commands/whoami/index.ts
var WhoamiTelemetryClient = class extends TelemetryClient {
};

// src/commands/whoami/index.ts
async function whoami(client) {
  let parsedArgs = null;
  const flagsSpecification = getFlagsSpecification(whoamiCommand.options);
  const telemetry2 = new WhoamiTelemetryClient({
    opts: {
      store: client.telemetryEventStore
    }
  });
  try {
    parsedArgs = parseArguments(client.argv.slice(2), flagsSpecification);
  } catch (error) {
    printError(error);
    return 1;
  }
  if (parsedArgs.flags["--help"]) {
    telemetry2.trackCliFlagHelp("whoami");
    output_manager_default.print(help(whoamiCommand, { columns: client.stderr.columns }));
    return 0;
  }
  const formatResult = validateJsonOutput(parsedArgs.flags);
  if (!formatResult.valid) {
    output_manager_default.error(formatResult.error);
    return 1;
  }
  const asJson = formatResult.jsonOutput;
  telemetry2.trackCliOptionFormat(parsedArgs.flags["--format"]);
  const scope = await getScope(client, { resolveLocalScope: true });
  const { user, team, globalTeam } = scope;
  const hasLocalOverride = !scope.explicitScopeProvided && ((team?.id ?? null) !== (globalTeam?.id ?? null) || // `team` being null while a local project linked to personal scope
  // exists while a global team is selected is also a mismatch.
  scope.scopeMismatch);
  if (asJson) {
    const jsonOutput = {
      username: user.username,
      email: user.email,
      name: user.name,
      team: team ? { id: team.id, slug: team.slug, name: team.name } : null
    };
    if (hasLocalOverride) {
      jsonOutput.localOverride = true;
      jsonOutput.globalTeam = globalTeam ? { id: globalTeam.id, slug: globalTeam.slug, name: globalTeam.name } : null;
    }
    client.stdout.write(`${JSON.stringify(jsonOutput, null, 2)}
`);
  } else if (client.stdout.isTTY) {
    output_manager_default.log(`Logged in as ${import_chalk201.default.bold(user.username)}`);
    if (team) {
      output_manager_default.log(
        `Active team: ${import_chalk201.default.bold(team.slug)}${team.name && team.name !== team.slug ? ` (${team.name})` : ""}`
      );
    } else {
      output_manager_default.log(`Active team: ${import_chalk201.default.bold("Personal Account")}`);
    }
    if (hasLocalOverride) {
      const globalLabel = globalTeam ? globalTeam.slug : "Personal Account";
      const localLabel = team ? team.slug : "Personal Account";
      output_manager_default.log(
        `${import_chalk201.default.yellow("Local override:")} scope is set to ${import_chalk201.default.bold(
          localLabel
        )} by the linked project in this directory (globally selected: ${import_chalk201.default.bold(
          globalLabel
        )}).`
      );
    }
  } else {
    client.stdout.write(`${user.username}
`);
  }
  return 0;
}
export {
  activity,
  agent,
  main2 as aiGateway,
  alerts,
  alias,
  api,
  bisect,
  main3 as blob,
  main4 as buy,
  main5 as cache,
  main6 as certs,
  connex,
  contract,
  main7 as crons,
  curl,
  main8 as deployHooks,
  dns,
  main9 as domains,
  main16 as edgeConfig,
  main15 as firewall,
  main17 as flags,
  main18 as git,
  guidance,
  httpstat,
  main19 as init,
  inspect4 as inspect,
  install,
  main21 as integration,
  main20 as integrationResource,
  login,
  logout2 as logout,
  logs,
  main22 as mcp,
  metrics,
  main23 as microfrontends,
  main24 as oauthApps,
  openCommandHandler as open,
  main25 as project,
  promote_default as promote,
  main as pull,
  redeploy,
  main26 as redirects,
  remove7 as remove,
  rollback_default as rollback,
  rollingRelease,
  main27 as routes,
  sandbox,
  skills,
  main28 as target,
  teams,
  telemetry,
  main29 as tokens,
  traces,
  upgrade,
  usage,
  main30 as webhooks,
  whoami
};
